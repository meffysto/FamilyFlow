---
phase: quick-260415-j0b
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/mascot/expedition-engine.ts
  - components/mascot/ExpeditionsSheet.tsx
  - components/mascot/ExpeditionChest.tsx
autonomous: true
requirements: [QUICK-J0B]
must_haves:
  truths:
    - "L'onglet Resultats affiche le nom lisible et l'emoji du loot (pas le raw itemId)"
    - "Le coffre affiche un recapitulatif clair des gains (loot) et pertes (mise) selon l'outcome"
    - "Les outcomes failure et partial affichent un message explicite sur la perte de la mise"
  artifacts:
    - path: "lib/mascot/expedition-engine.ts"
      provides: "Fonction getLootDisplay(itemId, type) retournant label+emoji"
      exports: ["getLootDisplay"]
    - path: "components/mascot/ExpeditionsSheet.tsx"
      provides: "ResultRow avec label+emoji lisibles via getLootDisplay"
    - path: "components/mascot/ExpeditionChest.tsx"
      provides: "Section recapitulatif gains/pertes dans le coffre ouvert"
  key_links:
    - from: "components/mascot/ExpeditionsSheet.tsx"
      to: "lib/mascot/expedition-engine.ts"
      via: "import getLootDisplay"
      pattern: "getLootDisplay"
    - from: "components/mascot/ExpeditionChest.tsx"
      to: "lib/mascot/expedition-engine.ts"
      via: "import getLootDisplay"
      pattern: "getLootDisplay"
---

<objective>
Afficher les gains et pertes des expeditions de maniere lisible dans l'onglet Resultats et dans le coffre anime.

Purpose: Actuellement le ResultRow affiche le raw `lootItemId` (ex: "boost_recolte_2x") et le coffre ne montre rien sur la mise perdue en cas d'echec. L'utilisateur n'a aucune visibilite claire sur ce qu'il a gagne ou perdu.

Output: Gains lisibles (emoji + label) dans ResultRow + recapitulatif gains/pertes dans ExpeditionChest
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@hooks/useExpeditions.ts
@lib/mascot/expedition-engine.ts
@components/mascot/ExpeditionsSheet.tsx
@components/mascot/ExpeditionChest.tsx
@lib/types.ts (ActiveExpedition interface)

<interfaces>
<!-- expedition-engine.ts exports utilises -->
```typescript
export interface ExpeditionLoot {
  itemId: string;
  type: 'inhabitant' | 'seed' | 'booster';
  label: string;
  emoji: string;
}

// EXPEDITION_LOOT_TABLE est prive — il faut exposer un helper lookup
// ActiveExpedition stocke lootItemId + lootType mais pas label/emoji
```

<!-- ActiveExpedition depuis lib/types.ts -->
```typescript
export interface ActiveExpedition {
  missionId: string;
  difficulty: ExpeditionDifficulty;
  startedAt: string;
  durationHours: number;
  result?: ExpeditionOutcome;
  lootItemId?: string;
  lootType?: 'inhabitant' | 'seed' | 'booster';
}
```

<!-- ExpeditionChest props -->
```typescript
interface Props {
  visible: boolean;
  outcome: ExpeditionOutcome;
  loot?: ExpeditionLoot;
  missionName: string;
  onClose: () => void;
}
```

<!-- ExpeditionsSheet ResultRow -->
```typescript
// Actuellement affiche : expedition.lootItemId (raw string)
// Devrait afficher : emoji + label lisible
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Helper getLootDisplay + ameliorer ResultRow et ExpeditionChest</name>
  <files>lib/mascot/expedition-engine.ts, components/mascot/ExpeditionsSheet.tsx, components/mascot/ExpeditionChest.tsx</files>
  <action>
1. Dans `lib/mascot/expedition-engine.ts` :
   - Ajouter une fonction exportee `getLootDisplay(itemId: string, lootType?: string): { label: string; emoji: string } | null`
   - Chercher dans EXPEDITION_LOOT_TABLE (toutes les difficultes) l'item correspondant au itemId
   - Retourner `{ label, emoji }` ou null si introuvable
   - Exporter cette fonction

2. Dans `components/mascot/ExpeditionsSheet.tsx` — composant `ResultRow` (ligne ~508) :
   - Importer `getLootDisplay` depuis expedition-engine
   - Remplacer l'affichage brut `expedition.lootItemId` par un appel a getLootDisplay
   - Si loot trouve : afficher `{emoji} {label}` dans un style lisible (chip colore comme le costChip du catalogue)
   - Si pas de loot (failure/partial) : afficher un texte "Mise perdue" en colors.error pour failure, "Retour partiel — pas de butin" en colors.warning pour partial
   - Si success sans loot (cas edge) : ne rien afficher de supplementaire

3. Dans `components/mascot/ExpeditionChest.tsx` :
   - Apres le lootCard existant (loot visible), ajouter une section recapitulatif pour failure/partial :
     - failure : afficher un bandeau "Toute la mise a ete perdue" avec icone MaterialCommunityIcons "close-circle" en colors.error
     - partial : afficher "Retour partiel — butin perdu" en colors.warning
   - Utiliser les styles existants (outcomeBadge pattern) pour coherence visuelle
   - Ne pas modifier les props — l'info outcome est deja disponible
  </action>
  <verify>
    <automated>cd /Users/gabrielwaltio/Documents/family-vault && npx tsc --noEmit 2>&1 | grep -E "(expedition|Expedition)" | head -20; echo "---"; npx tsc --noEmit 2>&1 | tail -3</automated>
  </verify>
  <done>
    - getLootDisplay exporte et fonctionnel dans expedition-engine.ts
    - ResultRow affiche emoji+label au lieu du raw itemId, avec message explicite pour failure/partial
    - ExpeditionChest affiche un bandeau perte pour failure/partial outcomes
    - tsc --noEmit passe (hors erreurs pre-existantes)
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passe sans nouvelles erreurs
- Ouvrir l'onglet Resultats dans ExpeditionsSheet : les loots affichent emoji + label lisible
- Ouvrir un coffre avec outcome failure : bandeau "mise perdue" visible
- Ouvrir un coffre avec outcome success + loot : loot card inchangee (regression zero)
</verification>

<success_criteria>
- Les gains d'expedition sont affiches avec emoji + nom lisible (pas de raw ID)
- Les pertes sont explicitement communiquees a l'utilisateur (failure = mise perdue, partial = pas de butin)
- Zero regression sur le flow existant (lancement, collecte, coffre animation)
</success_criteria>

<output>
After completion, create `.planning/quick/260415-j0b-afficher-les-gains-dans-les-exp-ditions/260415-j0b-SUMMARY.md`
</output>
