---
phase: quick-260423-jw6
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/elevenlabs.ts
  - lib/fish-audio.ts
  - components/stories/StoryPlayer.tsx
  - app/(tabs)/stories.tsx
autonomous: true
requirements:
  - QUICK-260423-jw6
must_haves:
  truths:
    - "Un MP3 ElevenLabs/Fish Audio généré pour une histoire est persisté dans le vault iCloud à 09 - Histoires/{enfant}/{storyId}.mp3"
    - "Lors d'un ReplayStep, aucun appel auto à generateSpeech/Fish Audio n'est déclenché si le cache documentDirectory est absent"
    - "Si le MP3 existe dans le vault mais pas en documentDirectory, il est copié vers documentDirectory et le Waveform peut le lire"
    - "Quand autoGenerate=false et qu'aucun MP3 n'est disponible, l'utilisateur voit un bouton 'Générer l'audio' qui déclenche la génération à la demande"
  artifacts:
    - path: "lib/elevenlabs.ts"
      provides: "storyVaultAudioRelPath() helper + getCachedStoryAudio(storyId, voiceId, vaultUri?) avec fallback vault"
    - path: "lib/fish-audio.ts"
      provides: "getCachedStoryAudioFish(storyId, voiceId, vaultUri?) avec fallback vault"
    - path: "components/stories/StoryPlayer.tsx"
      provides: "Prop autoGenerate + lecture vault + copie post-génération + UI 'Générer l'audio' quand needsGeneration"
    - path: "app/(tabs)/stories.tsx"
      provides: "ReplayStep passe autoGenerate={false} au StoryPlayer"
  key_links:
    - from: "components/stories/StoryPlayer.tsx"
      to: "lib/elevenlabs.ts + lib/fish-audio.ts"
      via: "getCachedStoryAudio(storyId, voiceId, vaultUri) / getCachedStoryAudioFish(storyId, voiceId, vaultUri)"
      pattern: "getCachedStoryAudio\\(.*vaultUri"
    - from: "components/stories/StoryPlayer.tsx"
      to: "vault"
      via: "vault.copyFileToVault(result.audioUri, storyVaultAudioRelPath(...)) post-génération"
      pattern: "copyFileToVault.*storyVaultAudioRelPath"
    - from: "app/(tabs)/stories.tsx (ReplayStep)"
      to: "StoryPlayer"
      via: "autoGenerate={false}"
      pattern: "autoGenerate=\\{false\\}"
---

<objective>
Persister les MP3 histoires (ElevenLabs + Fish Audio) dans le vault iCloud pour qu'ils survivent aux réinstalls, et bloquer la regénération auto lors des replays (ReplayStep) afin d'éviter des appels API involontaires et payants.

Purpose: Aujourd'hui les MP3 sont uniquement en documentDirectory (perdus à la réinstall), et ReplayStep regénère silencieusement l'histoire via ElevenLabs si le cache local est vide. On centralise l'audio dans le vault et on rend la génération explicite côté replay.
Output: Vault iCloud contient 09 - Histoires/{enfant}/{storyId}.mp3 pour chaque histoire générée ; ReplayStep affiche un bouton "Générer l'audio" uniquement si besoin.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@lib/elevenlabs.ts
@lib/fish-audio.ts
@components/stories/StoryPlayer.tsx
@app/(tabs)/stories.tsx
@contexts/VaultContext.tsx
@lib/vault.ts

<interfaces>
<!-- Contrats clés à connaître avant d'implémenter — à vérifier dans les fichiers ci-dessus -->

Vault (via useVault()):
- `vault` peut être null si iCloud indisponible
- `vault.uri(relPath: string): string` — URI absolue du fichier vault
- `vault.copyFileToVault(srcUri: string, destRelPath: string): Promise<void>` — copie best-effort

expo-file-system (déjà utilisé):
- `FileSystem.getInfoAsync(uri): Promise<{ exists: boolean, ... }>`
- `FileSystem.copyAsync({ from, to }): Promise<void>`

Convention path vault histoires: `09 - Histoires/{enfant}/{storyId}.mp3`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Helpers vault + fallback vault dans getCachedStoryAudio (ElevenLabs + Fish Audio)</name>
  <files>lib/elevenlabs.ts, lib/fish-audio.ts</files>
  <action>
Dans `lib/elevenlabs.ts`:
1. Ajouter en export :
```ts
export function storyVaultAudioRelPath(enfant: string, storyId: string): string {
  return `09 - Histoires/${enfant}/${storyId}.mp3`;
}
```
2. Modifier la signature de `getCachedStoryAudio` pour accepter un 3e paramètre optionnel :
```ts
export async function getCachedStoryAudio(
  storyId: string,
  voiceId: string,
  vaultUri?: string,
): Promise<string | null>
```
Comportement :
- Garder la logique existante de lookup cache documentDirectory en premier
- Si le cache documentDir n'existe pas ET `vaultUri` est fourni :
  - `FileSystem.getInfoAsync(vaultUri)` → si `exists`, calculer l'URI cible documentDir (même que celui utilisé par la logique actuelle de cache) et `FileSystem.copyAsync({ from: vaultUri, to: cacheUri })` puis retourner `cacheUri`
  - Entourer la copie d'un try/catch silencieux (`if (__DEV__) console.warn(...)`), retourner `null` en cas d'échec
- Sinon retourner `null` comme avant

Dans `lib/fish-audio.ts`:
- Appliquer EXACTEMENT le même pattern à `getCachedStoryAudioFish` (signature étendue `(storyId, voiceId, vaultUri?)` + fallback vault → copy vers documentDir)
- Le helper `storyVaultAudioRelPath` reste exporté uniquement depuis `lib/elevenlabs.ts` (pas de duplication)

Ne PAS toucher aux call-sites de `getCachedStoryAudio*` autres que StoryPlayer (fait en Task 2) — la signature reste rétrocompatible (paramètre optionnel).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>
- `storyVaultAudioRelPath(enfant, storyId)` exporté depuis `lib/elevenlabs.ts` et retourne `09 - Histoires/{enfant}/{storyId}.mp3`
- `getCachedStoryAudio` et `getCachedStoryAudioFish` acceptent un 3e paramètre `vaultUri?: string`
- Quand le cache documentDir est absent mais `vaultUri` pointe vers un fichier existant, le MP3 est copié dans documentDir et l'URI locale est retournée
- Aucune régression TS sur les call-sites existants (paramètre optionnel)
  </done>
</task>

<task type="auto">
  <name>Task 2: StoryPlayer — prop autoGenerate + lecture vault + copie post-génération + UI bouton manuel</name>
  <files>components/stories/StoryPlayer.tsx, app/(tabs)/stories.tsx</files>
  <action>
Dans `components/stories/StoryPlayer.tsx`:

1. Étendre l'interface Props :
```ts
interface Props {
  histoire: BedtimeStory;
  voiceConfig: StoryVoiceConfig;
  elevenLabsKey: string;
  fishAudioKey?: string;
  onFinish: () => void;
  autoGenerate?: boolean; // défaut true
}
```
Destructurer `autoGenerate = true` dans le composant.

2. Imports :
- Ajouter `import { useVault } from '../../contexts/VaultContext';`
- Ajouter import `storyVaultAudioRelPath` depuis `../../lib/elevenlabs`
- Destructurer `const { vault } = useVault();`

3. État local :
- Ajouter `const [needsGeneration, setNeedsGeneration] = useState(false);`

4. Dans le `useEffect` de chargement (autour de la ligne 257, là où on appelle `getCachedStoryAudio*` puis `generateSpeech`) :
- Calculer `const vaultUri = vault ? vault.uri(storyVaultAudioRelPath(histoire.enfant, histoire.id)) : undefined;`
- Passer `vaultUri` aux appels `getCachedStoryAudio(storyId, voiceId, vaultUri)` et `getCachedStoryAudioFish(storyId, voiceId, vaultUri)` selon le moteur utilisé
- Si aucun cache n'est trouvé (ni documentDir ni vault) :
  - Si `autoGenerate === false` : `setNeedsGeneration(true)` et NE PAS appeler `generateSpeech` / Fish Audio — sortir du useEffect proprement (pas de loader, pas de génération)
  - Si `autoGenerate === true` : comportement existant (appel auto à la génération)

5. Extraire la logique de génération dans une fonction `run()` appelable à la demande (si ce n'est pas déjà le cas dans le code existant — sinon adapter en conséquence, garder la sémantique actuelle).

6. Après une génération réussie (dans `run()`, juste après avoir obtenu `result.audioUri`) :
```ts
if (vault) {
  vault
    .copyFileToVault(result.audioUri, storyVaultAudioRelPath(histoire.enfant, histoire.id))
    .catch(() => { /* best-effort — iCloud indisponible */ });
}
```
Appliquer pour les deux moteurs (ElevenLabs et Fish Audio) — même chemin vault.

7. Rendu conditionnel quand `needsGeneration === true` et pas encore de `localUri` :
- Afficher une vue centrée avec un bouton primaire (style cohérent avec `primaryButton` existant du fichier) libellé "🔊 Générer l'audio"
- `onPress` → setNeedsGeneration(false) puis appeler `run()` (déclencher la génération manuelle, qui affichera ensuite le Waveform normal)
- Pendant la génération manuelle (loader existant `isLoading`/équivalent déjà présent) : afficher `ActivityIndicator` + texte "Génération en cours…"
- Respecter le thème via `useThemeColors()` — aucun hardcoded color
- Textes en français

Dans `app/(tabs)/stories.tsx`:
- Dans `ReplayStep` (autour de la ligne ~1304 où le `StoryPlayer` est instancié), ajouter la prop `autoGenerate={false}` sur le JSX `<StoryPlayer ... />`. Ne PAS toucher aux autres usages de `StoryPlayer` (flux création d'histoire neuve doit garder le comportement auto-génération par défaut).

Contraintes rappel :
- Vault copy best-effort : toujours `.catch(() => {})` (silencieux si iCloud indisponible)
- Langue UI : français
- Animations : si ajout d'anim sur le bouton, utiliser `react-native-reanimated` (pas RN Animated) — mais un simple Pressable sans anim est acceptable ici
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>
- `StoryPlayer` accepte `autoGenerate?: boolean` (défaut `true`)
- Au montage, `vaultUri` est calculé via `storyVaultAudioRelPath` et passé à `getCachedStoryAudio` / `getCachedStoryAudioFish`
- Quand `autoGenerate={false}` et aucun cache (local ou vault) n'est disponible, aucun appel à `generateSpeech` / Fish Audio n'est déclenché ; l'UI affiche un bouton "🔊 Générer l'audio"
- Un tap sur le bouton lance la génération manuelle, puis le Waveform s'affiche normalement
- Après toute génération réussie (auto ou manuelle), le MP3 est copié dans le vault à `09 - Histoires/{enfant}/{storyId}.mp3` (best-effort silencieux)
- `ReplayStep` dans `app/(tabs)/stories.tsx` passe `autoGenerate={false}` au `StoryPlayer`
- `npx tsc --noEmit` ne remonte pas de nouvelle erreur (erreurs pré-existantes MemoryEditor/cooklang/useVault ignorées)
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` : pas de nouvelle erreur TS introduite par ce plan (erreurs pré-existantes ignorées)
- Lecture manuelle : `grep -n "autoGenerate" components/stories/StoryPlayer.tsx app/(tabs)/stories.tsx` → prop déclarée + passée en ReplayStep
- Lecture manuelle : `grep -n "storyVaultAudioRelPath\\|copyFileToVault" components/stories/StoryPlayer.tsx lib/elevenlabs.ts lib/fish-audio.ts` → helper exporté, appelé dans StoryPlayer, fallback vault dans les deux libs
</verification>

<success_criteria>
- Au replay d'une histoire déjà générée sur un device réinstallé mais avec vault iCloud sync : le MP3 est récupéré depuis le vault, copié en documentDir, et le Waveform joue sans appel API
- Au replay d'une histoire dont le MP3 n'existe ni en cache local ni dans le vault : un bouton "🔊 Générer l'audio" s'affiche, aucun appel automatique à ElevenLabs/Fish Audio n'est fait
- Après une génération (initiale ou manuelle post-replay), le MP3 est présent dans le vault à `09 - Histoires/{enfant}/{storyId}.mp3`
- Aucune régression sur le flux de création d'une nouvelle histoire (autoGenerate par défaut true)
</success_criteria>

<output>
After completion, create `.planning/quick/260423-jw6-stocker-les-mp3-elevenlabs-dans-le-vault/260423-jw6-SUMMARY.md`
</output>
