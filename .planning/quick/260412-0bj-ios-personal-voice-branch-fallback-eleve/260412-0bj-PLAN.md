---
phase: 260412-0bj
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/types.ts
  - components/stories/StoryPlayer.tsx
autonomous: true
requirements:
  - IVC-VOICE-iOS-PERSONAL
  - IVC-VOICE-FALLBACK-GENDER
must_haves:
  truths:
    - "Un profil adulte avec voiceSource='ios-personal' et voicePersonalId lit l'histoire via expo-speech en utilisant l'identifier iOS Personal Voice"
    - "Un profil adulte sans voiceElevenLabsId sélectionné déclenche un fallback ElevenLabs par genre (Bella féminin / Adam masculin/inconnu)"
    - "Le fallback est session-only : le profil n'est jamais muté et le bouton + reste visible pour permettre le clonage"
    - "tsc --noEmit passe sans nouvelle erreur"
  artifacts:
    - path: "lib/types.ts"
      provides: "Champ voiceIdentifier optionnel dans StoryVoiceConfig"
      contains: "voiceIdentifier?: string"
    - path: "components/stories/StoryPlayer.tsx"
      provides: "Branche iOS Personal Voice + fallback ElevenLabs par genre"
      contains: "voiceIdentifier"
  key_links:
    - from: "components/stories/StoryPlayer.tsx"
      to: "expo-speech Speech.speak"
      via: "voice: effectiveVoiceConfig.voiceIdentifier"
      pattern: "voice.*effectiveVoiceConfig\\.voiceIdentifier"
    - from: "components/stories/StoryPlayer.tsx"
      to: "ELEVENLABS_FRENCH_VOICES"
      via: "import + lookup par id (Bella/Adam) selon gender"
      pattern: "ELEVENLABS_FRENCH_VOICES"
---

<objective>
Brancher le vrai support iOS Personal Voice dans StoryPlayer (via l'identifier expo-speech) et ajouter un fallback ElevenLabs par genre quand un profil adulte sélectionné n'a pas encore de voix clonée.

Purpose: Terminer la boucle "Voix du narrateur" — aujourd'hui `voiceSource === 'ios-personal'` retombe silencieusement sur la voix système par défaut, et sélectionner un parent sans clone ElevenLabs ne produit aucun effet audible. Les deux branches doivent être fonctionnelles session-only, sans mutation de profil.
Output: Histoire lue avec la voix Personal iOS du parent OU avec un preset ElevenLabs (Bella/Adam) selon le genre, selon la configuration du profil sélectionné.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@./CLAUDE.md
@lib/types.ts
@lib/stories.ts
@components/stories/StoryPlayer.tsx
@lib/personal-voice.ts

<interfaces>
Contrats clés extraits du codebase — l'exécutant NE DOIT PAS explorer davantage.

From lib/types.ts (actuel, à étendre en Tâche 1) :

```typescript
export type StoryVoiceEngine = 'expo-speech' | 'elevenlabs';

export interface StoryVoiceConfig {
  engine: StoryVoiceEngine;
  language: 'fr' | 'en';
  elevenLabsVoiceId?: string;
  // AJOUT Tâche 1 :
  voiceIdentifier?: string; // identifier iOS Personal Voice pour expo-speech
}

export type Gender = 'garçon' | 'fille';

export interface Profile {
  // ... champs existants ...
  gender?: Gender; // 'garçon' | 'fille'
  voiceElevenLabsId?: string;
  voicePersonalId?: string;
  voiceSource?: 'ios-personal' | 'elevenlabs-cloned' | 'elevenlabs-preset' | 'expo-speech';
}
```

NOTE gender : le type `Gender` est `'garçon' | 'fille'`. Le task_detail mentionne "féminin/masculin" — il faut donc mapper :
- `gender === 'fille'` → Bella (féminin)
- `gender === 'garçon'` ou absent → Adam (masculin/inconnu)

From lib/stories.ts :

```typescript
export const ELEVENLABS_FRENCH_VOICES = [
  { id: 'pNInz6obpgDQGcFmaJgB', label: 'Adam — doux et chaleureux' },
  { id: 'EXAVITQu4vr4xnSDxMaL', label: 'Bella — féminin, apaisant' },
  { id: 'VR6AewLTigWG4xSOukaG', label: 'Arnold — grave, posé' },
];
```

→ NE PAS hardcoder les IDs dans StoryPlayer. Importer la constante et faire `ELEVENLABS_FRENCH_VOICES.find(v => v.id === 'EXAVITQu4vr4xnSDxMaL')` pour Bella et `find(v => v.id === 'pNInz6obpgDQGcFmaJgB')` pour Adam.

From components/stories/StoryPlayer.tsx (état actuel, lignes clés) :
- ligne 133-145 : `effectiveVoiceConfig` useMemo — actuellement la branche `ios-personal` retombe sur `{ engine: 'expo-speech', language: 'fr' }` sans identifier (voir commentaire "finalisé ultérieurement" à remplacer).
- ligne 174-205 : `startPlayback` — branche expo-speech appelle `Speech.speak(histoire.texte, { language, voice: voiceId, rate: speed, ... })` où `voiceId` vient actuellement de `getAvailableVoicesAsync()`. Il faut prioriser `effectiveVoiceConfig.voiceIdentifier` quand défini.
- ligne 116 : signature `function StoryPlayer({ histoire, voiceConfig, elevenLabsKey, onFinish }: Props)` — les profiles sont lus via `useVault()` ligne 124.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1 : Étendre StoryVoiceConfig avec voiceIdentifier</name>
  <files>lib/types.ts</files>
  <action>
Dans `lib/types.ts`, localiser l'interface `StoryVoiceConfig` (autour de la ligne 611) et ajouter le champ optionnel `voiceIdentifier?: string` avec un commentaire FR :

```typescript
export interface StoryVoiceConfig {
  engine: StoryVoiceEngine;
  language: 'fr' | 'en';
  elevenLabsVoiceId?: string;
  voiceIdentifier?: string; // identifier iOS Personal Voice (expo-speech) — session-only
}
```

Ne rien modifier d'autre. Ne PAS toucher à `BedtimeStory` ni aux parsers/serializers — le champ est session-only, jamais persisté dans le markdown.
  </action>
  <verify>
    <automated>cd /Users/gabrielwaltio/Documents/family-vault && npx tsc --noEmit 2>&1 | grep -v "MemoryEditor\|cooklang\|useVault" | head -30</automated>
  </verify>
  <done>Le champ `voiceIdentifier?: string` existe dans `StoryVoiceConfig`. tsc ne remonte aucune nouvelle erreur (les erreurs pré-existantes MemoryEditor/cooklang/useVault sont tolérées).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2 : Brancher iOS Personal Voice + fallback ElevenLabs par genre dans StoryPlayer</name>
  <files>components/stories/StoryPlayer.tsx</files>
  <action>
Trois modifications dans `components/stories/StoryPlayer.tsx`.

**2.A — Import ELEVENLABS_FRENCH_VOICES**

Ajouter l'import en haut du fichier (près de l'import `generateSpeech`) :

```typescript
import { ELEVENLABS_FRENCH_VOICES } from '../../lib/stories';
```

**2.B — Remplacer le useMemo `effectiveVoiceConfig` (lignes ~133-145)**

Remplacer le bloc actuel par la logique complète (priorité : iOS Personal Voice > clone ElevenLabs > fallback ElevenLabs par genre > fallback voiceConfig initial) :

```typescript
const effectiveVoiceConfig = React.useMemo<StoryVoiceConfig>(() => {
  if (!selectedParentId) return voiceConfig;
  const parent = adultProfiles.find((p: Profile) => p.id === selectedParentId);
  if (!parent) return voiceConfig;

  // 1. iOS Personal Voice — prioritaire si configurée
  if (parent.voiceSource === 'ios-personal' && parent.voicePersonalId) {
    return {
      engine: 'expo-speech',
      language: voiceConfig.language,
      voiceIdentifier: parent.voicePersonalId,
    };
  }

  // 2. Clone ElevenLabs si disponible
  if (parent.voiceElevenLabsId) {
    return {
      engine: 'elevenlabs',
      language: voiceConfig.language,
      elevenLabsVoiceId: parent.voiceElevenLabsId,
    };
  }

  // 3. Fallback ElevenLabs preset par genre — uniquement si engine initial est elevenlabs
  //    (session-only, aucune persistance, bouton + reste visible)
  if (voiceConfig.engine === 'elevenlabs') {
    const BELLA_ID = 'EXAVITQu4vr4xnSDxMaL';
    const ADAM_ID = 'pNInz6obpgDQGcFmaJgB';
    const targetId = parent.gender === 'fille' ? BELLA_ID : ADAM_ID;
    const preset = ELEVENLABS_FRENCH_VOICES.find(v => v.id === targetId);
    if (preset) {
      return {
        engine: 'elevenlabs',
        language: voiceConfig.language,
        elevenLabsVoiceId: preset.id,
      };
    }
  }

  return voiceConfig;
}, [selectedParentId, adultProfiles, voiceConfig]);
```

Notes importantes :
- Le type `Gender` est `'garçon' | 'fille'` — mapper `fille → Bella`, tout le reste (`garçon` ou `undefined`) → `Adam`.
- Les IDs BELLA/ADAM sont définis comme constantes locales dans le useMemo et comparés contre `ELEVENLABS_FRENCH_VOICES` — on NE hardcode PAS un objet, on résout via `.find(...)`.
- Ne PAS muter le profil : aucun `updateProfile(...)` dans cette branche.
- Le bouton + (lignes 299-310) reste inchangé : il s'affiche déjà quand `!hasClone` — laisser tel quel.

**2.C — Utiliser voiceIdentifier dans Speech.speak (branche expo-speech, lignes ~174-205)**

Dans `startPlayback`, branche `expo-speech`, prioriser `effectiveVoiceConfig.voiceIdentifier` quand défini (iOS Personal Voice) avant la résolution par langue :

```typescript
if (effectiveVoiceConfig.engine === 'expo-speech') {
  const targetLang = effectiveVoiceConfig.language === 'en' ? 'en-US' : 'fr-FR';

  let voiceId: string | undefined = effectiveVoiceConfig.voiceIdentifier;

  // Si pas d'identifier explicite (voix iOS Personal), résoudre par langue
  if (!voiceId) {
    try {
      const voices = await Speech.getAvailableVoicesAsync();
      if (__DEV__) console.log('Voix disponibles:', voices.map(v => `${v.identifier} (${v.language})`));
      const match = voices.find(v => v.language.startsWith(effectiveVoiceConfig.language === 'en' ? 'en' : 'fr'));
      voiceId = match?.identifier;
      if (__DEV__) console.log('Voix choisie:', voiceId ?? 'aucune');
    } catch (e) {
      if (__DEV__) console.warn('getAvailableVoicesAsync failed:', e);
    }
  } else if (__DEV__) {
    console.log('Voix iOS Personal Voice explicite:', voiceId);
  }

  Speech.speak(histoire.texte, {
    language: targetLang,
    voice: voiceId,
    rate: speed,
    onStart: () => { if (__DEV__) console.log('Speech démarré'); },
    onDone: () => setIsPlaying(false),
    onStopped: () => setIsPlaying(false),
    onError: (err) => {
      if (__DEV__) console.error('Speech error:', err);
      Alert.alert('Erreur de lecture', "La voix système n'a pas pu lire l'histoire. Essayez ElevenLabs ou redémarrez l'app.");
      setIsPlaying(false);
    },
  });
  setIsPlaying(true);
} else {
  // Branche ElevenLabs — inchangée (garder le code actuel tel quel)
}
```

Contraintes :
- Langue FR pour tous les commentaires, logs, messages Alert.
- useThemeColors() — déjà utilisé, ne rien changer au styling.
- Spacing tokens — déjà utilisés.
- Ne PAS toucher au VoiceRecorder modal ni à `onVoiceReady` (updateProfile reste autorisé UNIQUEMENT dans ce flow clonage, pas dans le fallback).
- Garder la branche ElevenLabs de `startPlayback` strictement identique (elle lit déjà `effectiveVoiceConfig.elevenLabsVoiceId`, donc le fallback Bella/Adam fonctionne automatiquement).
  </action>
  <verify>
    <automated>cd /Users/gabrielwaltio/Documents/family-vault && npx tsc --noEmit 2>&1 | grep -v "MemoryEditor\|cooklang\|useVault" ; grep -q "voiceIdentifier" components/stories/StoryPlayer.tsx && grep -q "ELEVENLABS_FRENCH_VOICES" components/stories/StoryPlayer.tsx && grep -q "parent.gender === 'fille'" components/stories/StoryPlayer.tsx && echo OK_GREPS</automated>
  </verify>
  <done>
- Import `ELEVENLABS_FRENCH_VOICES` présent.
- `effectiveVoiceConfig` retourne `voiceIdentifier: parent.voicePersonalId` pour la branche ios-personal.
- Fallback par genre résout Bella/Adam via `.find()` sur `ELEVENLABS_FRENCH_VOICES`.
- `Speech.speak` utilise `effectiveVoiceConfig.voiceIdentifier` en priorité quand défini.
- Aucun `updateProfile` ajouté dans la logique de fallback.
- tsc ne remonte aucune nouvelle erreur (hors erreurs pré-existantes tolérées).
  </done>
</task>

</tasks>

<verification>
Vérification globale après les deux tâches :

1. **Type check** :

```bash
cd /Users/gabrielwaltio/Documents/family-vault && npx tsc --noEmit 2>&1 | grep -v "MemoryEditor\|cooklang\|useVault"
```

Aucune nouvelle erreur ne doit apparaître.

2. **Présence des modifications** :

```bash
grep -n "voiceIdentifier" lib/types.ts components/stories/StoryPlayer.tsx
grep -n "ELEVENLABS_FRENCH_VOICES" components/stories/StoryPlayer.tsx
grep -n "parent.gender === 'fille'" components/stories/StoryPlayer.tsx
grep -n "voiceSource === 'ios-personal' && parent.voicePersonalId" components/stories/StoryPlayer.tsx
```

Tous ces patterns doivent renvoyer des lignes.

3. **Absence de régression** :

```bash
grep -n "updateProfile" components/stories/StoryPlayer.tsx
```

Doit montrer UNIQUEMENT l'appel existant dans `onVoiceReady` du VoiceRecorder (clonage), pas dans la logique fallback.

4. **Test manuel device** (post-merge, non bloquant pour le plan) :
   - Profil adulte "Maman" avec `voiceSource: 'ios-personal'` + `voicePersonalId: '<id Apple>'` → Speech.speak utilise cette voix.
   - Profil adulte "Papa" sans voice* → engine ElevenLabs tombe sur Adam.
   - Profil adulte "Maman" gender=fille sans voice* → engine ElevenLabs tombe sur Bella.
   - Bouton + reste affiché dans tous les cas de fallback.
</verification>

<success_criteria>
- `lib/types.ts` : `StoryVoiceConfig` contient `voiceIdentifier?: string`.
- `components/stories/StoryPlayer.tsx` : import `ELEVENLABS_FRENCH_VOICES` présent.
- `effectiveVoiceConfig` implémente les 3 branches dans le bon ordre (iOS Personal > clone ElevenLabs > fallback par genre).
- `Speech.speak` utilise `effectiveVoiceConfig.voiceIdentifier` en priorité.
- Le fallback par genre est session-only (aucun `updateProfile` ajouté).
- Le bouton + reste visible pour les profils sans clone.
- `npx tsc --noEmit` ne remonte aucune nouvelle erreur.
</success_criteria>

<output>
Après complétion, créer `.planning/quick/260412-0bj-ios-personal-voice-branch-fallback-eleve/260412-0bj-SUMMARY.md` résumant :
- Modifications exactes apportées à `lib/types.ts` et `components/stories/StoryPlayer.tsx`
- Résultat du `tsc --noEmit`
- Résultat des greps de vérification
- Notes éventuelles sur les tests manuels device restants
</output>
