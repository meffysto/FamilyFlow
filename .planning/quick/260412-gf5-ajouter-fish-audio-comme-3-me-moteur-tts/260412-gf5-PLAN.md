---
phase: quick-260412-gf5
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/types.ts
  - lib/fish-audio.ts
  - contexts/StoryVoiceContext.tsx
  - lib/voice-clone.ts
  - app/(tabs)/stories.tsx
  - components/stories/StoryPlayer.tsx
  - components/settings/SettingsFishAudio.tsx
autonomous: true
requirements: [FISH-TTS]
must_haves:
  truths:
    - "L'utilisateur peut sélectionner Fish Audio comme moteur TTS dans l'écran histoires"
    - "Une histoire générée avec Fish Audio produit un MP3 lisible"
    - "Le clonage vocal fonctionne via Fish Audio (upload sample -> reference_id)"
    - "La clé API Fish Audio est persistée de manière sécurisée"
  artifacts:
    - path: "lib/fish-audio.ts"
      provides: "Service TTS Fish Audio (generateSpeech + cache MP3)"
    - path: "components/settings/SettingsFishAudio.tsx"
      provides: "Modal configuration clé API Fish Audio"
  key_links:
    - from: "components/stories/StoryPlayer.tsx"
      to: "lib/fish-audio.ts"
      via: "generateFishSpeech() quand engine === 'fish-audio'"
    - from: "app/(tabs)/stories.tsx"
      to: "contexts/StoryVoiceContext.tsx"
      via: "fishAudioKey + isFishAudioConfigured"
---

<objective>
Ajouter Fish Audio comme 3eme moteur TTS pour les histoires du soir, en miroir exact de l'integration ElevenLabs existante.

Purpose: Offrir une alternative TTS avec voix francaises natives et clonage vocal via Fish Audio.
Output: Moteur Fish Audio fonctionnel (TTS, clonage, settings, selecteur UI).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@lib/elevenlabs.ts (pattern a miroir pour fish-audio.ts)
@lib/voice-clone.ts (pattern clonage a etendre)
@contexts/StoryVoiceContext.tsx (ajouter fishAudioKey)
@lib/types.ts (etendre StoryVoiceEngine + StoryVoiceConfig + Profile)
@app/(tabs)/stories.tsx (ajouter onglet Fish Audio)
@components/stories/StoryPlayer.tsx (brancher engine fish-audio)
@components/settings/SettingsElevenLabs.tsx (pattern pour SettingsFishAudio)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Types + Service TTS Fish Audio + Clonage</name>
  <files>lib/types.ts, lib/fish-audio.ts, lib/voice-clone-fish.ts</files>
  <action>
1. Dans lib/types.ts:
   - Etendre StoryVoiceEngine: `'expo-speech' | 'elevenlabs' | 'fish-audio'`
   - Ajouter champ optionnel `fishAudioReferenceId?: string` dans StoryVoiceConfig
   - Ajouter dans Profile: `voiceFishAudioId?: string` (reference_id du modele clone)
   - Etendre voiceSource union: ajouter `'fish-audio-cloned' | 'fish-audio-preset'`

2. Creer lib/fish-audio.ts en miroir exact de lib/elevenlabs.ts:
   - Meme structure: AUDIO_DIR partage (stories-audio/), storyAudioPath(), getCachedStoryAudio() reutilises depuis elevenlabs.ts (import)
   - Interface FishAudioOptions { referenceId?: string }
   - generateFishSpeech(apiKey, text, referenceId, storyId, options?) -> {audioUri} | {error}
   - POST https://api.fish.audio/v1/tts avec headers {Authorization: `Bearer ${apiKey}`, 'Content-Type': 'application/json'}, body {text, reference_id: referenceId, format: 'mp3', chunk_length: 200}
   - Response = binary MP3 stream (arrayBuffer -> base64 -> writeAsStringAsync)
   - Dedup inflight requests (meme pattern inflightRequests Map)
   - Cache persistant identique (storyAudioPath reutilise)

3. Creer lib/voice-clone-fish.ts:
   - uploadFishVoiceClone(audioUri, profileName, apiKey) -> string (reference_id)
   - POST https://api.fish.audio/model avec FormData: title=profileName, voices=fichier audio, description="Voix clonee {profileName}"
   - Response JSON: {_id: string} -> retourner _id comme reference_id
   - Meme pattern validation fichier que voice-clone.ts
  </action>
  <verify>npx tsc --noEmit</verify>
  <done>Types etendus, lib/fish-audio.ts exporte generateFishSpeech avec cache/dedup, lib/voice-clone-fish.ts exporte uploadFishVoiceClone</done>
</task>

<task type="auto">
  <name>Task 2: Context + Settings + UI stories + StoryPlayer</name>
  <files>contexts/StoryVoiceContext.tsx, components/settings/SettingsFishAudio.tsx, app/(tabs)/stories.tsx, components/stories/StoryPlayer.tsx</files>
  <action>
1. Dans contexts/StoryVoiceContext.tsx:
   - Ajouter constante FISH_AUDIO_KEY_STORAGE = 'story_fish_audio_key'
   - Ajouter state fishAudioKey (meme pattern que elevenLabsKey)
   - Ajouter isFishAudioConfigured: fishAudioKey.length > 0
   - Ajouter setFishAudioKey, clearFishAudioKey (meme pattern)
   - Charger depuis SecureStore au mount (ajouter au Promise.all existant)
   - Exporter dans StoryVoiceState interface

2. Creer components/settings/SettingsFishAudio.tsx:
   - Copier le pattern exact de SettingsElevenLabs.tsx
   - Modal avec TextInput pour cle API, bouton sauver, statut, bouton clear
   - Utiliser useStoryVoice().fishAudioKey / setFishAudioKey / clearFishAudioKey

3. Dans app/(tabs)/stories.tsx:
   - Etendre le type local de localVoiceEngine: `'expo-speech' | 'elevenlabs' | 'fish-audio'`
   - Ajouter un 3eme onglet dans le array voiceEngineRow: { key: 'fish-audio', label: `🐟 Fish Audio${isFishAudioConfigured ? '' : ' (cle)'}` }
   - Ajouter section conditionnelle `localVoiceEngine === 'fish-audio'` apres la section elevenlabs:
     - Si adultProfiles avec voiceFishAudioId: afficher selecteur parent (meme pattern elevenlabs)
     - Sinon: message "Configurez Fish Audio dans les parametres"
   - Etendre buildFinalVoiceConfig: si localVoiceEngine === 'fish-audio', retourner { engine: 'fish-audio', language: lang, fishAudioReferenceId: parent.voiceFishAudioId ?? undefined }
   - Importer isFishAudioConfigured depuis useStoryVoice()

4. Dans components/stories/StoryPlayer.tsx:
   - Ajouter prop fishAudioKey dans Props
   - Importer generateFishSpeech depuis lib/fish-audio
   - Ajouter condition: si voiceConfig.engine === 'fish-audio', utiliser generateFishSpeech(fishAudioKey, texte, voiceConfig.fishAudioReferenceId, storyId)
   - Meme pattern de fallback vers expo-speech si erreur
   - Mettre a jour le parent qui passe fishAudioKey (chercher usage de StoryPlayer et ajouter fishAudioKey={fishAudioKey} depuis useStoryVoice)

5. Dans VoiceRecorder (si utilise pour clonage):
   - Ajouter support uploadFishVoiceClone quand engine fish-audio selectionne
   - Sauvegarder le reference_id retourne dans profile.voiceFishAudioId via updateProfile
  </action>
  <verify>npx tsc --noEmit</verify>
  <done>Onglet Fish Audio visible dans selecteur voix, cle API persistable, StoryPlayer genere audio via Fish Audio API, clonage vocal Fish Audio fonctionnel</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passe sans erreur
- L'onglet Fish Audio apparait dans l'ecran histoires
- Le flow complet: selecteur -> generation -> lecture MP3 fonctionne
</verification>

<success_criteria>
- Fish Audio est un 3eme moteur TTS completement fonctionnel
- Pattern identique a ElevenLabs (cache MP3, dedup, clonage, settings)
- Aucune regression sur expo-speech et elevenlabs existants
- Types corrects (tsc passe)
</success_criteria>

<output>
After completion, create `.planning/quick/260412-gf5-ajouter-fish-audio-comme-3-me-moteur-tts/260412-gf5-SUMMARY.md`
</output>
