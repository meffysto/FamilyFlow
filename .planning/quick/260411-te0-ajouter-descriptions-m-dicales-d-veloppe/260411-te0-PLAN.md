---
phase: quick-260411-te0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - locales/fr/skills.json
  - components/SkillDetailModal.tsx
  - app/(tabs)/skills.tsx
autonomous: true
requirements: [TE0-01, TE0-02, TE0-03]
must_haves:
  truths:
    - "Chaque skill ID (~277) affiche une description médicale/développementale dans le modal détail"
    - "Les descriptions sont rédigées en français avec un ton médical professionnel"
    - "La zone de description est scrollable pour préserver le layout sur petits écrans"
    - "Une skill sans description affiche le modal comme aujourd'hui (pas de crash)"
  artifacts:
    - path: "locales/fr/skills.json"
      provides: "Objet descriptions avec une entrée par skill ID"
      contains: "\"descriptions\""
    - path: "components/SkillDetailModal.tsx"
      provides: "Prop description? + ScrollView d'affichage"
      contains: "description?: string"
    - path: "app/(tabs)/skills.tsx"
      provides: "Passage de la description via t('skills.descriptions.{id}')"
      pattern: "descriptions\\."
  key_links:
    - from: "app/(tabs)/skills.tsx"
      to: "locales/fr/skills.json"
      via: "t('skills.descriptions.{skillId}', { defaultValue: '' })"
      pattern: "skills\\.descriptions"
    - from: "components/SkillDetailModal.tsx"
      to: "SkillDetailModalProps.description"
      via: "rendu conditionnel dans ScrollView"
      pattern: "description &&"
---

<objective>
Ajouter des descriptions médicales/développementales dans l'onglet Compétences : chaque skill du tree (~277 entrées) obtient une description courte, de ton médical/pédiatrique, affichée dans le modal détail sous le XP. Les descriptions vivent directement dans le fichier i18n `locales/fr/skills.json` et sont lues via `useTranslation('skills')`.

Purpose: donner du contexte développemental aux parents — expliquer pourquoi chaque jalon compte neurologiquement/physiologiquement et à quel âge il est typiquement attendu selon les référentiels (OMS, Denver, Brazelton).
Output: 3 fichiers modifiés, descriptions rédigées directement dans le JSON, modal avec zone scrollable.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@locales/fr/skills.json
@components/SkillDetailModal.tsx
@app/(tabs)/skills.tsx

<interfaces>
<!-- Structure actuelle de skills.json (extrait) -->
```json
{
  "categories": { "motricite_globale": "...", "motricite_fine": "...", ... },
  "ageBrackets": { "0-1": {...}, "1-2": {...}, ... },
  "tree": {
    "motricite_globale_0-1_1": "Soulève la tête sur le ventre",
    "motricite_globale_0-1_2": "Tient sa tête droite",
    ...
    "responsabilite_15+_5": "Prend des responsabilités civiques"
  }
}
```

Les IDs de `tree` (~277 au total) sont la source de vérité. Chaque clé de `descriptions` DOIT correspondre exactement à une clé de `tree`.

<!-- Props actuelles de SkillDetailModal -->
```typescript
interface SkillDetailModalProps {
  visible: boolean;
  onClose: () => void;
  skill: {
    id: string;
    label: string;
    categoryEmoji: string;
    categoryLabel: string;
    ageBracketLabel: string;
    xp: number;
  } | null;
  state: 'locked' | 'unlockable' | 'unlocked';
  unlockedAt?: string;
  unlockedBy?: string;
  onUnlock?: () => void;
}
```

<!-- Appel actuel dans app/(tabs)/skills.tsx (lignes 325-340) -->
```tsx
<SkillDetailModal
  visible={modalVisible}
  onClose={() => setModalVisible(false)}
  skill={selectedSkill ? {
    id: selectedSkill.id,
    label: sk.skillLabel(selectedSkill.id),
    ...
  } : null}
  state={selectedSkillState}
  ...
/>
```

`sk` est l'helper i18n déclaré dans skills.tsx (voir ligne 41 « Traduit les labels statiques via le namespace skills »). Utiliser le même helper `useTranslation('skills')` pour lire `descriptions.{id}`.

Namespace i18n: `skills` (déjà configuré dans `lib/i18n.ts`).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rédiger les descriptions médicales dans skills.json</name>
  <files>locales/fr/skills.json</files>
  <action>
    Ajouter un objet `"descriptions"` à la racine de `locales/fr/skills.json`, entre `"ageBrackets"` et `"tree"`. Chaque clé DOIT correspondre exactement à une clé existante de `tree` (environ 277 entrées au total — parcourir `tree` exhaustivement pour ne rien oublier).

    Format de chaque description :
    - Français, 2-3 phrases courtes (max ~280 caractères)
    - Ton médical/développemental professionnel, à destination des parents
    - Mentionner quand pertinent :
      1) l'importance développementale (pourquoi ce jalon compte)
      2) le support neurologique/physiologique (ex: maturation cortico-spinale, coordination œil-main, développement phonologique, gestion des émotions via cortex préfrontal, etc.)
      3) l'âge typique d'acquisition selon un référentiel reconnu (OMS, Denver II, Brazelton, HAS, carnet de santé français)

    Exemples de ton attendu :
    - `motricite_globale_0-1_2` : "Le contrôle de la tête marque la maturation du tonus axial cervical. Selon l'OMS, la tenue de tête stable est attendue vers 3-4 mois. C'est le premier jalon moteur évalué en consultation pédiatrique."
    - `langage_1-2_5` : "L'explosion lexicale, décrite par Bloom, survient typiquement entre 18 et 24 mois. L'enfant passe de quelques mots isolés à ~50 mots en quelques semaines. Reflète la maturation du cortex temporal gauche."
    - `proprete_1-2_1` : "La maturation sphinctérienne dépend du développement neurologique du contrôle vésical. Selon la HAS, elle commence rarement avant 18-24 mois — attendre les signes de prêt de l'enfant."
    - `social_2-3_6` : "L'empathie précoce repose sur les neurones miroirs et le début de la théorie de l'esprit. Elle apparaît vers 24-30 mois (Brazelton) et se consolide vers 3-4 ans."
    - `responsabilite_12-14_2` : "La gestion d'un compte bancaire jeune développe la planification exécutive (cortex préfrontal en maturation à l'adolescence). Étape recommandée par les programmes d'éducation financière dès 12 ans."

    Règles de rédaction :
    - Pas de jargon excessif — rester accessible aux parents
    - Éviter les formulations anxiogènes (« retard de », « anomalie ») — préférer « typiquement », « généralement », « en moyenne »
    - Citer un référentiel (OMS, Denver, Brazelton, HAS, Carnet de santé) quand l'âge est précisé
    - Pas de jugement, pas de conseils médicaux prescriptifs
    - Ne pas dupliquer le label — la description complète, ne répète pas
    - Fournir UNE description pour CHAQUE skill de `tree` — ne pas en oublier

    Parcourir `tree` catégorie par catégorie (motricité globale, motricité fine, langage, social, cuisine, propreté, ménage, autonomie, organisation, responsabilité) et tranche d'âge par tranche d'âge (0-1, 1-2, 2-3, 3-5, 6-8, 9-11, 12-14, 15+) pour garantir l'exhaustivité.

    Valider le JSON avec `node -e "JSON.parse(require('fs').readFileSync('locales/fr/skills.json','utf8'))"` après édition.

    NE PAS modifier le fichier `locales/en/skills.json` — anglais non demandé par la spec. Le `defaultValue: ''` dans t() côté UI gère le fallback.
  </action>
  <verify>
    <automated>node -e "const s=require('./locales/fr/skills.json'); const tree=Object.keys(s.tree); const descs=Object.keys(s.descriptions||{}); const missing=tree.filter(k=>!descs.includes(k)); if(missing.length){console.error('Missing descriptions:',missing.length,missing.slice(0,5));process.exit(1)} else console.log('OK',tree.length,'descriptions')"</automated>
  </verify>
  <done>
    - `locales/fr/skills.json` contient un objet `descriptions` avec une entrée pour CHAQUE clé de `tree` (~277 entrées)
    - Chaque description fait 2-3 phrases en français
    - JSON valide (parse sans erreur)
    - Référentiels médicaux cités quand pertinent (OMS, Denver, Brazelton, HAS)
  </done>
</task>

<task type="auto">
  <name>Task 2: Ajouter prop description + ScrollView au SkillDetailModal</name>
  <files>components/SkillDetailModal.tsx</files>
  <action>
    Modifier `components/SkillDetailModal.tsx` pour accepter et afficher une description médicale.

    1) Ajouter `ScrollView` à l'import `react-native` (ligne 2) :
       ```tsx
       import { Modal, View, Text, Alert, StyleSheet, ScrollView } from 'react-native';
       ```

    2) Ajouter le champ optionnel `description?: string` à `SkillDetailModalProps` (après `onUnlock?`) :
       ```tsx
       description?: string;
       ```

    3) Ajouter `description` à la destructuration des props dans la signature de `SkillDetailModal`.

    4) Remplacer le `<View style={styles.content}>` par un `<ScrollView>` avec `contentContainerStyle={styles.content}` et `showsVerticalScrollIndicator={false}`. Raison : sur petits écrans, la description longue + le reste du contenu peut dépasser la hauteur du pageSheet — le ScrollView évite tout clipping.

    5) Ajouter une section description APRÈS le bloc XP (`<Text style={[styles.xp...]}>+{skill.xp} XP</Text>`) et AVANT les sections `unlocked` / `unlockable`. Pattern :
       ```tsx
       {description ? (
         <View style={styles.descriptionSection}>
           <Text style={[styles.description, { color: colors.textSub }]}>
             {description}
           </Text>
         </View>
       ) : null}
       ```

    6) Ajouter les styles correspondants dans `StyleSheet.create` en bas du fichier :
       ```tsx
       descriptionSection: {
         width: '100%',
         paddingHorizontal: Spacing.md,
         marginTop: Spacing.sm,
       },
       description: {
         fontSize: FontSize.md,
         lineHeight: FontSize.md * 1.5,
         textAlign: 'center',
       },
       ```
       Utiliser les tokens `Spacing.*` / `FontSize.*` existants (pas de valeurs numériques hardcodées — convention CLAUDE.md). Si `FontSize.md` n'existe pas, utiliser `FontSize.body` ou la valeur existante équivalente après inspection du fichier `constants/typography.ts`.

    7) Ajouter au `content` style `paddingBottom: Spacing['5xl']` pour garantir l'espacement au scroll.

    Ne PAS toucher au reste du layout (emoji, label, chip age, XP, sections unlocked/unlockable) — la modification doit être additive. Le rendu reste identique lorsque `description` est `undefined`.

    Lancer `npx tsc --noEmit` pour valider les types après l'édition.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "SkillDetailModal" || echo "OK no new errors in SkillDetailModal"</automated>
  </verify>
  <done>
    - `SkillDetailModalProps.description?: string` ajouté
    - `ScrollView` enveloppe le contenu avec `contentContainerStyle`
    - La description s'affiche sous le XP quand fournie, invisible sinon
    - `npx tsc --noEmit` ne génère AUCUNE nouvelle erreur dans `SkillDetailModal.tsx`
    - Aucune couleur hardcodée — `useThemeColors()` utilisé (`colors.textSub`)
  </done>
</task>

<task type="auto">
  <name>Task 3: Passer la description depuis skills.tsx via useTranslation</name>
  <files>app/(tabs)/skills.tsx</files>
  <action>
    Modifier l'appel à `<SkillDetailModal ... />` dans `app/(tabs)/skills.tsx` (autour de la ligne 325) pour passer la description lue via i18n.

    1) Utiliser le helper `t` déjà disponible via `useTranslation` dans le fichier (déjà importé pour `sk.*`). Vérifier l'existence d'une instance `t` accessible ; sinon, utiliser `sk` si c'est un wrapper.

    2) Ajouter la prop `description` à l'appel du modal :
       ```tsx
       <SkillDetailModal
         visible={modalVisible}
         onClose={() => setModalVisible(false)}
         skill={selectedSkill ? { ... } : null}
         state={selectedSkillState}
         unlockedAt={selectedSkillUnlock?.unlockedAt}
         unlockedBy={...}
         onUnlock={...}
         description={
           selectedSkill
             ? t(`descriptions.${selectedSkill.id}`, { ns: 'skills', defaultValue: '' }) || undefined
             : undefined
         }
       />
       ```

       Notes :
       - `defaultValue: ''` empêche i18next de retourner la clé brute si la description manque
       - `|| undefined` transforme la string vide en `undefined` → le modal n'affiche rien (pattern `{description ? ... : null}` du Task 2)
       - Si le fichier utilise déjà `useTranslation('skills')` sans `ns`, adapter : `t(\`descriptions.${selectedSkill.id}\`, { defaultValue: '' })`

    3) Si `t` n'est pas encore destructuré du hook `useTranslation` dans skills.tsx, ajouter sa destructuration près des autres imports de hooks (NE PAS créer un second `useTranslation` — réutiliser l'instance existante). Lire le haut du fichier (lignes 1-70) d'abord pour voir le pattern exact.

    4) Valider : `npx tsc --noEmit` doit passer sans nouvelle erreur sur skills.tsx.

    Pas d'autre modification dans skills.tsx — c'est purement le passage de la prop.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "app/\(tabs\)/skills\.tsx" || echo "OK no new errors in skills.tsx"</automated>
  </verify>
  <done>
    - `<SkillDetailModal>` reçoit la prop `description` dérivée de `t('descriptions.{id}')` du namespace `skills`
    - Fallback `undefined` propre quand la description manque
    - `npx tsc --noEmit` n'ajoute aucune erreur liée à skills.tsx
    - Test manuel : ouvrir l'onglet Compétences → tapper une skill → le modal affiche la description médicale
  </done>
</task>

</tasks>

<verification>
Checks globaux après exécution des 3 tâches :
1. `node -e "const s=require('./locales/fr/skills.json'); const tree=Object.keys(s.tree); const descs=Object.keys(s.descriptions||{}); const missing=tree.filter(k=>!descs.includes(k)); console.log('tree:',tree.length,'descs:',descs.length,'missing:',missing.length)"` — doit afficher `missing: 0`
2. `npx tsc --noEmit` — 0 nouvelle erreur (ignorer erreurs pré-existantes de MemoryEditor.tsx, cooklang.ts, useVault.ts)
3. Test manuel : ouvrir l'onglet Compétences, tapper une skill de chaque tranche d'âge (0-1, 3-5, 12-14, 15+), vérifier que la description médicale s'affiche et que le contenu est scrollable si long.
</verification>

<success_criteria>
- Toutes les ~277 skills ont une description médicale française rédigée avec un ton professionnel citant les référentiels pédiatriques
- Le JSON est valide et exhaustif (zéro clé de `tree` sans description correspondante)
- Le modal affiche la description sous le XP, dans une zone scrollable
- Aucune régression TypeScript (`npx tsc --noEmit` ne génère aucune nouvelle erreur)
- Aucune couleur hardcodée (respect CLAUDE.md : `useThemeColors()` uniquement)
- Aucune modification en dehors des 3 fichiers listés
</success_criteria>

<output>
After completion, create `.planning/quick/260411-te0-ajouter-descriptions-m-dicales-d-veloppe/260411-te0-SUMMARY.md` résumant :
- Nombre exact de descriptions rédigées
- Référentiels cités (OMS, Denver, Brazelton, HAS)
- Statut TypeScript
- Prochaines étapes éventuelles (i.e. EN translation différée)
</output>
