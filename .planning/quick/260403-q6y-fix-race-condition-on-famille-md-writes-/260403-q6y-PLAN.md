---
phase: quick
plan: 260403-q6y
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/famille-queue.ts
  - hooks/useFarm.ts
  - hooks/useVault.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "Toutes les ecritures sur famille.md passent par une seule file d'attente partagee"
    - "useFarm.ts et useVault.ts utilisent le meme enqueueWrite depuis lib/famille-queue.ts"
    - "Les 5 fonctions useVault.ts (updateProfileTheme, updateTreeSpecies, buyMascotItem, placeMascotItem, unplaceMascotItem) ne peuvent plus ecraser les donnees de useFarm.ts"
  artifacts:
    - path: "lib/famille-queue.ts"
      provides: "Module partage enqueueWrite + patchProfileField + patchProfileFields"
      exports: ["enqueueWrite", "patchProfileField", "patchProfileFields"]
    - path: "hooks/useFarm.ts"
      provides: "Import enqueueWrite depuis lib/famille-queue.ts (plus de definition locale)"
    - path: "hooks/useVault.ts"
      provides: "Les 5 fonctions famille.md wrappees dans enqueueWrite"
  key_links:
    - from: "hooks/useFarm.ts"
      to: "lib/famille-queue.ts"
      via: "import { enqueueWrite, patchProfileField, patchProfileFields }"
      pattern: "import.*famille-queue"
    - from: "hooks/useVault.ts"
      to: "lib/famille-queue.ts"
      via: "import { enqueueWrite }"
      pattern: "import.*famille-queue"
---

<objective>
Corriger la race condition sur les ecritures concurrentes de famille.md entre useFarm.ts et useVault.ts.

Purpose: useFarm.ts possede deja une file d'attente (enqueueWrite) pour serialiser ses ecritures sur famille.md, mais useVault.ts fait des read-modify-write bruts sans queue. Quand les deux hooks ecrivent en parallele, useVault.ts peut ecraser des donnees fraichement ecrites par useFarm.ts (ex: niveaux farm_buildings perdus). Extraire la queue dans un module partage resout le probleme.

Output: lib/famille-queue.ts (module partage), useFarm.ts et useVault.ts mis a jour.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@hooks/useFarm.ts (lignes 47-108 — enqueueWrite, patchProfileField, patchProfileFields)
@hooks/useVault.ts (lignes 1203-1535 — les 5 fonctions a wrapper)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extraire enqueueWrite et helpers dans lib/famille-queue.ts</name>
  <files>lib/famille-queue.ts, hooks/useFarm.ts</files>
  <action>
1. Creer `lib/famille-queue.ts` avec :
   - `let _familleWriteQueue: Promise<void> = Promise.resolve();` (variable module-level)
   - `export function enqueueWrite<T = void>(fn: () => Promise<T>): Promise<T>` — meme implementation que useFarm.ts lignes 51-55
   - `export function patchProfileField(lines: string[], profileId: string, fieldKey: string, value: string): void` — copie exacte de useFarm.ts lignes 57-78
   - `export function patchProfileFields(lines: string[], profileId: string, fields: Record<string, string>): void` — copie exacte de useFarm.ts lignes 80-108
   - Ce module est pure TS (pas d'import React, pas d'import vault) — conforme a la convention lib/.

2. Modifier `hooks/useFarm.ts` :
   - Supprimer les definitions locales de `_familleWriteQueue`, `enqueueWrite`, `patchProfileField`, `patchProfileFields` (lignes 48-108)
   - Ajouter `import { enqueueWrite, patchProfileField, patchProfileFields } from '../lib/famille-queue';` en haut du fichier (apres les imports existants)
   - Les callbacks `writeProfileField` et `writeProfileFields` continuent a fonctionner sans changement car ils appellent deja `enqueueWrite` et `patchProfileField`/`patchProfileFields` par nom.
  </action>
  <verify>
    <automated>cd /Users/gabrielwaltio/Documents/family-vault && npx tsc --noEmit 2>&1 | grep -E "famille-queue|useFarm" | head -20; echo "Exit: $?"</automated>
  </verify>
  <done>lib/famille-queue.ts existe avec les 3 exports. useFarm.ts importe depuis lib/famille-queue.ts. Pas de regression tsc sur ces fichiers.</done>
</task>

<task type="auto">
  <name>Task 2: Wrapper les 5 fonctions useVault.ts avec enqueueWrite</name>
  <files>hooks/useVault.ts</files>
  <action>
1. Ajouter `import { enqueueWrite } from '../lib/famille-queue';` en haut de useVault.ts (apres les imports existants).

2. Wrapper les 5 fonctions suivantes — chaque read-modify-write sur FAMILLE_FILE doit etre enveloppe dans `enqueueWrite(async () => { ... })`. La logique interne reste identique, seul l'enveloppement change :

   a) **updateProfileTheme** (~ligne 1203) : le bloc `try { const content = ... await writeFile(...) }` devient `return enqueueWrite(async () => { ... })`. Le `setProfiles()` apres le writeFile reste DANS le enqueueWrite car il depend du contenu ecrit.

   b) **updateTreeSpecies** (~ligne 1243) : meme pattern — wrapper le try/catch entier dans enqueueWrite.

   c) **buyMascotItem** (~ligne 1283) : wrapper uniquement la partie "2. Ajouter l'item dans famille.md" (lignes 1333-1364) dans enqueueWrite. La partie "1. Deduire les points dans gami-{profileId}.md" (lignes 1308-1330) ecrit dans un AUTRE fichier (gami-*.md) et n'a pas besoin de la queue. Concretement : le readFile + boucle for + writeFile de famille.md (lignes 1333-1364) et le setProfiles qui suit doivent etre dans le enqueueWrite. Le setGamiData peut rester hors queue (il ne touche pas famille.md).

   d) **placeMascotItem** (~ligne 1406) : wrapper le bloc entier (read + modification placements + write + setProfiles) dans enqueueWrite.

   e) **unplaceMascotItem** (~ligne 1477) : meme pattern que placeMascotItem.

3. IMPORTANT : ne PAS modifier updateProfile (~ligne 1537), deleteProfile, addProfile, ni les autres fonctions qui ecrivent dans famille.md. Elles seront traitees dans un ticket futur. Le scope est strictement les 5 fonctions listees.

4. Pour buyMascotItem, le pattern est :
```typescript
const buyMascotItem = useCallback(async (...) => {
  // ... validations (pas de file I/O) ...
  try {
    // 1. gami file write (hors queue — fichier different)
    ...
    // 2. famille.md write (DANS la queue)
    await enqueueWrite(async () => {
      const content = await vaultRef.current!.readFile(FAMILLE_FILE);
      const lines = content.split('\n');
      // ... modification lines ...
      const familleStr = lines.join('\n');
      await vaultRef.current!.writeFile(FAMILLE_FILE, familleStr);
      // setProfiles avec familleStr
      setProfiles(prev => { ... });
    });
    // setGamiData (hors queue)
    setGamiData(prev => { ... });
  } catch (e) { ... }
}, [profiles]);
```

Pour les 4 autres fonctions plus simples (updateProfileTheme, updateTreeSpecies, placeMascotItem, unplaceMascotItem), le pattern est :
```typescript
const fn = useCallback(async (...) => {
  if (!vaultRef.current) return;
  return enqueueWrite(async () => {
    // tout le try/catch existant ici
  });
}, [...]);
```
  </action>
  <verify>
    <automated>cd /Users/gabrielwaltio/Documents/family-vault && npx tsc --noEmit 2>&1 | grep -c "error" || echo "0 errors"</automated>
  </verify>
  <done>Les 5 fonctions useVault.ts passent par enqueueWrite. `npx tsc --noEmit` ne montre pas de nouvelles erreurs. Toute ecriture sur famille.md (depuis useFarm ou useVault) transite par la meme queue partagee.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` ne produit pas de nouvelles erreurs
- `grep -n "enqueueWrite" hooks/useVault.ts` montre l'import + 5 usages
- `grep -n "enqueueWrite" hooks/useFarm.ts` montre l'import (plus de definition locale)
- `grep -n "export function" lib/famille-queue.ts` montre enqueueWrite, patchProfileField, patchProfileFields
- `grep -c "_familleWriteQueue" hooks/useFarm.ts` retourne 0 (supprime)
- `grep -c "_familleWriteQueue" lib/famille-queue.ts` retourne 1 (deplace ici)
</verification>

<success_criteria>
- Un seul module (lib/famille-queue.ts) detient la queue d'ecriture famille.md
- useFarm.ts et useVault.ts importent depuis ce module
- Les 5 fonctions specifiees dans useVault.ts sont wrappees dans enqueueWrite
- Aucune regression TypeScript
</success_criteria>

<output>
After completion, create `.planning/quick/260403-q6y-fix-race-condition-on-famille-md-writes-/260403-q6y-SUMMARY.md`
</output>
