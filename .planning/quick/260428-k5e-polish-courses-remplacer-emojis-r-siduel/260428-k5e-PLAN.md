---
phase: quick-260428-k5e
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/(tabs)/meals.tsx
  - components/CourseItemEditor.tsx
  - components/dashboard/DashboardCourses.tsx
  - locales/fr.json
  - locales/en.json
autonomous: true
requirements:
  - QUICK-260428-k5e
must_haves:
  truths:
    - "L'empty state du tab Courses affiche une icône lucide ShoppingCart (plus d'emoji 🛒)"
    - "Le picker de section affiche FolderOpen lucide quand aucune section n'est sélectionnée (plus d'emoji 📂)"
    - "Les checkboxes cochées affichent l'icône Check lucide (plus de caractère ✓)"
    - "Le bouton supprimer course affiche l'icône X lucide (plus de caractère ✕)"
    - "Le DashboardCourses affiche 'à prendre' via la clé i18n t() (plus de hardcoded FR)"
    - "La palette emojis CourseItemEditor reste inchangée (commentaire explicatif ajouté)"
    - "npx tsc --noEmit passe (modulo erreurs pré-existantes documentées)"
  artifacts:
    - path: "app/(tabs)/meals.tsx"
      provides: "Tab Courses avec icônes lucide ShoppingCart/FolderOpen/Check/X"
    - path: "components/CourseItemEditor.tsx"
      provides: "Commentaire expliquant pourquoi les emojis restent dans NEW_CATEGORY_EMOJIS"
    - path: "components/dashboard/DashboardCourses.tsx"
      provides: "Chaîne 'à prendre' i18n via t()"
    - path: "locales/fr.json"
      provides: "Nouvelle clé dashboard.courses.* pour 'à prendre'"
    - path: "locales/en.json"
      provides: "Nouvelle clé dashboard.courses.* (traduction EN)"
  key_links:
    - from: "app/(tabs)/meals.tsx"
      to: "lucide-react-native"
      via: "import des icônes ShoppingCart, FolderOpen, Check, X"
      pattern: "from ['\"]lucide-react-native['\"]"
    - from: "components/dashboard/DashboardCourses.tsx"
      to: "locales/fr.json + locales/en.json"
      via: "t('dashboard.courses.<key>')"
      pattern: "t\\(['\"]dashboard\\.courses\\."
---

<objective>
Polish final du feature Courses : remplacer les emojis résiduels par des icônes lucide-react-native (cohérence avec la migration emoji→lucide déjà appliquée à c9b7439), internationaliser une chaîne FR hardcodée dans DashboardCourses, et documenter pourquoi la palette emojis dans CourseItemEditor reste volontairement inchangée (compatibilité vault Obsidian).

Purpose: Finaliser l'audit UI courses — la migration emoji→lucide doit être complète sur les éléments de chrome (empty state, picker, check, delete) tout en préservant les emojis stockés en données (sections vault).

Output: 4 emojis remplacés par lucide dans meals.tsx, 1 chaîne i18n dans DashboardCourses, 1 commentaire explicatif dans CourseItemEditor, 2 clés ajoutées dans locales fr/en.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@app/(tabs)/meals.tsx
@components/CourseItemEditor.tsx
@components/dashboard/DashboardCourses.tsx
@locales/fr.json
@locales/en.json

<interfaces>
<!-- Icônes lucide à utiliser (déjà disponibles dans le projet via lucide-react-native) -->
- ShoppingCart : icône panier (empty state Courses)
- FolderOpen : icône dossier ouvert (picker fallback)
- Check : icône check (checkbox cochée)
- X : icône X (bouton supprimer)

<!-- Convention thème (CLAUDE.md) -->
- TOUJOURS useThemeColors() — colors.textMuted, colors.textSub, colors.onPrimary
- Jamais de couleur hardcoded (#FFFFFF, etc.)

<!-- Convention i18n -->
- t('dashboard.courses.<key>') — vérifier les clés existantes dans locales/fr.json sous dashboard.courses.*
- Choisir un nom cohérent (ex: dashboard.courses.toTake) avec la convention déjà en place
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remplacer emojis résiduels par icônes lucide dans meals.tsx + commenter palette CourseItemEditor</name>
  <files>app/(tabs)/meals.tsx, components/CourseItemEditor.tsx</files>
  <action>
**Étape 1 — meals.tsx (4 remplacements):**

1. Vérifier les imports lucide en haut du fichier. Ajouter les manquants parmi: `ShoppingCart`, `FolderOpen`, `Check`, `X`. Si certains sont déjà importés (probable pour `X` et/ou `ShoppingCart`), ne pas dupliquer.

2. **Ligne ~1597 (empty state tab Courses):**
   Remplacer `<Text style={styles.emptyEmoji}>🛒</Text>` par:
   ```tsx
   <ShoppingCart size={48} color={colors.textMuted} />
   ```
   Si `styles.emptyEmoji` apportait un margin/spacing utile, conserver le wrapping `<View>` ou ajouter un style inline `{ marginBottom: ... }` cohérent avec le rendu original.

3. **Ligne ~1761 (section picker fallback):**
   Le `<Text>` actuel affiche soit le label de section soit `'📂'`. Restructurer en conditionnel :
   ```tsx
   {selectedSection ? (
     <Text style={styles.sectionPickerText} numberOfLines={1}>
       {selectedSection}
     </Text>
   ) : (
     <FolderOpen size={16} color={colors.textSub} />
   )}
   ```
   Adapter au code réel — préserver toute logique de troncage/style existante côté Text.

4. **Ligne ~1630 (checkbox check):**
   Remplacer `<Text style={styles.checkboxCheck}>✓</Text>` par:
   ```tsx
   <Check size={14} color={colors.onPrimary} strokeWidth={3} />
   ```

5. **Ligne ~1674 (bouton supprimer course):**
   Remplacer `<Text style={styles.courseRemoveText}>✕</Text>` par:
   ```tsx
   <X size={16} color={colors.textMuted} />
   ```

**Étape 2 — CourseItemEditor.tsx (commentaire seulement):**

À la ligne ~39 au-dessus de `NEW_CATEGORY_EMOJIS`, ajouter le commentaire FR :
```ts
// Emojis stockés dans le label de section (vault Obsidian) — pas de migration lucide.
// Les sections existantes sont parsées avec préfixe emoji (ex: '🥬 Légumes'),
// remplacer ici casserait la compatibilité bidirectionnelle markdown.
const NEW_CATEGORY_EMOJIS = ['🏷️', '🛒', '🥬', '🍎', '🥩', '🍞', '🥛', '🧊'];
```

**Contraintes:**
- Conventions CLAUDE.md strictes : `useThemeColors()` pour toutes les couleurs, jamais de hardcoded
- Pas toucher aux styles `emptyEmoji` / `checkboxCheck` / `courseRemoveText` dans `StyleSheet.create` — les laisser ou nettoyer si plus référencés (au choix selon impact tsc)
- Si un style devient orphelin et déclenche un warning TS, le supprimer du StyleSheet
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -v -E "(MemoryEditor|cooklang|useVault)" | grep -E "error TS" | head -20</automated>
  </verify>
  <done>
- meals.tsx : 4 emojis remplacés par icônes lucide avec couleurs via useThemeColors()
- Imports lucide à jour (ShoppingCart, FolderOpen, Check, X présents)
- CourseItemEditor.tsx : commentaire FR ajouté au-dessus de NEW_CATEGORY_EMOJIS, palette inchangée
- npx tsc --noEmit ne signale aucune NOUVELLE erreur (les pré-existantes restent)
  </done>
</task>

<task type="auto">
  <name>Task 2: i18n DashboardCourses + ajout clé locales fr/en</name>
  <files>components/dashboard/DashboardCourses.tsx, locales/fr.json, locales/en.json</files>
  <action>
**Étape 1 — Inspecter locales/fr.json:**

Chercher la section `dashboard.courses.*` existante pour comprendre la convention de nommage (camelCase ou kebab, niveau d'imbrication). Choisir une clé cohérente, par exemple `dashboard.courses.toTake`.

**Étape 2 — DashboardCourses.tsx ligne ~30:**

Localiser la chaîne `{' à prendre'}` hardcodée. Remplacer par :
```tsx
{` ${t('dashboard.courses.toTake')}`}
```
Vérifier que `t` (ou `useTranslation`) est déjà importé/utilisé dans le fichier — sinon, ajouter l'import standard du projet (suivre le pattern d'un autre composant dashboard, ex: DashboardTasks ou DashboardMeals).

**Étape 3 — locales/fr.json:**

Ajouter sous `dashboard.courses` (créer la sous-arbre si absente, sinon insérer dans l'existante) :
```json
"toTake": "à prendre"
```

**Étape 4 — locales/en.json:**

Symétriquement, ajouter :
```json
"toTake": "to buy"
```

**Note:** si la convention de nommage existante diffère (ex: `dashboard.courses.itemsToTake`), utiliser cette convention plutôt que `toTake`. Cohérence > nom suggéré.

**Contraintes:**
- JSON valide (virgules correctes, pas de trailing comma)
- Garder le tri alphabétique des clés si c'est la convention déjà en place dans le fichier
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -v -E "(MemoryEditor|cooklang|useVault)" | grep -E "error TS" | head -10 && node -e "JSON.parse(require('fs').readFileSync('locales/fr.json'));JSON.parse(require('fs').readFileSync('locales/en.json'));console.log('locales OK')"</automated>
  </verify>
  <done>
- DashboardCourses.tsx : `' à prendre'` remplacé par `t('dashboard.courses.<key>')` (ou clé équivalente cohérente)
- locales/fr.json contient la nouvelle clé sous dashboard.courses (FR: "à prendre")
- locales/en.json contient la même clé (EN: "to buy")
- Les deux fichiers JSON sont valides
- npx tsc --noEmit ne signale aucune NOUVELLE erreur
  </done>
</task>

</tasks>

<verification>
**Validation finale:**
1. `npx tsc --noEmit` — aucune erreur nouvelle (modulo pré-existantes documentées CLAUDE.md: MemoryEditor.tsx, cooklang.ts, useVault.ts)
2. Inspection visuelle (à valider par l'utilisateur post-merge):
   - Tab Courses vide : icône panier lucide (plus d'emoji)
   - Picker de section sans sélection : icône dossier lucide
   - Cocher un item : check lucide blanc
   - Bouton supprimer : X lucide
   - DashboardCourses : "X à prendre" rendu via i18n (et "X to buy" si app en EN)
3. Inspection statique:
   - `grep -n "🛒\|📂\|✓\|✕" app/\(tabs\)/meals.tsx` ne retourne aucun résultat dans les 4 zones modifiées
   - `grep -n "à prendre" components/dashboard/DashboardCourses.tsx` ne retourne plus la chaîne hardcodée
   - `grep -n "// Emojis stockés" components/CourseItemEditor.tsx` retourne le commentaire ajouté
</verification>

<success_criteria>
- 4 emojis chrome remplacés par icônes lucide dans meals.tsx
- 1 chaîne FR hardcodée internationalisée dans DashboardCourses
- 1 commentaire explicatif dans CourseItemEditor (palette emojis préservée)
- 2 clés i18n ajoutées (fr + en)
- 0 nouvelle erreur TypeScript
- Couleurs strictement via `useThemeColors()` (CLAUDE.md)
- Commits français
</success_criteria>

<output>
After completion, create `.planning/quick/260428-k5e-polish-courses-remplacer-emojis-r-siduel/260428-k5e-SUMMARY.md`
</output>
