---
phase: quick-260404-j7v
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/mascot/WorldGridView.tsx
  - hooks/useFarm.ts
  - app/(tabs)/tree.tsx
  - components/mascot/BuildingDetailSheet.tsx
autonomous: true
must_haves:
  truths:
    - "L'overlay blockedOverlay est orange (pas rouge)"
    - "Planter sur une parcelle bloquée par clôture cassée lève une erreur"
    - "collectBuilding reçoit damagedBuildings — la production réduite est appliquée"
    - "Taper une parcelle avec herbes déclenche une Alert de réparation"
    - "Taper un bâtiment avec nuisibles déclenche une Alert de réparation"
    - "Le BuildingDetailSheet affiche un bouton Réparer si isDamaged"
    - "checkWear retourne les nouveaux événements → toast affiché pour chacun"
  artifacts:
    - path: "components/mascot/WorldGridView.tsx"
      provides: "Overlay orange, props onRepairWeed/onRepairPest, tap herbes/nuisibles"
    - path: "hooks/useFarm.ts"
      provides: "Blocage plant() sur parcelle bloquée, damagedBuildings passé à collectBuilding"
    - path: "app/(tabs)/tree.tsx"
      provides: "Handlers repair weed/pest/roof, toasts détection usure, Alert réparation"
    - path: "components/mascot/BuildingDetailSheet.tsx"
      provides: "Bouton Réparer le toit conditionnel"
  key_links:
    - from: "tree.tsx checkWear()"
      to: "showToast"
      via: "nouveaux WearEvent[] retournés"
    - from: "WorldGridView onRepairWeed/onRepairPest"
      to: "tree.tsx repairWear()"
      via: "props callbacks"
    - from: "useFarm plant()"
      to: "getActiveWearEffects().blockedPlots"
      via: "check avant plantCrop()"
---

<objective>
Brancher le système d'usure de la ferme sur l'UI — le backend wear-engine.ts est complet
mais les overlays sont morts (rouge au lieu d'orange), plant() n'a aucun blocage,
collectBuilding n'applique pas les malus, et aucun toast/alert n'informe l'utilisateur
quand un événement survient ou quand il peut réparer.

Purpose: Rendre le système d'usure fonctionnel — les alertes, blocages et réparations
doivent être visibles et actionnables pour l'utilisateur.
Output: Overlay orange, blocage plant, damagedBuildings dans collect, toasts détection,
Alert réparation herbes/nuisibles, bouton toit dans BuildingDetailSheet.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

## Interfaces clés

### wear-engine.ts (lib/mascot/wear-engine.ts)
```typescript
export type WearEventType = 'broken_fence' | 'damaged_roof' | 'weeds' | 'pests';
export interface WearEvent { id: string; type: WearEventType; targetId: string; startedAt: string; repairedAt?: string; }
export interface WearEffects { blockedPlots: number[]; damagedBuildings: string[]; weedyPlots: number[]; pestBuildings: string[]; }
export const REPAIR_COSTS: Record<WearEventType, number> = { broken_fence: 15, damaged_roof: 25, weeds: 0, pests: 0 };
export function getActiveWearEffects(events: WearEvent[]): WearEffects
export function repairWearEvent(events, eventId, currentCoins, now?): { events, cost } | null
```

### building-engine.ts — signature collectBuilding
```typescript
export function collectBuilding(
  buildings: PlacedBuilding[],
  inventory: FarmInventory,
  cellId: string,
  now?: Date,
  techBonuses?: TechBonuses,
  wearEffects?: WearEffects,          // <-- 6e param, déjà dans la signature
): { buildings: PlacedBuilding[]; inventory: FarmInventory; collected: number }
```

### useFarm.ts — hooks exposés
```typescript
// getWearEffects : mémoisé, lit depuis profiles[]
const getWearEffects = (profileId: string): WearEffects
// getWearEvents : accès brut aux WearEvent[]
const getWearEvents = (profileId: string): WearEvent[]
// repairWear : déduit les feuilles, écrit vault, rafraîchit
const repairWear = async (profileId: string, eventId: string): Promise<boolean>
// checkWear : génère nouveaux événements, retourne WearEvent[] (les nouveaux)
const checkWear = async (profileId: string): Promise<WearEvent[]>
// plant : lève Error si pas assez feuilles / pas graine rare
const plant = async (profileId: string, plotIndex: number, cropId: string): Promise<void>
```

### WorldGridViewProps (components/mascot/WorldGridView.tsx)
```typescript
interface WorldGridViewProps {
  treeStage: TreeStage; farmCropsCSV: string; ownedBuildings: PlacedBuilding[];
  containerWidth: number; containerHeight: number; techBonuses?: TechBonuses;
  wearEffects?: WearEffects;
  onCropPlotPress?: (cellId: string, crop: PlantedCrop | null) => void;
  onBuildingCellPress?: (cellId: string, building: PlacedBuilding | null) => void;
  // À AJOUTER :
  // onRepairWeed?: (plotIndex: number) => void;
  // onRepairPest?: (cellId: string) => void;
}
```

### BuildingDetailSheetProps (components/mascot/BuildingDetailSheet.tsx)
```typescript
interface BuildingDetailSheetProps {
  visible: boolean; building: PlacedBuilding; coins: number;
  techBonuses?: TechBonuses;
  onCollect: (cellId: string) => void;
  onUpgrade: (cellId: string) => void;
  onClose: () => void;
  // À AJOUTER :
  // isDamaged?: boolean;
  // onRepairRoof?: () => void;
}
```

### Locales wear (déjà présentes dans locales/fr/common.json et locales/en/common.json)
```
farm.wear.brokenFence      "Clôture cassée"
farm.wear.brokenFenceDesc  "Une clôture est cassée ! Cette parcelle est inutilisable."
farm.wear.damagedRoof      "Toit endommagé"
farm.wear.weeds            "Mauvaises herbes"
farm.wear.weedsDesc        "Des mauvaises herbes ont envahi cette parcelle."
farm.wear.pests            "Nuisibles"
farm.wear.repair           "Réparer ({{cost}} 🍃)"
farm.wear.repairFree       "Arracher"
farm.wear.repairPests      "Collecter pour chasser"
farm.wear.repaired         "Réparé !"
farm.wear.notEnoughCoins   "Pas assez de feuilles"
farm.wear.newEvent         "Oh non ! {{description}}"
```

### tree.tsx — état actuel
- ligne 706 : `await checkWear(profile.id)` — retour ignoré (pas de toast)
- ligne 1516 : `wearEffects={profile ? getWearEffects(profile.id) : undefined}` — passé à WorldGridView
- handleCropCellPress ligne ~960 : gère plant() mais pas les herbes/clôtures
- handleBuildingCellPress ligne ~1000 : gère collecte/upgrade mais pas nuisibles/toit
- `getWearEvents` n'est PAS destructuré ligne 256 — ajouter dans le destructuring de useFarm()
</context>

<tasks>

<task type="auto">
  <name>Task 1 : Overlay orange + blocage plant + damagedBuildings dans collect (backend + visuel)</name>
  <files>components/mascot/WorldGridView.tsx, hooks/useFarm.ts</files>
  <action>
**WorldGridView.tsx — 3 changements :**

1. Overlay couleur (lignes 893-896) : changer rouge → orange dans StyleSheet.create
   - `backgroundColor: 'rgba(239, 68, 68, 0.3)'` → `'rgba(255, 165, 0, 0.3)'`
   - `borderColor: 'rgba(239, 68, 68, 0.6)'` → `'rgba(255, 165, 0, 0.6)'`

2. Ajouter props dans WorldGridViewProps :
   ```typescript
   onRepairWeed?: (plotIndex: number) => void;
   onRepairPest?: (cellId: string) => void;
   ```

3. Propager les props dans le composant WorldGridView (destructuring ligne ~553) et les passer vers CropCell et BuildingCell :
   - CropCell reçoit `onRepairWeed?: (plotIndex: number) => void`
   - Dans CropCell, modifier le `onPress` de la TouchableOpacity : si `hasWeeds && !crop && onRepairWeed`, appeler `onRepairWeed(plotIndex)`. Sinon comportement existant (`onPress` habituel).
   - BuildingCell reçoit `onRepairPest?: (cellId: string) => void`
   - Dans BuildingCell, modifier le `onPress` : si `hasPests && onRepairPest`, appeler `onRepairPest(cell.id)`. Sinon comportement existant.

   Pour propager depuis WorldGridView vers CropCell/BuildingCell, passer les props aux appels de CropCell et BuildingCell dans le render (lignes ~597-706). Déstructurer `onRepairWeed, onRepairPest` dans le corps de WorldGridView.

**useFarm.ts — 2 changements :**

1. Dans `plant()` (après la ligne ~230 où on parse les crops) : ajouter le check wear avant `plantCrop()` :
   ```typescript
   // Vérifier blocage usure
   const freshWearEffects = getActiveWearEffects(freshFarm.wearEvents ?? []);
   if (freshWearEffects.blockedPlots.includes(plotIndex)) {
     throw new Error('Cette parcelle est bloquée par une clôture cassée');
   }
   ```
   Importer `getActiveWearEffects` est déjà fait (ligne 48). Importer `parseWearEvents` si pas déjà là pour convertir `freshFarm.wearEvents`.
   Note: `freshFarm.wearEvents` est déjà `WearEvent[]` (parsé par parseFarmProfile ligne 113), pas besoin de re-parser.

2. Dans `collectBuildingResources()` (ligne ~452) : passer `wearEffects` à `collectBuilding` :
   ```typescript
   const wearEffects = getActiveWearEffects(profile.wearEvents ?? []);
   const result = collectBuilding(currentBuildings, currentInventory, cellId, new Date(), profileTechBonuses, wearEffects);
   ```

3. Dans `collectPassiveIncome()` (ligne ~481) : passer `wearEffects` à `collectBuilding` dans la boucle :
   ```typescript
   const passiveWearEffects = getActiveWearEffects(profile.wearEvents ?? []);
   // ...dans la boucle for...
   const result = collectBuilding(currentBuildings, updatedInventory, building.cellId, new Date(), passiveTechBonuses, passiveWearEffects);
   ```
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "(wear|WorldGrid|useFarm|collectBuilding)" | head -20</automated>
  </verify>
  <done>
    - Overlay blockedOverlay orange (pas rouge) dans StyleSheet
    - WorldGridViewProps a onRepairWeed et onRepairPest
    - plant() rejette si blockedPlots.includes(plotIndex)
    - collectBuildingResources et collectPassiveIncome passent wearEffects à collectBuilding
    - tsc --noEmit sans nouvelles erreurs
  </done>
</task>

<task type="auto">
  <name>Task 2 : Toasts détection usure + Alert réparation + bouton toit BuildingDetailSheet</name>
  <files>app/(tabs)/tree.tsx, components/mascot/BuildingDetailSheet.tsx</files>
  <action>
**tree.tsx — 4 changements :**

1. Ajouter `getWearEvents` dans le destructuring de `useFarm()` (ligne ~256) :
   ```typescript
   const { plant, harvest, ..., getWearEffects, getWearEvents } = useFarm();
   ```

2. Après `await checkWear(profile.id)` (ligne ~706), lire les nouveaux events et afficher un toast par type :
   ```typescript
   const newWearEvents = await checkWear(profile.id);
   for (const ev of newWearEvents) {
     if (ev.type === 'broken_fence') showToast(t('farm.wear.brokenFence'), 'warning');
     else if (ev.type === 'damaged_roof') showToast(t('farm.wear.damagedRoof'), 'warning');
     else if (ev.type === 'weeds') showToast(t('farm.wear.weeds'), 'warning');
     else if (ev.type === 'pests') showToast(t('farm.wear.pests'), 'warning');
   }
   ```
   showToast accepte (message: string, type?: 'success'|'error'|'warning'|'info') — vérifier la signature du ToastContext si 'warning' n'existe pas, utiliser 'error' comme fallback.

3. Créer `handleRepairWeed` (pour onRepairWeed de WorldGridView) :
   ```typescript
   const handleRepairWeed = useCallback(async (plotIndex: number) => {
     if (!profile) return;
     const events = getWearEvents(profile.id);
     const event = events.find(e => e.type === 'weeds' && e.targetId === String(plotIndex) && !e.repairedAt);
     if (!event) return;
     const ok = await repairWear(profile.id, event.id);
     if (ok) showToast(t('farm.wear.repaired'));
   }, [profile, getWearEvents, repairWear, showToast, t]);
   ```

4. Créer `handleRepairPest` (pour onRepairPest de WorldGridView) :
   ```typescript
   const handleRepairPest = useCallback(async (cellId: string) => {
     if (!profile) return;
     const events = getWearEvents(profile.id);
     const event = events.find(e => e.type === 'pests' && e.targetId === cellId && !e.repairedAt);
     if (!event) return;
     const ok = await repairWear(profile.id, event.id);
     if (ok) showToast(t('farm.wear.repaired'));
   }, [profile, getWearEvents, repairWear, showToast, t]);
   ```

5. Passer les handlers à WorldGridView (lignes ~1509-1519) :
   ```tsx
   <WorldGridView
     ...
     onRepairWeed={isOwnTree ? handleRepairWeed : undefined}
     onRepairPest={isOwnTree ? handleRepairPest : undefined}
   />
   ```

6. Pour le toit endommagé : dans le handler existant `handleBuildingCellPress` (ou un nouveau handler `handleRepairRoof`), créer la logique de réparation. Trouver où BuildingDetailSheet est rendu dans tree.tsx (chercher `<BuildingDetailSheet`), ajouter les props `isDamaged` et `onRepairRoof` :
   ```typescript
   // Calculer isDamaged pour le bâtiment sélectionné
   const selectedBuildingDamaged = selectedBuildingCellId
     ? (profile ? getWearEffects(profile.id).damagedBuildings.includes(selectedBuildingCellId) : false)
     : false;

   const handleRepairRoof = useCallback(async () => {
     if (!profile || !selectedBuildingCellId) return;
     const events = getWearEvents(profile.id);
     const event = events.find(e => e.type === 'damaged_roof' && e.targetId === selectedBuildingCellId && !e.repairedAt);
     if (!event) return;
     const ok = await repairWear(profile.id, event.id);
     if (ok) showToast(t('farm.wear.repaired'));
   }, [profile, selectedBuildingCellId, getWearEvents, repairWear, showToast, t]);
   ```
   Passer à BuildingDetailSheet : `isDamaged={selectedBuildingDamaged}` et `onRepairRoof={handleRepairRoof}`.

**BuildingDetailSheet.tsx — 2 changements :**

1. Ajouter props dans l'interface :
   ```typescript
   isDamaged?: boolean;
   onRepairRoof?: () => void;
   ```
   Déstructurer dans la fonction.

2. Afficher le bouton Réparer au-dessus du bouton upgrade, conditionnel `isDamaged && onRepairRoof` :
   ```tsx
   {isDamaged && onRepairRoof && (
     <TouchableOpacity
       style={[styles.repairButton, { backgroundColor: colors.warning ?? '#FF9800' }]}
       onPress={() => { Haptics.impactAsync(Haptics.ImpactFeedbackStyle.Medium); onRepairRoof(); }}
     >
       <Text style={styles.repairButtonText}>
         {t('farm.wear.repair', { cost: 25 })}
       </Text>
     </TouchableOpacity>
   )}
   ```
   Ajouter dans StyleSheet.create :
   ```typescript
   repairButton: {
     borderRadius: Radius.md,
     paddingVertical: Spacing.sm,
     paddingHorizontal: Spacing.lg,
     alignItems: 'center',
     marginBottom: Spacing.sm,
   },
   repairButtonText: {
     fontSize: FontSize.body,
     fontWeight: FontWeight.bold,
     color: '#FFFFFF',
   },
   ```
   Note: `colors.warning` peut ne pas exister dans le type — utiliser `(colors as any).warning ?? '#FF9800'` ou simplement `'#FF9800'` comme constante cosmétique (pattern orange non-sémantique, identique aux couleurs dorées des phases précédentes : `[Phase 4]: Couleurs dorées (#FFD700) définies dans StyleSheet comme constantes cosmétiques`).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "(tree|BuildingDetail|wear|repair)" | head -20</automated>
  </verify>
  <done>
    - checkWear retourne WearEvent[] et chaque nouveau event génère un showToast 'warning'
    - handleRepairWeed et handleRepairPest passés à WorldGridView via onRepairWeed/onRepairPest
    - BuildingDetailSheet affiche bouton orange "Réparer (25 🍃)" si isDamaged=true
    - handleRepairRoof calculé depuis selectedBuildingCellId + getWearEvents
    - tsc --noEmit sans nouvelles erreurs sur les fichiers modifiés
  </done>
</task>

</tasks>

<verification>
```bash
npx tsc --noEmit 2>&1 | tail -5
```
Zéro nouvelles erreurs TypeScript (les erreurs pre-existantes dans MemoryEditor.tsx, cooklang.ts, useVault.ts sont ignorées).

Vérification manuelle rapide :
- Ouvrir écran arbre → si parcelle avec `broken_fence` : overlay orange visible (pas rouge)
- Tapper une parcelle avec herbes (weedyPlots) sans culture → handleRepairWeed déclenché → réparation gratuite → toast "Réparé !"
- Tapper un bâtiment avec nuisibles → handleRepairPest → toast "Réparé !"
- Ouvrir BuildingDetailSheet sur bâtiment damaged_roof → bouton orange "Réparer (25 🍃)" visible
</verification>

<success_criteria>
- Overlay blockedOverlay = orange rgba(255,165,0) dans WorldGridView.tsx
- plant() lève `Error('Cette parcelle est bloquée par une clôture cassée')` si plotIndex dans blockedPlots
- collectBuilding appelé avec wearEffects dans collectBuildingResources ET collectPassiveIncome
- WorldGridViewProps expose onRepairWeed et onRepairPest, propagés jusqu'à CropCell et BuildingCell
- checkWear() dans tree.tsx affiche un toast par nouveau WearEvent
- handleRepairWeed/handleRepairPest créés et passés à WorldGridView
- BuildingDetailSheet affiche bouton réparation conditionnel (isDamaged)
- handleRepairRoof créé dans tree.tsx et passé à BuildingDetailSheet
- `npx tsc --noEmit` sans nouvelles erreurs
</success_criteria>

<output>
Après complétion, créer `.planning/quick/260404-j7v-fix-complet-syst-me-usure-ferme-overlay-/260404-j7v-SUMMARY.md`
</output>
