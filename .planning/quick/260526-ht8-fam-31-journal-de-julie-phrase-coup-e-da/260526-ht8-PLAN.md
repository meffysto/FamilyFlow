---
phase: quick-260526-ht8
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["app/(tabs)/journal.tsx"]
autonomous: true
requirements: [FAM-31]

must_haves:
  truths:
    - "Une saisie de symptôme/observation multi-lignes est conservée en entier au save"
    - "Au relire (read-back), aucune partie de la phrase n'est tronquée"
    - "Les entrées sur une seule ligne restent inchangées (non-cassant)"
  artifacts:
    - path: "app/(tabs)/journal.tsx"
      provides: "Normalisation des retours à la ligne dans le rowContent texte (branches edit + add)"
  key_links:
    - from: "confirmModal (branche edit ~L649)"
      to: "ligne markdown `N. ...`"
      via: "rowContent texte normalisé (pas de \\n)"
      pattern: "replace.*\\\\n"
    - from: "confirmModal (branche add ~L668)"
      to: "ligne markdown `N. ...`"
      via: "rowContent texte normalisé (pas de \\n)"
      pattern: "replace.*\\\\n"
---

<objective>
Corriger FAM-31 : dans le Journal de Julie, une phrase de symptôme/observation saisie en
multi-lignes est coupée au relire (retours à la ligne perdus).

Purpose: Le champ `text` des types `SymptomeAdulte`/`ObservationAdulte` est rendu en
`multiline`, donc l'utilisateur peut taper des `\n`. Au save, le contenu est écrit comme
un unique item de liste markdown `${n}. ${rowContent}`. Les `\n` intégrés brisent l'item
en plusieurs lignes brutes. Au read-back, seul la première ligne matche `/^(\d+)\.\s*(.*)/`
— le reste est silencieusement perdu (la « phrase coupée »).

Output: `app/(tabs)/journal.tsx` modifié — les `\n` du contenu texte sont normalisés en
espaces simples avant l'écriture, sur les deux branches (edit ~L649, add ~L668).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md

<interfaces>
<!-- Code existant à modifier, déjà en contexte. Pas d'exploration nécessaire. -->

app/(tabs)/journal.tsx — confirmModal, branche NUMBERED_LIST_TYPES :

Branche EDIT (~L647-649) :
```ts
const rowContent = modal.type === 'DouleurAdulte'
  ? buildRowFromFields(modal.type, modal.fields, now)
  : modal.fields.text?.trim() || '';
```

Branche ADD (~L666-668) :
```ts
const rowContent = modal.type === 'DouleurAdulte'
  ? buildRowFromFields(modal.type, modal.fields, now)
  : modal.fields.text?.trim() || '';
```

Read-back (~L852-859) : chaque ligne de section testée contre `/^(\d+)\.\s*(.*)/`.
DouleurAdulte passe par `buildRowFromFields` (structuré) → non concerné par le bug `\n`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Normaliser les retours à la ligne du contenu texte (branches edit + add)</name>
  <files>app/(tabs)/journal.tsx</files>
  <action>
    Dans `confirmModal`, sur les DEUX branches `NUMBERED_LIST_TYPES` (edit ~L647-649 et
    add ~L666-668), remplacer l'expression `modal.fields.text?.trim() || ''` qui produit le
    `rowContent` pour les types non-`DouleurAdulte` (SymptomeAdulte, ObservationAdulte) par
    une version qui collapse tout run de whitespace contenant un retour à la ligne en un
    espace simple, AVANT le trim.

    Concrètement, pour chacune des deux branches, remplacer :
    ```ts
    : modal.fields.text?.trim() || '';
    ```
    par :
    ```ts
    : (modal.fields.text?.replace(/\s*\n\s*/g, ' ').trim() || '');
    ```
    (FAM-31 — les `\n` intégrés brisent l'item de liste markdown unique et tronquent la
    phrase au read-back ; on les normalise en espace simple.)

    Ajouter un commentaire FR concis au-dessus de chaque branche modifiée, ex :
    `// FAM-31 — normalise les retours à la ligne pour ne pas casser l'item de liste markdown`

    Ne PAS toucher à la branche `DouleurAdulte` (structurée via buildRowFromFields),
    ni au rendu `multiline` (~L1168) : l'utilisateur peut toujours taper en multi-lignes,
    seul le stockage est aplati. Les entrées mono-ligne existantes sont inchangées.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -v "MemoryEditor.tsx\|cooklang.ts\|useVault.ts" | grep "journal.tsx" || echo "OK aucune nouvelle erreur tsc dans journal.tsx"</automated>
  </verify>
  <done>
    Les deux branches (edit + add) appliquent `.replace(/\s*\n\s*/g, ' ').trim()` au
    contenu texte des types non-DouleurAdulte. `npx tsc --noEmit` ne produit aucune
    nouvelle erreur dans journal.tsx (erreurs pré-existantes MemoryEditor/cooklang/useVault
    ignorées). DouleurAdulte et le rendu multiline restent intacts.
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` : aucune nouvelle erreur dans `app/(tabs)/journal.tsx`.
- Revue manuelle : un symptôme saisi sur plusieurs lignes est stocké sur une seule ligne
  markdown `N. phrase complète` et relu en entier (plus de troncature).
</verification>

<success_criteria>
- Saisie multi-lignes d'un SymptomeAdulte/ObservationAdulte conservée intégralement au
  save/read-back.
- Aucune régression sur les entrées mono-ligne ni sur DouleurAdulte.
- `npx tsc --noEmit` propre (hors erreurs pré-existantes connues).
</success_criteria>

<output>
After completion, create `.planning/quick/260526-ht8-fam-31-journal-de-julie-phrase-coup-e-da/260526-ht8-SUMMARY.md`
</output>
