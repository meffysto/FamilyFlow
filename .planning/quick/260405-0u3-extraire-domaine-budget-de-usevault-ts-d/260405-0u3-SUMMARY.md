---
phase: quick-260405-0u3
plan: "01"
subsystem: hooks
tags: [refactoring, budget, useVault, extraction]
dependency_graph:
  requires: []
  provides: [hooks/useVaultBudget.ts]
  affects: [hooks/useVault.ts]
tech_stack:
  added: []
  patterns: [hook-per-domain, spread-delegation]
key_files:
  created:
    - hooks/useVaultBudget.ts
  modified:
    - hooks/useVault.ts
decisions:
  - useVaultBudget reçoit vaultRef directement (pas de vault extrait) — cohérent avec le pattern des autres callbacks internes
  - Spread ...budgetState dans useMemo + destructuration resetBudget — garde resetBudget interne à useVaultInternal()
  - warnUnexpected redéclarée localement dans useVaultBudget — évite import croisé hooks↔hooks
metrics:
  duration: "8min"
  completed: "2026-04-05"
  tasks_completed: 2
  files_changed: 2
---

# Phase quick-260405-0u3: Extraire domaine Budget de useVault.ts

**One-liner:** Extraction de toute la logique Budget (~100 lignes) vers hooks/useVaultBudget.ts avec délégation transparente via spread dans useVaultInternal.

## Tasks Completed

| # | Task | Commit | Files |
|---|------|--------|-------|
| 1 | Créer useVaultBudget.ts avec logique budget extraite | da37300 | hooks/useVaultBudget.ts (créé) |
| 2 | Brancher useVaultBudget dans useVault.ts | 6ffd56b | hooks/useVault.ts (modifié) |

## Decisions Made

- **Pattern de délégation:** `const { resetBudget, ...budgetState } = budget` puis `...budgetState` dans le return useMemo. `resetBudget` reste interne car utilisé uniquement dans `setVaultPath`.
- **Dépendance useMemo:** Remplace 9 refs budget individuelles par un seul objet `budgetState` dans le tableau de deps.
- **warnUnexpected local:** Redéclarée dans useVaultBudget (même implémentation) pour éviter tout couplage inter-hooks.

## Verification

```
grep -c "budget" hooks/useVault.ts   → 10 (réduit de ~50 à 10)
grep "useVaultBudget" hooks/useVault.ts → import + appel confirmés
npx tsc --noEmit → 0 nouvelles erreurs (seules les erreurs docs/ pre-existantes)
```

## Deviations from Plan

None - plan exécuté exactement comme écrit.

## Self-Check: PASSED

- hooks/useVaultBudget.ts: FOUND
- Commit da37300: FOUND
- Commit 6ffd56b: FOUND
- Interface VaultState inchangée (lignes 215-223): VERIFIED
