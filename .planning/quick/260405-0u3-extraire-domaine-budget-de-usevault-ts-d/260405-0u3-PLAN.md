---
phase: quick-260405-0u3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - hooks/useVaultBudget.ts
  - hooks/useVault.ts
autonomous: true
requirements: [QUICK-01]
must_haves:
  truths:
    - "useVault() retourne exactement la meme interface qu'avant — aucun changement visible pour les consommateurs"
    - "Le domaine Budget fonctionne identiquement (loadBudgetData, addExpense, deleteExpense, etc.)"
  artifacts:
    - path: "hooks/useVaultBudget.ts"
      provides: "Hook useVaultBudget extrait — toute la logique Budget isolee"
      exports: ["useVaultBudget"]
    - path: "hooks/useVault.ts"
      provides: "Hook principal allege — importe et spread useVaultBudget"
  key_links:
    - from: "hooks/useVault.ts"
      to: "hooks/useVaultBudget.ts"
      via: "import + appel useVaultBudget(vaultRef) + spread dans useMemo return"
      pattern: "useVaultBudget"
---

<objective>
Extraire le domaine Budget de useVault.ts (3837 lignes) vers un hook dedie useVaultBudget.ts.

Purpose: Reduire la taille du monolithe useVault.ts — premier pas vers un decoupage par domaine. Refactoring interne uniquement, l'interface VaultState ne change pas.
Output: hooks/useVaultBudget.ts contenant toute la logique budget, useVault.ts allege.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@hooks/useVault.ts
@lib/budget.ts
@lib/types.ts
</context>

<tasks>

<task type="auto">
  <name>Tache 1: Creer useVaultBudget.ts avec toute la logique budget extraite</name>
  <files>hooks/useVaultBudget.ts</files>
  <action>
Creer hooks/useVaultBudget.ts qui exporte une fonction useVaultBudget(vaultRef: React.MutableRefObject&lt;VaultManager | null&gt;).

Le hook doit contenir :
- Les constantes: BUDGET_DIR = '05 - Budget', BUDGET_CONFIG_FILE = '05 - Budget/config.md'
- Les 3 useState: budgetEntries (BudgetEntry[]), budgetConfig (BudgetConfig), budgetMonth (string, init format(new Date(), 'yyyy-MM'))
- La fonction resetBudget() qui remet budgetEntries=[] et budgetConfig=DEFAULT_BUDGET_CONFIG (appelee par useVault lors de setVaultPath)
- Les 5 useCallback: loadBudgetData, addExpense, deleteExpense, loadBudgetMonths, updateBudgetConfig — copie exacte du code actuel dans useVault.ts lignes 2707-2800
- Utiliser la meme fonction utilitaire warnUnexpected — la re-declarer localement (meme pattern que isFileNotFound + warnUnexpected, ~5 lignes)

Retourner un objet avec: budgetEntries, budgetConfig, budgetMonth, setBudgetMonth, addExpense, deleteExpense, updateBudgetConfig, loadBudgetData, loadBudgetMonths, resetBudget.

Imports necessaires:
- useState, useCallback de 'react'
- format de 'date-fns'
- BudgetEntry, BudgetConfig de '../lib/types'
- parseBudgetConfig, parseBudgetMonth, serializeBudgetMonth, serializeBudgetConfig, DEFAULT_BUDGET_CONFIG de '../lib/budget'
- VaultManager de '../lib/vault'

Type de retour explicite: exporter l'interface UseVaultBudgetResult avec tous les champs.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -i budget || echo "No budget errors"</automated>
  </verify>
  <done>hooks/useVaultBudget.ts existe avec toute la logique budget, compile sans erreur</done>
</task>

<task type="auto">
  <name>Tache 2: Brancher useVaultBudget dans useVault.ts et supprimer le code duplique</name>
  <files>hooks/useVault.ts</files>
  <action>
Dans hooks/useVault.ts :

1. Ajouter l'import: import { useVaultBudget } from './useVaultBudget';

2. Supprimer les imports budget de '../lib/budget' (parseBudgetConfig, parseBudgetMonth, serializeBudgetMonth, serializeBudgetConfig, DEFAULT_BUDGET_CONFIG) — lignes 84-90.

3. Supprimer les constantes BUDGET_DIR (ligne 423) et BUDGET_CONFIG_FILE (ligne 424).

4. Supprimer les 3 useState budget (lignes 521-523): budgetEntries, budgetConfig, budgetMonth.

5. Dans le corps de useVaultInternal(), ajouter l'appel:
   const budget = useVaultBudget(vaultRef);
   Placer cet appel juste apres les useState existants (vers ligne 530).

6. Supprimer tout le bloc "Budget CRUD" (lignes 2705-2800): loadBudgetData, addExpense, deleteExpense, loadBudgetMonths, updateBudgetConfig.

7. Dans le reset de setVaultPath (lignes 1153-1154), remplacer setBudgetEntries([]) et setBudgetConfig(DEFAULT_BUDGET_CONFIG) par budget.resetBudget().

8. Dans le return useMemo, remplacer les references directes:
   - budgetEntries → budget.budgetEntries
   - budgetConfig → budget.budgetConfig
   - budgetMonth → budget.budgetMonth
   - setBudgetMonth → budget.setBudgetMonth
   - addExpense → budget.addExpense
   - deleteExpense → budget.deleteExpense
   - updateBudgetConfig → budget.updateBudgetConfig
   - loadBudgetData → budget.loadBudgetData
   - loadBudgetMonths → budget.loadBudgetMonths

   OU plus elegant: spread ...budget dans le return (sauf resetBudget qui est interne).
   Pour cela, destructurer: const { resetBudget, ...budgetState } = budget; puis ...budgetState dans le useMemo.

9. Mettre a jour le tableau de dependances du useMemo: remplacer les 9 references budget individuelles par budgetState (ou budget si spread).

10. Supprimer BudgetEntry et BudgetConfig de l'import '../lib/types' SI plus utilises directement dans useVault.ts (verifier — ils sont probablement encore dans l'interface VaultState donc garder).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>useVault.ts compile, ne contient plus aucune logique budget inline, delegue tout a useVaultBudget. L'interface VaultState est inchangee — aucun consommateur n'est impacte.</done>
</task>

</tasks>

<verification>
- npx tsc --noEmit compile sans nouvelles erreurs
- grep -c "budget" hooks/useVault.ts montre une reduction significative (de ~50 occurrences a ~15)
- grep "useVaultBudget" hooks/useVault.ts confirme le branchement
- L'interface VaultState dans useVault.ts contient toujours les memes champs budget (lignes 215-223 inchangees)
</verification>

<success_criteria>
- hooks/useVaultBudget.ts existe et exporte useVaultBudget
- hooks/useVault.ts n'a plus de logique budget inline (constantes, useState, useCallback supprimes)
- L'interface VaultState est strictement identique
- npx tsc --noEmit passe sans nouvelles erreurs
</success_criteria>

<output>
After completion, create `.planning/quick/260405-0u3-extraire-domaine-budget-de-usevault-ts-d/260405-0u3-SUMMARY.md`
</output>
