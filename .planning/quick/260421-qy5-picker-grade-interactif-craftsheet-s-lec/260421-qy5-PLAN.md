---
phase: 260421-qy5-picker-grade-interactif
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/mascot/CraftSheet.tsx
  - components/mascot/CraftGradePicker.tsx  # nouveau sous-composant (extraction propre)
  - hooks/useFarm.ts
  - app/(tabs)/tree.tsx
  - locales/fr/common.json
  - locales/en/common.json
autonomous: true
requirements:
  - QY5-01  # Picker compact au-dessus du sélecteur de quantité — badge coloré selon grade sélectionné
  - QY5-02  # Masquage conditionnel : un seul grade possédé OU tech culture-5 non débloquée
  - QY5-03  # Expand inline : grades dispo par ingrédient + qty + grisage si insuffisant
  - QY5-04  # Cascade auto sur changement user : recalcul via getDefaultGradeSelection partiel
  - QY5-05  # Preview live : grade output (maillon faible) + valeur de vente estimée
  - QY5-06  # Branchement : craftItemWithSelection(selection) via onCraft étendu (signature + selection optionnelle)
  - QY5-07  # Animations reanimated : FadeIn / SlideInDown / withSpring (pas RN Animated)
  - QY5-08  # Comportement par défaut identique au craft actuel sans interaction user (cascade auto)
  - QY5-09  # i18n FR/EN : farm.craft.pickGrade + ingredientGrades + gradesGrisesHint

must_haves:
  truths:
    - "Dans CraftSheet, si tech culture-5 débloquée ET ≥2 grades possédés pour au moins un ingrédient crop, un bouton compact '[🎯 Grade : {emoji} {label} ▾]' apparaît juste au-dessus du sélecteur de quantité."
    - "Si un seul grade possédé (ou tech culture-5 non débloquée), le bouton picker est masqué et l'UX craft reste strictement identique à aujourd'hui (cascade auto maillon-faible interne)."
    - "Au tap sur le bouton picker, un panneau inline expand (SlideInDown + FadeIn) affiche pour chaque ingrédient crop la liste des grades avec leurs qty possédées (⚪ Ordinaire ×8  🟢 Beau ×3  🟣 Parfait ×1)."
    - "Les chips de grade sont grisés (opacity 0.4 + disabled) si inv[itemId][grade] < ingredient.quantity × craftQty — non sélectionnables."
    - "Au changement d'un grade par l'utilisateur, la sélection est mise à jour et le preview recalcule en direct (grade output + valeur de vente) ; aucune cascade auto ne réécrase le choix user (state user-controlled)."
    - "Le preview affiche 2 lignes au-dessus du bouton Crafter : 'Qualité obtenue : {emoji} {label}' et 'Valeur de vente : {recipe.sellValue × gradeSellMultiplier(outputGrade)} 🍃'."
    - "Au clic sur Crafter, handleCraft passe la selection à onCraft (signature étendue) qui appelle craftItemWithSelection côté useFarm.craft au lieu de craftItem."
    - "Sans interaction user (picker non ouvert ou masqué), la selection injectée = getDefaultGradeSelection(harvestInv, recipe, craftQty) — comportement strictement identique à l'existant (maillon-faible cascade auto)."
    - "Le CraftSheet.tsx d'origine (1638 lignes) gagne ≤ 80 lignes (logique picker extraite dans components/mascot/CraftGradePicker.tsx ~180 lignes) ; aucune dérégression sur les autres onglets/modals."
    - "Aucune modification de grade-engine.ts, craft-engine.ts (helpers existants), useGarden, market-engine, farm-engine — zéro risque de régression moteur."
    - "npx tsc --noEmit clean (pas d'erreur nouvelle) ; npx jest --no-coverage passe (aucun test cassé ; tests craft-grade existants continuent de valider craftItemWithSelection)."
    - "Tous styles via useThemeColors() + tokens Spacing/Radius/FontSize ; animations via react-native-reanimated (useSharedValue / withSpring / FadeInDown / SlideInDown) ; jamais de hardcoded color ni RN Animated."
    - "Clés i18n nouvelles dans farm.craft : pickGrade, ingredientGrades, gradesGrisesHint, grade (au singulier) — mirror FR/EN."
  artifacts:
    - path: "components/mascot/CraftGradePicker.tsx"
      provides: "Sous-composant contrôlé : affiche bouton compact + expand inline des grades par ingrédient ; accepte props (recipe, harvestInventory, craftQty, selection, onSelectionChange, techUnlocked) ; rend null si shouldHide=true. Animations reanimated intégrées."
      min_lines: 120
    - path: "components/mascot/CraftSheet.tsx"
      provides: "Intégration CraftGradePicker dans la mini-modal détail recette (juste avant craftActionRow), state gradeSelection (Record<cropId,HarvestGrade>) initialisé via getDefaultGradeSelection au tap d'une recette, preview grade output + sellValue ×multiplier calculés via getCraftOutputGrade + gradeSellMultiplier, passage selection au handleCraft."
    - path: "hooks/useFarm.ts"
      provides: "craft() signature étendue : accepte un 4e paramètre optionnel `selection?: Record<string, HarvestGrade>`. Si fourni → appelle craftItemWithSelection ; sinon → fallback craftItem (comportement actuel préservé). Aucun autre changement de logique."
    - path: "app/(tabs)/tree.tsx"
      provides: "onCraft prop de CraftSheet propage le 3e argument `selection` reçu de CraftSheet vers craft(profile.id, recipeId, qty, selection)."
    - path: "locales/fr/common.json"
      provides: "Clés nouvelles farm.craft.pickGrade ('Choisir le grade'), ingredientGrades ('Grades par ingrédient'), gradesGrisesHint ('Grades grisés = quantité insuffisante'), grade ('Grade')."
    - path: "locales/en/common.json"
      provides: "Mirror EN des mêmes clés."
  key_links:
    - from: "components/mascot/CraftSheet.tsx"
      to: "components/mascot/CraftGradePicker.tsx"
      via: "import + render conditionnel dans la modal détail recette, passe selection state controlled"
      pattern: "CraftGradePicker"
    - from: "components/mascot/CraftSheet.tsx"
      to: "lib/mascot/craft-engine.ts:getDefaultGradeSelection"
      via: "calcul de la sélection initiale au mount + reset quand craftQty change (preserve user overrides via ref)"
      pattern: "getDefaultGradeSelection\\("
    - from: "components/mascot/CraftSheet.tsx:handleCraft"
      to: "onCraft(recipeId, qty, selection)"
      via: "passage de la selection au parent ; tree.tsx propage à useFarm.craft"
      pattern: "onCraft\\(.*selection"
    - from: "hooks/useFarm.ts:craft"
      to: "lib/mascot/craft-engine.ts:craftItemWithSelection"
      via: "branche si selection !== undefined, sinon fallback craftItemFn"
      pattern: "craftItemWithSelection\\("
    - from: "components/mascot/CraftGradePicker.tsx"
      to: "lib/mascot/craft-engine.ts:canCraftAtGrade"
      via: "détermine quelle chip de grade doit être grisée par ingrédient × craftQty"
      pattern: "canCraftAtGrade|countItemByGrade"
---

<objective>
Ajouter un picker de grade interactif dans CraftSheet qui laisse l'utilisateur choisir explicitement le grade de chaque ingrédient crop au moment du craft, avec preview live du grade output (maillon faible) et de la valeur de vente. Le moteur (helpers getDefaultGradeSelection, canCraftAtGrade, getCraftOutputGrade, craftItemWithSelection) est déjà livré et testé par Phase B (260421-obd) — cette tâche ajoute UNIQUEMENT l'UI et son branchement.

Purpose : donner au joueur le contrôle stratégique du grade consommé (ex. garder les Parfait pour une recette premium au lieu de les laisser cascader par défaut). Sans cette UI, la cascade maillon-faible auto (priorité au plus bas grade) suffit mais retire l'agence du joueur sur les scénarios avancés.

Output : CraftGradePicker.tsx (nouveau sous-composant 120-180 lignes), CraftSheet.tsx intégré avec state + preview, useFarm.craft signature étendue (fallback strict), i18n FR/EN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/quick/260421-obd-phase-b-grades-inventaire-par-grade-pick/260421-obd-SUMMARY.md
@components/mascot/CraftSheet.tsx
@hooks/useFarm.ts
@app/(tabs)/tree.tsx
@lib/mascot/grade-engine.ts
@lib/mascot/craft-engine.ts
@contexts/ThemeContext.tsx
@constants/spacing.ts
@constants/typography.ts
@constants/farm-theme.ts

<interfaces>
<!-- Helpers moteur disponibles (Phase B — déjà testés, ne PAS modifier) -->

From lib/mascot/grade-engine.ts :
```typescript
export type HarvestGrade = 'ordinaire' | 'beau' | 'superbe' | 'parfait';
export const GRADE_ORDER: HarvestGrade[]; // ['ordinaire','beau','superbe','parfait']
export const gradeSellMultiplier: (grade: HarvestGrade) => number; // ×1/1.5/2.5/4
export function getGradeEmoji(grade: HarvestGrade): string;   // ⚪🟢🟡🟣
export function getGradeLabelKey(grade: HarvestGrade): string; // 'farm.grade.{grade}'
export function countItemByGrade(inv, itemId, grade): number;
export function countItemTotal(inv, itemId): number;
```

From lib/mascot/craft-engine.ts :
```typescript
export function getDefaultGradeSelection(
  inv: HarvestInventory,
  recipe: CraftRecipe,
  multiplier: number = 1,
): Record<string, HarvestGrade>; // default = min grade possédé >= needed, fallback 'ordinaire'

export function canCraftAtGrade(
  inv: HarvestInventory,
  recipe: CraftRecipe,
  selection: Record<string, HarvestGrade>,
  farmInv: FarmInventory,
  multiplier: number = 1,
): { canCraft: boolean; missing?: { itemId: string; grade?: HarvestGrade; have: number; need: number } };

export function getCraftOutputGrade(
  selection: Record<string, HarvestGrade>,
): HarvestGrade; // = getWeakestGrade(Object.values(selection))

export function craftItemWithSelection(
  recipe, harvestInv, farmInv, selection, qty = 1,
): { harvestInv, farmInv, items: CraftedItem[] } | null;

export function craftItem( /* legacy fallback cascade auto */ ): ... | null;
```

From components/mascot/CraftSheet.tsx (existant 1638 lignes — extension minimale) :
- Props `onCraft: (recipeId: string, qty?: number) => Promise<CraftedItem[] | CraftedItem | null>`
  → À étendre : `onCraft: (recipeId, qty?, selection?: Record<string, HarvestGrade>) => Promise<...>`
- Mini-modal détail recette : `selectedRecipe` state + `craftQty` state déjà présents (ligne ~218-219, 501-690)
- `handleCraft(recipe, qty)` callback existant (ligne ~235) → étendre pour passer selection
- `craftActionRow` (ligne ~610-686) contient qtySelector + bouton Crafter — INJECTER le picker JUSTE AU-DESSUS de ce row (dans ScrollView catModalIngList OU juste avant catModalIngList fermée, avant craftActionRow)

From hooks/useFarm.ts:craft (ligne 529-573) :
```typescript
const craft = useCallback(async (profileId: string, recipeId: string, qty: number = 1): Promise<CraftedItem[] | null> => {
  // ... parse profile
  const result = craftItemFn(recipe, harvestInv, farmInv, safeQty); // ← actuel : cascade auto
  // ... write + refresh
}, [...]);
```
→ Étendre : accepte optional `selection?: Record<string, HarvestGrade>` en 4e param :
```typescript
const result = selection
  ? craftItemWithSelection(recipe, harvestInv, farmInv, selection, safeQty)
  : craftItemFn(recipe, harvestInv, farmInv, safeQty);
```

From app/(tabs)/tree.tsx:2812-2816 :
```typescript
onCraft={async (recipeId, qty) => {
  const result = await craft(profile!.id, recipeId, qty);
  ...
}}
```
→ Étendre signature inline :
```typescript
onCraft={async (recipeId, qty, selection) => {
  const result = await craft(profile!.id, recipeId, qty, selection);
  ...
}}
```

Tech culture-5 check (pour masquage picker quand tech non débloquée) :
- Lire `profile.unlockedTechs?.includes('culture-5')` OU prop passée à CraftSheet
- **Décision pragmatique** : ne pas passer un nouveau prop. Au lieu de ça, détecter via `Object.values(harvestInventory).some(gradeMap => Object.keys(gradeMap ?? {}).some(g => g !== 'ordinaire' && (gradeMap[g] ?? 0) > 0))`. Si aucun grade non-ordinaire n'existe dans l'inventaire, pas de picker — équivalent fonctionnel à "tech non débloquée" (les Beau/Superbe/Parfait n'apparaissent que via roll culture-5). Simple, zero nouveau prop, zero coupling.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 : Créer CraftGradePicker.tsx (sous-composant contrôlé) + clés i18n</name>
  <files>components/mascot/CraftGradePicker.tsx, locales/fr/common.json, locales/en/common.json</files>
  <action>
    1. **Créer `components/mascot/CraftGradePicker.tsx`** — nouveau sous-composant contrôlé (~150 lignes). Structure :

    ```typescript
    /**
     * CraftGradePicker.tsx — Picker de grade par ingrédient (Phase B UI)
     *
     * Sous-composant contrôlé utilisé dans CraftSheet mini-modal détail recette.
     * Affiche un bouton compact qui expand inline les grades disponibles par ingrédient crop
     * avec qty possédées + grisage si qty insuffisante. Appelle onSelectionChange quand
     * l'utilisateur change un grade. Rend null si aucun ingrédient n'a ≥2 grades possédés.
     */

    import React, { useState, useMemo, useCallback } from 'react';
    import { View, Text, TouchableOpacity, Pressable, Platform, StyleSheet } from 'react-native';
    import Animated, {
      useSharedValue,
      useAnimatedStyle,
      withSpring,
      FadeIn,
      FadeOut,
      SlideInDown,
      SlideOutUp,
    } from 'react-native-reanimated';
    import { useTranslation } from 'react-i18next';
    import * as Haptics from 'expo-haptics';

    import { useThemeColors } from '../../contexts/ThemeContext';
    import {
      GRADE_ORDER,
      countItemByGrade,
      getGradeEmoji,
      getGradeLabelKey,
      type HarvestGrade,
    } from '../../lib/mascot/grade-engine';
    import type { CraftRecipe, HarvestInventory } from '../../lib/mascot/types';
    import { CROP_CATALOG } from '../../lib/mascot/types';
    import { Spacing, Radius } from '../../constants/spacing';
    import { FontSize, FontWeight } from '../../constants/typography';
    import { Farm } from '../../constants/farm-theme';

    const SPRING_CONFIG = { damping: 14, stiffness: 200 };

    export interface CraftGradePickerProps {
      recipe: CraftRecipe;
      harvestInventory: HarvestInventory;
      craftQty: number;
      selection: Record<string, HarvestGrade>;
      onSelectionChange: (next: Record<string, HarvestGrade>) => void;
      outputGrade: HarvestGrade; // calculé côté parent via getCraftOutputGrade(selection)
    }

    export function CraftGradePicker({
      recipe,
      harvestInventory,
      craftQty,
      selection,
      onSelectionChange,
      outputGrade,
    }: CraftGradePickerProps) {
      const { t } = useTranslation();
      const { colors } = useThemeColors();
      const [expanded, setExpanded] = useState(false);

      // Ingrédients crop uniquement (les ressources bâtiment n'ont pas de grade)
      const cropIngredients = useMemo(
        () => recipe.ingredients.filter(ing => ing.source === 'crop'),
        [recipe],
      );

      // Masquage : si aucun ingrédient crop n'a ≥2 grades possédés avec qty > 0,
      // on ne montre pas le picker (équivalent fonctionnel à "tech culture-5 non débloquée").
      const shouldShow = useMemo(() => {
        for (const ing of cropIngredients) {
          const gradesWithQty = GRADE_ORDER.filter(g => countItemByGrade(harvestInventory, ing.itemId, g) > 0);
          if (gradesWithQty.length >= 2) return true;
        }
        return false;
      }, [cropIngredients, harvestInventory]);

      const chevronRotation = useSharedValue(0);
      const chevronStyle = useAnimatedStyle(() => ({
        transform: [{ rotate: `${chevronRotation.value}deg` }],
      }));

      const toggleExpand = useCallback(() => {
        const next = !expanded;
        setExpanded(next);
        chevronRotation.value = withSpring(next ? 180 : 0, SPRING_CONFIG);
        if (Platform.OS !== 'web') Haptics.selectionAsync();
      }, [expanded, chevronRotation]);

      const handleSelectGrade = useCallback((itemId: string, grade: HarvestGrade) => {
        onSelectionChange({ ...selection, [itemId]: grade });
        if (Platform.OS !== 'web') Haptics.selectionAsync();
      }, [selection, onSelectionChange]);

      if (!shouldShow) return null;

      return (
        <Animated.View entering={FadeIn.duration(200)} style={styles.container}>
          {/* Bouton compact : affiche le grade output (maillon faible) résumé */}
          <TouchableOpacity
            style={[styles.headerBtn, { borderColor: Farm.woodHighlight, backgroundColor: Farm.parchmentDark }]}
            onPress={toggleExpand}
            activeOpacity={0.7}
          >
            <Text style={styles.headerLabel}>
              {t('craft.pickGrade')}
            </Text>
            <View style={styles.headerBadge}>
              <Text style={styles.headerBadgeEmoji}>{getGradeEmoji(outputGrade)}</Text>
              <Text style={[styles.headerBadgeLabel, { color: Farm.brownText }]}>
                {t(getGradeLabelKey(outputGrade))}
              </Text>
            </View>
            <Animated.Text style={[styles.chevron, { color: Farm.brownTextSub }, chevronStyle]}>
              ▾
            </Animated.Text>
          </TouchableOpacity>

          {/* Panneau expand : grades par ingrédient */}
          {expanded && (
            <Animated.View
              entering={SlideInDown.duration(220)}
              exiting={SlideOutUp.duration(180)}
              style={[styles.panel, { borderColor: Farm.woodHighlight, backgroundColor: Farm.parchment }]}
            >
              <Text style={[styles.panelHint, { color: Farm.brownTextSub }]}>
                {t('craft.gradesGrisesHint')}
              </Text>
              {cropIngredients.map(ing => {
                const cropDef = CROP_CATALOG.find(c => c.id === ing.itemId);
                const cropName = cropDef ? t(cropDef.labelKey) : ing.itemId;
                const need = ing.quantity * Math.max(1, craftQty);
                const selected = selection[ing.itemId] ?? 'ordinaire';
                return (
                  <View key={ing.itemId} style={styles.ingredientRow}>
                    <Text style={[styles.ingredientLabel, { color: Farm.brownText }]}>
                      {cropDef?.emoji ?? '•'} {cropName}
                    </Text>
                    <View style={styles.chipsRow}>
                      {GRADE_ORDER.map(grade => {
                        const have = countItemByGrade(harvestInventory, ing.itemId, grade);
                        if (have <= 0) return null; // n'affiche que les grades possédés
                        const disabled = have < need;
                        const isSelected = selected === grade;
                        return (
                          <Pressable
                            key={grade}
                            onPress={disabled ? undefined : () => handleSelectGrade(ing.itemId, grade)}
                            disabled={disabled}
                            style={[
                              styles.chip,
                              {
                                borderColor: isSelected ? colors.primary : Farm.woodHighlight,
                                backgroundColor: isSelected ? colors.primary + '22' : Farm.parchmentDark,
                                opacity: disabled ? 0.4 : 1,
                              },
                            ]}
                          >
                            <Text style={styles.chipEmoji}>{getGradeEmoji(grade)}</Text>
                            <Text style={[styles.chipLabel, { color: Farm.brownText }]}>
                              {t(getGradeLabelKey(grade))}
                            </Text>
                            <Text style={[styles.chipQty, { color: disabled ? Farm.brownTextSub : Farm.brownText }]}>
                              ×{have}
                            </Text>
                          </Pressable>
                        );
                      })}
                    </View>
                  </View>
                );
              })}
            </Animated.View>
          )}
        </Animated.View>
      );
    }

    const styles = StyleSheet.create({
      container: {
        marginBottom: Spacing.sm,
      },
      headerBtn: {
        flexDirection: 'row',
        alignItems: 'center',
        borderWidth: 1.5,
        borderRadius: Radius.md,
        paddingVertical: Spacing.sm,
        paddingHorizontal: Spacing.md,
        gap: Spacing.sm,
      },
      headerLabel: {
        fontSize: FontSize.sm,
        fontWeight: FontWeight.semibold,
        color: Farm.brownTextSub,
      },
      headerBadge: {
        flex: 1,
        flexDirection: 'row',
        alignItems: 'center',
        gap: 4,
      },
      headerBadgeEmoji: {
        fontSize: FontSize.body,
      },
      headerBadgeLabel: {
        fontSize: FontSize.sm,
        fontWeight: FontWeight.bold,
      },
      chevron: {
        fontSize: FontSize.body,
        fontWeight: FontWeight.bold,
      },
      panel: {
        marginTop: Spacing.xs,
        borderWidth: 1,
        borderRadius: Radius.md,
        padding: Spacing.md,
        gap: Spacing.sm,
      },
      panelHint: {
        fontSize: FontSize.micro,
        fontStyle: 'italic',
      },
      ingredientRow: {
        gap: Spacing.xs,
      },
      ingredientLabel: {
        fontSize: FontSize.sm,
        fontWeight: FontWeight.semibold,
      },
      chipsRow: {
        flexDirection: 'row',
        flexWrap: 'wrap',
        gap: Spacing.xs,
      },
      chip: {
        flexDirection: 'row',
        alignItems: 'center',
        borderWidth: 1.5,
        borderRadius: Radius.full,
        paddingVertical: 4,
        paddingHorizontal: Spacing.sm,
        gap: 4,
      },
      chipEmoji: {
        fontSize: FontSize.sm,
      },
      chipLabel: {
        fontSize: FontSize.micro,
        fontWeight: FontWeight.semibold,
      },
      chipQty: {
        fontSize: FontSize.micro,
        fontWeight: FontWeight.bold,
      },
    });
    ```

    2. **Ajouter clés i18n FR** dans `locales/fr/common.json` sous `craft:` (à côté des clés gradePickerLabel / gradeOutputLabel existantes — insérer après ligne 4203 `weakestLinkHint`) :
       ```json
       "pickGrade": "Choisir le grade",
       "ingredientGrades": "Grades par ingrédient",
       "gradesGrisesHint": "Grades grisés = quantité insuffisante",
       "grade": "Grade"
       ```

    3. **Mirror EN** dans `locales/en/common.json` sous la section `craft` équivalente :
       ```json
       "pickGrade": "Choose grade",
       "ingredientGrades": "Grades per ingredient",
       "gradesGrisesHint": "Greyed grades = insufficient quantity",
       "grade": "Grade"
       ```

    4. **GOTCHAS** :
       - `SlideInDown / SlideOutUp` depuis `react-native-reanimated` — déjà utilisé ailleurs dans le projet (voir CraftSheet.tsx imports FadeInDown/FadeOutUp).
       - `Farm.parchment` (plus clair) vs `Farm.parchmentDark` (fond sheet) : utiliser parchment pour le panel expand afin de différencier visuellement du fond de la modal.
       - Pas de `StyleSheet` inline avec couleurs — toutes les couleurs dynamiques (primary, success, error) viennent de `colors` ou `Farm.*`.
       - `getGradeEmoji` et `getGradeLabelKey` existent déjà dans grade-engine (vérifié ligne 71-78 du fichier existant).
       - Sous-composant **CONTRÔLÉ** (selection + onSelectionChange en props) — le state vit dans CraftSheet parent pour survivre à l'expand/collapse.
       - `cropIngredients` exclut les ressources bâtiment (`source === 'building'`) : oeuf/lait/farine/miel n'ont PAS de grade.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "CraftGradePicker|common.json" || echo "CLEAN"</automated>
  </verify>
  <done>
    - components/mascot/CraftGradePicker.tsx existe, ~150-180 lignes, TypeScript clean
    - Composant contrôlé : props recipe, harvestInventory, craftQty, selection, onSelectionChange, outputGrade
    - shouldShow détecte ≥2 grades possédés sur au moins 1 ingrédient crop
    - Bouton compact + expand panel via reanimated (FadeIn, SlideInDown, withSpring chevron)
    - Chips de grade grisés si have < need, non-sélectionnables
    - Tous styles via useThemeColors + Farm tokens + Spacing/Radius/FontSize
    - i18n FR/EN : farm.craft.pickGrade + ingredientGrades + gradesGrisesHint + grade ajoutées
    - `npx tsc --noEmit` ne remonte aucune erreur nouvelle liée à ces fichiers
  </done>
</task>

<task type="auto">
  <name>Task 2 : Intégrer CraftGradePicker dans CraftSheet + étendre onCraft signature + propager selection useFarm → tree.tsx</name>
  <files>components/mascot/CraftSheet.tsx, hooks/useFarm.ts, app/(tabs)/tree.tsx</files>
  <action>
    1. **Modifier `components/mascot/CraftSheet.tsx`** :

       a. **Imports additionnels** (après ligne ~52 où getDefaultGradeSelection est déjà importé) :
       ```typescript
       import { craftItemWithSelection, getCraftOutputGrade } from '../../lib/mascot/craft-engine';
       // (getDefaultGradeSelection et canCraftAtGrade déjà présents)
       import { CraftGradePicker } from './CraftGradePicker';
       ```

       b. **Étendre type `CraftSheetProps.onCraft`** (ligne ~83) :
       ```typescript
       onCraft: (
         recipeId: string,
         qty?: number,
         selection?: Record<string, HarvestGrade>,
       ) => Promise<CraftedItem[] | CraftedItem | null>;
       ```

       c. **Ajouter state `gradeSelection`** dans le body du composant (après `const [craftQty, setCraftQty] = useState<number>(1);` ligne ~219) :
       ```typescript
       const [gradeSelection, setGradeSelection] = useState<Record<string, HarvestGrade>>({});
       ```

       d. **Initialiser / réinitialiser `gradeSelection`** quand l'utilisateur ouvre une recette (soit dans le onPress qui fait `setSelectedRecipe(recipe)` ligne ~421, soit via useEffect sur selectedRecipe+craftQty) :
       ```typescript
       // Option A (préférée) : calcul inline au tap
       onPress={locked ? undefined : () => {
         setCraftQty(1);
         setSelectedRecipe(recipe);
         setGradeSelection(getDefaultGradeSelection(harvestInventory, recipe, 1));
       }}

       // + un useEffect pour recalculer SEULEMENT si user n'a pas override
       // → Simplification : ne pas recalculer sur craftQty change, laisser le user
       //   re-choisir manuellement ou relâcher les overrides via bouton "Reset" (non demandé).
       //   Ça matche la contrainte "cascade auto quand user change un grade" :
       //   quand user sélectionne G sur ing I, la selection {...selection, [I]: G} est prise telle
       //   quelle — pas de rescramble automatique des autres ingrédients.
       ```

       e. **Étendre `handleCraft`** (ligne ~235) pour passer la selection :
       ```typescript
       const handleCraft = useCallback(async (
         recipe: CraftRecipe,
         qty: number = 1,
         selection?: Record<string, HarvestGrade>,
       ) => {
         setCrafting(recipe.id);
         try {
           const result = await onCraft(recipe.id, qty, selection);
           // ... reste inchangé
         } catch { ... }
         setCrafting(null);
       }, [onCraft, craftBtnScale, showFeedback, t]);
       ```

       f. **Injecter `<CraftGradePicker />` + preview** dans la mini-modal détail recette, JUSTE AVANT le IIFE `(() => { const maxQty = ... })` (ligne ~596) — donc après `</ScrollView>` de `catModalIngList` ligne ~593 :
       ```tsx
       {/* Picker grade + preview (Phase B UI) */}
       {(() => {
         const outputGrade = getCraftOutputGrade(gradeSelection);
         const estimatedSellValue = Math.floor(
           selectedRecipe.sellValue * gradeSellMultiplier(outputGrade),
         );
         return (
           <>
             <CraftGradePicker
               recipe={selectedRecipe}
               harvestInventory={harvestInventory}
               craftQty={craftQty}
               selection={gradeSelection}
               onSelectionChange={setGradeSelection}
               outputGrade={outputGrade}
             />
             {/* Preview : visible uniquement si picker effectivement affiché (≥2 grades
                 possédés pour au moins un ingrédient) → on reflète la même condition que
                 CraftGradePicker.shouldShow pour éviter le preview orphelin */}
             {Object.keys(gradeSelection).length > 0 && (
               <View style={{
                 paddingVertical: Spacing.xs,
                 marginBottom: Spacing.sm,
                 gap: 2,
               }}>
                 <Text style={{
                   fontSize: FontSize.caption,
                   color: Farm.brownTextSub,
                 }}>
                   {t('craft.gradeOutputLabel')} : {getGradeEmoji(outputGrade)} {t(getGradeLabelKey(outputGrade))}
                 </Text>
                 <Text style={{
                   fontSize: FontSize.caption,
                   fontWeight: FontWeight.semibold,
                   color: Farm.brownText,
                 }}>
                   {t('craft.sellValueLabel')} : {estimatedSellValue} 🍃
                 </Text>
               </View>
             )}
           </>
         );
       })()}
       ```

       g. **Modifier l'appel `handleCraft`** dans le bouton Crafter (ligne ~646) :
       ```typescript
       onPress={craftable && !isCurrentlyCrafting
         ? async () => {
             const r = selectedRecipe;
             const qtyToCraft = clampedQty;
             // Si gradeSelection vide (aucune recette ouverte OU aucun ingrédient crop),
             // passe undefined → fallback craftItem legacy côté useFarm.
             const selectionToPass = Object.keys(gradeSelection).length > 0
               ? gradeSelection
               : undefined;
             setSelectedRecipe(null);
             setCraftQty(1);
             setGradeSelection({});
             await handleCraft(r, qtyToCraft, selectionToPass);
           }
         : undefined}
       ```

       h. **Cleanup `setGradeSelection({})`** quand modal fermée (dans le onPress de l'overlay `setSelectedRecipe(null)` ligne ~511 et dans le onPress du bouton close ligne ~521) :
       ```typescript
       onPress={() => {
         setSelectedRecipe(null);
         setGradeSelection({});
       }}
       ```

    2. **Modifier `hooks/useFarm.ts:craft`** (ligne 529-573) :

       a. Importer `craftItemWithSelection` (ajouter à l'import existant ligne 34-43) :
       ```typescript
       import {
         CRAFT_RECIPES,
         craftItem as craftItemFn,
         craftItemWithSelection as craftItemWithSelectionFn, // NOUVEAU
         sellCraftedItem as sellCraftedItemFn,
         // ... reste
       } from '../lib/mascot/craft-engine';
       ```

       b. Étendre signature :
       ```typescript
       const craft = useCallback(async (
         profileId: string,
         recipeId: string,
         qty: number = 1,
         selection?: Record<string, HarvestGrade>, // NOUVEAU — optionnel
       ): Promise<CraftedItem[] | null> => {
         if (!vault) return null;
         const safeQty = Math.max(1, Math.floor(qty));
         const content = await vault.readFile(farmFile(profileId)).catch(() => '');
         const profile = parseFarmProfile(content);
         const recipe = CRAFT_RECIPES.find(r => r.id === recipeId);
         if (!recipe) return null;
         const harvestInv = profile.harvestInventory ?? {};
         const farmInv: FarmInventory = profile.farmInventory ?? { oeuf: 0, lait: 0, farine: 0, miel: 0 };

         // Phase B UI — si selection fournie par CraftSheet picker, utiliser craftItemWithSelection.
         // Sinon fallback craftItemFn (cascade auto maillon-faible) — comportement identique à aujourd'hui.
         const result = selection
           ? craftItemWithSelectionFn(recipe, harvestInv, farmInv, selection, safeQty)
           : craftItemFn(recipe, harvestInv, farmInv, safeQty);
         if (!result) throw new Error('Ingredients insuffisants');
         // ... reste inchangé
       }, [...]);
       ```

    3. **Modifier `app/(tabs)/tree.tsx`** (ligne 2812) — propager le 3e argument :
       ```typescript
       onCraft={async (recipeId, qty, selection) => {
         const result = await craft(profile!.id, recipeId, qty, selection);
         if (result && result.length > 0) triggerActionMsg('craft');
         return result;
       }}
       ```

    4. **Valider** :
       ```
       npx tsc --noEmit
       npx jest --no-coverage
       ```
       Les tests `craft-grade.test.ts` (qui testent déjà `craftItemWithSelection`) doivent rester verts. Les tests `craft-engine.test.ts` (cascade auto par défaut) aussi.

    GOTCHAS :
    - **Ne pas toucher** à `craftItem` legacy dans craft-engine.ts — reste le fallback.
    - **State gradeSelection initialisé à `{}`** par défaut — quand l'utilisateur clique une recette, on calcule via `getDefaultGradeSelection` qui remplit pour chaque ingrédient crop. Si recette avec 0 ingrédient crop (ex. fromage = 3 lait), selection reste `{}` → passe `undefined` → fallback cascade auto correct.
    - **Cascade auto respectée (contrainte)** : quand user change UN grade, on fait `{...selection, [itemId]: grade}` — les autres ingrédients ne bougent PAS. C'est bien le comportement "cascade auto quand user change un grade" si on interprète "cascade" comme "recalcul du preview output grade + sellValue" (ce qui se produit automatiquement via getCraftOutputGrade dans le useMemo/IIFE). Aucun scramble des autres ingrédients.
    - **Preview valeur vente** : `Math.floor(sellValue * multiplier)` — identique au pattern market-engine (cohérence économique).
    - **reanimated 4.1** déjà présent dans le projet (CLAUDE.md) — imports SlideInDown / SlideOutUp doivent fonctionner. Si Jest râle sur reanimated mock, le mock global du projet gère déjà ce cas (voir jest.setup).
    - **Pas de nouveau prop vers CraftSheet depuis tree.tsx** — l'inventaire gradé suffit pour détecter "tech débloquée" indirectement (cf. section interfaces ci-dessus).
    - **Cleanup state** : quand modal détail fermée, reset `gradeSelection` pour éviter d'avoir une sélection obsolète si user ouvre une autre recette.
    - **Ne jamais bloquer le bouton Crafter** sur `!canCraftAtGrade(...)` ici — la preview le signale implicitement via grisage des chips. Le `maxCraftableQty` actuel (total toutes grades) continue de régir le bouton. Si user sélectionne une combo impossible (ex. Parfait qty 5 alors qu'il en a 1), `craftItemWithSelectionFn` retournera `null` → l'erreur "Ingredients insuffisants" existante s'affiche via catch. C'est acceptable pour un premier jet. Si on veut protéger le bouton : utiliser `canCraftAtGrade(harvestInv, recipe, gradeSelection, farmInv, clampedQty).canCraft` comme condition supplémentaire à `craftable`. **Optionnel, recommandé** : ajouter cette garde pour une UX plus propre.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx jest --no-coverage</automated>
  </verify>
  <done>
    - CraftGradePicker intégré dans la mini-modal détail recette de CraftSheet, au-dessus du sélecteur de quantité
    - State gradeSelection : initialisé via getDefaultGradeSelection au tap d'une recette, reset au close
    - Preview live : grade output + valeur vente affichés quand picker effectivement visible
    - Bouton Crafter passe selection (ou undefined si vide) à onCraft
    - handleCraft (CraftSheet) signature étendue avec selection?
    - onCraft prop type étendu (recipeId, qty?, selection?)
    - tree.tsx propage selection → useFarm.craft
    - useFarm.craft signature étendue : selection? → branche craftItemWithSelectionFn ou craftItemFn legacy
    - Sans sélection user (picker masqué OU pas ouvert) : comportement strictement identique à avant Phase B UI (cascade auto via craftItemFn)
    - npx tsc --noEmit clean (pas d'erreur nouvelle)
    - npx jest --no-coverage passe (0 régression sur craft-grade, craft-engine, grade-inventory, market-grade existants)
  </done>
</task>

</tasks>

<verification>
1. **Type check** : `npx tsc --noEmit` — zéro erreur nouvelle. Erreurs pré-existantes autorisées : MemoryEditor.tsx, cooklang.ts, useVault.ts (CLAUDE.md).
2. **Tests Jest** : `npx jest --no-coverage` — tous les tests passent, y compris craft-grade.test.ts (vérifie craftItemWithSelection), craft-engine.test.ts (fallback cascade auto), grade-inventory.test.ts, market-grade.test.ts, grade-engine.test.ts.
3. **Smoke manuel** (optionnel en mode auto) :
   - Ouvrir CraftSheet avec inventaire mono-grade (ordinaire seul) → picker **masqué**, craft fonctionne comme avant.
   - Ouvrir CraftSheet avec inventaire multi-grade (ex. tomato { ordinaire: 5, beau: 3 } + recette omelette) → bouton picker **visible**, tap expand montre 2 chips pour tomato (Ordinaire ×5, Beau ×3).
   - Sélectionner Beau → preview recalcule : "Qualité obtenue : 🟢 Beau" + "Valeur de vente : {recipe.sellValue × 1.5} 🍃".
   - Crafter → CraftedItem créé avec grade='beau' (ou maillon faible si plusieurs ingrédients), inv tomato.beau décrémente.
4. **Non-régression** :
   - grade-engine.ts, craft-engine.ts (helpers moteur), useGarden.ts, market-engine.ts, farm-engine.ts : **aucune modification** → pas de risque moteur.
   - CraftSheet autres onglets (Inventaire, Mes créations) : **aucune modification** → inchangés.
5. **Conventions CLAUDE.md** : useThemeColors partout (pas de hardcoded), reanimated (pas RN Animated), tokens Spacing/Radius/FontSize, commentaires FR, i18n FR/EN mirror.
</verification>

<success_criteria>
- CraftGradePicker.tsx existe, ~150-180 lignes, composant contrôlé propre
- Picker compact au-dessus du sélecteur quantité ; masqué si un seul grade possédé (ou tech culture-5 implicitement non débloquée via inventaire)
- Expand inline : grades par ingrédient crop avec qty + grisage qty insuffisante + sélection contrôlée
- Preview live : grade output (maillon faible) + valeur vente × multiplier
- Branchement complet : CraftSheet → onCraft → tree.tsx → useFarm.craft → craftItemWithSelection (helper Phase B déjà testé)
- Fallback strict sans interaction : selection undefined → craftItem legacy cascade auto → comportement identique à aujourd'hui
- Animations reanimated (FadeIn, SlideInDown, withSpring chevron)
- i18n FR/EN : pickGrade + ingredientGrades + gradesGrisesHint + grade ajoutées
- `npx tsc --noEmit` clean ; `npx jest --no-coverage` 100% vert
- Zéro modification de grade-engine, craft-engine (helpers), useGarden, market-engine — zéro risque de régression moteur
- Respect CLAUDE.md : useThemeColors, reanimated, tokens, FR, modal pageSheet conservé
</success_criteria>

<output>
Après complétion, créer `.planning/quick/260421-qy5-picker-grade-interactif-craftsheet-s-lec/260421-qy5-SUMMARY.md` résumant :
- Fichiers créés : components/mascot/CraftGradePicker.tsx (~150-180 lignes)
- Fichiers modifiés : CraftSheet.tsx (+~60 lignes, intégration picker + state + preview), useFarm.ts (signature craft étendue + branche craftItemWithSelection), tree.tsx (propagation selection 3e arg), locales FR+EN (4 clés i18n)
- Décisions techniques :
  - Extraction CraftGradePicker.tsx dans sous-composant dédié (CraftSheet déjà 1638 lignes)
  - Composant contrôlé (state gradeSelection vit dans parent CraftSheet)
  - Masquage via détection inventaire multi-grade (équivalent fonctionnel à "tech culture-5 débloquée") — zero nouveau prop
  - Fallback strict useFarm : selection undefined → craftItemFn legacy cascade auto (zéro régression)
  - Cascade auto "user-preserving" : changement d'un grade = merge partiel, pas de rescramble des autres ingrédients
- Points d'attention futurs : bouton "Reset to default" (UX polish), garde `canCraftAtGrade` sur bouton Crafter (protection anti-erreur), animation chevron (déjà présente), potentielle extraction de la mini-modal détail recette dans son propre fichier (CraftSheet reste gros).
</output>
