---
phase: quick
plan: 260404-l62
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/vault.ts
  - hooks/useVault.ts
  - components/TaskCard.tsx
  - components/dashboard/types.ts
  - components/dashboard/DashboardMenage.tsx
  - components/dashboard/DashboardOverdue.tsx
  - components/dashboard/DashboardVacation.tsx
  - app/(tabs)/tasks.tsx
  - app/(tabs)/index.tsx
  - locales/fr/common.json
  - locales/en/common.json
autonomous: true
requirements: []
must_haves:
  truths:
    - "Tache recurrente avec date affiche un bouton skip qui avance la date sans donner de points"
    - "Tache non-recurrente avec date affiche un bouton skip qui repousse au lendemain"
    - "Tache sans date n'affiche pas de bouton skip"
    - "Le skip ne declenche aucune gamification (pas de completeTask, pas de points, pas de loot)"
  artifacts:
    - path: "lib/vault.ts"
      provides: "skipTask method on VaultManager"
      contains: "skipTask"
    - path: "hooks/useVault.ts"
      provides: "skipTask callback with optimistic state update"
      contains: "skipTask"
    - path: "components/TaskCard.tsx"
      provides: "Skip button conditionnel dans la carte"
      contains: "onSkip"
  key_links:
    - from: "components/TaskCard.tsx"
      to: "hooks/useVault.ts"
      via: "onSkip prop -> skipTask callback"
      pattern: "onSkip.*task"
    - from: "hooks/useVault.ts"
      to: "lib/vault.ts"
      via: "vaultRef.current.skipTask"
      pattern: "skipTask"
---

<objective>
Ajouter un bouton "skip" sur les TaskCard pour reporter une tache sans impact gamification.

Purpose: Permettre de passer une tache recurrente (ex: nettoyer chaise quand enfant absent) sans perdre la recurrence ni gagner de points.
Output: Bouton skip visible sur les taches avec date, fonctionnel dans tasks.tsx et les 3 dashboard sections.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@lib/vault.ts (toggleTask method lines 295-338 — pattern a suivre pour skipTask)
@hooks/useVault.ts (VaultState interface line 169, toggleTask callback lines 1826-1850, exports lines 3637+3746)
@components/TaskCard.tsx (full file — ajouter onSkip prop + bouton)
@components/dashboard/types.ts (DashboardSectionWithTaskToggleProps — ajouter handleTaskSkip)
@app/(tabs)/tasks.tsx (handleTaskToggle lines 290-333 — creer handleTaskSkip similaire sans gamification)
@app/(tabs)/index.tsx (sectionPropsWithToggle line 751 — ajouter skip)
@lib/recurrence.ts (nextOccurrence — reutiliser pour recurring skip)

<interfaces>
From lib/vault.ts:
```typescript
import { format, addDays, parseISO } from 'date-fns';
import { nextOccurrence } from './recurrence';
// toggleTask pattern at line 295 — skipTask follows same enqueueWrite + line-editing pattern
```

From hooks/useVault.ts (VaultState interface ~line 169):
```typescript
toggleTask: (task: Task, completed: boolean) => Promise<void>;
// skipTask goes right after this line
```

From components/TaskCard.tsx (props ~line 29):
```typescript
interface TaskCardProps {
  task: Task;
  onToggle: (task: Task, completed: boolean) => void;
  onLongPress?: () => void;
  showSource?: boolean;
  hideSection?: boolean;
  compact?: boolean;
  profiles?: Profile[];
  pointsOnComplete?: number;
}
```

From components/dashboard/types.ts:
```typescript
export interface DashboardSectionWithTaskToggleProps extends DashboardSectionProps {
  handleTaskToggle: (task: Task, completed: boolean) => void;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend — VaultManager.skipTask + useVault.skipTask + traductions</name>
  <files>lib/vault.ts, hooks/useVault.ts, locales/fr/common.json, locales/en/common.json</files>
  <action>
1. **lib/vault.ts** — Ajouter methode `skipTask(relativePath: string, lineIndex: number)` juste apres `toggleTask` (ligne 338). Logique dans `enqueueWrite`:
   - Lire le contenu, splitter en lignes
   - Detecter recurrence: `line.match(/🔁\s*(every\s+(?:\d+\s+)?(?:day|week|month)s?)/)`
   - Detecter dueDate: `line.match(/📅\s*(\d{4}-\d{2}-\d{2})/)`
   - Si recurrence + dueDate: avancer date via `nextOccurrence(currentDate, recurrence)`, remplacer dans la ligne. Garder `- [ ]`, retirer `✅` si present. (Meme logique que toggleTask recurring)
   - Si dueDate sans recurrence: remplacer la date par `format(addDays(parseISO(currentDate), 1), 'yyyy-MM-dd')` (demain)
   - Si pas de dueDate: no-op (return early)
   - Ecrire les lignes modifiees via `_writeFileDirect`

2. **hooks/useVault.ts** — Trois modifications:
   a. VaultState interface (~ligne 169): ajouter `skipTask: (task: Task) => Promise<void>;` apres `toggleTask`
   b. Callback (~apres ligne 1850): creer `skipTask` useCallback. Appeler `vaultRef.current.skipTask(task.sourceFile, task.lineIndex)`. Optimistic update identique au pattern toggleTask recurring:
      - Si `task.recurrence && task.dueDate`: `newDate = nextOccurrence(t.dueDate, t.recurrence)`, retourner `{ ...t, dueDate: newDate, completed: false }`
      - Si `task.dueDate` sans recurrence: `newDate = format(addDays(parseISO(t.dueDate), 1), 'yyyy-MM-dd')`, retourner `{ ...t, dueDate: newDate }`
      - Appliquer sur `setTasks` et `setVacationTasks`
      - `setTimeout(triggerWidgetRefresh, 0)`
   c. Ajouter `skipTask` aux deux lignes d'exports (~3637 et ~3746). Importer `addDays, parseISO` de date-fns (ajouter aux imports existants ligne 102)

3. **Traductions** — Ajouter dans la section `taskCard` des deux fichiers:
   - FR: `"skip": "Passer"`, `"skipped": "Tache reportee"`, `"skippedTomorrow": "Reportee a demain"`
   - EN: `"skip": "Skip"`, `"skipped": "Task skipped"`, `"skippedTomorrow": "Postponed to tomorrow"`
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>skipTask disponible dans VaultState, ecrit correctement dans le vault, traductions presentes en FR et EN</done>
</task>

<task type="auto">
  <name>Task 2: UI — Bouton skip sur TaskCard + propagation dans tasks.tsx et dashboard</name>
  <files>components/TaskCard.tsx, components/dashboard/types.ts, components/dashboard/DashboardMenage.tsx, components/dashboard/DashboardOverdue.tsx, components/dashboard/DashboardVacation.tsx, app/(tabs)/tasks.tsx, app/(tabs)/index.tsx</files>
  <action>
1. **components/TaskCard.tsx**:
   a. Ajouter `onSkip?: (task: Task) => void;` dans TaskCardProps
   b. Destructurer `onSkip` dans le composant
   c. Creer `handleSkip` callback: `Haptics.selectionAsync()` + `onSkip(task)`
   d. Condition d'affichage: `const showSkip = !!onSkip && !!task.dueDate && !task.completed`
   e. Ajouter le bouton skip dans la zone `meta` (View style={styles.meta}), AVANT les badges date/recurrence. Rendu:
      ```
      {showSkip && (
        <TouchableOpacity
          onPress={handleSkip}
          style={[styles.skipButton, { backgroundColor: colors.cardAlt }]}
          hitSlop={{ top: 6, bottom: 6, left: 6, right: 6 }}
          accessibilityLabel={t('taskCard.skip')}
          accessibilityRole="button"
        >
          <Text style={[styles.skipButtonText, { color: colors.textMuted }]}>⏭️ {t('taskCard.skip')}</Text>
        </TouchableOpacity>
      )}
      ```
   f. Ajouter styles: `skipButton: { paddingHorizontal: Spacing.sm, paddingVertical: Spacing.xxs, borderRadius: Radius.sm }`, `skipButtonText: { fontSize: FontSize.xs }`

2. **components/dashboard/types.ts**: Ajouter `handleTaskSkip?: (task: Task) => void;` dans `DashboardSectionWithTaskToggleProps`

3. **DashboardMenage.tsx, DashboardOverdue.tsx, DashboardVacation.tsx**: Destructurer `handleTaskSkip` des props, passer `onSkip={handleTaskSkip}` sur chaque `<TaskCard>`

4. **app/(tabs)/tasks.tsx**:
   a. Destructurer `skipTask` depuis `useVault()` (a cote de `toggleTask`)
   b. Creer `handleTaskSkip` callback (~apres handleTaskToggle):
      ```typescript
      const handleTaskSkip = useCallback(async (task: Task) => {
        try {
          await skipTask(task);
          const msg = task.recurrence ? t('taskCard.skipped') : t('taskCard.skippedTomorrow');
          showToast(`⏭️ ${msg}`);
        } catch (e) {
          showToast(String(e), 'error');
        }
      }, [skipTask, showToast, t]);
      ```
   c. Passer `onSkip={handleTaskSkip}` sur les deux `<TaskCard>` (lignes 609 et 711)

5. **app/(tabs)/index.tsx**:
   a. Destructurer `skipTask` depuis `useVault()`
   b. Creer `handleTaskSkip` callback identique a tasks.tsx (sans gamification)
   c. Ajouter `handleTaskSkip` dans `sectionPropsWithToggle` useMemo (~ligne 751)
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>Bouton skip visible sur toutes les TaskCard avec date (ecran taches + dashboard), feedback haptic + toast, aucun appel gamification</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` compile sans nouvelles erreurs
2. Sur device: ouvrir ecran taches, verifier qu'une tache recurrente avec date affiche le bouton skip
3. Tapper skip sur tache recurrente: date avance selon recurrence, pas de points/loot
4. Tapper skip sur tache non-recurrente avec date: date passe au lendemain
5. Tache sans date: pas de bouton skip visible
6. Dashboard sections (menage, overdue, vacation) affichent aussi le bouton skip
</verification>

<success_criteria>
- Bouton skip present sur toute TaskCard ayant une dueDate et non completee
- Recurrente: date avancee via nextOccurrence, tache reste non-cochee
- Non-recurrente avec date: date = lendemain
- Sans date: pas de bouton
- Zero impact gamification (pas de completeTask, pas de points, pas de loot)
- Toast de confirmation affiche
- Feedback haptic au tap
</success_criteria>

<output>
After completion, create `.planning/quick/260404-l62-bouton-skip-taches-recurrentes-sans-gami/260404-l62-SUMMARY.md`
</output>
