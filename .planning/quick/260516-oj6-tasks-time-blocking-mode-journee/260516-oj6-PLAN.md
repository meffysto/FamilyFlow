---
phase: quick-260516-oj6
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/types.ts
  - lib/parser.ts
  - lib/vault-cache.ts
  - lib/time-blocking/slot-mapping.ts
  - lib/time-blocking/auto-placement.ts
  - lib/time-blocking/completion-history.ts
  - lib/time-blocking/index.ts
  - lib/__tests__/parser-slot.test.ts
  - lib/__tests__/auto-placement.test.ts
  - hooks/useVaultTasks.ts
  - app/(tabs)/tasks.tsx
  - components/TaskCard.tsx
  - components/time-blocking/SlotBadge.tsx
  - components/time-blocking/SlotPickerSheet.tsx
  - components/time-blocking/FreeTimeBand.tsx
  - components/time-blocking/SummaryPill.tsx
autonomous: true
requirements:
  - oj6-time-blocking
must_haves:
  truths:
    - "L'utilisateur peut basculer entre mode Liste et mode Journée via un toggle segmenté dans le ScreenHeader"
    - "Le mode est persisté en SecureStore et restauré au prochain lancement"
    - "En mode Liste, le comportement actuel (sections par fichier vault, filter chips profils) est strictement préservé"
    - "En mode Journée, les tâches du jour sélectionné sont auto-placées dans 4 slots (Matin/Midi/Aprem/Soir)"
    - "Le parser reconnaît et strippe les emojis slot (☀️🍽️☕🌙) en début de label"
    - "Le serializer écrit l'emoji slot dans le markdown UNIQUEMENT si task.timeSlot est défini (verrou utilisateur)"
    - "Tap sur le slot badge d'une tâche ouvre une sheet picker permettant de verrouiller le slot"
    - "Long-press sur le slot badge renvoie la tâche au backlog (clear timeSlot + retire emoji marker du markdown)"
    - "Les sections de slot vides ou sous-chargées affichent une bande de temps libre proportionnelle"
    - "La gamification (XP/level/coins/streak/loot box) ne lit jamais timeSlot et reste strictement inchangée"
    - "Le tests Jest parser existants passent sans modification"
  artifacts:
    - path: "lib/types.ts"
      provides: "Champ timeSlot?: 'matin' | 'midi' | 'aprem' | 'soir' ajouté à interface Task"
      contains: "timeSlot?:"
    - path: "lib/parser.ts"
      provides: "parseTask reconnaît emojis slot + stripEmoji les retire ; serialization écrit emoji marker conditionnellement"
      contains: "matin|midi|aprem|soir"
    - path: "lib/time-blocking/slot-mapping.ts"
      provides: "SLOT_IDS, SLOT_DEFINITIONS (id/label/iconName/timeRange/capacityMinutes), timeToSlot(HH:MM)→slot, fileToSlot(path)→slot|null"
      min_lines: 50
    - path: "lib/time-blocking/auto-placement.ts"
      provides: "computeAutoSlot(task, dayTasks, history) → { slot, source: 'explicit'|'time'|'history'|'file'|'nextfit' }"
      min_lines: 60
    - path: "lib/time-blocking/completion-history.ts"
      provides: "loadHistory(), saveCompletion(title, slot), getDominantSlot(title, history) — wrapper SecureStore clé tasks.completionHistory"
      min_lines: 40
    - path: "lib/vault-cache.ts"
      provides: "CACHE_VERSION bumpé de 13 à 14 + commentaire mentionnant timeSlot"
      contains: "CACHE_VERSION = 14"
    - path: "lib/__tests__/parser-slot.test.ts"
      provides: "Suite Jest parse/strip/serialize emojis slot (round-trip)"
      min_lines: 50
    - path: "lib/__tests__/auto-placement.test.ts"
      provides: "Suite Jest chaîne de décision (explicit > time > history > file > nextfit)"
      min_lines: 60
    - path: "hooks/useVaultTasks.ts"
      provides: "toggleTask étendu pour mettre à jour completionHistory (silent catch) ; editTask injecte emoji slot quand timeSlot défini ; nouvelle fonction setTaskSlot(task, slot|null)"
      contains: "setTaskSlot"
    - path: "app/(tabs)/tasks.tsx"
      provides: "Toggle Liste/Journée dans ScreenHeader, viewMode persisté, sections conditionnelles (par fichier vs par slot), day pills filter, summary pill"
      contains: "viewMode"
    - path: "components/TaskCard.tsx"
      provides: "SlotBadge intégré (Lucide icon + variant auto en pointillés)"
      contains: "SlotBadge"
    - path: "components/time-blocking/SlotBadge.tsx"
      provides: "Badge slot avec icône Lucide (Sunrise/Sun/Sunset/Moon/Wand2), variant explicit vs auto, props onPress + onLongPress"
      min_lines: 50
    - path: "components/time-blocking/SlotPickerSheet.tsx"
      provides: "Bottom sheet modal 4 options slot + Annuler"
      min_lines: 60
    - path: "components/time-blocking/FreeTimeBand.tsx"
      provides: "Bande verte sage rayures diagonales, icône contextuelle + libellé Temps libre + sous-label slot"
      min_lines: 50
    - path: "components/time-blocking/SummaryPill.tsx"
      provides: "Pill avec icône Sparkles + total libre + meta N tâches placées"
      min_lines: 30
  key_links:
    - from: "lib/parser.ts:stripEmoji"
      to: "lib/parser.ts:parseTask"
      via: "extension du strip + nouvelle extraction timeSlot via regex emoji slot"
      pattern: "timeSlot|☀️|🍽️|☕|🌙"
    - from: "hooks/useVaultTasks.ts:toggleTask"
      to: "lib/time-blocking/completion-history.ts:saveCompletion"
      via: "appel fire-and-forget après vault write, catch silencieux"
      pattern: "saveCompletion|completionHistory"
    - from: "app/(tabs)/tasks.tsx:sections useMemo"
      to: "lib/time-blocking/auto-placement.ts:computeAutoSlot"
      via: "groupBy(slot) en mode Journée, computed à chaque render"
      pattern: "computeAutoSlot|viewMode"
    - from: "components/TaskCard.tsx:SlotBadge onPress"
      to: "hooks/useVaultTasks.ts:setTaskSlot"
      via: "ouverture SlotPickerSheet puis setTaskSlot écrit emoji marker dans vault"
      pattern: "setTaskSlot"
---

<objective>
Ajouter un mode "Journée" (time blocking) à l'écran tâches existant `app/(tabs)/tasks.tsx`, avec auto-placement des tâches dans 4 créneaux du jour (Matin / Midi / Après-midi / Soir).

Purpose: Donner à l'utilisateur une vue temporelle de sa journée par-dessus la liste de tâches existante, sans casser le mode Liste actuel et sans aucun impact gamification. L'auto-placement utilise une chaîne de décision (explicit > time > history > file > nextfit) qui privilégie l'apprentissage des habitudes sans polluer le vault.

Output:
- Champ optionnel `timeSlot` sur `Task` + parser bidirectionnel pour emojis slot (☀️🍽️☕🌙)
- Moteur pur `lib/time-blocking/` avec tests Jest
- UI mode toggle + day pills + sections slot + free time bands + summary pill
- SlotBadge sur TaskCard avec tap (picker) + long-press (backlog)
- Aucune régression : tests parser existants verts, gamification intouchée, CACHE_VERSION bumpé
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@app/(tabs)/tasks.tsx
@lib/types.ts
@lib/parser.ts
@lib/vault-cache.ts
@hooks/useVaultTasks.ts
@components/TaskCard.tsx
@components/ui/Chip.tsx
@components/ui/ScreenHeader.tsx
@constants/themes.ts
@.planning/sketches/003-time-blocking/B-integration-tasks.html

<interfaces>
<!-- Contrats clés extraits du codebase pour éviter toute exploration côté executor -->

From lib/types.ts (Task interface — extension à ajouter) :
```typescript
export interface Task {
  id: string;
  text: string;
  completed: boolean;
  dueDate?: string;       // YYYY-MM-DD from 📅
  recurrence?: string;
  completedDate?: string;
  reminderTime?: string;  // HH:MM from ⏰
  tags: string[];
  mentions: string[];
  sourceFile: string;
  lineIndex: number;
  section?: string;
  secret?: boolean;
  targetProfileId?: string;
  secretStatus?: 'active' | 'pending' | 'validated';
  xpOverride?: number;
  // ─── À AJOUTER (Plan 1) ──────────────────────────────────────────
  /** Slot temporel verrouillé par l'utilisateur (parsé depuis emoji marker
   *  en début de label : ☀️ matin / 🍽️ midi / ☕ aprem / 🌙 soir).
   *  Absent = auto-placement par computeAutoSlot en mode Journée. */
  timeSlot?: 'matin' | 'midi' | 'aprem' | 'soir';
}
```

From lib/parser.ts (regex et helpers EXISTANTS — pattern à étendre) :
```typescript
const TASK_REGEX = /^(\s*)-\s+\[([ xX])\]\s+(.+)$/;
const DUE_DATE_REGEX = /📅\s*(\d{4}-\d{2}-\d{2})/;
const RECURRENCE_REGEX = /🔁\s*(every\s+(?:\d+\s+)?(?:day|week|month)s?)/;
const REMINDER_TIME_REGEX = /⏰\s*(\d{2}:\d{2})/;
const XP_OVERRIDE_REGEX = /⭐\s*(\d+)/;

function stripEmoji(text: string): string {
  return text
    .replace(/📅\s*\d{4}-\d{2}-\d{2}/g, '')
    .replace(/🔁\s*every\s+(?:\d+\s+)?(?:day|week|month)s?/g, '')
    .replace(/✅\s*\d{4}-\d{2}-\d{2}/g, '')
    .replace(/⏰\s*\d{2}:\d{2}/g, '')
    .replace(/⭐\s*\d+/g, '')
    .replace(/\s{2,}/g, ' ')
    .trim();
}

// IMPORTANT : il n'existe PAS de fonction `serializeTask` dans parser.ts.
// La sérialisation se fait inline dans hooks/useVaultTasks.ts (addTask + editTask)
// par concaténation de string avec les emojis. C'est là qu'il faut injecter
// l'emoji slot quand timeSlot est défini.
```

From hooks/useVaultTasks.ts (editTask construit la ligne markdown — pattern à étendre) :
```typescript
let taskLine = newText;
if (newRecurrence) taskLine += ` 🔁 ${newRecurrence}`;
if (newDueDate) taskLine += ` 📅 ${newDueDate}`;
if (newReminderTime) taskLine += ` ⏰ ${newReminderTime}`;
if (newXpOverride !== undefined) taskLine += ` ⭐ ${newXpOverride}`;
// À AJOUTER : emoji slot PRÉFIXÉ (début de label) UNIQUEMENT si timeSlot défini
const fullLine = `- [${task.completed ? 'x' : ' '}] ${taskLine}`;
```

From components/ui/ScreenHeader.tsx (slot actions à droite) :
```typescript
interface ScreenHeaderProps {
  title: string;
  subtitle?: string | React.ReactNode;
  actions?: React.ReactNode;  // <-- slot pour insérer le toggle Liste/Journée
  // ...
}
```

From components/TaskCard.tsx (props existantes) :
```typescript
interface TaskCardProps {
  task: Task;
  onToggle: (task: Task, completed: boolean) => void;
  onSkip?: (task: Task) => void;
  onLongPress?: () => void;
  showSource?: boolean;
  hideSection?: boolean;
  compact?: boolean;
  profiles?: Profile[];
  pointsOnComplete?: number;
  // À AJOUTER (Plan 3) :
  // slotBadge?: { slot: SlotId; isAuto: boolean; onPress: () => void; onLongPress: () => void };
}
```

From lib/vault-cache.ts (CACHE_VERSION à bumper) :
```typescript
const CACHE_VERSION = 13;             // → 14
const CACHE_FILENAME = 'vault-cache-v13.json';  // → 'vault-cache-v14.json'
// Ajouter ligne commentaire au-dessus :
// v14: Phase quick-260516-oj6 — Task.timeSlot? ('matin'|'midi'|'aprem'|'soir')
//      ajouté au parser. Le champ est optionnel mais la shape Task cachée change.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tâche 1 : Data layer — types + parser + moteur time-blocking + tests</name>
  <files>
    lib/types.ts,
    lib/parser.ts,
    lib/vault-cache.ts,
    lib/time-blocking/slot-mapping.ts,
    lib/time-blocking/auto-placement.ts,
    lib/time-blocking/completion-history.ts,
    lib/time-blocking/index.ts,
    lib/__tests__/parser-slot.test.ts,
    lib/__tests__/auto-placement.test.ts
  </files>
  <behavior>
    - Test parser-slot.test.ts :
      - parseTask reconnaît "☀️ Faire le petit-déj" → timeSlot='matin', text='Faire le petit-déj' (emoji strippé)
      - parseTask reconnaît "🍽️ Préparer le déjeuner" → timeSlot='midi'
      - parseTask reconnaît "☕ Goûter Lucas" → timeSlot='aprem'
      - parseTask reconnaît "🌙 Bain Emma" → timeSlot='soir'
      - parseTask sans emoji slot → timeSlot=undefined, text inchangé
      - parseTask "☀️ Matin 📅 2026-05-16 ⏰ 07:30" → timeSlot='matin', dueDate, reminderTime tous extraits, label = "Matin"
      - Round-trip : parser strippe l'emoji ; le moteur de réécriture (testé via reconstruction manuelle de ligne) doit reproduire l'emoji marker UNIQUEMENT si timeSlot défini
      - Tâche sans timeSlot ne doit JAMAIS gagner d'emoji slot au reserialize (verrou utilisateur respecté)
    - Test auto-placement.test.ts (chaîne de décision exhaustive) :
      - explicit : task.timeSlot='soir' → { slot: 'soir', source: 'explicit' } (court-circuite tout le reste)
      - time via reminderTime : task.reminderTime='07:30' → { slot: 'matin', source: 'time' }
      - time via reminderTime midi : '12:30' → 'midi'
      - time via reminderTime aprem : '15:00' → 'aprem'
      - time via reminderTime soir : '20:00' → 'soir'
      - time via reminderTime soir wrap : '03:00' → 'soir' (englobe nuit)
      - time via dueDate avec heure (format ISO) : '2026-05-16T08:00' → 'matin'
      - history : task.title='Méditation', history a 3 entrées slot='soir' sur 3 → { slot: 'soir', source: 'history' } (mode statistique)
      - history minimum 2 entrées requis : 1 entrée → fallback file/nextfit
      - file : task.sourceFile contient "Routine matin" → { slot: 'matin', source: 'file' }
      - file : task.sourceFile contient "Routine soir" → 'soir'
      - nextfit : aucune autre source, dayTasks remplissent matin à 80%, midi à 50% → return midi (premier slot ≤ 75% chargé)
      - Test slot-mapping :
        - timeToSlot('06:00') → 'matin', timeToSlot('11:59') → 'matin', timeToSlot('12:00') → 'midi'
        - timeToSlot('13:59') → 'midi', timeToSlot('14:00') → 'aprem'
        - timeToSlot('17:59') → 'aprem', timeToSlot('18:00') → 'soir'
        - timeToSlot('23:59') → 'soir', timeToSlot('05:59') → 'soir' (wrap nuit)
      - Test completion-history (mockable SecureStore via __DEV__ guard) :
        - saveCompletion ajoute en FIFO max 10 par titre
        - getDominantSlot retourne le mode statistique
        - getDominantSlot retourne undefined si < 2 entrées
  </behavior>
  <action>
    **Étape 1 — Étendre `lib/types.ts` :**
    Ajouter dans l'interface `Task` (après xpOverride, avant la fermeture) :
    ```typescript
    /** Slot temporel verrouillé par l'utilisateur (parsé depuis emoji marker
     *  en début de label : ☀️ matin / 🍽️ midi / ☕ aprem / 🌙 soir).
     *  Absent = auto-placement par computeAutoSlot en mode Journée. */
    timeSlot?: 'matin' | 'midi' | 'aprem' | 'soir';
    ```
    Exporter aussi un type alias `export type SlotId = NonNullable<Task['timeSlot']>;` après l'interface.

    **Étape 2 — Étendre `lib/parser.ts` :**

    1. Ajouter une regex au-dessus de TASK_REGEX :
    ```typescript
    // Emojis slot reconnus EN DÉBUT de label (après le checkbox, éventuellement après espaces)
    // ☀️ matin (U+2600 U+FE0F) / 🍽️ midi (U+1F37D U+FE0F) / ☕ aprem (U+2615) / 🌙 soir (U+1F319)
    const SLOT_EMOJI_REGEX = /^(\s*)(☀️|🍽️|☕|🌙)\s+/;
    const SLOT_EMOJI_TO_ID: Record<string, NonNullable<Task['timeSlot']>> = {
      '☀️': 'matin',
      '🍽️': 'midi',
      '☕': 'aprem',
      '🌙': 'soir',
    };
    ```
    Note : utiliser des littéraux Unicode tels quels (cohérent avec les emojis existants dans le fichier comme 📅 🔁 ⏰).

    2. Modifier `parseTask` : juste après `const completed = ...`, extraire le slot :
    ```typescript
    let timeSlot: Task['timeSlot'] | undefined;
    const slotMatch = rawText.match(SLOT_EMOJI_REGEX);
    if (slotMatch) {
      timeSlot = SLOT_EMOJI_TO_ID[slotMatch[2]];
    }
    ```
    Puis dans la construction du retour, ajouter `timeSlot,` (undefined si pas de match).

    3. Étendre `stripEmoji` : ajouter en première position de la chaîne `.replace()` :
    ```typescript
    .replace(/^(☀️|🍽️|☕|🌙)\s+/, '')  // emoji slot en début de label
    ```
    (Avant les replace existants pour 📅/🔁/✅/⏰/⭐.)

    **Étape 3 — Créer `lib/time-blocking/slot-mapping.ts` :**
    ```typescript
    import type { SlotId } from '../types';

    export const SLOT_IDS: readonly SlotId[] = ['matin', 'midi', 'aprem', 'soir'] as const;

    export interface SlotDefinition {
      id: SlotId;
      label: string;                    // 'Matin', 'Midi', 'Après-midi', 'Soir'
      iconName: 'Sunrise' | 'Sun' | 'Sunset' | 'Moon';
      startHour: number;                // inclusif (24h)
      endHour: number;                  // exclusif (peut wrap)
      capacityMinutes: number;          // 75% de la durée du slot
      timeRangeLabel: string;           // '06:00 – 12:00' etc.
      emojiMarker: '☀️' | '🍽️' | '☕' | '🌙';
      freeTimeIconName: 'Coffee' | 'Sun' | 'Leaf' | 'Heart';
      freeTimeLabel: string;            // 'PAUSE', 'RESPIRE', 'PROFITE', 'DÉTENTE' (couple traité côté UI)
    }

    export const SLOT_DEFINITIONS: Record<SlotId, SlotDefinition> = {
      matin: { id: 'matin', label: 'Matin', iconName: 'Sunrise', startHour: 6, endHour: 12, capacityMinutes: 270, timeRangeLabel: '06:00 – 12:00', emojiMarker: '☀️', freeTimeIconName: 'Coffee', freeTimeLabel: 'PAUSE' },
      midi: { id: 'midi', label: 'Midi', iconName: 'Sun', startHour: 12, endHour: 14, capacityMinutes: 90, timeRangeLabel: '12:00 – 14:00', emojiMarker: '🍽️', freeTimeIconName: 'Sun', freeTimeLabel: 'RESPIRE' },
      aprem: { id: 'aprem', label: 'Après-midi', iconName: 'Sunset', startHour: 14, endHour: 18, capacityMinutes: 180, timeRangeLabel: '14:00 – 18:00', emojiMarker: '☕', freeTimeIconName: 'Leaf', freeTimeLabel: 'PROFITE' },
      soir: { id: 'soir', label: 'Soir', iconName: 'Moon', startHour: 18, endHour: 6, capacityMinutes: 180, timeRangeLabel: '18:00 – 06:00', emojiMarker: '🌙', freeTimeIconName: 'Heart', freeTimeLabel: 'DÉTENTE' },
    };

    /** Map une heure HH:MM ou HH:MM:SS vers un slot. Le soir wrap après minuit. */
    export function timeToSlot(time: string): SlotId {
      const h = parseInt(time.slice(0, 2), 10);
      if (h >= 6 && h < 12) return 'matin';
      if (h >= 12 && h < 14) return 'midi';
      if (h >= 14 && h < 18) return 'aprem';
      return 'soir';  // 18-23 et 0-5
    }

    /** Détecte un slot depuis le sourceFile (pattern Routine matin/soir). */
    export function fileToSlot(sourceFile: string): SlotId | null {
      if (/Routine\s+matin/i.test(sourceFile)) return 'matin';
      if (/Routine\s+soir/i.test(sourceFile)) return 'soir';
      return null;
    }

    /** Durée estimée d'une tâche en minutes (défaut 15 si pas d'info). */
    export const DEFAULT_TASK_DURATION_MIN = 15;
    ```

    **Étape 4 — Créer `lib/time-blocking/auto-placement.ts` :**
    ```typescript
    import type { Task, SlotId } from '../types';
    import { SLOT_IDS, SLOT_DEFINITIONS, timeToSlot, fileToSlot, DEFAULT_TASK_DURATION_MIN } from './slot-mapping';
    import { getDominantSlot, type CompletionHistory } from './completion-history';

    export type AutoPlacementSource = 'explicit' | 'time' | 'history' | 'file' | 'nextfit';

    export interface AutoPlacementResult {
      slot: SlotId;
      source: AutoPlacementSource;
    }

    /** Chaîne de décision : explicit > time > history > file > nextfit. */
    export function computeAutoSlot(
      task: Task,
      dayTasks: Task[],         // tâches déjà placées dans le jour sélectionné (pour nextfit)
      history: CompletionHistory,
    ): AutoPlacementResult {
      // 1. Explicit : timeSlot verrouillé
      if (task.timeSlot) return { slot: task.timeSlot, source: 'explicit' };

      // 2. Time : reminderTime ou dueDate avec heure
      if (task.reminderTime) return { slot: timeToSlot(task.reminderTime), source: 'time' };
      if (task.dueDate && task.dueDate.includes('T')) {
        const timePart = task.dueDate.split('T')[1];
        if (timePart && /^\d{2}:\d{2}/.test(timePart)) {
          return { slot: timeToSlot(timePart), source: 'time' };
        }
      }

      // 3. History : slot dominant si >= 2 entrées
      const dominant = getDominantSlot(task.text, history);
      if (dominant) return { slot: dominant, source: 'history' };

      // 4. File : pattern Routine matin/soir
      const fromFile = fileToSlot(task.sourceFile);
      if (fromFile) return { slot: fromFile, source: 'file' };

      // 5. Next-fit : premier slot ≤ 75% chargé
      const used: Record<SlotId, number> = { matin: 0, midi: 0, aprem: 0, soir: 0 };
      for (const t of dayTasks) {
        const placed = t.timeSlot ?? null;
        if (!placed) continue;  // ne compte que les tâches déjà placées (évite récursion)
        used[placed] += estimateTaskDuration(t);
      }
      for (const slot of SLOT_IDS) {
        if (used[slot] < 0.75 * SLOT_DEFINITIONS[slot].capacityMinutes) {
          return { slot, source: 'nextfit' };
        }
      }
      return { slot: 'soir', source: 'nextfit' };  // fallback
    }

    /** Estimation de la durée d'une tâche. Si reminderTime contient une durée
     *  "HH:MM · Xmin" la lire, sinon 15min par défaut. */
    export function estimateTaskDuration(task: Task): number {
      if (task.reminderTime) {
        const m = task.reminderTime.match(/(\d+)\s*min/i);
        if (m) return parseInt(m[1], 10);
      }
      return DEFAULT_TASK_DURATION_MIN;
    }
    ```

    **Étape 5 — Créer `lib/time-blocking/completion-history.ts` :**
    ```typescript
    import * as SecureStore from 'expo-secure-store';
    import type { SlotId } from '../types';
    import { SLOT_IDS } from './slot-mapping';

    const STORAGE_KEY = 'tasks.completionHistory';
    const MAX_ENTRIES_PER_TITLE = 10;

    export interface CompletionEntry {
      slot: SlotId;
      timestamp: string;  // ISO
    }

    export type CompletionHistory = Record<string, CompletionEntry[]>;

    export async function loadHistory(): Promise<CompletionHistory> {
      try {
        const raw = await SecureStore.getItemAsync(STORAGE_KEY);
        if (!raw) return {};
        const parsed = JSON.parse(raw);
        if (typeof parsed !== 'object' || parsed === null) return {};
        return parsed as CompletionHistory;
      } catch {
        return {};
      }
    }

    export async function saveCompletion(title: string, slot: SlotId): Promise<void> {
      try {
        const history = await loadHistory();
        const entries = history[title] ?? [];
        entries.push({ slot, timestamp: new Date().toISOString() });
        // FIFO max 10
        while (entries.length > MAX_ENTRIES_PER_TITLE) entries.shift();
        history[title] = entries;
        await SecureStore.setItemAsync(STORAGE_KEY, JSON.stringify(history));
      } catch {
        // Silent : historique = bonus, jamais critique
      }
    }

    /** Retourne le slot dominant (mode statistique) si >= 2 entrées, sinon undefined. */
    export function getDominantSlot(title: string, history: CompletionHistory): SlotId | undefined {
      const entries = history[title] ?? [];
      if (entries.length < 2) return undefined;
      const counts: Record<SlotId, number> = { matin: 0, midi: 0, aprem: 0, soir: 0 };
      for (const e of entries) counts[e.slot]++;
      let best: SlotId = SLOT_IDS[0];
      let bestCount = 0;
      for (const slot of SLOT_IDS) {
        if (counts[slot] > bestCount) {
          best = slot;
          bestCount = counts[slot];
        }
      }
      return bestCount >= 2 ? best : undefined;
    }
    ```

    **Étape 6 — Créer `lib/time-blocking/index.ts` (barrel) :**
    ```typescript
    export * from './slot-mapping';
    export * from './auto-placement';
    export * from './completion-history';
    ```

    **Étape 7 — Bumper le cache (`lib/vault-cache.ts`) :**
    - Modifier les lignes 71-72 : `CACHE_VERSION = 14` et `CACHE_FILENAME = 'vault-cache-v14.json'`
    - Ajouter juste au-dessus de `const CACHE_VERSION = ...` la ligne commentaire :
      ```
      // v14: Phase quick-260516-oj6 — Task.timeSlot? ('matin'|'midi'|'aprem'|'soir')
      //      ajouté au parser (nouveau champ optionnel). Bump pour éviter shape
      //      desync entre cache v13 (sans timeSlot) et runtime v14.
      ```

    **Étape 8 — Tests Jest :**

    Créer `lib/__tests__/parser-slot.test.ts` : tester `parseTask` (extraction emoji slot + stripping + cohabitation avec 📅⏰🔁). Inclure un test "verrou utilisateur" : appel à `parseTask("- [ ] Bain Emma")` → `timeSlot` undefined ; cas négatif aussi pour "- [ ] Texte ☀️ avec emoji au milieu" → timeSlot doit rester undefined (regex anchored au début).

    Créer `lib/__tests__/auto-placement.test.ts` : tester `computeAutoSlot` avec la chaîne de décision exhaustive (cf. behavior ci-dessus), `timeToSlot`, `getDominantSlot`. Mocker `expo-secure-store` (jest.mock) pour les tests qui touchent loadHistory.

    **Pitfalls à éviter (lus dans STATE.md/CLAUDE.md) :**
    - Pas de pollution vault : `parseTask` reconnaît mais ne RÉINJECTE jamais d'emoji slot tout seul ; l'injection se fait uniquement dans `editTask`/`setTaskSlot` côté hook (Plan 1 étape data, Plan 2 wiring)
    - Regex anchored début : `/^(\s*)(☀️|...)/` pour éviter de matcher un emoji slot au milieu du texte
    - `stripEmoji` ne strippe que l'emoji EN DÉBUT — ajouter le replace en premier dans la chaîne
    - Pas de double pollution avec tests Jest existants : créer de NOUVEAUX fichiers de test, ne pas modifier `lib/__tests__/parser.test.ts`
    - `expo-secure-store` mock : utiliser `jest.mock('expo-secure-store', () => ({ getItemAsync: jest.fn(), setItemAsync: jest.fn() }))`
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx jest lib/__tests__/parser.test.ts lib/__tests__/parser-slot.test.ts lib/__tests__/auto-placement.test.ts --no-coverage</automated>
  </verify>
  <done>
    - `Task.timeSlot` typed et reconnu par parser
    - Module `lib/time-blocking/` complet (3 fichiers + barrel)
    - CACHE_VERSION bumpé 13→14 avec commentaire
    - Tests parser-slot + auto-placement passent
    - Test parser.test.ts existant continue de passer (zéro régression)
    - `npx tsc --noEmit` ne produit aucune nouvelle erreur (les pré-existantes MemoryEditor/cooklang/useVault restent ignorées)
  </done>
</task>

<task type="auto" tdd="false">
  <name>Tâche 2 : UI mode toggle + mode Journée (tasks.tsx + composants time-blocking)</name>
  <files>
    app/(tabs)/tasks.tsx,
    components/time-blocking/FreeTimeBand.tsx,
    components/time-blocking/SummaryPill.tsx
  </files>
  <action>
    **Étape 1 — Persistance viewMode (`app/(tabs)/tasks.tsx`) :**

    Ajouter dans la zone des `useState` du composant principal (à côté de `sectionOrder`) :
    ```typescript
    const [viewMode, setViewMode] = useState<'liste' | 'journee'>('liste');
    const VIEW_MODE_KEY = 'tasks.viewMode';

    useEffect(() => {
      SecureStore.getItemAsync(VIEW_MODE_KEY)
        .then(v => { if (v === 'journee') setViewMode('journee'); })
        .catch(() => { /* silent */ });
    }, []);

    const handleSetViewMode = useCallback((next: 'liste' | 'journee') => {
      Haptics.selectionAsync();
      setViewMode(next);
      SecureStore.setItemAsync(VIEW_MODE_KEY, next).catch(() => { /* silent */ });
    }, []);
    ```

    Ajouter aussi `selectedDay` (mode Journée) :
    ```typescript
    const [selectedDay, setSelectedDay] = useState<string>(new Date().toISOString().slice(0, 10));
    // 'hier' / 'aujourd-hui' / 'demain' / 'semaine' filter actif
    const [dayFilter, setDayFilter] = useState<'hier' | 'aujourdhui' | 'demain' | 'semaine'>('aujourdhui');
    ```

    **Étape 2 — Toggle dans ScreenHeader :**

    Repérer le `<ScreenHeader ... actions={...}>` existant. Ajouter dans le slot `actions` (en premier, AVANT le bouton + et le bouton reorder) un toggle segmenté React Native pur :
    ```typescript
    import { List as ListIcon, Clock as ClockIcon } from 'lucide-react-native';
    // ...
    <View style={styles.modeToggle}>
      <TouchableOpacity
        onPress={() => handleSetViewMode('liste')}
        style={[
          styles.modeToggleSegment,
          { backgroundColor: viewMode === 'liste' ? colors.brand.soil : 'transparent' },
        ]}
        hitSlop={ICON_BTN_HITSLOP}
        accessibilityLabel="Mode liste"
      >
        <ListIcon size={14} color={viewMode === 'liste' ? colors.bg : colors.textSub} strokeWidth={2.5} />
      </TouchableOpacity>
      <TouchableOpacity
        onPress={() => handleSetViewMode('journee')}
        style={[
          styles.modeToggleSegment,
          { backgroundColor: viewMode === 'journee' ? colors.brand.soil : 'transparent' },
        ]}
        hitSlop={ICON_BTN_HITSLOP}
        accessibilityLabel="Mode journée"
      >
        <ClockIcon size={14} color={viewMode === 'journee' ? colors.bg : colors.textSub} strokeWidth={2.5} />
      </TouchableOpacity>
    </View>
    ```
    Styles statiques (StyleSheet bottom) :
    ```typescript
    modeToggle: { flexDirection: 'row', borderRadius: Radius.full, padding: 2, gap: 2, borderWidth: 1 },
    modeToggleSegment: { paddingHorizontal: Spacing.md, paddingVertical: Spacing.xs, borderRadius: Radius.full },
    ```
    Note : `borderColor` du container `modeToggle` doit être appliqué inline avec `colors.border` (style dynamique).

    **Étape 3 — Day pills (mode Journée uniquement) :**

    Juste au-dessus de la SectionList, conditionner :
    ```tsx
    {viewMode === 'journee' && (
      <ScrollView horizontal showsHorizontalScrollIndicator={false} contentContainerStyle={styles.dayPillsRow}>
        <Chip label="Hier" selected={dayFilter === 'hier'} onPress={() => {
          setDayFilter('hier');
          setSelectedDay(formatDateOffset(-1));
        }} />
        <Chip label="Aujourd'hui" selected={dayFilter === 'aujourdhui'} onPress={() => {
          setDayFilter('aujourdhui');
          setSelectedDay(new Date().toISOString().slice(0, 10));
        }} />
        <Chip label="Demain" selected={dayFilter === 'demain'} onPress={() => {
          setDayFilter('demain');
          setSelectedDay(formatDateOffset(1));
        }} />
        <Chip label="Semaine" selected={dayFilter === 'semaine'} onPress={() => {
          Haptics.selectionAsync();
          Alert.alert('Bientôt', 'La vue Semaine arrive dans une prochaine mise à jour.');
        }} />
      </ScrollView>
    )}
    ```
    Helper `formatDateOffset(days: number)` : retourne `new Date(Date.now() + days*86400000).toISOString().slice(0, 10)`.

    **Étape 4 — Conditionner les sections par viewMode :**

    Modifier le `useMemo` existant pour `sections` (autour de la ligne 605) : extraire la logique actuelle dans une branche `if (viewMode === 'liste')` (laisser le code TEL QUEL), et ajouter une branche `if (viewMode === 'journee')` :

    ```typescript
    import { computeAutoSlot, SLOT_IDS, SLOT_DEFINITIONS, type SlotId } from '../../lib/time-blocking';
    import { loadHistory, type CompletionHistory } from '../../lib/time-blocking/completion-history';

    // Charger l'historique au mount
    const [completionHistory, setCompletionHistory] = useState<CompletionHistory>({});
    useEffect(() => {
      loadHistory().then(setCompletionHistory).catch(() => { /* silent */ });
    }, []);

    const sections: TaskSection[] = useMemo(() => {
      if (viewMode === 'liste') {
        // … code existant inchangé (groupBy sourceFile)
      }

      // Mode Journée : filtre par selectedDay (dueDate match OU récurrente qui tombe ce jour)
      const dayTasks = activeTasks.filter(t => {
        if (t.dueDate && t.dueDate.slice(0, 10) === selectedDay) return true;
        // Récurrentes : on les inclut si selectedDay correspond à la prochaine occurrence
        if (t.recurrence && (!t.dueDate || t.dueDate.slice(0, 10) <= selectedDay)) return true;
        return false;
      });

      // Compute placement pour chaque tâche
      const placedTasks = dayTasks.map(t => {
        const result = computeAutoSlot(t, dayTasks, completionHistory);
        return { task: t, slot: result.slot, isAuto: result.source !== 'explicit' };
      });

      // 4 sections fixes (toujours présentes, même vides)
      return SLOT_IDS.map(slotId => {
        const def = SLOT_DEFINITIONS[slotId];
        const slotTasks = placedTasks.filter(p => p.slot === slotId).map(p => ({ ...p.task, _isAutoSlot: p.isAuto }));
        return {
          title: def.label,
          sourceFile: `slot:${slotId}`,  // clé unique distincte des fichiers vault
          slotId,
          data: slotTasks,
        };
      });
    }, [viewMode, activeTasks, sectionOrder, t, selectedDay, completionHistory]);
    ```

    Étendre l'interface `TaskSection` locale pour inclure `slotId?: SlotId` (optionnel).

    **Étape 5 — Section header en mode Journée :**

    Modifier `renderSectionHeader` pour afficher le slot avec icône Lucide + time range quand `viewMode === 'journee'` :
    ```typescript
    const renderSectionHeader = useCallback(({ section }: { section: TaskSection }) => {
      if (viewMode === 'journee' && section.slotId) {
        const def = SLOT_DEFINITIONS[section.slotId];
        const IconComponent = { Sunrise, Sun, Sunset, Moon }[def.iconName];
        return (
          <View style={styles.slotHeader}>
            <IconComponent size={18} color={colors.brand.soil} strokeWidth={2} />
            <Text style={[styles.slotHeaderLabel, { color: colors.text }]}>{def.label}</Text>
            <Text style={[styles.slotHeaderRange, { color: colors.textMuted }]}>{def.timeRangeLabel}</Text>
          </View>
        );
      }
      // … code existant pour mode Liste
    }, [viewMode, colors]);
    ```
    Imports Lucide à ajouter : `Sunrise, Sun, Sunset, Moon`.

    **Étape 6 — Free time band en fin de section (Journée) :**

    Modifier `renderSectionFooter` (créer la prop sur la SectionList si absente). En mode Journée, calculer pour chaque slot :
    ```typescript
    const renderSectionFooter = useCallback(({ section }: { section: TaskSection }) => {
      if (viewMode !== 'journee' || !section.slotId) return null;
      const def = SLOT_DEFINITIONS[section.slotId];
      const usedMin = section.data.reduce((sum, t) => sum + estimateTaskDuration(t), 0);
      const freeMin = def.capacityMinutes - usedMin;
      if (freeMin < 60) return null;  // bande affichée si reste ≥ 1h
      // Choix Heart si soir + adulte avec partenaire, sinon icône par défaut
      const isCouple = section.slotId === 'soir' && activeProfile?.role === 'adulte' && profiles.filter(p => p.role === 'adulte').length >= 2;
      const sublabel = isCouple ? 'SOIRÉE COUPLE' : def.freeTimeLabel;
      return <FreeTimeBand slot={section.slotId} freeMinutes={freeMin} sublabel={sublabel} isCouple={isCouple} />;
    }, [viewMode, activeProfile, profiles]);
    ```
    Imports : `import { estimateTaskDuration } from '../../lib/time-blocking';`

    **Étape 7 — Summary pill (entre filter chips et SectionList, Journée only) :**

    ```tsx
    {viewMode === 'journee' && (
      <SummaryPill
        totalFreeMinutes={sections.reduce((sum, s) => {
          if (!s.slotId) return sum;
          const def = SLOT_DEFINITIONS[s.slotId];
          const used = s.data.reduce((acc, t) => acc + estimateTaskDuration(t), 0);
          return sum + Math.max(0, def.capacityMinutes - used);
        }, 0)}
        placedCount={sections.reduce((sum, s) => sum + s.data.length, 0)}
        dayLabel={dayFilter === 'aujourdhui' ? "aujourd'hui" : dayFilter === 'hier' ? 'hier' : 'demain'}
      />
    )}
    ```

    **Étape 8 — Créer `components/time-blocking/FreeTimeBand.tsx` :**
    ```typescript
    import React from 'react';
    import { View, Text, StyleSheet } from 'react-native';
    import { Coffee, Sun, Leaf, Heart } from 'lucide-react-native';
    import { useThemeColors } from '../../contexts/ThemeContext';
    import { Spacing, Radius } from '../../constants/spacing';
    import { FontSize, FontWeight } from '../../constants/typography';
    import { SLOT_DEFINITIONS, type SlotId } from '../../lib/time-blocking';

    interface FreeTimeBandProps {
      slot: SlotId;
      freeMinutes: number;
      sublabel: string;
      isCouple?: boolean;
    }

    const ICON_MAP = { Coffee, Sun, Leaf, Heart };

    function formatFreeMinutes(min: number): string {
      const h = Math.floor(min / 60);
      const m = min % 60;
      if (h === 0) return `${m}min`;
      if (m === 0) return `${h}h`;
      return `${h}h ${m}min`;
    }

    export const FreeTimeBand = React.memo(function FreeTimeBand({ slot, freeMinutes, sublabel, isCouple }: FreeTimeBandProps) {
      const { colors } = useThemeColors();
      const def = SLOT_DEFINITIONS[slot];
      const iconName = isCouple ? 'Heart' : def.freeTimeIconName;
      const Icon = ICON_MAP[iconName];
      return (
        <View style={[styles.container, { backgroundColor: colors.successSurface ?? colors.surface, borderColor: colors.successBorder ?? colors.border }]}>
          {/* Rayures diagonales : utiliser un overlay simple avec opacity */}
          <View style={styles.row}>
            <Icon size={16} color={colors.success ?? colors.brand.soil} strokeWidth={2} />
            <Text style={[styles.label, { color: colors.text }]}>
              Temps libre · {formatFreeMinutes(freeMinutes)}
            </Text>
            <Text style={[styles.sublabel, { color: colors.textMuted }]}>{sublabel}</Text>
          </View>
        </View>
      );
    });

    const styles = StyleSheet.create({
      container: {
        marginHorizontal: Spacing['2xl'],
        marginVertical: Spacing.sm,
        paddingHorizontal: Spacing.xl,
        paddingVertical: Spacing.lg,
        borderRadius: Radius.lg,
        borderWidth: 1,
        borderStyle: 'dashed',
      },
      row: { flexDirection: 'row', alignItems: 'center', gap: Spacing.md },
      label: { fontSize: FontSize.label, fontWeight: FontWeight.semibold, flex: 1 },
      sublabel: { fontSize: FontSize.xs, fontWeight: FontWeight.bold, letterSpacing: 1 },
    });
    ```
    Note : si `colors.successSurface`/`successBorder`/`success` n'existent pas, utiliser un fallback `colors.brand.leaf` ou `colors.tagColors.vert` (à vérifier au moment de l'écriture en lisant `constants/themes.ts` ou `useThemeColors()`). En cas d'incertitude, utiliser `colors.surface` + `colors.border` (cohérence sûre).

    **Étape 9 — Créer `components/time-blocking/SummaryPill.tsx` :**
    ```typescript
    import React from 'react';
    import { View, Text, StyleSheet } from 'react-native';
    import { Sparkles } from 'lucide-react-native';
    import { useThemeColors } from '../../contexts/ThemeContext';
    import { Spacing, Radius } from '../../constants/spacing';
    import { FontSize, FontWeight } from '../../constants/typography';

    interface SummaryPillProps {
      totalFreeMinutes: number;
      placedCount: number;
      dayLabel: string;
    }

    function formatTotal(min: number): string {
      const h = Math.floor(min / 60);
      const m = min % 60;
      if (h === 0) return `${m}min`;
      if (m === 0) return `${h}h`;
      return `${h}h${m}`;
    }

    export const SummaryPill = React.memo(function SummaryPill({ totalFreeMinutes, placedCount, dayLabel }: SummaryPillProps) {
      const { colors } = useThemeColors();
      return (
        <View style={[styles.container, { backgroundColor: colors.surface, borderColor: colors.border }]}>
          <View style={[styles.iconCircle, { backgroundColor: colors.brand.gold ?? colors.brand.soil }]}>
            <Sparkles size={14} color={colors.bg} strokeWidth={2.5} />
          </View>
          <View style={{ flex: 1 }}>
            <Text style={[styles.title, { color: colors.text }]}>
              {formatTotal(totalFreeMinutes)} de libre {dayLabel}
            </Text>
            <Text style={[styles.meta, { color: colors.textMuted }]}>
              {placedCount} {placedCount > 1 ? 'tâches placées' : 'tâche placée'} · 0 en attente
            </Text>
          </View>
        </View>
      );
    });

    const styles = StyleSheet.create({
      container: {
        flexDirection: 'row', alignItems: 'center', gap: Spacing.md,
        marginHorizontal: Spacing['2xl'], marginBottom: Spacing.md,
        paddingHorizontal: Spacing.lg, paddingVertical: Spacing.md,
        borderRadius: Radius.lg, borderWidth: 1,
      },
      iconCircle: { width: 28, height: 28, borderRadius: 14, alignItems: 'center', justifyContent: 'center' },
      title: { fontSize: FontSize.md, fontWeight: FontWeight.semibold },
      meta: { fontSize: FontSize.xs, marginTop: 2 },
    });
    ```

    **Étape 10 — Adapter filtres :**

    En mode Journée, masquer les filter chips actuels (profils) ou les remplacer par day pills. Décision : remplacer entièrement la rangée de filter chips par day pills en mode Journée (les filter chips profils restent strictement réservés au mode Liste). Pas de cumul.

    **Pitfalls à éviter :**
    - `colors.*` UNIQUEMENT, jamais hardcoded
    - `Haptics.selectionAsync()` sur tap toggle + day pills
    - Tests existants `parser.test.ts` doivent rester verts (ne pas toucher au mode Liste)
    - Composants memoïsés (`React.memo`) sur FreeTimeBand + SummaryPill
    - Imports Lucide groupés en haut du fichier
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>
    - Toggle Liste/Journée visible dans ScreenHeader, persiste après reload
    - Mode Liste : comportement actuel strictement préservé (sections par fichier, filter chips profils)
    - Mode Journée : 4 sections fixes (Matin/Midi/Aprem/Soir) avec icônes Lucide + time range
    - Day pills (Hier/Aujourd'hui/Demain/Semaine) affichées en Journée
    - Summary pill affichée entre day pills et sections
    - Free time band rendue sous une section quand reste ≥ 1h
    - `npx tsc --noEmit` ne produit aucune nouvelle erreur
  </done>
</task>

<task type="auto" tdd="false">
  <name>Tâche 3 : SlotBadge sur TaskCard + interactions (picker + long-press backlog) + completion history</name>
  <files>
    components/TaskCard.tsx,
    components/time-blocking/SlotBadge.tsx,
    components/time-blocking/SlotPickerSheet.tsx,
    hooks/useVaultTasks.ts,
    app/(tabs)/tasks.tsx
  </files>
  <action>
    **Étape 1 — Créer `components/time-blocking/SlotBadge.tsx` :**

    ```typescript
    import React, { useCallback, useRef } from 'react';
    import { TouchableOpacity, View, StyleSheet, Pressable } from 'react-native';
    import { Sunrise, Sun, Sunset, Moon, Wand2 } from 'lucide-react-native';
    import * as Haptics from 'expo-haptics';
    import { useThemeColors } from '../../contexts/ThemeContext';
    import { Spacing, Radius } from '../../constants/spacing';
    import { SLOT_DEFINITIONS, type SlotId } from '../../lib/time-blocking';

    const ICON_MAP = { Sunrise, Sun, Sunset, Moon } as const;

    interface SlotBadgeProps {
      slot: SlotId;
      isAuto: boolean;
      onPress: () => void;
      onLongPress: () => void;
    }

    export const SlotBadge = React.memo(function SlotBadge({ slot, isAuto, onPress, onLongPress }: SlotBadgeProps) {
      const { colors } = useThemeColors();
      const def = SLOT_DEFINITIONS[slot];
      const Icon = isAuto ? Wand2 : ICON_MAP[def.iconName];

      const handlePress = useCallback(() => {
        Haptics.selectionAsync();
        onPress();
      }, [onPress]);

      const handleLongPress = useCallback(() => {
        Haptics.impactAsync(Haptics.ImpactFeedbackStyle.Medium);
        onLongPress();
      }, [onLongPress]);

      return (
        <Pressable
          onPress={handlePress}
          onLongPress={handleLongPress}
          delayLongPress={600}
          style={[
            styles.badge,
            {
              borderColor: isAuto ? (colors.partner ?? colors.brand.soil) : colors.border,
              borderStyle: isAuto ? 'dashed' : 'solid',
              backgroundColor: colors.surface,
            },
          ]}
          accessibilityLabel={`${def.label}${isAuto ? ' (placement automatique)' : ''}`}
        >
          <Icon size={12} color={isAuto ? (colors.partner ?? colors.brand.soil) : colors.textSub} strokeWidth={2.5} />
        </Pressable>
      );
    });

    const styles = StyleSheet.create({
      badge: {
        width: 24, height: 24, borderRadius: Radius.full,
        borderWidth: 1, alignItems: 'center', justifyContent: 'center',
      },
    });
    ```
    Note : si `colors.partner` n'existe pas, fallback `colors.brand.soil` ou `colors.tagColors.bleu`.

    **Étape 2 — Créer `components/time-blocking/SlotPickerSheet.tsx` :**

    Modal pageSheet drag-to-dismiss (conformité CLAUDE.md) avec 4 options + Annuler :
    ```typescript
    import React, { useCallback } from 'react';
    import { Modal, View, Text, TouchableOpacity, StyleSheet, Pressable } from 'react-native';
    import { Sunrise, Sun, Sunset, Moon } from 'lucide-react-native';
    import * as Haptics from 'expo-haptics';
    import { useThemeColors } from '../../contexts/ThemeContext';
    import { ModalHeader } from '../ui/ModalHeader';
    import { Spacing, Radius } from '../../constants/spacing';
    import { FontSize, FontWeight } from '../../constants/typography';
    import { SLOT_DEFINITIONS, SLOT_IDS, type SlotId } from '../../lib/time-blocking';

    const ICON_MAP = { Sunrise, Sun, Sunset, Moon };

    interface SlotPickerSheetProps {
      visible: boolean;
      currentSlot: SlotId | null;
      onClose: () => void;
      onSelect: (slot: SlotId) => void;
    }

    export function SlotPickerSheet({ visible, currentSlot, onClose, onSelect }: SlotPickerSheetProps) {
      const { colors } = useThemeColors();

      const handleSelect = useCallback((slot: SlotId) => {
        Haptics.selectionAsync();
        onSelect(slot);
        onClose();
      }, [onSelect, onClose]);

      return (
        <Modal visible={visible} animationType="slide" presentationStyle="pageSheet" onRequestClose={onClose}>
          <View style={[styles.container, { backgroundColor: colors.bg }]}>
            <ModalHeader title="Choisir un créneau" onClose={onClose} />
            <View style={styles.list}>
              {SLOT_IDS.map(slotId => {
                const def = SLOT_DEFINITIONS[slotId];
                const Icon = ICON_MAP[def.iconName];
                const isSelected = currentSlot === slotId;
                return (
                  <Pressable
                    key={slotId}
                    onPress={() => handleSelect(slotId)}
                    style={[
                      styles.row,
                      { backgroundColor: isSelected ? colors.surface : 'transparent', borderColor: colors.border },
                    ]}
                  >
                    <Icon size={20} color={colors.brand.soil} strokeWidth={2} />
                    <View style={{ flex: 1 }}>
                      <Text style={[styles.label, { color: colors.text }]}>{def.label}</Text>
                      <Text style={[styles.range, { color: colors.textMuted }]}>{def.timeRangeLabel}</Text>
                    </View>
                  </Pressable>
                );
              })}
            </View>
          </View>
        </Modal>
      );
    }

    const styles = StyleSheet.create({
      container: { flex: 1 },
      list: { padding: Spacing['2xl'], gap: Spacing.md },
      row: {
        flexDirection: 'row', alignItems: 'center', gap: Spacing.lg,
        padding: Spacing.xl, borderRadius: Radius.lg, borderWidth: 1,
      },
      label: { fontSize: FontSize.lg, fontWeight: FontWeight.semibold },
      range: { fontSize: FontSize.label, marginTop: 2 },
    });
    ```

    **Étape 3 — Ajouter `setTaskSlot` à `hooks/useVaultTasks.ts` :**

    Nouvelle fonction (après `editTask`, avant `deleteTask`) :
    ```typescript
    const setTaskSlot = useCallback(async (task: Task, slot: SlotId | null) => {
      if (!vaultRef.current) return;

      // Reconstruction de la ligne markdown avec/sans emoji slot
      const SLOT_EMOJI: Record<SlotId, string> = { matin: '☀️', midi: '🍽️', aprem: '☕', soir: '🌙' };

      // Reconstituer le label brut (text + autres emojis existants en queue)
      let taskLine = task.text;
      if (task.recurrence) taskLine += ` 🔁 ${task.recurrence}`;
      if (task.dueDate) taskLine += ` 📅 ${task.dueDate}`;
      if (task.reminderTime) taskLine += ` ⏰ ${task.reminderTime}`;
      if (task.xpOverride !== undefined) taskLine += ` ⭐ ${task.xpOverride}`;

      // Préfixer emoji slot UNIQUEMENT si slot défini (sinon vault reste vierge)
      if (slot) taskLine = `${SLOT_EMOJI[slot]} ${taskLine}`;

      const fullLine = `- [${task.completed ? 'x' : ' '}] ${taskLine}`;

      const content = await vaultRef.current.readFile(task.sourceFile);
      const lines = content.split('\n');
      if (task.lineIndex >= 0 && task.lineIndex < lines.length) {
        lines[task.lineIndex] = fullLine;
        await vaultRef.current.writeFile(task.sourceFile, lines.join('\n'));
      }

      setTasks(prev => prev.map(t => {
        if (t.sourceFile !== task.sourceFile || t.lineIndex !== task.lineIndex) return t;
        return { ...t, timeSlot: slot ?? undefined };
      }));
      setTimeout(triggerWidgetRefresh, 0);
    }, [triggerWidgetRefresh]);
    ```

    Imports à ajouter en haut : `import type { Task, SlotId } from '../lib/types';`
    Ajouter `setTaskSlot` au type `UseVaultTasksResult` (signature `(task: Task, slot: SlotId | null) => Promise<void>`) ET au return final.

    **Étape 4 — Hook completion history dans `toggleTask` :**

    Toujours dans `hooks/useVaultTasks.ts`, à l'intérieur du bloc `if (completed && !wasCompleted)` (autour de la ligne 99), AVANT la boucle des listeners (ou juste après — peu importe, fire-and-forget) :
    ```typescript
    // Phase quick-260516-oj6 : mettre à jour completionHistory (silent catch — non-critique)
    try {
      const { computeAutoSlot, loadHistory, saveCompletion } = await import('../lib/time-blocking');
      const history = await loadHistory();
      const result = computeAutoSlot(task, [], history);
      // saveCompletion fire-and-forget
      saveCompletion(task.text, result.slot).catch(() => { /* silent */ });
    } catch { /* time-blocking — non-critical */ }
    ```
    Note : import dynamique pour éviter cycles et alléger le bundle si non utilisé. Le catch est silencieux (CLAUDE.md pattern : `catch { /* non-critical */ }`).

    **Étape 5 — Brancher SlotBadge dans TaskCard :**

    Étendre `TaskCardProps` :
    ```typescript
    interface TaskCardProps {
      // ... props existantes ...
      slotBadge?: {
        slot: SlotId;
        isAuto: boolean;
        onPress: () => void;
        onLongPress: () => void;
      };
    }
    ```

    Dans le JSX TaskCard, juste avant le bloc des tags badges (ou à côté du texte de la tâche, à droite), conditionner :
    ```tsx
    {slotBadge && (
      <SlotBadge
        slot={slotBadge.slot}
        isAuto={slotBadge.isAuto}
        onPress={slotBadge.onPress}
        onLongPress={slotBadge.onLongPress}
      />
    )}
    ```
    Imports : `import { SlotBadge } from './time-blocking/SlotBadge';` et `import type { SlotId } from '../lib/types';`

    **Étape 6 — Brancher dans `tasks.tsx` :**

    Dans `renderItem` (ou équivalent qui rend TaskCard), conditionner le passage de `slotBadge` :
    ```tsx
    const renderItem = useCallback(({ item, section }: { item: Task & { _isAutoSlot?: boolean }; section: TaskSection }) => {
      const slotBadge = viewMode === 'journee' && section.slotId ? {
        slot: section.slotId,
        isAuto: item._isAutoSlot ?? false,
        onPress: () => {
          setPickerTask(item);
        },
        onLongPress: async () => {
          // Long press = renvoyer au backlog
          try {
            await setTaskSlot(item, null);
            // Toast feedback
            toast?.show?.('Tâche renvoyée au backlog', 'success');
          } catch (e) {
            Alert.alert('Erreur', 'Impossible de modifier le créneau.');
          }
        },
      } : undefined;

      return (
        <TaskCard
          task={item}
          onToggle={handleToggleTask}
          onSkip={handleSkipTask}
          onLongPress={() => handleLongPressTask(item)}
          showSource={viewMode === 'liste'}
          profiles={profiles}
          pointsOnComplete={POINTS_PER_TASK}
          slotBadge={slotBadge}
        />
      );
    }, [viewMode, handleToggleTask, handleSkipTask, handleLongPressTask, profiles, setTaskSlot, toast]);
    ```

    Ajouter le state pour le picker :
    ```typescript
    const [pickerTask, setPickerTask] = useState<Task | null>(null);
    ```

    Et le composant SlotPickerSheet en bas du JSX (avant la fermeture du SafeAreaView) :
    ```tsx
    <SlotPickerSheet
      visible={pickerTask !== null}
      currentSlot={pickerTask?.timeSlot ?? null}
      onClose={() => setPickerTask(null)}
      onSelect={async (slot) => {
        if (!pickerTask) return;
        try {
          await setTaskSlot(pickerTask, slot);
        } catch (e) {
          Alert.alert('Erreur', 'Impossible de définir le créneau.');
        }
      }}
    />
    ```

    Imports à ajouter : `import { SlotPickerSheet } from '../../components/time-blocking/SlotPickerSheet';`

    Et l'accès au hook : importer `setTaskSlot` depuis `useVault()` (vérifier que useVault le re-expose — sinon depuis le contexte plus profond ; dans le doute, ajouter `setTaskSlot: tasksHook.setTaskSlot` dans le return du useVaultInternal qui assemble VaultState — fichier `hooks/useVault.ts` ligne ~2755).

    **Pitfalls à éviter :**
    - L'emoji marker doit être PRÉFIXÉ au label (début de ligne après `- [ ] `), PAS suffixé
    - Long press `delayLongPress={600}` exactement (per CONTEXT)
    - Ne JAMAIS modifier `completeTask` / `POINTS_PER_TASK` / `useGamification` / `useFarm` / `useGarden`
    - completionHistory update est fire-and-forget, JAMAIS bloquant
    - L'auto-placement est purement en mémoire → ne PAS appeler `setTaskSlot` pour les placements `'time'`/`'history'`/`'file'`/`'nextfit'` (sinon pollution vault non désirée)
    - `setTaskSlot(task, null)` doit supprimer l'emoji du markdown et `timeSlot` du state Task
    - Pas de perspective dans les transforms (CLAUDE.md)
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx jest --no-coverage lib/__tests__/parser.test.ts lib/__tests__/parser-slot.test.ts lib/__tests__/auto-placement.test.ts</automated>
  </verify>
  <done>
    - SlotBadge visible sur chaque TaskCard en mode Journée
    - Variante `isAuto` rend l'icône Wand2 en pointillés partner-color
    - Variante explicit rend l'icône slot (Sunrise/Sun/Sunset/Moon) en bordure solide
    - Tap badge ouvre SlotPickerSheet pageSheet avec les 4 options
    - Sélection écrit emoji marker dans markdown vault + met à jour state Task
    - Long press (600ms + haptic medium) supprime emoji + retire timeSlot
    - completionHistory mis à jour à chaque toggleTask completed (silent catch)
    - Tests Jest (parser.test, parser-slot.test, auto-placement.test) passent tous
    - `npx tsc --noEmit` ne produit aucune nouvelle erreur
    - Gamification non-régression : `POINTS_PER_TASK`, `useGamification`, `useFarm`, `useGarden` non modifiés
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` : zéro nouvelle erreur (les pré-existantes MemoryEditor.tsx, cooklang.ts, useVault.ts restent ignorées)
- `npx jest --no-coverage lib/__tests__/parser.test.ts lib/__tests__/parser-slot.test.ts lib/__tests__/auto-placement.test.ts` : tous verts
- Lancement app (`npx expo run:ios --device`) :
  1. Écran Tâches en mode Liste : tout identique à avant (sections par fichier, filter chips, swipe to delete, etc.)
  2. Toggle "Journée" : day pills apparaissent, sections deviennent Matin/Midi/Aprem/Soir
  3. Tâche du jour avec `reminderTime='07:30'` → apparaît dans Matin avec badge Wand2 pointillés
  4. Tap badge → SlotPickerSheet → choisir "Soir" → tâche déplace vers Soir, badge devient Moon solid, vault contient `- [ ] 🌙 ...`
  5. Long-press badge → tâche revient en placement auto, vault ne contient plus 🌙
  6. Bande "Temps libre" sous les slots peu chargés
  7. Summary pill affiche le total libre du jour
  8. Reload app → mode Journée toujours actif (SecureStore persisté)
  9. Compléter une tâche → XP/coins/loot box continuent de tomber normalement
</verification>

<success_criteria>
- Mode toggle Liste/Journée fonctionnel, persistant, sans régression Liste
- Auto-placement chaîne de décision exhaustivement testée (5 sources)
- Parser bidirectionnel pour emojis slot (verrou utilisateur, pas de pollution vault)
- CACHE_VERSION bumpé proprement (13→14) avec commentaire
- SlotBadge tap (picker) + long-press (backlog) fonctionnels avec haptics
- completionHistory mis à jour silencieusement, jamais bloquant
- Zéro impact gamification (XP/level/coins/streak/loot box/jardin/ferme intouchés)
- Tests Jest existants verts + 2 nouvelles suites vertes
- Tout en français, Lucide uniquement (aucun emoji UI), useThemeColors() partout
- `npx tsc --noEmit` propre
</success_criteria>

<output>
Après complétion : créer `.planning/quick/260516-oj6-tasks-time-blocking-mode-journee/260516-oj6-SUMMARY.md` listant les fichiers créés/modifiés, les patterns établis (regex emoji slot anchored début, chaîne de décision computeAutoSlot, completionHistory SecureStore FIFO 10), les pitfalls évités (pollution vault, perspective transform, regression gamif), et le statut tests (tsc + 3 suites Jest).
</output>
