---
phase: quick-260414-l12
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/mascot/BuildingShopSheet.tsx
  - components/mascot/BuildingDetailSheet.tsx
autonomous: true
requirements: [VISUAL-COZY]
must_haves:
  truths:
    - "Les modals batiments ont un look cozy/farm game distinct du style flat generique"
    - "Les boutons ont un effet 3D (ombre portee dessous) et feedback spring au tap"
    - "La barre de progression a un style ameliore avec lueur et animation"
    - "Les cartes sections ont un fond terre/bois chaud"
    - "Le header est decoratif avec banniere et emoji agrandi"
  artifacts:
    - path: "components/mascot/BuildingShopSheet.tsx"
      provides: "Modal boutique avec style cozy farm"
    - path: "components/mascot/BuildingDetailSheet.tsx"
      provides: "Modal detail avec animations spring et barre amelioree"
  key_links:
    - from: "BuildingShopSheet.tsx"
      to: "react-native-reanimated"
      via: "useSharedValue + useAnimatedStyle pour animations entree et boutons"
    - from: "BuildingDetailSheet.tsx"
      to: "react-native-reanimated"
      via: "useSharedValue + withSpring pour barre progression et boutons 3D"
---

<objective>
Ameliorer le visuel des modals BuildingShopSheet et BuildingDetailSheet pour un look cozy/farm game.

Purpose: Les modals actuelles ont un style flat/generique (fond blanc, boutons plats, barre 6px). On veut un look immersif farm game : header decoratif avec banniere, fond parchment chaud, cartes terre/bois, boutons 3D avec ombre, animations spring a l'entree, et barre de progression amelioree avec lueur.
Output: Deux fichiers .tsx mis a jour avec le nouveau look cozy.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@components/mascot/BuildingShopSheet.tsx
@components/mascot/BuildingDetailSheet.tsx
@constants/spacing.ts
@constants/typography.ts
@constants/shadows.ts
@contexts/ThemeContext.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: BuildingShopSheet — style cozy farm + animations spring</name>
  <files>components/mascot/BuildingShopSheet.tsx</files>
  <action>
Transformer le BuildingShopSheet en look cozy farm game :

**Imports a ajouter :**
- `Animated` depuis `react-native-reanimated` (useSharedValue, useAnimatedStyle, withSpring, withTiming, FadeIn, SlideInDown)
- Remplacer `View`/`TouchableOpacity` par `Animated.View` la ou necessaire

**Header decoratif :**
- Banniere decorative : fond warm gradient simule via deux View empiles (colors.cardAlt + overlay leger dore rgba(255,215,0,0.08))
- Emoji marteau/construction agrandi (fontSize 28) a gauche du titre
- Affichage du solde pieces 🍃 dans le header (coins prop deja disponible)
- Separateur decoratif fin sous le header (borderBottom dore subtil rgba(255,215,0,0.15))

**Fond parchment :**
- Sheet background : utiliser `colors.cardAlt` au lieu de `colors.card` pour un fond plus chaud
- Ajouter un overlay subtil en haut du sheet (LinearGradient simule via View avec opacity) pour effet parchment

**Cartes batiment style terre/bois :**
- Augmenter borderRadius a Radius['2xl'] (20)
- Ajouter borderWidth: 1.5 avec borderColor plus prononce (colors.border au lieu de borderLight)
- Ombre plus forte : Shadows.md sur chaque carte
- SpriteBox : fond circulaire derriere le sprite (backgroundColor colors.card, borderRadius Radius.full, taille 56x56)
- Padding plus genereux : Spacing['2xl'] dans les cartes

**Boutons 3D :**
- Bouton construire : ajouter une ombre inferieure simulee via un View wrapper avec:
  - Vue principale du bouton
  - Vue ombre 3px en dessous (meme largeur, darker du primary, borderBottomLeftRadius + borderBottomRightRadius)
- borderRadius augmente a Radius.lg (12)
- paddingVertical a Spacing.lg (10) et paddingHorizontal a Spacing['3xl'] (20)

**Animations spring (react-native-reanimated obligatoire) :**
- Constante module : `const SPRING_CONFIG = { damping: 12, stiffness: 180 }` (per CLAUDE.md)
- Animation entree des cartes : `FadeIn.delay(index * 80).springify()` sur chaque buildingRow via Animated.View
- Les cartes verrouilees apparaissent aussi avec FadeIn mais delayees apres les disponibles

**Nettoyage couleurs hardcodees :**
- `#4ADE80` (vert) → `colors.success`
- `#FFFFFF` sur bouton → `colors.bg` (ou blanc via le theme)
- Garder '#FFD700' uniquement comme constante cosmétique doree (cf decision Phase 4)
  </action>
  <verify>
    <automated>cd /Users/gabrielwaltio/Documents/family-vault && npx tsc --noEmit 2>&1 | grep -i "BuildingShopSheet" || echo "OK — no type errors in BuildingShopSheet"</automated>
  </verify>
  <done>BuildingShopSheet a un look cozy farm : header decoratif avec banniere, cartes terre/bois avec ombres, boutons 3D, animations spring a l'entree, couleurs chaudes parchment. Aucune couleur hardcodee sauf dore cosmetique.</done>
</task>

<task type="auto">
  <name>Task 2: BuildingDetailSheet — style cozy farm + barre progression amelioree + animations</name>
  <files>components/mascot/BuildingDetailSheet.tsx</files>
  <action>
Transformer le BuildingDetailSheet en look cozy farm game :

**Imports a ajouter :**
- `Animated` depuis `react-native-reanimated` (useSharedValue, useAnimatedStyle, withSpring, withTiming, FadeIn, useEffect as reanimatedUseEffect)
- Conserver expo-haptics existant

**Spring config :**
- Constante module : `const SPRING_CONFIG = { damping: 12, stiffness: 180 }`

**Header decoratif :**
- Emoji du batiment agrandi (fontSize 32) dans un cercle decoratif (backgroundColor colors.cardAlt, borderRadius Radius.full, width/height 48)
- Nom + niveau sur deux lignes (nom en gras, niveau en badge dore petit)
- Badge niveau : fond '#FFD700' (cosmetique), color '#7B5300', borderRadius Radius.full, paddingHorizontal Spacing.xl, fontSize FontSize.label

**Sprite display ameliore :**
- Fond decoratif derriere le sprite : cercle doux (colors.cardAlt, borderRadius Radius['3xl'], size 96x96)
- Ombre legere sous le sprite (Shadows.sm)
- Animation entree du sprite : FadeIn.springify() + scale de 0.8 a 1

**Sections style terre/bois :**
- Meme style que Task 1 : borderRadius Radius['2xl'], borderWidth 1.5, Shadows.md
- Titre section avec petit emoji a gauche (⚙️ Production, 📦 Collecte, ⬆️ Amelioration)
- FadeIn.delay(N * 100).springify() sur chaque section

**Barre de progression amelioree :**
- Hauteur augmentee : 10px au lieu de 6px
- borderRadius 5 pour les deux (bg + fill)
- Fond de la barre : rgba(0,0,0,0.06) avec borderWidth 0.5 borderColor rgba(0,0,0,0.04)
- Fill anime : useSharedValue pour la largeur, withTiming(progressRatio, { duration: 600 }) a chaque changement
- Lueur sur le fill : ajouter un shadow inset simule via un View overlay blanc semi-transparent (opacity 0.3, height 4px, top 1px) pour effet glossy
- Si isFull : fill clignote doucement (withRepeat + withTiming opacity 0.7-1.0) pour attirer l'attention
- Indicateur texte sous la barre : "{elapsed}/{total} min" en FontSize.xs, color textMuted

**Boutons 3D :**
- Meme pattern que Task 1 : View wrapper avec ombre 3px inferieure
- Bouton collecte : fond `colors.success` au lieu de '#4ADE80'
- Bouton amelioration : fond primary (inchange), ombre plus foncee
- Bouton reparation : garder le orange mais ajouter l'ombre 3D
- Animation spring au press : scale 0.95 via useSharedValue + onPressIn/onPressOut avec Animated.View wrapping le Pressable
  NOTE: utiliser Pressable au lieu de TouchableOpacity pour les boutons principaux (collecte, amelioration) afin de capturer onPressIn/onPressOut

**Nettoyage couleurs hardcodees :**
- `#4ADE80` → `colors.success`
- `#FFFFFF` sur boutons → `colors.bg`
- `#F59E0B` (warning full) → `colors.warning`
- `rgba(0,0,0,0.08)` progressBar → constante locale
- Garder '#FFD700' et '#7B5300' comme constantes cosmetiques dorees (decision Phase 4)
- Garder '#FF9800' repair comme constante cosmetique orange
  </action>
  <verify>
    <automated>cd /Users/gabrielwaltio/Documents/family-vault && npx tsc --noEmit 2>&1 | grep -i "BuildingDetailSheet" || echo "OK — no type errors in BuildingDetailSheet"</automated>
  </verify>
  <done>BuildingDetailSheet a un look cozy farm : header avec emoji cercle + badge niveau dore, sprite avec fond decoratif + animation entree, sections terre/bois avec emojis, barre de progression 10px avec lueur glossy et animation fill + clignotement si plein, boutons 3D avec animation spring au press. Couleurs nettoyees vers tokens theme sauf cosmetiques dorees.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passe sans nouvelles erreurs sur les deux fichiers
- Les deux modals s'affichent correctement sur device (checkpoint visuel par l'utilisateur lors de l'execution)
- react-native-reanimated est utilise pour TOUTES les animations (pas RN Animated)
- useThemeColors() pour toutes les couleurs dynamiques
- Spring configs en constantes module
</verification>

<success_criteria>
- Les deux modals ont un look cozy/farm game distinct du style flat precedent
- Animations spring fluides a l'ouverture (cartes, sprite, sections)
- Boutons avec effet 3D visible (ombre inferieure)
- Barre de progression amelioree avec lueur et animation
- Zero regression fonctionnelle (build, collect, upgrade, repair marchent)
- TypeScript compile sans erreur
</success_criteria>

<output>
After completion, create `.planning/quick/260414-l12-am-liorer-le-visuel-des-modals-buildings/260414-l12-SUMMARY.md`
</output>
