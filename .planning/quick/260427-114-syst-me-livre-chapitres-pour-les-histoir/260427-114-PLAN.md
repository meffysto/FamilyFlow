---
phase: quick-260427-114
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/types.ts
  - lib/parser.ts
  - lib/story-books.ts
  - lib/ai-service.ts
  - app/(tabs)/stories.tsx
  - components/stories/StoryPlayer.tsx
autonomous: true
requirements:
  - LIVRE-01  # modèle de données livre/chapitre rétrocompat
  - LIVRE-02  # helpers groupage + casting verrouillé
  - LIVRE-03  # extension generateBedtimeStory (book context + trancheAge + memorySummary)
  - LIVRE-04  # UI : sélecteur tranche d'âge création + modal "ajouter chapitre" + bouton "écrire le chapitre suivant" + liste regroupée par livre

must_haves:
  truths:
    - "Une histoire existante (sans livreId) reste lisible et apparaît comme un livre mono-chapitre — aucune migration."
    - "À la création d'une nouvelle histoire, l'utilisateur choisit une tranche d'âge (3-5 / 6-8 / 9+) qui est sauvegardée dans le frontmatter."
    - "Depuis une histoire terminée (StoryPlayer onFinish → FinStep), un bouton « 📖 Écrire le chapitre suivant » est visible."
    - "Cliquer ce bouton lance la génération d'un chapitre 2 (puis 3, 4...) qui réutilise univers, voix, multi-voix on/off et tranche d'âge VERROUILLÉS du chapitre précédent."
    - "Le prompt Claude du chapitre N reçoit : casting verrouillé (slugs déjà introduits), texte intégral du chapitre N-1, résumés des chapitres antérieurs, et règles de tranche d'âge — Claude renvoie aussi un champ memorySummary persisté."
    - "L'onglet Stories regroupe les histoires par livreId : 1 chapitre → carte simple comme avant ; 2+ chapitres → carte avec liste numérotée des chapitres."
  artifacts:
    - path: "lib/story-books.ts"
      provides: "Helpers livres : groupStoriesByBook, getBookCasting, getNextChapterNumber, buildBookContextForPrompt, slugifyBookTitle"
    - path: "lib/types.ts"
      provides: "Champs optionnels BedtimeStory : livreId, livreTitre, chapitre, chapitreTitre, personnages, memorySummary, trancheAge"
    - path: "lib/parser.ts"
      provides: "parseBedtimeStory et serializeBedtimeStory lisent/écrivent les nouveaux champs (rétrocompat : tous optionnels)"
    - path: "lib/ai-service.ts"
      provides: "StoryGenerationConfig accepte book? et trancheAge?, system prompt enrichi, JSON output inclut memorySummary"
    - path: "app/(tabs)/stories.tsx"
      provides: "Sélecteur tranche d'âge à l'étape personnaliser ; flux 'ajouter chapitre' réutilisant GenerationStep avec champs verrouillés ; liste regroupée par livre"
    - path: "components/stories/StoryPlayer.tsx"
      provides: "Bouton « Écrire le chapitre suivant » dans l'écran fin (FinStep)"
  key_links:
    - from: "FinStep (stories.tsx)"
      to: "GenerationStep avec book context"
      via: "goTo({ etape: 'generation', book: { livreId, livreTitre, chapitre, lockedCasting, previousChapterFullText, olderSummaries }, trancheAge })"
      pattern: "etape.*generation.*book"
    - from: "generateBedtimeStory (ai-service.ts)"
      to: "Claude API"
      via: "system prompt avec blocs CASTING VERROUILLÉ + MÉMOIRE NARRATIVE + tranche d'âge"
      pattern: "CASTING VERROUILLÉ|MÉMOIRE NARRATIVE"
    - from: "parseBedtimeStory (parser.ts)"
      to: "frontmatter livre_id/livre_titre/chapitre/personnages/memory_summary/tranche_age"
      via: "parseStoryFrontmatter (parseur YAML maison)"
      pattern: "livre_id|chapitre|tranche_age"
    - from: "stories.tsx liste"
      to: "groupStoriesByBook"
      via: "useMemo qui regroupe filteredStories par livreId (fallback: id si livreId absent)"
      pattern: "groupStoriesByBook"
---

<objective>
Transformer les histoires du soir indépendantes en LIVRES de plusieurs CHAPITRES, avec continuité narrative (mémoire des chapitres précédents transmise à Claude), casting évolutif verrouillé, et tranche d'âge appliquée au vocabulaire et thèmes.

Purpose: Les enfants (et leur parent) veulent des sagas qui durent. Aujourd'hui chaque histoire est isolée. On veut pouvoir lancer un « tome 1 » et continuer chapitre après chapitre, avec les mêmes personnages qui réapparaissent et une vraie cohérence narrative — sans casser une seule histoire existante.

Output:
- Modèle de données étendu (rétrocompat 100%)
- Helpers `lib/story-books.ts`
- Prompt Claude enrichi (book context + tranche d'âge + memorySummary en sortie)
- UI : sélecteur tranche d'âge, bouton « écrire le chapitre suivant », flux génération chapitre N avec champs verrouillés, liste regroupée par livre
</objective>

<context>
@CLAUDE.md
@.planning/STATE.md
@lib/types.ts
@lib/parser.ts
@lib/ai-service.ts
@lib/stories.ts
@lib/story-characters.ts
@app/(tabs)/stories.tsx
@components/stories/StoryPlayer.tsx

<interfaces>
<!-- Contrats existants à respecter (extraits du codebase) -->

From lib/types.ts (BedtimeStory actuel — on AJOUTE des champs optionnels) :
```typescript
export interface BedtimeStory {
  id: string;
  titre: string;
  enfant: string;
  enfantId: string;
  univers: StoryUniverseId;
  detail?: string;
  texte: string;
  date: string;
  duree_lecture: number;
  voice: StoryVoiceConfig;
  length?: StoryLength;
  spectacle?: boolean;
  audioMode?: StoryAudioMode;
  ambienceVolume?: number;
  script?: StoryScript;
  alignment?: StoryAudioAlignment;
  version: number;
  sourceFile: string;
}
```

From lib/ai-service.ts (StoryGenerationConfig — on AJOUTE book? et trancheAge?) :
```typescript
export interface StoryGenerationConfig {
  enfantAnon: string;
  enfantAge: string;
  universId: string;
  universTitre: string;
  detail?: string;
  language: 'fr' | 'en';
  length?: StoryLength;
  context: StoryPersonalizationContext;
  spectacle?: boolean;
  availableSfxTags?: string[];
  multiVoice?: boolean;
}
```

From app/(tabs)/stories.tsx (state machine wizard) :
```typescript
type StoryFlowStep =
  | { etape: 'choisir_enfant' }
  | { etape: 'choisir_univers'; enfantId; enfantName }
  | { etape: 'personnaliser'; enfantId; enfantName; universId }
  | { etape: 'generation'; enfantId; enfantName; universId; detail; length }
  | { etape: 'fin'; histoire }
  | { etape: 'replay'; histoire };
```
On ÉTEND `personnaliser` et `generation` avec `trancheAge?` et `book?` (tous deux optionnels = rétrocompat).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 — Données : types BedtimeStory + parser/serializer + helpers lib/story-books.ts</name>
  <files>lib/types.ts, lib/parser.ts, lib/story-books.ts</files>
  <action>
Étendre le modèle de données pour supporter livres/chapitres, en gardant 100% de rétrocompat (tous nouveaux champs OPTIONNELS).

1) **lib/types.ts** — étendre `BedtimeStory` avec champs optionnels :
```typescript
livreId?: string;
livreTitre?: string;
chapitre?: number;
chapitreTitre?: string;
personnages?: string[];
memorySummary?: string;
trancheAge?: '3-5' | '6-8' | '9+';
```
Ajouter aussi un type exporté : `export type StoryAgeRange = '3-5' | '6-8' | '9+';` et l'utiliser dans `BedtimeStory.trancheAge`.

2) **lib/parser.ts** :
- `serializeBedtimeStory` : ajouter émission YAML si présents :
  - `livre_id: <slug>` (snake_case dans le frontmatter, camelCase côté TS — convention déjà utilisée pour `voice_engine`)
  - `livre_titre: "..."` (quoter)
  - `chapitre: <number>`
  - `chapitre_titre: "..."` (quoter)
  - `personnages: [slug1, slug2]` — sérialiser sur UNE ligne format `[a, b, c]` (le parseur maison ne gère pas les séquences multi-lignes ; ce format reste lisible et roundtrip-safe avec un parser simple)
  - `memory_summary: "..."` (quoter, échapper les `"` internes)
  - `tranche_age: 3-5|6-8|9+`
- `parseBedtimeStory` : lire chacun de ces champs depuis `d.<key>`. Pour `personnages` : trim crochets, split sur `,`, trim chaque slug, filter vide. Pour `chapitre` : `Number(d.chapitre)` si défini et non NaN. Pour `tranche_age` : valider via `new Set(['3-5','6-8','9+'])`. Pour `memory_summary` : déjà déquoté par `parseStoryFrontmatter`.

3) **NOUVEAU lib/story-books.ts** :
```typescript
import type { BedtimeStory, StoryUniverseId, StoryAgeRange } from './types';

export interface StoryBook {
  livreId: string;
  livreTitre: string;
  enfantId: string;
  universId: StoryUniverseId;
  trancheAge?: StoryAgeRange;
  chapters: BedtimeStory[]; // triés par chapitre asc (chapitre 1 d'abord)
  casting: string[];        // union ordonnée des personnages
  isImplicit: boolean;       // true si le livre n'a pas de livreId (mono-chapitre legacy)
}

/**
 * Regroupe les histoires en livres.
 * - Histoires SANS livreId → chacune devient un livre implicite mono-chapitre (livreId = story.id, isImplicit=true)
 * - Histoires AVEC livreId → groupées et triées par chapitre asc (fallback : ordre par date si chapitre absent)
 */
export function groupStoriesByBook(stories: BedtimeStory[]): StoryBook[];

/** Union ordonnée (premier chapitre d'apparition d'abord) des slugs personnages. */
export function getBookCasting(chapters: BedtimeStory[]): string[];

/** max(chapitre) + 1, ou 2 si aucun chapitre numéroté trouvé (= le 1er chapitre était implicite). */
export function getNextChapterNumber(chapters: BedtimeStory[]): number;

/**
 * Construit le payload "book" envoyé à generateBedtimeStory.
 * - previousChapterFullText : `texte` du chapitre N-1 entier
 * - olderSummaries : pour les chapitres < N-1, [{chapitre, titre, summary: memorySummary || (titre + " — résumé manquant")}]
 *   triés par chapitre asc
 */
export function buildBookContextForPrompt(book: StoryBook): {
  livreId: string;
  livreTitre: string;
  chapitre: number;
  lockedCasting: string[];
  previousChapterFullText: string;
  olderSummaries: Array<{ chapitre: number; titre: string; summary: string }>;
};

/**
 * Slugify un titre de livre en kebab-case ASCII safe.
 * - lowercase
 * - normaliser les accents (NFD + strip diacritics)
 * - remplacer non [a-z0-9]+ par '-'
 * - trim '-' debut/fin
 * - max 60 chars
 * Append `-<6-char timestamp>` pour garantir l'unicité (Date.now().toString(36).slice(-6)).
 */
export function slugifyBookTitle(titre: string): string;
```
Pas de dépendance externe (utiliser `String.normalize('NFD').replace(/[̀-ͯ]/g, '')`).

4) Vérifier qu'aucune autre lecture frontmatter ne casse en présence des nouveaux champs (le parseur YAML maison `parseStoryFrontmatter` ignore silencieusement les clés inconnues — OK).

**Conventions** :
- Pas de couleurs, pas de UI ici. Code pur TS.
- Commentaires en français.
- `useThemeColors()` non concerné.
- Cache `lib/vault-cache.ts` : stories NON cachées (vérifié dans CLAUDE.md), donc PAS besoin de bumper `CACHE_VERSION`.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>
- `lib/types.ts` étend BedtimeStory + exporte StoryAgeRange
- `lib/parser.ts` roundtrip les 7 nouveaux champs (test manuel : sérialise puis reparse → identique)
- `lib/story-books.ts` créé avec les 5 helpers exportés
- `npx tsc --noEmit` passe (tolérance pour les erreurs pré-existantes listées dans CLAUDE.md)
- Une histoire existante sans aucun nouveau champ continue de parser sans warning ni perte de données
  </done>
</task>

<task type="auto">
  <name>Task 2 — Prompt Claude : extension generateBedtimeStory (book + trancheAge + memorySummary)</name>
  <files>lib/ai-service.ts</files>
  <action>
Étendre `generateBedtimeStory` (lib/ai-service.ts:975) pour accepter un contexte de livre et une tranche d'âge.

1) **Étendre `StoryGenerationConfig`** (vers ligne 958) :
```typescript
trancheAge?: '3-5' | '6-8' | '9+';
book?: {
  livreId: string;
  livreTitre: string;
  chapitre: number;       // numéro du chapitre EN COURS de génération
  lockedCasting: string[]; // slugs personnages déjà introduits dans les chapitres précédents
  previousChapterFullText: string;
  olderSummaries: Array<{ chapitre: number; titre: string; summary: string }>;
};
```

2) **Bloc tranche d'âge** — Construire `ageRangeRules` (string) :
   - Si `trancheAge='3-5'` : `Vocabulaire TRÈS simple. Phrases COURTES (8-12 mots). Vocabulaire concret (objets, animaux, couleurs). Émotions de base (content, peur, amour). Pas d'abstraction.`
   - Si `trancheAge='6-8'` : `Vocabulaire riche mais accessible. Phrases moyennes (12-18 mots). Concepts narratifs simples (amitié, courage, partage). Petits dilemmes résolus.`
   - Si `trancheAge='9+'` : `Vocabulaire varié. Phrases construites (15-25 mots). Thèmes plus profonds possibles (responsabilité, identité, mystères). Sous-texte autorisé.`
   - Si non défini : pas de bloc (fallback comportement actuel `Vocabulaire adapté à ${enfantAge}`).
   Quand `ageRangeRules` est défini, REMPLACE la ligne `- Vocabulaire adapté à ${enfantAge}` dans les RÈGLES STRICTES par `- ${ageRangeRules}`.

3) **Bloc casting verrouillé** (uniquement si `book && multiVoiceEnabled`) — construire `castingLockedSection` :
   - Importer `getUniversCasting` (déjà importé) pour récupérer le casting complet de l'univers.
   - Splitter en `lockedChars` (slugs ∈ `book.lockedCasting`) et `availableChars` (les autres).
   - Format :
   ```
   CASTING VERROUILLÉ — personnages déjà introduits dans les chapitres précédents :
     - "<slug>" : <label> — <hint>
     ...
   Ces personnages doivent garder leur personnalité et peuvent réapparaître. Tu DOIS en utiliser au moins 1 dans ce chapitre s'il y en a.

   CASTING DISPONIBLE — personnages que tu peux introduire (max 1 NOUVEAU dans ce chapitre) :
     - "<slug>" : <label> — <hint>
     ...
   ```
   Ce bloc REMPLACE celui généré par `buildCharactersPromptSection` quand `book` est présent (sinon comportement inchangé).

4) **Bloc mémoire narrative** (uniquement si `book` présent) — construire `bookMemorySection` :
   ```
   LIVRE EN COURS — "${book.livreTitre}" — Chapitre ${book.chapitre}

   MÉMOIRE NARRATIVE :
   ${book.olderSummaries.length > 0 ? `Résumés des chapitres antérieurs :\n${book.olderSummaries.map(s => `  Chapitre ${s.chapitre} — "${s.titre}" : ${s.summary}`).join('\n')}\n` : ''}
   Texte intégral du chapitre PRÉCÉDENT (chapitre ${book.chapitre - 1}) :
   """
   ${book.previousChapterFullText}
   """

   RÈGLES DE CONTINUITÉ :
   - NE FAIS PAS de récap/résumé du chapitre précédent au début — l'enfant l'a écouté, pars directement dans la suite
   - Reprends le fil narratif là où le chapitre précédent s'arrêtait (heure de la journée, lieu, état émotionnel)
   - Garde la cohérence des personnages verrouillés (personnalité, façon de parler, relations)
   - Termine ce chapitre sur une note APAISÉE (pas de cliffhanger angoissant — c'est une histoire du soir)
   ```

5) **Output JSON** — toujours demander un champ supplémentaire `memorySummary` :
   - Modifier `outputFormat` pour inclure `"memorySummary": "..."` dans le template (avant la fermeture du JSON).
   - Ajouter dans le prompt : `Le champ "memorySummary" doit être un résumé NEUTRE en 4-5 phrases focalisé sur ce qui pourrait revenir dans un chapitre futur (lieux, personnages introduits, objets clés, état final du héros). Pas d'émotion, pas de style narratif — c'est une note de continuité.`

6) **Insertion dans le system prompt** : ajouter `${bookMemorySection}${castingLockedSection}` après `${charactersRules}` (et le `charactersRules` standard ne doit pas être ajouté quand `book` est présent — le `castingLockedSection` le remplace).

7) **Garder strict le reste** : SFX/spectacle/multi-voix/alignment/performance tags inchangés. `max_tokens` : si `book` présent, ajouter `+400` au budget pour absorber le `memorySummary`.

**Important** : pas de breaking change — appel sans `book` ni `trancheAge` = comportement strictement identique à aujourd'hui.

8) **Logs DEV** : sous `if (__DEV__)`, logger `{ hasBook: !!story.book, chapitre, lockedCount, availableCount, trancheAge }` pour diagnostic.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>
- `StoryGenerationConfig` accepte `book?` et `trancheAge?` typés correctement
- Le system prompt inclut conditionnellement les blocs (CASTING VERROUILLÉ + MÉMOIRE NARRATIVE + règles tranche d'âge)
- Le JSON de sortie inclut TOUJOURS `memorySummary` (extractable via `JSON.parse(rawText).memorySummary`)
- Appel SANS book/trancheAge → prompt identique à avant (rétrocompat)
- `npx tsc --noEmit` passe
  </done>
</task>

<task type="auto">
  <name>Task 3 — UI : sélecteur tranche d'âge + bouton « Écrire le chapitre suivant » + flux chapitre verrouillé + liste regroupée par livre</name>
  <files>app/(tabs)/stories.tsx, components/stories/StoryPlayer.tsx</files>
  <action>
Brancher tout le flux UI sur les fondations posées par les tasks 1 & 2.

**A. Étendre la state machine wizard (stories.tsx, vers ligne 52-57)** :
```typescript
type BookContext = {
  livreId: string;
  livreTitre: string;
  chapitre: number;
  lockedCasting: string[];
  previousChapterFullText: string;
  olderSummaries: Array<{ chapitre: number; titre: string; summary: string }>;
};

type StoryFlowStep =
  | { etape: 'choisir_enfant' }
  | { etape: 'choisir_univers'; enfantId; enfantName }
  | { etape: 'personnaliser'; enfantId; enfantName; universId; book?: BookContext; trancheAge?: StoryAgeRange }
  | { etape: 'generation'; enfantId; enfantName; universId; detail; length; book?: BookContext; trancheAge?: StoryAgeRange }
  | { etape: 'fin'; histoire }
  | { etape: 'replay'; histoire };
```

**B. Étape « personnaliser » — sélecteur tranche d'âge (3 pills)** :
- Si `step.book` est présent : NE PAS afficher le sélecteur (déjà verrouillé par le livre — `step.trancheAge` est figé). Au-dessus, afficher un bandeau lecture seule : `📖 {livreTitre} — Chapitre {chapitre}` + ligne `🔒 Univers, voix, multi-voix et tranche d'âge sont verrouillés par le livre`.
- Si `step.book` ABSENT (création initiale) : ajouter un bloc avec 3 pills `3-5 ans` / `6-8 ans` / `9+ ans`. Default calculé depuis le profil enfant : trouver `Profile.dateNaissance` (si dispo), calculer l'âge en années, mapper :
  - âge < 6 → '3-5'
  - 6 ≤ âge < 9 → '6-8'
  - âge ≥ 9 → '9+'
  - Si dateNaissance indisponible → '6-8' par défaut.
  - Stocker dans un local state `selectedTrancheAge: StoryAgeRange`.
- Au clic « Lancer la génération » (vers ligne 997), passer `trancheAge: selectedTrancheAge` (et `book: step.book` si présent) dans `goTo({ etape: 'generation', ..., trancheAge, book })`.

**C. Étape « generation » — passer book + trancheAge à generateBedtimeStory** :
- Dans `GenerationStep`, étendre la `useCallback` de `generate` (vers ligne 1915) pour passer `book: step.book` et `trancheAge: step.trancheAge` au config.
- Après réception de la réponse Claude, lire `parsed.memorySummary` (peut être absent si Claude n'a pas suivi → fallback `''`).
- Construire le `BedtimeStory` final en injectant :
  - Si `step.book` présent : `livreId = step.book.livreId`, `livreTitre = step.book.livreTitre`, `chapitre = step.book.chapitre`, `chapitreTitre = parsed.titre`.
  - Si `step.book` ABSENT (1er chapitre d'un nouveau livre potentiel) : `livreId = slugifyBookTitle(parsed.titre)`, `livreTitre = parsed.titre`, `chapitre = 1`, `chapitreTitre = parsed.titre`.
  - `personnages` : extraire les slugs uniques des `script.beats` de type dialogue (si script existe), sinon `[]`.
  - `memorySummary = parsed.memorySummary || undefined`.
  - `trancheAge = step.trancheAge`.
- Le titre de l'histoire (`titre`) reste `parsed.titre` (titre du chapitre).

**D. FinStep — bouton « 📖 Écrire le chapitre suivant »** (stories.tsx, vers ligne 2014) :
- Au-dessus du bouton « ▶ Relire » existant, ajouter un nouveau Pressable PRIMARY :
  ```
  📖 Écrire le chapitre suivant
  ```
- onPress : construire le `BookContext` à partir de `histoire` + des chapitres existants du même `livreId` (récupérés via `stories.filter(s => s.livreId === histoire.livreId || (!histoire.livreId && s.id === histoire.id))`) en utilisant `buildBookContextForPrompt` du `lib/story-books.ts` après avoir construit le `StoryBook` via `groupStoriesByBook`.
- Puis `goTo({ etape: 'personnaliser', enfantId: histoire.enfantId, enfantName: histoire.enfant, universId: histoire.univers, book, trancheAge: histoire.trancheAge })`.
- (On passe par `personnaliser` plutôt que direct `generation` pour que l'utilisateur puisse écrire le détail/prompt « et après... » et la longueur.)

**E. UI personnaliser quand `book` présent — header + champs verrouillés** :
- En haut de l'écran, afficher : `📖 {book.livreTitre}` puis sous-titre `Chapitre {book.chapitre}`.
- Univers : afficher l'univers actuel avec icône cadenas 🔒, non cliquable (sinon comportement inchangé).
- Voix : afficher la config actuelle avec 🔒 (la config voix vient de `voiceConfig` global, déjà verrouillée par enfant — pas besoin de logique nouvelle).
- Multi-voix toggle : afficher state actuel avec 🔒.
- Tranche d'âge : pas de pills (verrouillée).
- Détail/prompt : champ TextInput ÉDITABLE avec placeholder `« Et après... »` au lieu du placeholder normal.
- Longueur : pills ÉDITABLES (l'utilisateur peut choisir une longueur différente par chapitre).
- Mode spectacle : toggle ÉDITABLE.

**F. Liste regroupée par livre (stories.tsx, vers ligne 203 `filteredStories` + ligne 222)** :
- Après `filteredStories` (useMemo), créer `booksByUniverse = useMemo(() => groupStoriesByBook(filteredStories), [filteredStories])`.
- Modifier le rendu de chaque univers (vers ligne 222) pour itérer sur les `StoryBook` au lieu d'histoires brutes :
  - `book.chapters.length === 1 && book.isImplicit` → rendre la `StoryCard` actuelle inchangée (passer `book.chapters[0]`).
  - `book.chapters.length === 1 && !book.isImplicit` → idem mais ajouter petit badge `📖 Tome 1` discret.
  - `book.chapters.length >= 2` → rendre une `BookCard` (NOUVEAU composant inline) :
    - Header : titre du livre, sous-titre `{N} chapitres`, avatars/emojis du casting (`book.casting.map(slug => slug)` → afficher le `label` court du casting via `getUniversCasting(book.universId).find(c => c.slug === slug)?.label.split(' ')[0]` ou simplement `🎭 {casting.join(' · ')}` en chips).
    - Liste des chapitres : `{chapitre}. {chapitreTitre || titre}` avec ▶ devant le dernier écouté (heuristique : dernier `audioAvailableMap[story.id] === true`, sinon le dernier de la liste).
    - Chaque chapitre → onPress = `onStoryPress(chapter)` (ouvre le replay comme aujourd'hui).
- Couleurs : EXCLUSIVEMENT `useThemeColors()` (`colors.surface`, `colors.text`, `colors.textMuted`, `colors.border`, `primary`).

**G. StoryPlayer.tsx — pas de modif fonctionnelle** : le bouton « Écrire le chapitre suivant » est dans `FinStep` (stories.tsx), pas dans le player. Vérifier que `onFinish` est bien appelé en fin de lecture (déjà le cas → goTo fin). **Si** la spec veut le bouton DANS le player en fin, l'ajouter aussi dans le footer de StoryPlayer après l'état "ended" : un Pressable qui appelle un nouveau prop `onWriteNextChapter?: () => void` (optionnel). Pour l'instant, **se contenter du bouton dans FinStep** (cohérent avec le flow existant `onFinish → goTo('fin')`). Ne PAS modifier StoryPlayer.tsx si pas nécessaire — le retirer de `files_modified` au commit si pas touché.

**H. Conventions** :
- Tout en français (UI + commentaires)
- `useThemeColors()` partout — JAMAIS de hex
- Tokens : `Spacing.*`, `Radius.*`, `FontSize.*`
- Animations : `useSharedValue` + `withSpring` si besoin (pas obligatoire ici)
- `Haptics.selectionAsync()` au tap des pills tranche d'âge et au tap « écrire le chapitre suivant »
- `React.memo` sur `BookCard` si > 5 chapitres possibles
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>
- À l'étape personnaliser (sans book), 3 pills tranche d'âge visibles avec default calculé depuis le profil enfant
- À l'étape personnaliser (avec book), header `📖 {titre} — Chapitre N` + indicateurs 🔒 sur univers/voix/multi-voix/tranche d'âge ; détail/longueur/spectacle restent éditables
- En FinStep, le bouton « 📖 Écrire le chapitre suivant » apparaît et lance le wizard avec `book` correctement construit
- Génération du chapitre 2 → fichier .md écrit avec frontmatter `livre_id`, `livre_titre`, `chapitre: 2`, `chapitre_titre`, `tranche_age`, `personnages`, `memory_summary`
- Liste regroupée : 1 chapitre → carte simple ; 2+ chapitres → BookCard avec liste numérotée + casting + indicateur ▶ pour le dernier écouté
- Histoires existantes (sans `livreId`) → toujours affichées comme cartes simples (livre implicite mono-chapitre)
- `npx tsc --noEmit` passe
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passe sur l'ensemble du repo (tolérance pour erreurs pré-existantes listées dans CLAUDE.md : MemoryEditor.tsx, cooklang.ts, useVault.ts).
- Test manuel rétrocompat : ouvrir une histoire existante (ancienne, sans `livreId`) → s'affiche, se lit, se rejoue normalement, apparaît dans la liste comme carte simple.
- Test manuel nouveau livre :
  1. Créer une histoire (univers princesse, multi-voix on, tranche '6-8') → fichier .md a `tranche_age: 6-8`, `livre_id: <slug>`, `chapitre: 1`, `personnages: [...]`, `memory_summary: "..."`.
  2. À la fin, taper « Écrire le chapitre suivant » → wizard préremplit univers/voix/multi-voix/tranche en lecture seule, header montre `📖 {titre} — Chapitre 2`.
  3. Taper « et le dragon revient... » dans le détail, lancer → Claude reçoit le bloc MÉMOIRE NARRATIVE avec le texte du chapitre 1 + le casting verrouillé.
  4. Le chapitre 2 sauvegarde un .md avec `chapitre: 2` et le même `livre_id`.
  5. Liste : la carte du livre montre 2 chapitres avec leurs titres.
- Test multi-voix verrouillé : si chapitre 1 a introduit "fée" et "dragon", le chapitre 2 a `lockedCasting: ['fée','dragon']` injecté dans le prompt.
</verification>

<success_criteria>
- [ ] `BedtimeStory` étendu avec 7 champs optionnels (rétrocompat 100%)
- [ ] `lib/story-books.ts` créé avec 5 helpers exportés et typés
- [ ] `parser.ts` roundtrip les nouveaux champs (sérialisation + parsing)
- [ ] `generateBedtimeStory` accepte `book?` et `trancheAge?`, prompt enrichi conditionnellement
- [ ] Output JSON Claude inclut `memorySummary` persisté dans le frontmatter
- [ ] UI : sélecteur tranche d'âge à la création (3 pills, default depuis profil)
- [ ] UI : bouton « 📖 Écrire le chapitre suivant » dans FinStep
- [ ] UI : flux chapitre N avec champs verrouillés (cadenas) sauf détail/longueur/spectacle
- [ ] UI : liste stories regroupée par livre (1 chap = carte simple, 2+ = BookCard)
- [ ] Histoires existantes sans `livreId` continuent de fonctionner sans migration
- [ ] `npx tsc --noEmit` passe
- [ ] Aucune couleur hardcodée (audit `grep -E "#[0-9a-fA-F]{6}" stories.tsx` ne révèle pas de NOUVELLES occurrences)
</success_criteria>

<output>
Après exécution, créer `.planning/quick/260427-114-syst-me-livre-chapitres-pour-les-histoir/260427-114-SUMMARY.md` résumant : champs frontmatter ajoutés, helpers exportés, comportement du prompt avec/sans `book`, points d'attention pour l'utilisateur (ex : « les histoires existantes sont des livres implicites mono-chapitre — pour les transformer en livres explicites, il faudrait éditer manuellement leur frontmatter »).
</output>
