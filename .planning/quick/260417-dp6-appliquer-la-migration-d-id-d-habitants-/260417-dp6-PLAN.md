---
phase: quick-260417-dp6
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/parser.ts
autonomous: true
requirements:
  - DP6-01
must_haves:
  truths:
    - "parseFarmProfile applique la migration aigle_expedition → aigle_dore et renard_expedition → renard_arctique lors du parse de mascot_inhabitants"
    - "parseFarmProfile applique la migration sur les itemId de mascot_placements"
    - "Les habitants migrés apparaissent correctement dans la modal Embellir (match avec INHABITANTS catalogue)"
  artifacts:
    - path: "lib/parser.ts"
      provides: "parseFarmProfile avec migration ID habitants appliquée au parse"
      contains: "INHABITANT_ID_MIGRATIONS"
  key_links:
    - from: "lib/parser.ts"
      to: "parseFarmProfile mascot_inhabitants/mascot_placements"
      via: "migrateInhabitantId helper local"
      pattern: "migrateInhabitantId"
---

<objective>
Appliquer la migration d'ID d'habitants (aigle_expedition → aigle_dore, renard_expedition → renard_arctique) dans `parseFarmProfile` afin que les habitants issus du loot d'expédition soient correctement reconnus à la lecture du vault (avant même toute écriture via `applyFarmField`).

Purpose: Bug — les profils qui contiennent encore les anciens IDs dans `farm-{id}.md` (écrits avant la migration dans `useFarm.ts`) ne voient pas leurs habitants dans la modal "Embellir" car `profile.mascotInhabitants` contient `aigle_expedition` sans correspondance dans INHABITANTS.
Output: `parseFarmProfile` retourne des IDs migrés, cohérent avec `applyFarmField`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@lib/parser.ts
@hooks/useFarm.ts

<interfaces>
<!-- Migration map existante dans hooks/useFarm.ts:81-88 (source de vérité) -->

From hooks/useFarm.ts:
```typescript
const INHABITANT_ID_MIGRATIONS: Record<string, string> = {
  aigle_expedition: 'aigle_dore',
  renard_expedition: 'renard_arctique',
};

function migrateInhabitantId(id: string): string {
  return INHABITANT_ID_MIGRATIONS[id] ?? id;
}
```

From lib/parser.ts:633-642 (zone à patcher dans parseFarmProfile):
```typescript
const mascotInhabitants = props.mascot_inhabitants
  ? props.mascot_inhabitants.split(',').map((s) => s.trim()).filter(Boolean)
  : [];
const mascotPlacements: Record<string, string> = {};
if (props.mascot_placements) {
  props.mascot_placements.split(',').forEach((pair) => {
    const [slotId, itemId] = pair.split(':').map((s) => s.trim());
    if (slotId && itemId) mascotPlacements[slotId] = itemId;
  });
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Appliquer migrateInhabitantId dans parseFarmProfile</name>
  <files>lib/parser.ts</files>
  <action>
Dans `lib/parser.ts`, au-dessus de la fonction `parseFarmProfile` (ou juste avant la zone ligne 633), ajouter la constante de migration et le helper — dupliqué depuis `hooks/useFarm.ts:81-88` (parser.ts NE DOIT PAS importer depuis hooks/ pour éviter les dépendances circulaires).

```typescript
// Migration: anciens itemId de loot d'expédition → IDs réels dans INHABITANTS
// (duplique volontairement hooks/useFarm.ts INHABITANT_ID_MIGRATIONS — parser.ts ne doit pas importer depuis hooks/)
const INHABITANT_ID_MIGRATIONS: Record<string, string> = {
  aigle_expedition: 'aigle_dore',
  renard_expedition: 'renard_arctique',
};

function migrateInhabitantId(id: string): string {
  return INHABITANT_ID_MIGRATIONS[id] ?? id;
}
```

Puis dans `parseFarmProfile` (lignes 633-642), patcher les deux endroits :

1. `mascot_inhabitants` (ligne 633-635) :
```typescript
const mascotInhabitants = props.mascot_inhabitants
  ? props.mascot_inhabitants.split(',').map((s) => migrateInhabitantId(s.trim())).filter(Boolean)
  : [];
```

2. `mascot_placements` (ligne 636-642) — appliquer migrateInhabitantId sur itemId :
```typescript
const mascotPlacements: Record<string, string> = {};
if (props.mascot_placements) {
  props.mascot_placements.split(',').forEach((pair) => {
    const [slotId, itemId] = pair.split(':').map((s) => s.trim());
    if (slotId && itemId) mascotPlacements[slotId] = migrateInhabitantId(itemId);
  });
}
```

Comportement miroir exact de `hooks/useFarm.ts:applyFarmField` (lignes 98-112) — même logique pour cohérence lecture/écriture.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>
- `INHABITANT_ID_MIGRATIONS` + `migrateInhabitantId` présents dans `lib/parser.ts`
- `parseFarmProfile` applique `migrateInhabitantId` sur chaque item de `mascot_inhabitants`
- `parseFarmProfile` applique `migrateInhabitantId` sur chaque `itemId` de `mascot_placements`
- `npx tsc --noEmit` ne rapporte aucune nouvelle erreur (les pré-existantes ignorées)
- Aucun import depuis `hooks/` dans `lib/parser.ts`
  </done>
</task>

</tasks>

<verification>
- Code review diff `lib/parser.ts` : migration ajoutée, logique parallèle à `applyFarmField`
- `npx tsc --noEmit` passe sans nouvelle erreur
- Test manuel (optionnel) : un profil ayant `mascot_inhabitants: aigle_expedition` dans `farm-{id}.md` retourne `['aigle_dore']` via `parseFarmProfile`
</verification>

<success_criteria>
- Les habitants legacy (`aigle_expedition`, `renard_expedition`) sont automatiquement migrés au parse
- Plus d'écart entre parsing direct et `applyFarmField` (single source of truth comportementale)
- Items visibles dans la modal "Embellir" pour les profils avec legacy IDs
</success_criteria>

<output>
After completion, create `.planning/quick/260417-dp6-appliquer-la-migration-d-id-d-habitants-/260417-dp6-SUMMARY.md`
</output>
