---
phase: quick-260423-l1j
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/(tabs)/stories.tsx
autonomous: false
requirements:
  - L1J-01
  - L1J-02
  - L1J-03

must_haves:
  truths:
    - "L'utilisateur voit deux onglets (Nouvelle histoire / Bibliothèque) en haut de l'écran stories quand il est à l'étape choisir_enfant"
    - "L'onglet Bibliothèque affiche toutes les histoires existantes groupées par univers, triées par date décroissante"
    - "Tap sur une carte histoire ouvre le ReplayStep existant (autoGenerate=false, pas de régénération)"
    - "Le retour depuis ReplayStep (onFinish) ramène à la vue Bibliothèque, pas au flux de création"
    - "Un badge 🔊 apparaît sur les cartes dont le MP3 est disponible en cache (ElevenLabs ou Fish Audio)"
    - "Le flux Nouvelle histoire (choisir_univers → personnaliser → generation → fin) reste 100% identique"
  artifacts:
    - path: "app/(tabs)/stories.tsx"
      provides: "Tab switcher + BibliothequeView component + retour tab-aware depuis ReplayStep"
      contains: "activeTab, BibliothequeView, StoryCard"
  key_links:
    - from: "StoriesScreen"
      to: "BibliothequeView"
      via: "conditional render when activeTab==='bibliotheque' && step.etape==='choisir_enfant'"
    - from: "StoryCard tap"
      to: "ReplayStep"
      via: "goTo({ etape: 'replay', histoire })"
      pattern: "etape: 'replay'"
    - from: "ReplayStep onFinish"
      to: "Bibliothèque"
      via: "setActiveTab('bibliotheque'); goTo({ etape: 'choisir_enfant' })"
    - from: "StoryCard audio badge"
      to: "cache MP3 vault"
      via: "getCachedStoryAudio / getCachedStoryAudioFish selon voice.engine"
---

<objective>
Ajouter un onglet "Bibliothèque" dans app/(tabs)/stories.tsx qui affiche toutes les histoires existantes groupées par univers, sans casser le flux actuel de création d'histoire.

Purpose: Permettre à l'utilisateur de retrouver et relire ses histoires passées, organisées par thème.
Output: Nouveau tab switcher + composant BibliothequeView dans le fichier existant, câblage retour depuis ReplayStep.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@app/(tabs)/stories.tsx
@lib/stories.ts
@lib/elevenlabs.ts
@lib/fish-audio.ts

<interfaces>
<!-- Contrats déjà existants dans le codebase — utiliser tels quels. -->

From lib/types.ts:
```typescript
export type StoryUniverseId =
  | 'espace' | 'ocean' | 'foret' | 'dinosaures'
  | 'princesse' | 'super-heros' | 'pirates' | 'robots' | 'surprise';

export interface BedtimeStory {
  id: string;
  titre: string;
  enfant: string;
  enfantId: string;
  univers: StoryUniverseId;
  date: string;             // ISO ou YYYY-MM-DD
  duree_lecture: string;
  voice: StoryVoiceConfig;  // { engine: 'elevenlabs' | 'fish-audio' | 'ios' | ... }
  texte: string;
  sourceFile?: string;
}
```

From lib/stories.ts:
```typescript
export const STORY_UNIVERSES: StoryUniverse[];
// Ordre canonique: espace, ocean, foret, dinosaures, princesse, super-heros, pirates, robots, surprise
// Chaque item a: { id, titre, description, emoji, couleurAccent, couleurGlow }
```

From lib/elevenlabs.ts:
```typescript
export function storyVaultAudioRelPath(enfant: string, storyId: string): string;
export async function getCachedStoryAudio(enfant: string, storyId: string): Promise<string | null>;
```

From lib/fish-audio.ts:
```typescript
export async function getCachedStoryAudioFish(enfant: string, storyId: string): Promise<string | null>;
```

From app/(tabs)/stories.tsx (existant — ne pas modifier structure):
```typescript
// goTo existe déjà dans StoriesScreen
// step state machine existe — on ajoute activeTab en parallèle
// ReplayStep reçoit {histoire} et appelle onFinish en interne via StoryPlayer
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tab switcher + activeTab state + BibliothequeView avec groupes univers</name>
  <files>app/(tabs)/stories.tsx</files>
  <action>
Dans `app/(tabs)/stories.tsx` (StoriesScreen) :

**1. State activeTab**
Ajouter à côté du state `step` :
```tsx
const [activeTab, setActiveTab] = useState<'nouvelle' | 'bibliotheque'>('nouvelle');
```

**2. Tab switcher UI (pills animés reanimated)**
Créer un composant local `TabSwitcher` (ou inline JSX) :
- Deux `Pressable` côte à côte : "✨ Nouvelle histoire" et "📚 Bibliothèque"
- Indicateur de fond animé avec `useSharedValue` + `useAnimatedStyle` + `withSpring` (SPRING_CONFIG = { damping: 18, stiffness: 180 } en constante module)
- Sur tap : `Haptics.selectionAsync()` puis `setActiveTab(...)` et animer translateX de l'indicateur (0 ou 1 * tabWidth)
- Utiliser `useThemeColors()` — bg pill actif = `primary`, inactif = `colors.card` ou `colors.border`, texte actif = `colors.bg`, inactif = `colors.textSecondary`
- Affiché UNIQUEMENT quand `step.etape === 'choisir_enfant'` (caché en cours de flux création/lecture)

**3. Rendu conditionnel renderContent**
Modifier `renderContent()` :
```tsx
if (step.etape === 'choisir_enfant' && activeTab === 'bibliotheque') {
  return <BibliothequeView />;
}
// ... switch existant inchangé
```

**4. Composant BibliothequeView (dans le même fichier)**
Props : aucune (lit `stories`, `profiles`, handlers via closure parent).

Logique :
- État local `selectedEnfantId: string | null` (null = tous enfants)
- Extraire `childProfiles = profiles.filter(p => p.role === 'enfant')`
- Si `childProfiles.length > 1` : afficher scroll horizontal de chips enfant (avatar + prénom) + chip "Tous" → `setSelectedEnfantId`
- Sinon `selectedEnfantId = childProfiles[0]?.id ?? null` fixé (pas de sélecteur affiché)
- `filteredStories = stories.filter(s => !selectedEnfantId || s.enfantId === selectedEnfantId)`
- Grouper par `univers` dans l'ordre canonique de `STORY_UNIVERSES` :
  ```tsx
  const groupes = STORY_UNIVERSES
    .map(u => ({
      universe: u,
      histoires: filteredStories
        .filter(s => s.univers === u.id)
        .sort((a, b) => b.date.localeCompare(a.date)),
    }))
    .filter(g => g.histoires.length > 0);
  ```
- État `collapsedUnivers: Record<StoryUniverseId, boolean>` (tous ouverts par défaut)
- En-tête groupe : `Pressable` → toggle collapse, affiche `{emoji} {titre} · {count}` + chevron rotate animé (reanimated)
- Liste : `groupes.map(g => ...)` rendant `<StoryCard key={story.id} story={story} showEnfantName={!selectedEnfantId} onPress={handleStoryPress} />`
- État vide (`groupes.length === 0`) : View centrée avec texte "Aucune histoire pour l'instant" + sous-texte "Créez votre première histoire ✨" + bouton primary "Créer une histoire" → `setActiveTab('nouvelle')`

**5. handleStoryPress**
```tsx
const handleStoryPress = useCallback((histoire: BedtimeStory) => {
  Haptics.selectionAsync();
  goTo({ etape: 'replay', histoire });
}, [goTo]);
```

**6. Composant StoryCard (dans le même fichier, React.memo)**
Props : `{ story: BedtimeStory; showEnfantName: boolean; onPress: (s: BedtimeStory) => void }`
- Pressable carte avec bg `colors.card`, padding `Spacing.md`, borderRadius `Radius.md`
- Titre (FontWeight.semibold, FontSize.base, colors.text)
- Ligne meta : date formatée JJ/MM/AAAA + "·" + duree_lecture
- Si `showEnfantName` : chip discret avec `story.enfant`
- Badge `🔊` absolu top-right si `audioAvailable === true` (voir Task 2 pour le state — placeholder `false` dans Task 1)
- `React.memo` sur l'export du composant

**Contraintes strictes :**
- `useThemeColors()` partout — jamais de hex hardcoded
- Tokens `Spacing.*`, `Radius.*`, `FontSize.*`, `FontWeight.*` (cohérent avec le reste du fichier)
- `react-native-reanimated` pour tab indicator + chevron collapse (pas RN Animated)
- Date formatée via `date-fns` `format(new Date(story.date), 'dd/MM/yyyy')` (déjà importé dans le fichier)
- Langue FR, commentaires FR
- Pas de nouvelle dépendance npm

**Ne PAS toucher** : le flux step (ChoisirEnfantStep, renderChoisirUniversStep, PersonnaliserStep, GenerationStep, FinStep) — laisser intact.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "stories\.tsx" | grep -v "MemoryEditor|cooklang|useVault"</automated>
  </verify>
  <done>
- activeTab state présent, TabSwitcher rendu seulement step==='choisir_enfant'
- BibliothequeView rendu quand activeTab==='bibliotheque' et step==='choisir_enfant'
- Groupes affichés par univers dans l'ordre canonique, triés par date desc
- Filtre enfant fonctionnel (ou masqué si 1 seul enfant)
- État vide avec CTA vers onglet "Nouvelle histoire"
- Tap carte → goTo replay
- `npx tsc --noEmit` sans nouvelle erreur dans stories.tsx
  </done>
</task>

<task type="auto">
  <name>Task 2: Audio badge async + retour tab-aware depuis ReplayStep</name>
  <files>app/(tabs)/stories.tsx</files>
  <action>
**1. State audio disponibilité (dans BibliothequeView)**
```tsx
const [audioAvailableMap, setAudioAvailableMap] = useState<Record<string, boolean>>({});
```

Dans un `useEffect` déclenché par `filteredStories` :
- Pour chaque story, vérifier en parallèle (Promise.all) :
  - Si `story.voice?.engine === 'fish-audio'` → `getCachedStoryAudioFish(story.enfant, story.id)`
  - Sinon → `getCachedStoryAudio(story.enfant, story.id)` (ElevenLabs / default)
- Construire un map `{ [story.id]: !!path }` et `setAudioAvailableMap`
- Guard avec un flag `cancelled = false` (cleanup) pour éviter setState après unmount
- Ne PAS bloquer le rendu initial — le badge apparaît progressivement
- Sous `if (__DEV__)` : log.warn si une vérif throw

Passer `audioAvailable={audioAvailableMap[story.id] ?? false}` en prop à `StoryCard`.
Ajouter la prop au type de StoryCard et afficher le badge conditionnel en conséquence.

**2. Retour tab-aware dans ReplayStep**
Modifier le `onFinish` du `StoryPlayer` dans `ReplayStep` (ligne ~1309) :
```tsx
onFinish={() => {
  if (activeTab === 'bibliotheque') {
    goTo({ etape: 'choisir_enfant' });
    // activeTab reste 'bibliotheque' → user revient sur la bibliothèque
  } else {
    goTo({ etape: 'choisir_enfant' });
  }
}}
```
Note : puisque `activeTab` est du state de StoriesScreen et ReplayStep est défini DANS StoriesScreen (closure), c'est accessible directement. Pas besoin de props supplémentaires.

Vérifier aussi `FinStep` (étape 'fin' après génération) : son CTA "Retour" appelle déjà `goTo({ etape: 'choisir_enfant' })` — forcer `setActiveTab('nouvelle')` si on vient du flux de création pour ne pas surprendre (conserver sur 'nouvelle' après création). Si c'est déjà le comportement par défaut (activeTab inchangé au cours du flux de création), aucune modif — vérifier et ne rien changer si superflu.

**3. Vérif finale**
- Badge 🔊 apparaît progressivement sur les cartes dont le MP3 existe
- Relecture depuis bibliothèque → ReplayStep → bouton finir → retour sur la vue Bibliothèque (pas sur flux création)
- Aucune régression du flux création (créer une nouvelle histoire reste identique)
- `npx tsc --noEmit` sans nouvelle erreur
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "stories\.tsx" | grep -v "MemoryEditor|cooklang|useVault"</automated>
  </verify>
  <done>
- audioAvailableMap peuplé async, badge conditionnel sur StoryCard
- Cleanup useEffect avec flag cancelled
- onFinish ReplayStep respecte activeTab (retour bibliothèque si venu de la bibliothèque)
- Pas de régression du flux création
- tsc vert sur stories.tsx (hors erreurs pré-existantes listées dans CLAUDE.md)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Vérification device + UX</name>
  <what-built>
Onglet Bibliothèque dans l'écran Histoires avec :
- Tab switcher animé "Nouvelle histoire / Bibliothèque"
- Groupes par univers avec collapse
- Filtre enfant si plusieurs enfants
- Cartes histoire avec badge audio 🔊
- Relecture via ReplayStep existant sans régénération
- Retour tab-aware
  </what-built>
  <how-to-verify>
1. `npx expo run:ios --device`
2. Ouvrir l'onglet Histoires
3. Vérifier présence du tab switcher en haut
4. Taper "Bibliothèque" → vérifier animation indicateur + haptic
5. Vérifier groupes par univers, ordre, tri date desc
6. Toggle collapse d'un groupe → chevron animé
7. Si plusieurs enfants : tester filtre
8. Taper une carte avec audio (badge 🔊) → ReplayStep s'ouvre, pas de régénération (autoGenerate=false)
9. Finir la lecture → retour sur Bibliothèque (pas sur création)
10. Taper "Nouvelle histoire" → flux création identique (choisir enfant → univers → etc.)
11. État vide : filtrer sur un enfant sans histoires → message + CTA fonctionnel
  </how-to-verify>
  <resume-signal>Tape "approuvé" ou décris les problèmes</resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` : aucune nouvelle erreur (ignorer MemoryEditor, cooklang, useVault pré-existantes)
- Test manuel device : tous les scénarios how-to-verify passent
</verification>

<success_criteria>
- Onglet Bibliothèque fonctionnel avec groupes par univers
- Flux création d'histoire intact (zéro régression)
- Relecture sans régénération via ReplayStep existant
- Badge audio progressif non-bloquant
- 100% thème via useThemeColors, animations via reanimated, FR
</success_criteria>

<output>
Après complétion, créer `.planning/quick/260423-l1j-ajouter-onglet-biblioth-que-dans-stories/260423-l1j-SUMMARY.md`
</output>
