---
phase: quick-260608-nx8
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [components/mascot/BuildingDetailSheet.tsx]
autonomous: true
requirements: [FAM-37]

must_haves:
  truths:
    - "Une ligne 'cadence de production' (ex: '1 miel toutes les 12h') est visible dans la hero card de chaque bâtiment productif, quel que soit son niveau"
    - "La ruche au niveau MAX affiche toujours sa cadence (le bug d'origine est corrigé — plus besoin du texte barré de 'niveau suivant')"
    - "La cadence reste visible quand le stockage est plein"
    - "La cadence affichée ignore la pénalité de dégât (taux normal, comme la ligne d'upgrade existante)"
  artifacts:
    - path: "components/mascot/BuildingDetailSheet.tsx"
      provides: "Ligne cadence toujours visible dans la hero card + style dédié"
      contains: "currentCycleHours / wearMul"
  key_links:
    - from: "hero card (isProductive)"
      to: "labels.cycleLabel + formatHours(currentCycleHours / wearMul)"
      via: "nouvelle View cadenceRow rendue inconditionnellement dans le bloc heroCard"
      pattern: "cadenceRow"
---

<objective>
FAM-37 — Afficher la cadence de production des bâtiments de la ferme de façon toujours visible.

Problème : la cadence ("1 miel toutes les 12h") n'apparaît aujourd'hui que dans le texte barré (`fromText`) de la section "NIVEAU SUIVANT" (lignes 503-510), qui ne s'affiche que si `upgradable && nextTier`. Au niveau MAX, cette section est remplacée par `maxCard` (lignes 563-571) qui n'affiche AUCUNE cadence. Une ruche niveau max ne donne donc plus aucun moyen de lire son rythme de production.

Solution : ajouter une ligne cadence toujours visible dans la hero card (bloc `isProductive`, lignes 397-441), donc présente pour chaque bâtiment productif, à tous les niveaux, plein ou non.

Purpose: rendre la cadence de production lisible indépendamment du niveau ou de l'état du bâtiment.
Output: ligne cadence dans la hero card + un style `cadenceRow` ajouté à `makeStyles`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md

<interfaces>
<!-- Variables déjà en scope dans le composant (BuildingDetailSheet.tsx ~lignes 268-301) -->

- `currentCycleHours` (number) — inclut la pénalité de dégât (`wearMul`).
- `wearMul` (number) — 2 si toit endommagé, sinon 1.
- `labels.cycleLabel` (string) — ex: "1 miel toutes les", "1 œuf toutes les".
- `formatHours(hours: number): string` — ex: "12h", "1h30", "45min".
- `isProductive` (boolean) — gate du bloc hero card.
- `isFull` (boolean), `minutesUntilNext` (number).

La ligne d'upgrade existante (ligne 506) affiche déjà la cadence comme :
`formatHours(currentCycleHours / wearMul)` — taux NORMAL hors pénalité de dégât (la pénalité est expliquée par la bannière "Toit endommagé"). On réutilise exactement cette expression.

`makeStyles(farm: FarmPalette)` est une factory unique (ligne 582) appelée pour `stylesLight = makeStyles(Farm)` et `stylesDark = makeStyles(FarmDarkPalette)` (lignes 1121-1122). Ajouter UN style suffit pour les deux thèmes — pas de duplication light/dark.

Couleurs farm disponibles : `farm.woodHighlight` (déjà utilisée comme bordure de la heroCard, ligne 751), `farm.brownTextSub`, `farm.brownText`.
Tokens : `Spacing.*`, `Radius.*` importés ligne 32.
Styles meta réutilisables : `progressMetaLeft`, `progressMetaRight`, `progressMetaRightBold` (lignes 823-835).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ajouter la ligne cadence toujours visible dans la hero card</name>
  <files>components/mascot/BuildingDetailSheet.tsx</files>
  <action>
    Dans le bloc `isProductive` de la hero card (Animated.View ~lignes 399-441), APRÈS la `View style={styles.progressMeta}` (qui se ferme ligne 440) et AVANT la fermeture du `Animated.View` (ligne 441), ajouter une nouvelle ligne cadence rendue INCONDITIONNELLEMENT (pas de garde `isFull` / `upgradable`) :

    ```tsx
    <View style={styles.cadenceRow}>
      <Text style={styles.progressMetaLeft}>{labels.cycleLabel}</Text>
      <Text style={styles.progressMetaRight}>
        <Text style={styles.progressMetaRightBold}>
          {formatHours(currentCycleHours / wearMul)}
        </Text>
      </Text>
    </View>
    ```

    Notes :
    - Réutiliser les styles meta existants (`progressMetaLeft`, `progressMetaRight`, `progressMetaRightBold`) pour la cohérence typographique avec la ligne "prochaine ressource".
    - Utiliser `currentCycleHours / wearMul` (taux normal, hors pénalité de dégât) — identique à la ligne d'upgrade existante (ligne 506). NE PAS utiliser `currentCycleHours` brut.
    - Ne rien retirer ni modifier dans le bloc `progressMeta` existant (la ligne "prochaine ressource dans" / "production en pause" reste).
    - UI en français : `labels.cycleLabel` est déjà en français.
  </action>
  <verify>
    <automated>grep -n "cadenceRow" components/mascot/BuildingDetailSheet.tsx | grep -v "cadenceRow:" | head</automated>
  </verify>
  <done>La hero card contient une `View style={styles.cadenceRow}` affichant `labels.cycleLabel` à gauche et `formatHours(currentCycleHours / wearMul)` en gras à droite, rendue sans condition de niveau ni de remplissage.</done>
</task>

<task type="auto">
  <name>Task 2: Ajouter le style cadenceRow et valider le type-check</name>
  <files>components/mascot/BuildingDetailSheet.tsx</files>
  <action>
    Dans la factory `makeStyles` (StyleSheet.create, à partir de la ligne 582), ajouter le style `cadenceRow` juste après le groupe `progressMeta*` (après `progressMetaRightBold`, ligne 835) :

    ```tsx
    cadenceRow: {
      flexDirection: 'row',
      justifyContent: 'space-between',
      alignItems: 'center',
      marginTop: Spacing.md,
      paddingTop: Spacing.md,
      borderTopWidth: 1,
      borderTopColor: farm.woodHighlight,
    },
    ```

    Notes :
    - Un seul ajout dans `makeStyles` couvre stylesLight ET stylesDark (factory unique appelée deux fois, lignes 1121-1122) — `farm.woodHighlight` s'adapte au thème.
    - Utiliser les tokens `Spacing.*` (pas de valeurs numériques brutes) et la couleur du thème `farm.woodHighlight` (pas de hardcoded) — conforme CLAUDE.md.
    - Le divider subtil (`borderTopWidth: 1` + `farm.woodHighlight`) sépare visuellement la cadence de la ligne "prochaine ressource".
  </action>
  <verify>
    <automated>grep -n "cadenceRow:" components/mascot/BuildingDetailSheet.tsx && npx tsc --noEmit 2>&1 | grep -i "BuildingDetailSheet" || echo "OK no BuildingDetailSheet errors"</automated>
  </verify>
  <done>Le style `cadenceRow` existe dans `makeStyles`, utilise tokens + couleur thème. `npx tsc --noEmit` ne produit aucune nouvelle erreur sur BuildingDetailSheet.tsx.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passe sans nouvelle erreur (les erreurs pré-existantes de MemoryEditor.tsx, cooklang.ts, useVault.ts sont ignorées).
- Vérification visuelle manuelle (hors scope automatisé) : ouvrir une ruche niveau MAX → la cadence "1 miel toutes les 12h" est visible dans la hero card. Ouvrir un bâtiment non-max → la cadence est visible en plus de la ligne "prochaine ressource". Remplir le stockage → la cadence reste affichée même avec "Production en pause".
</verification>

<success_criteria>
- La cadence de production est visible dans la hero card de tout bâtiment productif, à tous les niveaux (y compris MAX) et quel que soit l'état de remplissage.
- Aucune régression : le bloc upgrade et la ligne "prochaine ressource" existants sont inchangés.
- Aucune couleur hardcodée, tokens utilisés, tsc vert.
- Bâtiments non-productifs (Auberge) hors scope — inchangés.
</success_criteria>

<output>
After completion, create `.planning/quick/260608-nx8-fam-37-afficher-la-cadence-de-production/260608-nx8-SUMMARY.md`
</output>
