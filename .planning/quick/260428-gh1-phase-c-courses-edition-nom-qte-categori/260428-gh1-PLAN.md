---
phase: quick-260428-gh1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - hooks/useVaultCourses.ts
  - lib/auto-courses.ts
  - lib/courses-text.ts
  - components/CourseItemEditor.tsx
  - app/(tabs)/meals.tsx
  - locales/fr/common.json
  - locales/en/common.json
autonomous: true
requirements:
  - GH1-01
  - GH1-02
  - GH1-03
  - GH1-04
must_haves:
  truths:
    - "Tap sur le texte d'un item de courses ouvre un bottom sheet d'édition"
    - "Long-press sur un item de courses affiche un menu contextuel (Dupliquer / Supprimer / Annuler)"
    - "L'utilisateur peut modifier le nom, la quantité et la catégorie d'un item dans le bottom sheet"
    - "Enregistrer applique le patch en optimistic UI puis persiste dans le vault"
    - "Le composant respecte le thème (useThemeColors) — aucune couleur hardcoded"
  artifacts:
    - path: "lib/courses-text.ts"
      provides: "splitCourseText / joinCourseText helpers"
    - path: "components/CourseItemEditor.tsx"
      provides: "Bottom sheet pageSheet pour éditer un CourseItem"
    - path: "hooks/useVaultCourses.ts"
      provides: "updateCourseItem(lineIndex, patch)"
  key_links:
    - from: "app/(tabs)/meals.tsx"
      to: "components/CourseItemEditor.tsx"
      via: "<CourseItemEditor visible item sections onClose onSave />"
    - from: "components/CourseItemEditor.tsx"
      to: "hooks/useVaultCourses.ts:updateCourseItem"
      via: "props.onSave({ text, section }) -> updateCourseItem(lineIndex, patch)"
    - from: "hooks/useVaultCourses.ts:updateCourseItem"
      to: "vault file 02 - Maison/Liste de courses.md"
      via: "enqueueWrite + writeFile (réécrit la ligne, déplace la section)"
---

<objective>
Phase C courses — éditer un item (nom + qté + catégorie) via bottom sheet déclenché par tap sur le texte ; long-press devient un menu contextuel léger (Dupliquer / Supprimer).

Purpose: Compléter Phases A (constantes + queue) et B (optimistic UI) avec une UX d'édition complète sans quitter l'écran courses, tout en remplaçant le picker de catégorie isolé par un éditeur unifié.

Output: 1 helper texte (lib/courses-text.ts), 1 méthode hook (updateCourseItem), 1 composant (CourseItemEditor), wiring meals.tsx, clés i18n.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@hooks/useVaultCourses.ts
@lib/auto-courses.ts
@lib/courses-constants.ts
@lib/types.ts
@components/ui/ModalHeader.tsx
@app/(tabs)/meals.tsx

<interfaces>
<!-- Contrats clés à respecter — extraits du codebase. -->

From hooks/useVaultCourses.ts (pattern à étendre):
```ts
export interface UseVaultCoursesResult {
  courses: CourseItem[];
  // ... existing
  moveCourseItem: (lineIndex: number, text: string, newSection: string) => Promise<void>;
  // À ajouter:
  updateCourseItem: (lineIndex: number, patch: { text: string; section: string }) => Promise<void>;
}
```

From lib/auto-courses.ts (réutiliser, exporter COURSE_TEXT_RE):
```ts
const COURSE_TEXT_RE = /^(\d+(?:[.,]\d+)?)\s*(g|kg|ml|cl|dl|l|cs|cc|...|tsp)?\s*(?:de\s+|d')?(.+)/i;
```

From lib/types.ts:
```ts
interface CourseItem {
  id: string;
  text: string;
  section: string;
  completed: boolean;
  lineIndex: number;
  pending?: boolean; // Phase B
}
```

From components/ui/ModalHeader.tsx:
```ts
<ModalHeader title onClose rightLabel onRight rightDisabled closeLeft />
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Helper splitCourseText/joinCourseText + export COURSE_TEXT_RE</name>
  <files>lib/auto-courses.ts, lib/courses-text.ts</files>
  <action>
    1. Dans `lib/auto-courses.ts` : ajouter `export` devant la déclaration `const COURSE_TEXT_RE = ...` (ligne ~165). Aucune autre modification du fichier.

    2. Créer `lib/courses-text.ts` :
    ```ts
    import { COURSE_TEXT_RE } from './auto-courses';

    export interface SplitCourseText { quantity: string; name: string; }

    export function splitCourseText(text: string): SplitCourseText {
      const trimmed = text.trim();
      const m = trimmed.match(COURSE_TEXT_RE);
      if (!m) return { quantity: '', name: trimmed };
      const num = m[1];
      const unit = (m[2] || '').trim();
      const name = (m[3] || '').trim();
      // unité de poids/volume → on garde dans la qté ("120g", "1.5kg")
      // unité comptable (sachet, paquet…) → on rejoint dans le name pour ne pas perdre l'info
      // Pour rester simple : si pas de unit → quantity = num, name = name.
      // Si unit présent → quantity = `${num}${unit}` (poids) OU on remet l'unité dans name pour comptable.
      const isWeight = /^(g|kg|ml|cl|dl|l|cs|cc|càs|càc|tasse|pincée|tbsp|tsp)$/i.test(unit);
      if (!unit) return { quantity: num, name };
      if (isWeight) return { quantity: `${num}${unit}`, name };
      // Comptable (sachet, paquet, boîte…) : on garde la qté numérique pure, on remet l'unité dans le nom
      return { quantity: num, name: `${unit} de ${name}` };
    }

    export function joinCourseText(quantity: string, name: string): string {
      const q = quantity.trim();
      const n = name.trim();
      if (!q) return n;
      if (/[a-zA-Z]/.test(q)) return `${q} de ${n}`; // "120g" → "120g de pecorino"
      return `${q} ${n}`; // "3" → "3 oeufs"
    }
    ```

    Note : pas de fichier de tests demandé (cf scope « pas de tests unitaires »). Le round-trip non strict est accepté pour les unités comptables (cf scope, qty unit gardée en string libre).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "(courses-text|auto-courses)\.ts" || echo "OK no new errors"</automated>
  </verify>
  <done>`COURSE_TEXT_RE` exporté ; `lib/courses-text.ts` créé et compile ; aucune erreur TS nouvelle.</done>
</task>

<task type="auto">
  <name>Task 2: Étendre useVaultCourses avec updateCourseItem</name>
  <files>hooks/useVaultCourses.ts</files>
  <action>
    Ajouter la méthode `updateCourseItem` dans le hook, modelée sur `moveCourseItem` (cf lignes 135-171 du fichier).

    1. Dans l'interface `UseVaultCoursesResult` (lignes 33-43), ajouter :
    ```ts
    updateCourseItem: (lineIndex: number, patch: { text: string; section: string }) => Promise<void>;
    ```

    2. Implémenter avant `mergeCourseIngredients` :
    ```ts
    const updateCourseItem = useCallback(async (lineIndex: number, patch: { text: string; section: string }) => {
      const snapshot = coursesRef.current;
      const current = snapshot.find(c => c.lineIndex === lineIndex);
      if (!current) return;
      // No-op si rien ne change
      if (current.text === patch.text && current.section === patch.section) return;

      // Optimistic
      setCourses((prev) => prev.map((c) =>
        c.lineIndex === lineIndex ? { ...c, text: patch.text, section: patch.section, pending: true } : c
      ));

      return enqueueWrite(async () => {
        if (!vaultRef.current) return;
        try {
          const content = await vaultRef.current.readFile(COURSES_FILE_LEGACY);
          const lines = content.split('\n');
          if (lineIndex < 0 || lineIndex >= lines.length) return;

          const originalLine = lines[lineIndex];
          // Préserver l'état coché [x] vs [ ]
          const checkedMatch = originalLine.match(/^-\s+\[(x| )\]/i);
          const mark = checkedMatch && checkedMatch[1].toLowerCase() === 'x' ? 'x' : ' ';

          if (current.section === patch.section) {
            // Section inchangée → réécrire la ligne en place
            lines[lineIndex] = `- [${mark}] ${patch.text}`;
          } else {
            // Section changée → splice + réinsérer (comme moveCourseItem)
            lines.splice(lineIndex, 1);
            const sectionHeader = `## ${patch.section}`;
            let sectionIdx = lines.findIndex(l => l.trim() === sectionHeader);
            if (sectionIdx === -1) {
              lines.push('', sectionHeader, `- [${mark}] ${patch.text}`);
            } else {
              let insertIdx = sectionIdx + 1;
              while (insertIdx < lines.length && lines[insertIdx].startsWith('- [')) {
                insertIdx++;
              }
              lines.splice(insertIdx, 0, `- [${mark}] ${patch.text}`);
            }
          }

          const newContent = lines.join('\n');
          await vaultRef.current.writeFile(COURSES_FILE_LEGACY, newContent);
          setCourses(parseCourses(newContent, COURSES_FILE_LEGACY));
        } catch (e) {
          setCourses(snapshot);
          throw e;
        }
      });
    }, [enqueueWrite]);
    ```

    3. Exporter dans le `return` final (entre `moveCourseItem` et `mergeCourseIngredients`) :
    ```ts
    return {
      courses,
      setCourses,
      addCourseItem,
      toggleCourseItem,
      removeCourseItem,
      moveCourseItem,
      updateCourseItem,
      mergeCourseIngredients,
      clearCompletedCourses,
      resetCourses,
    };
    ```

    Vérifier que `useVaultInternal` dans `hooks/useVault.ts` propage automatiquement la nouvelle méthode (via spread `...coursesResult`). Si l'agrégateur fait un pick explicite, ajouter `updateCourseItem` dans la liste.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "useVaultCourses|useVault\.ts" | grep -v "MemoryEditor\|cooklang\|useVault\.ts:.*pre-existing" || echo "OK"</automated>
  </verify>
  <done>`updateCourseItem` exposé par useVaultCourses, propagé via useVault, no-op si patch identique, optimistic + rollback, préserve l'état coché, gère section identique vs changée.</done>
</task>

<task type="auto">
  <name>Task 3: Créer components/CourseItemEditor.tsx (bottom sheet pageSheet)</name>
  <files>components/CourseItemEditor.tsx</files>
  <action>
    Créer le composant Modal pageSheet d'édition d'un CourseItem. Style cohérent avec StockEditor.tsx / RDVEditor.tsx (lire l'un d'eux en référence si besoin).

    Props :
    ```ts
    interface CourseItemEditorProps {
      visible: boolean;
      item: CourseItem | null;
      sections: string[]; // sections déjà présentes dans la liste courses
      onClose: () => void;
      onSave: (patch: { text: string; section: string }) => Promise<void>;
    }
    ```

    Structure :
    - `Modal` (react-native) avec `presentationStyle="pageSheet"`, `animationType="slide"`, `onRequestClose={onClose}`.
    - `KeyboardAvoidingView behavior="padding"` (iOS).
    - `useThemeColors()` pour TOUTES les couleurs (cf CLAUDE.md — interdit hardcoded).
    - `useTranslation()` pour les libellés FR (clés meals.shopping.edit*, voir Task 5).
    - `useEffect` sur `item` : si non null, splitter via `splitCourseText(item.text)` et pré-remplir state nom + qté ; setSection(item.section).
    - State : `name: string`, `qty: string`, `section: string`, `addingNewCategory: boolean`, `newCategoryLabel: string`, `newCategoryEmoji: string`.

    Layout (1 écran sans scroll vertical principal) :
    1. `<ModalHeader title={t('meals.shopping.editTitle')} onClose={onClose} rightLabel={t('meals.shopping.editSave')} onRight={handleSave} rightDisabled={!name.trim()} closeLeft />`
    2. **Champ Nom** : Label + `TextInput autoFocus` (référence `nameRef` pour focus initial).
    3. **Champ Qté** : Label + ligne avec `TextInput` (flex 1) + boutons `−` et `+`.
       - Les boutons `−`/`+` ne sont actifs que si `qty` est purement numérique (`/^\d+$/.test(qty.trim())`). Sinon, ils sont disabled (opacity 0.4).
       - `+` : `setQty(String((parseInt(qty) || 0) + 1))`. `−` : `setQty(String(Math.max(0, (parseInt(qty) || 0) - 1)))` (mais 0 → vide).
       - Haptics.selectionAsync() à chaque tap (cf CLAUDE.md).
    4. **Catégorie** : Label + `ScrollView horizontal` de chips. Chaque chip = TouchableOpacity { emoji + label tronqué }. Highlight `section === current` avec backgroundColor primary, sinon colors.surface + colors.border.
       - Sources des chips : déduplication de `sections` props + `COURSES_DEFAULT_SECTION`. Pas besoin de catalogue dur — l'utilisateur voit ses sections actuelles.
       - Dernière chip : `+ Nouvelle` → setAddingNewCategory(true).
    5. **Sous-bloc inline `Nouvelle catégorie`** (si `addingNewCategory`) :
       - Row : TextInput (label) + grille horizontale de 8 emojis fréquents (`🏷️ 🛒 🥬 🍎 🥩 🍞 🥛 🧊`) sélectionnables.
       - Bouton "Ajouter" → assemble `${emoji} ${label}` comme nouvelle section, l'applique à `section`, append à la liste locale des chips affichées, ferme le sous-bloc.

    Validation `handleSave` :
    - `name.trim().length === 0` → return early (le bouton est déjà disabled mais double-check).
    - `text = joinCourseText(qty, name)`.
    - `await onSave({ text, section })` — laisse l'appelant fermer le modal après résolution.
    - Try/catch silencieux ici, l'appelant gère l'Alert d'erreur.

    Imports : `Modal, View, Text, TextInput, TouchableOpacity, ScrollView, KeyboardAvoidingView, Platform, StyleSheet` de react-native ; `useThemeColors` ; `ModalHeader` ; `splitCourseText, joinCourseText` ; `Spacing, FontSize, FontWeight` ; `Haptics` ; `useTranslation`.

    Style : `useMemo(() => StyleSheet.create({...}))` ou inline avec tokens design (Spacing, FontSize). AUCUNE couleur hardcoded.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep "CourseItemEditor" || echo "OK no errors in new component"</automated>
  </verify>
  <done>Composant `CourseItemEditor.tsx` existe, prend props { visible, item, sections, onClose, onSave }, affiche header + nom + qté ± + chips catégories + ajout inline nouvelle catégorie, autofocus nom, validation nom non vide, joinCourseText pour reconstituer text, useThemeColors strict, KeyboardAvoidingView iOS.</done>
</task>

<task type="auto">
  <name>Task 4: Wiring dans app/(tabs)/meals.tsx — supprimer l'ancien picker, brancher l'éditeur + long-press menu</name>
  <files>app/(tabs)/meals.tsx</files>
  <action>
    Modifications ciblées (NE PAS réécrire le fichier entier).

    1. **Imports** : ajouter en haut du fichier :
    ```ts
    import { CourseItemEditor } from '../../components/CourseItemEditor';
    ```

    2. **Récupérer `updateCourseItem`** depuis `useVault()` (chercher la destructuration `const { ... } = useVault();` qui contient déjà `moveCourseItem`).

    3. **Supprimer** les states et handlers obsolètes (lignes ~240-245 pour les states, ~686-720 pour les handlers, + le Modal "category picker" associé que tu trouveras avec un grep `categoryPickerItem`) :
    - state `categoryPickerItem`
    - state `newCategoryName`, `newCategoryEmoji`, `showEmojiPicker` (s'ils ne sont utilisés QUE par le picker — vérifier par grep, ne pas casser un autre flux)
    - handler `handleChangeCourseCategory`
    - handler `handleSelectCategory`
    - handler `handleAddNewCategory`
    - le `<Modal>` JSX correspondant au category picker

    4. **Ajouter** les nouveaux states et handlers (après les states courses existants) :
    ```ts
    const [editingCourseItem, setEditingCourseItem] = useState<CourseItem | null>(null);

    const courseSections = useMemo(() => {
      const set = new Set<string>();
      for (const c of courses) set.add(c.section);
      return Array.from(set);
    }, [courses]);

    const handleEditCourse = useCallback((item: CourseItem) => {
      Haptics.selectionAsync();
      setEditingCourseItem(item);
    }, []);

    const handleEditSave = useCallback(async (patch: { text: string; section: string }) => {
      if (!editingCourseItem) return;
      try {
        await updateCourseItem(editingCourseItem.lineIndex, patch);
        setEditingCourseItem(null);
      } catch (e) {
        Alert.alert(t('common.error'), String(e));
      }
    }, [editingCourseItem, updateCourseItem, t]);

    const handleDuplicateCourse = useCallback((item: CourseItem) => {
      addCourseItem(item.text, item.section).catch(e => {
        Alert.alert(t('common.error'), String(e));
      });
    }, [addCourseItem, t]);

    const handleCourseLongPress = useCallback((item: CourseItem) => {
      Haptics.impactAsync(Haptics.ImpactFeedbackStyle.Medium);
      Alert.alert(item.text, undefined, [
        { text: t('meals.shopping.actionDuplicate'), onPress: () => handleDuplicateCourse(item) },
        { text: t('meals.shopping.actionDelete'), style: 'destructive', onPress: () => handleCourseRemove(item) },
        { text: t('common.cancel'), style: 'cancel' },
      ]);
    }, [handleDuplicateCourse, handleCourseRemove, t]);
    ```

    Note : si `handleCourseRemove` n'existe pas exactement sous ce nom, utiliser le handler existant qui appelle `removeCourseItem(item.lineIndex)`.

    5. **Adapter le rendu courseRow** (lignes ~1322-1372). Le `<Text>` qui affiche `item.text` doit devenir tappable :
    ```tsx
    <TouchableOpacity
      style={styles.courseTextWrap}
      onPress={() => handleEditCourse(item)}
      onLongPress={() => handleCourseLongPress(item)}
      delayLongPress={400}
      activeOpacity={0.6}
    >
      <Text style={[styles.courseText, item.completed && styles.courseTextDone, { color: colors.text }]}>
        {item.text}
      </Text>
    </TouchableOpacity>
    ```
    Préserver le `flex: 1` / mise en page actuelle. Si le row était déjà un TouchableOpacity pour autre chose, ajuster sans dupliquer le wrapping.

    6. **Rendre l'éditeur** près des autres modals du fichier :
    ```tsx
    <CourseItemEditor
      visible={!!editingCourseItem}
      item={editingCourseItem}
      sections={courseSections}
      onClose={() => setEditingCourseItem(null)}
      onSave={handleEditSave}
    />
    ```

    7. Vérifier qu'aucune référence orpheline aux states/handlers supprimés ne subsiste (grep `categoryPickerItem`, `handleChangeCourseCategory`, `handleSelectCategory`, `handleAddNewCategory` doit retourner 0 hits).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep "meals.tsx" | grep -v "pre-existing" | head -20 || echo "OK"; grep -c "categoryPickerItem\|handleChangeCourseCategory\|handleSelectCategory\|handleAddNewCategory" "app/(tabs)/meals.tsx" || echo "0 — clean"</automated>
  </verify>
  <done>meals.tsx : ancien picker supprimé (4 états + 3 handlers + modal JSX) ; tap texte ouvre CourseItemEditor ; long-press affiche Alert menu 3 options ; updateCourseItem branché ; aucun référence orpheline.</done>
</task>

<task type="auto">
  <name>Task 5: Ajouter clés i18n FR + EN</name>
  <files>locales/fr/common.json, locales/en/common.json</files>
  <action>
    Localiser le bloc `meals.shopping` dans `locales/fr/common.json` (ligne ~850 d'après le grep). Ajouter les clés suivantes dans ce bloc (préserver l'ordre alphabétique si présent, sinon ajouter en fin de bloc) :

    ```json
    "editTitle": "Modifier l'article",
    "editName": "Nom",
    "editQty": "Quantité",
    "editSection": "Catégorie",
    "editNewCategory": "+ Nouvelle",
    "editNewCategoryLabel": "Nom de la catégorie",
    "editNewCategoryAdd": "Ajouter",
    "editSave": "Enregistrer",
    "editCancel": "Annuler",
    "editEmpty": "Le nom ne peut pas être vide",
    "actionDuplicate": "Dupliquer",
    "actionDelete": "Supprimer"
    ```

    Faire la même chose dans `locales/en/common.json` (au même chemin meals.shopping) avec traductions :
    ```json
    "editTitle": "Edit item",
    "editName": "Name",
    "editQty": "Quantity",
    "editSection": "Category",
    "editNewCategory": "+ New",
    "editNewCategoryLabel": "Category name",
    "editNewCategoryAdd": "Add",
    "editSave": "Save",
    "editCancel": "Cancel",
    "editEmpty": "Name cannot be empty",
    "actionDuplicate": "Duplicate",
    "actionDelete": "Delete"
    ```

    Validation JSON : `node -e "JSON.parse(require('fs').readFileSync('locales/fr/common.json','utf8'))"` doit ne rien afficher.
  </action>
  <verify>
    <automated>node -e "JSON.parse(require('fs').readFileSync('locales/fr/common.json','utf8')); JSON.parse(require('fs').readFileSync('locales/en/common.json','utf8')); console.log('OK')"</automated>
  </verify>
  <done>12 clés ajoutées sous `meals.shopping` dans fr + en, JSON valide, libellés FR cohérents avec le ton du reste du fichier.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` ne produit aucune NOUVELLE erreur (les erreurs pré-existantes documentées dans CLAUDE.md — MemoryEditor.tsx, cooklang.ts, useVault.ts — sont ignorées).
- Aucune couleur hardcoded (#XXX) dans `components/CourseItemEditor.tsx` (grep `#[0-9a-fA-F]{3,6}` doit retourner 0 hits dans ce fichier).
- Test manuel : ouvrir onglet courses, taper sur le texte d'un item → bottom sheet s'ouvre avec nom + qté + catégorie pré-remplis. Modifier nom → Enregistrer → l'item se met à jour avec opacity pending puis revient normal. Long-press → Alert avec 3 options.
- Test manuel : changer la catégorie d'un item → l'item bouge dans la bonne section après le write. Toggle d'un item coché préservé après édition.
- Test manuel : créer une nouvelle catégorie inline (emoji + label) → la section apparaît, l'item y est rangé.
- Aucune ref orpheline aux symboles supprimés (`categoryPickerItem`, `handleChangeCourseCategory`, `handleSelectCategory`, `handleAddNewCategory`) dans meals.tsx.
</verification>

<success_criteria>
- 1 méthode hook ajoutée (updateCourseItem) avec optimistic + rollback + préservation [x].
- 1 helper texte (lib/courses-text.ts) + COURSE_TEXT_RE exporté depuis auto-courses.ts.
- 1 nouveau composant Modal pageSheet (CourseItemEditor.tsx) thème-strict.
- meals.tsx : ancien picker catégorie remplacé par bottom sheet unifié, long-press = menu contextuel.
- 12 clés i18n FR + EN ajoutées.
- `npx tsc --noEmit` clean (modulo erreurs pré-existantes).
</success_criteria>

<output>
After completion, create `.planning/quick/260428-gh1-phase-c-courses-edition-nom-qte-categori/260428-gh1-SUMMARY.md` documenting :
- Décisions prises (préservation [x], no-op patch identique, gestion unités comptables vs poids dans split/join)
- Fichiers modifiés/créés (avec lignes ajoutées)
- Pitfalls rencontrés (s'il y en a)
- Suite logique : Phase D (multi-listes) ou Phase E (prix lecture-only)
</output>
