---
phase: quick-260405-0wx
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/mascot/farm-engine.ts
  - packages/core/src/mascot/wear-engine.ts
  - packages/core/src/mascot/index.ts
  - apps/desktop/src/lib/farm-vault.ts
  - apps/desktop/src/pages/Tree.tsx
  - apps/desktop/src/pages/Tree.css
autonomous: true
requirements: [QUICK]

must_haves:
  truths:
    - "Core farm-engine.ts est identique entre mobile et desktop (memes exports, meme logique FIFO hybride)"
    - "wear-engine.ts existe dans le package core avec tous les exports du mobile"
    - "Desktop farm-vault.ts sait lire/ecrire les wear_events et appeler checkWearEvents/repairWearEvent"
    - "Desktop Tree.tsx affiche les overlays d'usure (cloture, herbes, nuisibles, toit) et permet la reparation"
    - "Le plot principal FIFO est visuellement distingue dans la grille desktop"
  artifacts:
    - path: "packages/core/src/mascot/wear-engine.ts"
      provides: "Systeme d'usure complet"
      exports: ["checkWearEvents", "repairWearEvent", "getActiveWearEffects", "getPestLoss", "serializeWearEvents", "parseWearEvents", "cleanupOldEvents", "WearEvent", "WearEffects", "WearEventType", "REPAIR_COSTS"]
    - path: "packages/core/src/mascot/farm-engine.ts"
      provides: "Farm engine synced avec mobile"
      exports: ["getMainPlotIndex", "plantCrop", "advanceFarmCrops", "parseCrops"]
    - path: "apps/desktop/src/lib/farm-vault.ts"
      provides: "Vault operations pour wear system"
      exports: ["checkWearInVault", "repairWearInVault"]
  key_links:
    - from: "apps/desktop/src/pages/Tree.tsx"
      to: "@family-vault/core wear-engine"
      via: "import getActiveWearEffects, getMainPlotIndex, REPAIR_COSTS"
      pattern: "getActiveWearEffects|getMainPlotIndex|REPAIR_COSTS"
    - from: "apps/desktop/src/pages/Tree.tsx"
      to: "apps/desktop/src/lib/farm-vault.ts"
      via: "import checkWearInVault, repairWearInVault"
      pattern: "checkWearInVault|repairWearInVault"
---

<objective>
Synchroniser les changements ferme mobile vers le desktop : mettre a jour farm-engine.ts dans le core (FIFO hybride, getMainPlotIndex, wearEffects sur plantCrop, parseFloat dans parseCrops), creer wear-engine.ts dans le core, ajouter les operations vault wear dans farm-vault.ts, et integrer le systeme d'usure + indicateur FIFO dans Tree.tsx.

Purpose: Le desktop doit avoir les memes mecaniques de ferme que le mobile — usure, FIFO hybride, blocage parcelles — pour coherence cross-platform.
Output: Core engine synchronise, vault operations ajoutees, UI desktop avec overlays usure et indicateur plot principal.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@lib/mascot/farm-engine.ts (source mobile — reference)
@lib/mascot/wear-engine.ts (source mobile — reference)
@packages/core/src/mascot/farm-engine.ts (desktop — a mettre a jour)
@packages/core/src/mascot/index.ts (desktop — ajouter export wear-engine)
@apps/desktop/src/lib/farm-vault.ts (desktop — ajouter wear operations)
@apps/desktop/src/pages/Tree.tsx (desktop — ajouter UI usure + FIFO indicator)

<interfaces>
<!-- Core package types needed (from packages/core/src/mascot/types.ts) -->
PlantedCrop { plotIndex, cropId, currentStage, tasksCompleted, plantedAt, isGolden }
PlacedBuilding { buildingId, cellId, level, lastCollectAt }
FarmInventory { oeuf, lait, farine, miel }
TreeStage = 'graine' | 'pousse' | 'arbuste' | 'arbre' | 'majestueux' | 'legendaire'

<!-- farm-vault.ts helpers already available -->
patchProfileField(content, profileId, key, value) -> string
patchProfileFields(content, profileId, fields) -> string
readProfileField(familleContent, profileId, key) -> string (private, defined in file)
readCoins(gamiContent, profileName) -> number
updateCoins(gamiContent, profileName, delta) -> string
addGamiHistory(gamiContent, profileId, action, note) -> string

<!-- Desktop Tree.tsx farm-vault imports -->
plantCropInVault, harvestCropInVault, sellHarvestInVault,
collectBuildingInVault, buyBuildingInVault, upgradeBuildingInVault
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tache 1: Synchroniser core engines — farm-engine.ts + wear-engine.ts + index.ts</name>
  <files>
    packages/core/src/mascot/farm-engine.ts
    packages/core/src/mascot/wear-engine.ts
    packages/core/src/mascot/index.ts
  </files>
  <action>
**farm-engine.ts** — Appliquer les 4 changements du mobile sur le fichier desktop existant :

1. Ajouter `import { type WearEffects } from './wear-engine';` en haut (apres import TechBonuses)
2. Modifier `plantCrop()` : ajouter parametre optionnel `wearEffects?: WearEffects` apres `cropId`. Ajouter au debut du corps : `if (wearEffects?.blockedPlots.includes(plotIndex)) { throw new Error('Parcelle bloquée par une clôture cassée'); }` (avant le check "parcelle vide")
3. Modifier `advanceFarmCrops()` : remplacer la logique FIFO single-target par le systeme hybride. Au lieu d'avancer seulement le premier crop non-mature, boucler sur TOUS les crops non-matures. Le plot principal (premier non-mature dans le tri FIFO) recoit `seasonBonus` complet, les autres recoivent `seasonBonus * 0.5`. Chaque crop est evalue individuellement pour le passage de stade. Copier la logique exacte du mobile (lignes 81-123 de lib/mascot/farm-engine.ts).
4. Ajouter la fonction `getMainPlotIndex(crops: PlantedCrop[]): number | null` — copier du mobile (lignes 129-136)
5. Modifier `parseCrops()` : changer `parseInt(tasksCompleted, 10)` en `parseFloat(tasksCompleted)` pour supporter les demi-vitesses

**wear-engine.ts** — Copier le fichier complet depuis `lib/mascot/wear-engine.ts` (mobile) vers `packages/core/src/mascot/wear-engine.ts`. C'est un fichier pur sans dependances React, il est directement compatible.

**index.ts** — Ajouter la ligne `export * from './wear-engine';` dans le barrel export (apres la ligne `export * from './farm-engine';`).
  </action>
  <verify>
    <automated>cd /Users/gabrielwaltio/Documents/family-vault && npx tsc --noEmit -p packages/core/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <done>
    - farm-engine.ts a plantCrop avec wearEffects optionnel, advanceFarmCrops hybride FIFO, getMainPlotIndex, parseCrops avec parseFloat
    - wear-engine.ts existe dans core avec tous les exports (checkWearEvents, repairWearEvent, getActiveWearEffects, getPestLoss, serialize/parse, cleanupOldEvents)
    - index.ts re-exporte wear-engine
    - Compilation TypeScript passe
  </done>
</task>

<task type="auto">
  <name>Tache 2: Ajouter wear operations dans farm-vault.ts + integrer UI dans Tree.tsx</name>
  <files>
    apps/desktop/src/lib/farm-vault.ts
    apps/desktop/src/pages/Tree.tsx
    apps/desktop/src/pages/Tree.css
  </files>
  <action>
**farm-vault.ts** — Ajouter les imports wear-engine depuis core et 2 nouvelles fonctions :

1. Ajouter aux imports core : `parseWearEvents, serializeWearEvents, checkWearEvents, repairWearEvent, getActiveWearEffects, cleanupOldEvents, REPAIR_COSTS, type WearEvent, type WearEffects`
2. Ajouter `import { type PlantedCrop, type PlacedBuilding } from '@family-vault/core';` si pas deja present (PlantedCrop et PlacedBuilding sont deja importes, verifier WearEvent/WearEffects)

Fonction `checkWearInVault(readFile, writeFile, profileId, crops, buildings, totalPlots)`:
- Lire famille.md, extraire `wear_events` du profil via readProfileField
- Parser avec parseWearEvents
- Appeler cleanupOldEvents puis checkWearEvents (fullBuildingSince={} pour simplifier)
- Si nouveaux events, concatener aux existants et ecrire avec patchProfileField + serializeWearEvents
- Retourner getActiveWearEffects sur la liste finale

Fonction `repairWearInVault(readFile, writeFile, profileId, profileName, eventId)`:
- Lire famille.md, parser wear_events
- Lire gami pour les coins (readCoins)
- Appeler repairWearEvent (si null, throw "Reparation impossible")
- Ecrire wear_events mis a jour dans famille.md
- Deduire le cout des coins via updateCoins + addGamiHistory
- Retourner le cout

Modifier `plantCropInVault` : passer wearEffects comme 4e argument a plantCrop. Avant l'appel, lire wear_events et calculer getActiveWearEffects.

**Tree.tsx** — Integrer le systeme d'usure et l'indicateur FIFO :

1. Ajouter aux imports core : `getActiveWearEffects, parseWearEvents, getMainPlotIndex, REPAIR_COSTS, type WearEffects, type WearEventType`
2. Ajouter aux imports farm-vault : `checkWearInVault, repairWearInVault`
3. Ajouter state : `const [wearEffects, setWearEffects] = useState<WearEffects>({ blockedPlots: [], damagedBuildings: [], weedyPlots: [], pestBuildings: [] });` et `const [wearEvents, setWearEvents] = useState<WearEvent[]>([]);` et `const [mainPlotIdx, setMainPlotIdx] = useState<number | null>(null);`
4. Dans le useEffect qui charge les donnees farm (celui qui parse farm_crops, farm_buildings), ajouter : parser wear_events du profil, calculer wearEffects, calculer mainPlotIdx via getMainPlotIndex(crops)
5. Appeler checkWearInVault au chargement initial (une fois par session) pour generer de nouveaux events si conditions remplies
6. Dans le rendu des cellules crop (CROP_CELLS), ajouter des overlays visuels :
   - Si `wearEffects.blockedPlots.includes(plotIndex)` : overlay orange semi-transparent avec icone cloture cassee (emoji ou texte "Cloture cassee") + bouton "Reparer ({REPAIR_COSTS.broken_fence} feuilles)"
   - Si `wearEffects.weedyPlots.includes(plotIndex)` : overlay vert clair avec icone herbes + bouton "Desherber (gratuit)"
   - Si `plotIndex === mainPlotIdx` et crop non-mature : bordure doree ou indicateur etoile pour montrer le plot principal FIFO
7. Dans le rendu des cellules building (BUILDING_CELLS), ajouter :
   - Si `wearEffects.damagedBuildings.includes(cellId)` : overlay avec icone toit abime + bouton "Reparer ({REPAIR_COSTS.damaged_roof} feuilles)"
   - Si `wearEffects.pestBuildings.includes(cellId)` : overlay avec icone nuisibles + bouton "Chasser (gratuit)"
8. Handler `handleRepairWear(eventId)` : appeler repairWearInVault, puis re-lire les donnees et mettre a jour wearEffects/wearEvents state. Afficher le cout dans un toast ou alert.
9. Bloquer le bouton "Planter" sur les parcelles bloquees (disabled si blockedPlots.includes(plotIndex))

**Tree.css** — Ajouter les styles pour les overlays :
- `.wear-overlay` : position absolute, inset 0, border-radius herite, display flex align center justify center, z-index au-dessus du contenu cell
- `.wear-overlay--fence` : background rgba(255, 165, 0, 0.3) (orange)
- `.wear-overlay--weeds` : background rgba(76, 175, 80, 0.25) (vert)
- `.wear-overlay--roof` : background rgba(255, 87, 34, 0.3) (rouge-orange)
- `.wear-overlay--pests` : background rgba(139, 69, 19, 0.3) (brun)
- `.wear-repair-btn` : petit bouton style pill dans l'overlay, cursor pointer, font-size 11px
- `.main-plot-indicator` : border 2px solid gold, box-shadow 0 0 8px rgba(255,215,0,0.5)
  </action>
  <verify>
    <automated>cd /Users/gabrielwaltio/Documents/family-vault && npx tsc --noEmit -p apps/desktop/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <done>
    - farm-vault.ts exporte checkWearInVault et repairWearInVault
    - plantCropInVault passe wearEffects a plantCrop (bloque si parcelle cassee)
    - Tree.tsx affiche les overlays d'usure sur les cellules affectees avec boutons de reparation
    - Tree.tsx affiche l'indicateur de plot principal FIFO (bordure doree)
    - Les boutons de reparation appellent repairWearInVault et mettent a jour l'etat
    - Le bouton Planter est desactive sur les parcelles bloquees
    - Compilation TypeScript passe
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit -p packages/core/tsconfig.json` — pas d'erreur
2. `npx tsc --noEmit -p apps/desktop/tsconfig.json` — pas d'erreur
3. Verifier que `getMainPlotIndex` et `WearEffects` sont exportes depuis `@family-vault/core`
4. Verifier que plantCropInVault rejette les parcelles bloquees
</verification>

<success_criteria>
- Core farm-engine.ts est synchronise avec le mobile (4 changements appliques)
- wear-engine.ts existe dans packages/core/src/mascot/ avec tous les exports
- Desktop farm-vault.ts a checkWearInVault et repairWearInVault
- Desktop Tree.tsx affiche overlays usure + indicateur FIFO + boutons reparation
- Compilation TypeScript passe sur core et desktop
</success_criteria>

<output>
Apres completion, creer `.planning/quick/260405-0wx-r-pliquer-changements-ferme-mobile-sur-d/260405-0wx-SUMMARY.md`
</output>
