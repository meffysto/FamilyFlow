---
phase: quick
plan: ps4
type: execute
wave: 1
depends_on: []
files_modified:
  - components/mascot/FarmPlots.tsx
  - components/mascot/WorldGridView.tsx
  - app/(tabs)/tree.tsx
  - components/mascot/TreeView.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "Les dots de croissance des plantes sont marron/terre, visibles sur fond vert"
    - "Le tooltip CropWhisper ne deborde jamais des bords de l'ecran"
    - "Les bulles de pensee des animaux ne debordent pas du conteneur visible"
  artifacts:
    - path: "components/mascot/FarmPlots.tsx"
      provides: "Couleur marron pour stageDotFilled"
      contains: "#8B6914"
    - path: "components/mascot/WorldGridView.tsx"
      provides: "Couleur marron pour stageDotFilled"
      contains: "#8B6914"
    - path: "app/(tabs)/tree.tsx"
      provides: "Clamp du CropWhisper tooltip"
      contains: "Math.min"
    - path: "components/mascot/TreeView.tsx"
      provides: "Clamp de la bulle animaux"
      contains: "Math.max"
  key_links: []
---

<objective>
Corriger deux problemes visuels dans la vue arbre/ferme mascotte :
1. Les dots de stade de croissance des plantes sont verts sur fond vert (invisibles) — les passer en marron/terre
2. Les tooltips (CropWhisper + bulles animaux) debordent de l'ecran quand les elements sont pres des bords

Purpose: Ameliorer la lisibilite et eviter le clipping visuel
Output: 4 fichiers modifies avec corrections cosmetiques
</objective>

<execution_context>
@/Users/gabrielwaltio/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gabrielwaltio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@components/mascot/FarmPlots.tsx
@components/mascot/WorldGridView.tsx
@app/(tabs)/tree.tsx
@components/mascot/TreeView.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dots croissance en marron + CropWhisper clamp</name>
  <files>components/mascot/FarmPlots.tsx, components/mascot/WorldGridView.tsx, app/(tabs)/tree.tsx</files>
  <action>
1. **FarmPlots.tsx ligne 229** — Changer `stageDotFilled: { backgroundColor: '#4ADE80' }` en `backgroundColor: '#8B6914'` (marron terre, bon contraste sur vert).

2. **WorldGridView.tsx ligne 283** — Meme changement : `stageDotFilled: { backgroundColor: '#4ADE80' }` en `backgroundColor: '#8B6914'`.

3. **tree.tsx lignes 110-111** — Clamper le positionnement du CropWhisper tooltip. Le tooltip fait environ 140px de large (paddingHorizontal 8 + texte). Remplacer :
```typescript
const wx = cell.x * SCREEN_W - 60;
const wy = cell.y * (DIORAMA_HEIGHT_BY_STAGE[stageIdx] ?? SCREEN_H * 0.60) - 40;
```
par :
```typescript
const TOOLTIP_W = 140;
const TOOLTIP_H = 28;
const rawWx = cell.x * SCREEN_W - TOOLTIP_W / 2;
const rawWy = cell.y * (DIORAMA_HEIGHT_BY_STAGE[stageIdx] ?? SCREEN_H * 0.60) - TOOLTIP_H - 12;
const wx = Math.max(4, Math.min(rawWx, SCREEN_W - TOOLTIP_W - 4));
const wy = Math.max(4, rawWy);
```
Le 4px de marge empeche le tooltip de coller au bord exact.
  </action>
  <verify>
    <automated>cd /Users/gabrielwaltio/Documents/family-vault && npx tsc --noEmit 2>&1 | grep -E "FarmPlots|WorldGridView|tree\\.tsx" || echo "OK - no type errors in modified files"</automated>
  </verify>
  <done>Les dots de croissance affichent #8B6914 (marron) au lieu de #4ADE80 (vert). Le CropWhisper tooltip est clampe entre 4px et SCREEN_W - TOOLTIP_W - 4px horizontalement, et >= 4px verticalement.</done>
</task>

<task type="auto">
  <name>Task 2: Clamp bulle de pensee animaux</name>
  <files>components/mascot/TreeView.tsx</files>
  <action>
Dans la fonction `AnimatedAnimal` (ligne ~2008), ajouter un parametre `containerWidth: number` a la signature.

A l'appel de `AnimatedAnimal` (ligne ~376), passer `containerWidth={size}` (le `size` du TreeView qui est la largeur du conteneur parent).

Dans le rendu de la bulle de pensee (lignes 2090-2102), remplacer le positionnement fixe :
```typescript
left: size / 2 - 14,
```
par un calcul clampe :
```typescript
left: Math.max(-x, Math.min(size / 2 - 14, containerWidth - x - 28)),
```

Explication du clamp :
- `Math.max(-x, ...)` : empeche la bulle de sortir a gauche (position absolue dans le parent = x + left >= 0)
- `Math.min(..., containerWidth - x - 28)` : empeche la bulle de sortir a droite (x + left + bubbleMinWidth <= containerWidth)
- `size / 2 - 14` reste la position centree par defaut

Note: `size` ici = la taille du sprite animal (le prop existant), `containerWidth` = nouvelle prop.
  </action>
  <verify>
    <automated>cd /Users/gabrielwaltio/Documents/family-vault && npx tsc --noEmit 2>&1 | grep -E "TreeView" || echo "OK - no type errors in TreeView"</automated>
  </verify>
  <done>La bulle de pensee des animaux est clampee pour rester dans le conteneur visible. Quand un animal est au bord gauche, la bulle se decale a droite. Quand il est au bord droit, elle se decale a gauche.</done>
</task>

</tasks>

<verification>
npx tsc --noEmit — pas de nouvelles erreurs TypeScript dans les 4 fichiers modifies.
Verification visuelle recommandee : ouvrir l'arbre avec des plantes en croissance et des animaux positionnes pres des bords.
</verification>

<success_criteria>
- stageDotFilled utilise #8B6914 dans FarmPlots.tsx et WorldGridView.tsx
- CropWhisper tooltip clampe entre 4px et SCREEN_W - TOOLTIP_W - 4px
- Bulle animaux clampee dans le containerWidth
- Aucune nouvelle erreur TypeScript
</success_criteria>

<output>
After completion, create `.planning/quick/260328-ps4-dots-croissance-marron-tooltips-clamp-bo/260328-ps4-SUMMARY.md`
</output>
