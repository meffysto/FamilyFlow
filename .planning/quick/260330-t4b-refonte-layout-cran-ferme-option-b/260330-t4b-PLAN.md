---
phase: quick
plan: 260330-t4b
type: execute
wave: 1
depends_on: []
files_modified:
  - app/(tabs)/tree.tsx
  - components/mascot/WorldGridView.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "Le diorama affiche un arbre plus petit avec plus d'espace pour les parcelles"
    - "Un HUD overlay en haut du diorama montre feuilles, streak, plantations, saison"
    - "Les cellules batiment posees n'ont plus de fond violet"
    - "La section Toutes les evolutions est supprimee"
    - "Le titre Mon arbre et le badge saison sont supprimes"
  artifacts:
    - path: "app/(tabs)/tree.tsx"
      provides: "Layout refonte Option B"
    - path: "components/mascot/WorldGridView.tsx"
      provides: "BuildingCell sans fond violet quand batiment pose"
  key_links:
    - from: "app/(tabs)/tree.tsx"
      to: "components/mascot/WorldGridView.tsx"
      via: "WorldGridView component usage"
      pattern: "WorldGridView"
---

<objective>
Refonte layout ecran ferme (tree.tsx) selon Option B : diorama agrandi avec arbre plus petit, HUD overlay en haut, suppression des elements redondants (titre, badge saison, section toutes les evolutions), et nettoyage visuel des cellules batiment.

Purpose: Rendre l'ecran ferme plus immersif et compact — le diorama devient le hero, les stats montent dans un HUD overlay, les sections redondantes disparaissent.
Output: Ecran ferme refactorise avec layout Option B.
</objective>

<execution_context>
@/Users/gabrielwaltio/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gabrielwaltio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@app/(tabs)/tree.tsx
@components/mascot/WorldGridView.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Nettoyage layout tree.tsx — supprimer sections redondantes + reduire arbre</name>
  <files>app/(tabs)/tree.tsx</files>
  <action>
1. Reduire TREE_SIZE : changer `Math.min(SCREEN_W * 0.9, 400)` en `Math.min(SCREEN_W * 0.65, 280)` (arbre plus petit pour laisser de l'espace aux parcelles/batiments).

2. Supprimer le header "Mon arbre" (lignes ~518-527) : le bloc View avec styles.header contenant le backBtn, le titre i18n 'mascot.screen.title', et le View spacer. Garder tout ce qui suit (bandeau saga, bandeau placement).

3. Supprimer le badge saison (lignes ~530-534) : le View avec styles.seasonBadge contenant seasonInfo.emoji + t(seasonInfo.labelKey). La saison sera affichee dans le HUD.

4. Supprimer la section "Toutes les evolutions" (lignes ~1002-1040) : le bloc Animated.View contenant le titre t('mascot.screen.allStages') et le ScrollView horizontal avec TREE_STAGES.map. Garder la section "prochaine evolution" dans l'info card (lignes ~960-998).

5. Ajouter un HUD overlay en haut du diorama. Juste apres l'ouverture du View diorama (apres la ligne `{/* Diorama */}`), ajouter un composant HUD en position absolue :

```tsx
{/* HUD ferme */}
<View style={styles.farmHud}>
  <LinearGradient
    colors={['rgba(0,0,0,0.5)', 'rgba(0,0,0,0)']}
    style={StyleSheet.absoluteFill}
  />
  <View style={styles.hudContent}>
    <View style={styles.hudItem}>
      <Text style={styles.hudEmoji}>{'🍃'}</Text>
      <Text style={styles.hudValue}>{profile.coins ?? 0}</Text>
    </View>
    <View style={styles.hudItem}>
      <Text style={styles.hudEmoji}>{'🔥'}</Text>
      <Text style={styles.hudValue}>{profile.streak ?? 0}</Text>
    </View>
    <View style={styles.hudItem}>
      <Text style={styles.hudEmoji}>{'🌿'}</Text>
      <Text style={styles.hudValue}>{growingCount}</Text>
    </View>
    <View style={styles.hudItem}>
      <Text style={styles.hudEmoji}>{seasonInfo.emoji}</Text>
      <Text style={styles.hudValue}>{t(seasonInfo.labelKey)}</Text>
    </View>
  </View>
</View>
```

Le HUD doit etre positionne en absolute, top: 0, left: 0, right: 0, zIndex: 10, avec un gradient semi-transparent. Utiliser SafeAreaView ou un padding-top inset si necessaire.

6. Calculer `growingCount` avant le return : compter les crops en cours de croissance (currentStage < 4) depuis `parseCrops(profile.farmCrops ?? '')`. Importer parseCrops depuis lib/mascot/farm-engine si pas deja importe.

7. Ajouter les styles pour le HUD dans le StyleSheet.create :
- `farmHud`: position absolute, top 0, left 0, right 0, zIndex 10, paddingTop Spacing.md, paddingHorizontal Spacing.lg, overflow 'hidden', borderTopLeftRadius et borderTopRightRadius pour matcher le diorama
- `hudContent`: flexDirection 'row', justifyContent 'space-between', alignItems 'center'
- `hudItem`: flexDirection 'row', alignItems 'center', gap Spacing.xxs
- `hudEmoji`: fontSize 14
- `hudValue`: color '#fff', fontSize FontSize.sm, fontWeight '600'

8. Preparer l'integration future du compagnon animal : ajouter un commentaire dans le diorama juste avant le TreeView :
```tsx
{/* TODO: Zone compagnon animal — sera rendu ici a cote de l'arbre */}
{/* <PetCompanion pet={profile.pet} position={{ x: 0.3, y: 0.7 }} /> */}
```

9. Supprimer les sections FarmStats et StreakFlames en dessous du diorama (lignes ~832-836) puisque ces infos sont maintenant dans le HUD. Garder WeeklyGoal.
  </action>
  <verify>npx tsc --noEmit 2>&1 | head -30</verify>
  <done>Le header, badge saison, section toutes les evolutions, FarmStats et StreakFlames sont supprimes. L'arbre est plus petit (0.65 au lieu de 0.9). Le HUD overlay avec feuilles/streak/plantations/saison est visible en haut du diorama. Commentaire placeholder pour le compagnon animal est present.</done>
</task>

<task type="auto">
  <name>Task 2: Supprimer fond violet des cellules batiment posees dans WorldGridView</name>
  <files>components/mascot/WorldGridView.tsx</files>
  <action>
Dans le composant BuildingCell (ligne ~216), modifier le style de la TouchableOpacity pour conditionner le fond violet :

Actuellement :
```tsx
<TouchableOpacity onPress={onPress} activeOpacity={0.7} style={styles.buildingCell}>
```

Changer en :
```tsx
<TouchableOpacity onPress={onPress} activeOpacity={0.7} style={[
  styles.buildingCell,
  placedBuilding && styles.buildingCellPlaced,
]}>
```

Ajouter le style `buildingCellPlaced` dans le StyleSheet :
```tsx
buildingCellPlaced: {
  backgroundColor: 'transparent',
  borderColor: 'transparent',
  borderWidth: 0,
},
```

Cela conserve le fond violet pour les cellules vides (invitant a poser un batiment) mais le supprime quand un batiment est effectivement pose — le sprite du batiment s'integre directement dans le diorama sans cadre violet.
  </action>
  <verify>npx tsc --noEmit 2>&1 | head -30</verify>
  <done>Les cellules batiment avec un batiment pose n'ont plus de fond/bordure violet. Les cellules vides gardent le style violet existant.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passe sans nouvelles erreurs
- L'ecran tree.tsx compile et le layout est coherent
- Le HUD s'affiche en haut du diorama avec les 4 stats
- Les cellules batiment posees n'ont plus de fond violet
- Les sections supprimees (header, badge saison, toutes les evolutions, FarmStats, StreakFlames) ne sont plus presentes
</verification>

<success_criteria>
- L'arbre est plus petit dans le diorama (TREE_SIZE reduit a 0.65)
- Le HUD overlay affiche feuilles, streak, plantations en cours, et saison
- Pas de fond violet sur les cellules batiment quand un batiment est pose
- La section "Toutes les evolutions" est supprimee
- Le titre "Mon arbre" et le badge saison sont supprimes
- FarmStats et StreakFlames sous le diorama sont supprimes (infos dans le HUD)
- Commentaire placeholder pour compagnon animal present dans le diorama
- Compilation TypeScript sans nouvelles erreurs
</success_criteria>

<output>
After completion, create `.planning/quick/260330-t4b-refonte-layout-cran-ferme-option-b/260330-t4b-SUMMARY.md`
</output>
