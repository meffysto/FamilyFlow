---
phase: quick
plan: 260402-x63
type: execute
wave: 1
depends_on: []
files_modified: [app/(tabs)/tree.tsx]
autonomous: true
requirements: [HUD-FLOAT]
must_haves:
  truths:
    - "Le HUD ferme flotte en position absolute par-dessus le ScrollView"
    - "Le fond du SafeAreaView est transparent — le monde pixel est visible derriere"
    - "Le contenu du ScrollView ne passe pas sous le HUD (paddingTop compense)"
    - "Le HUD a un fond semi-transparent adapte au mode dark/light"
  artifacts:
    - path: "app/(tabs)/tree.tsx"
      provides: "HUD flottant absolute + SafeAreaView transparent"
  key_links:
    - from: "farmHud style"
      to: "useSafeAreaInsets().top"
      via: "top offset pour positionner sous la status bar"
---

<objective>
Rendre le farmHud flottant en position absolute par-dessus le ScrollView sur l'ecran tree, avec fond semi-transparent et SafeAreaView transparent pour que le monde pixel soit le fond visible.

Purpose: Le HUD doit superposer le diorama au lieu de pousser le contenu vers le bas — effet immersif.
Output: tree.tsx modifie avec HUD absolute, padding compense, SafeAreaView transparent.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@app/(tabs)/tree.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: HUD flottant absolute + SafeAreaView transparent</name>
  <files>app/(tabs)/tree.tsx</files>
  <action>
Modifier `app/(tabs)/tree.tsx` :

1. **Import** : ajouter `useSafeAreaInsets` depuis `react-native-safe-area-context` (a cote du SafeAreaView existant ligne 25).

2. **Hook** : dans le composant, ajouter `const insets = useSafeAreaInsets();` pres des autres hooks.

3. **SafeAreaView** : remplacer le wrapper `SafeAreaView` par une simple `View` avec `style={styles.safe}` (PAS de backgroundColor). On ne veut plus le SafeAreaView car le HUD gere lui-meme le top inset. Garder `flex: 1` sur styles.safe.

4. **farmHud JSX** (ligne ~963) : deplacer le bloc `<View style={[styles.farmHud, ...]}> ... </View>` APRES le `<ScrollView>` dans le JSX (pour qu'il soit rendu par-dessus via zIndex). Appliquer le style inline :
   ```
   style={[
     styles.farmHud,
     {
       position: 'absolute',
       top: insets.top,
       left: 0,
       right: 0,
       zIndex: 10,
       backgroundColor: isDark ? 'rgba(0,0,0,0.55)' : 'rgba(255,255,255,0.7)',
     }
   ]}
   ```

5. **ScrollView contentContainerStyle** (ligne ~984) : ajouter `paddingTop: insets.top + 44` dans le contentContainerStyle pour compenser la hauteur du HUD flottant. Le 44 correspond a la hauteur du HUD (paddingVertical Spacing.sm * 2 + line height ~20 + marge). Resultat :
   ```
   contentContainerStyle={[styles.scroll, Layout.contentContainer, { paddingTop: insets.top + 44 }]}
   ```

6. **styles.farmHud** (ligne ~1641) : garder paddingVertical et paddingHorizontal existants. NE PAS ajouter position/top/left/right ici (ils sont en inline car dependent de insets dynamique).

Notes :
- NE PAS utiliser BlurView (cout perf inutile, le rgba suffit).
- Utiliser `View` au lieu de `SafeAreaView` comme wrapper racine car le HUD gere le safe area top manuellement.
  </action>
  <verify>
    <automated>cd /Users/gabrielwaltio/Documents/family-vault && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
- Le HUD ferme est en position absolute, flotte par-dessus le ScrollView
- SafeAreaView remplace par View sans backgroundColor
- Le ScrollView a un paddingTop = insets.top + 44 pour ne pas etre cache par le HUD
- Le HUD a un fond semi-transparent (dark: rgba(0,0,0,0.55), light: rgba(255,255,255,0.7))
- tsc --noEmit passe sans nouvelles erreurs
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` ne produit pas de nouvelles erreurs
- Visuellement : le HUD flotte au-dessus du diorama, le monde pixel est visible en fond
</verification>

<success_criteria>
Le HUD ferme est en overlay absolu sur l'ecran tree, le SafeAreaView est transparent, et le contenu scroll est decale pour ne pas passer sous le HUD.
</success_criteria>

<output>
After completion, create `.planning/quick/260402-x63-hud-flottant-option-a-sur-l-cran-tree-re/260402-x63-SUMMARY.md`
</output>
