---
phase: quick-260428-g5n
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/types.ts
  - hooks/useVaultCourses.ts
  - app/(tabs)/meals.tsx
autonomous: true
requirements: [COURSES-OPTIMISTIC-B]
must_haves:
  truths:
    - "Ajouter un article apparaît instantanément dans la liste (avant write vault)"
    - "Cocher / décocher un article met à jour la checkbox sans attendre l'I/O vault"
    - "Supprimer un article le retire immédiatement de la liste"
    - "En cas d'erreur d'écriture vault, l'état UI revient à l'état pré-action (rollback)"
    - "API publique UseVaultCoursesResult inchangée — meals.tsx compile sans changement de signature"
  artifacts:
    - path: "lib/types.ts"
      provides: "CourseItem.pending?: boolean (transient, non sérialisé)"
    - path: "hooks/useVaultCourses.ts"
      provides: "6 writes optimistes (add/toggle/remove/move/merge/clear) + coursesRef snapshot"
    - path: "app/(tabs)/meals.tsx"
      provides: "Style opacity 0.6 conditionnel sur courseRow quand item.pending"
  key_links:
    - from: "useVaultCourses.addCourseItem"
      to: "setCourses (optimiste) + enqueueWrite (réconciliation parseCourses)"
      via: "tempId + filter rollback"
      pattern: "tmp-.*"
    - from: "useVaultCourses (toggle/remove/move/merge/clear)"
      to: "coursesRef.current snapshot"
      via: "useEffect sync sur courses"
      pattern: "coursesRef.current = courses"
    - from: "meals.tsx courseRow"
      to: "item.pending"
      via: "style conditionnel { opacity: 0.6 }"
      pattern: "item\\.pending.*opacity"
---

<objective>
Phase B courses — Rendre instantanée la perception de tous les writes du hook `useVaultCourses` via Optimistic UI, avec rollback fiable en cas d'erreur d'écriture vault. Suite logique de Phase A 260428-fnw (queue d'écritures déjà en place).

Purpose: Supprimer la latence perçue (lecture vault iCloud + parse) sur add/toggle/remove/move/merge/clear. La queue séquentielle Phase A garantit l'ordre, l'optimiste de Phase B garantit la fluidité visuelle.
Output: Hook `useVaultCourses` 6/6 writes optimistes + indicateur visuel `pending` (opacity 0.6) sur les rows en cours d'écriture.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@hooks/useVaultCourses.ts
@lib/types.ts
@lib/courses-constants.ts

<interfaces>
<!-- Contrats à respecter strictement (API publique inchangée) -->

From hooks/useVaultCourses.ts (à NE PAS modifier en signature):
```ts
export interface UseVaultCoursesResult {
  courses: CourseItem[];
  setCourses: (courses: CourseItem[]) => void;
  addCourseItem: (text: string, section?: string) => Promise<void>;
  toggleCourseItem: (item: CourseItem, completed: boolean) => Promise<void>;
  removeCourseItem: (lineIndex: number) => Promise<void>;
  moveCourseItem: (lineIndex: number, text: string, newSection: string) => Promise<void>;
  mergeCourseIngredients: (items: { text: string; name: string; quantity: number | null; section: string }[]) => Promise<{ added: number; merged: number }>;
  clearCompletedCourses: () => Promise<void>;
  resetCourses: () => void;
}
```

From lib/types.ts (CourseItem actuel):
```ts
export interface CourseItem {
  id: string;
  text: string;
  completed: boolean;
  lineIndex: number;
  section?: string;
}
```

From lib/courses-constants.ts:
```ts
export const COURSES_FILE_LEGACY = '02 - Maison/Liste de courses.md';
export const COURSES_DEFAULT_SECTION = '📦 Divers';
```

From lib/parser.ts:
```ts
export function parseCourses(content: string, relativePath: string): CourseItem[];
// Retourne CourseItem[] sans champ pending (le pending est purement transient/UI).
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ajouter le champ transient pending sur CourseItem</name>
  <files>lib/types.ts</files>
  <action>
    Dans `lib/types.ts` ligne 244-250, ajouter un champ optionnel `pending?: boolean` à l'interface `CourseItem`. Ajouter un commentaire d'avertissement : champ transient/UI, **JAMAIS sérialisé** dans le markdown vault — seulement présent en mémoire pendant un write optimiste, jamais persisté par parser.ts/serializer.

    Forme finale :
    ```ts
    export interface CourseItem {
      id: string;
      text: string;
      completed: boolean;
      lineIndex: number;
      section?: string;     // section header (e.g. "🥩 Frais")
      pending?: boolean;    // transient UI flag — JAMAIS sérialisé dans le vault (Phase B optimistic UI)
    }
    ```

    Aucune autre modification dans ce fichier. Ne PAS toucher `lib/parser.ts` (parseCourses retourne naturellement des items sans `pending`, c'est l'effet voulu : à la réconciliation post-write, le flag disparaît).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -i "CourseItem" || echo "OK: pas d'erreur sur CourseItem"</automated>
  </verify>
  <done>Le champ `pending?: boolean` apparaît dans `CourseItem`, `npx tsc --noEmit` ne lève aucune erreur nouvelle liée à CourseItem.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor useVaultCourses — 6 writes en Optimistic UI + rollback</name>
  <files>hooks/useVaultCourses.ts</files>
  <action>
    Refactor `hooks/useVaultCourses.ts` pour appliquer Optimistic UI sur les 6 writes. **Garder toute l'API publique strictement identique** (`UseVaultCoursesResult` inchangé). Garder `enqueueWrite` et `writeQueueRef` Phase A tels quels.

    ### A. Imports & ref de snapshot

    Ajouter au top du module :
    ```ts
    import { useState, useCallback, useRef, useEffect } from 'react';
    import { COURSES_FILE_LEGACY, COURSES_DEFAULT_SECTION } from '../lib/courses-constants';
    ```

    Dans le corps du hook, juste après `const [courses, setCourses] = useState<CourseItem[]>([])` :
    ```ts
    // Snapshot synchrone pour rollback hors du closure setState
    const coursesRef = useRef<CourseItem[]>([]);
    useEffect(() => { coursesRef.current = courses; }, [courses]);
    ```

    Helper local en haut du hook (non exporté) :
    ```ts
    const makeTempId = () => `tmp-${Date.now()}-${Math.random().toString(36).slice(2, 8)}`;
    ```

    ### B. addCourseItem — append optimiste avec tempId

    Remplacer l'implémentation actuelle par :
    1. Avant `enqueueWrite` : générer `tempId`, créer `optimistic: CourseItem = { id: tempId, text, section: section ?? COURSES_DEFAULT_SECTION, completed: false, lineIndex: -1, pending: true }`, faire `setCourses(prev => [...prev, optimistic])` synchrone.
    2. `return enqueueWrite(async () => { try { ... append + read + setCourses(parseCourses(...)) } catch (e) { setCourses(prev => prev.filter(c => c.id !== tempId)); throw e; } })`.
    3. Le `parseCourses` final remplace l'item temp par le vrai item (avec lineIndex correct, sans pending).

    ### C. toggleCourseItem — flip immédiat + rollback snapshot

    1. Avant `enqueueWrite` : `const snapshot = coursesRef.current;` puis `setCourses(prev => prev.map(c => c.id === item.id ? { ...c, completed, pending: true } : c))`.
    2. Dans `enqueueWrite` : `try { await vault.toggleTask(...); setCourses(prev => prev.map(c => c.id === item.id ? { ...c, completed, pending: false } : c)); } catch (e) { setCourses(snapshot); throw e; }`.

    ### D. removeCourseItem — retrait immédiat + rollback

    1. Avant `enqueueWrite` : `const snapshot = coursesRef.current;` puis `setCourses(prev => prev.filter(c => c.lineIndex !== lineIndex))`.
    2. Dans `enqueueWrite` : `try { ...read + splice + writeFile + setCourses(parseCourses(...))) } catch (e) { setCourses(snapshot); throw new Error(...); }`. Le re-parse réconcilie le shift des lineIndex restants.

    ### E. moveCourseItem — section update optimiste + rollback

    1. Avant `enqueueWrite` : `const snapshot = coursesRef.current;` puis `setCourses(prev => prev.map(c => c.lineIndex === lineIndex ? { ...c, section: newSection, pending: true } : c))`.
    2. Dans `enqueueWrite` : try logique actuelle (read/splice/insert/write/parseCourses → setCourses) ; catch → `setCourses(snapshot)` + re-throw.

    ### F. mergeCourseIngredients — append optimiste lourd + rollback

    1. Avant `enqueueWrite` : `const snapshot = coursesRef.current;`. Pour chaque item, créer un optimistic `CourseItem = { id: makeTempId(), text: item.text, section: item.section, completed: false, lineIndex: -1, pending: true }`. `setCourses(prev => [...prev, ...optimistics])` (la dédup réelle sera faite par parseCourses post-write — on accepte un flash de doublons pendant l'I/O, c'est correct car le parse final est source de vérité).
    2. Dans `enqueueWrite` : try logique actuelle inchangée (merge dédup + writeFile + parseCourses → setCourses) ; catch → `setCourses(snapshot)` + re-throw, `return { added, merged }` ; **gardez le retour `{ added, merged }`** (pas de changement de signature).

    ### G. clearCompletedCourses — filter immédiat + rollback

    1. Avant `enqueueWrite` : `const snapshot = coursesRef.current;` puis `setCourses(prev => prev.filter(c => !c.completed))`.
    2. Dans `enqueueWrite` : try logique actuelle (read + filter `^- [x]` + write + parseCourses → setCourses) ; catch → `setCourses(snapshot)` + re-throw.

    ### Notes implémentation

    - **Ordre critique** : la mutation optimiste DOIT se faire AVANT `enqueueWrite()` (pas dans la callback) pour être visible immédiatement, sinon elle attend la résolution de la queue.
    - Le `snapshot = coursesRef.current` doit être pris AVANT le setCourses optimiste, pour que le rollback restaure l'état pré-mutation.
    - Le `pending: true` n'est appliqué que sur les items modifiés/ajoutés ; les autres items du tableau restent inchangés.
    - Aucun `console.warn` ajouté (rester cohérent avec Phase A).
    - Conserver `warnUnexpected` pour les `mergeCourses-read` non-bloquants.
    - Tous les `useCallback` gardent leur dependency `[enqueueWrite]` (pas besoin d'ajouter `coursesRef` car les refs sont stables).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "useVaultCourses|courses" | head -20 || echo "OK"</automated>
  </verify>
  <done>
    - 6 writes (add/toggle/remove/move/merge/clear) appliquent une mutation optimiste avant `enqueueWrite`.
    - Chaque write capture un `snapshot = coursesRef.current` (sauf addCourseItem qui filtre par tempId).
    - Catch → rollback (`setCourses(snapshot)` ou `filter(tempId)`) puis `throw` (l'erreur remonte au caller).
    - `coursesRef` synchronisé via `useEffect`.
    - `UseVaultCoursesResult` strictement inchangé.
    - `npx tsc --noEmit` n'introduit aucune erreur nouvelle dans le hook.
  </done>
</task>

<task type="auto">
  <name>Task 3: Indicateur visuel pending (opacity 0.6) dans meals.tsx</name>
  <files>app/(tabs)/meals.tsx</files>
  <action>
    Dans `app/(tabs)/meals.tsx` ligne 1327, ajouter un style conditionnel `opacity: 0.6` sur la `<View>` `courseRow` quand `item.pending === true`.

    Modification ciblée — remplacer :
    ```tsx
    <View key={item.id} style={[styles.courseRow, { borderTopColor: colors.cardAlt }]}>
    ```
    par :
    ```tsx
    <View
      key={item.id}
      style={[
        styles.courseRow,
        { borderTopColor: colors.cardAlt },
        item.pending && { opacity: 0.6 },
      ]}
    >
    ```

    Aucune autre modification dans le fichier. Pas d'animation Reanimated (volontairement simple, conforme au scope). Pas de spinner. Le feedback visuel est l'opacité statique seule.

    **Attention path** : le fichier est `app/(tabs)/meals.tsx` — utiliser le tool Edit sur ce chemin (parenthèses gérées par l'API directement, pas de quoting nécessaire).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "meals\.tsx" | head -10 || echo "OK: pas d'erreur meals.tsx"</automated>
  </verify>
  <done>La `View` courseRow ligne ~1327 applique `{ opacity: 0.6 }` quand `item.pending` est truthy. `npx tsc --noEmit` n'introduit aucune erreur nouvelle dans meals.tsx.</done>
</task>

</tasks>

<verification>
**Validation TypeScript globale :**
```bash
npx tsc --noEmit
```
Doit passer modulo les erreurs pré-existantes documentées (MemoryEditor.tsx, cooklang.ts, useVault.ts) — aucune nouvelle erreur dans `lib/types.ts`, `hooks/useVaultCourses.ts`, `app/(tabs)/meals.tsx`.

**Inspection manuelle (optionnelle, non-bloquante) :**
- Lancer l'app, ajouter un article courses → apparaît instantanément (pas d'attente parse vault).
- Cocher un article → check immédiat, pas de blink.
- Supprimer un article → disparaît instantanément.
- Long-press changer catégorie → l'article migre vers la nouvelle section sans flash.
</verification>

<success_criteria>
- [ ] `CourseItem.pending?: boolean` ajouté dans `lib/types.ts` avec commentaire de garde "JAMAIS sérialisé".
- [ ] `useVaultCourses` : 6/6 writes optimistes (add/toggle/remove/move/merge/clear).
- [ ] `coursesRef` ref + `useEffect` sync présents.
- [ ] Rollback systématique en catch (snapshot ou filter tempId).
- [ ] API `UseVaultCoursesResult` strictement inchangée — aucune signature modifiée.
- [ ] `meals.tsx` applique `opacity: 0.6` conditionnel sur `courseRow` quand `item.pending`.
- [ ] `npx tsc --noEmit` ne régresse pas (zéro nouvelle erreur sur les 3 fichiers touchés).
- [ ] Hors scope respecté : pas de multi-listes, pas d'animation, pas de spinner, pas de tests unitaires.
</success_criteria>

<output>
After completion, create `.planning/quick/260428-g5n-phase-b-courses-optimistic-ui-sur-les-wr/260428-g5n-SUMMARY.md`
</output>
