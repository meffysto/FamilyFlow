---
phase: quick
plan: 260328-py4
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/parser.ts
  - hooks/useVault.ts
  - app/(tabs)/loot.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "L'onglet 'Récompenses' de loot.tsx affiche une nouvelle section 'Mes récompenses'"
    - "Un parent peut cocher un loot physique comme 'utilisé'"
    - "Un profil enfant/ado voit les loots mais le bouton est désactivé"
    - "Les loots utilisés passent dans l'onglet Historique"
    - "Les données sont persistées dans gamification.md et survivent à un refresh"
  artifacts:
    - path: "lib/parser.ts"
      provides: "parseGamification + serializeGamification supportent usedLoots[]"
    - path: "hooks/useVault.ts"
      provides: "markLootUsed(lootId) action exportée dans VaultState"
    - path: "app/(tabs)/loot.tsx"
      provides: "Section 'Mes récompenses' avec onglets À utiliser / Historique"
  key_links:
    - from: "app/(tabs)/loot.tsx"
      to: "hooks/useVault.ts"
      via: "markLootUsed(lootId)"
      pattern: "markLootUsed"
    - from: "hooks/useVault.ts"
      to: "lib/parser.ts"
      via: "serializeGamification avec usedLoots"
      pattern: "serializeGamification"
---

<objective>
Ajouter la section "Mes récompenses" dans l'écran loot pour afficher les loots physiques (rewardType === 'reward') gagnés via loot boxes. Deux sous-onglets : "À utiliser" (non-cochés) et "Historique" (utilisés). Un parent peut cocher un loot comme utilisé, un enfant/ado voit la liste mais ne peut pas cocher.

Purpose: Les familles ont actuellement aucun suivi des récompenses physiques gagnées — elles se perdent dans l'historique. Cette section donne de la visibilité et permet aux parents de valider que la récompense a bien été donnée.
Output: Nouveau onglet dans loot.tsx + persistence dans gamification.md
</objective>

<execution_context>
@/Users/gabrielwaltio/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gabrielwaltio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

Clés de compréhension du codebase :

**Structure GamificationData (lib/types.ts) :**
```typescript
export interface GamificationData {
  profiles: Profile[];
  history: GamificationEntry[];      // loot:commun, loot:rare, etc.
  activeRewards: ActiveReward[];
  // À AJOUTER : usedLoots: UsedLoot[]
}

export interface GamificationEntry {
  profileId: string;
  action: string;   // "loot:commun" | "loot:rare" | "+10" | ...
  points: number;
  note: string;     // "🍪 Un cookie/goûter au choix" pour les loots physiques
  timestamp: string; // ISO string
}
```

**Loots physiques** = history entries où `action.startsWith('loot:')` ET le note correspond à un reward de rewardType 'reward'.
Les loots physiques dans REWARDS ont `rewardType: 'reward'` (exemples : '🍪', '📱', '🎵', '🍕', '🍦', '🎮', '😴', '🎉', '🎂').

**Rôle actif** : `activeProfile?.role` est 'adulte', 'ado', ou 'enfant'. Seul 'adulte' peut cocher.

**Serialization gamification.md** : Fichier `lib/parser.ts`, fonctions `parseGamification()` et `serializeGamification()`. Nouvelle section à ajouter : `## Récompenses utilisées` avec lignes `- lootId | profileId | emoji | label | usedAt`.

**Écriture vault** : `await vault.writeFile(GAMI_FILE, serializeGamification(updatedGami))` — pattern exact de useVault.ts ligne 618.

**Tab existant** dans loot.tsx : activeTab est `'rewards' | 'collection'`. Ajouter `'mes-recompenses'` comme troisième valeur.
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Tâche 1 : Ajouter UsedLoot au modèle + parser + VaultState</name>
  <files>lib/types.ts, lib/parser.ts, hooks/useVault.ts</files>
  <action>
**lib/types.ts** — Ajouter l'interface `UsedLoot` et étendre `GamificationData` :

```typescript
export interface UsedLoot {
  id: string;          // unique : `${profileId}_${timestamp}` du loot entry original
  profileId: string;
  emoji: string;
  label: string;
  earnedAt: string;    // timestamp ISO original (de GamificationEntry)
  usedAt: string;      // timestamp ISO quand marqué utilisé
}
```

Dans `GamificationData`, ajouter le champ : `usedLoots: UsedLoot[];`

---

**lib/parser.ts** — Modifier `parseGamification()` pour parser une nouvelle section `## Récompenses utilisées` :

Format de ligne : `- {id} | {profileId} | {emoji} | {label} | {usedAt} | {earnedAt}`

Dans la boucle `for (const line of lines)`, ajouter `inUsedLoots` analogiquement à `inActiveRewards`. Parser les lignes en `UsedLoot[]`. Initialiser `const usedLoots: UsedLoot[] = [];` et retourner `{ profiles, history, activeRewards, usedLoots }`.

Modifier `serializeGamification()` pour sérialiser `usedLoots` :
```
## Récompenses utilisées
- {id} | {profileId} | {emoji} | {label} | {usedAt} | {earnedAt}
```
Si `data.usedLoots` est absent ou vide, écrire la section vide (juste le header) pour compatibilité.

---

**hooks/useVault.ts** — Ajouter `markLootUsed` dans `VaultState` et l'implémenter :

Dans `VaultState` interface, ajouter :
```typescript
markLootUsed: (loot: UsedLoot) => Promise<void>;
```

Dans l'implémentation (useVaultInternal), ajouter la fonction :
```typescript
const markLootUsed = useCallback(async (loot: UsedLoot) => {
  if (!vaultRef.current || !gamiData) return;
  const updated: GamificationData = {
    ...gamiData,
    usedLoots: [...(gamiData.usedLoots ?? []), loot],
  };
  await vaultRef.current.writeFile(GAMI_FILE, serializeGamification(updated));
  setGamiData(updated);
}, [gamiData]);
```

Exporter `markLootUsed` dans le return de `useVaultInternal` (chercher la ligne `return {` vers la fin du hook). Importer `UsedLoot` depuis `./types` si pas déjà importé.

S'assurer que `parseGamification` initialise `usedLoots: []` dans les données parsées (compatibilité backward si la section est absente du fichier).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -v "MemoryEditor\|cooklang\|useVault.ts:.*error TS" | head -20</automated>
  </verify>
  <done>
    - Interface `UsedLoot` existe dans lib/types.ts
    - `GamificationData.usedLoots: UsedLoot[]` existe
    - `parseGamification` parse la section "Récompenses utilisées"
    - `serializeGamification` écrit cette section
    - `markLootUsed` est dans VaultState et l'implémentation
    - `npx tsc --noEmit` ne produit pas de nouvelles erreurs liées à ces fichiers
  </done>
</task>

<task type="auto">
  <name>Tâche 2 : Section "Mes récompenses" dans loot.tsx</name>
  <files>app/(tabs)/loot.tsx</files>
  <action>
Dans `loot.tsx`, ajouter un troisième onglet "Mes récompenses" à la tab bar existante (`'rewards' | 'collection'` → ajouter `'mes-recompenses'`).

**Données nécessaires** (à extraire depuis useVault et gamiData) :
- `activeProfile` depuis `useVault()` (déjà disponible via le hook — ajouter à la destructuration si absent)
- `markLootUsed` depuis `useVault()`
- `usedLoots` depuis `gamiData?.usedLoots ?? []`

**Détecter les loots physiques gagnés** : Parcourir `gamiData?.history` pour trouver toutes les entrées de loot physique. Un loot est physique si `entry.action.startsWith('loot:')` ET que `entry.note` ne contient pas "Badge" ET ne commence pas par "+". Créer une liste `earnedPhysicalLoots` = history entries physiques avec leur `id = entry.profileId + '_' + entry.timestamp`.

**Filtrer** :
- `usedLootIds = new Set(usedLoots.map(u => u.id))`
- `toUse = earnedPhysicalLoots.filter(l => !usedLootIds.has(l.id))` — onglet "À utiliser"
- `used = usedLoots` — onglet "Historique" (triés par `usedAt` décroissant)

**Sous-onglets internes** à la section "Mes récompenses" :
`rewardsSubTab: 'to-use' | 'used'` — state local avec `useState<'to-use' | 'used'>('to-use')`.

**Rendu onglet "Mes récompenses"** :
```
{activeTab === 'mes-recompenses' && (
  <View style={styles.section}>
    {/* Sous-onglets compacts */}
    <View style={[styles.subTabRow, ...]}>
      <TouchableOpacity pour 'to-use' />
      <TouchableOpacity pour 'used' />
    </View>

    {/* Contenu À utiliser */}
    {rewardsSubTab === 'to-use' && (
      toUse.length === 0
        ? <Text style texte vide en français />
        : toUse.map(loot => <LootRewardCard loot={loot} canMark={activeProfile?.role === 'adulte'} onMark={...} />)
    )}

    {/* Contenu Historique */}
    {rewardsSubTab === 'used' && (
      used.length === 0
        ? <Text style texte vide />
        : used.map(usedLoot => <LootRewardCard loot={usedLoot} isUsed={true} />)
    )}
  </View>
)}
```

**Composant inline `LootRewardCard`** (défini dans le même fichier comme fonction locale) :
- Avatar du profil (trouver dans `profiles` par `profileId`)
- Emoji + label de la récompense
- Si `canMark && !isUsed` : bouton "✓ Utilisé" (`TouchableOpacity` avec `backgroundColor: colors.success`) qui appelle `markLootUsed({ id, profileId, emoji, label, earnedAt, usedAt: new Date().toISOString() })` + `Haptics.notificationAsync(Haptics.NotificationFeedbackType.Success)`
- Si `!canMark && !isUsed` : bouton désactivé avec `opacity: 0.4` et texte "Parent requis" (pas de `onPress`)
- Si `isUsed` : badge vert "✓ Utilisé" + date `usedAt` au format JJ/MM/AAAA (utiliser `date-fns/format`)

**Styles à ajouter dans StyleSheet.create** :
- `subTabRow`: flexDirection row, marginBottom 8, gap 8
- `subTab`: flex 1, paddingVertical 8, borderRadius 8, alignItems center, backgroundColor colors.bg
- `subTabActive`: backgroundColor primary
- `rewardCard`: flexDirection row, alignItems center, gap 12, padding 14, borderRadius 14, backgroundColor colors.card, marginBottom 8, ...Shadows.sm
- `markBtn`: paddingHorizontal 12, paddingVertical 8, borderRadius 10, backgroundColor colors.success
- `markBtnText`: color colors.onPrimary, fontSize FontSize.label, fontWeight FontWeight.bold
- `usedBadge`: paddingHorizontal 8, paddingVertical 4, borderRadius 8, backgroundColor colors.successBg
- `usedBadgeText`: color colors.success, fontSize FontSize.caption, fontWeight FontWeight.bold

**Tab bar** — dans la `tabRow` existante (3 onglets côte à côte) :
```tsx
<TouchableOpacity
  style={[styles.tab, activeTab === 'mes-recompenses' && { borderBottomColor: primary, borderBottomWidth: 2 }]}
  onPress={() => setActiveTab('mes-recompenses')}
>
  <Text style={[styles.tabText, { color: activeTab === 'mes-recompenses' ? primary : colors.textMuted }]}>
    Mes récompenses{toUse.length > 0 ? ` (${toUse.length})` : ''}
  </Text>
</TouchableOpacity>
```

Importer `expo-haptics` : `import * as Haptics from 'expo-haptics';`
Importer `UsedLoot` depuis `../../lib/types`.
Importer `format` depuis `date-fns`.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep "loot.tsx" | head -10</automated>
  </verify>
  <done>
    - Onglet "Mes récompenses" visible dans la tab bar de loot.tsx
    - Sous-onglets "À utiliser" et "Historique" fonctionnels
    - Bouton "✓ Utilisé" visible pour les adultes, désactivé pour enfants/ados
    - Appel à markLootUsed persiste dans gamification.md via writeFile
    - Aucune erreur TypeScript dans loot.tsx
    - `npx tsc --noEmit` sans nouvelles erreurs
  </done>
</task>

</tasks>

<verification>
```bash
npx tsc --noEmit 2>&1 | grep -v "MemoryEditor\|cooklang\.ts" | grep "error TS" | head -20
```
Aucune nouvelle erreur TypeScript. Les erreurs pré-existantes dans MemoryEditor.tsx, cooklang.ts et useVault.ts sont ignorées.

Vérification manuelle : ouvrir l'onglet Récompenses → onglet "Mes récompenses" visible → sous-onglets "À utiliser" / "Historique" → les loots physiques de l'historique apparaissent → en profil adulte le bouton "✓ Utilisé" est actif → en profil enfant/ado il est grisé.
</verification>

<success_criteria>
- `GamificationData.usedLoots` existe et est parsé/sérialisé dans gamification.md
- `markLootUsed` est dans VaultState et écrit dans le vault
- Onglet "Mes récompenses" affiché dans loot.tsx avec badge de count
- Profil adulte : bouton actif, haptic feedback au coche
- Profil enfant/ado : bouton désactivé visuellement
- Les loots marqués utilisés passent de "À utiliser" à "Historique" instantanément
- `npx tsc --noEmit` sans nouvelles erreurs
</success_criteria>

<output>
Après complétion, créer `.planning/quick/260328-py4-ecran-mes-recompenses-loots-reels-avec-o/260328-py4-SUMMARY.md`
</output>
