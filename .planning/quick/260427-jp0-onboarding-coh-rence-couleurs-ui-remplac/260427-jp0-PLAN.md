---
phase: quick-260427-jp0
title: Onboarding — cohérence couleurs/UI
created: 2026-04-27
must_haves:
  - colors.success / colors.error pour badges Tinder Yes/No
  - colors.border / colors.textMuted pour progress bar
  - Shadows.* pour les 6 shadowColor:'#000'
  - FontSize token pour fontSize:48 (tinderQuote)
  - tsc --noEmit passe
---

# Quick Task 260427-jp0 — Onboarding cohérence couleurs/UI

## Contexte
`app/onboarding.tsx` contient des couleurs et tokens hardcoded qui cassent le dark mode et violent les conventions CLAUDE.md (palette atténuée + tokens).

## Task 1 — Remplacer hardcoded par tokens

**Fichier:** `app/onboarding.tsx`

### Imports à ajouter
- `Shadows` depuis `../constants/shadows`

### Remplacements

| Ligne | Avant | Après |
|------|------|------|
| 1202 | `backgroundColor: '#E5E7EB'` | dynamique via `colors.border` (style inline sur l'élément) |
| 1211 | `color: '#9CA3AF'` | dynamique via `colors.textMuted` (style inline) |
| 1237-1241 | `shadowColor: '#000'` + offset/opacity/radius/elevation | `...Shadows.md` |
| 1373-1377 | shadow inline | `...Shadows.sm` |
| 1438-1442 | shadow inline | `...Shadows.lg` |
| 1458 | `borderColor: '#22C55E'` | dynamique `colors.success` |
| 1459 | `backgroundColor: '#22C55E22'` | dynamique `colors.success + '22'` |
| 1463 | `borderColor: '#EF4444'` | dynamique `colors.error` |
| 1464 | `backgroundColor: '#EF444422'` | dynamique `colors.error + '22'` |
| 1471 | `fontSize: 48` | `FontSize.hero` (32) ou garder taille via constante locale |
| 1473 | `color: '#D1D5DB'` | dynamique `colors.borderLight` ou `colors.textFaint` |

### Approche
- Les valeurs liées au thème (couleurs) → déplacer vers style inline avec `colors.*` au point d'usage (ex: `<View style={[styles.tinderBadgeYes, { borderColor: colors.success, backgroundColor: colors.success + '22' }]}/>`)
- Pour `progressTrack` / `progressLabel` : inline `backgroundColor: colors.border` sur le `<View>`, `color: colors.textMuted` sur le `<Text>`
- Pour les shadows: spread `Shadows.*` dans le StyleSheet (statique, OK)
- Pour `fontSize: 48` → remplacer par `FontSize.hero` (32px) — la lineHeight passera à `LineHeight.title` ou laisser 48 si visuellement nécessaire (gros guillemet décoratif)

### Vérification
- `npx tsc --noEmit` passe
- Inspection visuelle du flow onboarding (light + dark mode si togglable)

## Done
- [ ] Imports Shadows ajouté
- [ ] 11 remplacements appliqués
- [ ] tsc passe
- [ ] Commit feat(quick-260427-jp0)
