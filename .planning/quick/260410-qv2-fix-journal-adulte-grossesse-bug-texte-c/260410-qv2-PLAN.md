---
phase: quick-260410-qv2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/(tabs)/journal.tsx
  - lib/parser.ts
autonomous: true
requirements: [QV2-01-bugfix-texte-coupe, QV2-02-template-grossesse, QV2-03-render-adult-sections]

must_haves:
  truths:
    - "Le texte long dans Humeur/Observations ne se coupe plus (multiline visible)"
    - "Le journal adulte grossesse genere les sections Sommeil, Symptomes, Humeur"
    - "Les sections adultes Sommeil, Symptomes, Humeur s'affichent correctement dans le rendu"
    - "Le journal adulte NON-grossesse garde le template existant inchange"
  artifacts:
    - path: "app/(tabs)/journal.tsx"
      provides: "Bug fix obsRow + rendu sections adultes"
    - path: "lib/parser.ts"
      provides: "Template grossesse avec sections structurees"
  key_links:
    - from: "lib/parser.ts"
      to: "app/(tabs)/journal.tsx"
      via: "generateAdultJournalTemplate genere le markdown, renderSections+typeFromHeading le parse"
      pattern: "typeFromHeading.*sommeil|symptom"
---

<objective>
Corriger le bug de texte coupe dans les observations du journal + ajouter des sections structurees (Sommeil, Symptomes, Humeur) pour le journal adulte grossesse.

Purpose: Le journal adulte grossesse doit offrir un suivi structure adapte (sommeil, symptomes) et le texte des observations ne doit plus etre tronque visuellement.
Output: Bug fix + template grossesse + rendu des nouvelles sections
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@app/(tabs)/journal.tsx
@lib/parser.ts
@lib/types.ts (Profile.statut: 'grossesse' | 'ne')

<interfaces>
From app/(tabs)/journal.tsx:
```typescript
type EntryType = 'Biberon' | 'Couche' | 'Sieste' | 'Observation' | 'Medicament';

// Line 176-184 — typeFromHeading maps heading text to EntryType
function typeFromHeading(heading: string): EntryType | null;

// Line 574 — renderSections parses content and renders sections
// For entryType 'Observation': renders numbered list items (1. text)
// For other types: renders markdown tables
// Returns null if typeFromHeading returns null (section invisible)

// Line 1021-1023 — styles.obsRow
obsRow: { flexDirection: 'row', alignItems: 'center', paddingHorizontal: 14, paddingVertical: 10, borderBottomWidth: 1, gap: 8 }

// Line 617 — MarkdownText dans obsRow
<MarkdownText style={{ color: colors.textSub }}>{obs.text}</MarkdownText>
```

From lib/parser.ts:
```typescript
export function generateAdultJournalTemplate(prenom: string): string;
// Currently generates: Notes du jour, Humeur, Objectifs, Gratitude
```

From lib/types.ts:
```typescript
interface Profile {
  statut?: 'grossesse' | 'ne'; // pregnancy mode vs born
  // ...
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tache 1: Fix bug texte coupe + ajouter rendu sections adultes</name>
  <files>app/(tabs)/journal.tsx</files>
  <action>
1. **Bug fix texte coupe (obsRow):**
   - Ligne ~1021: Dans le style `obsRow`, changer `alignItems: 'center'` en `alignItems: 'flex-start'` pour que le texte multiline s'aligne en haut
   - Ligne ~617: Ajouter `flex: 1` au style du `MarkdownText` dans les observations pour qu'il occupe l'espace disponible:
     `<MarkdownText style={{ color: colors.textSub, flex: 1 }}>{obs.text}</MarkdownText>`

2. **Etendre typeFromHeading pour les sections adultes:**
   - Ligne ~176: Ajouter `EntryType` pour les sections adultes. Elargir le type:
     `type EntryType = 'Biberon' | 'Couche' | 'Sieste' | 'Observation' | 'Medicament' | 'SommeilAdulte' | 'SymptomeAdulte';`
   - Dans `typeFromHeading`, avant le `return null` final, ajouter:
     - `if (h.includes('symptom') || h.includes('symptôme')) return 'SymptomeAdulte';`
     - NB: 'sommeil' existe deja et retourne 'Sieste' — pour le cas adulte, c'est un format different. Ajouter un check specifique: si le heading contient 'sommeil' ET que le contenu est un format blockquote/liste (pas table), traiter comme adulte. MAIS pour simplifier: laisser 'sommeil' mapper vers 'Sieste' pour bebe, et dans renderSections ajouter un fallback pour le format non-table (liste a puces / blockquote). Alternative plus propre: ajouter `'SommeilAdulte'` comme type et dans typeFromHeading, distinguer via le heading exact. Le heading grossesse sera "Sommeil" (sans emoji bebe). Utiliser: `if (h === 'sommeil' || h === 'sommeil') return 'SommeilAdulte';` — NON, le heading inclut emoji. Le heading sera `"😴 Sommeil"` donc `h.includes('sommeil')` matchera. Pour distinguer: le template bebe utilise le heading "Sommeil" avec table, le template adulte utilise "😴 Sommeil" avec blockquote. Approche: dans typeFromHeading, si heading a un emoji sommeil ET inclut 'sommeil', retourner 'SommeilAdulte'. Sinon le mapping existant vers 'Sieste' reste. Concretement:
     - Reordonner les checks: mettre le check `'SommeilAdulte'` AVANT le check 'Sieste' existant:
       `if (h.includes('sommeil') && (h.includes('😴') || !h.includes('alimentation'))) ...` — trop fragile.
     - Approche SIMPLE et robuste: le template grossesse utilise des headings uniques. Template: `## 😴 Suivi Sommeil`, `## 🤒 Symptomes`, `## 😊 Humeur & Observations`. Mapping:
       - `if (h.includes('suivi sommeil')) return 'SommeilAdulte';`
       - `if (h.includes('symptôme') || h.includes('symptome')) return 'SymptomeAdulte';`
       - Le heading `Humeur` existant mappe deja vers 'Observation' — ok.

3. **Ajouter le rendu pour SommeilAdulte dans renderSections:**
   Apres le bloc `if (entryType === 'Observation')` (ligne ~592-638), ajouter:

   ```
   if (entryType === 'SommeilAdulte') {
     // Parse le format blockquote: "> Coucher: ...", "> Lever: ...", "> Qualite: ..."
     const fields: { label: string; value: string }[] = [];
     const notes: string[] = [];
     for (const line of sectionLines) {
       const bqMatch = line.match(/^>\s*\*\*(.+?)\*\*\s*:\s*(.*)/);
       if (bqMatch) {
         fields.push({ label: bqMatch[1].trim(), value: bqMatch[2].trim() });
       } else if (line.trim() && !line.startsWith('>') && !line.startsWith('#')) {
         notes.push(line.trim());
       }
     }
     return (
       <View key={si} style={[styles.tableSection, { backgroundColor: colors.card }]}>
         <View style={[styles.sectionHeader, { backgroundColor: colors.cardAlt, borderBottomColor: colors.border }]}>
           <Text style={[styles.tableSectionTitle, { color: colors.text }]}>{sec.heading}</Text>
         </View>
         {fields.length > 0 ? fields.map((f, fi) => (
           <View key={fi} style={[styles.obsRow, { borderBottomColor: colors.borderLight, alignItems: 'flex-start' }, fi % 2 === 1 && { backgroundColor: colors.cardAlt }]}>
             <Text style={[styles.obsNumber, { color: primary, width: 'auto' as any }]}>{f.label}:</Text>
             <Text style={{ color: colors.textSub, flex: 1 }}>{f.value || '—'}</Text>
           </View>
         )) : (
           <View style={styles.emptySection}>
             <Text style={[styles.emptySectionText, { color: colors.textFaint }]}>Aucune donnee sommeil</Text>
           </View>
         )}
       </View>
     );
   }
   ```

4. **Ajouter le rendu pour SymptomeAdulte dans renderSections:**
   Meme pattern que Observation (liste numerotee). Reutiliser le code Observation existant — la seule difference est le heading et le type de modal. Concretement: dans le bloc `if (entryType === 'Observation')`, etendre la condition:
   `if (entryType === 'Observation' || entryType === 'SymptomeAdulte')` — les deux utilisent le format liste numerotee.
   Ajuster le `openEditModal` et `openAddModal` pour passer le bon type.

5. **Ajouter ENTRY_META pour les nouveaux types** (si necessaire pour emoji/label dans les boutons ajouter). Trouver la constante ENTRY_META dans le fichier et ajouter:
   - `SommeilAdulte: { emoji: '😴', label: 'Sommeil' }`
   - `SymptomeAdulte: { emoji: '🤒', label: 'Symptome' }`
  </action>
  <verify>
    <automated>cd /Users/gabrielwaltio/Documents/family-vault && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>Le texte des observations ne se coupe plus (flex:1 + alignItems flex-start). Les sections SommeilAdulte et SymptomeAdulte s'affichent dans le journal adulte grossesse.</done>
</task>

<task type="auto">
  <name>Tache 2: Template journal adulte grossesse avec sections structurees</name>
  <files>lib/parser.ts, app/(tabs)/journal.tsx</files>
  <action>
1. **Modifier generateAdultJournalTemplate dans lib/parser.ts** (ligne ~2189):
   - Ajouter un parametre optionnel `options?: { grossesse?: boolean }` a la signature
   - Si `options?.grossesse` est true, generer un template avec les sections:
     ```
     ## 😴 Suivi Sommeil

     > **Coucher**: 
     > **Lever**: 
     > **Qualite** (1-5): 
     > **Notes**: 

     ## 🤒 Symptomes

     1. 

     ## 😊 Humeur & Observations

     > Comment te sens-tu aujourd'hui ?

     ## 🎯 Objectifs
     - [ ]

     ## 🙏 Gratitude

     > [!tip] 3 choses positives aujourd'hui
     ```
   - Si `options?.grossesse` est false/absent, garder le template actuel tel quel (Notes du jour, Humeur, Objectifs, Gratitude)
   - Ajouter le tag `journal-grossesse` au frontmatter quand grossesse est true (en plus de `journal-adulte`)

2. **Passer le flag grossesse dans journal.tsx** (ligne ~440-441):
   - Au lieu de `generateAdultJournalTemplate(activeProfile.name)`, appeler:
     `generateAdultJournalTemplate(activeProfile.name, { grossesse: activeProfile.statut === 'grossesse' })`
   - Importer si necessaire (deja importe)
  </action>
  <verify>
    <automated>cd /Users/gabrielwaltio/Documents/family-vault && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>Le template grossesse genere les sections Sommeil/Symptomes/Humeur structurees. Le template adulte normal reste inchange. Le flag grossesse est passe depuis le profil actif.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passe sans nouvelles erreurs
- Journal adulte grossesse: le template contient les sections Sommeil, Symptomes, Humeur
- Journal adulte normal: le template reste identique (Notes du jour, Humeur, Objectifs, Gratitude)
- Texte long dans Observations ne se coupe plus
- Les nouvelles sections s'affichent dans le rendu (pas null)
</verification>

<success_criteria>
- Bug texte coupe corrige: flex:1 sur MarkdownText + alignItems flex-start sur obsRow
- Template grossesse: 5 sections (Suivi Sommeil, Symptomes, Humeur & Observations, Objectifs, Gratitude)
- Template normal: inchange
- Rendu: typeFromHeading reconnait les headings grossesse, renderSections les affiche
- tsc --noEmit passe
</success_criteria>

<output>
After completion, create `.planning/quick/260410-qv2-fix-journal-adulte-grossesse-bug-texte-c/260410-qv2-SUMMARY.md`
</output>
