---
phase: quick-260428-e5h
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/course-history.ts
  - lib/parse-voice-courses.ts
  - app/(tabs)/meals.tsx
  - locales/fr/common.json
  - locales/en/common.json
autonomous: true
requirements:
  - QT-FREQ-01
  - QT-VOICE-01
must_haves:
  truths:
    - "L'utilisateur voit un bandeau horizontal de chips d'articles fréquents au-dessus de la barre d'ajout des courses"
    - "Tap sur un chip ajoute immédiatement l'article à la liste (avec catégorie auto)"
    - "Chaque ajout manuel est enregistré dans l'historique de fréquence persisté (expo-secure-store)"
    - "Un bouton micro 🎙️ dans la barre d'ajout ouvre une modale Dictaphone"
    - "Le transcript vocal est parsé en items individuels et tous ajoutés à la liste avec catégorie auto"
  artifacts:
    - path: "lib/course-history.ts"
      provides: "API trackCourseAdd() + getFrequentCourses() persistant via expo-secure-store"
    - path: "lib/parse-voice-courses.ts"
      provides: "parseVoiceCourses(transcript) -> { text, name, quantity, section }[]"
    - path: "app/(tabs)/meals.tsx"
      provides: "Bandeau chips fréquents + bouton micro + modale Dictaphone branchés sur l'onglet courses"
  key_links:
    - from: "app/(tabs)/meals.tsx (handleAddCourse)"
      to: "lib/course-history.ts trackCourseAdd"
      via: "appel après addCourseItem"
    - from: "app/(tabs)/meals.tsx (modale vocale onResult)"
      to: "lib/parse-voice-courses.ts parseVoiceCourses + mergeCourseIngredients"
      via: "transcript -> items -> merge"
---

<objective>
Ajouter deux features à l'onglet courses de meals.tsx :
1. **Articles fréquents** — chips tap-to-add au-dessus de la barre d'ajout, alimentés par un historique d'usage persisté
2. **Entrée vocale** — bouton micro qui ouvre DictaphoneRecorder, parse le transcript en items et les ajoute en batch

Purpose: Réduire le frottement d'ajout d'articles courants (1 tap au lieu de saisie) et permettre l'ajout multi-items mains-libres.
Output: 2 nouveaux modules lib/, modifications meals.tsx (imports + state + handlers + UI), clés i18n FR/EN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@app/(tabs)/meals.tsx
@lib/cooklang.ts
@components/DictaphoneRecorder.tsx
@hooks/useVaultCourses.ts
@locales/fr/common.json

<interfaces>
<!-- Contrats existants à réutiliser tels quels -->

From components/DictaphoneRecorder.tsx:
```typescript
interface DictaphoneRecorderProps {
  rdv?: RDV;
  context?: { title: string; subtitle?: string };
  onResult: (text: string) => void;
  onClose: () => void;
}
```
Note: déjà utilisé en modal pageSheet ~ligne 2334 de meals.tsx.

From lib/cooklang.ts (ligne 489):
```typescript
export function categorizeIngredient(name: string): string;  // retourne ex: "🥬 Frais"
export const COURSE_CATEGORIES: string[];
```

From hooks/useVaultCourses.ts:
```typescript
addCourseItem: (text: string, section?: string) => Promise<void>;
mergeCourseIngredients: (items: { text: string; name: string; quantity: number | null; section: string }[]) => Promise<{ added: number; merged: number }>;
```
Note: `mergeCourseIngredients` est l'API idéale pour l'ajout batch vocal — gère doublons et fusion de quantités.

From app/(tabs)/meals.tsx (~ligne 1339):
- `handleAddCourse` est le handler du bouton + existant
- `selectedSection` est la catégorie picker (peut être null = auto)
- Structure barre actuelle : `<View styles.addBar>[picker][TextInput][+ button]</View>`
- Modale Dictaphone exemple ~ligne 2334 (recettes) — pattern Modal pageSheet à reproduire

From expo-secure-store (déjà utilisé ailleurs) :
```typescript
import * as SecureStore from 'expo-secure-store';
SecureStore.getItemAsync(key) / SecureStore.setItemAsync(key, value)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1 : Créer lib/course-history.ts (tracking fréquence) et lib/parse-voice-courses.ts (parser vocal)</name>
  <files>lib/course-history.ts, lib/parse-voice-courses.ts</files>
  <behavior>
    course-history.ts :
    - `trackCourseAdd(name: string): Promise<void>` — incrémente le compteur de l'article (clé normalisée lowercase + trim + NFD/diacritiques retirés), met à jour `lastUsedAt`, persiste via SecureStore sous clé `course_history_v1`
    - `getFrequentCourses(limit = 8): Promise<{ name: string; count: number }[]>` — retourne top N triés par count desc puis lastUsedAt desc, en filtrant les articles non utilisés depuis 30 jours
    - `clearCourseHistory(): Promise<void>` — utilitaire reset (debug)
    - Interne : Map<normalizedKey, { displayName, count, lastUsedAt }> sérialisée JSON
    - Cap : garder max 100 entrées (drop oldest lastUsedAt si dépassement) pour éviter ballonnement SecureStore

    parse-voice-courses.ts :
    - `parseVoiceCourses(transcript: string): { text: string; name: string; quantity: number | null; section: string }[]`
    - Découpe sur séparateurs FR : ` et `, `, `, ` puis `, ` ensuite `, ` plus `, ` aussi `, retours ligne, points
    - Pour chaque segment non-vide :
      - Extrait quantité optionnelle en tête : `(\d+(?:[.,]\d+)?)\s+` (ex "3 tomates", "2 paquets de pâtes")
      - Si pas de quantité numérique mais "du/de la/des/un/une" en tête → quantity=null, retire le déterminant pour `name`
      - `name` = reste nettoyé (trim, lowercase doux, mais préserve l'affichage original dans `text`)
      - `text` = forme reconstruite "{qty} {name}" ou juste "{name}" sans déterminant
      - `section` = `categorizeIngredient(name)` importé depuis ./cooklang
    - Filtre les segments < 2 caractères, dédoublonne par name normalisé (NFD lowercase)
    - Retourne tableau vide si transcript vide/whitespace
  </behavior>
  <action>
    Créer `lib/course-history.ts` :
    - Import `* as SecureStore from 'expo-secure-store'`
    - Constante `STORAGE_KEY = 'course_history_v1'`, `MAX_ENTRIES = 100`, `STALE_DAYS = 30`
    - Fonction `normalizeKey(name)` : lowercase + trim + `.normalize('NFD').replace(/[̀-ͯ]/g, '')`
    - Implémenter trackCourseAdd, getFrequentCourses, clearCourseHistory comme décrit dans <behavior>
    - Erreurs SecureStore silencieuses (catch + return [] ou void) sous `if (__DEV__) console.warn`
    - Pas de dépendance externe au-delà de expo-secure-store

    Créer `lib/parse-voice-courses.ts` :
    - Import `categorizeIngredient` depuis `./cooklang`
    - Implémenter parseVoiceCourses comme décrit
    - Liste de séparateurs : split sur `/\s+et\s+|,|;|\.\s+|\n+/i`
    - Liste déterminants à strip : `/^(du|de la|de l'|des|un|une|le|la|les)\s+/i` puis aussi extraction quantité : `/^(\d+(?:[.,]\d+)?)\s+(?:paquets?|boîtes?|boites?|sachets?|bouteilles?|kg|g|l|ml|cl)?\s*(?:de\s+|d')?(.+)/i` puis fallback `/^(\d+(?:[.,]\d+)?)\s+(.+)/`
    - Reconstruire `text` propre : si quantity → `"${qty} ${name}"`, sinon `name` (capitalisé première lettre)
    - Pas de tests Jest formels (cohérent avec scope quick-task), mais code défensif sur transcripts vides/anormaux

    Aucun changement à categorizeIngredient ni à useVaultCourses.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>
    - lib/course-history.ts compile sans erreur TS et exporte trackCourseAdd, getFrequentCourses, clearCourseHistory
    - lib/parse-voice-courses.ts compile sans erreur TS et exporte parseVoiceCourses
    - `npx tsc --noEmit` ne montre aucune nouvelle erreur (les pré-existantes sur MemoryEditor.tsx, cooklang.ts, useVault.ts restent ignorées)
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2 : Câbler chips fréquents + bouton micro + modale vocale dans meals.tsx + clés i18n</name>
  <files>app/(tabs)/meals.tsx, locales/fr/common.json, locales/en/common.json</files>
  <action>
    **i18n** — Ajouter sous `meals.shopping` dans locales/fr/common.json (et équivalents EN dans en/common.json) :
    - `frequentLabel` FR: "Articles fréquents" / EN: "Frequent items"
    - `frequentHintA11y` FR: "Tap pour ajouter rapidement" / EN: "Tap to add quickly"
    - `frequentChipA11y` FR: "Ajouter {{name}} à la liste" / EN: "Add {{name}} to list"
    - `voiceAddBtnA11y` FR: "Ajouter par la voix" / EN: "Add by voice"
    - `voiceAddTitle` FR: "Dicter des articles" / EN: "Dictate items"
    - `voiceAddSubtitle` FR: "Ex : 3 tomates, du lait et 2 paquets de pâtes" / EN: "Ex: 3 tomatoes, milk and 2 packs of pasta"
    - `voiceAddedToast` FR: "{{count}} article(s) ajouté(s)" / EN: "{{count}} item(s) added"
    - `voiceNothingDetected` FR: "Aucun article détecté dans la dictée" / EN: "No item detected in dictation"

    **meals.tsx** :

    1. **Imports** — Ajouter en haut du fichier :
       ```typescript
       import { trackCourseAdd, getFrequentCourses } from '../../lib/course-history';
       import { parseVoiceCourses } from '../../lib/parse-voice-courses';
       ```
       (DictaphoneRecorder déjà importé ligne 62, categorizeIngredient déjà importé ligne 43.)

    2. **State** dans le composant principal (près des autres states courses) :
       ```typescript
       const [frequentItems, setFrequentItems] = useState<{ name: string; count: number }[]>([]);
       const [showVoiceModal, setShowVoiceModal] = useState(false);
       ```

    3. **useEffect** chargement initial + reload quand on bascule sur l'onglet courses :
       ```typescript
       useEffect(() => {
         if (tab === 'courses') {
           getFrequentCourses(8).then(setFrequentItems).catch(() => {});
         }
       }, [tab]);
       ```

    4. **Modifier `handleAddCourse`** (chercher la fonction existante qui appelle `addCourseItem`) :
       - Après l'appel réussi à `addCourseItem(text, section)`, ajouter :
         ```typescript
         trackCourseAdd(text).then(() => getFrequentCourses(8).then(setFrequentItems)).catch(() => {});
         ```

    5. **Handler chip fréquent** :
       ```typescript
       const handleAddFrequent = useCallback(async (name: string) => {
         const section = categorizeIngredient(name);
         await addCourseItem(name, section);
         Haptics.selectionAsync().catch(() => {});
         trackCourseAdd(name).then(() => getFrequentCourses(8).then(setFrequentItems)).catch(() => {});
       }, [addCourseItem]);
       ```

    6. **Handler résultat vocal** :
       ```typescript
       const handleVoiceResult = useCallback(async (transcript: string) => {
         const items = parseVoiceCourses(transcript);
         if (items.length === 0) {
           showToast(t('meals.shopping.voiceNothingDetected'), 'info');
           setShowVoiceModal(false);
           return;
         }
         const result = await mergeCourseIngredients(items);
         items.forEach(i => trackCourseAdd(i.name).catch(() => {}));
         getFrequentCourses(8).then(setFrequentItems).catch(() => {});
         showToast(t('meals.shopping.voiceAddedToast', { count: result.added + result.merged }), 'success');
         setShowVoiceModal(false);
       }, [mergeCourseIngredients, showToast, t]);
       ```
       (Vérifier que `mergeCourseIngredients` et `showToast` sont accessibles ; sinon les destructurer depuis useVault / useToast comme déjà fait ailleurs dans le fichier.)

    7. **UI bandeau chips** — Insérer JUSTE AVANT `<KeyboardAvoidingView>` (~ligne 1335), seulement si `frequentItems.length > 0` :
       ```tsx
       {frequentItems.length > 0 && (
         <View style={[styles.frequentBar, { backgroundColor: colors.card, borderTopColor: colors.borderLight }]}>
           <ScrollView
             horizontal
             showsHorizontalScrollIndicator={false}
             contentContainerStyle={styles.frequentScrollContent}
           >
             {frequentItems.map(item => (
               <TouchableOpacity
                 key={item.name}
                 style={[styles.frequentChip, { backgroundColor: colors.cardAlt, borderColor: colors.borderLight }]}
                 onPress={() => handleAddFrequent(item.name)}
                 activeOpacity={0.7}
                 accessibilityLabel={t('meals.shopping.frequentChipA11y', { name: item.name })}
                 accessibilityRole="button"
               >
                 <Text style={[styles.frequentChipText, { color: colors.text }]} numberOfLines={1}>
                   {item.name}
                 </Text>
               </TouchableOpacity>
             ))}
           </ScrollView>
         </View>
       )}
       ```

    8. **Bouton micro dans addBar** — Ajouter APRÈS le bouton `+` existant (~ligne 1379) :
       ```tsx
       <TouchableOpacity
         style={[styles.micBtn, { backgroundColor: colors.cardAlt, borderColor: colors.borderLight }]}
         onPress={() => setShowVoiceModal(true)}
         activeOpacity={0.7}
         accessibilityLabel={t('meals.shopping.voiceAddBtnA11y')}
         accessibilityRole="button"
       >
         <Text style={styles.micBtnIcon}>🎙️</Text>
       </TouchableOpacity>
       ```

    9. **Modale vocale** — Ajouter à la fin du composant (après les autres `<Modal>` existants, pattern identique au Modal Dictaphone recettes ~ligne 2334) :
       ```tsx
       <Modal
         visible={showVoiceModal}
         animationType="slide"
         presentationStyle="pageSheet"
         onRequestClose={() => setShowVoiceModal(false)}
       >
         <DictaphoneRecorder
           context={{
             title: t('meals.shopping.voiceAddTitle'),
             subtitle: t('meals.shopping.voiceAddSubtitle'),
           }}
           onResult={handleVoiceResult}
           onClose={() => setShowVoiceModal(false)}
         />
       </Modal>
       ```

    10. **Styles** — Ajouter au StyleSheet en bas du fichier (utilise tokens design uniquement, pas de hardcoded) :
        ```typescript
        frequentBar: {
          borderTopWidth: StyleSheet.hairlineWidth,
          paddingVertical: Spacing.md,
        },
        frequentScrollContent: {
          paddingHorizontal: Spacing['2xl'],
          gap: Spacing.md,
        },
        frequentChip: {
          paddingVertical: Spacing.md,
          paddingHorizontal: Spacing.xl,
          borderRadius: Radius.full,
          borderWidth: 1,
          maxWidth: 180,
        },
        frequentChipText: {
          fontSize: FontSize.label,
          fontWeight: FontWeight.semibold,
        },
        micBtn: {
          width: 44,
          height: 44,
          borderRadius: Radius.full,
          borderWidth: 1,
          alignItems: 'center',
          justifyContent: 'center',
          marginLeft: Spacing.md,
        },
        micBtnIcon: {
          fontSize: FontSize.lg,
        },
        ```

    **Contraintes strictes** :
    - Aucune couleur hardcoded — toujours `useThemeColors()` / `colors.*` / `primary` / `tint`
    - Langue UI française pour clés FR
    - Ne pas modifier les features existantes (recettes, repas, autres modales) — ajout pur
    - `mergeCourseIngredients` doit être accessible dans le scope ; si pas déjà destructuré dans le composant, l'ajouter à la déstructuration `const { ..., mergeCourseIngredients } = useVault();`
    - Pattern Modal et imports `Modal` depuis 'react-native' déjà présents dans le fichier (vérifier, sinon ajouter à l'import RN existant)
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>
    - L'onglet courses affiche un bandeau horizontal de chips au-dessus de la barre d'ajout (visible uniquement quand l'historique a au moins 1 entrée)
    - Tap sur un chip ajoute l'item à la liste avec catégorie auto via categorizeIngredient
    - Chaque ajout manuel (handleAddCourse) appelle trackCourseAdd et rafraîchit les chips
    - Un bouton 🎙️ apparaît dans la barre d'ajout, à droite du `+`
    - Tap sur le bouton micro ouvre une Modal pageSheet avec DictaphoneRecorder
    - Quand DictaphoneRecorder appelle onResult, le transcript est parsé via parseVoiceCourses et les items sont ajoutés en batch via mergeCourseIngredients
    - Toast affiche le nombre d'items ajoutés (ou "Aucun article détecté" si parser retourne vide)
    - `npx tsc --noEmit` ne montre aucune nouvelle erreur
    - Clés i18n présentes dans FR et EN sous `meals.shopping.frequent*` et `meals.shopping.voiceAdd*`
  </done>
</task>

</tasks>

<verification>
**Type check** : `npx tsc --noEmit` (les erreurs pré-existantes dans MemoryEditor.tsx, cooklang.ts, useVault.ts restent — ne pas y toucher)

**Smoke manuel suggéré (optionnel)** :
- Ouvrir l'onglet courses, ajouter manuellement "tomate", "lait", "pâtes" → après refresh, les chips apparaissent
- Tap sur un chip → l'article s'ajoute en haut de la liste avec sa catégorie
- Tap sur 🎙️ → modale Dictaphone s'ouvre, dicter "3 tomates et du lait et 2 paquets de pâtes", valider → 3 items ajoutés à la liste

**Vérifications de non-régression** :
- L'ajout manuel via TextInput + bouton `+` fonctionne toujours
- La modale Dictaphone des recettes (ligne ~2334) n'est pas affectée
- Aucune couleur hardcoded introduite (grep `#[0-9A-Fa-f]{6}` sur les diffs)
</verification>

<success_criteria>
1. `npx tsc --noEmit` passe sans nouvelle erreur
2. Les 2 nouveaux fichiers `lib/course-history.ts` et `lib/parse-voice-courses.ts` existent et compilent
3. `app/(tabs)/meals.tsx` contient : imports des 2 nouveaux modules, useEffect de chargement, handleAddFrequent, handleVoiceResult, JSX bandeau chips, bouton micro, Modal vocal
4. Les 8 clés i18n existent dans `locales/fr/common.json` ET `locales/en/common.json`
5. Toutes les couleurs passent par `useThemeColors()` (zéro hardcoded sur les ajouts)
</success_criteria>

<output>
Après complétion, créer `.planning/quick/260428-e5h-articles-fr-quents-et-entr-e-vocale-pour/260428-e5h-SUMMARY.md` résumant :
- Fichiers créés (course-history.ts, parse-voice-courses.ts)
- Fichiers modifiés (meals.tsx, locales FR/EN)
- Décisions clés (normalisation NFD, cap 100 entrées, séparateurs FR retenus, pattern Modal pageSheet)
- Limitations connues (parsing vocal heuristique non exhaustif, pas de tests Jest)
</output>
