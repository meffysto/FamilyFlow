---
phase: quick-260425-0qf
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/mascot/companion-sprites.ts
  - lib/mascotte-live-activity.ts
  - hooks/useVault.ts
  - modules/vault-access/src/index.ts
  - modules/vault-access/ios/VaultAccessModule.swift
  - modules/live-activity-shared/ios/Sources/MascotteActivityAttributes.swift
  - ios/MaJourneeWidget/MascotteLiveActivity.swift
autonomous: false
requirements:
  - LA-POSE-01
  - LA-POSE-02
  - LA-POSE-03
must_haves:
  truths:
    - "Le sprite affiché en Live Activity (Lock Screen + DI compact) varie selon la pose courante (idle/happy/sleeping/eating/celebrating)"
    - "Les 5 PNG (idle_1, happy, sleeping, eating, celebrating) pour l'espèce/stade actif sont écrits dans l'App Group au start() de la LA"
    - "Le payload ContentState ne contient plus de base64 sprite — uniquement un champ `pose: String` (default 'idle')"
    - "Cocher une tâche pendant la LA active flash la pose 'happy' 2s puis revient à idle (ou pose narrative en cours)"
    - "Le stage narratif pilote la pose persistante : dodo→sleeping, midi→eating, recap 100%→celebrating, autres→idle"
    - "Les anciens consommateurs (companionSpriteBase64 / companionSpriteToken) sont retirés proprement sans casser le décodage Codable d'une LA en cours"
  artifacts:
    - path: "lib/mascot/companion-sprites.ts"
      provides: "CompanionMood union étendu + 3 nouvelles poses dans COMPANION_SPRITES (sleeping/eating/celebrating)"
    - path: "lib/mascotte-live-activity.ts"
      provides: "writeCompanionPosesToAppGroup() + champ pose dans MascotteSnapshot, retrait companionSpriteBase64"
    - path: "modules/vault-access/src/index.ts"
      provides: "Bridge writeCompanionPoseFile(pose, base64) + signatures start/update Mascotte sans companionSpriteBase64, avec pose"
    - path: "modules/vault-access/ios/VaultAccessModule.swift"
      provides: "Function writeCompanionPoseFile écrivant `companion-sprite-{pose}.png` dans App Group + propagation pose dans ContentState"
    - path: "modules/live-activity-shared/ios/Sources/MascotteActivityAttributes.swift"
      provides: "ContentState.pose: String (default 'idle'), companionSpriteToken retiré"
    - path: "ios/MaJourneeWidget/MascotteLiveActivity.swift"
      provides: "CompanionSpriteCache keyed par pose, lit `companion-sprite-{pose}.png`"
    - path: "hooks/useVault.ts"
      provides: "Triggers feedCompanion → patchMascotte({pose:'happy'}) avec revert 2s ; pose narrative dérivée du stage"
  key_links:
    - from: "lib/mascotte-live-activity.ts:startMascotte"
      to: "modules/vault-access (writeCompanionPoseFile × 5)"
      via: "Promise.all écriture des 5 PNG App Group avant startMascotteActivity"
      pattern: "writeCompanionPoseFile.*idle|happy|sleeping|eating|celebrating"
    - from: "ios/MaJourneeWidget/MascotteLiveActivity.swift (CompanionSpriteCache)"
      to: "App Group `companion-sprite-{pose}.png`"
      via: "FileManager containerURL + appendingPathComponent dynamique"
      pattern: "companion-sprite-\\\\(pose\\\\)"
    - from: "hooks/useVault.ts:feedCompanion"
      to: "lib/mascotte-live-activity.ts:patchMascotte"
      via: "patchMascotte({pose:'happy'}) puis setTimeout 2s patchMascotte({pose:'idle'})"
      pattern: "patchMascotte\\\\(\\\\{\\\\s*pose"
---

<objective>
Étendre la Live Activity mascotte pour afficher 5 poses sprite distinctes (idle / happy / sleeping / eating / celebrating) au lieu du couple idle/happy actuel, et passer d'un payload base64 lourd (~5-15KB) à un simple champ `pose: String` (<500B) en écrivant les 5 PNG dans l'App Group au démarrage de la LA.

Purpose: Réduire drastiquement la taille du ContentState ActivityKit (limite ~4KB), donner du caractère narratif à la LA (le compagnon dort la nuit, mange à midi, célèbre la journée bouclée), et préparer le terrain pour les futurs AppIntents (FeedCompanionIntent v2b).
Output: 5 poses fonctionnelles côté Lock Screen et DI compact, triggers TS câblés, ContentState allégé.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@lib/mascot/companion-sprites.ts
@lib/mascotte-live-activity.ts
@modules/vault-access/src/index.ts
@modules/live-activity-shared/ios/Sources/MascotteActivityAttributes.swift
@ios/MaJourneeWidget/MascotteLiveActivity.swift

<interfaces>
<!-- Contrats clés extraits du codebase — l'exécutant ne devrait pas avoir besoin de fouiller. -->

From lib/mascot/companion-sprites.ts (current):
```ts
export const COMPANION_SPRITES: Record<CompanionSpecies, Record<CompanionStage, { idle_1: any; idle_2: any; happy: any }>> = { ... };
export type CompanionMood = 'idle' | 'happy';
```

From lib/mascotte-live-activity.ts (current):
```ts
export interface MascotteSnapshot {
  mascotteName: string;
  tasksDone: number;
  tasksTotal: number;
  xpGained: number;
  currentMeal: string | null;
  stageOverride?: MascotteStageOverride | null;
  companionSpriteBase64?: string | null;  // À RETIRER
  bonusText?: string | null;
  nextTaskText?: string | null;
  nextTaskId?: string | null;
  nextRdvText?: string | null;
  speechBubble?: string | null;
  feedBuffActive?: { multiplier: number; expiresAtIso: string } | null;
  // À AJOUTER : pose?: CompanionMood | null
}
export type MascotteStageOverride = 'reveil' | 'travail' | 'midi' | 'jeu' | 'routine' | 'dodo' | 'recap';
```

From modules/vault-access/src/index.ts (current bridge signatures):
```ts
startMascotteActivity(mascotteName, tasksDone, tasksTotal, xpGained, currentMeal,
  stageOverride, companionSpriteBase64, bonusText, nextTaskPayload, extrasPayload): Promise<boolean>;
updateMascotteActivity(...même shape...): Promise<void>;
```
Le bridge passe à 10 args max (limite Swift 6.3). Stratégie : remplacer `companionSpriteBase64` par `pose` (toujours 10 args, additif neutre côté JSI).

From MascotteActivityAttributes.swift (current ContentState):
```swift
public var companionSpriteToken: String?  // À RETIRER
// À AJOUTER : public var pose: String  (default "idle")
```

From MascotteLiveActivity.swift CompanionSpriteCache (current):
```swift
private static var cache: [String: UIImage] = [:]
let fileURL = container.appendingPathComponent("companion-sprite.png")  // hardcoded
```
À transformer en : `appendingPathComponent("companion-sprite-\(pose).png")` avec cache keyed par pose.

App Group identifier (déjà câblé) : `group.com.familyvault.dev`

Stage → pose mapping (déterministe, lib/mascotte-live-activity.ts dérivation) :
- 'dodo' → 'sleeping'
- 'midi' → 'eating'
- 'recap' & tasksDone === tasksTotal && tasksTotal > 0 → 'celebrating'
- autres → 'idle'

Override transitoire (priorité haute) : flash 'happy' 2s déclenché par patchMascotte({ pose: 'happy' }) — revert via setTimeout patchMascotte({ pose: <pose dérivée stage actuel> | 'idle' }).

CACHE_VERSION (lib/vault-cache.ts) : actuellement 7. **PAS de bump** — `pose` est state runtime de la LA, jamais persisté dans VaultCacheState.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 — Étendre sprites + bridge JS/Swift + ContentState (couche données)</name>
  <files>lib/mascot/companion-sprites.ts, lib/mascotte-live-activity.ts, modules/vault-access/src/index.ts, modules/vault-access/ios/VaultAccessModule.swift, modules/live-activity-shared/ios/Sources/MascotteActivityAttributes.swift</files>
  <action>
1. **lib/mascot/companion-sprites.ts** :
   - Étendre `CompanionMood` : `'idle' | 'happy' | 'sleeping' | 'eating' | 'celebrating'`
   - Étendre `COMPANION_SPRITES` : pour chaque espèce × stade, ajouter `sleeping`, `eating`, `celebrating` via `require('../../assets/garden/animals/<espece>/<stade>/{sleeping,eating,celebrating}.png')`. Les 45 PNG existent (5 espèces × 3 stades × 3 nouvelles poses).
   - Garder idle_1/idle_2/happy intacts (compat).

2. **lib/mascotte-live-activity.ts** :
   - Retirer `companionSpriteBase64` de `MascotteSnapshot`.
   - Ajouter `pose?: CompanionMood | null` dans `MascotteSnapshot` (default 'idle' côté logique).
   - Garder `loadCompanionSpriteBase64()` exporté (utilisé pour écrire les fichiers App Group), étendre pour gérer les 5 moods : `entry[mood]` direct (sauf 'idle' qui retombe sur `idle_1`).
   - Nouvelle fonction `writeCompanionPosesToAppGroup(species, stage)` qui charge les 5 base64 (idle/happy/sleeping/eating/celebrating) et appelle `writeCompanionPoseFile(pose, base64)` pour chaque (5 écritures parallèles via Promise.all).
   - `startMascotte()` : appeler `writeCompanionPosesToAppGroup` AVANT `startMascotteActivity`. Passer `snap.pose ?? 'idle'` au bridge à la place de `companionSpriteBase64`.
   - `refreshMascotte()` : idem, passer `snap.pose ?? 'idle'`. **Ne pas réécrire les fichiers** au refresh (fait au start uniquement, espèce/stade ne changent pas pendant la LA).
   - `patchMascotte()` : déjà OK, propage juste `pose` automatiquement par spread.

3. **modules/vault-access/src/index.ts** :
   - Ajouter au type `VaultAccessModuleType` : `writeCompanionPoseFile(pose: string, base64: string): Promise<void>`.
   - Exporter wrapper TS `writeCompanionPoseFile(pose, base64)` (no-op non-iOS).
   - Modifier signatures `startMascotteActivity` / `updateMascotteActivity` : remplacer paramètre `companionSpriteBase64: string | null` par `pose: string | null` (default 'idle'). Garder l'arité 10 (taskPayload + extrasPayload restent encodés en payloads compacts).

4. **modules/vault-access/ios/VaultAccessModule.swift** (à lire d'abord pour respecter le pattern existant) :
   - Ajouter `AsyncFunction("writeCompanionPoseFile") { (pose: String, base64: String) in ... }` qui décode base64 et écrit `companion-sprite-{pose}.png` dans `group.com.familyvault.dev` via FileManager containerURL.
   - Modifier `startMascotteActivity` / `updateMascotteActivity` : remplacer le paramètre `companionSpriteBase64` par `pose: String?`. Stocker dans `ContentState.pose ?? "idle"`. **Ne plus** écrire le fichier `companion-sprite.png` ici (déplacé côté JS via writeCompanionPoseFile).
   - **Migration douce** : si une activity en cours décode un ancien ContentState avec `companionSpriteToken`, le champ est ignoré (Codable additif, default 'idle' pour pose côté Swift).

5. **modules/live-activity-shared/ios/Sources/MascotteActivityAttributes.swift** :
   - Retirer `companionSpriteToken: String?` (et son init param).
   - Ajouter `public var pose: String` avec default `"idle"` dans l'init (commentaire explicite : "additive, ignoré par anciens widgets").
   - **Compatibilité Codable** : un decode d'un ancien JSON sans `pose` doit défaulter à "idle". Pour ça, marquer `pose` non-optional MAIS implémenter `init(from decoder)` custom OU rendre pose `String?` avec fallback "idle" à la lecture côté widget. Choisir l'option `String?` (plus simple et zéro custom decoder).

**Validation** :
- `npx tsc --noEmit` → 0 nouvelle erreur.
- Build iOS : `npx expo prebuild --platform ios` puis vérifier que MascotteActivityAttributes compile dans le pod live-activity-shared.

Conventions CLAUDE.md respectées : commentaires FR, pas de couleurs hardcoded (ce ticket ne touche pas l'UI), pas de console.log hors `__DEV__`.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "(companion-sprites|mascotte-live-activity|vault-access)" | grep -v "MemoryEditor\|cooklang\|useVault" | head -20</automated>
  </verify>
  <done>
    - CompanionMood union à 5 valeurs, COMPANION_SPRITES contient les 45 nouvelles entrées
    - MascotteSnapshot expose `pose`, plus de `companionSpriteBase64`
    - Bridge JS/Swift accepte `pose: string` à la place du base64 sprite
    - ContentState a `pose: String?` (avec fallback "idle"), plus de `companionSpriteToken`
    - writeCompanionPoseFile fonctionnel et exporté
    - tsc clean (sauf erreurs pré-existantes)
  </done>
</task>

<task type="auto">
  <name>Task 2 — Widget Swift : cache par pose + triggers TS (UI + orchestration)</name>
  <files>ios/MaJourneeWidget/MascotteLiveActivity.swift, hooks/useVault.ts, lib/mascotte-live-activity.ts</files>
  <action>
1. **ios/MaJourneeWidget/MascotteLiveActivity.swift** :
   - Réécrire `CompanionSpriteCache` : `cache: [String: UIImage]` keyed par pose name. Méthode `image(for pose: String) -> UIImage?` qui lit `companion-sprite-\(pose).png` dans App Group.
   - Mettre à jour `companionCompactView()` et tout autre call site : passer `state.pose ?? "idle"` au lieu de `state.companionSpriteToken`. Fallback emoji si lecture échoue (pattern existant préservé).
   - Vérifier toutes les autres vues (Lock Screen, expanded DI, minimal) : remplacer chaque `state.companionSpriteToken` par `state.pose ?? "idle"`.
   - Pas de Symbol effects, pas d'animation supplémentaire (scope v2a).

2. **lib/mascotte-live-activity.ts** — helper `derivePoseFromStage` :
   ```ts
   export function derivePoseFromStage(
     stage: MascotteStageOverride | null | undefined,
     tasksDone: number,
     tasksTotal: number,
   ): CompanionMood {
     if (stage === 'dodo') return 'sleeping';
     if (stage === 'midi') return 'eating';
     if (stage === 'recap' && tasksTotal > 0 && tasksDone >= tasksTotal) return 'celebrating';
     return 'idle';
   }
   ```
   Appliquer dans `startMascotte` et `refreshMascotte` : si `snap.pose` est undefined, dériver via `derivePoseFromStage`.

3. **hooks/useVault.ts** (Phase 42 feedCompanion, lignes 2050-2080) :
   - Supprimer le double load `loadCompanionSpriteBase64(...,'happy')` + `loadCompanionSpriteBase64(...,'idle')` + setTimeout 4s.
   - Remplacer par :
     ```ts
     if (shouldFlashHappy) {
       await patchMascotte({ feedBuffActive: buffPayload, pose: 'happy' });
       setTimeout(() => {
         // Reverse vers la pose dérivée du stage courant (lastSnapshot interne)
         patchMascotte({ pose: 'idle' }).catch(() => {});
         // Note : si un stage narratif (dodo/midi/recap) est actif, le prochain
         // refreshMascotte naturel re-dérivera la bonne pose. 2s suffisent pour le flash.
       }, 2000);
     } else {
       await patchMascotte({ feedBuffActive: buffPayload });
     }
     ```
   - Garder `try/catch { /* non-critical */ }` autour.

4. **Triggers narratifs** (déjà câblés via `stageOverride` dans les call sites existants de `refreshMascotte` — TimelineView 260420-fsj). Aucun changement nécessaire ici : `derivePoseFromStage` les capture automatiquement à chaque refresh.

**Conventions CLAUDE.md** :
- Commentaires FR, useThemeColors non concerné (Swift uniquement)
- Animations Reanimated non concernées (LA = SwiftUI native iOS)
- expo-haptics non concerné
- Pas de bump CACHE_VERSION (state runtime LA, pas dans VaultCacheState)

**Validation finale** :
- `npx tsc --noEmit` clean (hors erreurs pré-existantes)
- Build iOS Live Activity widget compile sans erreur
- Tester manuellement (checkpoint Task 3) sur device physique
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "useVault|mascotte-live" | grep -v "MemoryEditor\|cooklang" | head -20</automated>
  </verify>
  <done>
    - CompanionSpriteCache keyed par pose, lit le bon fichier App Group
    - state.companionSpriteToken supprimé partout, remplacé par state.pose ?? "idle"
    - derivePoseFromStage exportée et appliquée dans start/refresh Mascotte
    - feedCompanion utilise patchMascotte({pose:'happy'}) + revert 2s
    - tsc clean
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3 — Vérification device Live Activity 5 poses</name>
  <what-built>
    Live Activity mascotte affiche désormais 5 poses sprite (idle/happy/sleeping/eating/celebrating) au lieu du couple idle/happy. Le payload ContentState est passé d'un base64 ~5-15KB à un simple `pose: String` (<500B). Les 5 PNG sont écrits dans l'App Group au start de la LA.
  </what-built>
  <how-to-verify>
    1. Build & install : `npx expo run:ios --device`
    2. Ouvrir l'app, démarrer la Live Activity mascotte (bouton ferme/dashboard).
    3. **Vérifier les 5 poses** :
       - Lock Screen + DI compact : sprite **idle** affiché par défaut.
       - Cocher une tâche → flash **happy** ~2s puis retour idle. ✅
       - Attendre/forcer stage 'midi' (ou modifier l'heure simulateur) → pose **eating** persistante. ✅
       - Stage 'dodo' (soir) → pose **sleeping**. ✅
       - Compléter toutes les tâches du jour pendant stage recap → pose **celebrating** ✓ + checkmark vert. ✅
    4. **Vérifier le payload allégé** : aucun comportement de freeze/lag à l'update (le base64 est gone), updates fluides.
    5. **Backward compat** : si une LA tournait avant le rebuild (ancien format avec companionSpriteToken), elle ne crash pas — fallback emoji acceptable, le prochain start affichera le sprite correct.
    6. **Migration douce** : tester un cold start avec une vieille LA active → ne crash pas.
  </how-to-verify>
  <resume-signal>Réponds "approuvé" si les 5 poses fonctionnent et que la LA ne crash pas, ou décris les régressions observées.</resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` → 0 nouvelle erreur (les 3 erreurs pré-existantes MemoryEditor/cooklang/useVault restent)
- Live Activity démarre, affiche les 5 poses selon le stage et les triggers
- Aucune régression sur les autres champs ContentState (tasksDone, xpGained, speechBubble, feedBuffActive, justCompletedAt, etc.)
- App Group contient bien `companion-sprite-{idle,happy,sleeping,eating,celebrating}.png` après start (vérifiable via Xcode > Devices > Container)
</verification>

<success_criteria>
- [ ] CompanionMood union à 5 valeurs, sprites mappés
- [ ] writeCompanionPoseFile bridge fonctionne, 5 fichiers App Group écrits au start
- [ ] ContentState.pose remplace companionSpriteToken, payload < 500B
- [ ] Widget CompanionSpriteCache keyed par pose, lit le bon fichier
- [ ] feedCompanion → flash happy 2s + revert (au lieu du double-load base64 4s)
- [ ] derivePoseFromStage dérive pose narrative (dodo→sleeping, midi→eating, recap 100%→celebrating)
- [ ] Backward compat : ancienne LA en cours ne crash pas (Codable additif)
- [ ] Pas de bump CACHE_VERSION (state runtime, pas cache vault)
- [ ] Commit FR via `/ship` ou commit manuel : "feat(live-activity): 5 poses compagnon (idle/happy/sleeping/eating/celebrating)"
</success_criteria>

<output>
Après complétion, créer `.planning/quick/260425-0qf-int-grer-5-poses-sprite-compagnon-idle-h/260425-0qf-SUMMARY.md` documentant : les 5 poses livrées, la migration base64→pose, le fichier App Group pattern, les triggers narratifs, et toute découverte/pitfall (taille payload mesurée, comportement migration LA vivante, etc.).
</output>
