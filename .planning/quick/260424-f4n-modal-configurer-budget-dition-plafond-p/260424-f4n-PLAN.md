---
phase: quick-260424-f4n
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/(tabs)/budget.tsx
autonomous: true
requirements:
  - F4N-01
must_haves:
  truths:
    - "Un bouton 'Configurer' apparaît dans le header de budget.tsx, à gauche du bouton scan ticket"
    - "Tap sur 'Configurer' ouvre un modal pageSheet listant toutes les catégories existantes"
    - "Chaque ligne du modal affiche emoji + nom de la catégorie + TextInput décimal pré-rempli avec cat.limit"
    - "Bouton 'Enregistrer' appelle updateBudgetConfig avec la nouvelle config, déclenche Haptics.Success et toast FR de confirmation"
    - "Les emojis/noms des catégories ne sont pas modifiables — seuls les plafonds (limit) changent"
  artifacts:
    - path: "app/(tabs)/budget.tsx"
      provides: "Bouton header 'Configurer' + modal ConfigureBudgetModal inline + state configModalVisible + configLimits Record<string, string>"
      contains: "configModalVisible"
  key_links:
    - from: "app/(tabs)/budget.tsx (bouton Configurer)"
      to: "app/(tabs)/budget.tsx (Modal presentationStyle pageSheet)"
      via: "setConfigModalVisible(true)"
      pattern: "setConfigModalVisible"
    - from: "app/(tabs)/budget.tsx (bouton Enregistrer)"
      to: "useVault().updateBudgetConfig"
      via: "handleSaveBudgetConfig reconstruit BudgetConfig via budgetConfig.categories.map(c => ({...c, limit: parseFloat(configLimits[c.name])}))"
      pattern: "updateBudgetConfig\\("
---

<objective>
Ajouter un modal "Configurer le budget" dans `app/(tabs)/budget.tsx` permettant d'éditer le plafond (`limit`) de chaque catégorie individuellement, sans modifier emoji/nom.

Purpose: Permettre à l'utilisateur d'ajuster ses plafonds budgétaires directement depuis l'écran budget, sans passer par l'édition manuelle du vault.
Output: Bouton header "Configurer" + modal pageSheet avec liste d'inputs décimaux pré-remplis + sauvegarde via `updateBudgetConfig`.
</objective>

<context>
@./CLAUDE.md
@app/(tabs)/budget.tsx

<interfaces>
From hooks/useVault.ts:
```typescript
updateBudgetConfig: (config: BudgetConfig) => Promise<void>;
```

From lib/types.ts (BudgetConfig est déjà consommé dans budget.tsx via budgetConfig):
```typescript
interface BudgetCategory { name: string; emoji: string; limit: number; }
interface BudgetConfig { categories: BudgetCategory[]; /* ... */ }
```

Pattern déjà présent dans le fichier — réutiliser strictement :
- `const { budgetConfig, ... } = useVault()` — destructuration existante (ligne ~98)
- Modal pattern : `<Modal visible={...} animationType="slide" presentationStyle="pageSheet" onRequestClose={...}>` (ligne ~794)
- Drag handle : `<View style={styles.dragHandleBar}><View style={[styles.dragHandle, { backgroundColor: colors.separator }]} /></View>` (ligne ~804-807)
- Bouton header pattern (scan ticket) ligne 470-484 — copier le style `styles.scanBtn`/`scanBtnText`
- Haptics succès : `Haptics.notificationAsync(Haptics.NotificationFeedbackType.Success)` (déjà importé, ligne 355)
- Toast : `showToast('...', 'success')` (déjà disponible via useToast, ligne 97)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ajouter le modal "Configurer le budget" + bouton header</name>
  <files>app/(tabs)/budget.tsx</files>
  <action>
Dans `app/(tabs)/budget.tsx` :

1. **Destructuration useVault** (ligne ~98-108) : ajouter `updateBudgetConfig` à la destructuration existante :
   ```ts
   const { budgetEntries, budgetConfig, budgetMonth, loadBudgetData, loadBudgetMonths, addExpense, deleteExpense, updateBudgetConfig, activeProfile, refresh } = useVault();
   ```

2. **State local** (après `const [addModalVisible, setAddModalVisible] = useState(false);` ligne ~127) :
   ```ts
   const [configModalVisible, setConfigModalVisible] = useState(false);
   const [configLimits, setConfigLimits] = useState<Record<string, string>>({});
   ```

3. **Handler d'ouverture** `handleOpenConfigModal` (useCallback) :
   - Pré-remplit `configLimits` avec `Object.fromEntries(budgetConfig.categories.map(c => [c.name, String(c.limit)]))`
   - Puis `setConfigModalVisible(true)`

4. **Handler de sauvegarde** `handleSaveBudgetConfig` (useCallback async) :
   - Valide chaque montant : `parseFloat(configLimits[cat.name].replace(',', '.'))`. Si `NaN` ou `< 0` → `showToast('Montant invalide pour ' + cat.name, 'error')` + return.
   - Construit la nouvelle config : `{ ...budgetConfig, categories: budgetConfig.categories.map(c => ({ ...c, limit: parseFloat(configLimits[c.name].replace(',', '.')) })) }`
   - `await updateBudgetConfig(newConfig)`
   - `Haptics.notificationAsync(Haptics.NotificationFeedbackType.Success)`
   - `showToast('Plafonds mis à jour', 'success')`
   - `setConfigModalVisible(false)`
   - Dépendances : `[budgetConfig, configLimits, updateBudgetConfig, showToast]`

5. **Bouton header** : dans `<View style={styles.headerActions}>` (ligne ~468), AVANT le bouton scan ticket (donc en premier enfant du View) :
   ```tsx
   <TouchableOpacity
     style={[styles.scanBtn, { backgroundColor: colors.cardAlt, borderColor: colors.border }]}
     onPress={handleOpenConfigModal}
     activeOpacity={0.7}
     accessibilityLabel="Configurer les plafonds du budget"
     accessibilityRole="button"
   >
     <Text style={[styles.scanBtnText, { color: primary }]}>Configurer</Text>
   </TouchableOpacity>
   ```
   Réutilise `styles.scanBtn` / `styles.scanBtnText` existants (pas de nouveau style à ajouter pour le bouton).

6. **Modal "Configurer le budget"** : ajouter APRÈS le `</Modal>` du add-expense modal (ligne ~890) et AVANT `<ReceiptReview />` :
   ```tsx
   <Modal
     visible={configModalVisible}
     animationType="slide"
     presentationStyle="pageSheet"
     onRequestClose={() => setConfigModalVisible(false)}
   >
     <KeyboardAvoidingView
       behavior={Platform.OS === 'ios' ? 'padding' : undefined}
       style={[styles.modalContainer, { backgroundColor: colors.bg }]}
     >
       <View style={styles.dragHandleBar}>
         <View style={[styles.dragHandle, { backgroundColor: colors.separator }]} />
       </View>
       <ScrollView contentContainerStyle={styles.modalContent}>
         <Text style={[styles.modalTitle, { color: colors.text }]}>Configurer le budget</Text>
         <Text style={[styles.fieldLabel, { color: colors.textSub }]}>Plafond par catégorie (€)</Text>
         {budgetConfig.categories.map((cat) => (
           <View key={cat.name} style={styles.configRow}>
             <Text style={[styles.configCatLabel, { color: colors.text }]}>
               {cat.emoji} {cat.name}
             </Text>
             <TextInput
               style={[styles.configInput, { backgroundColor: colors.inputBg, borderColor: colors.inputBorder, color: colors.text }]}
               keyboardType="decimal-pad"
               value={configLimits[cat.name] ?? ''}
               onChangeText={(txt) => setConfigLimits(prev => ({ ...prev, [cat.name]: txt }))}
               placeholder="0"
               placeholderTextColor={colors.textFaint}
               accessibilityLabel={`Plafond ${cat.name}`}
             />
           </View>
         ))}
         <TouchableOpacity
           style={[styles.submitBtn, { backgroundColor: primary }]}
           onPress={handleSaveBudgetConfig}
           activeOpacity={0.7}
           accessibilityRole="button"
         >
           <Text style={[styles.submitBtnText, { color: colors.onPrimary }]}>Enregistrer</Text>
         </TouchableOpacity>
         <TouchableOpacity
           style={styles.cancelBtn}
           onPress={() => setConfigModalVisible(false)}
           activeOpacity={0.7}
           accessibilityRole="button"
         >
           <Text style={[styles.cancelBtnText, { color: colors.textMuted }]}>Annuler</Text>
         </TouchableOpacity>
       </ScrollView>
     </KeyboardAvoidingView>
   </Modal>
   ```

7. **Styles** : ajouter dans `StyleSheet.create({ ... })` (bas de fichier, après `checkmark`) :
   ```ts
   configRow: {
     flexDirection: 'row',
     alignItems: 'center',
     justifyContent: 'space-between',
     marginTop: Spacing.lg,
     gap: Spacing.xl,
   },
   configCatLabel: {
     flex: 1,
     fontSize: FontSize.body,
     fontWeight: FontWeight.semibold,
   },
   configInput: {
     width: 110,
     borderWidth: 1,
     borderRadius: Radius.lg,
     paddingHorizontal: Spacing.xl,
     paddingVertical: Spacing.md,
     fontSize: FontSize.body,
     textAlign: 'right',
   },
   ```

**Contraintes strictes** :
- Tous les libellés en français (D-UI)
- Uniquement `useThemeColors()` — jamais de hex hardcoded
- Tokens `Spacing`/`Radius`/`FontSize`/`FontWeight` (pas de nombres bruts)
- Ne PAS toucher emoji/name des catégories — seulement `limit`
- Modal `presentationStyle="pageSheet" + animationType="slide"` (drag-to-dismiss natif iOS)
- Ne pas régresser le flux "Add expense" existant (même fichier)
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>
- `npx tsc --noEmit` passe sans nouvelle erreur introduite par le fichier `app/(tabs)/budget.tsx`
- Un bouton "Configurer" (texte) visible dans le header de l'écran budget à gauche du bouton "Ticket"
- Tap ouvre un modal pageSheet avec drag handle, titre "Configurer le budget", et une ligne par catégorie de `budgetConfig.categories`
- Chaque TextInput est pré-rempli avec la valeur actuelle de `cat.limit` au moment de l'ouverture
- "Enregistrer" écrit la nouvelle config via `updateBudgetConfig`, déclenche haptique Succès + toast FR "Plafonds mis à jour", ferme le modal
- "Annuler" ferme le modal sans écrire
- Saisie invalide (non-numérique ou négative) → toast d'erreur en FR, pas d'écriture
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` : aucune nouvelle erreur TS
- Lancement app (`npx expo run:ios --device`) : ouvrir Budget, tap "Configurer", modifier un plafond, enregistrer, vérifier que la barre de progression de la catégorie reflète le nouveau plafond.
- Vérifier `03 - Budget/config.md` (ou fichier correspondant) mis à jour dans le vault iCloud après enregistrement.
</verification>

<success_criteria>
- Modal "Configurer le budget" fonctionnel, accessible via header
- Plafonds éditables, sauvegardés via `updateBudgetConfig`, reflétés immédiatement dans l'onglet Résumé
- Zéro régression sur les flux existants (add expense, scan ticket, filtres, sélection multiple, évolution)
- Conformité CLAUDE.md : français, `useThemeColors`, tokens design, reanimated non requis ici (pas d'animations custom)
</success_criteria>

<output>
Mettre à jour STATE.md Quick Tasks après exécution via le workflow `/ship` habituel.
</output>
