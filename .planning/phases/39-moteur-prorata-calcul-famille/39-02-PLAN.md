---
phase: 39-moteur-prorata-calcul-famille
plan: 02
type: execute
wave: 2
depends_on: [39-01]
files_modified:
  - lib/mascot/wager-engine.ts
  - lib/__tests__/wager-engine.test.ts
autonomous: true
requirements: [SPOR-03, SPOR-04, SPOR-05, SPOR-06, SPOR-13]
must_haves:
  truths:
    - "Le moteur wager-engine.ts est pur (zéro I/O, zéro new Date() sans paramètre, zéro import hooks/)"
    - "computeAgeCategory retourne la bonne catégorie pour les 5 brackets (D-02)"
    - "resolveWeight priorise weight_override sur birthdate dérivé (D-03)"
    - "isProfileActive7d détecte correctement la fenêtre 7j glissants (SPOR-05)"
    - "filterTasksForWager garde uniquement les Tasks sourceFile matchant Tâches récurrentes (SPOR-06)"
    - "computeCumulTarget applique la formule (poids_sealeur / somme_actifs) × pending avec Math.ceil, fallback D-04"
    - "shouldRecompute + maybeRecompute respectent la fenêtre 23h30 + catchup jour suivant (SPOR-03, D-06)"
    - "validateWagerOnHarvest retourne won=true si cumulTarget===0 ou cumulCurrent >= cumulTarget"
    - "canSealWager refuse les sealeurs à poids 0 (D-04)"
    - "Suite Jest wager-engine 7 describe blocs verts"
  artifacts:
    - path: lib/mascot/wager-engine.ts
      provides: "7 fonctions pures + constantes WEIGHT_BY_CATEGORY + types discriminés résultats"
      exports: ["computeAgeCategory", "resolveWeight", "isProfileActive7d", "filterTasksForWager", "computeCumulTarget", "shouldRecompute", "maybeRecompute", "validateWagerOnHarvest", "canSealWager", "WEIGHT_BY_CATEGORY"]
      min_lines: 220
    - path: lib/__tests__/wager-engine.test.ts
      provides: "suite Jest complète 7 describe blocs — couverture cas bornes D-02/03/04/05/06 + round-trip prorata"
      min_lines: 350
  key_links:
    - from: "lib/mascot/wager-engine.ts resolveWeight"
      to: "lib/types.ts Profile.weight_override + birthdate"
      via: "import Profile; priorité override, fallback computeAgeCategory(birthdate, today)"
      pattern: "resolveWeight"
    - from: "lib/mascot/wager-engine.ts"
      to: "lib/mascot/sporee-economy.ts getLocalDateKey"
      via: "import réutilisé pour shouldRecompute today key"
      pattern: "from.*sporee-economy"
    - from: "lib/mascot/wager-engine.ts"
      to: "lib/village/parser.ts FamilySnapshot (Plan 01)"
      via: "import type { FamilySnapshot } pour signature computeCumulTarget"
      pattern: "FamilySnapshot"
---

<objective>
Livrer le moteur de calcul PUR du pari Sporée dans `lib/mascot/wager-engine.ts` : 7 concepts (age→catégorie, résolution poids, activité 7j, filtre domaine Tasks, prorata cumul, déclencheur recompute, validation récolte) + suite Jest exhaustive. Zéro I/O, zéro UI, zéro hook. Le moteur sera consommé intégralement par la Phase 40.

Purpose: Isoler toute la logique stratégique du pari dans un module testable en isolation — Phase 40 n'aura qu'à brancher les appels et gérer l'I/O vault. Pureté stricte = testabilité maximale + réutilisabilité.
Output: `lib/mascot/wager-engine.ts` (~220-280 lignes) + `lib/__tests__/wager-engine.test.ts` (~350-450 lignes).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/39-moteur-prorata-calcul-famille/39-CONTEXT.md
@.planning/phases/39-moteur-prorata-calcul-famille/39-RESEARCH.md
@.planning/phases/39-moteur-prorata-calcul-famille/39-01-PLAN.md
@lib/mascot/sporee-economy.ts
@lib/mascot/types.ts
@lib/types.ts
@lib/__tests__/sporee-economy.test.ts

<interfaces>
<!-- Types exacts à consommer — pas d'interprétation -->

Depuis lib/types.ts (Plan 01 a ajouté weight_override) :
```typescript
export type WagerAgeCategory = 'adulte' | 'ado' | 'enfant' | 'jeune' | 'bebe';
export interface Profile {
  id: string;
  name: string;
  role: 'enfant' | 'ado' | 'adulte';
  birthdate?: string;       // YYYY-MM-DD ou YYYY
  statut?: 'grossesse' | 'ne';
  weight_override?: WagerAgeCategory; // Phase 39 — ajouté Plan 01
  // ...
}
export interface Task {
  id: string;
  text: string;
  completed: boolean;
  completedDate?: string;   // YYYY-MM-DD
  mentions: string[];       // @user
  sourceFile: string;       // chemin relatif vault
  // ...
}
```

Depuis lib/mascot/sporee-economy.ts (Phase 38 livré) :
```typescript
export function getLocalDateKey(d?: Date): string; // YYYY-MM-DD local
```

Depuis lib/village/parser.ts (Plan 01 ajouté) :
```typescript
export interface FamilySnapshot {
  date: string;              // YYYY-MM-DD local
  pending: number;
  activeProfileIds: string[];
}
```

Depuis lib/mascot/types.ts (Phase 38 livré) :
```typescript
export type WagerDuration = 'chill' | 'engage' | 'sprint';
export type WagerMultiplier = 1.3 | 1.7 | 2.5;
export interface WagerModifier {
  sporeeId: string;
  duration: WagerDuration;
  multiplier: WagerMultiplier;
  appliedAt: string;
  sealerProfileId: string;
  cumulTarget?: number;   // peuplé par ce moteur
  cumulCurrent?: number;  // peuplé par ce moteur
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Écrire wager-engine.ts (7 concepts) + types résultats discriminés</name>
  <files>lib/mascot/wager-engine.ts</files>
  <read_first>
    - lib/mascot/sporee-economy.ts (intégral — pattern fonction pure, injection now, types discriminés BuySporeeCheck)
    - lib/mascot/types.ts (lignes 295-330 — WagerModifier complet)
    - lib/types.ts (Profile + Task après Plan 01)
    - lib/village/parser.ts (type FamilySnapshot exporté Plan 01)
    - hooks/useVault.ts (lignes 295-305 — STATIC_TASK_FILES pour comprendre le pattern sourceFile Tasks)
    - .planning/phases/39-moteur-prorata-calcul-famille/39-RESEARCH.md §"Code Examples" + §"Pitfalls"
  </read_first>
  <behavior>
    <!-- TDD : ces comportements seront encodés en Jest dans Task 2 -->
    - computeAgeCategory : bornes inclusives 0-2 bebe, 3-5 jeune, 6-12 enfant, 13-17 ado, 18+ adulte
    - resolveWeight : override prioritaire, sinon dérivation, sinon fallback adulte 1.0 avec console.warn __DEV__
    - isProfileActive7d : ≥1 tâche avec completed=true ET completedDate dans [today-7j, today]
    - filterTasksForWager : ne conserve que les tâches avec sourceFile matchant `Tâches récurrentes` (exclut Courses/Repas/Routines/Anniversaires/Notes/Moods via pattern sourceFile)
    - computeCumulTarget : Math.ceil((sealerWeight / familyWeightSum) * pendingCount), fallback cumulTarget=pendingCount si diviseur=0
    - shouldRecompute : true si lastRecomputeDate < today OU (même jour mais heure >= 23:30 ET pas encore recompute) — le cas "déjà recompute aujourd'hui" retourne false
    - maybeRecompute : wrapper qui appelle recompute et met à jour lastRecomputeDate si shouldRecompute=true
    - validateWagerOnHarvest : won=true ssi cumulTarget===0 || cumulCurrent >= cumulTarget
    - canSealWager : { ok: false, reason: 'zero_weight' } si weight=0, sinon { ok: true }
  </behavior>
  <action>
    Créer `lib/mascot/wager-engine.ts` avec EXACTEMENT cette structure (types + implémentations) :

    ```typescript
    /**
     * Moteur prorata Sporée — Phase 39
     * Fonctions pures, zéro I/O, zéro new Date() sans paramètre.
     * Pattern identique à sporee-economy.ts (Phase 38).
     */
    import type { Profile, Task, WagerAgeCategory } from '../types';
    import type { FamilySnapshot } from '../village/parser';
    import { getLocalDateKey } from './sporee-economy';

    // ─── Constantes poids par âge (D-02) ─────────────────────────────
    export const WEIGHT_BY_CATEGORY: Record<WagerAgeCategory, number> = {
      adulte: 1.0,
      ado:    0.7,
      enfant: 0.4,
      jeune:  0.15,
      bebe:   0.0,
    };

    // ─── 1. yearsDiff + computeAgeCategory (D-02) ────────────────────
    /** Différence en années révolues — équivalent differenceInYears sans dépendance externe. */
    export function yearsDiff(from: Date, to: Date): number {
      let age = to.getFullYear() - from.getFullYear();
      const m = to.getMonth() - from.getMonth();
      if (m < 0 || (m === 0 && to.getDate() < from.getDate())) age--;
      return age;
    }

    /** Dérive la catégorie d'âge depuis birthdate (YYYY-MM-DD ou YYYY) + date de référence. */
    export function computeAgeCategory(birthdate: string, today: string): WagerAgeCategory {
      const normalizedBirth = birthdate.length === 4 ? `${birthdate}-01-01` : birthdate;
      const birth = new Date(normalizedBirth + (birthdate.length === 4 ? '' : 'T00:00:00'));
      const now = new Date(today + 'T00:00:00');
      const age = yearsDiff(birth, now);
      if (age <= 2)  return 'bebe';
      if (age <= 5)  return 'jeune';
      if (age <= 12) return 'enfant';
      if (age <= 17) return 'ado';
      return 'adulte';
    }

    // ─── 2. resolveWeight (D-03) ─────────────────────────────────────
    export function resolveWeight(profile: Profile, today: string): number {
      if (profile.weight_override) {
        return WEIGHT_BY_CATEGORY[profile.weight_override];
      }
      if (!profile.birthdate) {
        if (__DEV__) {
          console.warn(`[wager-engine] profil ${profile.id} sans birthdate ni weight_override — fallback adulte 1.0`);
        }
        return 1.0;
      }
      return WEIGHT_BY_CATEGORY[computeAgeCategory(profile.birthdate, today)];
    }

    // ─── 3. isProfileActive7d (SPOR-05) ──────────────────────────────
    /**
     * Détecte si un profil a complété ≥1 tâche dans la fenêtre [today - 7j, today].
     * Requiert completedDate explicite — tâches completed sans date sont ignorées (Pitfall 3).
     * L'appartenance tâche→profil utilise mentions.includes(profileId) OU profile name présent dans sourceFile.
     */
    export function isProfileActive7d(
      tasks: Task[],
      profile: Profile,
      today: string,
    ): boolean {
      const todayMs = new Date(today + 'T00:00:00').getTime();
      const sevenDaysAgoMs = todayMs - 7 * 86400000;
      return tasks.some(t => {
        if (!t.completed || !t.completedDate) return false;
        const byProfile =
          t.mentions.includes(profile.id) ||
          t.mentions.includes(profile.name) ||
          t.sourceFile.toLowerCase().includes(profile.name.toLowerCase());
        if (!byProfile) return false;
        const tMs = new Date(t.completedDate + 'T00:00:00').getTime();
        return tMs >= sevenDaysAgoMs && tMs <= todayMs;
      });
    }

    // ─── 4. filterTasksForWager (SPOR-06) ────────────────────────────
    /**
     * Ne conserve que les tâches du domaine "Tasks" — celles avec sourceFile
     * pointant vers un fichier "Tâches récurrentes.md" (pattern STATIC_TASK_FILES
     * dans hooks/useVault.ts). Exclut explicitement Courses/Repas/Routines/Anniversaires/Notes/Moods.
     */
    export function filterTasksForWager(tasks: Task[]): Task[] {
      return tasks.filter(t => {
        const sf = t.sourceFile.toLowerCase();
        return sf.includes('tâches récurrentes') || sf.includes('taches recurrentes');
      });
    }

    // ─── 5. computeCumulTarget (SPOR-03/04/05, formule prorata D-04) ─
    export interface FamilyWeightResult {
      cumulTarget: number;
      activeProfileIds: string[];
      weights: Record<string, number>;
      sealerWeight: number;
      familyWeightSum: number;
    }

    export function computeCumulTarget(opts: {
      sealerProfileId: string;
      allProfiles: Profile[];
      tasks: Task[];           // tâches déjà filtrées domain Tasks
      today: string;
      pendingCount: number;
    }): FamilyWeightResult {
      const { sealerProfileId, allProfiles, tasks, today, pendingCount } = opts;
      // Exclure profils en statut grossesse (Pitfall 2)
      const eligible = allProfiles.filter(p => p.statut !== 'grossesse');
      // Résoudre poids par profil
      const weights: Record<string, number> = {};
      const activeProfileIds: string[] = [];
      let familyWeightSum = 0;
      for (const p of eligible) {
        const w = resolveWeight(p, today);
        weights[p.id] = w;
        const isActive = p.id === sealerProfileId || isProfileActive7d(tasks, p, today);
        if (isActive && w > 0) {
          activeProfileIds.push(p.id);
          familyWeightSum += w;
        }
      }
      const sealer = eligible.find(p => p.id === sealerProfileId);
      const sealerWeight = sealer ? (weights[sealerProfileId] ?? 0) : 0;
      // D-04 fallback : diviseur = 0 OU sealeur introuvable → cumulTarget = pendingCount
      if (familyWeightSum === 0 || !sealer) {
        return { cumulTarget: pendingCount, activeProfileIds, weights, sealerWeight, familyWeightSum };
      }
      const ratio = sealerWeight / familyWeightSum;
      return {
        cumulTarget: Math.ceil(ratio * pendingCount),
        activeProfileIds,
        weights,
        sealerWeight,
        familyWeightSum,
      };
    }

    // ─── 6. canSealWager (D-04) ──────────────────────────────────────
    export type CanSealResult = { ok: true } | { ok: false; reason: 'zero_weight' | 'profile_not_found' };

    export function canSealWager(opts: { sealerProfileId: string; allProfiles: Profile[]; today: string }): CanSealResult {
      const { sealerProfileId, allProfiles, today } = opts;
      const sealer = allProfiles.find(p => p.id === sealerProfileId);
      if (!sealer) return { ok: false, reason: 'profile_not_found' };
      const w = resolveWeight(sealer, today);
      if (w === 0) return { ok: false, reason: 'zero_weight' };
      return { ok: true };
    }

    // ─── 7. shouldRecompute + maybeRecompute (D-05, D-06) ────────────
    /**
     * true si : (a) lastRecomputeDate est antérieur au jour courant (catchup boot)
     *        OU (b) même jour mais heure >= 23:30 ET pas encore recompute (edge matin/soir)
     */
    export function shouldRecompute(now: Date, lastRecomputeDate: string): boolean {
      const todayKey = getLocalDateKey(now);
      if (lastRecomputeDate === todayKey) return false;
      // Jour différent → catchup (peu importe l'heure)
      if (lastRecomputeDate < todayKey) return true;
      // lastRecomputeDate > todayKey ne devrait jamais arriver ; défensif :
      return false;
    }

    /**
     * Wrapper qui délègue le calcul à computeCumulTarget si shouldRecompute=true,
     * sinon retourne un no-op. Le hook Phase 40 gère l'I/O (lecture/écriture snapshot).
     */
    export function maybeRecompute(opts: {
      now: Date;
      lastRecomputeDate: string;
      snapshot: FamilySnapshot | null;
      sealerProfileId: string;
      allProfiles: Profile[];
      tasks: Task[];
    }): { recomputed: false } | { recomputed: true; result: FamilyWeightResult; newRecomputeDate: string } {
      if (!shouldRecompute(opts.now, opts.lastRecomputeDate)) return { recomputed: false };
      const today = getLocalDateKey(opts.now);
      const pendingCount = opts.snapshot?.pending ?? 0;
      const result = computeCumulTarget({
        sealerProfileId: opts.sealerProfileId,
        allProfiles: opts.allProfiles,
        tasks: opts.tasks,
        today,
        pendingCount,
      });
      return { recomputed: true, result, newRecomputeDate: today };
    }

    // ─── 8. validateWagerOnHarvest (SPOR-07 fondation, consommé Phase 40) ─
    export interface WagerHarvestResult {
      won: boolean;
      cumulCurrent: number;
      cumulTarget: number;
    }

    export function validateWagerOnHarvest(cumulCurrent: number, cumulTarget: number): WagerHarvestResult {
      return {
        won: cumulTarget === 0 || cumulCurrent >= cumulTarget,
        cumulCurrent,
        cumulTarget,
      };
    }
    ```

    **Contraintes strictes :**
    - Aucun `new Date()` SANS paramètre (toutes les dates injectées ou dérivées de `now`)
    - Aucun `import` depuis `hooks/`, `components/`, `app/`, `contexts/`
    - Aucune dépendance externe (date-fns, lodash, etc.)
    - Commentaires en français (règle CLAUDE.md)
    - `if (__DEV__)` pour console.warn (règle CLAUDE.md)
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep "error TS" | grep -v "MemoryEditor.tsx\|cooklang.ts\|useVault.ts" && exit 1 || exit 0</automated>
  </verify>
  <acceptance_criteria>
    - Fichier `lib/mascot/wager-engine.ts` existe avec ≥220 lignes
    - `grep "export " lib/mascot/wager-engine.ts` retourne ≥10 matches (constantes + fonctions + types)
    - `grep "new Date()" lib/mascot/wager-engine.ts` retourne 0 match (pureté — uniquement `new Date(paramètre)`)
    - `grep "from.*hooks\|from.*components\|from.*app/" lib/mascot/wager-engine.ts` retourne 0 match
    - `grep "date-fns\|lodash" lib/mascot/wager-engine.ts` retourne 0 match
    - `grep "if (__DEV__)" lib/mascot/wager-engine.ts` retourne ≥1 match
    - `grep "export function computeAgeCategory\|export function resolveWeight\|export function isProfileActive7d\|export function filterTasksForWager\|export function computeCumulTarget\|export function canSealWager\|export function shouldRecompute\|export function maybeRecompute\|export function validateWagerOnHarvest" lib/mascot/wager-engine.ts` retourne 9 matches
    - `npx tsc --noEmit` clean hors erreurs pré-existantes
  </acceptance_criteria>
  <done>
    Moteur pur complet, types exportés, TypeScript clean. Prêt pour la suite Jest (Task 2).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Suite Jest wager-engine.test.ts — 7 describe blocs + matrice complète</name>
  <files>lib/__tests__/wager-engine.test.ts</files>
  <read_first>
    - lib/mascot/wager-engine.ts (sortie Task 1 — pour copier les signatures exactes)
    - lib/__tests__/sporee-economy.test.ts (pattern describe-par-concept, privacy IDs, structure test)
    - .planning/phases/39-moteur-prorata-calcul-famille/39-CONTEXT.md §specifics (liste 7 describe)
    - .planning/phases/39-moteur-prorata-calcul-famille/39-RESEARCH.md §"Pitfalls" (cas à couvrir)
  </read_first>
  <behavior>
    Suite exhaustive en 7 `describe` blocs :

    **describe('computeAgeCategory')** (≥8 tests)
    - 'bebe' pour 0, 1, 2 ans révolus (3 tests avec dates précises)
    - 'jeune' pour 3 et 5 ans (2 tests)
    - 'enfant' pour 6 et 12 ans (2 tests)
    - 'ado' pour 13 et 17 ans (2 tests)
    - 'adulte' pour 18, 35, 65 ans (3 tests)
    - Format birthdate YYYY seul (ex: '1990') → parsé comme 1990-01-01
    - Anniversaire non passé dans l'année → yearsDiff -1 (ex: né 2020-06-15, today=2026-04-18 → 5 ans = jeune)

    **describe('resolveWeight')** (≥6 tests)
    - override='adulte' → 1.0 quelle que soit birthdate
    - override='bebe' → 0.0
    - Pas d'override + birthdate 2018-01-01 + today 2026-04-18 → 0.4 (enfant)
    - Pas d'override + pas de birthdate → 1.0 (fallback) + console.warn appelé (spy)
    - override='jeune' écrase birthdate adulte
    - Valeur invalide weight_override (via cast) → NE DOIT PAS arriver côté parser, mais test défensif OK si skip

    **describe('isProfileActive7d')** (≥6 tests)
    - Profile avec 1 tâche completed today → true
    - Profile avec 1 tâche completed il y a 7 jours exactement → true (borne inclusive)
    - Profile avec 1 tâche completed il y a 8 jours → false
    - Profile avec 1 tâche completed=true mais completedDate absent → false (Pitfall 3)
    - Profile sans tâche associée → false
    - Attribution par mentions : task.mentions includes profile.id → compté
    - Attribution par sourceFile : sourceFile contient profile.name (case-insensitive) → compté

    **describe('filterTasksForWager')** (≥5 tests)
    - Task sourceFile='01 - Enfants/Lucas/Tâches récurrentes.md' → gardée
    - Task sourceFile='02 - Maison/Tâches récurrentes.md' → gardée
    - Task sourceFile='03 - Cuisine/Liste de courses.md' → exclue
    - Task sourceFile='04 - Gamification/routines.md' → exclue
    - Task sourceFile='05 - Journal/moods.md' → exclue
    - Task sourceFile='02 - Maison/anniversaires.md' → exclue
    - Mix de 10 tâches → seules les 2 "Tâches récurrentes" sortent

    **describe('computeCumulTarget')** (≥8 tests)
    - Famille 2 adultes actifs (1.0 + 1.0) + sealeur adulte + 20 pending → cumulTarget=10
    - Famille 1 adulte (sealeur) seul actif + 10 pending → cumulTarget=10 (prorata=1.0)
    - Famille 1 adulte sealeur + 1 ado actif (0.7) + 15 pending → Math.ceil(1.0/1.7 × 15) = 9
    - Profils grossesse exclus : 2 adultes + 1 grossesse → somme = 2.0 (grossesse ignorée)
    - Diviseur=0 (aucun actif 7j + sealeur bébé) → cumulTarget = pendingCount (fallback D-04)
    - pendingCount=0 → cumulTarget=0
    - Sealeur introuvable → cumulTarget = pendingCount (fallback)
    - Prorata fractionnaire arrondi ceil : 1.0 / 3.0 × 10 = 3.33 → cumulTarget=4

    **describe('canSealWager')** (≥4 tests)
    - Adulte avec poids 1.0 → { ok: true }
    - Bébé poids 0 → { ok: false, reason: 'zero_weight' }
    - Override='bebe' sur adulte → { ok: false, reason: 'zero_weight' }
    - Profil inexistant → { ok: false, reason: 'profile_not_found' }

    **describe('shouldRecompute + maybeRecompute + validateWagerOnHarvest')** (≥8 tests)
    - shouldRecompute : lastRecomputeDate === today → false
    - shouldRecompute : lastRecomputeDate < today (jour passé) → true (catchup)
    - shouldRecompute : lastRecomputeDate > today (défensif, jamais en prod) → false
    - shouldRecompute : lastRecomputeDate vide → true
    - maybeRecompute : shouldRecompute=false → { recomputed: false }
    - maybeRecompute : shouldRecompute=true → { recomputed: true, result, newRecomputeDate }
    - validateWagerOnHarvest : cumulCurrent >= cumulTarget → won=true
    - validateWagerOnHarvest : cumulCurrent < cumulTarget → won=false
    - validateWagerOnHarvest : cumulTarget===0 → won=true (pari auto-gagné D-04)
  </behavior>
  <action>
    1. Créer `lib/__tests__/wager-engine.test.ts` avec la structure `describe` en 7 blocs listée en `<behavior>`.
    2. Imports en tête :
       ```typescript
       import {
         computeAgeCategory, resolveWeight, isProfileActive7d, filterTasksForWager,
         computeCumulTarget, canSealWager, shouldRecompute, maybeRecompute,
         validateWagerOnHarvest, yearsDiff, WEIGHT_BY_CATEGORY,
       } from '../mascot/wager-engine';
       import type { Profile, Task } from '../types';
       ```
    3. Helper factories inline en tête du fichier :
       ```typescript
       const makeProfile = (overrides: Partial<Profile> = {}): Profile => ({
         id: 'lucas_adulte', name: 'Lucas', role: 'adulte', avatar: '👨',
         mascotDecorations: [], mascotInhabitants: [], mascotPlacements: {},
         points: 0, coins: 0, level: 1, streak: 0, lootBoxesAvailable: 0,
         multiplier: 1, multiplierRemaining: 0, pityCounter: 0,
         ...overrides,
       });
       const makeTask = (overrides: Partial<Task> = {}): Task => ({
         id: 't1', text: 'test', completed: false, mentions: [], sourceFile: '', ...overrides,
       } as Task);
       ```
       (adapter les champs obligatoires au Task actuel — check lib/types.ts)
    4. IDs utilisés : `lucas_adulte`, `emma_adulte`, `sofia_enfant`, `noah_ado`, `bebe_leo` (privacy conv).
    5. Pour tester `resolveWeight` fallback console.warn : utiliser `jest.spyOn(console, 'warn').mockImplementation(() => {})` + restore.
    6. Pour computeCumulTarget, tester les cas bornes de l'énumération ci-dessus en vérifiant les champs `cumulTarget`, `activeProfileIds`, `familyWeightSum` un par un (pas juste le snapshot `toEqual` complet — plus lisible en cas d'échec).
    7. Total tests attendus : ≥45 (8+6+7+6+8+4+8+ edge cases).
  </action>
  <verify>
    <automated>npx jest --no-coverage lib/__tests__/wager-engine.test.ts --verbose 2>&1 | tail -20 && npx tsc --noEmit 2>&1 | grep "error TS" | grep -v "MemoryEditor.tsx\|cooklang.ts\|useVault.ts" && exit 1 || exit 0</automated>
  </verify>
  <acceptance_criteria>
    - Fichier `lib/__tests__/wager-engine.test.ts` existe avec ≥350 lignes
    - `grep -c "describe(" lib/__tests__/wager-engine.test.ts` retourne ≥7
    - `grep -c "it(\|test(" lib/__tests__/wager-engine.test.ts` retourne ≥45
    - `npx jest --no-coverage lib/__tests__/wager-engine.test.ts` — 100% pass, ≥45 tests
    - `npx tsc --noEmit` clean hors pré-existant
    - Aucun `fs`, aucun `readFile`, aucun `vault.` — pure fonctions uniquement
    - Aucun nom réel (grep insensitive pour noms propres non-Lucas/Emma/Sofia/Noah/Leo)
  </acceptance_criteria>
  <done>
    Suite Jest verte couvrant SPOR-03/04/05/06/13 — moteur validé, prêt pour consommation Phase 40.
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` clean (hors MemoryEditor.tsx, cooklang.ts, useVault.ts pré-existants)
- `npx jest --no-coverage lib/__tests__/wager-engine.test.ts lib/__tests__/snapshots-parser.test.ts lib/__tests__/famille-weight-override.test.ts` — tous passent (≥61 tests au total Phase 39)
- Global Jest : `npx jest --no-coverage` reste clean (pas de régression)
- `grep "new Date()" lib/mascot/wager-engine.ts` retourne 0 (pureté stricte)
- Aucune nouvelle dépendance npm ajoutée
- Aucun import du moteur depuis hooks/ ou components/ (Phase 40 s'en chargera)
</verification>

<success_criteria>
1. `lib/mascot/wager-engine.ts` livré avec 9 fonctions pures exportées + constantes WEIGHT_BY_CATEGORY + types résultats discriminés
2. Couverture formule prorata D-04 (divide-by-zero + sealeur manquant) testée
3. Bornes D-02 (brackets âge) vérifiées sur valeurs limites (2/3, 5/6, 12/13, 17/18)
4. Override D-03 prioritaire confirmé par tests
5. Filtre Tasks SPOR-06 exclut Courses/Repas/Routines/Anniversaires/Notes/Moods testé
6. Fenêtre 7j SPOR-05 bornes inclusives testées
7. Recompute D-05/D-06 : catchup jour suivant + no-op même jour testés
8. Suite Jest ≥45 tests verte, TypeScript clean
</success_criteria>

<output>
Après completion, créer `.planning/phases/39-moteur-prorata-calcul-famille/39-02-SUMMARY.md` :
- Liste exacte des exports `wager-engine.ts` (signatures copiées)
- Tableau matrice test × requirement (quels tests couvrent SPOR-03, -04, -05, -06)
- Décisions d'implémentation (attribution tâche→profil par mentions+name+sourceFile triple check)
- Contrat de consommation Phase 40 (inputs/outputs de `maybeRecompute`, à câbler dans `useFarm` ou `useGarden`)
</output>
