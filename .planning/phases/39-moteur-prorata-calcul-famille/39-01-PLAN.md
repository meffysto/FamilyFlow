---
phase: 39-moteur-prorata-calcul-famille
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/types.ts
  - lib/parser.ts
  - lib/village/parser.ts
  - lib/__tests__/famille-weight-override.test.ts
  - lib/__tests__/snapshots-parser.test.ts
autonomous: true
requirements: [SPOR-04, SPOR-13]
must_haves:
  truths:
    - "Profile supporte weight_override et round-trip famille.md le préserve (absent si non défini)"
    - "jardin-familial.md peut stocker une section ## Snapshots (parseSnapshots / appendSnapshot / pruneSnapshots pures)"
    - "Round-trip CSV snapshot date:pending:ids1|ids2 lossless, rétention 14 jours fonctionne"
    - "Backward compat : profils sans weight_override restent valides ; jardin-familial.md sans ## Snapshots reste parsable"
    - "Suite Jest Phase 39 fondations passe (npx jest lib/__tests__/famille-weight-override lib/__tests__/snapshots-parser)"
  artifacts:
    - path: lib/types.ts
      provides: "champ optionnel Profile.weight_override: WagerAgeCategory"
      contains: "weight_override?"
    - path: lib/parser.ts
      provides: "parseFamille lit weight_override, serializeFamille écrit weight_override si défini"
      contains: "weight_override"
    - path: lib/village/parser.ts
      provides: "parseSnapshots, appendSnapshot, pruneSnapshots (append-only ## Snapshots)"
      contains: "parseSnapshots"
    - path: lib/__tests__/famille-weight-override.test.ts
      provides: "round-trip parse/serialize weight_override + backward-compat profil legacy"
      min_lines: 40
    - path: lib/__tests__/snapshots-parser.test.ts
      provides: "round-trip snapshot CSV + rétention 14j + section absente + pitfall 4 (pas d'append fin de fichier)"
      min_lines: 60
  key_links:
    - from: "lib/parser.ts parseFamille"
      to: "lib/types.ts Profile.weight_override"
      via: "lecture currentProps.weight_override + cast sur 5 valeurs valides"
      pattern: "weight_override"
    - from: "lib/village/parser.ts appendSnapshot"
      to: "section ## Snapshots dans jardin-familial.md"
      via: "pattern identique appendBuilding — jamais append en fin si section suivante existe"
      pattern: "## Snapshots"
---

<objective>
Étendre les fondations data pour Phase 39 : (1) ajouter `weight_override` au type `Profile` et au parser/serializer `famille.md` (D-03) ; (2) livrer les fonctions pures `parseSnapshots` / `appendSnapshot` / `pruneSnapshots` dans `lib/village/parser.ts` pour gérer la section `## Snapshots` append-only dans `jardin-familial.md` (D-01). Zéro UI, zéro hook, zéro moteur de calcul ici — ce sont les primitives data que le moteur pur du Plan 02 et les hooks de la Phase 40 consommeront.

Purpose: Isoler les modifications de shape (Profile étendu + nouvelle section vault) dans un plan unique validable indépendamment — évite de mélanger extension de type et logique de calcul.
Output: `Profile.weight_override` stable bidirectionnel + trio de fonctions pures `## Snapshots` testées.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/39-moteur-prorata-calcul-famille/39-CONTEXT.md
@.planning/phases/39-moteur-prorata-calcul-famille/39-RESEARCH.md
@lib/types.ts
@lib/parser.ts
@lib/village/parser.ts
@lib/__tests__/sporee-economy.test.ts

<interfaces>
<!-- Contrats existants à respecter — extraits du codebase, pas de scavenger hunt -->

Profile (lib/types.ts:67-118) — champs existants pertinents :
```typescript
export interface Profile {
  id: string;               // snake_case
  name: string;
  role: 'enfant' | 'ado' | 'adulte';
  birthdate?: string;       // YYYY-MM-DD ou YYYY
  statut?: 'grossesse' | 'ne';
  // ... autres champs farm/mascot/food
}
```

serializeFamille (lib/parser.ts:907-944) — pattern conditionnel :
```typescript
if (profile.birthdate) lines.push(`birthdate: ${profile.birthdate}`);
if (profile.statut) lines.push(`statut: ${profile.statut}`);
```

parseFamille (lib/parser.ts:~850) — lecture via currentProps :
```typescript
statut: statut,
dateTerme: currentProps.dateTerme,
```

appendBuilding (lib/village/parser.ts:472) — pattern append-only à reproduire :
- Trouve la section `## Constructions` via regex
- Insère AVANT la prochaine section `## `
- Si absente, crée avant `## Historique` ou en fin de fichier
- Idempotence stricte (regex existingLineRegex)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Étendre Profile.weight_override + parseFamille/serializeFamille bidirectionnel</name>
  <files>lib/types.ts, lib/parser.ts, lib/__tests__/famille-weight-override.test.ts</files>
  <read_first>
    - lib/types.ts (lignes 67-118 — interface Profile complète, emplacement exact du champ à ajouter)
    - lib/parser.ts (lignes 820-900 — fonction parseFamille, bloc flush() avec currentProps)
    - lib/parser.ts (lignes 907-945 — fonction serializeFamille, pattern conditionnel `if (profile.X)`)
    - .planning/phases/39-moteur-prorata-calcul-famille/39-CONTEXT.md §D-03
    - .planning/phases/39-moteur-prorata-calcul-famille/39-RESEARCH.md §"Open Questions" point 2
  </read_first>
  <behavior>
    - Test 1 : parse profil avec `weight_override: adulte` → Profile.weight_override === 'adulte'
    - Test 2 : parse profil SANS weight_override → Profile.weight_override === undefined (backward-compat)
    - Test 3 : parse profil avec valeur invalide `weight_override: bogus` → ignoré (undefined), pas de throw
    - Test 4 : serialize profil avec weight_override='enfant' → ligne `weight_override: enfant` présente
    - Test 5 : serialize profil SANS weight_override → AUCUNE ligne `weight_override` (cohérent avec pattern conditionnel existant)
    - Test 6 : round-trip parse(serialize(parse(input))) === parse(input) sur les 5 valeurs valides
  </behavior>
  <action>
    1. Dans `lib/types.ts`, ajouter le type `WagerAgeCategory` exporté (AVANT l'interface Profile) :
       ```typescript
       /** Catégorie d'âge utilisée pour pondérer les profils dans le moteur Sporée (Phase 39) */
       export type WagerAgeCategory = 'adulte' | 'ado' | 'enfant' | 'jeune' | 'bebe';
       ```
    2. Dans `lib/types.ts`, ajouter le champ optionnel dans l'interface `Profile` (après `statut?` ligne 76, avant `dateTerme?`) :
       ```typescript
       weight_override?: WagerAgeCategory; // Phase 39 — override poids pondération Sporée
       ```
    3. Dans `lib/parser.ts`, étendre `parseFamille` (dans le bloc `flush()` ~ligne 850, dans l'objet poussé via profiles.push) :
       - Valider via whitelist : `const validOverrides = ['adulte', 'ado', 'enfant', 'jeune', 'bebe'] as const;`
       - Ajouter après la ligne `statut,` : `weight_override: (validOverrides as readonly string[]).includes(currentProps.weight_override) ? (currentProps.weight_override as WagerAgeCategory) : undefined,`
       - Import `WagerAgeCategory` depuis `./types` en haut de parser.ts si pas déjà importé (grep d'abord)
    4. Dans `lib/parser.ts` `serializeFamille` (~ligne 920), ajouter APRÈS `if (profile.dateTerme) ...` :
       ```typescript
       if (profile.weight_override) lines.push(`weight_override: ${profile.weight_override}`);
       ```
    5. Créer `lib/__tests__/famille-weight-override.test.ts` :
       - `describe('parseFamille weight_override')` avec les 6 tests ci-dessus
       - Utiliser des fixtures inline (string multiline) — PAS de fs
       - IDs profils : `lucas_adulte`, `emma_adulte` (privacy convention, RESEARCH §Project Constraints)
       - Format fixture minimal : `### lucas_adulte\nname: Lucas\nrole: adulte\navatar: 👨\nweight_override: adulte\n`
    6. Aucune modification de PlantedCrop, vault, ou autre module. Zéro couplage.
  </action>
  <verify>
    <automated>npx jest --no-coverage lib/__tests__/famille-weight-override.test.ts && npx tsc --noEmit 2>&1 | grep -v "MemoryEditor.tsx\|cooklang.ts\|useVault.ts" | grep "error TS" && exit 1 || exit 0</automated>
  </verify>
  <acceptance_criteria>
    - `grep "weight_override?: WagerAgeCategory" lib/types.ts` retourne 1 match
    - `grep "export type WagerAgeCategory" lib/types.ts` retourne 1 match
    - `grep "weight_override:" lib/parser.ts` retourne ≥2 matches (parse + serialize)
    - `npx jest --no-coverage lib/__tests__/famille-weight-override.test.ts` — 6/6 tests pass
    - `npx tsc --noEmit` — aucune nouvelle erreur (erreurs pré-existantes MemoryEditor/cooklang/useVault ignorées)
    - Aucun ajout d'import externe (pas de date-fns, etc.)
  </acceptance_criteria>
  <done>
    Profile.weight_override round-trip préservé, suite Jest verte, TypeScript clean hors pré-existant.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: parseSnapshots + appendSnapshot + pruneSnapshots dans lib/village/parser.ts</name>
  <files>lib/village/parser.ts, lib/__tests__/snapshots-parser.test.ts</files>
  <read_first>
    - lib/village/parser.ts (lignes 460-540 — fonction appendBuilding complète, pattern append-only à répliquer exactement)
    - lib/village/parser.ts (lignes 300-320 — section `## Constructions` génération dans serializeGardenFile pour référence)
    - .planning/phases/39-moteur-prorata-calcul-famille/39-CONTEXT.md §D-01
    - .planning/phases/39-moteur-prorata-calcul-famille/39-RESEARCH.md §"Pattern 5" et §"Pitfall 4"
  </read_first>
  <behavior>
    - Test 1 : parseSnapshots('') → {} (objet vide)
    - Test 2 : parseSnapshots(content sans ## Snapshots) → {} (absence = objet vide, pas de throw)
    - Test 3 : parseSnapshots avec 3 lignes `YYYY-MM-DD:pending:ids` → dictionnaire clé = date, valeur = {pending, activeProfileIds}
    - Test 4 : parseSnapshots ignore lignes mal formées (ex: `2026-04-18:abc:ids`, `incomplete`, `## Snapshots` lui-même)
    - Test 5 : appendSnapshot(content, {date, pending, activeProfileIds}) sur content sans ## Snapshots → crée la section AVANT ## Historique si présente, sinon en fin
    - Test 6 : appendSnapshot idempotent — appel avec la même date 2 fois laisse une seule ligne (remplace, pas duplique)
    - Test 7 : appendSnapshot sur content avec section ## Snapshots existante + autre section après → insère AVANT la prochaine `## `, jamais en fin (Pitfall 4)
    - Test 8 : pruneSnapshots({today, maxDays: 14}) supprime les entrées > 14 jours de today (strict), conserve exactement 14 plus récentes
    - Test 9 : round-trip — appendSnapshot puis parseSnapshots restitue le même objet (clé `2026-04-18` → `{pending: 5, activeProfileIds: ['lucas_adulte', 'emma_adulte']}`)
    - Test 10 : activeProfileIds vide → ligne `YYYY-MM-DD:5:` (séparateur ids absent mais `:` final présent), parse restitue tableau []
  </behavior>
  <action>
    1. Dans `lib/village/parser.ts`, AJOUTER APRÈS `appendBuilding` (après la ligne ~540) :
    2. Définir le type exporté (en haut du fichier si interfaces présentes, sinon inline près des fonctions) :
       ```typescript
       export interface FamilySnapshot {
         date: string;              // YYYY-MM-DD local
         pending: number;           // nombre de tâches pending au matin
         activeProfileIds: string[]; // profils actifs 7j au moment du snapshot
       }
       ```
    3. Implémenter `parseSnapshots(content: string): Record<string, FamilySnapshot>` :
       - Regex pour extraire la section : `/## Snapshots\s*\n([\s\S]*?)(?=\n## |\n*$)/`
       - Si pas de match → retourner `{}`
       - Pour chaque ligne du groupe 1, parser format `YYYY-MM-DD:pending:id1|id2|...`
       - Split par `:` (maxsplit 3 — attention : date contient `-`, pending est entier, ids joinés par `|`)
       - Utiliser regex de validation : `/^(\d{4}-\d{2}-\d{2}):(\d+):(.*)$/`
       - Si ids vide → `activeProfileIds: []` ; sinon `split('|').filter(Boolean)`
       - Ignorer silencieusement les lignes non conformes (pas de throw)
    4. Implémenter `appendSnapshot(content: string, snapshot: FamilySnapshot): string` :
       - Reproduire EXACTEMENT le pattern `appendBuilding` (lignes 472-540)
       - Idempotence : si une ligne pour `snapshot.date` existe déjà, la REMPLACER (différent de appendBuilding qui skip — car un snapshot du jour doit pouvoir être rafraîchi au premier recompute matinal)
       - Format ligne : `${date}:${pending}:${activeProfileIds.join('|')}`
       - Insérer AVANT la prochaine section `## ` (jamais en fin si section suivante existe — Pitfall 4)
       - Créer section avant `## Historique` si absent, sinon en fin de fichier
    5. Implémenter `pruneSnapshots(snapshots: Record<string, FamilySnapshot>, today: string, maxDays: number = 14): Record<string, FamilySnapshot>` :
       - Calculer `cutoffMs = new Date(today + 'T00:00:00').getTime() - (maxDays - 1) * 86400000`
       - Filtrer les entrées où `new Date(date + 'T00:00:00').getTime() >= cutoffMs`
       - Retourner nouvel objet (immutabilité — ne pas muter l'entrée)
    6. Créer `lib/__tests__/snapshots-parser.test.ts` avec les 10 tests ci-dessus, organisés en `describe('parseSnapshots')`, `describe('appendSnapshot')`, `describe('pruneSnapshots')`, `describe('round-trip')`.
    7. IDs de test : `lucas_adulte`, `emma_adulte`, `papa_adulte` (privacy conv).
  </action>
  <verify>
    <automated>npx jest --no-coverage lib/__tests__/snapshots-parser.test.ts && npx tsc --noEmit 2>&1 | grep "error TS" | grep -v "MemoryEditor.tsx\|cooklang.ts\|useVault.ts" && exit 1 || exit 0</automated>
  </verify>
  <acceptance_criteria>
    - `grep "export function parseSnapshots" lib/village/parser.ts` retourne 1 match
    - `grep "export function appendSnapshot" lib/village/parser.ts` retourne 1 match
    - `grep "export function pruneSnapshots" lib/village/parser.ts` retourne 1 match
    - `grep "export interface FamilySnapshot" lib/village/parser.ts` retourne 1 match
    - `grep "## Snapshots" lib/village/parser.ts` retourne ≥2 matches (regex + création section)
    - `npx jest --no-coverage lib/__tests__/snapshots-parser.test.ts` — 10/10 tests pass
    - `npx tsc --noEmit` — aucune nouvelle erreur
    - Aucun import externe ajouté (pas de date-fns — utiliser `new Date(...).getTime()` inline)
    - Fonctions pures : pas de `await`, pas de `vault.readFile`, pas de `FileSystem`
  </acceptance_criteria>
  <done>
    Trio de fonctions snapshot livrées, testées, pures, prêtes pour consommation par moteur (Plan 02) et hooks (Phase 40).
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` clean hors erreurs pré-existantes
- `npx jest --no-coverage lib/__tests__/famille-weight-override.test.ts lib/__tests__/snapshots-parser.test.ts` — 16/16 tests pass
- `grep "weight_override" lib/types.ts lib/parser.ts` retourne ≥3 matches
- `grep "FamilySnapshot\|parseSnapshots\|appendSnapshot\|pruneSnapshots" lib/village/parser.ts` retourne ≥8 matches
- Aucun ajout de dépendance npm (check : `git diff package.json` vide)
- Aucune modification de fichiers hors scope (useVault, hooks/, components/, app/)
</verification>

<success_criteria>
1. Profile supporte `weight_override?: WagerAgeCategory` avec round-trip lossless famille.md
2. `lib/village/parser.ts` exporte `FamilySnapshot`, `parseSnapshots`, `appendSnapshot`, `pruneSnapshots` purs et testés
3. Backward compat : profils et jardin-familial.md legacy restent parsables (tests dédiés verts)
4. Suite Jest Phase 39 fondations : 16/16 tests pass
5. TypeScript clean (hors pré-existant)
</success_criteria>

<output>
Après completion, créer `.planning/phases/39-moteur-prorata-calcul-famille/39-01-SUMMARY.md` résumant :
- Signatures exactes exposées (copier la declaration TypeScript finale)
- Exemples round-trip parse/serialize pour weight_override et snapshot
- Choix d'implémentation (idempotence snapshot replace vs skip, format ligne)
- Connection vers Plan 02 (moteur consommera FamilySnapshot type)
</output>
