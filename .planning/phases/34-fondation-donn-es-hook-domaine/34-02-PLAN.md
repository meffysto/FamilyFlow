---
phase: 34-fondation-donn-es-hook-domaine
plan: 02
type: tdd
wave: 2
depends_on: ["34-01"]
files_modified:
  - lib/__tests__/parser-lovenotes.test.ts
autonomous: true
requirements: [LOVE-17]
must_haves:
  truths:
    - "parseLoveNote retourne null si un champ frontmatter requis manque"
    - "parseLoveNote retourne null si status est hors de l'enum 'pending' | 'revealed' | 'read'"
    - "serializeLoveNote produit un frontmatter YAML valide avec delimitateurs triple-tiret"
    - "serializeLoveNote n'emet readAt: que si note.readAt est defini (pas de 'undefined' literal)"
    - "Round-trip parseLoveNote(path, serializeLoveNote(data)) === { ...data, sourceFile: path } — loss-less"
    - "loveNoteFileName('2026-04-16T14:32:17') produit un .md deterministe"
    - "Deux createdAt differant d'1 ms produisent deux filenames differents (collision-safe)"
    - "loveNotePath(to, createdAt) produit '03 - Famille/LoveNotes/{to}/{slug}.md'"
  artifacts:
    - path: "lib/__tests__/parser-lovenotes.test.ts"
      provides: "Suite Jest couvrant parseLoveNote, serializeLoveNote, round-trip, helpers path, listing par destinataire"
      min_lines: 180
  key_links:
    - from: "lib/__tests__/parser-lovenotes.test.ts"
      to: "lib/parser.ts"
      via: "import parseLoveNote, serializeLoveNote, loveNoteFileName, loveNotePath, LOVENOTES_DIR"
      pattern: "import.*from.*parser"
    - from: "lib/__tests__/parser-lovenotes.test.ts"
      to: "lib/types.ts"
      via: "import type LoveNote"
      pattern: "import type.*LoveNote"
---

<objective>
Creer la suite Jest `lib/__tests__/parser-lovenotes.test.ts` couvrant integralement LOVE-17 : parse/serialize roundtrip, gestion frontmatter invalide, listing par destinataire, helpers path (loveNoteFileName collision-safe + loveNotePath).

Purpose: Valider la fiabilite du parser LoveNote avant que Plan 03 (hook useVaultLoveNotes) ne le consomme en production. Detecter les regressions sur le round-trip, le format frontmatter YAML, et les champs optionnels (readAt).
Output: `lib/__tests__/parser-lovenotes.test.ts` avec >=16 tests couvrant les cas normaux, edge cases, et tous les pitfalls identifies en RESEARCH.md (matter.stringify corruption, 'undefined' literal, slug collision, etc.).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/34-fondation-donn-es-hook-domaine/34-RESEARCH.md
@.planning/phases/34-fondation-donn-es-hook-domaine/34-01-SUMMARY.md

@lib/types.ts
@lib/parser.ts
@lib/__tests__/village-parser.test.ts

<interfaces>
<!-- Types crees en Plan 01 — lib/types.ts -->
From lib/types.ts:
```typescript
export type LoveNoteStatus = 'pending' | 'revealed' | 'read';
export interface LoveNote {
  from: string;
  to: string;
  createdAt: string;
  revealAt: string;
  status: LoveNoteStatus;
  readAt?: string;
  body: string;
  sourceFile: string;
}
```

<!-- Parser + helpers crees en Plan 01 — lib/parser.ts -->
From lib/parser.ts:
```typescript
export const LOVENOTES_DIR = '03 - Famille/LoveNotes';
export function loveNoteFileName(createdAt: string): string;
export function loveNotePath(toProfileId: string, createdAt: string): string;
export function parseLoveNote(relativePath: string, content: string): LoveNote | null;
export function serializeLoveNote(note: Omit<LoveNote, 'sourceFile'>): string;
```

<!-- Pattern de test existant — lib/__tests__/village-parser.test.ts -->
Structure : describe/it, fixtures string literal, import direct depuis lib/, pas de mock gray-matter.
Langue commentaires : francais (convention CLAUDE.md).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Creer la suite Jest parser-lovenotes.test.ts</name>
  <files>lib/__tests__/parser-lovenotes.test.ts</files>
  <read_first>
    - lib/parser.ts (verifier les signatures exactes de parseLoveNote, serializeLoveNote, loveNoteFileName, loveNotePath, LOVENOTES_DIR — creees en Plan 01)
    - lib/types.ts (verifier la signature exacte de LoveNote et LoveNoteStatus)
    - lib/__tests__/village-parser.test.ts (pattern de structure describe/it, fixtures, convention import depuis `../parser` ou barrel)
    - lib/__tests__/parser.test.ts si existant (pattern de test parser monolithique)
    - .planning/phases/34-fondation-donn-es-hook-domaine/34-RESEARCH.md (section "Test Strategy" lignes 696-762 — 16 tests detailles avec groupes describe)
  </read_first>
  <behavior>
    **parseLoveNote — 6 tests :**
    - `parse un fichier complet valide (pending)` : frontmatter complet from/to/createdAt/revealAt/status + body, retourne LoveNote avec sourceFile egal au relativePath passe
    - `parse un fichier valide avec status=read et readAt present` : readAt est lu comme string (pas Date)
    - `retourne null si frontmatter manque from` (et similaire pour to, createdAt, revealAt, status) — verifier au moins un champ absent = null
    - `retourne null si status est invalide (ex: "unknown")` : enum strict 'pending' | 'revealed' | 'read'
    - `ignore body vide` : body = '' est permis, retourne note avec body: ''
    - `trim le body` : body avec whitespace leading/trailing est trimme (pattern parseNote existant)

    **serializeLoveNote — 4 tests :**
    - `produit un frontmatter YAML valide avec delimitateurs ---` : commence par `---\n` et contient un deuxieme `---\n` avant le body
    - `inclut tous les champs requis (from, to, createdAt, revealAt, status)` : chaque champ present dans le frontmatter avec guillemets
    - `inclut readAt uniquement si defini` : avec readAt: undefined, la chaine serializee ne contient PAS `readAt:` (Pitfall 7 — pas de 'undefined' literal)
    - `preserve le body markdown intact` : caracteres speciaux, newlines, accents francais, tous preserves

    **Round-trip parseLoveNote(path, serializeLoveNote(data)) — 2 tests :**
    - `preserve une note complete (status=read avec readAt)` : .toEqual strict apres merge du sourceFile
    - `preserve une note pending sans readAt` : readAt reste undefined apres round-trip (verifie Pitfall 2 + 7)

    **loveNoteFileName — 2 tests :**
    - `genere un nom .md deterministe depuis createdAt` : meme input -> meme output (stable)
    - `deux createdAt differants d'1ms produisent deux filenames differents` : collision-safe a la milliseconde (Pitfall 3)

    **loveNotePath — 2 tests :**
    - `construit le chemin 03 - Famille/LoveNotes/{to}/{slug}.md` : format exact avec LOVENOTES_DIR prefix
    - `respecte le toProfileId (destinataires differents -> dossiers differents)`

    **Listing par destinataire (LOVE-17) — 2 tests :**
    - `filter by recipient` : un helper inline `(notes, toId) => notes.filter(n => n.to === toId)` retourne correctement les notes pour un destinataire
    - `tri par createdAt desc` : un helper inline `[...notes].sort((a, b) => b.createdAt.localeCompare(a.createdAt))` retourne les plus recentes en premier (pattern consomme par Plan 03 loadLoveNotes)
  </behavior>
  <action>
Creer `lib/__tests__/parser-lovenotes.test.ts` avec la structure suivante. **Langue commentaires : francais** (convention CLAUDE.md).

**Imports :**
```typescript
import {
  parseLoveNote,
  serializeLoveNote,
  loveNoteFileName,
  loveNotePath,
  LOVENOTES_DIR,
} from '../parser';
import type { LoveNote } from '../types';
```

**Fixtures communes (en tete du fichier, apres imports) :**
```typescript
const FULL_LOVENOTE_FILE = `---
from: "lucas"
to: "emma"
createdAt: "2026-04-16T14:32:17"
revealAt: "2026-04-20T08:00:00"
status: "pending"
---

Joyeux anniversaire ma cherie, je t'aime.
`;

const FULL_LOVENOTE_DATA: Omit<LoveNote, 'sourceFile'> = {
  from: 'lucas',
  to: 'emma',
  createdAt: '2026-04-16T14:32:17',
  revealAt: '2026-04-20T08:00:00',
  status: 'pending',
  body: "Joyeux anniversaire ma cherie, je t'aime.",
};

const READ_LOVENOTE_DATA: Omit<LoveNote, 'sourceFile'> = {
  from: 'emma',
  to: 'lucas',
  createdAt: '2026-04-16T14:32:17',
  revealAt: '2026-04-16T14:32:17',
  status: 'read',
  readAt: '2026-04-16T15:00:00',
  body: 'Merci mon amour.',
};

const SAMPLE_RELATIVE_PATH = '03 - Famille/LoveNotes/emma/2026-04-16-abc.md';
```

**Groupes describe (16 tests minimum) :**

1. `describe('parseLoveNote')` — 6 tests utilisant FULL_LOVENOTE_FILE :
   - Cas complet pending
   - Cas complet read avec readAt
   - null si frontmatter manque `from` (creer une variante sans `from:`)
   - null si status === 'unknown'
   - body vide permis (frontmatter seul)
   - trim body (ajouter whitespace leading/trailing dans fixture variante)

2. `describe('serializeLoveNote')` — 4 tests :
   - Verifier `expect(result).toMatch(/^---\n/)` et contient `\n---\n`
   - Verifier que chaque champ YAML est present avec guillemets : `expect(result).toContain('from: "lucas"')`
   - Cas sans readAt : `expect(result).not.toContain('readAt')`
   - Body preserve : passer un body avec accents FR + newlines + caracteres speciaux, verifier match exact

3. `describe('round-trip parseLoveNote(serializeLoveNote(data))')` — 2 tests :
   - `parseLoveNote(SAMPLE_RELATIVE_PATH, serializeLoveNote(READ_LOVENOTE_DATA))` doit egal `{ ...READ_LOVENOTE_DATA, sourceFile: SAMPLE_RELATIVE_PATH }` via `.toEqual`
   - `parseLoveNote(SAMPLE_RELATIVE_PATH, serializeLoveNote(FULL_LOVENOTE_DATA))` (sans readAt) doit avoir `readAt` === undefined dans le resultat

4. `describe('loveNoteFileName')` — 2 tests :
   - `expect(loveNoteFileName('2026-04-16T14:32:17')).toMatch(/^2026-04-16-[a-z0-9]+\.md$/)` — format deterministe
   - Deux appels avec createdAt differants d'1ms produisent deux filenames distincts : `expect(loveNoteFileName('2026-04-16T14:32:17.123')).not.toEqual(loveNoteFileName('2026-04-16T14:32:17.124'))`

5. `describe('loveNotePath')` — 2 tests :
   - `expect(loveNotePath('emma', '2026-04-16T14:32:17')).toMatch(/^03 - Famille\/LoveNotes\/emma\/2026-04-16-.+\.md$/)` — format exact avec LOVENOTES_DIR prefix
   - `loveNotePath('emma', t) !== loveNotePath('lucas', t)` pour le meme t — dossier par destinataire

6. `describe('listing par destinataire (LOVE-17)')` — 2 tests :
   - Creer un tableau `notes: LoveNote[]` avec 3 notes vers 2 destinataires, filter par `to === 'emma'` retourne les bonnes notes
   - Meme tableau, trie par `createdAt` desc via `[...notes].sort((a, b) => b.createdAt.localeCompare(a.createdAt))`, verifier que la plus recente est en premier

**IMPORTANT — NE PAS :**
- Mocker `gray-matter` ni `parseFrontmatter` (test parser reel)
- Mocker `VaultManager` (tests purs parser, pas de file I/O)
- Utiliser `matter.stringify` dans les fixtures
- Utiliser `title` en frontmatter (pas de titre)
- Ecrire les commentaires en anglais (convention CLAUDE.md : francais)
- Utiliser des prenoms reels (convention CLAUDE.md : Lucas, Emma, Dupont uniquement dans le repo public)

**Verification a l'ecriture** : Apres ecriture, lancer `npx jest lib/__tests__/parser-lovenotes.test.ts --no-coverage` et confirmer 16+ tests passing, 0 failures.
  </action>
  <verify>
    <automated>cd /Users/gabrielwaltio/Documents/family-vault && npx jest lib/__tests__/parser-lovenotes.test.ts --no-coverage 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `lib/__tests__/parser-lovenotes.test.ts` existe avec au moins 180 lignes
    - Le fichier importe `parseLoveNote, serializeLoveNote, loveNoteFileName, loveNotePath, LOVENOTES_DIR` depuis `'../parser'`
    - Le fichier importe `import type { LoveNote } from '../types'`
    - Le fichier contient 6 describe : `parseLoveNote`, `serializeLoveNote`, `round-trip`, `loveNoteFileName`, `loveNotePath`, `listing par destinataire`
    - Le fichier contient au minimum 16 tests (grep `^\s*(it|test)\(` retourne >= 16)
    - Le test round-trip contient `.toEqual` (verification stricte, pas juste `.toBe`)
    - Le test 'pas de readAt' contient `.not.toContain('readAt')`
    - Le test 'collision-safe' contient `.not.toEqual(` sur deux loveNoteFileName avec timestamps differants d'1ms
    - `npx jest lib/__tests__/parser-lovenotes.test.ts --no-coverage` passe avec 0 failures
    - Aucun prenom reel dans les fixtures (grep les noms communs comme 'Gabriel', 'Claire', etc. retourne 0 ligne — seulement Lucas/Emma/Dupont autorises)
    - Aucun appel a matter.stringify dans les fixtures (grep 'matter.stringify' retourne 0 ligne)
  </acceptance_criteria>
  <done>Suite Jest complete couvrant LOVE-17 : parse/serialize roundtrip loss-less, gestion frontmatter invalide (null si champ manquant ou status hors enum), helpers path deterministes et collision-safe, listing par destinataire. Tous les tests passent. Le parser LoveNote est valide pour consommation par Plan 03 (hook) et phases ulterieures.</done>
</task>

</tasks>

<verification>
1. `npx jest lib/__tests__/parser-lovenotes.test.ts --no-coverage` — tous les tests passent (0 failures)
2. `npx tsc --noEmit` — pas de nouvelles erreurs
3. `grep -c "^\s*\(it\|test\)(" lib/__tests__/parser-lovenotes.test.ts` retourne >= 16
4. `grep -c "describe(" lib/__tests__/parser-lovenotes.test.ts` retourne 6
5. Aucun mock de gray-matter dans le fichier test
</verification>

<success_criteria>
- 16+ tests unitaires couvrant parseLoveNote, serializeLoveNote, round-trip, loveNoteFileName, loveNotePath, listing par destinataire
- Round-trip fidelity verifiee (toEqual strict)
- Pitfall 2 (matter.stringify) protege par round-trip test
- Pitfall 3 (slug collision) protege par test collision-safe 1ms
- Pitfall 7 (readAt 'undefined' literal) protege par test .not.toContain('readAt')
- Tous les tests passent avec 0 failures
- Fichier en francais (commentaires, strings descriptives)
- Fixtures respectent privacy (Lucas/Emma uniquement)
</success_criteria>

<output>
After completion, create `.planning/phases/34-fondation-donn-es-hook-domaine/34-02-SUMMARY.md`
</output>
