---
phase: 34-fondation-donn-es-hook-domaine
plan: 03
type: execute
wave: 2
depends_on: ["34-01"]
files_modified:
  - hooks/useVaultLoveNotes.ts
  - hooks/useVault.ts
autonomous: true
requirements: [LOVE-03, LOVE-04]
must_haves:
  truths:
    - "hooks/useVaultLoveNotes.ts exporte useVaultLoveNotes(vaultRef) avec CRUD complet"
    - "useVaultLoveNotes expose loveNotes, loadLoveNotes, setLoveNotes, addLoveNote, updateLoveNoteStatus, deleteLoveNote, resetLoveNotes"
    - "addLoveNote cree le dossier destinataire via ensureDir puis ecrit le fichier via writeFile"
    - "loadLoveNotes scanne LOVENOTES_DIR recursivement et filter-out les null (frontmatter invalide)"
    - "useVault().loveNotes est expose dans VaultState et propage via VaultContext (pattern 21 hooks domaine)"
    - "Le cache hydrate setLoveNotes(cached.loveNotes) au boot (pattern secretMissions)"
    - "saveCache recoit loveNotes dans son payload (persistance cross-boot)"
    - "useVault.ts grandit de <=15 lignes"
  artifacts:
    - path: "hooks/useVaultLoveNotes.ts"
      provides: "Hook domaine CRUD Love Notes (pattern useVaultNotes)"
      exports: ["useVaultLoveNotes", "UseVaultLoveNotesResult"]
      min_lines: 100
    - path: "hooks/useVault.ts"
      provides: "loveNotesHook instancie + cablage VaultState + loadVaultData + cache hydrate + saveCache payload + return useMemo"
      contains: "loveNotesHook"
  key_links:
    - from: "hooks/useVaultLoveNotes.ts"
      to: "lib/parser.ts"
      via: "import LOVENOTES_DIR, parseLoveNote, serializeLoveNote, loveNotePath"
      pattern: "import.*from.*parser"
    - from: "hooks/useVaultLoveNotes.ts"
      to: "lib/types.ts"
      via: "import type LoveNote, LoveNoteStatus"
      pattern: "import type.*LoveNote"
    - from: "hooks/useVault.ts"
      to: "hooks/useVaultLoveNotes.ts"
      via: "import useVaultLoveNotes + instanciation sous-hook + cablage"
      pattern: "useVaultLoveNotes"
    - from: "hooks/useVault.ts"
      to: "lib/vault-cache.ts"
      via: "saveCache payload contient loveNotes, hydrateFromCache lit cached.loveNotes"
      pattern: "cached.loveNotes"
---

<objective>
Creer le hook domaine `hooks/useVaultLoveNotes.ts` (pattern exact `useVaultNotes`) et cabler integralement dans `hooks/useVault.ts` : instanciation sous-hook + VaultState + loadVaultData [23] + hydrate cache + saveCache payload + return useMemo.

Purpose: Exposer `useVault().loveNotes` (LOVE-03) consommable depuis n'importe quel composant via `useVault()`. Faire persister les love notes dans le cache pour survie cross-boot sans re-parse (LOVE-04). Fondation hook complete prete pour Phase 35 (UI dashboard) et Phase 36 (composition/reveal).
Output: 1 nouveau fichier `hooks/useVaultLoveNotes.ts` (~130 lignes) + `hooks/useVault.ts` modifie en 7 points precis (~15 lignes ajoutees).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-fondation-donn-es-hook-domaine/34-RESEARCH.md
@.planning/phases/34-fondation-donn-es-hook-domaine/34-01-SUMMARY.md

@hooks/useVaultNotes.ts
@hooks/useVault.ts
@lib/vault.ts
@contexts/VaultContext.tsx

<interfaces>
<!-- Types + helpers crees en Plan 01 — disponibles -->
From lib/types.ts:
```typescript
export type LoveNoteStatus = 'pending' | 'revealed' | 'read';
export interface LoveNote {
  from: string; to: string;
  createdAt: string; revealAt: string;
  status: LoveNoteStatus; readAt?: string;
  body: string; sourceFile: string;
}
```

From lib/parser.ts:
```typescript
export const LOVENOTES_DIR = '03 - Famille/LoveNotes';
export function parseLoveNote(relativePath: string, content: string): LoveNote | null;
export function serializeLoveNote(note: Omit<LoveNote, 'sourceFile'>): string;
export function loveNotePath(toProfileId: string, createdAt: string): string;
```

<!-- Pattern hook a repliquer — hooks/useVaultNotes.ts (116 lignes) -->
Structure : useState<T[]>, resetX useCallback, loadX(vault) useCallback avec ensureDir + listFilesRecursive + Promise.all + parse + filter non-null + sort, addX/updateX/deleteX useCallback avec vaultRef.current, return object.

<!-- VaultManager API — lib/vault.ts -->
From lib/vault.ts:
```typescript
export class VaultManager {
  readFile(path: string): Promise<string>;
  writeFile(path: string, content: string): Promise<void>;
  deleteFile(path: string): Promise<void>;
  ensureDir(path: string): Promise<void>;
  exists(path: string): Promise<boolean>;
  listFilesRecursive(path: string, extension?: string): Promise<string[]>;
}
```

<!-- Lignes precises a modifier dans useVault.ts (verifie par grep — voir RESEARCH.md "Pattern 4: Cablage") -->
Point 1 — Import sous-hook : ligne ~90-95 (section imports useVaultX) — actuellement `import { useVaultStories } from './useVaultStories';` ligne 28 et `import { useVaultNotes } from './useVaultNotes';` ligne 90. Ajouter `import { useVaultLoveNotes } from './useVaultLoveNotes';` dans ce groupe.

Point 2 — Instanciation sous-hook : ligne ~585 apres `const storiesHook = useVaultStories(vaultRef);` — ajouter `const loveNotesHook = useVaultLoveNotes(vaultRef);` AVANT `const [gardenRaw, setGardenRaw] = useState<string>('');` ligne 588.

Point 3 — Interface VaultState : ligne ~286-287 (apres `gardenRaw: string; setGardenRaw: ...;`). L'interface VaultState commence vers ligne 128+. Chercher `stories:` vers ligne 288 pour positionnement. Ajouter apres gardenRaw et AVANT `stories:` :
```typescript
loveNotes: LoveNote[];
addLoveNote: (note: Omit<LoveNote, 'sourceFile'>) => Promise<void>;
updateLoveNoteStatus: (sourceFile: string, status: LoveNoteStatus, readAt?: string) => Promise<void>;
deleteLoveNote: (sourceFile: string) => Promise<void>;
```
Et ajouter `LoveNote, LoveNoteStatus` a l'import existant de `../lib/types`.

Point 4 — Hydrate cache : ligne ~685 apres `missionsHook.setSecretMissions(cached.secretMissions);` — ajouter `loveNotesHook.setLoveNotes(cached.loveNotes);`.

Point 5 — loadVaultData Promise.allSettled : apres ligne 1211 `storiesHook.loadStories(...)` (element [22]). Ajouter :
```typescript
// [23] Love Notes — 1 fichier par note classe par destinataire (Phase 34)
loveNotesHook.loadLoveNotes(vault).catch(() => [] as LoveNote[]),
```
Puis dans le traitement des resultats (apres ligne 1287 `storiesHook.setStories(val(results[22]...`)) :
```typescript
loveNotesHook.setLoveNotes(val(results[23] as PromiseSettledResult<LoveNote[]>, []));
```

Point 6 — saveCache payload : ligne ~1321 (apres `secretMissions: val(results[20], []) as Task[],` et AVANT `}).catch...`). Ajouter :
```typescript
loveNotes: val(results[23] as PromiseSettledResult<LoveNote[]>, []) as LoveNote[],
```

Point 7 — Return useMemo : ligne ~1849-1850 (apres `setGardenRaw,`). Ajouter :
```typescript
loveNotes: loveNotesHook.loveNotes,
addLoveNote: loveNotesHook.addLoveNote,
updateLoveNoteStatus: loveNotesHook.updateLoveNoteStatus,
deleteLoveNote: loveNotesHook.deleteLoveNote,
```
Et dans le deps array useMemo ligne ~1862 : ajouter `loveNotesHook.loveNotes` a la ligne des state values + `loveNotesHook` a la ligne des callbacks (~1880).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Creer hooks/useVaultLoveNotes.ts — hook domaine CRUD (pattern useVaultNotes)</name>
  <files>hooks/useVaultLoveNotes.ts</files>
  <read_first>
    - hooks/useVaultNotes.ts (lire INTEGRALEMENT — 116 lignes — c'est le modele EXACT a repliquer : structure useState + resetX + loadX + addX + updateX + deleteX + return)
    - lib/parser.ts (confirmer signatures de LOVENOTES_DIR, parseLoveNote, serializeLoveNote, loveNotePath creees en Plan 01)
    - lib/types.ts (confirmer signatures de LoveNote, LoveNoteStatus)
    - lib/vault.ts (lignes 95-250 — VaultManager API : readFile, writeFile, deleteFile, ensureDir, exists, listFilesRecursive)
    - .planning/phases/34-fondation-donn-es-hook-domaine/34-RESEARCH.md (section "Pattern 3: Hook domaine" lignes 263-372 — code complet recommande)
  </read_first>
  <action>
Creer `hooks/useVaultLoveNotes.ts` en repliquant EXACTEMENT la structure de `useVaultNotes.ts`. **Langue commentaires : francais** (convention CLAUDE.md).

Contenu complet du fichier :

```typescript
/**
 * useVaultLoveNotes.ts — Hook dedie au domaine Love Notes (Phase 34)
 *
 * Extrait de useVault.ts pour alleger le monolithe.
 * Appele uniquement par useVaultInternal() via useVaultLoveNotes(vaultRef).
 *
 * Pattern identique a useVaultNotes — 1 fichier = 1 note, classe par destinataire
 * dans `03 - Famille/LoveNotes/{to-profileId}/{YYYY-MM-DD-slug}.md`.
 */

import { useState, useCallback } from 'react';
import type React from 'react';
import type { LoveNote, LoveNoteStatus } from '../lib/types';
import {
  LOVENOTES_DIR,
  parseLoveNote,
  serializeLoveNote,
  loveNotePath,
} from '../lib/parser';
import type { VaultManager } from '../lib/vault';

// ─── Utilitaires locaux ──────────────────────────────────────────────────────

function isFileNotFound(e: unknown): boolean {
  const msg = String(e);
  return msg.includes('cannot read') || msg.includes('not exist') || msg.includes('no such') || msg.includes('ENOENT');
}
function warnUnexpected(context: string, e: unknown) {
  if (!isFileNotFound(e)) console.warn(`[useVaultLoveNotes] ${context}:`, e);
}

// ─── Interface de retour ─────────────────────────────────────────────────────

export interface UseVaultLoveNotesResult {
  loveNotes: LoveNote[];
  loadLoveNotes: (vault: VaultManager) => Promise<LoveNote[]>;
  setLoveNotes: React.Dispatch<React.SetStateAction<LoveNote[]>>;
  addLoveNote: (note: Omit<LoveNote, 'sourceFile'>) => Promise<void>;
  updateLoveNoteStatus: (sourceFile: string, status: LoveNoteStatus, readAt?: string) => Promise<void>;
  deleteLoveNote: (sourceFile: string) => Promise<void>;
  resetLoveNotes: () => void;
}

// ─── Hook ────────────────────────────────────────────────────────────────────

export function useVaultLoveNotes(
  vaultRef: React.MutableRefObject<VaultManager | null>
): UseVaultLoveNotesResult {
  const [loveNotes, setLoveNotes] = useState<LoveNote[]>([]);

  const resetLoveNotes = useCallback(() => {
    setLoveNotes([]);
  }, []);

  const loadLoveNotes = useCallback(async (vault: VaultManager): Promise<LoveNote[]> => {
    try {
      await vault.ensureDir(LOVENOTES_DIR);
      const files = await vault.listFilesRecursive(LOVENOTES_DIR, '.md');
      const results = await Promise.all(
        files.map(async (file) => {
          try {
            const content = await vault.readFile(file);
            return parseLoveNote(file, content);
          } catch (e) { warnUnexpected(`loveNote(${file})`, e); return null; }
        })
      );
      const loaded = results.filter((n): n is LoveNote => n !== null);
      // Tri par createdAt desc (plus recentes en premier — coherent avec useVaultNotes)
      loaded.sort((a, b) => b.createdAt.localeCompare(a.createdAt));
      return loaded;
    } catch {
      return [];
    }
  }, []);

  const addLoveNote = useCallback(async (note: Omit<LoveNote, 'sourceFile'>) => {
    if (!vaultRef.current) return;
    const dir = `${LOVENOTES_DIR}/${note.to}`;
    await vaultRef.current.ensureDir(dir);
    const relPath = loveNotePath(note.to, note.createdAt);
    await vaultRef.current.writeFile(relPath, serializeLoveNote(note));
    const exists = await vaultRef.current.exists(relPath);
    if (!exists) throw new Error("Echec de l'ecriture de la love note");
    setLoveNotes((prev) => [{ ...note, sourceFile: relPath }, ...prev]);
  }, [vaultRef]);

  const updateLoveNoteStatus = useCallback(async (
    sourceFile: string,
    status: LoveNoteStatus,
    readAt?: string,
  ) => {
    if (!vaultRef.current) return;
    const current = await vaultRef.current.readFile(sourceFile).catch(() => '');
    const parsed = parseLoveNote(sourceFile, current);
    if (!parsed) return;
    const updated: Omit<LoveNote, 'sourceFile'> = {
      from: parsed.from,
      to: parsed.to,
      createdAt: parsed.createdAt,
      revealAt: parsed.revealAt,
      status,
      readAt: status === 'read' ? (readAt ?? new Date().toISOString().slice(0, 19)) : parsed.readAt,
      body: parsed.body,
    };
    await vaultRef.current.writeFile(sourceFile, serializeLoveNote(updated));
    setLoveNotes((prev) => prev.map((n) =>
      n.sourceFile === sourceFile ? { ...updated, sourceFile } : n
    ));
  }, [vaultRef]);

  const deleteLoveNote = useCallback(async (sourceFile: string) => {
    if (!vaultRef.current) return;
    await vaultRef.current.deleteFile(sourceFile);
    setLoveNotes((prev) => prev.filter((n) => n.sourceFile !== sourceFile));
  }, [vaultRef]);

  return {
    loveNotes,
    loadLoveNotes,
    setLoveNotes,
    addLoveNote,
    updateLoveNoteStatus,
    deleteLoveNote,
    resetLoveNotes,
  };
}
```

**NE PAS** :
- Ajouter un provider / createContext (pattern hook sous-domaine — consomme par useVault.ts directement)
- Exposer des helpers filter (Phase 35/36 feront la derivation dans les composants/lib-helpers)
- Modifier directement loveNotes via prev.push (use spread ou map/filter — patterns React state)
- Importer gray-matter (parseLoveNote le fait deja)
  </action>
  <verify>
    <automated>cd /Users/gabrielwaltio/Documents/family-vault && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `hooks/useVaultLoveNotes.ts` existe avec >=100 lignes
    - `grep "export function useVaultLoveNotes" hooks/useVaultLoveNotes.ts` retourne 1 ligne
    - `grep "export interface UseVaultLoveNotesResult" hooks/useVaultLoveNotes.ts` retourne 1 ligne
    - Le fichier importe `LOVENOTES_DIR, parseLoveNote, serializeLoveNote, loveNotePath` depuis `'../lib/parser'`
    - Le fichier importe `import type { LoveNote, LoveNoteStatus } from '../lib/types'`
    - Le fichier importe `import type { VaultManager } from '../lib/vault'`
    - Le hook retourne exactement 7 proprietes : loveNotes, loadLoveNotes, setLoveNotes, addLoveNote, updateLoveNoteStatus, deleteLoveNote, resetLoveNotes
    - `addLoveNote` appelle `ensureDir` puis `writeFile` puis verifie `exists` puis setLoveNotes prepend (ordre exact)
    - `loadLoveNotes` appelle `ensureDir(LOVENOTES_DIR)` puis `listFilesRecursive(LOVENOTES_DIR, '.md')` puis trie par createdAt desc
    - Aucun `createContext` ni `Provider` dans le fichier (grep retourne 0 ligne)
    - `npx tsc --noEmit` passe sans nouvelles erreurs
  </acceptance_criteria>
  <done>Hook useVaultLoveNotes autonome et complet, pattern identique a useVaultNotes, pret a etre cable dans useVault.ts.</done>
</task>

<task type="auto">
  <name>Task 2: Cabler useVaultLoveNotes dans hooks/useVault.ts (7 points precis)</name>
  <files>hooks/useVault.ts</files>
  <read_first>
    - hooks/useVault.ts (lire integralement les zones cles — lignes 85-115 imports, 125-295 VaultState interface, 575-595 sous-hooks instanciation, 660-695 hydrate cache, 1200-1295 loadVaultData + results processing, 1295-1325 saveCache payload, 1820-1882 return useMemo + deps)
    - hooks/useVaultLoveNotes.ts (cree en Task 1 — signatures exactes de useVaultLoveNotes et UseVaultLoveNotesResult)
    - .planning/phases/34-fondation-donn-es-hook-domaine/34-RESEARCH.md (section "Pattern 4: Cablage" lignes 374-429 — 7 points detailles)
  </read_first>
  <action>
Modifier `hooks/useVault.ts` en **7 points precis**. Budget total : ~15 lignes ajoutees (respect boundary god hook). Utiliser Edit ou Write avec soin.

**Point 1 — Import du sous-hook (ligne ~90-95)** :
Chercher le groupe d'imports `import { useVaultX } from './useVaultX';`. Apres `import { useVaultNotes } from './useVaultNotes';` (ligne 90), ajouter :
```typescript
import { useVaultLoveNotes } from './useVaultLoveNotes';
```

**Point 2 — Ajouter LoveNote, LoveNoteStatus a l'import de `../lib/types`** :
Chercher l'import type depuis `'../lib/types'` existant dans le fichier. Ajouter `LoveNote, LoveNoteStatus` a la liste (respecter ordre alphabetique si liste ordonnee) :
```typescript
import type { /* existants */, LoveNote, LoveNoteStatus, /* suite */ } from '../lib/types';
```

**Point 3 — Instanciation du sous-hook (ligne ~585-588)** :
Chercher `const storiesHook = useVaultStories(vaultRef);` (ligne 585). Ajouter JUSTE APRES, et AVANT `const [gardenRaw, setGardenRaw] = useState<string>('');` (ligne 588) :
```typescript
  // Domaine Love Notes delegue a useVaultLoveNotes (Phase 34)
  const loveNotesHook = useVaultLoveNotes(vaultRef);
```

**Point 4 — Interface VaultState (ligne ~287-290)** :
Chercher la ligne `setGardenRaw: React.Dispatch<React.SetStateAction<string>>;` (ligne 287). Ajouter JUSTE APRES, et AVANT `stories:` (ligne 288) :
```typescript
  loveNotes: LoveNote[];
  addLoveNote: (note: Omit<LoveNote, 'sourceFile'>) => Promise<void>;
  updateLoveNoteStatus: (sourceFile: string, status: LoveNoteStatus, readAt?: string) => Promise<void>;
  deleteLoveNote: (sourceFile: string) => Promise<void>;
```

**Point 5 — Hydrate cache (ligne ~685)** :
Chercher `missionsHook.setSecretMissions(cached.secretMissions);` (ligne 685). Ajouter JUSTE APRES, dans le meme bloc try :
```typescript
              loveNotesHook.setLoveNotes(cached.loveNotes);
```

**Point 6 — loadVaultData Promise.allSettled (ligne 1211 + 1287)** :
a) Dans le tableau Promise.allSettled, apres la ligne 1211 `storiesHook.loadStories(vault, enfantNames).catch(() => [] as BedtimeStory[]),` et AVANT `]);` ligne 1212 :
```typescript
        // [23] Love Notes — 1 fichier par note classe par destinataire (Phase 34)
        loveNotesHook.loadLoveNotes(vault).catch(() => [] as LoveNote[]),
```

b) Dans le traitement des resultats, apres `storiesHook.setStories(val(results[22] as PromiseSettledResult<BedtimeStory[]>, []));` (ligne 1287), ajouter :
```typescript
      loveNotesHook.setLoveNotes(val(results[23] as PromiseSettledResult<LoveNote[]>, []));
```

**Point 7 — saveCache payload (ligne ~1321)** :
Chercher le bloc `saveCache({ vaultPath: vault.vaultPath, ... })` qui commence ligne 1298. Apres la ligne `secretMissions: val(results[20], []) as Task[],` (ligne 1321), ajouter :
```typescript
        loveNotes: val(results[23] as PromiseSettledResult<LoveNote[]>, []) as LoveNote[],
```

**Point 8 — Return useMemo (ligne ~1850-1853)** :
a) Chercher `setGardenRaw,` (ligne 1850) dans l'objet retourne. Ajouter JUSTE APRES, avant `stories: storiesHook.stories,` (ligne 1851) :
```typescript
    loveNotes: loveNotesHook.loveNotes,
    addLoveNote: loveNotesHook.addLoveNote,
    updateLoveNoteStatus: loveNotesHook.updateLoveNoteStatus,
    deleteLoveNote: loveNotesHook.deleteLoveNote,
```

b) Dans le deps array useMemo (ligne 1862), ajouter `loveNotesHook.loveNotes` a la liste des state values :
```typescript
    // Ligne 1862 — ajouter a la fin de la liste state values
    quotes, moods, skillTrees, secretMissions, gardenRaw, storiesHook.stories, loveNotesHook.loveNotes,
```

c) Dans les deps callbacks (ligne ~1880, apres `storiesHook,`) ajouter :
```typescript
    loveNotesHook,
```

**NE PAS** :
- Ajouter un reset dans `setVaultPath` (ligne 1343-1366) — le pattern storiesHook.resetStories() pourrait etre repliquer mais n'est PAS requis pour Phase 34 (acceptable de le laisser absent si hook deja clear via setLoveNotes([]) implicite)
- Oublier l'un des 7 points ci-dessus (cablage incomplet = loveNotes non expose via useVault())
- Modifier la signature de `loadVaultData` ou `refresh`
- Ajouter un profilesRef pattern comme missionsHook (love notes n'en ont pas besoin)

**Recommandation ajout optionnel (bonne pratique mais non bloquant)** : Ajouter `loveNotesHook.resetLoveNotes();` dans `setVaultPath` (ligne ~1366 apres `storiesHook.resetStories();`) pour nettoyer l'etat quand le user change de vault. Si ajoute, cela compte comme 1 ligne supplementaire dans le budget 15.
  </action>
  <verify>
    <automated>cd /Users/gabrielwaltio/Documents/family-vault && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep "import { useVaultLoveNotes } from './useVaultLoveNotes'" hooks/useVault.ts` retourne 1 ligne
    - `grep "LoveNote, LoveNoteStatus" hooks/useVault.ts` OU `grep "LoveNote," hooks/useVault.ts` retourne au moins 1 ligne dans un import depuis '../lib/types'
    - `grep "const loveNotesHook = useVaultLoveNotes(vaultRef)" hooks/useVault.ts` retourne 1 ligne
    - `grep "loveNotes: LoveNote\[\]" hooks/useVault.ts` retourne au moins 1 ligne (VaultState interface)
    - `grep "addLoveNote:" hooks/useVault.ts` retourne au moins 2 lignes (declaration VaultState + return useMemo)
    - `grep "updateLoveNoteStatus:" hooks/useVault.ts` retourne au moins 2 lignes
    - `grep "deleteLoveNote:" hooks/useVault.ts` retourne au moins 2 lignes
    - `grep "loveNotesHook.setLoveNotes(cached.loveNotes)" hooks/useVault.ts` retourne 1 ligne (hydrate cache)
    - `grep "loveNotesHook.loadLoveNotes(vault)" hooks/useVault.ts` retourne 1 ligne (loadVaultData)
    - `grep "results\[23\]" hooks/useVault.ts` retourne au moins 2 lignes (setLoveNotes + saveCache)
    - `grep "loveNotes: val(results\[23\]" hooks/useVault.ts` retourne 1 ligne (saveCache payload)
    - `grep "loveNotes: loveNotesHook.loveNotes" hooks/useVault.ts` retourne 1 ligne (return useMemo)
    - `grep "loveNotesHook.loveNotes" hooks/useVault.ts` retourne au moins 2 lignes (return + deps array)
    - `npx tsc --noEmit` passe sans nouvelles erreurs (hors pre-existantes useVault.ts documentees CLAUDE.md)
    - Diff ligne sur useVault.ts : au plus 18 lignes ajoutees (budget 15 + 3 reset tolerance)
  </acceptance_criteria>
  <done>useVault.ts expose `loveNotes`, `addLoveNote`, `updateLoveNoteStatus`, `deleteLoveNote` dans VaultState et les propage via VaultContext. Le cache save/load integre loveNotes (LOVE-04 cross-boot). VaultContext auto-propage — aucun changement requis cote VaultContext.tsx. Boundary god hook respectee. LOVE-03 atteint.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passe sans nouvelles erreurs (hors pre-existantes)
2. `grep -c "loveNote\|LoveNote" hooks/useVault.ts` retourne >= 10 (import + type + hook instance + VaultState x4 + hydrate + load + save + return + deps)
3. `grep -c "loveNote\|LoveNote" hooks/useVaultLoveNotes.ts` retourne >= 15 (imports + type + interface + hook + CRUD)
4. `grep "createContext\|<.*Provider>" hooks/useVaultLoveNotes.ts` retourne 0 ligne (pas de provider)
5. `wc -l hooks/useVaultLoveNotes.ts` retourne >= 100 lignes
6. Diff `git diff hooks/useVault.ts` : au plus 18 lignes ajoutees (boundary respectee)
</verification>

<success_criteria>
- hooks/useVaultLoveNotes.ts existe, exporte useVaultLoveNotes (hook CRUD complet) et UseVaultLoveNotesResult (interface)
- Hook implemente loadLoveNotes (scan recursif + parse + tri desc), addLoveNote (ensureDir + writeFile + exists check + prepend state), updateLoveNoteStatus (reparse + write + map state), deleteLoveNote (deleteFile + filter state), resetLoveNotes
- useVault().loveNotes est lisible depuis n'importe quel composant via useVault() (LOVE-03)
- Le cache hydrate setLoveNotes au boot + saveCache persiste loveNotes dans le payload (LOVE-04)
- CACHE_VERSION bump (Plan 01) garantit invalidation propre pour utilisateurs TestFlight existants
- useVault.ts grandit de <=18 lignes (boundary god hook respectee)
- tsc --noEmit passe
- Aucune modification de contexts/VaultContext.tsx necessaire (auto-propage VaultState)
- Pattern Notes 100% replique sans divergence
</success_criteria>

<output>
After completion, create `.planning/phases/34-fondation-donn-es-hook-domaine/34-03-SUMMARY.md`
</output>
