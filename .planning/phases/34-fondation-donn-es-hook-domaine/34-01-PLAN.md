---
phase: 34-fondation-donn-es-hook-domaine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/types.ts
  - lib/parser.ts
  - lib/vault-cache.ts
autonomous: true
requirements: [LOVE-01, LOVE-02, LOVE-04]
must_haves:
  truths:
    - "Le type LoveNote expose from/to/createdAt/revealAt/status + readAt? + body + sourceFile"
    - "LoveNoteStatus est l'enum strict 'pending' | 'revealed' | 'read'"
    - "parseLoveNote('', '') retourne null (frontmatter manquant)"
    - "parseLoveNote(relPath, serializeLoveNote(data)) retourne un LoveNote egal a data (+ sourceFile) — round-trip loss-less"
    - "serializeLoveNote n'emet readAt: uniquement si note.readAt est defini (pas de 'undefined' literal)"
    - "loveNotePath('emma', '2026-04-16T14:32:17') retourne '03 - Famille/LoveNotes/emma/2026-04-16-<suffix>.md'"
    - "CACHE_VERSION === 2 dans lib/vault-cache.ts (bump 1 -> 2)"
    - "VaultCacheState expose loveNotes: LoveNote[]"
    - "Aucune import de matter.stringify dans la section LoveNote de lib/parser.ts"
  artifacts:
    - path: "lib/types.ts"
      provides: "Type LoveNote + LoveNoteStatus ajoutes apres Note"
      contains: "export interface LoveNote"
    - path: "lib/parser.ts"
      provides: "LOVENOTES_DIR, loveNoteFileName, loveNotePath, parseLoveNote, serializeLoveNote"
      exports: ["LOVENOTES_DIR", "loveNoteFileName", "loveNotePath", "parseLoveNote", "serializeLoveNote"]
    - path: "lib/vault-cache.ts"
      provides: "CACHE_VERSION bumpe a 2 + loveNotes dans VaultCacheState"
      contains: "CACHE_VERSION = 2"
  key_links:
    - from: "lib/parser.ts"
      to: "lib/types.ts"
      via: "import type { LoveNote, LoveNoteStatus }"
      pattern: "import type.*LoveNote.*from.*types"
    - from: "lib/vault-cache.ts"
      to: "lib/types.ts"
      via: "import type LoveNote"
      pattern: "LoveNote"
---

<objective>
Creer la fondation donnees du domaine LoveNote : type canonique + helpers path + parser bidirectionnel (parseLoveNote / serializeLoveNote) + extension du cache vault avec bump CACHE_VERSION.

Purpose: Fournir les briques statiques que Plan 02 (tests) et Plan 03 (hook + cablage) consommeront. Aucun hook React, aucune UI — pur TypeScript + types + fonctions. Replique exacte du pattern Notes (file-per-entity avec frontmatter YAML + body markdown).
Output: 3 fichiers modifies (lib/types.ts, lib/parser.ts, lib/vault-cache.ts) — ~70 lignes ajoutees au total, zero dependance npm nouvelle, backward-compatible.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/34-fondation-donn-es-hook-domaine/34-RESEARCH.md

@lib/types.ts
@lib/parser.ts
@lib/vault-cache.ts

<interfaces>
<!-- Pattern Note existant a repliquer — lib/types.ts ligne 567 -->
From lib/types.ts:
```typescript
export interface Note {
  title: string;
  url?: string;
  category: string;
  created: string;
  tags: string[];
  content: string;
  sourceFile: string;
}
```

<!-- Pattern parseNote/serializeNote a repliquer — lib/parser.ts lignes 2688-2751 -->
From lib/parser.ts:
```typescript
export const NOTES_DIR = '08 - Notes';
export function noteFileName(title: string): string { /* slug generator */ }
export function parseNote(relativePath: string, content: string): Note | null {
  const { data, content: body } = parseFrontmatter(content);
  if (!data.title) return null;
  return { title, url?, category, created, tags, content: body.trim(), sourceFile: relativePath };
}
export function serializeNote(note: Omit<Note, 'sourceFile'>): string {
  const lines = ['---', `title: "..."`, ...];
  return lines.join('\n');
}
```

<!-- parseFrontmatter helper commun — lib/parser.ts ligne 191 -->
From lib/parser.ts:
```typescript
export function parseFrontmatter(content: string): { data: Record<string, any>; content: string };
```

<!-- VaultCacheState actuel — lib/vault-cache.ts ligne 70 -->
From lib/vault-cache.ts:
```typescript
const CACHE_VERSION = 1;  // LIGNE 44 — bumper a 2
const CACHE_FILE_URI = FileSystem.documentDirectory + 'vault-cache-v1.json';  // LIGNE 45 — bumper a v2
export interface VaultCacheState {
  version: number;
  savedAt: string;
  vaultPath: string | null;
  // ... 22 champs existants ...
  secretMissions: Task[];
  // ← LoveNote[] a ajouter ici
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ajouter type LoveNote + LoveNoteStatus dans lib/types.ts</name>
  <files>lib/types.ts</files>
  <read_first>
    - lib/types.ts (lire integralement — reperer ligne 567 `export interface Note` et ligne 497 `export interface Anniversary` pour style)
    - .planning/phases/34-fondation-donn-es-hook-domaine/34-RESEARCH.md (section "Pattern 1: Type canonique LoveNote" lignes 156-180)
  </read_first>
  <action>
Ajouter deux declarations dans `lib/types.ts`, apres l'interface `Note` (ligne ~576), et AVANT la section suivante. Inserer exactement ce bloc (commentaires en francais, convention CLAUDE.md) :

```typescript
// ─── Love Notes (messages affectifs programmes — Phase 34) ──────────────────

/** Statut de revelation d'une love note (cycle pending -> revealed -> read) */
export type LoveNoteStatus = 'pending' | 'revealed' | 'read';

/**
 * Une note affective programmee entre membres de la famille.
 * Un fichier = une note, classe par destinataire dans `03 - Famille/LoveNotes/{to}/`.
 * revealAt stocke en heure locale de l'appareil au moment de la composition
 * (convention ISO sans Z, cohérente avec museum/village). La reveal logic
 * (Phase 36) interpretera cette string comme heure locale a la lecture.
 */
export interface LoveNote {
  /** ID du profil emetteur (expediteur) */
  from: string;
  /** ID du profil destinataire (recipiendaire) */
  to: string;
  /** Timestamp ISO 8601 de creation — YYYY-MM-DDTHH:mm:ss (sans Z) */
  createdAt: string;
  /** Timestamp ISO 8601 de revelation programmee — YYYY-MM-DDTHH:mm:ss */
  revealAt: string;
  /** Statut actuel : pending (en attente), revealed (revealee, non lue), read (lue) */
  status: LoveNoteStatus;
  /** Timestamp ISO 8601 de lecture (present uniquement si status === 'read') */
  readAt?: string;
  /** Corps markdown de la note (message de l'expediteur) */
  body: string;
  /** Chemin relatif dans le vault — non serialise, utilise pour update/delete */
  sourceFile: string;
}
```

Inserer apres la fermeture de `export interface Note { ... }` (autour ligne 576). Si un autre groupe `// ─── ...` suit immediatement, inserer AVANT ce groupe.

**NE PAS** :
- Ajouter un champ `title` (pas de titre explicite — discretion Claude documentee dans RESEARCH.md section "Pitfall / Discretion" — les love notes sont courtes et intimes, le body parle de lui-meme)
- Ajouter de dependance npm
- Modifier des types existants
  </action>
  <verify>
    <automated>cd /Users/gabrielwaltio/Documents/family-vault && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep "export type LoveNoteStatus" lib/types.ts` retourne 1 ligne avec la valeur `'pending' | 'revealed' | 'read'`
    - `grep "export interface LoveNote" lib/types.ts` retourne 1 ligne
    - `grep "readAt" lib/types.ts` retourne au moins 1 ligne dans le bloc LoveNote (champ optionnel)
    - `grep "sourceFile" lib/types.ts` montre `sourceFile: string` present dans le bloc LoveNote
    - Le bloc LoveNote ne contient PAS de champ `title` (grep `title` dans la section LoveNote retourne 0 ligne)
    - `npx tsc --noEmit` passe sans nouvelles erreurs (hors erreurs pre-existantes CLAUDE.md : MemoryEditor.tsx, cooklang.ts, useVault.ts)
  </acceptance_criteria>
  <done>Types LoveNote et LoveNoteStatus exportes depuis lib/types.ts, backward-compatible, pret a etre consomme par parser et hook.</done>
</task>

<task type="auto">
  <name>Task 2: Ajouter LOVENOTES_DIR + helpers path + parseLoveNote + serializeLoveNote dans lib/parser.ts</name>
  <files>lib/parser.ts</files>
  <read_first>
    - lib/parser.ts (lignes 2685-2755 — section Notes complete : NOTES_DIR, noteFileName, parseNote, serializeNote — modele EXACT a repliquer)
    - lib/parser.ts (ligne 191 — signature de parseFrontmatter, helper commun)
    - lib/types.ts (apres Task 1 — import type LoveNote, LoveNoteStatus)
    - .planning/phases/34-fondation-donn-es-hook-domaine/34-RESEARCH.md (section "Pattern 2: Parser bidirectionnel" lignes 184-256, et "Pitfalls 2, 3, 7" — dates, slug collision, readAt optionnel)
  </read_first>
  <action>
Ajouter un bloc complet dans `lib/parser.ts` APRES la section Notes (apres la fin de `serializeNote` vers ligne 2751) et AVANT la section suivante `// ─── Arbre de compétences` (ligne 2753).

**1. Verifier l'import de LoveNote** — Chercher l'import existant des types depuis `./types`. Ajouter `LoveNote`, `LoveNoteStatus` dans ce bloc d'import. Exemple :
```typescript
import type { /* existants */, LoveNote, LoveNoteStatus } from './types';
```

**2. Inserer le bloc complet ci-dessous** (copie verbatim, convention commentaires FR) :

```typescript
// ─── Love Notes (messages affectifs programmes — Phase 34) ──────────────────

/** Repertoire racine des love notes dans le vault (classement par destinataire) */
export const LOVENOTES_DIR = '03 - Famille/LoveNotes';

/**
 * Genere un nom de fichier slug a partir d'un timestamp createdAt ISO.
 * Format : YYYY-MM-DD-{suffix}.md ou suffix est le base36 des 9 caracteres HHMMSSmmm
 * (heure:minute:seconde:ms sans separateurs). Collision-safe a la milliseconde pres.
 * Ex: createdAt='2026-04-16T14:32:17.123' -> '2026-04-16-8bzn7f.md' (deterministe).
 */
export function loveNoteFileName(createdAt: string): string {
  const datePart = createdAt.slice(0, 10); // YYYY-MM-DD
  // Hash base36 des caracteres HH:mm:ss.mmm (9 chiffres apres slice+replace)
  const timePart = createdAt.slice(11).replace(/[:.]/g, '').slice(0, 9);
  const digits = timePart.padEnd(9, '0'); // garantit au moins 9 chiffres si ms manquant
  const suffix = Number(digits).toString(36);
  return `${datePart}-${suffix}.md`;
}

/**
 * Construit le chemin relatif complet d'une love note pour un destinataire donne.
 * Pattern : 03 - Famille/LoveNotes/{toProfileId}/{YYYY-MM-DD-suffix}.md (LOVE-01).
 */
export function loveNotePath(toProfileId: string, createdAt: string): string {
  return `${LOVENOTES_DIR}/${toProfileId}/${loveNoteFileName(createdAt)}`;
}

/**
 * Parse une love note markdown avec frontmatter YAML.
 * Retourne null si un champ requis manque ou si le statut est invalide.
 */
export function parseLoveNote(relativePath: string, content: string): LoveNote | null {
  const { data, content: body } = parseFrontmatter(content);

  // Validation stricte — tous les champs requis doivent etre presents
  if (!data.from || !data.to || !data.createdAt || !data.revealAt || !data.status) return null;

  const status = String(data.status);
  if (status !== 'pending' && status !== 'revealed' && status !== 'read') return null;

  return {
    from: String(data.from),
    to: String(data.to),
    createdAt: String(data.createdAt),
    revealAt: String(data.revealAt),
    status: status as LoveNoteStatus,
    readAt: data.readAt ? String(data.readAt) : undefined,
    body: body.trim(),
    sourceFile: relativePath,
  };
}

/**
 * Serialise une love note en markdown avec frontmatter YAML propre.
 * Construction manuelle (PAS matter.stringify — Pitfall: coerce les dates ISO en Date).
 * readAt n'est emis que si defini (pas de string 'undefined' literal — Pitfall 7).
 */
export function serializeLoveNote(note: Omit<LoveNote, 'sourceFile'>): string {
  const lines = [
    '---',
    `from: "${note.from}"`,
    `to: "${note.to}"`,
    `createdAt: "${note.createdAt}"`,
    `revealAt: "${note.revealAt}"`,
    `status: "${note.status}"`,
  ];
  if (note.readAt) {
    lines.push(`readAt: "${note.readAt}"`);
  }
  lines.push('---');
  lines.push('');
  lines.push(note.body);
  lines.push('');
  return lines.join('\n');
}
```

**NE PAS** :
- Utiliser `matter.stringify(...)` (Pitfall 2 de RESEARCH : coerce les dates ISO en objets Date, casse le round-trip)
- Emettre `readAt: "undefined"` literal si `note.readAt === undefined` (Pitfall 7 : template string naif)
- Ajouter un champ `title` au serialize/parse (decision : pas de titre explicite)
- Inventer un autre algo de slug — utiliser exactement le base36 de HHMMSSmmm
  </action>
  <verify>
    <automated>cd /Users/gabrielwaltio/Documents/family-vault && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep "export const LOVENOTES_DIR = '03 - Famille/LoveNotes'" lib/parser.ts` retourne 1 ligne exacte
    - `grep "export function loveNoteFileName" lib/parser.ts` retourne 1 ligne
    - `grep "export function loveNotePath" lib/parser.ts` retourne 1 ligne
    - `grep "export function parseLoveNote" lib/parser.ts` retourne 1 ligne
    - `grep "export function serializeLoveNote" lib/parser.ts` retourne 1 ligne
    - `grep "matter.stringify" lib/parser.ts` dans le bloc LoveNote retourne 0 ligne (recherche dans la section LoveNote uniquement)
    - `grep "if (note.readAt)" lib/parser.ts` retourne au moins 1 ligne (guard contre 'undefined' literal)
    - `grep "import type.*LoveNote" lib/parser.ts` retourne au moins 1 ligne
    - `grep "status !== 'pending' && status !== 'revealed' && status !== 'read'" lib/parser.ts` retourne 1 ligne (validation enum stricte)
    - `npx tsc --noEmit` passe sans nouvelles erreurs
  </acceptance_criteria>
  <done>lib/parser.ts expose LOVENOTES_DIR, loveNoteFileName, loveNotePath, parseLoveNote, serializeLoveNote — round-trip loss-less, dates ISO sans corruption, readAt optionnel correctement gere.</done>
</task>

<task type="auto">
  <name>Task 3: Bump CACHE_VERSION 1->2 + ajouter loveNotes dans VaultCacheState</name>
  <files>lib/vault-cache.ts</files>
  <read_first>
    - lib/vault-cache.ts (lire integralement — 185 lignes)
    - lib/types.ts (apres Task 1 — confirme que LoveNote est exporte)
    - CLAUDE.md (section "Cache (lib/vault-cache.ts)" — directive bump quand ajout domaine)
    - .planning/phases/34-fondation-donn-es-hook-domaine/34-RESEARCH.md (section "Pattern 5: Cache" lignes 436-462, et "Pitfall 1: Oublier de bump CACHE_VERSION")
  </read_first>
  <action>
Modifier `lib/vault-cache.ts` en 3 endroits precis :

**1. Ligne 44 — Bumper CACHE_VERSION de 1 a 2** (invalide les snapshots v1 existants des utilisateurs TestFlight) :
```typescript
// AVANT
const CACHE_VERSION = 1;
// APRES
const CACHE_VERSION = 2;
```

**2. Ligne 45 — Bumper CACHE_FILE_URI de v1 a v2** (coherence, force un nouveau fichier) :
```typescript
// AVANT
const CACHE_FILE_URI = FileSystem.documentDirectory + 'vault-cache-v1.json';
// APRES
const CACHE_FILE_URI = FileSystem.documentDirectory + 'vault-cache-v2.json';
```

**3. Ligne ~20-42 — Ajouter `LoveNote` a la liste des imports de `./types`** :
Trouver le bloc `import type { Anniversary, ChildQuote, ... } from './types';` (commence ligne 20). Ajouter `LoveNote,` dans la liste alphabetique, par exemple entre `HealthRecord` et `MealItem` :
```typescript
import type {
  Anniversary,
  ChildQuote,
  CourseItem,
  GratitudeDay,
  HealthRecord,
  LoveNote,  // ← AJOUT
  MealItem,
  // ... reste inchange
} from './types';
```

**4. Ligne 95 — Ajouter `loveNotes: LoveNote[]` dans `VaultCacheState`**, APRES `secretMissions: Task[];` et AVANT la fermeture `}` :
```typescript
export interface VaultCacheState {
  // ... champs existants inchanges ...
  secretMissions: Task[];
  loveNotes: LoveNote[];  // ← AJOUT (Phase 34)
}
```

**NE PAS** :
- Oublier le bump CACHE_VERSION (Pitfall 1 : crash au premier boot si `cached.loveNotes` est undefined)
- Oublier le bump CACHE_FILE_URI (coherence recommandee par CLAUDE.md)
- Modifier `hydrateFromCache` ni `saveCache` ni `stripProfileForCache` (Plan 03 fera le cablage cote useVault.ts : hydrate setLoveNotes + save loveNotes dans le payload)

**Note importante** : Apres ce bump, les utilisateurs TestFlight existants perdront leur cache v1 au prochain boot — ils auront un loadVaultData complet (~500ms-1s). C'est attendu, documenter dans le SUMMARY.
  </action>
  <verify>
    <automated>cd /Users/gabrielwaltio/Documents/family-vault && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep "const CACHE_VERSION = 2" lib/vault-cache.ts` retourne exactement 1 ligne
    - `grep "const CACHE_VERSION = 1" lib/vault-cache.ts` retourne 0 ligne (bump effectif, plus d'occurrence de v1)
    - `grep "vault-cache-v2.json" lib/vault-cache.ts` retourne 1 ligne
    - `grep "vault-cache-v1.json" lib/vault-cache.ts` retourne 0 ligne
    - `grep "LoveNote," lib/vault-cache.ts` retourne au moins 1 ligne (import)
    - `grep "loveNotes: LoveNote\[\]" lib/vault-cache.ts` retourne exactement 1 ligne (champ dans VaultCacheState)
    - `npx tsc --noEmit` passe sans nouvelles erreurs
  </acceptance_criteria>
  <done>CACHE_VERSION est bumpe a 2, CACHE_FILE_URI pointe sur v2, VaultCacheState expose loveNotes: LoveNote[]. Le cache est pret pour le cablage en Plan 03. Pitfall 1 evite — les snapshots v1 seront invalides proprement au premier boot post-deploy.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passe sans nouvelles erreurs (hors erreurs pre-existantes CLAUDE.md)
2. `grep -c "LoveNote" lib/types.ts` retourne >= 3 (type + interface + status)
3. `grep -c "LoveNote\|loveNote" lib/parser.ts` retourne >= 5 (import + 4 fonctions/constantes)
4. `grep "CACHE_VERSION = 2" lib/vault-cache.ts` retourne 1 ligne
5. Aucun usage de `matter.stringify` dans le bloc LoveNote de lib/parser.ts
</verification>

<success_criteria>
- Type `LoveNote` + `LoveNoteStatus` exportes depuis lib/types.ts avec signature exacte (from, to, createdAt, revealAt, status, readAt?, body, sourceFile)
- `LOVENOTES_DIR = '03 - Famille/LoveNotes'` exporte depuis lib/parser.ts
- `loveNoteFileName`, `loveNotePath`, `parseLoveNote`, `serializeLoveNote` exportes et fonctionnels
- Round-trip parseLoveNote(path, serializeLoveNote(data)) preserve toutes les donnees (verifie en Plan 02)
- `CACHE_VERSION = 2` et `loveNotes: LoveNote[]` dans VaultCacheState
- `npx tsc --noEmit` passe
- Backward-compatible : aucune modification des types/fonctions existants
</success_criteria>

<output>
After completion, create `.planning/phases/34-fondation-donn-es-hook-domaine/34-01-SUMMARY.md`
</output>
