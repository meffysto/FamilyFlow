---
phase: 54-monetisation-hybride-paiement
plan: 03
type: execute
wave: 3
depends_on: ["54-01", "54-02"]
files_modified:
  - contexts/EntitlementContext.tsx
  - app/_layout.tsx
autonomous: false
requirements:
  - "SC-1-entitlements-context"
  - "SC-2-revenuecat-purchase-restore"
  - "SC-7-regle-or-ia-autofinancee"

must_haves:
  truths:
    - "Au lancement, EntitlementProvider configure RevenueCat et lit le statut LIFETIME via CustomerInfo (D-03)"
    - "Le solde crédits + flag grandfather sont lus depuis 09 - Entitlements/quota.md (D-07)"
    - "Le flag grandfather est posé exactement une fois, quand le vault est prêt et contient déjà des données (D-04, D-05)"
    - "useEntitlements() expose status/quota/canGenerateStory/purchaseLifetime/purchaseStoryPack/restorePurchases/decrementStoryQuota"
    - "EntitlementProvider est monté entre ThemeProvider et AIProvider"
    - "Aucune donnée vault n'est transmise à RevenueCat (pas d'appUserID dérivé)"
  artifacts:
    - path: "contexts/EntitlementContext.tsx"
      provides: "EntitlementProvider + useEntitlements()"
      contains: "useEntitlements"
    - path: "app/_layout.tsx"
      provides: "EntitlementProvider monté dans la hiérarchie"
      contains: "EntitlementProvider"
  key_links:
    - from: "contexts/EntitlementContext.tsx"
      to: "RevenueCat CustomerInfo"
      via: "Purchases.getCustomerInfo + addCustomerInfoUpdateListener"
      pattern: "getCustomerInfo|addCustomerInfoUpdateListener"
    - from: "contexts/EntitlementContext.tsx"
      to: "09 - Entitlements/quota.md"
      via: "vault.readFile/writeFile + parseQuota/serializeQuota"
      pattern: "QUOTA_FILE|parseQuota"
    - from: "app/_layout.tsx"
      to: "EntitlementProvider"
      via: "insertion entre ThemeProvider et AIProvider"
      pattern: "EntitlementProvider"
---

<objective>
Câbler la couche pure (Wave 2) au natif (Wave 1) via `EntitlementContext.tsx` : init RevenueCat, lecture du statut LIFETIME (source de vérité D-03), lecture/écriture du fichier vault quota (D-07), détection grandfather posée une seule fois quand le vault est prêt (D-04/D-05, anti faux-négatif iCloud Piège 3), actions d'achat/restauration, et insertion du provider dans la hiérarchie. Couvre SC-1 (contexte), SC-2 (achat+restauration), SC-7 (règle d'or via gate).

Purpose: Centraliser achats et quota dans un provider unique, source de vérité React entre RevenueCat et les features (RESEARCH.md carte des responsabilités).
Output: `contexts/EntitlementContext.tsx` (provider + hook) + `app/_layout.tsx` modifié (provider monté).

App DÉJÀ PUBLIÉE — l'insertion du provider est non-cassante : aucun provider existant déplacé hors de son ordre relatif. EntitlementProvider seulement ajouté entre ThemeProvider et AIProvider.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/54-monetisation-hybride-paiement/54-CONTEXT.md
@.planning/phases/54-monetisation-hybride-paiement/54-RESEARCH.md
@.planning/phases/54-monetisation-hybride-paiement/54-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- Contrats consommés/produits — extraits du codebase. Pas d'exploration nécessaire. -->

Depuis lib/entitlements (Wave 2 — barrel index.ts) :
  type EntitlementStatus = 'FREE' | 'LIFETIME';
  interface QuotaData { grandfather; grandfatherDetectedAt; storyCredits; storyUsedThisMonth; storyResetMonth }
  interface EntitlementState { status; isGrandfathered; quota; isReady; isLoadingPurchase }
  const QUOTA_FILE = '09 - Entitlements/quota.md';
  const DEFAULT_QUOTA: QuotaData;
  parseQuota(content): QuotaData;  serializeQuota(q): string;
  canGenerateStory(quota, hasLifetime): boolean;  decrementQuota(quota, hasLifetime): QuotaData;
  detectGrandfatherEligibility({tasks, meals, profiles, memories}): boolean;
  currentLocalMonth(): string;

Depuis contexts/VaultContext.tsx :
  export function useVault(): VaultState;
  // VaultState (hooks/useVault.ts) expose :
  //   vault: VaultManager | null;      vaultPath: string | null;
  //   tasks: Task[];   meals: MealItem[];   profiles: Profile[];   memories: Memory[];
  // VaultManager (lib/vault.ts) : readFile(path):Promise<string>; writeFile(path,content):Promise<void>; deleteFile(path):Promise<void>;

Hiérarchie actuelle app/_layout.tsx (lignes 308-352) :
  SafeAreaProvider > [RN root] > VaultProvider > AuthProvider > ThemeProvider >
  AIProvider > StoryVoiceProvider > HelpProvider > ParentalControlsProvider > ToastProvider
  Imports contextes en tête de fichier (lignes 37-43).

RevenueCat (react-native-purchases, IDs verrouillés Wave 1) :
  ENTITLEMENT_PREMIUM = 'familyflow_premium'
  PRODUCT_LIFETIME    = 'familyflow_lifetime_v1'
  PRODUCT_STORY_PACK  = 'familyflow_story_pack_30'
  STORY_PACK_CREDITS  = 30
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Créer contexts/EntitlementContext.tsx (provider + useEntitlements)</name>
  <files>contexts/EntitlementContext.tsx</files>
  <read_first>
    - contexts/AIContext.tsx (ANALOGUE EXACT : lignes 10-12 imports, 60-77 init async, 95-160 useCallback actions, 160-178 useMemo value + hook useAI/throw)
    - contexts/AuthContext.tsx (lignes 143-178 : pattern isReady + init async + ne pas exposer données partielles)
    - .planning/phases/54-monetisation-hybride-paiement/54-PATTERNS.md §EntitlementContext.tsx (lignes 237-389 : code complet imports, constantes, init async, listener, useMemo value, hook, purchaseLifetime, restorePurchases)
    - .planning/phases/54-monetisation-hybride-paiement/54-RESEARCH.md §Patterns 1-5 (configure, getCustomerInfo, restore, listener, purchasePackage) + §Pièges 2,3,6,7
    - .planning/phases/54-monetisation-hybride-paiement/54-CONTEXT.md (D-02 aucune donnée vault, D-03 RevenueCat source vérité, D-05 flag une seule fois, D-06 règle d'or, D-09)
  </read_first>
  <action>
    Créer `contexts/EntitlementContext.tsx` en suivant 54-PATTERNS.md §EntitlementContext.tsx.

    Imports : React (createContext, useContext, useState, useEffect, useCallback, useMemo, useRef), `Purchases, { LOG_LEVEL }` de 'react-native-purchases', `Platform, Alert` de 'react-native', `useVault` de './VaultContext', et depuis '../lib/entitlements' : parseQuota, serializeQuota, QUOTA_FILE, DEFAULT_QUOTA, canGenerateStory (renommer à l'import si collision avec l'action exposée, ex. `canGenerateStory as canGenerateStoryPure`), decrementQuota, detectGrandfatherEligibility, currentLocalMonth, + types EntitlementStatus, EntitlementState, QuotaData.

    Constantes module :
    ```
    const RC_IOS_KEY = process.env.EXPO_PUBLIC_RC_IOS_KEY ?? '';
    const ENTITLEMENT_PREMIUM = 'familyflow_premium';
    const PRODUCT_LIFETIME = 'familyflow_lifetime_v1';
    const PRODUCT_STORY_PACK = 'familyflow_story_pack_30';
    const STORY_PACK_CREDITS = 30;
    ```

    Type d'actions exposées :
    ```
    interface EntitlementActions {
      canGenerateStory: () => boolean;
      purchaseLifetime: () => Promise<void>;
      purchaseStoryPack: () => Promise<void>;
      restorePurchases: () => Promise<void>;
      decrementStoryQuota: () => Promise<void>;
      lifetimePrice: string;   // priceString localisé RevenueCat (jamais hardcodé)
      packPrice: string;
    }
    ```

    State : status (EntitlementStatus, défaut 'FREE'), isGrandfathered (boolean), quota (QuotaData = DEFAULT_QUOTA), isReady (false), isLoadingPurchase (false), lifetimePrice/packPrice ('').

    **Init async au mount (useEffect dépendant de vault + vaultPath)** :
    1. `if (__DEV__) Purchases.setLogLevel(LOG_LEVEL.DEBUG);`
    2. `Purchases.configure({ apiKey: Platform.select({ ios: RC_IOS_KEY, default: 'test_' }) });` — NE JAMAIS passer d'appUserID (D-02).
    3. `const customerInfo = await Purchases.getCustomerInfo();` → `setStatus(!!customerInfo.entitlements.active[ENTITLEMENT_PREMIUM] ? 'LIFETIME' : 'FREE')` (D-03).
    4. Charger prix : `Purchases.getOfferings()` → priceString de chaque package (jamais hardcodé). En cas d'échec offline, laisser '' (UI affiche '…').
    5. **Lecture quota (D-07) + grandfather (D-05, Piège 3)** : SEULEMENT si `vault` non-null. Lire QUOTA_FILE :
       - Si le fichier existe → `parseQuota(content)` → setQuota + setIsGrandfathered(q.grandfather).
       - Si le fichier N'EXISTE PAS (catch sur readFile) → premier lancement de la version payante : appliquer la détection grandfather UNE SEULE FOIS via `detectGrandfatherEligibility({ tasks, meals, profiles, memories })`. Construire un QuotaData initial = `{ ...DEFAULT_QUOTA, grandfather: eligible, grandfatherDetectedAt: eligible ? new Date().toISOString() : '', storyResetMonth: currentLocalMonth() }`, l'écrire via `vault.writeFile(QUOTA_FILE, serializeQuota(initial))`, puis setQuota/setIsGrandfathered. NE PAS poser le flag si `vault` est null (Piège 3 — faux négatif iCloud : attendre que VaultProvider expose un vault non-null).
    6. `finally { setIsReady(true); }`.
    Wrapper dans try/catch ; erreurs techniques sous `if (__DEV__) console.warn('[EntitlementProvider] init failed:', e);`.

    **Listener temps réel** (useEffect séparé, cleanup) : `Purchases.addCustomerInfoUpdateListener((info) => setStatus(!!info.entitlements.active[ENTITLEMENT_PREMIUM] ? 'LIFETIME' : 'FREE'))` ; `return () => listener.remove();`.

    **Actions (useCallback)** :
    - `purchaseLifetime` : setIsLoadingPurchase(true) → getOfferings → `offerings.current?.getPackage(...)` (package lifetime) → `purchasePackage` → si entitlement actif setStatus('LIFETIME'). catch : ignorer `Purchases.PURCHASES_ERROR_CODE.PURCHASE_CANCELLED_ERROR` en silence, sinon `Alert.alert('Erreur', "L'achat a échoué. Réessayez plus tard.")`. finally setIsLoadingPurchase(false).
    - `purchaseStoryPack` : idem mais sur le package consommable → APRÈS succès, **créditer +30 dans le vault** (RevenueCat ne gère pas le solde — Piège 2) : relire quota via readFile/parseQuota, `serializeQuota({ ...q, storyCredits: q.storyCredits + STORY_PACK_CREDITS })`, writeFile(QUOTA_FILE, ...), setQuota.
    - `restorePurchases` : `Purchases.restorePurchases()` → maj status. catch → Alert FR. (Obligatoire Apple 3.1.1.)
    - `decrementStoryQuota` : relire QUOTA_FILE → parseQuota → `decrementQuota(q, status === 'LIFETIME')` → writeFile(serializeQuota(next)) → setQuota. À appeler APRÈS succès génération (D-09, Piège 6).
    - `canGenerateStory` exposé : `() => canGenerateStoryPure(quota, status === 'LIFETIME')`.

    **Value mémoïsée** (useMemo, deps = tous les states + callbacks) exposant EntitlementState & EntitlementActions. Provider `EntitlementCtx.Provider value={value}`.

    **Hook** : `export function useEntitlements() { const ctx = useContext(EntitlementCtx); if (!ctx) throw new Error('useEntitlements doit être utilisé dans un EntitlementProvider'); return ctx; }`.
  </action>
  <verify>
    <automated>grep -q "export function useEntitlements" contexts/EntitlementContext.tsx && npx tsc --noEmit 2>&1 | grep -v '^#' | grep -c "EntitlementContext" | grep -q "^0$" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "export function useEntitlements" contexts/EntitlementContext.tsx`.
    - `grep -q "ENTITLEMENT_PREMIUM = 'familyflow_premium'" contexts/EntitlementContext.tsx`.
    - `grep -q "addCustomerInfoUpdateListener" contexts/EntitlementContext.tsx` et `grep -q "listener.remove" contexts/EntitlementContext.tsx`.
    - Aucun appUserID passé à configure : `grep -v '^#' contexts/EntitlementContext.tsx | grep -c "appUserID"` == 0 (D-02).
    - Aucun prix hardcodé : `grep -v '^#' contexts/EntitlementContext.tsx | grep -c "29,99\|4,99"` == 0 (prix via priceString).
    - `grep -q "PURCHASE_CANCELLED_ERROR" contexts/EntitlementContext.tsx` (annulation silencieuse).
    - `npx tsc --noEmit` exit 0.
  </acceptance_criteria>
  <done>EntitlementContext.tsx créé : init RevenueCat + lecture statut + quota vault + grandfather one-shot + actions achat/restore/decrement + listener + hook, tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Monter EntitlementProvider dans app/_layout.tsx</name>
  <files>app/_layout.tsx</files>
  <read_first>
    - app/_layout.tsx (lignes 37-43 imports contextes ; lignes 308-352 hiérarchie providers — point d'insertion exact entre ThemeProvider ligne 312 et AIProvider ligne 313)
    - .planning/phases/54-monetisation-hybride-paiement/54-PATTERNS.md §app/_layout.tsx (lignes 580-611 : insertion + import)
    - .planning/phases/54-monetisation-hybride-paiement/54-RESEARCH.md §Pattern 8 (placement : après VaultProvider/ThemeProvider, avant AIProvider)
  </read_first>
  <action>
    Ajouter en tête de fichier (vers ligne 43, après les imports contextes existants) :
    ```
    import { EntitlementProvider } from '../contexts/EntitlementContext';
    ```
    Dans le JSX (hiérarchie lignes 312-348), insérer `<EntitlementProvider>` immédiatement APRÈS `<ThemeProvider>` (ligne 312) et AVANT `<AIProvider>` (ligne 313), avec la fermeture `</EntitlementProvider>` symétrique après `</AIProvider>` (ligne 347) et avant `</ThemeProvider>` (ligne 348). Conserver l'indentation existante. NE déplacer AUCUN autre provider.

    Hiérarchie cible :
    ```
    <ThemeProvider>
      <EntitlementProvider>
        <AIProvider>
          ...
        </AIProvider>
      </EntitlementProvider>
    </ThemeProvider>
    ```
  </action>
  <verify>
    <automated>grep -q "import { EntitlementProvider }" app/_layout.tsx && grep -q "<EntitlementProvider>" app/_layout.tsx && grep -q "</EntitlementProvider>" app/_layout.tsx && npx tsc --noEmit 2>&1 | grep -v '^#' | grep -c "_layout" | grep -q "^0$" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "<EntitlementProvider>" app/_layout.tsx` == 1 et `grep -c "</EntitlementProvider>" app/_layout.tsx` == 1.
    - L'ouverture `<EntitlementProvider>` apparaît APRÈS `<ThemeProvider>` et AVANT `<AIProvider>` (ordre vérifié visuellement).
    - Aucun autre provider déplacé (diff = seulement l'import + 2 lignes JSX ajoutées).
    - `npx tsc --noEmit` exit 0.
  </acceptance_criteria>
  <done>EntitlementProvider importé et monté entre ThemeProvider et AIProvider, ordre des autres providers inchangé, tsc clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Vérifier init + détection grandfather + achat sandbox sur device</name>
  <what-built>
    Task 1+2 ont créé EntitlementContext (init RevenueCat, lecture statut LIFETIME, lecture/écriture quota.md, détection grandfather one-shot, achat/restauration) et monté le provider. Le tsc est clean mais le comportement runtime RevenueCat + I/O vault ne se vérifie que sur device physique avec le sandbox configuré en Wave 1.
  </what-built>
  <how-to-verify>
    1. Lancer l'app sur l'iPhone physique (dev-client) avec un vault EXISTANT (déjà des tâches/profils). Au premier lancement : vérifier dans le vault Obsidian que `09 - Entitlements/quota.md` est créé avec `grandfather: true` (D-04/D-05 — utilisateur pré-payant détecté).
    2. Vérifier en logs DEV (`Purchases.setLogLevel DEBUG`) que `getCustomerInfo` répond et que le statut est `FREE` (aucun achat encore).
    3. Tester un achat sandbox du lifetime (depuis un écran de test temporaire appelant `purchaseLifetime`, ou attendre le paywall Wave 4) : le flux StoreKit sandbox s'ouvre, achat confirmé → status passe `LIFETIME` (listener temps réel).
    4. Tester « Restaurer mes achats » : doit retrouver le lifetime sandbox.
    5. Tester l'achat du Pack Histoires sandbox → vérifier que `story_credits` passe à 30 dans quota.md (RevenueCat ne gère pas le solde — Piège 2).
    6. Vérifier qu'AUCUNE donnée vault n'est transmise à RevenueCat (Dashboard RevenueCat → Customer : l'ID doit être anonyme `$RCAnonymousID`, pas un ID dérivé du contenu).
    7. Relancer l'app : `grandfather` ne doit PAS être re-détecté/réécrit (flag posé une seule fois — D-05).
  </how-to-verify>
  <resume-signal>Réponds "vérifié" si : quota.md créé avec grandfather correct + statut lu depuis RevenueCat + achat/restore sandbox OK + crédits +30 + ID anonyme RevenueCat + flag one-shot. Sinon décris l'écart (ex. faux négatif grandfather, achat sandbox échoue).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app → RevenueCat | events d'achat uniquement (D-02), statut = source de vérité (D-03) |
| app → vault quota.md | écriture du flag grandfather + solde crédits |
| init grandfather (one-shot) | décision irréversible (accès premium core à vie) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-07 | Spoofing | débloquer le premium sur un bool local falsifiable au lieu de RevenueCat | mitigate | status dérivé EXCLUSIVEMENT de `customerInfo.entitlements.active[ENTITLEMENT_PREMIUM]` (D-03) ; jamais d'écriture du statut LIFETIME dans le vault. |
| T-54-08 | Information disclosure | données vault envoyées à RevenueCat via appUserID | mitigate | `configure` sans appUserID → ID anonyme RevenueCat (D-02) ; vérifié en checkpoint (étape 6). |
| T-54-09 | Tampering | faux positif/négatif du flag grandfather (déblocage/verrouillage à tort) | mitigate | Détection only-when vault non-null (anti faux négatif iCloud Piège 3) + flag posé exactement une fois (jamais rejoué si quota.md existe) ; vérifié device (étapes 1,7). HIGH si raté → mitigation explicite obligatoire. |
| T-54-10 | Elevation of privilege | grandfather accordant l'IA illimitée gratuite (perte API) | mitigate | grandfather n'affecte QUE le premium core ; le quota IA reste géré par decrementQuota (status === 'LIFETIME' seul exempte, pas isGrandfathered) — D-06 tranché en faveur de la règle d'or. |
</threat_model>

<verification>
- `npx tsc --noEmit` exit 0 (non-cassant SC-6).
- useEntitlements exporté, provider monté entre ThemeProvider et AIProvider.
- Pas d'appUserID, pas de prix hardcodé, statut depuis RevenueCat.
- Checkpoint device : grandfather correct + achat/restore sandbox + ID anonyme + flag one-shot.
</verification>

<success_criteria>
- contexts/EntitlementContext.tsx fournit useEntitlements() avec statut FREE/LIFETIME + quota + actions achat/restauration (SC-1, SC-2).
- Statut d'achat = RevenueCat (D-03) ; quota/grandfather = vault (D-07) ; aucune fuite vault (D-02).
- Grandfather core uniquement (règle d'or préservée — SC-7).
- Provider monté sans casser la hiérarchie (SC-6).
</success_criteria>

<output>
After completion, create `.planning/phases/54-monetisation-hybride-paiement/54-03-SUMMARY.md`
</output>
