---
phase: 54-monetisation-hybride-paiement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - app.json
  - .env.example
autonomous: false
requirements:
  - "SC-2-revenuecat-sdk-native"
  - "SC-5-asc-products-sandbox"
user_setup:
  - service: revenuecat
    why: "Brique d'achat IAP (D-01) — source de vérité du statut d'achat (D-03)"
    env_vars:
      - name: EXPO_PUBLIC_RC_IOS_KEY
        source: "RevenueCat Dashboard → Project Settings → API Keys → clé publique iOS (préfixe appl_)"
    dashboard_config:
      - task: "Créer l'app iOS dans RevenueCat (bundle ID = celui d'ASC, vérifier correspondance exacte)"
        location: "RevenueCat Dashboard → Projects → Add app (App Store)"
      - task: "Ajouter App Store Connect API key (Issuer ID + Key ID + .p8)"
        location: "RevenueCat Dashboard → Project Settings → Integrations / App Store Connect"
      - task: "Créer 2 Products avec IDs identiques à ASC : familyflow_lifetime_v1 (non-consommable), familyflow_story_pack_30 (consommable)"
        location: "RevenueCat Dashboard → Products"
      - task: "Créer 1 Entitlement nommé familyflow_premium lié à familyflow_lifetime_v1 (nom englobe abo futur D-12)"
        location: "RevenueCat Dashboard → Entitlements"
      - task: "Créer 1 Offering 'default' (current) avec 2 packages : package id familyflow_lifetime_v1 (type LIFETIME) + package id familyflow_story_pack_30 (type CONSUMABLE)"
        location: "RevenueCat Dashboard → Offerings"
  - service: appstoreconnect
    why: "Produits IAP réels — sandbox testé avant release (SC-5)"
    dashboard_config:
      - task: "Créer In-App Purchase NON-CONSOMMABLE : « FamilyFlow à Vie », Product ID = familyflow_lifetime_v1, Tier ~29,99 € (Tier 29), descriptions FR + EN, cocher « disponible pour achat »"
        location: "App Store Connect (Team AKMNXGVVGX) → Mon app → In-App Purchases → (+)"
      - task: "Créer In-App Purchase CONSOMMABLE : « Pack Histoires 30 crédits », Product ID = familyflow_story_pack_30, Tier ~4,99 € (Tier 4), descriptions FR + EN"
        location: "App Store Connect → In-App Purchases → (+)"
      - task: "Créer un compte Sandbox Testeur (email @privaterelay) puis le connecter sur l'iPhone physique : Réglages → App Store → Compte Sandbox"
        location: "App Store Connect → Utilisateurs et accès → Testeurs Sandbox"

must_haves:
  truths:
    - "react-native-purchases est installé et le dev-client se lance sans crash NativeEventEmitter"
    - "Les 2 produits IAP existent dans App Store Connect avec les bons IDs et prix"
    - "Un compte sandbox permet de tester un achat sans débit réel"
    - "La clé RevenueCat iOS est disponible via EXPO_PUBLIC_RC_IOS_KEY"
  artifacts:
    - path: "package.json"
      provides: "Dépendance react-native-purchases@10.4.0"
      contains: "react-native-purchases"
    - path: ".env.example"
      provides: "Documentation de la variable EXPO_PUBLIC_RC_IOS_KEY"
      contains: "EXPO_PUBLIC_RC_IOS_KEY"
  key_links:
    - from: "package.json"
      to: "dev-client iOS"
      via: "npx expo run:ios --device après install (Piège 1)"
      pattern: "react-native-purchases"
---

<objective>
Installer la brique native RevenueCat (`react-native-purchases`) et configurer les 2 produits IAP côté App Store Connect + RevenueCat + sandbox. C'est la Wave 1 fondation : aucune logique applicative ici, uniquement le natif et la config service (D-01, D-03, SC-2, SC-5).

Purpose: Sans le module natif lié et sans produits ASC, aucun flux d'achat ne peut être testé. Cette phase pose le SDK + la config dashboard avant toute écriture de code entitlement.
Output: `react-native-purchases` installé, dev-client rebuildé, 2 produits ASC + entitlement + offering RevenueCat configurés, compte sandbox prêt, `EXPO_PUBLIC_RC_IOS_KEY` documentée.

App DÉJÀ PUBLIÉE — installation non-cassante : la dep s'auto-lie via autolinking, aucune feature existante touchée.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/54-monetisation-hybride-paiement/54-CONTEXT.md
@.planning/phases/54-monetisation-hybride-paiement/54-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Installer react-native-purchases + documenter la clé API</name>
  <files>package.json, .env.example</files>
  <read_first>
    - package.json (vérifier la version d'Expo SDK 54 / RN 0.81.5 et l'absence de react-native-purchases)
    - .planning/phases/54-monetisation-hybride-paiement/54-RESEARCH.md §Stack standard + §Piège 1 (rebuild dev-client) + §Question ouverte 1 (plugin Expo A1)
    - CLAUDE.md §Stack (dev-client requis, pas Expo Go)
    - app.json (vérifier le tableau `plugins` existant avant d'éventuellement y ajouter une entrée)
  </read_first>
  <action>
    Exécuter `npm install react-native-purchases@10.4.0` (version `latest` vérifiée par RESEARCH.md). Vérifier que `package.json` contient bien `"react-native-purchases": "10.4.0"` (ou `^10.4.0`) dans `dependencies`.

    Vérifier le besoin de plugin Expo (A1 RESEARCH.md) : lancer `npx expo prebuild --clean` (ou inspecter les warnings d'autolinking). Si un warning indique qu'une entrée plugin est requise, ajouter `"react-native-purchases"` dans le tableau `plugins` de `app.json`. Si l'autolinking suffit (cas attendu, comme expo-notifications), NE PAS ajouter d'entrée plugin superflue. Documenter le choix dans le SUMMARY.

    Créer ou mettre à jour `.env.example` (à la racine) avec une ligne documentant la variable :
    ```
    # RevenueCat — clé API publique iOS (préfixe appl_), récupérée dans le dashboard RevenueCat (Team Apple AKMNXGVVGX)
    EXPO_PUBLIC_RC_IOS_KEY=appl_xxxxxxxxxxxx
    ```
    NE PAS committer de vraie clé. La vraie valeur va dans `.env` (gitignored) et/ou EAS secrets.
  </action>
  <verify>
    <automated>grep -q "react-native-purchases" package.json && grep -q "EXPO_PUBLIC_RC_IOS_KEY" .env.example && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep "react-native-purchases" package.json` retourne la dépendance avec version 10.4.0.
    - `grep "EXPO_PUBLIC_RC_IOS_KEY" .env.example` retourne la ligne documentée.
    - Aucune vraie clé `appl_` réelle committée (la valeur dans `.env.example` est un placeholder `appl_xxxx`).
    - `npx tsc --noEmit` reste à exit code 0 (aucune régression de types).
  </acceptance_criteria>
  <done>react-native-purchases@10.4.0 dans package.json, EXPO_PUBLIC_RC_IOS_KEY documentée dans .env.example, choix plugin Expo tranché et noté.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Rebuild dev-client + config App Store Connect + RevenueCat + sandbox</name>
  <what-built>
    Task 1 a installé `react-native-purchases` et documenté la clé. Il reste 3 actions purement humaines (aucune CLI/API ne peut les faire) : rebuild du dev-client sur device physique, configuration des produits dans App Store Connect + RevenueCat Dashboard, et création du compte sandbox.
  </what-built>
  <how-to-verify>
    1. **Rebuild dev-client (OBLIGATOIRE — Piège 1 RESEARCH.md) :** lancer `npx expo run:ios --device`, sélectionner l'iPhone physique. L'app doit se lancer SANS le crash `Invariant Violation: 'new NativeEventEmitter()' requires a non-null argument`. Si ce crash apparaît, le module natif n'est pas lié → vérifier l'install + relancer le build.
    2. **App Store Connect (Team AKMNXGVVGX) :** créer les 2 produits IAP exactement avec ces Product IDs :
       - Non-consommable `familyflow_lifetime_v1` — « FamilyFlow à Vie » — Tier ~29,99 €
       - Consommable `familyflow_story_pack_30` — « Pack Histoires 30 crédits » — Tier ~4,99 €
       Ajouter descriptions FR + EN, prix localisé, cocher « disponible pour achat ».
    3. **RevenueCat Dashboard :** créer l'app iOS (bundle ID identique à ASC), ajouter l'ASC API key, créer les 2 Products (mêmes IDs), 1 Entitlement `familyflow_premium` lié à `familyflow_lifetime_v1`, 1 Offering `default` (current) avec 2 packages. Récupérer la clé publique iOS (`appl_…`) et la mettre dans `.env` (EXPO_PUBLIC_RC_IOS_KEY).
    4. **Sandbox :** créer un testeur Sandbox dans ASC (email @privaterelay), le connecter sur l'iPhone (Réglages → App Store → Compte Sandbox).
    5. **Vérif IDs :** confirmer que les Product IDs côté ASC et côté RevenueCat sont IDENTIQUES caractère par caractère, et que l'entitlement RevenueCat s'appelle bien `familyflow_premium` (sera lu en dur dans le code Wave 2/3).
  </how-to-verify>
  <resume-signal>Réponds "configuré" quand : dev-client se lance sans crash + 2 produits ASC créés + entitlement/offering RevenueCat créés + sandbox connecté + clé dans .env. Sinon décris le blocage (ex. produit en review, crash natif).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app iOS → serveur RevenueCat | events d'achat uniquement (D-02) ; reçus validés serveur-side (D-03) |
| app iOS → App Store / StoreKit | flux d'achat natif sandbox/prod |
| dépôt git → secrets | la clé RevenueCat ne doit jamais être committée |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-01 | Information disclosure | EXPO_PUBLIC_RC_IOS_KEY committée en clair | mitigate | Seul un placeholder `appl_xxxx` dans `.env.example` ; vraie valeur en `.env` gitignored + EAS secrets. La clé publique iOS RevenueCat est non-secrète côté client par design, mais on évite tout commit accidentel. |
| T-54-02 | Spoofing | bundle ID RevenueCat ≠ bundle ID ASC | mitigate | Vérification explicite (étape 5) que les Product IDs et bundle ID correspondent exactement — sinon validation des reçus échoue silencieusement. |
| T-54-03 | Tampering | données vault envoyées à RevenueCat | mitigate | Aucune config `appUserID` ici — ID anonyme RevenueCat (D-02). Renforcé en Wave 3 (configure sans appUserID). |
</threat_model>

<verification>
- `npx tsc --noEmit` exit 0 (non-cassant).
- `grep react-native-purchases package.json` présent.
- dev-client se lance sans crash natif (checkpoint humain).
- Produits ASC + entitlement RevenueCat + sandbox confirmés (checkpoint humain).
</verification>

<success_criteria>
- react-native-purchases@10.4.0 installé et lié (dev-client OK).
- 2 produits IAP créés dans ASC avec IDs `familyflow_lifetime_v1` + `familyflow_story_pack_30`.
- Entitlement `familyflow_premium` + Offering `default` configurés dans RevenueCat.
- Compte sandbox prêt sur device.
- `EXPO_PUBLIC_RC_IOS_KEY` documentée et renseignée.
</success_criteria>

<output>
After completion, create `.planning/phases/54-monetisation-hybride-paiement/54-01-SUMMARY.md`
</output>
