---
phase: 54-monetisation-hybride-paiement
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/entitlements/types.ts
  - lib/entitlements/entitlement-engine.ts
  - lib/entitlements/quota-parser.ts
  - lib/entitlements/index.ts
  - lib/entitlements/__tests__/entitlement-engine.test.ts
  - lib/entitlements/__tests__/quota-parser.test.ts
autonomous: true
requirements:
  - "SC-1-entitlements-infra-pure"
  - "SC-4-free-tier-cap-3-mois"
  - "SC-7-regle-or-ia-autofinancee"

must_haves:
  truths:
    - "canGenerateStory autorise exactement 3 histoires/mois en free tier, puis exige des crédits"
    - "Un utilisateur LIFETIME peut générer sans décrémenter (mais la règle d'or reste : voir D-06 tranché)"
    - "Le reset mensuel se déclenche quand storyResetMonth ≠ mois local courant"
    - "parseQuota/serializeQuota font un round-trip loss-less"
    - "detectGrandfatherEligibility retourne true ssi au moins un domaine vault a des données"
  artifacts:
    - path: "lib/entitlements/types.ts"
      provides: "EntitlementStatus, QuotaData, EntitlementState"
      contains: "EntitlementStatus"
    - path: "lib/entitlements/entitlement-engine.ts"
      provides: "logique pure canGenerateStory/decrementQuota/shouldResetMonth/detectGrandfatherEligibility"
      exports: ["canGenerateStory", "decrementQuota", "shouldResetMonth", "currentLocalMonth", "detectGrandfatherEligibility", "quotaExceededMessage"]
    - path: "lib/entitlements/quota-parser.ts"
      provides: "parseQuota/serializeQuota + QUOTA_FILE + DEFAULT_QUOTA"
      exports: ["parseQuota", "serializeQuota", "QUOTA_FILE", "DEFAULT_QUOTA"]
    - path: "lib/entitlements/index.ts"
      provides: "barrel re-export"
      contains: "export *"
  key_links:
    - from: "lib/entitlements/entitlement-engine.ts"
      to: "QuotaData"
      via: "import depuis types.ts"
      pattern: "QuotaData"
    - from: "lib/entitlements/__tests__"
      to: "entitlement-engine + quota-parser"
      via: "tests Jest non-régression"
      pattern: "canGenerateStory|parseQuota"
    - from: "lib/entitlements/index.ts"
      to: "contexts/EntitlementContext.tsx"
      via: "barrel consommé par le provider (Wave 2, Plan 54-03)"
      pattern: "lib/entitlements"
---

<objective>
Livrer la couche LOGIQUE PURE des entitlements : types partagés, moteur de quota testable (Jest), parser du fichier vault quota, et barrel. Zéro dépendance React, zéro RevenueCat, zéro I/O — uniquement des fonctions pures + parse/serialize de chaînes. Couvre SC-1 (infra entitlements), SC-4 (cap 3/mois), SC-7 (règle d'or IA).

Purpose: Isoler la logique critique (cap free tier, reset mensuel, décrémentation, détection grandfather) dans des fonctions pures testables AVANT tout câblage UI/contexte. C'est la couche où la règle d'or se vérifie (D-06, D-08, D-09).
Output: `lib/entitlements/` (types, engine, parser, barrel) + 2 suites Jest vertes.

Aucune dépendance native ici — ce plan est en Wave 1 et tourne en parallèle de 54-01 (zéro chevauchement de fichiers). Non-cassant : nouveau dossier isolé.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/54-monetisation-hybride-paiement/54-CONTEXT.md
@.planning/phases/54-monetisation-hybride-paiement/54-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- Contrats à produire — extraits du Pattern Map (54-PATTERNS.md). Pas d'exploration codebase nécessaire. -->

Analogue de référence (lib/elevenlabs-quota.ts) — exports existants à imiter en style :
  getDailyLimit, getDailyUsage, canConsume, recordConsumption, resetDailyQuota, quotaExceededError

Analogue parser (lib/parser.ts) :
  export function parseFrontmatter(content: string): { data: Record<string, string>; content: string }
  parseRDV / serializeRDV  (frontmatter construit ligne par ligne, PAS matter.stringify)

Types à créer (54-PATTERNS.md §types.ts) :
  export type EntitlementStatus = 'FREE' | 'LIFETIME';
  export interface QuotaData {
    grandfather: boolean;
    grandfatherDetectedAt: string;    // ISO ou ""
    storyCredits: number;             // solde Pack Histoires (D-07)
    storyUsedThisMonth: number;       // histoires générées ce mois (D-08)
    storyResetMonth: string;          // "YYYY-MM" mois LOCAL (D-08, Piège 7)
  }
  export interface EntitlementState {
    status: EntitlementStatus;
    isGrandfathered: boolean;
    quota: QuotaData;
    isReady: boolean;
    isLoadingPurchase: boolean;
  }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: types.ts + entitlement-engine.ts (logique pure) + tests Jest</name>
  <files>lib/entitlements/types.ts, lib/entitlements/entitlement-engine.ts, lib/entitlements/__tests__/entitlement-engine.test.ts</files>
  <read_first>
    - lib/elevenlabs-quota.ts (ANALOGUE EXACT — lignes 33-148 : pattern todayLocal, canConsume, recordConsumption, message d'erreur exporté)
    - .planning/phases/54-monetisation-hybride-paiement/54-PATTERNS.md §entitlement-engine.ts (lignes 65-156 : code complet canGenerateStory, decrementQuota, shouldResetMonth, detectGrandfatherEligibility, quotaExceededMessage)
    - .planning/phases/54-monetisation-hybride-paiement/54-CONTEXT.md (D-06 règle d'or, D-08 reset mensuel, D-09 seule génération décrémente)
    - .planning/phases/54-monetisation-hybride-paiement/54-RESEARCH.md §Piège 7 (reset UTC vs local) + §Pattern 7 (faux positifs grandfather)
  </read_first>
  <behavior>
    - currentLocalMonth() retourne le mois local au format "YYYY-MM" (date-fns format, PAS toISOString UTC — Piège 7).
    - shouldResetMonth("2026-05") = true si on est en juin ; shouldResetMonth(currentLocalMonth()) = false.
    - canGenerateStory(quota, hasLifetime=true) = true toujours.
    - canGenerateStory free tier : 3 slots gratuits/mois → true tant que storyUsedThisMonth < 3 (après reset éventuel) OU storyCredits > 0. Renvoie false quand 3/3 utilisés ET 0 crédit.
    - canGenerateStory free tier après reset : si storyResetMonth est un mois passé, considérer storyUsedThisMonth comme 0 → true.
    - decrementQuota free tier : épuise les crédits Pack EN PRIORITÉ (storyCredits-- si > 0), sinon storyUsedThisMonth++. Applique le reset mensuel si nécessaire (storyUsedThisMonth=0 + storyResetMonth=currentLocalMonth() avant incrément).
    - decrementQuota(quota, hasLifetime=true) = quota inchangé (LIFETIME ne décompte pas — D-06).
    - detectGrandfatherEligibility : true ssi tasks.length>0 OU meals.length>0 OU profiles.length>0 OU memories.length>0 ; false si tous vides.
    - quotaExceededMessage() retourne un message FR non vide invitant au Pack Histoires / lifetime.
  </behavior>
  <action>
    Créer `lib/entitlements/types.ts` avec EntitlementStatus, QuotaData, EntitlementState (cf. <interfaces>). Fichier types pur, zéro import runtime.

    Créer `lib/entitlements/entitlement-engine.ts` en copiant la logique de 54-PATTERNS.md §entitlement-engine.ts. Imports : `import { format } from 'date-fns';` et `import type { QuotaData } from './types';`. Exports requis :
    - `currentLocalMonth(): string` → `format(new Date(), 'yyyy-MM')`
    - `shouldResetMonth(storedMonth: string): boolean` → `storedMonth !== currentLocalMonth()`
    - `canGenerateStory(quota: QuotaData, hasLifetime: boolean): boolean`
    - `decrementQuota(quota: QuotaData, hasLifetime: boolean): QuotaData` (immutable, spread)
    - `detectGrandfatherEligibility(vaultState: { tasks: unknown[]; meals: unknown[]; profiles: unknown[]; memories: unknown[] }): boolean`
    - `quotaExceededMessage(): string` (FR)
    Constante `FREE_STORIES_PER_MONTH = 3` en haut de fichier (D-08 / SC-4).

    Vérifier que `date-fns` est déjà dispo (analogue elevenlabs-quota l'utilise via le pattern). Si absent, utiliser un format local manuel : `const d = new Date(); \`${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}\`` (heure LOCALE, pas UTC).

    Créer `lib/entitlements/__tests__/entitlement-engine.test.ts` couvrant TOUS les cas <behavior>. Pour tester le reset sans dépendre de l'horloge, injecter des mois figés (ex. `shouldResetMonth('2000-01')` toujours true). Pour les cas datant du mois courant, utiliser `currentLocalMonth()`.
  </action>
  <verify>
    <automated>npx jest --no-coverage --testPathPattern="entitlement-engine" 2>&1 | grep -v '^#' | grep -q "PASS" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `npx jest --no-coverage --testPathPattern="entitlement-engine"` : suite PASS, ≥ 8 tests (cap 3/mois, reset, priorité crédits, lifetime no-decrement, grandfather true/false).
    - `grep -c "export" lib/entitlements/entitlement-engine.ts` (hors commentaires) ≥ 6 fonctions exportées.
    - `npx tsc --noEmit` exit 0.
    - Aucun usage de `toISOString()` pour le calcul du mois (vérif : `grep -v '^#' lib/entitlements/entitlement-engine.ts | grep -c toISOString` == 0).
  </acceptance_criteria>
  <done>types.ts + entitlement-engine.ts créés, suite Jest engine verte couvrant cap/reset/priorité/lifetime/grandfather, tsc clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: quota-parser.ts (parse/serialize vault) + tests round-trip + barrel</name>
  <files>lib/entitlements/quota-parser.ts, lib/entitlements/index.ts, lib/entitlements/__tests__/quota-parser.test.ts</files>
  <read_first>
    - lib/parser.ts (lignes 223-260 parseFrontmatter ; lignes 361-418 parseRDV/serializeRDV — pattern frontmatter ligne par ligne)
    - .planning/phases/54-monetisation-hybride-paiement/54-PATTERNS.md §quota-parser.ts (lignes 160-218 : code parseQuota/serializeQuota + constante QUOTA_FILE) et §index.ts (barrel)
    - lib/gamification/index.ts (pattern barrel : export * uniquement, pas d'export default)
    - .planning/phases/54-monetisation-hybride-paiement/54-CONTEXT.md (D-07 fichier vault frontmatter dédié)
  </read_first>
  <behavior>
    - parseQuota(serializeQuota(q)) === q pour un q arbitraire (round-trip loss-less, tous les champs).
    - parseQuota("") (fichier vide/absent) retourne DEFAULT_QUOTA (grandfather:false, "", storyCredits:0, storyUsedThisMonth:0, storyResetMonth:"").
    - parseQuota coerce défensivement : story_credits string "30" → number 30 ; grandfather "true" string ou true bool → boolean true.
    - serializeQuota produit un frontmatter valide entre --- --- avec les 5 clés + tags entitlements + corps documentaire « ne pas modifier manuellement ».
  </behavior>
  <action>
    Créer `lib/entitlements/quota-parser.ts` en copiant 54-PATTERNS.md §quota-parser.ts :
    - `import { parseFrontmatter } from '../parser';` et `import type { QuotaData } from './types';`
    - Constante `export const QUOTA_FILE = '09 - Entitlements/quota.md';`
    - Constante `export const DEFAULT_QUOTA: QuotaData = { grandfather: false, grandfatherDetectedAt: '', storyCredits: 0, storyUsedThisMonth: 0, storyResetMonth: '' };`
    - `export function parseQuota(content: string): QuotaData` — coercions défensives (Number()||0, === 'true' || === true, String(... ?? '')). Si content vide → DEFAULT_QUOTA.
    - `export function serializeQuota(q: QuotaData): string` — frontmatter construit ligne par ligne (PAS matter.stringify, cf. serializeRDV), clés : grandfather, grandfather_detected_at, story_credits, story_used_this_month, story_reset_month + `tags:\n  - entitlements` + corps « > Géré automatiquement par FamilyFlow — ne pas modifier manuellement. ».

    Créer `lib/entitlements/index.ts` (barrel) :
    ```
    export * from './types';
    export * from './entitlement-engine';
    export * from './quota-parser';
    ```

    Créer `lib/entitlements/__tests__/quota-parser.test.ts` : round-trip loss-less sur ≥ 2 objets QuotaData distincts (un avec grandfather true + crédits, un par défaut), parse de fichier vide → DEFAULT_QUOTA, coercion string→number.
  </action>
  <verify>
    <automated>npx jest --no-coverage --testPathPattern="quota-parser" 2>&1 | grep -v '^#' | grep -q "PASS" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `npx jest --no-coverage --testPathPattern="quota-parser"` : suite PASS, round-trip vérifié.
    - `grep -q "export const QUOTA_FILE = '09 - Entitlements/quota.md'" lib/entitlements/quota-parser.ts`.
    - `grep -c "export \*" lib/entitlements/index.ts` == 3.
    - serializeQuota n'utilise PAS matter.stringify (`grep -v '^#' lib/entitlements/quota-parser.ts | grep -c "matter.stringify"` == 0).
    - `npx tsc --noEmit` exit 0.
  </acceptance_criteria>
  <done>quota-parser.ts + index.ts créés, suite Jest round-trip verte, barrel re-exporte les 3 modules, tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fichier vault quota.md → app | entrée parsée (utilisateur peut éditer le fichier dans Obsidian) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-04 | Tampering | édition manuelle de quota.md (crédits gonflés, reset triché) dans Obsidian | accept | App familiale solo, sans backend ; le modèle de menace réaliste est faible (l'utilisateur ne se triche que lui-même). La règle d'or reste protégée car LIFETIME vient de RevenueCat (D-03), pas du vault. Sur-sécuriser un fichier local serait disproportionné (RESEARCH.md §Domaine sécurité). |
| T-54-05 | Tampering | valeurs corrompues dans le frontmatter (NaN, types invalides) | mitigate | parseQuota coerce défensivement (Number()||0, fallback DEFAULT_QUOTA) — testé Jest. V5 ASVS (validation des entrées). |
| T-54-06 | Elevation of privilege | crédit IA illimité gratuit via bug de logique | mitigate | canGenerateStory/decrementQuota testés exhaustivement : cap dur 3/mois, priorité crédits, pas de chemin retournant true sans slot/crédit/lifetime (règle d'or SC-7). |
</threat_model>

<verification>
- `npx jest --no-coverage --testPathPattern="entitlement"` : 2 suites PASS.
- `npx tsc --noEmit` exit 0.
- Barrel exporte types + engine + parser.
- Aucun toISOString pour le mois ; aucun matter.stringify.
</verification>

<success_criteria>
- lib/entitlements/ contient types, engine, parser, barrel (SC-1).
- Cap dur 3 histoires/mois encodé et testé (SC-4).
- Règle d'or : aucun chemin n'autorise une génération sans slot/crédit/lifetime (SC-7).
- Tests Jest verts, tsc clean (SC-6 non-cassant).
</success_criteria>

<output>
After completion, create `.planning/phases/54-monetisation-hybride-paiement/54-02-SUMMARY.md`
</output>
