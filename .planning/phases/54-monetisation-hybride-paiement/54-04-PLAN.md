---
phase: 54-monetisation-hybride-paiement
plan: 04
type: execute
wave: 4
depends_on: ["54-03"]
files_modified:
  - components/paywalls/PaywallModal.tsx
  - components/paywalls/PremiumBanner.tsx
  - components/paywalls/index.ts
  - app/(tabs)/stories.tsx
autonomous: false
requirements:
  - "SC-3-paywall-pagesheet"
  - "SC-4-feature-gate-cap-3-mois"
  - "SC-7-regle-or-ia-autofinancee"

must_haves:
  truths:
    - "Le paywall s'ouvre en pageSheet + drag-to-dismiss avec useThemeColors (zéro hardcoded), prix localisés RevenueCat"
    - "Le paywall propose lifetime + Pack Histoires + bouton « Restaurer mes achats »"
    - "Le paywall n'apparaît JAMAIS au lancement — uniquement au point de friction (après la 3e histoire du mois)"
    - "Tenter de générer une 4e histoire gratuite déclenche le paywall au lieu d'appeler l'API (cap dur)"
    - "Après génération réussie, le quota est décrémenté (D-09, après succès uniquement)"
    - "La relecture d'un MP3 en cache ne décrémente jamais le quota (D-09)"
  artifacts:
    - path: "components/paywalls/PaywallModal.tsx"
      provides: "Modal pageSheet achat lifetime/pack + restauration"
      contains: "presentationStyle=\"pageSheet\""
    - path: "components/paywalls/PremiumBanner.tsx"
      provides: "Bannière aperçu + CTA (mode D-11)"
      contains: "PremiumBanner"
    - path: "components/paywalls/index.ts"
      provides: "barrel paywalls"
      contains: "export"
  key_links:
    - from: "app/(tabs)/stories.tsx"
      to: "useEntitlements().canGenerateStory"
      via: "gate avant generateBedtimeStory (ligne ~1958)"
      pattern: "canGenerateStory"
    - from: "app/(tabs)/stories.tsx"
      to: "useEntitlements().decrementStoryQuota"
      via: "appel après succès génération + saveStory"
      pattern: "decrementStoryQuota"
    - from: "PaywallModal.tsx"
      to: "useEntitlements().purchaseLifetime/purchaseStoryPack/restorePurchases"
      via: "boutons CTA"
      pattern: "purchaseLifetime|purchaseStoryPack|restorePurchases"
---

<objective>
Livrer la couche UI + le gate de friction : `PaywallModal` (pageSheet chaleureux, prix localisés, achat lifetime/pack + restauration), `PremiumBanner` (aperçu + CTA D-11), et le branchement du cap dur sur la génération d'histoires dans `app/(tabs)/stories.tsx` (gate avant API + décrément après succès). Couvre SC-3 (paywall), SC-4 (cap dur), SC-7 (règle d'or appliquée au point de génération).

Purpose: C'est la concrétisation visible du modèle : l'utilisateur free voit le paywall UNIQUEMENT quand il atteint la 3e histoire du mois (D-10, anti dark pattern), la relecture cache reste gratuite (D-09).
Output: `components/paywalls/` (modal + banner + barrel) + gate/décrément câblés dans stories.tsx.

App DÉJÀ PUBLIÉE — gate non-cassant : pour un utilisateur LIFETIME ou grandfathered+slots, le flux génération est INCHANGÉ. Le paywall n'apparaît jamais au lancement (Piège 4 — rejet App Store).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/54-monetisation-hybride-paiement/54-CONTEXT.md
@.planning/phases/54-monetisation-hybride-paiement/54-RESEARCH.md
@.planning/phases/54-monetisation-hybride-paiement/54-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- Contrats consommés — extraits du codebase + Wave 3. Pas d'exploration nécessaire. -->

Depuis contexts/EntitlementContext.tsx (Wave 3) :
  useEntitlements(): {
    status: 'FREE' | 'LIFETIME'; isGrandfathered; quota; isReady; isLoadingPurchase;
    canGenerateStory: () => boolean;
    purchaseLifetime: () => Promise<void>;
    purchaseStoryPack: () => Promise<void>;
    restorePurchases: () => Promise<void>;
    decrementStoryQuota: () => Promise<void>;
    lifetimePrice: string;  // priceString localisé — afficher tel quel
    packPrice: string;
  }

Briques UI (components/ui/index.ts) : ModalHeader, Button, Chip, Badge, CollapsibleSection.
Thème : useThemeColors() de '../../contexts/ThemeContext' → { primary, tint, colors, isDark }
  colors.* : text, textMuted, bg, card, border, error, errorBg, onPrimary
Tokens : Spacing (constants/spacing), FontSize/FontWeight (constants/typography), Radius.
Haptics : expo-haptics — Haptics.selectionAsync(), Haptics.impactAsync(Haptics.ImpactFeedbackStyle.Medium).

stories.tsx (point de gate — déjà lu) :
  - ligne 878 : const { config: aiConfig, storyConfig } = useAI();
  - ligne 883 : destructure ... saveStory, deleteStory ... from useVault()
  - ligne 1958 : const resp = await generateBedtimeStory(storyConfig ?? aiConfig, genParams);  ← gate AVANT cette ligne
  - ligne 2151 : 2e appel generateBedtimeStory (chapitre/2e génération) ← gate AVANT aussi
  - saveStory(finalStory) appelé après succès (lignes ~2199+) ← decrement après ce succès
  - La RELECTURE d'un MP3 en cache ne passe PAS par generateBedtimeStory → ne PAS gater (D-09).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PaywallModal.tsx + PremiumBanner.tsx + barrel</name>
  <files>components/paywalls/PaywallModal.tsx, components/paywalls/PremiumBanner.tsx, components/paywalls/index.ts</files>
  <read_first>
    - components/pdf/BookExportModal.tsx (ANALOGUE EXACT modal : lignes 11-44 imports, structure Modal pageSheet > SafeAreaView > ModalHeader + ScrollView + footer ; ligne 282 haptics)
    - components/ui/Button.tsx (lignes 1-76 : pattern React.memo, useThemeColors, props label/onPress/variant/fullWidth/size)
    - app/dietary.tsx (lignes 303-315 : Modal presentationStyle pageSheet + onRequestClose)
    - .planning/phases/54-monetisation-hybride-paiement/54-PATTERNS.md §PaywallModal.tsx (lignes 393-479) + §PremiumBanner.tsx (lignes 483-523) + §Patterns transversaux (useThemeColors, Modal pageSheet, Haptics, StyleSheet)
    - .planning/phases/54-monetisation-hybride-paiement/54-CONTEXT.md (D-11 aperçu+CTA, Claude's Discretion DA chaleureuse + prix localisés ; D-10 jamais au lancement)
    - CLAUDE.md (français, useThemeColors JAMAIS hardcoded, modals pageSheet + drag-to-dismiss)
  </read_first>
  <action>
    **`components/paywalls/PaywallModal.tsx`** (analogue BookExportModal) :
    - Props : `{ visible: boolean; onClose: () => void; context?: 'story_limit' | 'premium_feature' }` (context pour adapter le titre/sous-titre).
    - Imports : React/useCallback, RN (Modal, View, Text, ScrollView, StyleSheet, ActivityIndicator), SafeAreaView, expo-haptics, useThemeColors, useEntitlements, ModalHeader + Button depuis '../ui', Spacing/Radius, FontSize/FontWeight.
    - Structure : `<Modal visible animationType="slide" presentationStyle="pageSheet" onRequestClose={onClose}>` > `<SafeAreaView style={{ flex: 1, backgroundColor: colors.bg }}>` > `<ModalHeader title="FamilyFlow à Vie" onClose={onClose} />` > `<ScrollView contentContainerStyle={styles.content}>` (liste de bénéfices premium en FR, ton chaleureux slice-of-life : budget avancé, repas/recettes illimités, export livres PDF, mascotte/ferme/village avancés, carnet santé/courbes/grossesse, Lightning Wallet — chacun en featureRow avec une icône lucide/check) > footer.
    - Footer (`<View style={[styles.footer, { borderTopColor: colors.border }]}>`) :
      - Bloc lifetime : libellé « Payez une fois. Pour toujours. » + prix `{lifetimePrice || '…'}` (JAMAIS hardcodé) + `<Button label="Obtenir FamilyFlow à Vie" onPress={handleLifetime} fullWidth size="lg" />`.
      - Bloc Pack Histoires : « Pack Histoires — 30 histoires » + prix `{packPrice || '…'}` + `<Button label="Acheter un Pack Histoires" onPress={handlePack} variant="secondary" fullWidth />`.
      - `<Button label="Restaurer mes achats" onPress={handleRestore} variant="ghost" fullWidth />` (OBLIGATOIRE Apple 3.1.1).
    - Handlers : `handleLifetime` → Haptics.impactAsync(Medium) + `await purchaseLifetime()` + si status devient LIFETIME → onClose(). `handlePack` → Haptics + purchaseStoryPack. `handleRestore` → restorePurchases. Désactiver les boutons / afficher ActivityIndicator quand `isLoadingPurchase`.
    - Styles statiques en `StyleSheet.create` en bas (content padding Spacing['3xl'] gap Spacing['2xl'] ; footer padding Spacing['2xl'] gap Spacing.lg borderTopWidth 1 ; featureRow flexDirection row alignItems center gap Spacing.lg). Couleurs UNIQUEMENT inline via colors.*.

    **`components/paywalls/PremiumBanner.tsx`** (analogue Button.tsx — 54-PATTERNS.md §PremiumBanner) :
    - `export const PremiumBanner = React.memo(function PremiumBanner({ message, ctaLabel = 'Voir les offres', onPress }: { message: string; ctaLabel?: string; onPress: () => void }) {...})`.
    - useThemeColors → TouchableOpacity (activeOpacity 0.8, accessibilityRole button) affichant message + cta, fond `tint`, bordure `primary`, texte `colors.text`, cta `primary`. Styles statiques en bas.

    **`components/paywalls/index.ts`** (barrel) :
    ```
    export * from './PaywallModal';
    export * from './PremiumBanner';
    ```
    (PaywallModal exporté nommé : `export function PaywallModal(...)` ou `export const PaywallModal`.)
  </action>
  <verify>
    <automated>grep -q 'presentationStyle="pageSheet"' components/paywalls/PaywallModal.tsx && grep -q "Restaurer mes achats" components/paywalls/PaywallModal.tsx && grep -q "PremiumBanner" components/paywalls/PremiumBanner.tsx && npx tsc --noEmit 2>&1 | grep -v '^#' | grep -c "paywalls" | grep -q "^0$" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'presentationStyle="pageSheet"' components/paywalls/PaywallModal.tsx` (SC-3).
    - `grep -q "Restaurer mes achats" components/paywalls/PaywallModal.tsx` (Apple 3.1.1).
    - `grep -q "lifetimePrice" components/paywalls/PaywallModal.tsx` (prix dynamique).
    - Zéro hardcoded : `grep -v '^#' components/paywalls/PaywallModal.tsx | grep -c "#[0-9A-Fa-f]\{6\}\|29,99\|4,99"` == 0.
    - `grep -q "useThemeColors" components/paywalls/PaywallModal.tsx` et idem PremiumBanner.tsx.
    - `grep -c "export \*" components/paywalls/index.ts` == 2.
    - `npx tsc --noEmit` exit 0.
  </acceptance_criteria>
  <done>PaywallModal (pageSheet, prix localisés, lifetime+pack+restore), PremiumBanner (memo), barrel — useThemeColors partout, zéro hardcoded, tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Gate la génération d'histoires + décrément après succès dans stories.tsx</name>
  <files>app/(tabs)/stories.tsx</files>
  <read_first>
    - app/(tabs)/stories.tsx (ligne 878 useAI ; ligne 883 destructure useVault ; lignes 1930-1965 genParams + 1er generateBedtimeStory + gestion resp.error ; ligne 2151 2e generateBedtimeStory ; lignes ~2195-2220 saveStory après succès)
    - .planning/phases/54-monetisation-hybride-paiement/54-PATTERNS.md §useVaultStories.ts modification + §AIContext.tsx (note : ne pas appeler un hook dans une callback — lire l'état via le hook au niveau composant, gater dans le handler)
    - .planning/phases/54-monetisation-hybride-paiement/54-CONTEXT.md (D-09 seule la génération décrémente, relecture cache gratuite ; D-10 paywall au point de friction ; D-06 règle d'or)
    - .planning/phases/54-monetisation-hybride-paiement/54-RESEARCH.md §Piège 6 (décrémenter APRÈS succès API, jamais avant)
  </read_first>
  <action>
    Dans le composant écran (top-level, à côté de la ligne 878 `useAI()`), ajouter :
    ```
    import { useEntitlements } from '../../contexts/EntitlementContext';
    import { PaywallModal } from '../../components/paywalls';
    ...
    const { canGenerateStory, decrementStoryQuota } = useEntitlements();
    const [paywallVisible, setPaywallVisible] = useState(false);
    ```

    **Gate (cap dur, D-10/SC-4)** : juste AVANT chaque appel à `generateBedtimeStory` (lignes ~1958 ET ~2151), insérer :
    ```
    if (!canGenerateStory()) {
      Haptics.notificationAsync?.(Haptics.NotificationFeedbackType.Warning);
      setPaywallVisible(true);
      return;   // ne PAS appeler l'API (cap dur — règle d'or SC-7)
    }
    ```
    Le `return` doit interrompre proprement le handler de génération en cours (réinitialiser tout flag `isGenerating`/loading déjà posé avant ce point — vérifier l'ordre et placer le gate AVANT de set un état "génération en cours", sinon réinitialiser dans le bloc).

    **Décrément (APRÈS succès, D-09/Piège 6)** : après que la génération a réussi ET que `saveStory(...)` a été déclenché (succès de l'écriture vault), appeler `decrementStoryQuota().catch(() => { /* non-critique */ });`. Le décrément ne doit PAS bloquer l'affichage de l'histoire. Placer après le `saveStory(finalStory)` du flux de génération principal (et du flux chapitre si applicable). NE JAMAIS décrémenter dans le chemin de RELECTURE d'un MP3 en cache (qui ne passe pas par generateBedtimeStory — D-09).

    **Rendu du paywall** : ajouter `<PaywallModal visible={paywallVisible} onClose={() => setPaywallVisible(false)} context="story_limit" />` dans le JSX de l'écran (au même niveau que les autres modals de l'écran).

    NE PAS gater pour les utilisateurs LIFETIME (canGenerateStory renvoie true → flux inchangé). NE PAS ajouter de paywall au mount/lancement (Piège 4).
  </action>
  <verify>
    <automated>grep -q "canGenerateStory" "app/(tabs)/stories.tsx" && grep -q "decrementStoryQuota" "app/(tabs)/stories.tsx" && grep -q "PaywallModal" "app/(tabs)/stories.tsx" && npx tsc --noEmit 2>&1 | grep -v '^#' | grep -c "stories.tsx" | grep -q "^0$" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "canGenerateStory()" "app/(tabs)/stories.tsx"` ≥ 2 (gate avant chaque generateBedtimeStory).
    - `grep -q "setPaywallVisible(true)" "app/(tabs)/stories.tsx"` (friction → paywall).
    - `grep -q "decrementStoryQuota" "app/(tabs)/stories.tsx"` (décrément après succès).
    - Le décrément apparaît APRÈS un appel saveStory dans le flux (vérif manuelle de l'ordre — Piège 6).
    - `<PaywallModal` monté dans le JSX, jamais au mount (pas dans un useEffect [] qui ouvre le paywall).
    - `npx tsc --noEmit` exit 0.
  </acceptance_criteria>
  <done>Cap dur câblé : 4e histoire gratuite/mois → paywall au lieu de l'API ; décrément après succès uniquement ; relecture cache gratuite ; LIFETIME inchangé ; tsc clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Vérifier le flux de friction bout-en-bout sur device</name>
  <what-built>
    Task 1+2 ont livré le paywall pageSheet (lifetime/pack/restore, prix localisés) et le cap dur sur la génération d'histoires (gate avant API, décrément après succès, paywall au point de friction). tsc est clean mais le déclenchement réel du paywall, le décompte du quota et la gratuité de la relecture cache ne se vérifient qu'en utilisant l'app.
  </what-built>
  <how-to-verify>
    1. Avec un compte FREE (pas grandfathered — pour tester, mettre temporairement `grandfather: false` et `story_used_this_month: 0` dans quota.md), générer 3 histoires : chacune doit réussir et `story_used_this_month` s'incrémenter à 1, 2, 3 dans quota.md APRÈS chaque succès (Piège 6).
    2. Tenter une 4e génération : le PaywallModal doit s'ouvrir (pageSheet, drag-to-dismiss fonctionne), AUCUN appel API, prix affichés depuis le store (pas '…' si offerings dispo, pas de « 29,99 € » hardcodé).
    3. Relire une histoire déjà générée (MP3 en cache) : doit fonctionner librement, `story_used_this_month` NE bouge PAS (D-09 — relecture gratuite).
    4. Dans le paywall : tester achat lifetime sandbox → après succès, status LIFETIME → générer une 5e histoire : doit réussir sans décrémenter (D-06 — mais reste auto-financé côté logique).
    5. Vérifier qu'aucun paywall n'apparaît au lancement de l'app (D-10/Piège 4).
    6. Vérifier visuellement la DA : `useThemeColors` respecté en mode clair ET sombre (aucune couleur figée), ton chaleureux FR.
  </how-to-verify>
  <resume-signal>Réponds "approuvé" si : 3 histoires OK + décompte après succès + 4e → paywall sans API + relecture cache gratuite + achat lifetime débloque + zéro paywall au lancement + DA clair/sombre OK. Sinon décris l'écart (ex. décompte avant succès, paywall au mount, couleur hardcodée).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| handler génération → API Anthropic | gate de quota avant l'appel coûteux |
| paywall → RevenueCat | déclenchement des achats |
| relecture cache → quota | chemin qui NE doit PAS décrémenter |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-11 | Elevation of privilege | générer au-delà du cap free tier sans payer (perte API — règle d'or) | mitigate | Gate `if (!canGenerateStory()) return;` AVANT chaque generateBedtimeStory ; le `return` empêche tout appel API. Vérifié device (étape 2). HIGH → mitigation obligatoire. |
| T-54-12 | Denial of service / coût | décrément avant succès pénalise l'utilisateur sur erreur réseau | mitigate | decrementStoryQuota appelé APRÈS succès génération + saveStory (Piège 6) ; erreur API → quota inchangé. |
| T-54-13 | Tampering | relecture cache facturée par erreur (mauvaise UX) ou génération non comptée | mitigate | Décrément uniquement dans le chemin generateBedtimeStory ; relecture MP3 cache n'y passe pas (D-09). Vérifié device (étape 3). |
| T-54-14 | — (App Store compliance) | paywall au lancement → rejet Apple | mitigate | Paywall jamais monté au mount/useEffect [] ; uniquement sur setPaywallVisible au point de friction (D-10/Piège 4) ; bouton Restaurer présent (3.1.1). |
</threat_model>

<verification>
- `npx tsc --noEmit` exit 0 (non-cassant SC-6).
- Paywall pageSheet + restore + prix localisés (SC-3).
- Gate avant chaque generateBedtimeStory + décrément après succès (SC-4, SC-7).
- Checkpoint device : friction → paywall, décompte après succès, relecture cache gratuite, zéro paywall au lancement.
</verification>

<success_criteria>
- components/paywalls/ : PaywallModal (pageSheet, useThemeColors, prix localisés, lifetime+pack+restore) + PremiumBanner (SC-3).
- Cap dur 3 histoires/mois appliqué au point de génération ; 4e → paywall (SC-4).
- Décrément après succès uniquement, relecture cache gratuite (SC-7, D-09).
- LIFETIME/grandfathered : flux inchangé, aucune régression (SC-6).
</success_criteria>

<output>
After completion, create `.planning/phases/54-monetisation-hybride-paiement/54-04-SUMMARY.md`
</output>
