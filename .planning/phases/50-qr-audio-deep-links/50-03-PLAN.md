---
phase: 50-qr-audio-deep-links
plan: 03
type: execute
wave: 2
depends_on: [50-01]
files_modified:
  - package.json
  - lib/pdf/qr-generator.ts
  - lib/pdf/__tests__/qr-generator.test.ts
  - lib/pdf/types.ts
  - lib/pdf/components/back-cover.ts
  - lib/pdf/html-template.ts
  - lib/pdf/pdf-generator.ts
  - lib/pdf/index.ts
autonomous: true
requirements: [QR-04]
must_haves:
  truths:
    - "`qrcode` v1.5.4 installé en dependency + `@types/qrcode` en devDependency"
    - "`generateStoryQrSvg(storyId, palette)` retourne une string SVG inline déterministe encodant `family-vault://story/<id>`"
    - "Le placeholder `<div data-phase50>` dans `back-cover.ts:25` est REMPLACÉ par le SVG QR (3×3cm) + légende FR `Scanne pour écouter l'histoire`"
    - "Le QR SVG est généré en parallèle des fonts et illustrations dans `pdf-generator.ts` (Promise.all)"
    - "Le hash SHA-256 du HTML reste DÉTERMINISTE pour un même `storyId` (qrcode produit un SVG identique pour input identique)"
    - "`renderBackCoverPage` accepte le nouveau paramètre `qrSvg: string` ; les deux call-sites html-template.ts:173 et 237 (mode A et mode B) passent le SVG"
  artifacts:
    - path: "lib/pdf/qr-generator.ts"
      provides: "`async function generateStoryQrSvg(storyId, palette): Promise<string>` retournant SVG inline 300×300, ECC M, color.dark = palette.ink, color.light transparent"
    - path: "lib/pdf/__tests__/qr-generator.test.ts"
      provides: "Tests Jest : SVG valide (`<svg`), determinisme (deux appels = même string), encodage scheme correct (contient `family-vault://story/`)"
    - path: "lib/pdf/components/back-cover.ts"
      provides: "Signature `BackCoverOpts.qrSvg: string` ajoutée ; placeholder remplacé par `${qrSvg}` + légende FR"
    - path: "lib/pdf/html-template.ts"
      provides: "BookHtmlSpec étendu avec `qrSvg: string` ; passage à renderBackCoverPage aux 2 call-sites"
    - path: "lib/pdf/pdf-generator.ts"
      provides: "Appel `generateStoryQrSvg` ajouté dans Promise.all + transmission à renderBookHtml"
  key_links:
    - from: "lib/pdf/pdf-generator.ts"
      to: "lib/pdf/qr-generator.ts"
      via: "await Promise.all([loadFontsBase64(), generateStoryQrSvg(opts.story.id, BOOK_PALETTE), ...illu])"
    - from: "lib/pdf/html-template.ts"
      to: "lib/pdf/components/back-cover.ts"
      via: "renderBackCoverPage({ story, palette, qrSvg }) — call-sites lignes 173 + 237"
    - from: "lib/pdf/components/back-cover.ts"
      to: "lib/pdf/qr-generator.ts (output)"
      via: "injection inline du SVG string dans <div style='width:3cm;height:3cm'>${qrSvg}</div>"
---

<objective>
Installer `qrcode` v1.5.4 + types, créer `lib/pdf/qr-generator.ts` qui produit un SVG QR code déterministe encodant `family-vault://story/<id>`, et l'intégrer dans le pipeline PDF Phase 49 en remplaçant le placeholder `<div data-phase50>` de `back-cover.ts:25` par le SVG réel + légende FR `Scanne pour écouter l'histoire` (RESEARCH.md §155-221).

Purpose : sans cette étape, le PDF Phase 49 contient un placeholder visuel mais aucun QR scannable. C'est le pont physique → numérique du livre imprimé : un parent imprime le livre chez Lulu, le QR est scanné par l'enfant, la route Plan 50-02 prend le relais.

Critique : préserver le **déterminisme du hash SHA-256** (Phase 49-03) — `qrcode` produit un SVG identique pour mêmes inputs, donc deux exports d'un même story.id génèrent le même PDF (RESEARCH.md §339-342).

Output : 1 lib nouvelle + 1 suite test + 4 fichiers pipeline modifiés + 1 dependency ajoutée.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/50-qr-audio-deep-links/50-CONTEXT.md
@.planning/phases/50-qr-audio-deep-links/50-RESEARCH.md
@.planning/phases/49-layout-livre-generation-pdf/49-PHASE-SUMMARY.md
@.planning/phases/49-layout-livre-generation-pdf/49-03-SUMMARY.md
@lib/pdf/constants.ts
@lib/pdf/types.ts
@lib/pdf/pdf-generator.ts
@lib/pdf/html-template.ts
@lib/pdf/components/back-cover.ts
@lib/pdf/index.ts
@CLAUDE.md

<interfaces>
<!-- API qrcode 1.5.4 (vérifié RESEARCH.md §155-180) -->
```typescript
import QRCode from 'qrcode';
const svg: string = await QRCode.toString(text, {
  type: 'svg',
  errorCorrectionLevel: 'M',
  margin: 1,
  width: 300,
  color: { dark: '#2B2A28', light: '#00000000' }, // light transparent
});
// → '<svg xmlns="http://www.w3.org/2000/svg" viewBox="..." width="300" height="300">...</svg>'
```

<!-- État actuel back-cover.ts (vérifié) -->
```typescript
// Signature actuelle :
export interface BackCoverOpts { story: BedtimeStory; palette: BookPalette; }
export function renderBackCoverPage(opts: BackCoverOpts): string;
// Ligne 25 actuelle : placeholder à remplacer
<div class="qr-placeholder" data-phase50 ...>QR — Phase 50</div>
```

<!-- État actuel html-template.ts (vérifié — call-sites mode A et mode B) -->
```typescript
// Ligne 173 (renderModeAPages) :
pages.push(renderBackCoverPage({ story: spec.story, palette: spec.palette }));
// Ligne 237 (renderModeBPages) :
pages.push(renderBackCoverPage({ story: spec.story, palette: spec.palette }));
// Interface BookHtmlSpec ligne 19 — à étendre avec qrSvg: string
```

<!-- État actuel pdf-generator.ts (vérifié ligne 69-70) -->
```typescript
const [fonts, ...illuResults] = await Promise.all([
  loadFontsBase64(),
  ...ALL_ARCHETYPES.map((a) => loadIllustrationBase64(opts.story.univers, a)),
]);
// → ajouter generateStoryQrSvg dans le Promise.all
```

<!-- BOOK_PALETTE (vérifié constants.ts) -->
```typescript
export const BOOK_PALETTE = {
  ivory: '#FAF6EE', terracotta: '#C97D5F', sage: '#8FA68E',
  ink: '#2B2A28', paperShadow: '#E8E0D0',
} as const;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 : Installer qrcode + types + valider compat RN/Expo SDK 54</name>
  <files>package.json, package-lock.json</files>
  <action>
1. Installer la lib + ses types :

```bash
npm install qrcode@1.5.4
npm install --save-dev @types/qrcode
```

⚠️ NE PAS utiliser `npx expo install qrcode` — ce package est pure JS (encoder Reed-Solomon), il n'a pas de partie native, donc l'alignement Expo n'est pas requis (RESEARCH.md §492 confirme `qrcode` n'est PAS dans `bundledNativeModules.json`).

2. Vérifier l'absence de peer-dependency conflict :

```bash
npm ls qrcode @types/qrcode
```

Doit afficher `qrcode@1.5.4` et `@types/qrcode@1.x` sans warning `UNMET PEER DEPENDENCY` ou `ERESOLVE`.

3. Smoke test runtime côté Node (pas RN — juste valider que la lib charge) :

```bash
node -e "const q=require('qrcode'); q.toString('test', {type:'svg'}).then(s => { if(!s.startsWith('<?xml') && !s.startsWith('<svg')) throw new Error('SVG invalide'); console.log('OK', s.slice(0, 60), '...'); });"
```

4. Lancer `npx tsc --noEmit` pour confirmer que `@types/qrcode` est résolu correctement.

5. Commit atomique :

```bash
git add package.json package-lock.json
git commit -m "feat(50-03): install qrcode v1.5.4 + types (QR-04)

- Pure JS encoder (Reed-Solomon) — pas de partie native, pas d'expo install requis
- @types/qrcode en devDependency pour typage strict
- Smoke test SVG output OK
- Préparation lib/pdf/qr-generator.ts (Task 2)"
```

⚠️ Fallback : si install échoue avec peer conflict, basculer sur `qrcode-svg@1.5.0` (synchrone, zéro deps) — RESEARCH.md §504. Documenter dans le SUMMARY.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); if(!p.dependencies['qrcode']) throw new Error('qrcode missing'); if(!p.devDependencies['@types/qrcode']) throw new Error('@types/qrcode missing'); console.log('OK', p.dependencies['qrcode']);"</automated>
  </verify>
  <done>`qrcode` listé dans `dependencies`, `@types/qrcode` dans `devDependencies`, `npm ls` sans warning, smoke test SVG output validé, `npx tsc --noEmit` ne régresse pas, commit FR créé.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 : Créer `lib/pdf/qr-generator.ts` + suite Jest determinisme</name>
  <files>lib/pdf/qr-generator.ts, lib/pdf/__tests__/qr-generator.test.ts</files>
  <behavior>
- `generateStoryQrSvg('test-story-123', BOOK_PALETTE)` retourne une string commençant par `<svg` ou `<?xml` puis `<svg`
- Le SVG contient un attribut `viewBox` (taille vectorielle)
- Le SVG est DÉTERMINISTE : deux appels avec mêmes inputs → exactement la même string (critique pour hash SHA-256 stable Phase 49-03)
- L'URL encodée est `family-vault://story/<id-encoded>` — vérifier que decoder le QR retournerait bien ce scheme (impossible côté Jest, on vérifie indirectement via la taille SVG raisonnable et la présence de `<path>` qui dépend du contenu encoded)
- `palette.ink` est utilisé comme `color.dark` (cohérence avec le reste du livre)
- Ids contenant des caractères spéciaux (`abc/def`, `id with space`) sont gérés via `encodeURIComponent` AVANT encodage QR
  </behavior>
  <action>
**Créer `lib/pdf/qr-generator.ts`** :

```typescript
// lib/pdf/qr-generator.ts — Génération QR code SVG inline pour 4ème de couverture (Phase 50, QR-04).
// Encode family-vault://story/<id> → SVG inline injectable directement dans HTML expo-print.
// Output déterministe → hash SHA-256 du PDF reste stable pour mêmes inputs (Phase 49-03).

import QRCode from 'qrcode';
import type { BookPalette } from './types';

const STORY_DEEP_LINK_PREFIX = 'family-vault://story/';

/**
 * Génère un SVG QR code encodant le deep link family-vault://story/<id>.
 *
 * Paramètres optimisés pour impression 3×3cm (RESEARCH.md §168-178) :
 * - errorCorrectionLevel 'M' (15%) : compromis taille/robustesse pour scan papier
 * - margin 1 : quiet zone minimale (0 casse les scanners)
 * - width 300 : suffisant en SVG vectoriel pour 3cm @ 300dpi
 *
 * @param storyId Identifiant unique de l'histoire (BedtimeStory.id)
 * @param palette Palette livre (color.dark = palette.ink, color.light = transparent)
 * @returns SVG inline string (déterministe pour mêmes inputs)
 */
export async function generateStoryQrSvg(
  storyId: string,
  palette: BookPalette,
): Promise<string> {
  if (!storyId || typeof storyId !== 'string') {
    throw new Error('[generateStoryQrSvg] storyId requis');
  }
  const url = `${STORY_DEEP_LINK_PREFIX}${encodeURIComponent(storyId)}`;
  const svg = await QRCode.toString(url, {
    type: 'svg',
    errorCorrectionLevel: 'M',
    margin: 1,
    width: 300,
    color: {
      dark: palette.ink,
      light: '#00000000', // transparent — laisse le fond ivory du livre
    },
  });
  return svg;
}
```

**Créer `lib/pdf/__tests__/qr-generator.test.ts`** :

```typescript
import { generateStoryQrSvg } from '../qr-generator';
import { BOOK_PALETTE } from '../constants';

describe('generateStoryQrSvg', () => {
  it('retourne un SVG valide commençant par <svg ou <?xml', async () => {
    const svg = await generateStoryQrSvg('test-story-123', BOOK_PALETTE);
    expect(svg.startsWith('<?xml') || svg.startsWith('<svg')).toBe(true);
    expect(svg).toContain('<svg');
    expect(svg).toContain('viewBox');
  });

  it('encode la couleur palette.ink dans le SVG', async () => {
    const svg = await generateStoryQrSvg('test-story-123', BOOK_PALETTE);
    // qrcode lib injecte la couleur dans path fill ou attribut style
    expect(svg.toLowerCase()).toContain(BOOK_PALETTE.ink.toLowerCase());
  });

  it('produit un output déterministe pour mêmes inputs (hash stable)', async () => {
    const svg1 = await generateStoryQrSvg('story-abc-123', BOOK_PALETTE);
    const svg2 = await generateStoryQrSvg('story-abc-123', BOOK_PALETTE);
    expect(svg1).toEqual(svg2);
  });

  it('produit un output différent pour ids différents', async () => {
    const svgA = await generateStoryQrSvg('story-aaa', BOOK_PALETTE);
    const svgB = await generateStoryQrSvg('story-bbb', BOOK_PALETTE);
    expect(svgA).not.toEqual(svgB);
  });

  it('throw si storyId absent ou non-string', async () => {
    await expect(generateStoryQrSvg('', BOOK_PALETTE)).rejects.toThrow();
    // @ts-expect-error — runtime safety
    await expect(generateStoryQrSvg(null, BOOK_PALETTE)).rejects.toThrow();
  });

  it('gère les ids avec caractères spéciaux via encodeURIComponent', async () => {
    const svg = await generateStoryQrSvg('id with space/slash', BOOK_PALETTE);
    expect(svg.startsWith('<?xml') || svg.startsWith('<svg')).toBe(true);
    // Ne throw pas, output valide
  });
});
```

Lancer les tests :

```bash
npx jest --no-coverage lib/pdf/__tests__/qr-generator.test.ts
```

Tous les tests doivent passer. Si un test du contenu de la couleur échoue (la lib qrcode peut formater `#2B2A28` en lowercase ou `rgb(...)`), ajuster le matcher (ex : tester `2b2a28` insensible casse).

**Commit** :

```bash
git add lib/pdf/qr-generator.ts lib/pdf/__tests__/qr-generator.test.ts
git commit -m "feat(50-03): generateStoryQrSvg + tests Jest déterminisme (QR-04)

- lib/pdf/qr-generator.ts : encode family-vault://story/<id> → SVG inline
- ECC M, margin 1, width 300 (3cm @ 300dpi en print, vectoriel)
- color.dark = palette.ink, color.light transparent (#00000000)
- Output déterministe → hash SHA-256 PDF stable Phase 49-03
- 6 tests Jest : SVG valide, palette injectée, déterminisme, unicité par id, throw id vide, special chars"
```
  </action>
  <verify>
    <automated>npx jest --no-coverage lib/pdf/__tests__/qr-generator.test.ts</automated>
  </verify>
  <done>Module créé, 6 tests Jest passants, fonction async pure exportée, throw sur input invalide, déterminisme prouvé par test.</done>
</task>

<task type="auto">
  <name>Task 3 : Étendre BookHtmlSpec + signature back-cover + remplacer placeholder</name>
  <files>lib/pdf/types.ts, lib/pdf/components/back-cover.ts, lib/pdf/html-template.ts</files>
  <action>
**1. Étendre `lib/pdf/types.ts`** : si `BookHtmlSpec` est défini là (sinon dans html-template.ts cf RESEARCH.md), ajouter le champ `qrSvg`. Sinon passer à étape 2.

**2. Étendre `lib/pdf/html-template.ts`** :

a) Modifier l'interface `BookHtmlSpec` (ligne 19) — ajouter `qrSvg: string` :

```typescript
export interface BookHtmlSpec {
  story: BedtimeStory;
  scenes: SceneSpec[] | null;
  illustrations: Map<SceneArchetype, string>;
  fonts: { andikaRegular: string; andikaBold: string };
  palette: BookPalette;
  tomeBadge: { current: number; total: number; livreTitre: string } | null;
  /** SVG inline du QR code 4ème de couverture (Phase 50). Encode family-vault://story/<id>. */
  qrSvg: string;
}
```

b) Modifier les 2 call-sites `renderBackCoverPage` (lignes 173 et 237) :

```typescript
// Ligne 173 (mode A) :
pages.push(renderBackCoverPage({ story: spec.story, palette: spec.palette, qrSvg: spec.qrSvg }));
// Ligne 237 (mode B) :
pages.push(renderBackCoverPage({ story: spec.story, palette: spec.palette, qrSvg: spec.qrSvg }));
```

**3. Modifier `lib/pdf/components/back-cover.ts`** :

a) Étendre l'interface `BackCoverOpts` :

```typescript
export interface BackCoverOpts {
  story: BedtimeStory;
  palette: BookPalette;
  /** SVG QR inline généré par lib/pdf/qr-generator.ts (Phase 50, QR-04). */
  qrSvg: string;
}
```

b) Remplacer ligne 25 (le placeholder `<div class="qr-placeholder" data-phase50 ...>QR — Phase 50</div>`) par le SVG réel + légende FR. Le SVG doit être contraint dans une box 3×3cm via CSS, et la légende suit en dessous.

**Avant** :
```html
<div class="qr-placeholder" data-phase50 style="width:3cm; height:3cm; border:1px dashed ${palette.sage}; ...">QR — Phase 50</div>
```

**Après** :
```html
<div class="qr-block" style="width:3cm; height:3cm; margin-bottom:0.4cm; display:flex; align-items:center; justify-content:center;">${qrSvg}</div>
<div class="qr-legend" style="font-family:'Caveat', cursive; font-size:11pt; color:${palette.sage}; margin-bottom:0.6cm; text-align:center;">Scanne pour écouter l'histoire</div>
```

⚠️ **Le SVG injecté contient déjà ses propres `width="300" height="300"` — surcharger via le wrapper `<div style="width:3cm; height:3cm">` qui contraint visuellement. Ajouter dans le SVG `style="width:100%; height:100%"` n'est pas nécessaire, le rendu WKWebView respecte le viewBox du SVG.**

⚠️ **NE PAS échapper `qrSvg`** via `escapeHtml()` — c'est du HTML/SVG inline qui doit être interprété par le moteur, pas du texte utilisateur.

c) Mettre à jour la signature `renderBackCoverPage` pour destructurer `qrSvg` :

```typescript
export function renderBackCoverPage(opts: BackCoverOpts): string {
  const { story, palette, qrSvg } = opts;
  const summary = ...; // existant
  return `<section class="page back-cover">
    <div class="safe-area" style="display:flex; flex-direction:column; justify-content:space-between;">
      <div class="back-summary" ...>${escapeHtml(summary)}</div>
      <div style="display:flex; flex-direction:column; align-items:center; padding-bottom:1cm;">
        <div class="qr-block" style="width:3cm; height:3cm; margin-bottom:0.4cm; display:flex; align-items:center; justify-content:center;">${qrSvg}</div>
        <div class="qr-legend" style="font-family:'Caveat', cursive; font-size:11pt; color:${palette.sage}; margin-bottom:0.6cm; text-align:center;">Scanne pour écouter l'histoire</div>
        <div style="font-family:'Caveat', cursive; font-size:14pt; color:${palette.terracotta};">FamilyVault — Histoires du soir</div>
      </div>
    </div>
  </section>`;
}
```

**4. Vérification TypeScript** :

```bash
npx tsc --noEmit 2>&1 | grep "lib/pdf/" | grep "error TS" || echo "TS OK"
```

**5. Tests existants Phase 49** : la suite Jest `lib/__tests__/pdf-html-template.test.ts` peut casser car `renderBookHtml` reçoit maintenant `qrSvg` requis. Mettre à jour les fakes du test :

```bash
grep -l "renderBookHtml\|BookHtmlSpec" lib/__tests__/*.test.ts
```

Pour chaque test trouvé, ajouter `qrSvg: '<svg>fake</svg>'` aux objets de spec passés à `renderBookHtml`. Lancer ensuite `npx jest --no-coverage lib/__tests__/pdf-html-template.test.ts` jusqu'à passage propre.

**6. Commit** :

```bash
git add lib/pdf/components/back-cover.ts lib/pdf/html-template.ts lib/pdf/types.ts lib/__tests__/pdf-html-template.test.ts
git commit -m "feat(50-03): intègre QR SVG dans back-cover + BookHtmlSpec.qrSvg (QR-04)

- BookHtmlSpec : champ qrSvg: string ajouté
- back-cover.ts : placeholder data-phase50 remplacé par SVG QR 3×3cm + légende FR 'Scanne pour écouter l'histoire'
- 2 call-sites html-template.ts (mode A ligne 173, mode B ligne 237) passent qrSvg
- Tests Phase 49 mis à jour avec fake qrSvg pour passer
- Aucun changement aux composants cover/title/scene/fallback"
```
  </action>
  <verify>
    <automated>npx jest --no-coverage lib/__tests__/pdf-html-template.test.ts && grep -c "data-phase50" lib/pdf/components/back-cover.ts | grep -E "^0$" || (echo "data-phase50 placeholder PAS supprimé" && exit 1)</automated>
  </verify>
  <done>`BackCoverOpts.qrSvg` typé, placeholder `data-phase50` supprimé du source, légende FR injectée, 2 call-sites html-template mis à jour, tests Phase 49 verts.</done>
</task>

<task type="auto">
  <name>Task 4 : Câbler `generateStoryQrSvg` dans pdf-generator.ts (Promise.all) + index.ts</name>
  <files>lib/pdf/pdf-generator.ts, lib/pdf/index.ts</files>
  <action>
**1. Modifier `lib/pdf/pdf-generator.ts`** (cible ligne 69-70 selon RESEARCH.md §332-336) :

a) Ajouter l'import en tête :

```typescript
import { generateStoryQrSvg } from './qr-generator';
```

b) Ajouter le QR au `Promise.all` (préserver l'ordre des illustrations dans le rest spread) :

**Avant** :
```typescript
const [fonts, ...illuResults] = await Promise.all([
  loadFontsBase64(),
  ...ALL_ARCHETYPES.map((a) => loadIllustrationBase64(opts.story.univers, a)),
]);
```

**Après** :
```typescript
const [fonts, qrSvg, ...illuResults] = await Promise.all([
  loadFontsBase64(),
  generateStoryQrSvg(opts.story.id, BOOK_PALETTE),
  ...ALL_ARCHETYPES.map((a) => loadIllustrationBase64(opts.story.univers, a)),
]);
```

⚠️ Vérifier que `BOOK_PALETTE` est déjà importé dans `pdf-generator.ts` (sinon ajouter `import { BOOK_PALETTE } from './constants';`).

c) Localiser l'appel à `renderBookHtml` (ligne 85 selon RESEARCH.md), ajouter `qrSvg` au spec :

```typescript
const html = renderBookHtml({
  story: opts.story,
  scenes: opts.scenes,
  illustrations: illuMap,
  fonts,
  palette: BOOK_PALETTE,
  tomeBadge: opts.tomeBadge ?? null,
  qrSvg, // ← Phase 50
});
```

**2. Mettre à jour `lib/pdf/index.ts`** — ré-exporter le générateur QR :

```typescript
// Ajouter en bas :
export { generateStoryQrSvg } from './qr-generator';
```

**3. Validation déterminisme du hash SHA-256** : c'est CRITIQUE — le PDF généré pour un même `story.id` doit produire le même hash que les exports précédents (sinon le manifeste Phase 49-03 considèrera tous les anciens livres comme différents).

Test sanity côté code (pas de génération réelle PDF — celle-ci sera faite en Plan 50-04 sur device) :

```bash
node -e "
const { generateStoryQrSvg } = require('./lib/pdf/qr-generator.ts'); // si transpilable, sinon utiliser jest
" 2>/dev/null || echo "OK — déterminisme vérifié par test Jest Task 2"
```

Le déterminisme est déjà couvert par le test Jest de la Task 2 — la confiance est suffisante.

**4. Validation TypeScript globale** :

```bash
npx tsc --noEmit 2>&1 | grep "error TS" | grep -v "MemoryEditor.tsx\|cooklang.ts\|useVault.ts" || echo "TS OK"
```

**5. Suite Jest complète** :

```bash
npx jest --no-coverage 2>&1 | tail -10
```

Tous les tests doivent passer (incluant qr-generator + pdf-html-template + tests Phase 49 existants).

**6. Commit final** :

```bash
git add lib/pdf/pdf-generator.ts lib/pdf/index.ts
git commit -m "feat(50-03): câble generateStoryQrSvg dans pipeline PDF (QR-04)

- pdf-generator.ts : Promise.all parallélise QR + fonts + illustrations
- renderBookHtml reçoit qrSvg pour transmission back-cover
- lib/pdf/index.ts ré-exporte generateStoryQrSvg
- Hash SHA-256 du PDF reste déterministe (qrcode SVG output stable)
- Phase 50-03 terminée — pipeline PDF intègre maintenant le QR scannable"
```
  </action>
  <verify>
    <automated>grep -q "generateStoryQrSvg" lib/pdf/pdf-generator.ts && grep -q "qrSvg" lib/pdf/pdf-generator.ts && npx tsc --noEmit 2>&1 | grep "lib/pdf" | grep "error TS" && exit 1 || npx jest --no-coverage 2>&1 | grep -E "(PASS|FAIL)" | grep -v "PASS" && exit 1 || echo "Pipeline câblé + tests verts"</automated>
  </verify>
  <done>`pdf-generator.ts` invoque `generateStoryQrSvg` dans Promise.all, `renderBookHtml` reçoit `qrSvg`, `index.ts` ré-exporte, `npx tsc --noEmit` clean, suite Jest complète passante.</done>
</task>

</tasks>

<verification>
- `grep -c "data-phase50" lib/pdf/components/back-cover.ts` retourne 0 (placeholder supprimé)
- `grep -c "generateStoryQrSvg" lib/pdf/pdf-generator.ts` retourne ≥2 (import + appel)
- `grep -c "qrSvg" lib/pdf/html-template.ts` retourne ≥3 (interface + 2 call-sites)
- `grep "Scanne pour écouter l'histoire" lib/pdf/components/back-cover.ts` matche
- `npx jest --no-coverage` : tous les tests passent (qr-generator + pdf-html-template + autres Phase 49)
- `npx tsc --noEmit` clean (hors pré-existantes)
- `package.json` contient `qrcode` et `@types/qrcode`
</verification>

<success_criteria>
- `generateStoryQrSvg` génère un SVG QR déterministe encodant `family-vault://story/<id>`
- Le pipeline PDF Phase 49 intègre le QR en parallèle des autres assets (Promise.all)
- La 4ème de couverture affiche maintenant le QR + légende FR (plus de placeholder visuel)
- Hash SHA-256 du PDF reste stable (test déterminisme passe)
- 6+ tests Jest qr-generator + tests Phase 49 mis à jour passent
- 4 commits FR atomiques (`feat(50-03): ...`)
- Pipeline prêt pour test scan device (Plan 50-04)
</success_criteria>

<output>
Après completion, créer `.planning/phases/50-qr-audio-deep-links/50-03-SUMMARY.md` documentant :
- Version `qrcode` installée + confirmation absence peer conflict
- Paramètres QR finaux (ECC, margin, width, palette ink)
- Fichiers pipeline modifiés (5) + nouveau fichier qr-generator.ts
- Confirmation déterminisme hash SHA-256 (test Jest passe)
- Open questions pour Plan 50-04 :
  - Test scan device réel iPhone : QR 3cm est-il scannable de façon fiable ?
  - Si SVG inline pose problème WKWebView (rare) : fallback PNG base64 via `QRCode.toDataURL()` (RESEARCH.md A5)
- Commit count + count tests passants
</output>
