---
phase: 35-carte-enveloppe-dashboard-cran-bo-te-aux-lettres
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/lovenotes/selectors.ts
  - lib/lovenotes/index.ts
  - lib/__tests__/lovenotes-selectors.test.ts
  - app/(tabs)/lovenotes.tsx
  - app/(tabs)/_layout.tsx
autonomous: true
requirements: [LOVE-06, LOVE-07, LOVE-08]
must_haves:
  truths:
    - "Les sélecteurs unreadForProfile / receivedForProfile / sentByProfile / archivedForProfile existent comme fonctions pures et exportées depuis lib/lovenotes/selectors.ts"
    - "La route /(tabs)/lovenotes existe et est navigable via router.push (pas de crash 'action NAVIGATE not handled')"
    - "La route n'apparaît pas dans la tabbar visible (Tabs.Screen avec href: null)"
    - "npx tsc --noEmit reste clean hors erreurs pré-existantes (MemoryEditor.tsx, cooklang.ts, useVault.ts)"
  artifacts:
    - path: "lib/lovenotes/selectors.ts"
      provides: "Sélecteurs dérivés purs sur loveNotes[]"
      exports: ["isRevealed", "unreadForProfile", "receivedForProfile", "sentByProfile", "archivedForProfile"]
    - path: "lib/lovenotes/index.ts"
      provides: "Barrel export du module lovenotes"
    - path: "lib/__tests__/lovenotes-selectors.test.ts"
      provides: "Tests Jest des sélecteurs (isRevealed fallback pending+revealAt<=now, tris, filtres par profil)"
    - path: "app/(tabs)/lovenotes.tsx"
      provides: "Écran Boîte aux lettres (3 segments Reçues/Envoyées/Archivées + FlatList virtualisées + empty states textuels)"
    - path: "app/(tabs)/_layout.tsx"
      provides: "Déclaration Tabs.Screen lovenotes avec href: null (hidden)"
  key_links:
    - from: "app/(tabs)/lovenotes.tsx"
      to: "useVault().loveNotes + activeProfile + profiles"
      via: "useVault() + useMemo sélecteurs avec deps [loveNotes, activeProfile?.id]"
      pattern: "from '../../contexts/VaultContext'"
    - from: "app/(tabs)/lovenotes.tsx"
      to: "lib/lovenotes/selectors.ts"
      via: "import { receivedForProfile, sentByProfile, archivedForProfile }"
      pattern: "from '../../lib/lovenotes/selectors'"
---

<objective>
Poser les fondations exécutables de Phase 35 : sélecteurs dérivés purs testables + route écran Boîte aux lettres minimale opérationnelle (3 segments, empty states, FlatList virtualisées stubs de LoveNoteCard). Cette wave unblock les plans 02 (composants visuels) et 03 (injections dashboard + more) en éliminant le risque de crash route non déclarée (Pitfall 2 du RESEARCH).

Purpose: Établir les primitives testables (selectors) et la surface de navigation avant toute UI cosmétique. Plan 02 pourra alors livrer EnvelopeCard/WaxSeal/LoveNoteCard sans re-toucher à l'écran route. Plan 03 pourra appeler router.push('/(tabs)/lovenotes') sans risque runtime.
Output: 4 sélecteurs purs + tests Jest + route skeleton + déclaration layout.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/35-carte-enveloppe-dashboard-cran-bo-te-aux-lettres/35-RESEARCH.md
@.planning/REQUIREMENTS.md
@.planning/phases/34-fondation-donn-es-hook-domaine/34-03-SUMMARY.md
@hooks/useVaultLoveNotes.ts
@lib/types.ts
@components/ui/SegmentedControl.tsx
@CLAUDE.md

<interfaces>
<!-- API hook consommé (Phase 34 shipped) — lu directement depuis hooks/useVaultLoveNotes.ts -->

```typescript
// Exposé via useVault() :
loveNotes: LoveNote[];
addLoveNote: (note: Omit<LoveNote, 'sourceFile'>) => Promise<void>;
updateLoveNoteStatus: (sourceFile: string, status: LoveNoteStatus, readAt?: string) => Promise<void>;
deleteLoveNote: (sourceFile: string) => Promise<void>;
activeProfile: Profile | null;
profiles: Profile[];
```

```typescript
// Shape LoveNote (lib/types.ts:589-606) :
interface LoveNote {
  from: string;          // profile ID
  to: string;            // profile ID
  createdAt: string;     // ISO 'YYYY-MM-DDTHH:mm:ss' (sans Z)
  revealAt: string;      // ISO 'YYYY-MM-DDTHH:mm:ss' (sans Z)
  status: 'pending' | 'revealed' | 'read';
  readAt?: string;       // ISO lecture
  body: string;          // markdown
  sourceFile: string;    // '03 - Famille/LoveNotes/{to}/YYYY-MM-DD-slug.md'
}
type LoveNoteStatus = 'pending' | 'revealed' | 'read';
```

```typescript
// SegmentedControl API (components/ui/SegmentedControl.tsx) :
type Segment<T> = { id: T; label: string; badge?: number };
interface Props<T> {
  segments: Segment<T>[];
  value: T;
  onChange: (v: T) => void;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Créer lib/lovenotes/selectors.ts + tests Jest + barrel</name>
  <read_first>
    - .planning/phases/35-carte-enveloppe-dashboard-cran-bo-te-aux-lettres/35-RESEARCH.md (sections "Code Examples" exemple 1 — selectors verbatim, "Pitfall 5" — pending fallback)
    - hooks/useVaultLoveNotes.ts (shape LoveNote, pattern createdAt desc existant)
    - lib/types.ts (types LoveNote + LoveNoteStatus exacts)
    - lib/__tests__/parser-lovenotes.test.ts (pattern Jest de Phase 34-02 à répliquer)
    - CLAUDE.md (privacy : utiliser Lucas/Emma/Dupont dans tests, jamais de vrais noms)
  </read_first>
  <files>lib/lovenotes/selectors.ts, lib/lovenotes/index.ts, lib/__tests__/lovenotes-selectors.test.ts</files>
  <behavior>
    - isRevealed(note, now?): true si status === 'revealed' OR 'read' OR (status === 'pending' && revealAt <= now.toISOString().slice(0,19))
    - isRevealed supporte now injecté (testabilité, pattern getCompanionMood Phase 10)
    - unreadForProfile(notes, profileId): filtre to === profileId && status !== 'read' && isRevealed(n) — tri createdAt desc
    - receivedForProfile(notes, profileId): filtre to === profileId — tri non-lues d'abord puis createdAt desc
    - sentByProfile(notes, profileId): filtre from === profileId — tri createdAt desc
    - archivedForProfile(notes, profileId): filtre (to === profileId && status === 'read') OR (from === profileId && status === 'read') — tri (readAt ?? createdAt) desc
    - Tests : 18+ tests couvrant (1) isRevealed statuts + fallback pending<=now + pending>now, (2) unreadForProfile filtre et exclut read, (3) receivedForProfile tri non-lues d'abord, (4) sentByProfile filtre from, (5) archivedForProfile inclut to+read ET from+read, (6) profil sans notes retourne [], (7) notes avec profileId inconnu retourne [], (8) now injecté permet tests déterministes
    - Données de test utilisent exclusivement les IDs 'lucas', 'emma', 'dupont' (privacy)
  </behavior>
  <action>
    Créer `lib/lovenotes/selectors.ts` avec 5 fonctions pures exportées (`isRevealed`, `unreadForProfile`, `receivedForProfile`, `sentByProfile`, `archivedForProfile`) — copier verbatim l'Exemple 1 de RESEARCH.md (lignes 399-441) avec ces ajustements :
    - `isRevealed` prend un 2e paramètre optionnel `now = new Date()` pour injection testabilité
    - Utiliser `now.toISOString().slice(0, 19)` pour comparer au format ISO sans Z (shape LoveNote.revealAt)
    - Toutes les fonctions importent `type { LoveNote } from '../types'` (chemin relatif vers lib/types.ts)

    Créer `lib/lovenotes/index.ts` avec barrel : `export * from './selectors';`

    Créer `lib/__tests__/lovenotes-selectors.test.ts` avec 18+ tests :
    - describe('isRevealed') : 4 tests (revealed→true, read→true, pending+revealAt passé→true, pending+revealAt futur→false)
    - describe('unreadForProfile') : 3 tests (exclut read, exclut pending futur, filtre to !== profileId)
    - describe('receivedForProfile') : 3 tests (filtre to, tri non-lues d'abord, tri createdAt desc à statut égal)
    - describe('sentByProfile') : 2 tests (filtre from, tri createdAt desc)
    - describe('archivedForProfile') : 3 tests (inclut to+read, inclut from+read, tri readAt desc)
    - describe('edge cases') : 3 tests (profil inconnu → [], tableau vide → [], now injecté déterministe)
    - Fabriquer des notes fixture avec profiles 'lucas'/'emma'/'dupont' uniquement, dates ISO slice(0,19)
    - Utiliser `const FIXED_NOW = new Date('2026-04-17T12:00:00')` et passer à isRevealed

    Commentaires en français.
  </action>
  <verify>
    <automated>npx jest lib/__tests__/lovenotes-selectors.test.ts --no-coverage</automated>
  </verify>
  <acceptance_criteria>
    - `lib/lovenotes/selectors.ts` existe et exporte isRevealed, unreadForProfile, receivedForProfile, sentByProfile, archivedForProfile
    - `grep -q "export function isRevealed" lib/lovenotes/selectors.ts` returns 0
    - `grep -q "export function unreadForProfile" lib/lovenotes/selectors.ts` returns 0
    - `grep -q "export function receivedForProfile" lib/lovenotes/selectors.ts` returns 0
    - `grep -q "export function sentByProfile" lib/lovenotes/selectors.ts` returns 0
    - `grep -q "export function archivedForProfile" lib/lovenotes/selectors.ts` returns 0
    - `lib/lovenotes/index.ts` existe et exporte tout depuis ./selectors
    - `lib/__tests__/lovenotes-selectors.test.ts` : `npx jest lib/__tests__/lovenotes-selectors.test.ts --no-coverage` passe avec ≥18 tests verts
    - Aucun nom réel dans les fixtures : `grep -iE "(gabriel|marie|pierre|sophie|julien)" lib/__tests__/lovenotes-selectors.test.ts` returns 1 (not found)
    - `npx tsc --noEmit` : aucune nouvelle erreur (pré-existantes MemoryEditor.tsx, cooklang.ts, useVault.ts OK)
  </acceptance_criteria>
  <done>Les 5 sélecteurs existent, 18+ tests Jest passent, barrel en place, typage clean.</done>
</task>

<task type="auto">
  <name>Task 2: Créer route écran /(tabs)/lovenotes.tsx + déclaration _layout.tsx href:null</name>
  <read_first>
    - .planning/phases/35-carte-enveloppe-dashboard-cran-bo-te-aux-lettres/35-RESEARCH.md (section "Pattern 3" route + href:null, "Code Examples" exemple 2 écran verbatim lignes 443-525, "Pitfall 2" ordering crash route)
    - app/(tabs)/_layout.tsx (pattern `<Tabs.Screen name="loot" options={{ href: null }} />` ligne ~263-268 — respecter le style exact)
    - app/(tabs)/notes.tsx (écran pattern reference — SafeAreaView + ModalHeader + FlatList)
    - app/(tabs)/loot.tsx (écran pattern href:null reference)
    - components/ui/SegmentedControl.tsx (API exacte : segments[], value, onChange, Segment<T>)
    - components/ui/index.ts (barrel — confirmer ModalHeader + SegmentedControl exportés)
    - hooks/useVaultLoveNotes.ts (shape LoveNote + API)
    - lib/lovenotes/selectors.ts (créé Task 1 — fonctions à consommer)
    - CLAUDE.md (useThemeColors obligatoire, Spacing tokens, strings français)
  </read_first>
  <files>app/(tabs)/lovenotes.tsx, app/(tabs)/_layout.tsx</files>
  <action>
    **Fichier 1 : `app/(tabs)/lovenotes.tsx`** — créer l'écran Boîte aux lettres complet (implémentation copiée verbatim de RESEARCH.md exemple 2 lignes 443-525) avec ces éléments :
    - `'use client';` NON (pas React Server Components dans ce repo)
    - Imports : `useMemo`, `useState`, `useCallback` de 'react' ; `View`, `FlatList`, `StyleSheet`, `Text` de 'react-native' ; `SafeAreaView` de 'react-native-safe-area-context' ; `useVault` de '../../contexts/VaultContext' ; `useThemeColors` de '../../contexts/ThemeContext' ; `ModalHeader`, `SegmentedControl` de '../../components/ui' ; `receivedForProfile`, `sentByProfile`, `archivedForProfile` de '../../lib/lovenotes/selectors' ; `type { LoveNote }` de '../../lib/types' ; `Spacing` de '../../constants/spacing'
    - Type local : `type Segment = 'received' | 'sent' | 'archived';`
    - Hook state : `const [segment, setSegment] = useState<Segment>('received');`
    - 3 useMemo : `received`, `sent`, `archived` avec deps `[loveNotes, activeProfile?.id]` (Pitfall 4 — deps explicites)
    - `unreadCount = received.filter(n => n.status !== 'read').length` (calcul inline, pas memo car léger)
    - `data = segment === 'received' ? received : segment === 'sent' ? sent : archived`
    - Placeholder `LoveNoteCard` : stub inline `<View style={{ padding: Spacing.md, backgroundColor: colors.card, borderRadius: 12 }}><Text style={{ color: colors.text }}>{item.body.slice(0, 60)}</Text><Text style={{ color: colors.textMuted, fontSize: 12 }}>{item.status}</Text></View>` — Plan 02 remplacera par vrai composant
    - `renderItem` avec `useCallback` — déps `[colors]`
    - SegmentedControl avec 3 segments : `{ id: 'received', label: 'Reçues', badge: unreadCount || undefined }`, `{ id: 'sent', label: 'Envoyées' }`, `{ id: 'archived', label: 'Archivées' }`
    - Empty state textuel : `data.length === 0 ? <View style={styles.empty}><Text style={{ color: colors.textMuted }}>Aucune note pour l'instant.</Text></View>` (polish illustré = Phase 37)
    - FlatList props : `keyExtractor={(n) => n.sourceFile}`, `renderItem`, `contentContainerStyle={styles.list}`, `ItemSeparatorComponent={() => <View style={{ height: Spacing.md }} />}`, `initialNumToRender={10}`, `removeClippedSubviews`
    - StyleSheet.create en bas : `safe: { flex: 1 }`, `controls: { paddingHorizontal: Spacing['2xl'], paddingVertical: Spacing.md }`, `list: { padding: Spacing['2xl'] }`, `empty: { flex: 1, alignItems: 'center', justifyContent: 'center' }`
    - ModalHeader title : "Boîte aux lettres"
    - Export default function `LoveNotesScreen`
    - Commentaires en français

    **Fichier 2 : `app/(tabs)/_layout.tsx`** — ajouter UNE seule ligne dans le groupe des `href: null` (là où sont `loot`, `notes`) :
    - Edit : ajouter `<Tabs.Screen name="lovenotes" options={{ href: null }} />` juste après la ligne `<Tabs.Screen name="notes" options={{ href: null }} />` (ou entre loot/notes, peu importe la position dans le bloc tant que c'est parmi les href:null).
    - Ne pas modifier autre chose dans _layout.tsx.

    Ne PAS appeler `router.push('/(tabs)/lovenotes')` depuis ailleurs dans cette wave — c'est le travail du Plan 03.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -vE "(MemoryEditor|cooklang|useVault)\.(tsx|ts)" | grep -c "error TS"</automated>
  </verify>
  <acceptance_criteria>
    - `app/(tabs)/lovenotes.tsx` existe
    - `grep -q "export default function LoveNotesScreen" app/(tabs)/lovenotes.tsx` returns 0
    - `grep -q "useVault" app/(tabs)/lovenotes.tsx` returns 0
    - `grep -q "SegmentedControl" app/(tabs)/lovenotes.tsx` returns 0
    - `grep -q "receivedForProfile\|sentByProfile\|archivedForProfile" app/(tabs)/lovenotes.tsx` returns 0
    - `grep -q "Boîte aux lettres" app/(tabs)/lovenotes.tsx` returns 0
    - `grep -q "Reçues\|Envoyées\|Archivées" app/(tabs)/lovenotes.tsx` returns 0
    - `grep -q "useThemeColors" app/(tabs)/lovenotes.tsx` returns 0 (pas de hardcoded)
    - `grep -q "lovenotes" app/(tabs)/_layout.tsx` returns 0
    - `grep -q 'name="lovenotes".*href: null' app/(tabs)/_layout.tsx` returns 0 (ordre importe peu mais les 2 attributs présents)
    - `npx tsc --noEmit` : count d'erreurs nouvelles (hors MemoryEditor.tsx, cooklang.ts, useVault.ts) = 0
  </acceptance_criteria>
  <done>Route /(tabs)/lovenotes navigable (pas de crash au push), écran segmented monté, tabbar ne montre pas lovenotes, tsc clean.</done>
</task>

</tasks>

<verification>
- `npx jest lib/__tests__/lovenotes-selectors.test.ts --no-coverage` passe (≥18 tests)
- `npx tsc --noEmit` : aucune nouvelle erreur (pré-existantes ignorées per CLAUDE.md)
- Navigation manuelle : `router.push('/(tabs)/lovenotes')` (test manuel Plan 03 uniquement — ici on vérifie juste compile clean)
- Grep privacy : `grep -iE "(gabriel|marie|pierre|sophie|julien)" lib/__tests__/lovenotes-selectors.test.ts` = 0 résultat
</verification>

<success_criteria>
- Sélecteurs purs testables livrés (5 fonctions, ≥18 tests verts)
- Route /(tabs)/lovenotes existe, compile, utilisable via router.push
- Tabbar inchangée visuellement (lovenotes hidden via href:null)
- Aucune régression TypeScript/Jest
- Wave 2 peut démarrer en parallèle (2 plans : composants + injections) sur cette fondation stable
</success_criteria>

<output>
After completion, create `.planning/phases/35-carte-enveloppe-dashboard-cran-bo-te-aux-lettres/35-01-SUMMARY.md`
</output>
