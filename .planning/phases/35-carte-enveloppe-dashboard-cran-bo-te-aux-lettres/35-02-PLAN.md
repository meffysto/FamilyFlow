---
phase: 35-carte-enveloppe-dashboard-cran-bo-te-aux-lettres
plan: 02
type: execute
wave: 2
depends_on: [35-01]
files_modified:
  - components/lovenotes/WaxSeal.tsx
  - components/lovenotes/EnvelopeFlap.tsx
  - components/lovenotes/EnvelopeCard.tsx
  - components/lovenotes/LoveNoteCard.tsx
  - components/lovenotes/index.ts
  - app/(tabs)/lovenotes.tsx
autonomous: true
requirements: [LOVE-05, LOVE-06, LOVE-08]
must_haves:
  truths:
    - "EnvelopeCard rend une carte paysage ratio 2:1.15, papier ivoire (#f5ecd5), rabat triangulaire SVG, cachet rouge animé pulse 2400ms, tilt -1.5°"
    - "Quand count >= 2, EnvelopeCard montre un badge compteur sur le cachet + 2 enveloppes empilées en fond (rotation -3° / +2°, opacity 0.55 / 0.70)"
    - "WaxSeal.pulse utilise withRepeat(withTiming(1.06, {duration:1200}), -1, true) et cancelAnimation au unmount (pas de leak worklet)"
    - "LoveNoteCard est React.memo + affiche from (lookup via profiles), preview body 2 lignes (strip markdown inline), date createdAt format JJ/MM/AAAA, état via cachet non animé si pending futur / animé si unreadCount"
    - "L'écran /(tabs)/lovenotes consomme <LoveNoteCard> (remplace le stub Plan 01)"
  artifacts:
    - path: "components/lovenotes/WaxSeal.tsx"
      provides: "Cachet cire animé pulse Reanimated, badge compteur optionnel"
      exports: ["WaxSeal"]
    - path: "components/lovenotes/EnvelopeFlap.tsx"
      provides: "Rabat triangulaire SVG Polygon"
      exports: ["EnvelopeFlap"]
    - path: "components/lovenotes/EnvelopeCard.tsx"
      provides: "Carte enveloppe hero pinned dashboard"
      exports: ["EnvelopeCard"]
    - path: "components/lovenotes/LoveNoteCard.tsx"
      provides: "Item liste mémoïsé pour FlatList segments"
      exports: ["LoveNoteCard"]
    - path: "components/lovenotes/index.ts"
      provides: "Barrel export composants lovenotes"
  key_links:
    - from: "components/lovenotes/EnvelopeCard.tsx"
      to: "components/lovenotes/WaxSeal.tsx + EnvelopeFlap.tsx"
      via: "composition JSX interne"
      pattern: "<WaxSeal count={count}"
    - from: "app/(tabs)/lovenotes.tsx"
      to: "components/lovenotes/LoveNoteCard.tsx"
      via: "remplacement du stub inline du Plan 01 par <LoveNoteCard note={item} profiles={profiles} />"
      pattern: "<LoveNoteCard"
    - from: "components/lovenotes/WaxSeal.tsx"
      to: "react-native-reanimated"
      via: "useSharedValue + withRepeat + cancelAnimation cleanup"
      pattern: "withRepeat\\(withTiming"
---

<objective>
Livrer les 4 composants visuels du domaine Love Notes (WaxSeal, EnvelopeFlap, EnvelopeCard, LoveNoteCard) et remplacer le stub inline du Plan 01 dans l'écran Boîte aux lettres par un vrai LoveNoteCard mémoïsé. Après ce plan, les segments affichent des cartes riches (expéditeur, preview, état, date) et EnvelopeCard est prêt à être consommé par le Plan 03.

Purpose: Isoler toute la complexité visuelle (SVG Polygon, Reanimated pulse, StyleSheet constantes cosmétiques papier/cire) dans des composants dédiés et mémoïsés. Plan 03 restera une simple injection "usage".
Output: Dossier `components/lovenotes/` complet avec barrel, écran /(tabs)/lovenotes consommant LoveNoteCard.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/35-carte-enveloppe-dashboard-cran-bo-te-aux-lettres/35-RESEARCH.md
@.planning/phases/35-carte-enveloppe-dashboard-cran-bo-te-aux-lettres/35-01-SUMMARY.md
@.planning/mockups/lovenote-envelope.html
@lib/types.ts
@hooks/useVaultLoveNotes.ts
@components/ui/index.ts
@components/ui/PressableScale.tsx
@constants/colors.ts
@constants/spacing.ts
@constants/shadows.ts
@constants/typography.ts
@CLAUDE.md

<interfaces>
<!-- Contrats composants produits par ce plan -->

```typescript
// components/lovenotes/WaxSeal.tsx
interface WaxSealProps {
  count: number;             // >=2 affiche le badge
  initial?: string;          // lettre au centre, default 'M'
  pulse?: boolean;           // default true
}

// components/lovenotes/EnvelopeFlap.tsx
interface EnvelopeFlapProps {
  width: number;
  height: number;
}

// components/lovenotes/EnvelopeCard.tsx
interface EnvelopeCardProps {
  count: number;
  recipientName: string;
  onPress: () => void;
}

// components/lovenotes/LoveNoteCard.tsx
interface LoveNoteCardProps {
  note: LoveNote;
  profiles: Profile[];              // pour lookup from → name
  onPress?: (note: LoveNote) => void;
}
```

```typescript
// Tokens cosmétiques (constantes module — NE PAS ajouter dans constants/colors.ts per RESEARCH Open Question 1 recommendation implicit "impact commit footprint bas", garde scope Phase 35 minimal) :
const PAPER = '#f5ecd5';
const PAPER_SHADOW = '#c9b98a';
const FLAP_LIGHT = '#efdcb0';
const FLAP_DARK = '#d4bc85';
const WAX = '#c0392b';
const WAX_DARK = '#8b2518';
const WAX_HIGHLIGHT = '#e74c3c';
const INK = '#442434';
const INK_SOFT = '#6b5040';
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Créer WaxSeal.tsx + EnvelopeFlap.tsx + EnvelopeCard.tsx + barrel</name>
  <read_first>
    - .planning/phases/35-carte-enveloppe-dashboard-cran-bo-te-aux-lettres/35-RESEARCH.md (Pattern 2 pulse WaxSeal lignes 216-239, Pattern 4 SVG flap lignes 262-280, Pitfall 1 cleanup, Pitfall 7 texture, Pitfall 8 tilt hit-box)
    - .planning/mockups/lovenote-envelope.html (dimensions, couleurs CSS verbatim `--paper #f5ecd5`, `--wax #c0392b`, pulse 2.4s transform scale 1.06, stack rotation +2° -3° opacity 0.7/0.55, tilt -1.5°)
    - components/ui/PressableScale.tsx (wrapper press feedback)
    - constants/spacing.ts (Spacing['2xl'], Radius)
    - constants/shadows.ts (Shadows.card / Shadows.lg pour boxShadow multi-couche)
    - constants/typography.ts (FontSize)
    - CLAUDE.md (SPRING_CONFIG constante module, useThemeColors, cosmétiques OK inline, React.memo)
  </read_first>
  <files>components/lovenotes/WaxSeal.tsx, components/lovenotes/EnvelopeFlap.tsx, components/lovenotes/EnvelopeCard.tsx, components/lovenotes/index.ts</files>
  <action>
    **Fichier 1 : `components/lovenotes/WaxSeal.tsx`**
    - Imports : `React, { useEffect }` ; `View, Text, StyleSheet` de 'react-native' ; `Animated, useSharedValue, useAnimatedStyle, withRepeat, withTiming, cancelAnimation` de 'react-native-reanimated'
    - Constantes module (top): `const PULSE_DURATION = 1200; const PULSE_MAX = 1.06; const WAX = '#c0392b'; const WAX_DARK = '#8b2518'; const WAX_HIGHLIGHT = '#e74c3c'; const PAPER = '#f5ecd5';`
    - Export `WaxSeal` wrapped in `React.memo`
    - Props : `{ count: number; initial?: string; pulse?: boolean }` — default initial='M', pulse=true
    - Hook `useSharedValue(1)` pour scale
    - `useEffect`: si pulse → `scale.value = withRepeat(withTiming(PULSE_MAX, { duration: PULSE_DURATION }), -1, true);` ; cleanup `cancelAnimation(scale); scale.value = 1;` (Pitfall 1)
    - useAnimatedStyle `transform: [{ scale: scale.value }]`
    - Rendu : Animated.View 72x72, borderRadius 36, backgroundColor WAX, avec inner shadow simulé via inset style + Text central `{initial}` blanc
    - Badge : `{count >= 2 && <View style={styles.badge}><Text style={styles.badgeText}>{count}</Text></View>}` — position absolute top:-6 right:-6, minWidth 22, height 22, bg INK ('#442434'), border 2px PAPER, color white, fontWeight '700'
    - Commentaires français

    **Fichier 2 : `components/lovenotes/EnvelopeFlap.tsx`**
    - Import `Svg, { Polygon, Defs, LinearGradient, Stop }` de 'react-native-svg'
    - `View, StyleSheet` de 'react-native'
    - Export function `EnvelopeFlap({ width, height }: { width: number; height: number })`
    - Return Svg avec Polygon `points={\`0,0 ${width},0 ${width/2},${height}\`}` + fill via `url(#flap)` définissant un LinearGradient vertical stops #efdcb0 → #d4bc85
    - style={StyleSheet.absoluteFill} sur le Svg
    - Pas de memo nécessaire (déjà léger)

    **Fichier 3 : `components/lovenotes/EnvelopeCard.tsx`**
    - Imports : `React` ; `View, Text, StyleSheet, Pressable, useWindowDimensions` de 'react-native' ; `WaxSeal`, `EnvelopeFlap` locaux ; `PressableScale` de '../ui' (si dispo dans barrel, sinon direct path '../ui/PressableScale') — VÉRIFIER via read_first
    - Constantes module : `const PAPER = '#f5ecd5'; const PAPER_SHADOW = '#c9b98a'; const INK = '#442434'; const INK_SOFT = '#6b5040'; const TILT_DEG = '-1.5deg';`
    - Props : `{ count: number; recipientName: string; onPress: () => void }`
    - Layout :
      - Wrapper externe `View` avec padding vertical (Spacing.md), marginBottom Spacing.lg, contient le Pressable entier (Pitfall 8 : Pressable EXTERNE au transform rotate, hitSlop généreux { top:8, bottom:8, left:8, right:8 })
      - Pressable onPress={onPress} — wrap éventuellement via PressableScale (scale 0.97 au press)
      - Inner `<View style={[styles.envelope, { transform: [{ rotate: TILT_DEG }] }]}>` = zone visuelle tournée
      - Si count >= 2 : 2 Views absolute en fond (styles.stackBack1 + stackBack2) rendues AVANT le contenu, rotation +2° / -3°, opacity 0.70 / 0.55, backgroundColor '#e8d5a8' / '#dfc898', top 4 / 8
      - aspectRatio: 2/1.15 via style `aspectRatio: 2 / 1.15` (RN le supporte)
      - Flap : `<EnvelopeFlap width={cardWidth} height={cardHeight * 0.55} />` en absolute top:0 — calc avec useWindowDimensions (largeur écran - Spacing['2xl']*2) ou viaonLayout (préférer onLayout pour robustesse)
      - WaxSeal centré : View absolute left: '50%', top: '55%', transform [{ translateX: -36 }, { translateY: -36 }]
      - Recipient : View absolute left: Spacing.md, bottom: Spacing.md → 2 Texts : label 'POUR' (letterSpacing 2, fontSize FontSize.xs, color INK opacity 0.5) + name (fontSize FontSize.xl, color INK, fontStyle 'italic', fontWeight '600')
      - Tap hint : Text absolute bottom: -Spacing.lg center → 'tap pour ouvrir' (fontSize FontSize.xs, color blanc/muted, fontStyle italic) — OPTIONNEL, peut être omis pour minimiser visuel si thème light rend mal
    - Commentaires français
    - Export `React.memo(EnvelopeCard)`

    **Fichier 4 : `components/lovenotes/index.ts`**
    - Barrel : `export { WaxSeal } from './WaxSeal'; export { EnvelopeFlap } from './EnvelopeFlap'; export { EnvelopeCard } from './EnvelopeCard';`
    - Note : LoveNoteCard ajouté dans le même barrel à Task 2

    Ne PAS ajouter de tokens dans constants/colors.ts — rester en constantes module (per RESEARCH Open Question 1, MVP Phase 35 cosmétique inline).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -vE "(MemoryEditor|cooklang|useVault)\.(tsx|ts)" | grep -c "error TS"</automated>
  </verify>
  <acceptance_criteria>
    - `components/lovenotes/WaxSeal.tsx` existe et exporte WaxSeal (React.memo)
    - `grep -q "withRepeat(withTiming" components/lovenotes/WaxSeal.tsx` returns 0
    - `grep -q "cancelAnimation" components/lovenotes/WaxSeal.tsx` returns 0 (Pitfall 1 cleanup)
    - `grep -q "React.memo" components/lovenotes/WaxSeal.tsx` returns 0
    - `components/lovenotes/EnvelopeFlap.tsx` existe et exporte EnvelopeFlap utilisant `<Polygon points=`
    - `grep -q "react-native-svg" components/lovenotes/EnvelopeFlap.tsx` returns 0
    - `components/lovenotes/EnvelopeCard.tsx` existe et exporte EnvelopeCard
    - `grep -q "rotate.*-1.5" components/lovenotes/EnvelopeCard.tsx` returns 0 (tilt)
    - `grep -q "aspectRatio" components/lovenotes/EnvelopeCard.tsx` returns 0
    - `grep -q "WaxSeal\|EnvelopeFlap" components/lovenotes/EnvelopeCard.tsx` returns 0 (composition)
    - `grep -q "recipientName" components/lovenotes/EnvelopeCard.tsx` returns 0
    - `components/lovenotes/index.ts` existe et réexporte WaxSeal + EnvelopeFlap + EnvelopeCard
    - Aucun hardcoded sur propriétés thémées structurantes (text, bg card) : `grep -nE "color:\s*'#[0-9a-fA-F]" components/lovenotes/EnvelopeCard.tsx` — seules les constantes cosmétiques PAPER/INK/WAX sont acceptables (vérif manuelle, pas de assert strict)
    - `npx tsc --noEmit` : count nouvelles erreurs = 0
  </acceptance_criteria>
  <done>3 composants visuels + barrel en place, pulse cachet fonctionnel, SVG flap, tilt avec hit-box safe, tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Créer LoveNoteCard.tsx + câblage dans app/(tabs)/lovenotes.tsx</name>
  <read_first>
    - .planning/phases/35-carte-enveloppe-dashboard-cran-bo-te-aux-lettres/35-RESEARCH.md (Pattern 5 liste virtualisée, Pitfall 3 re-render cascade, Open Question 4 MarkdownText preview, Open Question 5 pending programmées affichage)
    - app/(tabs)/lovenotes.tsx (écran Plan 01 — chercher le stub inline View/Text à remplacer)
    - lib/types.ts (Profile.name, Profile.avatar, shape LoveNote)
    - lib/lovenotes/selectors.ts (Plan 01 — signatures selectors)
    - constants/colors.ts + themes.ts (colors.card, colors.text, colors.textMuted, colors.border)
    - constants/spacing.ts + typography.ts (tokens)
    - CLAUDE.md (format date JJ/MM/AAAA, React.memo list items, useCallback handlers, Haptics.impactAsync(Light))
    - hooks/useVaultLoveNotes.ts (LoveNote status 'pending' | 'revealed' | 'read')
  </read_first>
  <files>components/lovenotes/LoveNoteCard.tsx, components/lovenotes/index.ts, app/(tabs)/lovenotes.tsx</files>
  <action>
    **Fichier 1 : `components/lovenotes/LoveNoteCard.tsx`**
    - Imports : `React` ; `View, Text, StyleSheet, Pressable` de 'react-native' ; `useThemeColors` de '../../contexts/ThemeContext' ; `Spacing` de '../../constants/spacing' ; `FontSize` de '../../constants/typography' ; `type { LoveNote } from '../../lib/types'` ; `type { Profile } from '../../lib/types'` ; `Haptics` de 'expo-haptics'
    - Props : `{ note: LoveNote; profiles: Profile[]; onPress?: (note: LoveNote) => void }`
    - Lookup expéditeur : `const sender = profiles.find(p => p.id === note.from); const senderName = sender?.name ?? 'Famille';`
    - Format date : fonction locale `formatDateFR(iso: string)` qui prend `'2026-04-17T09:07:00'` et retourne `'17/04/2026'` — utiliser `iso.slice(0,10).split('-').reverse().join('/')`
    - Strip markdown inline pour preview : `const preview = note.body.replace(/[*_`#>]/g, '').trim();` (Open Question 4 recommendation)
    - Handle press : `const handlePress = useCallback(() => { Haptics.impactAsync(Haptics.ImpactFeedbackStyle.Light).catch(() => {}); onPress?.(note); }, [note, onPress]);`
    - Rendu Pressable :
      - Row horizontal : icône état à gauche (taille 44) + contenu flex:1 + date à droite
      - Icône état : couleur selon status — 'read' = muted subtle ('✉️'), 'revealed' = rouge cire animé (réutiliser WaxSeal mini size 32 avec pulse={true} count={0}) ; 'pending' avec revealAt futur = icône horloge '⏳' grise
      - Contenu : ligne 1 = `De ${senderName}` (fontSize FontSize.sm, color colors.textMuted, fontWeight '500') — ligne 2 = preview Text numberOfLines={2} (fontSize FontSize.md, color colors.text, fontStyle si non lu : italic)
      - Date à droite : formatDateFR(note.createdAt), fontSize FontSize.xs, color colors.textMuted
      - Background : colors.card ou colors.bgMuted, borderRadius 12, padding Spacing.md, borderWidth 1 StyleSheet.hairlineWidth, borderColor colors.border
      - Badge "programmée" si status === 'pending' && note.revealAt > now : small View sous la preview avec Text `🕓 Programmée pour ${formatDateFR(note.revealAt)}` (fontSize FontSize.xs, color colors.textMuted, fontStyle 'italic')
    - Export `React.memo(LoveNoteCard)` avec comparaison personnalisée : `(prev, next) => prev.note.sourceFile === next.note.sourceFile && prev.note.status === next.note.status && prev.note.readAt === next.note.readAt && prev.profiles === next.profiles` (Pitfall 3)
    - Commentaires français

    **Fichier 2 : `components/lovenotes/index.ts`** (mise à jour)
    - Ajouter : `export { LoveNoteCard } from './LoveNoteCard';`

    **Fichier 3 : `app/(tabs)/lovenotes.tsx`** (modification du Plan 01)
    - Remplacer le stub inline `renderItem` par :
      ```tsx
      const renderItem = useCallback(
        ({ item }: { item: LoveNote }) => <LoveNoteCard note={item} profiles={profiles} />,
        [profiles]
      );
      ```
    - Ajouter imports : `import { LoveNoteCard } from '../../components/lovenotes';`
    - Destructurer `profiles` depuis `useVault()` : `const { loveNotes, activeProfile, profiles } = useVault();`
    - Ne PAS changer la structure segmented / memo selectors — juste le renderItem

    Ne PAS appeler onPress (détail ouvert = Phase 36 unfold).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -vE "(MemoryEditor|cooklang|useVault)\.(tsx|ts)" | grep -c "error TS"</automated>
  </verify>
  <acceptance_criteria>
    - `components/lovenotes/LoveNoteCard.tsx` existe et exporte LoveNoteCard (React.memo)
    - `grep -q "React.memo" components/lovenotes/LoveNoteCard.tsx` returns 0
    - `grep -q "useThemeColors" components/lovenotes/LoveNoteCard.tsx` returns 0 (thème pour couleurs structurantes)
    - `grep -q "formatDateFR\|slice(0,10).split" components/lovenotes/LoveNoteCard.tsx` returns 0 (format JJ/MM/AAAA)
    - `grep -q "Haptics.impactAsync" components/lovenotes/LoveNoteCard.tsx` returns 0
    - `grep -q "numberOfLines" components/lovenotes/LoveNoteCard.tsx` returns 0
    - `grep -q "profiles.find" components/lovenotes/LoveNoteCard.tsx` returns 0 (lookup expéditeur)
    - `components/lovenotes/index.ts` : `grep -q "export { LoveNoteCard }" components/lovenotes/index.ts` returns 0
    - `app/(tabs)/lovenotes.tsx` : `grep -q "LoveNoteCard" app/(tabs)/lovenotes.tsx` returns 0
    - `app/(tabs)/lovenotes.tsx` : `grep -q "profiles" app/(tabs)/lovenotes.tsx` returns 0 (destructuré depuis useVault)
    - `npx tsc --noEmit` : count nouvelles erreurs = 0
  </acceptance_criteria>
  <done>LoveNoteCard livré et consommé par l'écran, preview + expéditeur + date + état affichés, memo stable.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` clean (hors erreurs pré-existantes CLAUDE.md)
- `npx jest --no-coverage` toujours clean (Plan 01 tests sélecteurs toujours verts)
- Vérification manuelle device : tous les composants se compilent (erreur runtime = fail)
</verification>

<success_criteria>
- WaxSeal, EnvelopeFlap, EnvelopeCard, LoveNoteCard livrés dans components/lovenotes/
- Barrel index.ts réexporte les 4 composants
- Écran /(tabs)/lovenotes affiche de vraies cartes (plus le stub du Plan 01)
- Aucune régression TS
- Plan 03 peut importer EnvelopeCard + calculer unreadCount pour more.tsx
</success_criteria>

<output>
After completion, create `.planning/phases/35-carte-enveloppe-dashboard-cran-bo-te-aux-lettres/35-02-SUMMARY.md`
</output>
