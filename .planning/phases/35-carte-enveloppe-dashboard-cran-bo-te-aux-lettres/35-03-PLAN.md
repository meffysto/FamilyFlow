---
phase: 35-carte-enveloppe-dashboard-cran-bo-te-aux-lettres
plan: 03
type: execute
wave: 3
depends_on: [35-01, 35-02]
files_modified:
  - app/(tabs)/index.tsx
  - app/(tabs)/more.tsx
autonomous: true
requirements: [LOVE-05, LOVE-07]
must_haves:
  truths:
    - "Dashboard (app/(tabs)/index.tsx) injecte <EnvelopeCard/> pinned tout en haut, AVANT le bloc sortedSections.map(), uniquement si pending.length > 0 pour le profil actif"
    - "Le sélecteur de visibilité considère une note comme 'à afficher' si status !== 'read' && (status === 'revealed' || (status === 'pending' && revealAt <= now)) — robuste à l'absence de bascule auto (Phase 36)"
    - "La carte disparaît automatiquement (render conditionnel pur, pas de flash) quand unreadCount passe à 0 (profile switch ou note lue)"
    - "Tap EnvelopeCard → Haptics.selectionAsync() + router.push('/(tabs)/lovenotes' as any) — pas de crash (route déclarée Plan 01)"
    - "more.tsx affiche une tuile permanente 'Love Notes' dans la catégorie 'famille' avec badge unreadCount || undefined, route '/(tabs)/lovenotes'"
  artifacts:
    - path: "app/(tabs)/index.tsx"
      provides: "Injection EnvelopeCard pinned avant sortedSections"
      contains: "EnvelopeCard"
    - path: "app/(tabs)/more.tsx"
      provides: "Tuile permanente Love Notes catégorie famille"
      contains: "Love Notes"
  key_links:
    - from: "app/(tabs)/index.tsx"
      to: "components/lovenotes/EnvelopeCard + lib/lovenotes/selectors.unreadForProfile"
      via: "import + useMemo([loveNotes, activeProfile?.id])"
      pattern: "unreadForProfile\\(loveNotes"
    - from: "app/(tabs)/index.tsx"
      to: "app/(tabs)/lovenotes.tsx"
      via: "router.push('/(tabs)/lovenotes' as any) dans onPress"
      pattern: "router\\.push.*lovenotes"
    - from: "app/(tabs)/more.tsx"
      to: "app/(tabs)/lovenotes.tsx"
      via: "entry dans array items[] catégorie famille avec route '/(tabs)/lovenotes'"
      pattern: "'/(tabs)/lovenotes'"
    - from: "app/(tabs)/index.tsx EnvelopeCard injection"
      to: "SectionErrorBoundary"
      via: "wrapping explicit"
      pattern: "SectionErrorBoundary name=\"Love Notes\""
---

<objective>
Câbler les consommateurs finaux : injecter EnvelopeCard conditionnelle pinned dans le dashboard (app/(tabs)/index.tsx) et ajouter la tuile permanente Love Notes dans le menu more.tsx (catégorie 'famille'). Après cette wave, Phase 35 est user-visible : depuis le dashboard (quand notes en attente) OU depuis more (toujours).

Purpose: Finaliser la boucle d'accès utilisateur en réutilisant les primitives livrées par Plan 01 (route + selectors) et Plan 02 (composant EnvelopeCard). Aucune logique métier nouvelle — pure composition.
Output: 2 fichiers édités, aucun nouveau fichier.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/35-carte-enveloppe-dashboard-cran-bo-te-aux-lettres/35-RESEARCH.md
@.planning/phases/35-carte-enveloppe-dashboard-cran-bo-te-aux-lettres/35-01-SUMMARY.md
@.planning/phases/35-carte-enveloppe-dashboard-cran-bo-te-aux-lettres/35-02-SUMMARY.md
@app/(tabs)/index.tsx
@app/(tabs)/more.tsx
@components/lovenotes/index.ts
@lib/lovenotes/selectors.ts
@components/SectionErrorBoundary.tsx
@CLAUDE.md

<interfaces>
<!-- Points d'injection précis (vérifiés RESEARCH.md) -->

**app/(tabs)/index.tsx :**
- Injection JUSTE AVANT le bloc `(() => { const visibleSections = sortedSections.filter(...` (RESEARCH indique ligne ~1038, exact selon version courante — repérer par grep pattern `sortedSections.filter` ou `visibleSections`)
- Destructurer `loveNotes` depuis `useVault()` si pas déjà présent
- Import `EnvelopeCard` depuis '../../components/lovenotes'
- Import `unreadForProfile` depuis '../../lib/lovenotes/selectors'
- Import `SectionErrorBoundary` (déjà importé vraisemblablement — vérifier)
- Import `Haptics` de 'expo-haptics' (déjà importé vraisemblablement — vérifier)
- Import `router` de 'expo-router' (déjà présent)

**app/(tabs)/more.tsx :**
- Ajouter dans le return de `items` (useMemo, après la ligne 147 `notes` vers 148) UNE entrée :
  ```tsx
  { emoji: '💌', label: 'Love Notes', route: '/(tabs)/lovenotes' as any, badge: loveNoteUnreadCount || undefined, color: colors.catFamille, category: 'famille' as const },
  ```
- Destructurer `loveNotes` depuis `useVault()` dans le composant
- Calculer `loveNoteUnreadCount` via selector unreadForProfile(loveNotes, activeProfile?.id ?? '') — ou inline filter équivalent
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Injecter EnvelopeCard pinned dans app/(tabs)/index.tsx</name>
  <read_first>
    - .planning/phases/35-carte-enveloppe-dashboard-cran-bo-te-aux-lettres/35-RESEARCH.md (Pattern 1 injection, Code Example 3 lignes 531-553 snippet verbatim, Pitfall 2 ordering, Pitfall 3 memo, Pitfall 4 deps activeProfile.id, Pitfall 5 pending fallback)
    - app/(tabs)/index.tsx (repérer `sortedSections.filter` pour point d'injection + vérifier useVault destructuring existant + imports Haptics/router présents)
    - components/lovenotes/index.ts (barrel — confirme export EnvelopeCard)
    - components/SectionErrorBoundary.tsx (prop name, wrapping)
    - lib/lovenotes/selectors.ts (unreadForProfile signature)
    - CLAUDE.md (useMemo dans consommateurs, SectionErrorBoundary obligatoire, Haptics.selectionAsync au tap)
  </read_first>
  <files>app/(tabs)/index.tsx</files>
  <action>
    1. **Imports** (top du fichier) — ajouter si absents :
       - `import { EnvelopeCard } from '../../components/lovenotes';`
       - `import { unreadForProfile } from '../../lib/lovenotes/selectors';`
       - Vérifier : `SectionErrorBoundary` (déjà importé pour autres sections — confirmer), `Haptics` de 'expo-haptics' (déjà utilisé dans le fichier — confirmer par grep), `router` de 'expo-router' (déjà importé — confirmer)

    2. **Destructuration `useVault()`** : ajouter `loveNotes` à la liste des valeurs destructurées (la ligne `const { ... } = useVault();` existe déjà — ajouter `loveNotes` à la liste).

    3. **Memo pending** : juste avant le point d'injection, ajouter :
       ```tsx
       const pendingLoveNotes = useMemo(
         () => (activeProfile ? unreadForProfile(loveNotes, activeProfile.id) : []),
         [loveNotes, activeProfile?.id]
       );
       ```
       Placer ce useMemo dans la zone des autres useMemo du composant (près des sortedSections ou équivalent).

    4. **Injection JSX** : repérer le bloc `(() => { const visibleSections = sortedSections.filter(...` (recherche par pattern `sortedSections.filter` ou `visibleSections`). JUSTE AVANT ce bloc, insérer :
       ```tsx
       {pendingLoveNotes.length > 0 && activeProfile && (
         <SectionErrorBoundary key="lovenotes-envelope" name="Love Notes">
           <EnvelopeCard
             count={pendingLoveNotes.length}
             recipientName={activeProfile.name}
             onPress={() => {
               Haptics.selectionAsync().catch(() => {});
               router.push('/(tabs)/lovenotes' as any);
             }}
           />
         </SectionErrorBoundary>
       )}
       ```

    5. Ne toucher à RIEN d'autre (pas de reorg, pas de modif sortedSections, pas de changement de ROADMAP).

    **Contraintes :**
    - activeProfile peut être null → guarded (`activeProfile &&`)
    - Ne PAS ajouter à sortedSections (la carte doit rester pinned hors config) — per RESEARCH Anti-Pattern
    - Haptics.selectionAsync() wrap .catch(() => {}) (pattern projet pour robustesse indispo)
    - router.push('/(tabs)/lovenotes' as any) — cast any obligatoire (route non typée par expo-router avant migration types)
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -vE "(MemoryEditor|cooklang|useVault)\.(tsx|ts)" | grep -c "error TS"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "import { EnvelopeCard }" app/(tabs)/index.tsx` returns 0
    - `grep -q "unreadForProfile" app/(tabs)/index.tsx` returns 0
    - `grep -q "pendingLoveNotes" app/(tabs)/index.tsx` returns 0
    - `grep -qE "useMemo\(\s*\(\)\s*=>.*unreadForProfile" app/(tabs)/index.tsx` OR multiline `grep -q "unreadForProfile(loveNotes" app/(tabs)/index.tsx` returns 0
    - `grep -q "SectionErrorBoundary.*Love Notes\|name=\"Love Notes\"" app/(tabs)/index.tsx` returns 0
    - `grep -q "router.push('/(tabs)/lovenotes' as any)" app/(tabs)/index.tsx` returns 0
    - `grep -q "Haptics.selectionAsync" app/(tabs)/index.tsx` returns 0 (au moins 1 occurrence — peut déjà exister ailleurs, OK)
    - `grep -q "recipientName={activeProfile.name}" app/(tabs)/index.tsx` returns 0
    - L'injection est AVANT `sortedSections.filter` : `awk '/pendingLoveNotes.length > 0/{found=1} /sortedSections.filter/{if (found) print "OK"; exit}' app/(tabs)/index.tsx` produit 'OK'
    - La carte n'apparaît PAS dans sortedSections : `grep -c "lovenotes-envelope\|Love Notes" app/(tabs)/index.tsx` ≤ 3 (uniquement lié à l'injection pinned, pas à une config section)
    - `npx tsc --noEmit` : count nouvelles erreurs = 0
  </acceptance_criteria>
  <done>EnvelopeCard injectée conditionnelle, pinned avant sortedSections, wrapping SectionErrorBoundary, navigation câblée, tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Ajouter tuile Love Notes permanente dans app/(tabs)/more.tsx</name>
  <read_first>
    - .planning/phases/35-carte-enveloppe-dashboard-cran-bo-te-aux-lettres/35-RESEARCH.md (Code Example 4 lignes 557-568 snippet tuile, Pitfall 2)
    - app/(tabs)/more.tsx (intégralement — repérer destructuring useVault, array items useMemo, ligne catégorie famille autour de 143-148, pattern badge count via filter notes.length)
    - lib/lovenotes/selectors.ts (unreadForProfile)
    - CLAUDE.md (tokens useThemeColors, catégorie famille = catFamille)
  </read_first>
  <files>app/(tabs)/more.tsx</files>
  <action>
    1. **Imports** :
       - Ajouter `import { unreadForProfile } from '../../lib/lovenotes/selectors';` (au bloc d'imports existants)

    2. **Destructuration useVault** : vérifier si `loveNotes` et `activeProfile` sont déjà destructurés (activeProfile l'est — cf ligne 140 reference `activeProfile?.gardenName`). Ajouter `loveNotes` à la destructuration de `useVault()`.

    3. **Calcul badge** : au niveau du composant, avant le `useMemo` items, ajouter :
       ```tsx
       const loveNoteUnreadCount = useMemo(
         () => (activeProfile ? unreadForProfile(loveNotes, activeProfile.id).length : 0),
         [loveNotes, activeProfile?.id]
       );
       ```

    4. **Ajouter entry** dans l'array `items` du useMemo, DANS la catégorie 'famille' (juste après `notes` ligne 147, avant `stats` ligne 148) :
       ```tsx
       { emoji: '💌', label: 'Love Notes', route: '/(tabs)/lovenotes' as any, badge: loveNoteUnreadCount || undefined, color: colors.catFamille, category: 'famille' as const },
       ```
       - Note : ne PAS utiliser `t(...)` (pas de clé i18n 'menu.items.loveNotes' à créer en Phase 35 — garder scope minimal, utiliser string literale 'Love Notes').
       - Inclure dans les deps du useMemo items : ajouter `loveNoteUnreadCount` aux dependencies array.

    5. Ne toucher à RIEN d'autre (ordre autres items, catégories, styles).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -vE "(MemoryEditor|cooklang|useVault)\.(tsx|ts)" | grep -c "error TS"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "unreadForProfile" app/(tabs)/more.tsx` returns 0
    - `grep -q "loveNoteUnreadCount" app/(tabs)/more.tsx` returns 0
    - `grep -q "loveNotes" app/(tabs)/more.tsx` returns 0
    - `grep -q "Love Notes" app/(tabs)/more.tsx` returns 0
    - `grep -q "'/(tabs)/lovenotes' as any\|'/(tabs)/lovenotes'" app/(tabs)/more.tsx` returns 0
    - `grep -q "💌" app/(tabs)/more.tsx` returns 0
    - `grep -q "catFamille" app/(tabs)/more.tsx` returns 0 (déjà présent — confirme pas cassé)
    - L'entry est dans la catégorie 'famille' : vérification manuelle — position entre les autres entries famille (anniversaires, wishlist, budget, notes, stats)
    - `npx tsc --noEmit` : count nouvelles erreurs = 0
  </acceptance_criteria>
  <done>Tuile Love Notes visible en permanence dans more.tsx catégorie famille, badge unreadCount affiché si > 0, tsc clean.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` clean hors erreurs pré-existantes
- `npx jest --no-coverage` clean (Plan 01 tests toujours verts)
- Flow manuel (device) :
  1. Seed 1 love note revealed pour profil actif → dashboard montre EnvelopeCard tout en haut avec cachet pulse, tap → /lovenotes
  2. Seed 2+ love notes revealed → badge compteur + stack effect visibles
  3. Toutes lues → EnvelopeCard disparaît sans flash
  4. Switch profil sans note → EnvelopeCard disparaît immédiatement
  5. more.tsx → tuile 💌 Love Notes visible en permanence catégorie famille, badge si notes en attente
</verification>

<success_criteria>
- Success Criterion 1 (carte enveloppe pinned) : LOVE-05 livré via injection index.tsx
- Success Criterion 2 (badge + stack ≥2) : LOVE-06 livré via EnvelopeCard Plan 02 (count prop) + cette injection passe le count
- Success Criterion 3 (accès via carte ET via more) : LOVE-07 livré via router.push + tuile more.tsx
- Success Criterion 4 (écran 3 segments) : LOVE-08 livré via Plan 01 + Plan 02 (LoveNoteCard + selectors)
- Success Criterion 5 (disparition auto render conditionnel) : LOVE-05 livré via `pendingLoveNotes.length > 0 && ...` guard
- Aucune régression : `npx tsc --noEmit` clean, `npx jest --no-coverage` clean
</success_criteria>

<output>
After completion, create `.planning/phases/35-carte-enveloppe-dashboard-cran-bo-te-aux-lettres/35-03-SUMMARY.md`
</output>
