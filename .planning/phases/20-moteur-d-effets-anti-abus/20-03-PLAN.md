---
phase: 20-moteur-d-effets-anti-abus
plan: 03
type: execute
wave: 2
depends_on: ["20-01", "20-02"]
files_modified:
  - hooks/useGamification.ts
  - hooks/useFarm.ts
autonomous: true
requirements:
  - SEMANTIC-08
  - SEMANTIC-09

must_haves:
  truths:
    - "Completer une tache avec tag #urgent active multiplier x2 pour 5 taches suivantes"
    - "Un streak >7j declenche un Double Loot Cascade (second loot box)"
    - "applyTaskEffect() est appele dans completeTask apres advanceFarmCrops, garde par isSemanticCouplingEnabled()"
    - "Un seul write farm-{id}.md par completeTask (pas de double write)"
    - "Les bonus temporels (turbo/sprint/capacity) sont consommes dans useFarm"
  artifacts:
    - path: "hooks/useGamification.ts"
      provides: "completeTask integre avec dispatcher + caps + urgent multiplier + streak cascade"
      contains: "applyTaskEffect"
    - path: "hooks/useFarm.ts"
      provides: "Consommation des bonus temporels dans les calculs ferme"
      contains: "buildingTurboUntil"
  key_links:
    - from: "hooks/useGamification.ts"
      to: "lib/semantic/effects.ts"
      via: "import applyTaskEffect"
      pattern: "import.*applyTaskEffect.*from.*semantic"
    - from: "hooks/useGamification.ts"
      to: "lib/semantic/caps.ts"
      via: "import loadCaps, saveCaps, isCapExceeded, incrementCap"
      pattern: "import.*loadCaps.*from.*semantic"
    - from: "hooks/useGamification.ts"
      to: "lib/semantic/flag.ts"
      via: "import isSemanticCouplingEnabled"
      pattern: "import.*isSemanticCouplingEnabled.*from.*semantic"
---

<objective>
Cabler le dispatcher d'effets dans `completeTask`, ajouter le multiplier urgent (SEMANTIC-08), le Double Loot Cascade streak (SEMANTIC-09), et la consommation des bonus temporels dans `useFarm`.

Purpose: C'est le plan de wiring — connecter le moteur d'effets (Plan 01) et les caps (Plan 02) dans le flux de completion de tache existant. Aussi implementer les deux bonus cross-categorie (urgent + streak).
Output: `useGamification.completeTask()` enrichi, `useFarm` consommant les bonus temporels.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-moteur-d-effets-anti-abus/20-RESEARCH.md

<interfaces>
From lib/semantic/effects.ts (Plan 01):
```typescript
export type EffectId = 'weeds_removed' | 'wear_repaired' | 'building_turbo' | 'companion_mood' | 'growth_sprint' | 'rare_seed_drop' | 'saga_trait_boost' | 'capacity_boost' | 'golden_harvest' | 'recipe_unlock';
export interface EffectResult {
  effectApplied: EffectId | null;
  farmData: FarmProfileData;
  sagaTraitDelta?: { trait: string; amount: number };
  companionEvent?: string;
  message?: string;
}
export function applyTaskEffect(match: CategoryMatch, farmData: FarmProfileData, now?: Date): EffectResult;
```

From lib/semantic/caps.ts (Plan 02):
```typescript
export async function loadCaps(profileId: string): Promise<CouplingCaps>;
export async function saveCaps(profileId: string, caps: CouplingCaps): Promise<void>;
export function isCapExceeded(categoryId: CategoryId, caps: CouplingCaps, now?: Date): boolean;
export function incrementCap(caps: CouplingCaps, categoryId: CategoryId, now?: Date): CouplingCaps;
```

From lib/semantic/index.ts (Phase 19):
```typescript
export { deriveTaskCategory } from './derive';
export { isSemanticCouplingEnabled } from './flag';
export type { CategoryId, CategoryMatch } from './categories';
```

From hooks/useGamification.ts (current):
```typescript
completeTask: (profile: Profile, taskText: string) => Promise<{
  updatedProfile: Profile;
  lootAwarded: boolean;
  pointsGained: number;
  cropsMatured: string[];
}>;
```

From lib/types.ts:
```typescript
interface Task {
  text: string;
  completed: boolean;
  tags?: string[];
  section?: string;
  sourceFile: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enrichir completeTask avec dispatcher + caps + urgent + streak</name>
  <files>hooks/useGamification.ts</files>
  <read_first>hooks/useGamification.ts, lib/semantic/effects.ts, lib/semantic/caps.ts, lib/semantic/derive.ts, lib/types.ts, lib/gamification/engine.ts</read_first>
  <action>
**CRITIQUE : Pitfall 2 RESEARCH.md — un seul write farm-{id}.md par completeTask.**

1. **Changer la signature de `completeTask`** (ligne 46 dans l'interface + ligne 76-77 dans le useCallback) :
- Ancien : `completeTask: (profile: Profile, taskText: string) => Promise<...>`
- Nouveau : `completeTask: (profile: Profile, taskText: string, taskMeta?: { tags?: string[]; section?: string; sourceFile?: string }) => Promise<...>`
- Le `taskMeta` est optionnel pour retrocompatibilite. Si absent, `deriveTaskCategory` recevra un Task minimal avec seulement `text` et `sourceFile: ''`.
- NE PAS changer `taskText` en objet — garder l'ancien parametre pour ne casser aucun appelant existant. Le `taskMeta` enrichit facultativement.

2. **Ajouter les imports** (en haut du fichier, apres les imports existants) :
```typescript
import {
  deriveTaskCategory,
  isSemanticCouplingEnabled,
  applyTaskEffect,
  loadCaps,
  saveCaps,
  isCapExceeded,
  incrementCap,
} from '../lib/semantic';
import type { EffectResult } from '../lib/semantic';
import type { Task } from '../lib/types';
```

3. **Ajouter le retour `effectResult` dans le type de retour** de `completeTask` :
```typescript
completeTask: (profile: Profile, taskText: string, taskMeta?: { tags?: string[]; section?: string; sourceFile?: string }) => Promise<{
  updatedProfile: Profile;
  lootAwarded: boolean;
  pointsGained: number;
  cropsMatured: string[];
  effectResult?: EffectResult | null;  // Phase 20 — resultat de l'effet semantique
}>;
```

4. **Inserer le bloc semantic entre le write farm et la notification** (apres la ligne `await vault.writeFile(fp, serializeFarmProfile(profile.name, farmData));` autour de la ligne 148, MAIS en restructurant pour eviter le double write) :

La restructuration du bloc farm dans completeTask doit suivre cette sequence :
```typescript
// --- Bloc farm restructure (remplace les lignes 128-148 environ) ---
let cropsMatured: string[] = [];
let effectResult: EffectResult | null = null;

const fp = farmFile(profile.id);
const farmContent = await vault.readFile(fp).catch(() => '');
let farmData = parseFarmProfile(farmContent);

// 1. Avancer les cultures (existant)
const currentCrops = parseCrops(farmData.farmCrops ?? '');
if (currentCrops.length > 0) {
  const profileTechBonuses = getTechBonuses(profile.farmTech ?? []);
  // Phase 20 : Growth Sprint bonus temporel (EFFECTS-05)
  if (farmData.growthSprintUntil && new Date(farmData.growthSprintUntil) > new Date()) {
    profileTechBonuses.tasksPerStageReduction = (profileTechBonuses.tasksPerStageReduction ?? 0) + 1;
  }
  let questFarmEffect: QuestFarmEffect | undefined;
  try {
    const questsContent = await vault.readFile(FAMILY_QUESTS_FILE).catch(() => '');
    const questsMeta = parseFamilyQuestsMeta(questsContent);
    const activeEffect = getActiveQuestEffect(questsMeta);
    if (activeEffect?.type === 'rain_bonus' || activeEffect?.type === 'golden_rain') {
      questFarmEffect = activeEffect.type;
    }
  } catch { /* Quest — non-critical */ }
  const farmResult = advanceFarmCrops(currentCrops, profileTechBonuses, questFarmEffect);
  cropsMatured = farmResult.matured.map(c => c.cropId);
  farmData.farmCrops = serializeCrops(farmResult.crops);
}

// 2. Phase 20 : Appliquer l'effet semantique (SEMANTIC-06..10, EFFECTS-01..10)
try {
  const enabled = await isSemanticCouplingEnabled();
  if (enabled && taskMeta) {
    const task: Pick<Task, 'text' | 'tags' | 'section' | 'sourceFile'> = {
      text: taskText,
      tags: taskMeta.tags,
      section: taskMeta.section,
      sourceFile: taskMeta.sourceFile ?? '',
    };
    const category = deriveTaskCategory(task as Task);
    if (category) {
      const caps = await loadCaps(profile.id);
      if (!isCapExceeded(category.id, caps)) {
        effectResult = applyTaskEffect(category, farmData);
        if (effectResult.effectApplied) {
          farmData = effectResult.farmData;
          await saveCaps(profile.id, incrementCap(caps, category.id));
        }
      }
    }
  }
} catch {
  if (__DEV__) console.warn('Semantic coupling — non-critical error');
}

// 3. Ecrire farm-{id}.md UNE SEULE FOIS (crops + effets combines)
await vault.writeFile(fp, serializeFarmProfile(profile.name, farmData));
```

5. **Multiplier urgent (SEMANTIC-08)** — inserer AVANT le bloc `awardTaskCompletion` (avant la ligne 97 actuelle), apres le calcul du companion bonus :
```typescript
// Phase 20 : Multiplier urgent ×2 pour 5 taches (SEMANTIC-08)
if (taskMeta?.tags?.includes('urgent') && profileWithCompanionBonus.multiplierRemaining === 0) {
  profileWithCompanionBonus = {
    ...profileWithCompanionBonus,
    multiplier: 2,
    multiplierRemaining: 5,
  };
}
```
Note : `profileWithCompanionBonus` devra etre declaree avec `let` au lieu de `const` pour cette mutation.

6. **Double Loot Cascade (SEMANTIC-09)** — inserer APRES le bloc `awardTaskCompletion` + updateProfileInData, avant le write gami :
```typescript
// Phase 20 : Double Loot Cascade si streak > 7j (SEMANTIC-09)
let cascadeBox: LootBox | undefined;
if ((currentStreak + 1) > 7 && lootAwarded) {
  try {
    const cascadeResult = doOpenLootBox(updatedProfile, newData);
    cascadeBox = cascadeResult.box;
    // Fusionner le profil et les donnees mis a jour
    Object.assign(newData, {
      profiles: cascadeResult.newData.profiles,
      history: cascadeResult.newData.history,
      activeRewards: cascadeResult.newData.activeRewards,
      usedLoots: cascadeResult.newData.usedLoots,
    });
    // Mettre a jour le profil pour refléter le second loot box
    const cascadeProfile = cascadeResult.newData.profiles.find((p: Profile) => p.id === profile.id);
    if (cascadeProfile) {
      updatedProfile = cascadeProfile;
    }
  } catch {
    if (__DEV__) console.warn('Double Loot Cascade — non-critical');
  }
}
```
Note : `updatedProfile` devra etre declaree avec `let` au lieu de `const` pour cette mutation.

7. **Ajouter `effectResult` au retour** :
```typescript
return {
  updatedProfile,
  lootAwarded,
  pointsGained: entry.points,
  cropsMatured,
  effectResult,  // Phase 20
};
```

**Variables a changer de `const` a `let` :**
- `profileWithCompanionBonus` (ligne ~94) — pour le multiplier urgent
- `updatedProfile` (ligne ~101-103) — pour le Double Loot Cascade
- Garder `const` pour tout le reste

**IMPORTANT — Pitfall 2 :** Le read `vault.readFile(fp)` et le write `vault.writeFile(fp, ...)` pour `farm-{id}.md` ne doivent apparaitre qu'UNE SEULE FOIS dans tout le `completeTask`. L'ancien bloc qui faisait `const farmContent = await vault.readFile(fp)...` vers la ligne 145 doit etre SUPPRIME et remplace par le nouveau bloc restructure ci-dessus.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep "useGamification" | head -10; echo "---"; grep -c "applyTaskEffect\|isCapExceeded\|isSemanticCouplingEnabled\|deriveTaskCategory\|multiplierRemaining\|Double Loot Cascade" hooks/useGamification.ts</automated>
  </verify>
  <acceptance_criteria>
    - hooks/useGamification.ts importe `applyTaskEffect` depuis `../lib/semantic`
    - hooks/useGamification.ts importe `loadCaps, saveCaps, isCapExceeded, incrementCap` depuis `../lib/semantic`
    - hooks/useGamification.ts importe `isSemanticCouplingEnabled` depuis `../lib/semantic`
    - hooks/useGamification.ts importe `deriveTaskCategory` depuis `../lib/semantic`
    - hooks/useGamification.ts contient `taskMeta?:` dans la signature de completeTask
    - hooks/useGamification.ts contient `applyTaskEffect(category, farmData)` dans le corps de completeTask
    - hooks/useGamification.ts contient `isCapExceeded(category.id, caps)` dans le corps de completeTask
    - hooks/useGamification.ts contient `tags?.includes('urgent')` pour le multiplier (SEMANTIC-08)
    - hooks/useGamification.ts contient `multiplierRemaining: 5` pour le multiplier urgent
    - hooks/useGamification.ts contient `(currentStreak + 1) > 7` pour le Double Loot Cascade (SEMANTIC-09)
    - hooks/useGamification.ts contient `doOpenLootBox` appele une seconde fois pour la cascade
    - hooks/useGamification.ts ne contient qu'UN SEUL `vault.writeFile(fp,` pour farm-{id}.md (pas de double write)
    - hooks/useGamification.ts contient `effectResult` dans le return du completeTask
    - `npx tsc --noEmit` ne produit aucune nouvelle erreur dans useGamification.ts
  </acceptance_criteria>
  <done>completeTask integre avec le dispatcher d'effets, les caps anti-abus, le multiplier urgent x2/5 taches, le Double Loot Cascade streak >7j, et un seul write farm par execution.</done>
</task>

<task type="auto">
  <name>Task 2: Consommer les bonus temporels dans useFarm</name>
  <files>hooks/useFarm.ts</files>
  <read_first>hooks/useFarm.ts, lib/types.ts, lib/mascot/building-engine.ts, lib/mascot/tech-engine.ts</read_first>
  <action>
Identifier les points de consommation des 3 bonus temporels dans `useFarm.ts` :

1. **EFFECTS-03 Building Turbo** : trouver l'appel a `getPendingResources()` ou le calcul du `questSpeedMultiplier`. Si `farmData.buildingTurboUntil` est dans le futur (`new Date(farmData.buildingTurboUntil) > new Date()`), passer un `questSpeedMultiplier = 2` (ou multiplier le multiplier existant par 2). Si le champ est absent ou dans le passe, ne rien changer.

Ajouter avant l'appel a `getPendingResources` (ou equivalent) :
```typescript
// Phase 20 : Building Turbo (EFFECTS-03) — production 2x plus rapide si actif
let effectiveSpeedMultiplier = questSpeedMultiplier ?? 1;
if (farmData.buildingTurboUntil && new Date(farmData.buildingTurboUntil) > new Date()) {
  effectiveSpeedMultiplier *= 2;
}
```
Puis passer `effectiveSpeedMultiplier` au lieu de `questSpeedMultiplier`.

2. **EFFECTS-08 Capacity Boost** : trouver l'appel a `getTechBonuses()` ou l'utilisation de `techBonuses.buildingCapacityMultiplier`. Si `farmData.capacityBoostUntil` est dans le futur, doubler la capacite :
```typescript
// Phase 20 : Capacity Boost (EFFECTS-08) — capacite batiments x2 si actif
if (farmData.capacityBoostUntil && new Date(farmData.capacityBoostUntil) > new Date()) {
  techBonuses.buildingCapacityMultiplier = (techBonuses.buildingCapacityMultiplier ?? 1) * 2;
}
```

3. **EFFECTS-09 Golden Harvest** : trouver le handler de recolte (harvest). Si `farmData.nextHarvestGolden === true`, forcer le resultat de la recolte comme "golden" avec le multiplicateur `EFFECT_GOLDEN_MULTIPLIER = 3` (importer depuis `../lib/semantic`). Apres consommation, remettre `farmData.nextHarvestGolden = false` et persister.
```typescript
import { EFFECT_GOLDEN_MULTIPLIER } from '../lib/semantic';

// Dans le handler de recolte :
let harvestMultiplier = isGolden ? GOLDEN_HARVEST_MULTIPLIER : 1;
if (farmData.nextHarvestGolden) {
  harvestMultiplier = EFFECT_GOLDEN_MULTIPLIER; // ×3 (EFFECTS-09)
  farmData.nextHarvestGolden = false;
  // Persister le reset du flag
  await vault.writeFile(farmFile(profile.id), serializeFarmProfile(profile.name, farmData));
}
```

**IMPORTANT :** Ne modifier que les points de consommation existants. Ne PAS restructurer le hook entier. Chaque bonus temporel est une condition supplementaire greffee au bon endroit. Chaque modification est protegee par un `if` qui teste la date/flag — zero impact si le champ est absent.

**Si `useFarm.ts` n'utilise pas directement `getPendingResources` ou `buildingCapacityMultiplier` :** localiser ou ces calculs sont faits (peut-etre dans le composant appelant ou dans un autre hook) et documenter dans le SUMMARY quel fichier a ete modifie a la place.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep "useFarm" | head -10; echo "---"; grep -c "buildingTurboUntil\|capacityBoostUntil\|nextHarvestGolden\|EFFECT_GOLDEN_MULTIPLIER" hooks/useFarm.ts</automated>
  </verify>
  <acceptance_criteria>
    - hooks/useFarm.ts contient `buildingTurboUntil` (check bonus turbo actif)
    - hooks/useFarm.ts contient `capacityBoostUntil` (check bonus capacite actif)
    - hooks/useFarm.ts contient `nextHarvestGolden` (check golden harvest flag)
    - hooks/useFarm.ts importe `EFFECT_GOLDEN_MULTIPLIER` depuis `../lib/semantic`
    - Chaque bonus temporel est conditionne par une comparaison de date (ou check boolean pour golden)
    - `npx tsc --noEmit` ne produit aucune nouvelle erreur
  </acceptance_criteria>
  <done>Les 3 bonus temporels (turbo batiments, capacite x2, golden harvest) sont consommes dans useFarm au bon moment, zero impact quand les champs sont absents.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` ne produit aucune nouvelle erreur dans useGamification.ts et useFarm.ts
- `grep "applyTaskEffect" hooks/useGamification.ts` retourne au moins 1 ligne
- `grep "buildingTurboUntil" hooks/useFarm.ts` retourne au moins 1 ligne
- `grep "vault.writeFile(fp," hooks/useGamification.ts | wc -l` retourne exactement 1 (un seul write farm)
</verification>

<success_criteria>
- completeTask integre : feature flag check -> derive category -> cap check -> apply effect -> save caps
- Multiplier urgent x2 active pour 5 taches quand tag #urgent present
- Double Loot Cascade declenche quand streak > 7j et loot accorde
- Un seul write farm-{id}.md par execution de completeTask
- Bonus temporels consommes dans useFarm (turbo, capacity, golden)
- Signature backward-compatible (taskMeta optionnel)
</success_criteria>

<output>
After completion, create `.planning/phases/20-moteur-d-effets-anti-abus/20-03-SUMMARY.md`
</output>
