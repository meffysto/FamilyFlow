---
phase: 20-moteur-d-effets-anti-abus
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/semantic/effects.ts
  - lib/semantic/index.ts
  - lib/types.ts
  - lib/parser.ts
autonomous: true
requirements:
  - SEMANTIC-06
  - EFFECTS-01
  - EFFECTS-02
  - EFFECTS-03
  - EFFECTS-04
  - EFFECTS-05
  - EFFECTS-06
  - EFFECTS-07
  - EFFECTS-08
  - EFFECTS-09
  - EFFECTS-10

must_haves:
  truths:
    - "Completer une tache menage quotidien retire un weed de la ferme (EFFECTS-01)"
    - "Completer une tache courses accelere les batiments pendant 24h (EFFECTS-03)"
    - "Completer une tache bebe soins marque la prochaine recolte comme doree (EFFECTS-09)"
    - "Completer une tache cuisine debloque une recette speciale (EFFECTS-10)"
    - "Les bonus temporels (turbo/sprint/capacite) persistent entre les restarts via le parser"
  artifacts:
    - path: "lib/semantic/effects.ts"
      provides: "Dispatcher applyTaskEffect() + 10 handlers + types EffectId/EffectResult"
      exports: ["applyTaskEffect", "EffectId", "EffectResult", "EFFECT_GOLDEN_MULTIPLIER"]
    - path: "lib/types.ts"
      provides: "FarmProfileData etendu avec champs temporels"
      contains: "buildingTurboUntil"
    - path: "lib/parser.ts"
      provides: "Parse/serialize des 6 nouveaux champs farm"
      contains: "building_turbo_until"
  key_links:
    - from: "lib/semantic/effects.ts"
      to: "lib/semantic/categories.ts"
      via: "import CategoryId, CategoryMatch"
      pattern: "import.*CategoryId.*from.*categories"
    - from: "lib/semantic/effects.ts"
      to: "lib/mascot/wear-engine.ts"
      via: "import repairWearEvent pour EFFECTS-01/02"
      pattern: "import.*repairWearEvent.*from.*wear-engine"
---

<objective>
Creer le dispatcher `applyTaskEffect()` avec ses 10 handlers d'effets, etendre `FarmProfileData` avec les champs temporels necessaires, et mettre a jour le parser pour la persistance.

Purpose: Fournir le moteur d'effets pur (logique metier sans UI) que Plan 03 cablera dans `completeTask`. Chaque `CategoryId` de Phase 19 est mappe 1:1 a un effet wow sur la ferme (SEMANTIC-06).
Output: `lib/semantic/effects.ts` (dispatcher + handlers), `FarmProfileData` etendu, parser mis a jour.

**Note architecturale (deviation deliberee de RESEARCH.md) :** Le RESEARCH recommande `applyTaskEffect` async. Le plan choisit SYNCHRONE intentionnellement — tous les handlers sont des mutations de donnees pures sans I/O. L'async n'est necessaire que pour les caps (Plan 02) et le feature flag, qui sont geres dans le caller (Plan 03). Garder le dispatcher sync simplifie le flux et evite les problemes async dans le hot path de completion de tache.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-moteur-d-effets-anti-abus/20-RESEARCH.md

<interfaces>
<!-- Key types and contracts from Phase 19 + existing engines -->

From lib/semantic/categories.ts:
```typescript
export type CategoryId =
  | 'menage_quotidien' | 'menage_hebdo' | 'courses'
  | 'enfants_routines' | 'enfants_devoirs' | 'rendez_vous'
  | 'gratitude_famille' | 'budget_admin' | 'bebe_soins' | 'cuisine_repas';

export type CategoryMatch = {
  id: CategoryId;
  matchedBy: 'tag' | 'section' | 'filepath';
  evidence: string;
};
```

From lib/types.ts (FarmProfileData current — line 571):
```typescript
export interface FarmProfileData {
  treeSpecies?: TreeSpecies;
  mascotDecorations: string[];
  mascotInhabitants: string[];
  mascotPlacements: Record<string, string>;
  farmCrops?: string;
  farmBuildings?: PlacedBuilding[];
  farmInventory?: FarmInventory;
  harvestInventory?: HarvestInventory;
  craftedItems?: CraftedItem[];
  farmTech?: string[];
  farmRareSeeds?: RareSeedInventory;
  wearEvents?: WearEvent[];
  companion?: CompanionData | null;
  giftHistory?: string;
  giftsSentToday?: string;
}
```

From lib/mascot/wear-engine.ts:
```typescript
export function repairWearEvent(
  events: WearEvent[], eventId: string, currentCoins: number, now?: Date
): { events: WearEvent[]; cost: number } | null;
```

From lib/mascot/sagas-types.ts:
```typescript
export type SagaTrait = 'courage' | 'sagesse' | 'générosité' | 'malice' | 'curiosité';
```

From lib/parser.ts (parseFarmProfile line 571, serializeFarmProfile line 630):
```typescript
export function parseFarmProfile(content: string): FarmProfileData;
export function serializeFarmProfile(profileName: string, data: FarmProfileData): string;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Etendre FarmProfileData + parser avec les 6 nouveaux champs</name>
  <files>lib/types.ts, lib/parser.ts</files>
  <read_first>lib/types.ts, lib/parser.ts</read_first>
  <action>
1. Dans `lib/types.ts`, interface `FarmProfileData` (apres la ligne `giftsSentToday`), ajouter les 6 nouveaux champs :
```typescript
  // Phase 20 — bonus temporels effets semantiques
  buildingTurboUntil?: string;      // ISO datetime — EFFECTS-03
  growthSprintUntil?: string;       // ISO datetime — EFFECTS-05
  capacityBoostUntil?: string;      // ISO datetime — EFFECTS-08
  nextHarvestGolden?: boolean;      // EFFECTS-09
  unlockedEffectRecipes?: string[]; // IDs recettes debloquees via EFFECTS-10
  effectGoldenMultiplier?: number;  // EFFECTS-09 : x3 (distinct du GOLDEN_HARVEST_MULTIPLIER = 5)
```

2. Dans `lib/parser.ts`, fonction `parseFarmProfile` (avant le `return` a la ligne 606), ajouter le parsing des 6 nouveaux champs :
```typescript
    buildingTurboUntil: props.building_turbo_until || undefined,
    growthSprintUntil: props.growth_sprint_until || undefined,
    capacityBoostUntil: props.capacity_boost_until || undefined,
    nextHarvestGolden: props.next_harvest_golden === 'true',
    unlockedEffectRecipes: props.unlocked_effect_recipes
      ? props.unlocked_effect_recipes.split(',').map(s => s.trim()).filter(Boolean)
      : [],
```
Ne PAS ajouter `effectGoldenMultiplier` au parser — c'est une constante dans effects.ts, pas une valeur persistee.

3. Dans `lib/parser.ts`, fonction `serializeFarmProfile` (avant le `return lines.join('\n')` a la ligne 659), ajouter la serialisation :
```typescript
  if (data.buildingTurboUntil) lines.push(`building_turbo_until: ${data.buildingTurboUntil}`);
  if (data.growthSprintUntil) lines.push(`growth_sprint_until: ${data.growthSprintUntil}`);
  if (data.capacityBoostUntil) lines.push(`capacity_boost_until: ${data.capacityBoostUntil}`);
  if (data.nextHarvestGolden) lines.push(`next_harvest_golden: true`);
  if (data.unlockedEffectRecipes && data.unlockedEffectRecipes.length > 0) {
    lines.push(`unlocked_effect_recipes: ${data.unlockedEffectRecipes.join(',')}`);
  }
```

Note : ne PAS supprimer `effectGoldenMultiplier` de l'interface — il ne sera juste jamais parse/serialise car c'est une constante runtime.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "types\.ts|parser\.ts" | head -20; echo "---"; grep -c "buildingTurboUntil" lib/types.ts lib/parser.ts</automated>
  </verify>
  <acceptance_criteria>
    - lib/types.ts contient `buildingTurboUntil?: string` dans FarmProfileData
    - lib/types.ts contient `growthSprintUntil?: string` dans FarmProfileData
    - lib/types.ts contient `capacityBoostUntil?: string` dans FarmProfileData
    - lib/types.ts contient `nextHarvestGolden?: boolean` dans FarmProfileData
    - lib/types.ts contient `unlockedEffectRecipes?: string[]` dans FarmProfileData
    - lib/parser.ts parseFarmProfile contient `building_turbo_until`
    - lib/parser.ts parseFarmProfile contient `growth_sprint_until`
    - lib/parser.ts parseFarmProfile contient `capacity_boost_until`
    - lib/parser.ts parseFarmProfile contient `next_harvest_golden`
    - lib/parser.ts parseFarmProfile contient `unlocked_effect_recipes`
    - lib/parser.ts serializeFarmProfile contient `building_turbo_until`
    - lib/parser.ts serializeFarmProfile contient `next_harvest_golden: true`
    - `npx tsc --noEmit` ne produit aucune nouvelle erreur dans types.ts ou parser.ts
  </acceptance_criteria>
  <done>FarmProfileData etendu avec 5 champs persistables, parser lit et ecrit les 5 champs, TypeScript compile sans nouvelle erreur.</done>
</task>

<task type="auto">
  <name>Task 2: Creer lib/semantic/effects.ts — dispatcher + 10 handlers</name>
  <files>lib/semantic/effects.ts, lib/semantic/index.ts</files>
  <read_first>lib/semantic/categories.ts, lib/types.ts, lib/mascot/wear-engine.ts, lib/mascot/craft-engine.ts, lib/semantic/index.ts, lib/mascot/sagas-types.ts</read_first>
  <action>
1. Creer `lib/semantic/effects.ts` avec le contenu suivant :

**Types exportes :**
```typescript
export type EffectId =
  | 'weeds_removed'      // EFFECTS-01
  | 'wear_repaired'      // EFFECTS-02
  | 'building_turbo'     // EFFECTS-03
  | 'companion_mood'     // EFFECTS-04
  | 'growth_sprint'      // EFFECTS-05
  | 'rare_seed_drop'     // EFFECTS-06
  | 'saga_trait_boost'   // EFFECTS-07
  | 'capacity_boost'     // EFFECTS-08
  | 'golden_harvest'     // EFFECTS-09
  | 'recipe_unlock';     // EFFECTS-10

export interface EffectResult {
  effectApplied: EffectId | null;
  farmData: FarmProfileData;
  sagaTraitDelta?: { trait: string; amount: number };
  companionEvent?: string;  // 'task_completed' — consomme dans Plan 03, rendu dans Phase 21
  message?: string;         // evidence brute pour Phase 21 toasts
}

export const EFFECT_GOLDEN_MULTIPLIER = 3; // EFFECTS-09 : x3 (distinct de GOLDEN_HARVEST_MULTIPLIER=5)
```

**Constante de mapping :**
```typescript
const CATEGORY_EFFECT_MAP: Record<CategoryId, EffectId> = {
  menage_quotidien: 'weeds_removed',
  menage_hebdo: 'wear_repaired',
  courses: 'building_turbo',
  enfants_routines: 'companion_mood',
  enfants_devoirs: 'growth_sprint',
  rendez_vous: 'rare_seed_drop',
  gratitude_famille: 'saga_trait_boost',
  budget_admin: 'capacity_boost',
  bebe_soins: 'golden_harvest',
  cuisine_repas: 'recipe_unlock',
};
```

**Fonction principale (SYNCHRONE — voir note architecturale dans l'objectif) :**
```typescript
export function applyTaskEffect(
  match: CategoryMatch,
  farmData: FarmProfileData,
  now: Date = new Date(),
): EffectResult
```
- Switch sur `match.id` via `CATEGORY_EFFECT_MAP`, delegue a un handler specifique.
- Chaque handler est une fonction pure locale (pas exportee) qui prend `(farmData, now)` et retourne `EffectResult`.
- Si le handler retourne `effectApplied: null` (no-op car pas de cible), l'effet n'est pas comptabilise.

**10 handlers (fonctions locales) :**

EFFECTS-01 `handleWeedsRemoved(farmData, now)`:
- Chercher le premier `WearEvent` avec `type === 'weeds'` et `!repairedAt` dans `farmData.wearEvents`.
- Si trouve : appeler `repairWearEvent(farmData.wearEvents!, weedEvent.id, Infinity, now)`.
- Si `repairWearEvent` retourne non-null : `{ effectApplied: 'weeds_removed', farmData: { ...farmData, wearEvents: result.events } }`.
- Si aucun weeds actif ou repairWearEvent null : `{ effectApplied: null, farmData }`.

EFFECTS-02 `handleWearRepaired(farmData, now)`:
- Chercher le premier `WearEvent` non-weeds avec `!repairedAt` dans `farmData.wearEvents` (priorite `broken_fence` > `damaged_roof` > `pest`).
- Si trouve : `repairWearEvent(farmData.wearEvents!, event.id, Infinity, now)`.
- Retour identique pattern EFFECTS-01 avec `effectApplied: 'wear_repaired'`.

EFFECTS-03 `handleBuildingTurbo(farmData, now)`:
- `farmData.buildingTurboUntil = new Date(now.getTime() + 24 * 3600 * 1000).toISOString()`.
- Retourne `{ effectApplied: 'building_turbo', farmData: { ...farmData, buildingTurboUntil } }`.

EFFECTS-04 `handleCompanionMood(farmData)`:
- Retourne `{ effectApplied: 'companion_mood', farmData, companionEvent: 'task_completed' }`.
- Note: le companionEvent est consomme dans Plan 03 (completeTask) pour propager l'evenement au companion engine. Le rendu visuel (mood spike + message IA) est gere dans Phase 21.

EFFECTS-05 `handleGrowthSprint(farmData, now)`:
- `farmData.growthSprintUntil = new Date(now.getTime() + 24 * 3600 * 1000).toISOString()`.
- Retourne `{ effectApplied: 'growth_sprint', farmData: { ...farmData, growthSprintUntil } }`.

EFFECTS-06 `handleRareSeedDrop(farmData)`:
- Pool : `['orchidee', 'rose_doree', 'truffe']` (pas `fruit_dragon` — trop rare).
- Choisir aleatoire : `rarePool[Math.floor(Math.random() * rarePool.length)]`.
- `const updated = { ...(farmData.farmRareSeeds ?? {}), [seedId]: ((farmData.farmRareSeeds ?? {})[seedId] ?? 0) + 1 }`.
- Retourne `{ effectApplied: 'rare_seed_drop', farmData: { ...farmData, farmRareSeeds: updated }, message: seedId }`.

EFFECTS-07 `handleSagaTraitBoost(farmData)`:
- Retourne `{ effectApplied: 'saga_trait_boost', farmData, sagaTraitDelta: { trait: 'générosité', amount: 1 } }`.
- IMPORTANT: utiliser `'générosité'` (SagaTrait valide) et PAS `'joy'` qui n'existe pas dans `SagaTrait`.
- Note: la mutation reelle de SagaProgress est faite dans le hook appelant (Plan 03) via sagas-storage. Ici on declare le delta.

EFFECTS-08 `handleCapacityBoost(farmData, now)`:
- `farmData.capacityBoostUntil = new Date(now.getTime() + 24 * 3600 * 1000).toISOString()`.
- Retourne `{ effectApplied: 'capacity_boost', farmData: { ...farmData, capacityBoostUntil } }`.

EFFECTS-09 `handleGoldenHarvest(farmData)`:
- `farmData.nextHarvestGolden = true`.
- Retourne `{ effectApplied: 'golden_harvest', farmData: { ...farmData, nextHarvestGolden: true } }`.

EFFECTS-10 `handleRecipeUnlock(farmData)`:
- Liste des recettes "effect-only" : `['confiture_truffee', 'gateau_dore', 'elixir_champignon']`.
- Filtrer celles deja dans `farmData.unlockedEffectRecipes ?? []`.
- Si toutes deja debloquees : `{ effectApplied: null, farmData }` (no-op).
- Sinon : choisir aleatoire parmi les non-debloquees, ajouter a `unlockedEffectRecipes`.
- Retourne `{ effectApplied: 'recipe_unlock', farmData: { ...farmData, unlockedEffectRecipes: [...existing, chosenId] }, message: chosenId }`.

2. Mettre a jour `lib/semantic/index.ts` pour re-exporter les types et la fonction publique de `effects.ts` :
```typescript
export { applyTaskEffect, EFFECT_GOLDEN_MULTIPLIER } from './effects';
export type { EffectId, EffectResult } from './effects';
```

Imports necessaires dans effects.ts :
- `import type { CategoryId, CategoryMatch } from './categories';`
- `import type { FarmProfileData } from '../types';`
- `import { repairWearEvent } from '../mascot/wear-engine';`

Ne PAS importer de hooks, de vault, de SecureStore. Ce fichier est 100% pur (fonctions synchrones).
`applyTaskEffect` est SYNCHRONE (pas async) — deviation deliberee du RESEARCH qui suggere async (voir note architecturale dans l'objectif). Tous les handlers sont des mutations de donnees pures sans I/O.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep "effects\.ts" | head -10; echo "---"; grep -c "applyTaskEffect\|EffectId\|EffectResult" lib/semantic/effects.ts lib/semantic/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - lib/semantic/effects.ts exporte `applyTaskEffect`
    - lib/semantic/effects.ts exporte `type EffectId` avec les 10 valeurs
    - lib/semantic/effects.ts exporte `interface EffectResult` avec `effectApplied`, `farmData`, `sagaTraitDelta?`, `companionEvent?`, `message?`
    - lib/semantic/effects.ts contient `CATEGORY_EFFECT_MAP` avec les 10 paires CategoryId:EffectId
    - lib/semantic/effects.ts contient `EFFECT_GOLDEN_MULTIPLIER = 3`
    - lib/semantic/effects.ts importe `repairWearEvent` depuis `../mascot/wear-engine`
    - lib/semantic/effects.ts contient les 10 fonctions handler (handleWeedsRemoved, handleWearRepaired, handleBuildingTurbo, handleCompanionMood, handleGrowthSprint, handleRareSeedDrop, handleSagaTraitBoost, handleCapacityBoost, handleGoldenHarvest, handleRecipeUnlock)
    - lib/semantic/index.ts contient `export { applyTaskEffect` et `export type { EffectId, EffectResult }`
    - handleSagaTraitBoost retourne `trait: 'générosité'` (PAS 'joy')
    - `npx tsc --noEmit` ne produit aucune nouvelle erreur dans effects.ts
  </acceptance_criteria>
  <done>Dispatcher `applyTaskEffect()` fonctionnel avec 10 handlers, chaque CategoryId mappe 1:1 a un EffectId (SEMANTIC-06), tous les 10 effets implementes comme mutations FarmProfileData pures (EFFECTS-01..10), barrel index mis a jour.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` ne produit aucune nouvelle erreur dans les fichiers modifies
- `grep -c "applyTaskEffect" lib/semantic/effects.ts` >= 1
- `grep -c "buildingTurboUntil" lib/types.ts` >= 1
- `grep -c "building_turbo_until" lib/parser.ts` >= 2 (parse + serialize)
- Les 10 EffectId sont presents dans effects.ts
</verification>

<success_criteria>
- Dispatcher pur cree avec les 10 effets
- FarmProfileData etendu avec 5 champs persistables
- Parser mis a jour pour lire/ecrire les nouveaux champs
- TypeScript compile sans nouvelle erreur
- Le barrel index exporte les nouveaux types
</success_criteria>

<output>
After completion, create `.planning/phases/20-moteur-d-effets-anti-abus/20-01-SUMMARY.md`
</output>
