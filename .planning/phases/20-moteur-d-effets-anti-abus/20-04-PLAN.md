---
phase: 20-moteur-d-effets-anti-abus
plan: 04
type: tdd
wave: 3
depends_on: ["20-01", "20-02", "20-03"]
files_modified:
  - CLAUDE.md
  - lib/__tests__/effects.test.ts
  - lib/__tests__/caps.test.ts
autonomous: true
requirements:
  - SEMANTIC-06
  - SEMANTIC-07
  - SEMANTIC-08
  - SEMANTIC-09
  - EFFECTS-01
  - EFFECTS-02
  - EFFECTS-03
  - EFFECTS-04
  - EFFECTS-05
  - EFFECTS-06
  - EFFECTS-07
  - EFFECTS-08
  - EFFECTS-09
  - EFFECTS-10

must_haves:
  truths:
    - "Chaque CategoryId mappe au bon EffectId (10 tests SEMANTIC-06)"
    - "Caps daily empechent le depassement (SEMANTIC-07)"
    - "Caps weekly empechent le depassement (SEMANTIC-07)"
    - "Caps se reinitient au changement de jour et semaine (SEMANTIC-07)"
    - "Tag urgent active multiplier x2 (SEMANTIC-08 — teste dans effects.test.ts via le mapping)"
    - "Chaque effet mute farmData correctement (EFFECTS-01..10)"
    - "Effet no-op quand aucune cible (weeds/wear/recipe deja toutes debloquees)"
    - "CLAUDE.md documente Jest comme pratique etablie (coherence Phase 19+)"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Section Testing mise a jour pour refleter Jest"
      contains: "npx jest"
    - path: "lib/__tests__/effects.test.ts"
      provides: "Tests unitaires des 10 handlers + mapping SEMANTIC-06"
      min_lines: 100
    - path: "lib/__tests__/caps.test.ts"
      provides: "Tests unitaires du systeme de caps SEMANTIC-07"
      min_lines: 60
  key_links:
    - from: "lib/__tests__/effects.test.ts"
      to: "lib/semantic/effects.ts"
      via: "import applyTaskEffect"
      pattern: "import.*applyTaskEffect.*from"
    - from: "lib/__tests__/caps.test.ts"
      to: "lib/semantic/caps.ts"
      via: "import isCapExceeded, incrementCap"
      pattern: "import.*isCapExceeded.*from"
---

<objective>
Mettre a jour CLAUDE.md pour documenter Jest comme pratique etablie, puis creer les tests Jest pour valider les 10 effets et le systeme de caps anti-abus.

Purpose: Phase 19 a introduit Jest (derive.test.ts, flag.test.ts — commits 77cff1b, 34eedc6) mais CLAUDE.md dit encore "pas de test suite". Resoudre la contradiction avant d'ajouter de nouveaux tests, puis verifier que chaque CategoryId produit le bon EffectId et la bonne mutation, que les caps bloquent le spam, et que les edge cases sont couverts.
Output: CLAUDE.md coherent, `lib/__tests__/effects.test.ts` + `lib/__tests__/caps.test.ts`, tous tests verts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-moteur-d-effets-anti-abus/20-RESEARCH.md

<interfaces>
From lib/semantic/effects.ts (Plan 01):
```typescript
export function applyTaskEffect(match: CategoryMatch, farmData: FarmProfileData, now?: Date): EffectResult;
export type EffectId = 'weeds_removed' | 'wear_repaired' | 'building_turbo' | 'companion_mood' | 'growth_sprint' | 'rare_seed_drop' | 'saga_trait_boost' | 'capacity_boost' | 'golden_harvest' | 'recipe_unlock';
export interface EffectResult { effectApplied: EffectId | null; farmData: FarmProfileData; sagaTraitDelta?: { trait: string; amount: number }; companionEvent?: string; message?: string; }
export const EFFECT_GOLDEN_MULTIPLIER = 3;
```

From lib/semantic/caps.ts (Plan 02):
```typescript
export function isCapExceeded(categoryId: CategoryId, caps: CouplingCaps, now?: Date): boolean;
export function incrementCap(caps: CouplingCaps, categoryId: CategoryId, now?: Date): CouplingCaps;
export function getWeekStart(now?: Date): string;
export const DAILY_CAPS: Record<CategoryId, number>;
export const WEEKLY_CAPS: Record<CategoryId, number>;
```

From lib/semantic/categories.ts:
```typescript
export type CategoryId = 'menage_quotidien' | 'menage_hebdo' | 'courses' | 'enfants_routines' | 'enfants_devoirs' | 'rendez_vous' | 'gratitude_famille' | 'budget_admin' | 'bebe_soins' | 'cuisine_repas';
export type CategoryMatch = { id: CategoryId; matchedBy: 'tag' | 'section' | 'filepath'; evidence: string; };
```

From lib/__tests__/derive.test.ts (pattern existant Phase 19):
```typescript
import { deriveTaskCategory } from '../semantic/derive';
// Tests organisees en describe blocks, it.each pour couverture systematique
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Mettre a jour CLAUDE.md — documenter Jest comme pratique etablie</name>
  <files>CLAUDE.md</files>
  <read_first>CLAUDE.md</read_first>
  <action>
Dans la section `## Testing` de CLAUDE.md, remplacer :
```
## Testing
- `npx tsc --noEmit` — seule validation
- Erreurs pré-existantes dans MemoryEditor.tsx, cooklang.ts, useVault.ts — **ignorer**
```

Par :
```
## Testing
- `npx tsc --noEmit` — validation types (obligatoire avant chaque commit)
- `npx jest --no-coverage` — tests unitaires (lib/__tests__/*.test.ts, etabli Phase 19)
- Erreurs pré-existantes dans MemoryEditor.tsx, cooklang.ts, useVault.ts — **ignorer**
```

Cela documente la realite du projet : Jest a ete introduit en Phase 19 (derive.test.ts, flag.test.ts) et Phase 20 continue la pratique.
  </action>
  <verify>
    <automated>grep -c "npx jest" CLAUDE.md</automated>
  </verify>
  <done>CLAUDE.md documente Jest comme outil de test etabli, coherent avec Phase 19+ et les tests ajoutes dans ce plan.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Tests unitaires effects.test.ts — 10 handlers + mapping</name>
  <files>lib/__tests__/effects.test.ts</files>
  <read_first>lib/semantic/effects.ts, lib/semantic/categories.ts, lib/__tests__/derive.test.ts, lib/types.ts</read_first>
  <behavior>
    - SEMANTIC-06: it.each sur les 10 CategoryId -> chacun retourne le bon EffectId (10 tests)
    - EFFECTS-01: menage_quotidien avec weeds actif -> effectApplied='weeds_removed', wearEvents[0].repairedAt set
    - EFFECTS-01 no-op: menage_quotidien sans weeds -> effectApplied=null
    - EFFECTS-02: menage_hebdo avec wear event -> effectApplied='wear_repaired'
    - EFFECTS-02 no-op: menage_hebdo sans wear -> effectApplied=null
    - EFFECTS-03: courses -> farmData.buildingTurboUntil est +24h dans le futur
    - EFFECTS-04: enfants_routines -> companionEvent='task_completed'
    - EFFECTS-05: enfants_devoirs -> farmData.growthSprintUntil est +24h dans le futur
    - EFFECTS-06: rendez_vous -> farmRareSeeds incremente (valeur dans ['orchidee','rose_doree','truffe'])
    - EFFECTS-07: gratitude_famille -> sagaTraitDelta = { trait: 'générosité', amount: 1 }
    - EFFECTS-08: budget_admin -> farmData.capacityBoostUntil est +24h dans le futur
    - EFFECTS-09: bebe_soins -> farmData.nextHarvestGolden === true
    - EFFECTS-10: cuisine_repas -> unlockedEffectRecipes contient une recette de ['confiture_truffee','gateau_dore','elixir_champignon']
    - EFFECTS-10 no-op: cuisine_repas avec toutes recettes deja debloquees -> effectApplied=null
  </behavior>
  <action>
Creer `lib/__tests__/effects.test.ts` :

**Structure :**
```typescript
import { applyTaskEffect, EFFECT_GOLDEN_MULTIPLIER } from '../semantic/effects';
import type { CategoryMatch } from '../semantic/categories';
import type { FarmProfileData } from '../types';

// Helper : FarmProfileData minimale vide
const emptyFarm = (): FarmProfileData => ({
  mascotDecorations: [],
  mascotInhabitants: [],
  mascotPlacements: {},
});

// Helper : CategoryMatch factory
const match = (id: CategoryMatch['id']): CategoryMatch => ({
  id,
  matchedBy: 'tag',
  evidence: 'test',
});

const fixedNow = new Date('2026-04-10T10:00:00Z');
```

**describe blocks :**

1. `describe('SEMANTIC-06 — mapping 1:1 CategoryId -> EffectId')` :
   - `it.each` sur les 10 paires `[categoryId, expectedEffectId]`
   - Pour chaque : appeler `applyTaskEffect(match(categoryId), farmWithTargets, fixedNow)` et verifier `result.effectApplied === expectedEffectId`
   - `farmWithTargets` doit avoir des cibles valides pour chaque effet (weeds pour EFFECTS-01, wear pour EFFECTS-02, etc.)

2. `describe('EFFECTS-01 — weeds_removed')` :
   - Test happy path : farm avec `wearEvents: [{ id: 'w1', type: 'weeds', occurredAt: '...', targetId: '0' }]` -> repairedAt set
   - Test no-op : farm sans weeds -> `effectApplied: null`

3. `describe('EFFECTS-02 — wear_repaired')` :
   - Test happy path avec `broken_fence`
   - Test no-op sans wear event

4. `describe('EFFECTS-03 — building_turbo')` :
   - Verifier `buildingTurboUntil` est ~24h apres `fixedNow`

5. `describe('EFFECTS-04 — companion_mood')` :
   - Verifier `companionEvent === 'task_completed'`

6. `describe('EFFECTS-05 — growth_sprint')` :
   - Verifier `growthSprintUntil` est ~24h apres `fixedNow`

7. `describe('EFFECTS-06 — rare_seed_drop')` :
   - Verifier `farmRareSeeds` incremente
   - Verifier `message` est dans le pool ['orchidee', 'rose_doree', 'truffe']

8. `describe('EFFECTS-07 — saga_trait_boost')` :
   - Verifier `sagaTraitDelta` = `{ trait: 'générosité', amount: 1 }`

9. `describe('EFFECTS-08 — capacity_boost')` :
   - Verifier `capacityBoostUntil` ~24h futur

10. `describe('EFFECTS-09 — golden_harvest')` :
    - Verifier `nextHarvestGolden === true`
    - Verifier `EFFECT_GOLDEN_MULTIPLIER === 3`

11. `describe('EFFECTS-10 — recipe_unlock')` :
    - Happy path : `unlockedEffectRecipes` augmente de 1
    - No-op : toutes les 3 recettes deja debloquees -> `effectApplied: null`

**Verification dates +24h :** Pour EFFECTS-03/05/08, parser la date retournee et verifier :
```typescript
const until = new Date(result.farmData.buildingTurboUntil!);
expect(until.getTime() - fixedNow.getTime()).toBe(24 * 3600 * 1000);
```
  </action>
  <verify>
    <automated>npx jest lib/__tests__/effects.test.ts --no-coverage --silent 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - lib/__tests__/effects.test.ts existe et contient >= 15 tests (it/test)
    - `npx jest lib/__tests__/effects.test.ts --no-coverage` exit code 0
    - Chaque describe EFFECTS-01..10 contient au moins 1 test
    - Le describe SEMANTIC-06 contient un it.each avec 10 paires
    - EFFECTS-01 et EFFECTS-02 ont un test no-op (effectApplied: null)
    - EFFECTS-07 verifie `trait: 'générosité'` (pas 'joy')
    - EFFECTS-10 a un test no-op (toutes recettes debloquees)
  </acceptance_criteria>
  <done>Tests des 10 effets verts, mapping SEMANTIC-06 verifie par it.each, no-op cases couverts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tests unitaires caps.test.ts — caps daily/weekly/reset</name>
  <files>lib/__tests__/caps.test.ts</files>
  <read_first>lib/semantic/caps.ts, lib/__tests__/derive.test.ts</read_first>
  <behavior>
    - SEMANTIC-07 daily: isCapExceeded retourne true apres avoir atteint le cap daily
    - SEMANTIC-07 weekly: isCapExceeded retourne true apres avoir atteint le cap weekly
    - SEMANTIC-07 cross-day: isCapExceeded retourne false le lendemain (reset daily)
    - SEMANTIC-07 cross-week: isCapExceeded retourne false la semaine suivante (reset weekly)
    - incrementCap : incremente daily et weekly correctement
    - incrementCap : reset daily si dayStart different de today
    - incrementCap : reset weekly si weekStart different de thisWeek
    - getWeekStart : retourne le lundi de la semaine
    - cuisine_repas : daily cap = 0 (jamais bloque en daily), weekly cap = 1
    - Caps vide : isCapExceeded retourne false (jamais utilise)
  </behavior>
  <action>
Creer `lib/__tests__/caps.test.ts` :

**Imports :**
```typescript
import { isCapExceeded, incrementCap, getWeekStart, DAILY_CAPS, WEEKLY_CAPS } from '../semantic/caps';
import type { CouplingCaps } from '../semantic/caps';
```

**describe blocks :**

1. `describe('getWeekStart')` :
   - Test mercredi 2026-04-08 -> retourne '2026-04-06' (lundi)
   - Test lundi 2026-04-06 -> retourne '2026-04-06'
   - Test dimanche 2026-04-12 -> retourne '2026-04-06'

2. `describe('isCapExceeded — daily')` :
   - `menage_quotidien` daily cap = 1 : caps `{ daily: 1, weekly: 1, dayStart: today }` -> `true`
   - `menage_quotidien` daily cap = 1 : caps `{ daily: 0, weekly: 0, dayStart: today }` -> `false`
   - `enfants_routines` daily cap = 2 : caps `{ daily: 1 }` -> `false`, `{ daily: 2 }` -> `true`

3. `describe('isCapExceeded — weekly')` :
   - `menage_quotidien` weekly cap = 5 : caps `{ weekly: 5, weekStart: thisWeek }` -> `true`
   - `cuisine_repas` daily cap = 0, weekly cap = 1 : caps `{ daily: 10, ... }` -> jamais bloque en daily, bloque apres 1 weekly

4. `describe('isCapExceeded — cross-day reset')` :
   - Caps `{ daily: 1, dayStart: 'yesterday' }` avec now=today -> `false` (reset)

5. `describe('isCapExceeded — cross-week reset')` :
   - Caps `{ weekly: 5, weekStart: 'last-week-monday' }` avec now=this-week -> `false` (reset)

6. `describe('incrementCap')` :
   - Caps vide + increment -> `{ daily: 1, weekly: 1, dayStart: today, weekStart: thisWeek }`
   - Caps existant meme jour -> `daily + 1`, `weekly + 1`
   - Caps avec dayStart different -> daily reset a 1 (pas +1 sur ancien)

7. `describe('DAILY_CAPS / WEEKLY_CAPS constants')` :
   - `DAILY_CAPS` a exactement 10 cles (1 par CategoryId)
   - `WEEKLY_CAPS` a exactement 10 cles
   - `DAILY_CAPS.cuisine_repas === 0`
   - `WEEKLY_CAPS.cuisine_repas === 1`

**Dates de test fixes :**
```typescript
const monday = new Date('2026-04-06T10:00:00Z');     // lundi
const wednesday = new Date('2026-04-08T10:00:00Z');   // mercredi
const nextMonday = new Date('2026-04-13T10:00:00Z');  // lundi suivant
const tuesday = new Date('2026-04-07T10:00:00Z');     // mardi
```
  </action>
  <verify>
    <automated>npx jest lib/__tests__/caps.test.ts --no-coverage --silent 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - lib/__tests__/caps.test.ts existe et contient >= 10 tests (it/test)
    - `npx jest lib/__tests__/caps.test.ts --no-coverage` exit code 0
    - Tests couvrent daily cap, weekly cap, cross-day reset, cross-week reset
    - Tests verifient `DAILY_CAPS.cuisine_repas === 0` et `WEEKLY_CAPS.cuisine_repas === 1`
    - Tests verifient `incrementCap` produit un nouvel objet (pas de mutation)
    - Tests verifient `getWeekStart` retourne le bon lundi
  </acceptance_criteria>
  <done>Tests du systeme de caps verts, daily/weekly/reset/constants verifies, zero mock SecureStore necessaire (fonctions pures).</done>
</task>

</tasks>

<verification>
- `npx jest lib/__tests__/effects.test.ts lib/__tests__/caps.test.ts --no-coverage` exit code 0
- Couverture minimale : 10 effets testes + 10 tests caps
- `grep "npx jest" CLAUDE.md` retourne au moins 1 ligne
</verification>

<success_criteria>
- CLAUDE.md documente Jest comme pratique etablie (coherent Phase 19+)
- Tous les tests verts pour effects.test.ts et caps.test.ts
- Chaque requirement EFFECTS-01..10, SEMANTIC-06..09 verifie par au moins un test
- Edge cases couverts (no-op, cross-day, cross-week, all-unlocked)
- Tests utilisent des dates fixes pour la reproductibilite
</success_criteria>

<output>
After completion, create `.planning/phases/20-moteur-d-effets-anti-abus/20-04-SUMMARY.md`
</output>
