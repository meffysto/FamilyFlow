---
phase: 20-moteur-d-effets-anti-abus
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/semantic/caps.ts
  - lib/semantic/index.ts
autonomous: true
requirements:
  - SEMANTIC-07

must_haves:
  truths:
    - "Un effet ne peut pas etre declenche plus que son cap daily"
    - "Un effet ne peut pas etre declenche plus que son cap weekly"
    - "Les caps se reinitialisant au changement de jour/semaine"
    - "Les caps persistent entre les restarts via SecureStore"
  artifacts:
    - path: "lib/semantic/caps.ts"
      provides: "Systeme de caps daily/weekly SecureStore"
      exports: ["loadCaps", "saveCaps", "isCapExceeded", "incrementCap", "CouplingCaps", "EffectCap", "DAILY_CAPS", "WEEKLY_CAPS"]
  key_links:
    - from: "lib/semantic/caps.ts"
      to: "expo-secure-store"
      via: "getItemAsync/setItemAsync avec cle coupling-caps-{profileId}"
      pattern: "coupling-caps-"
    - from: "lib/semantic/caps.ts"
      to: "lib/semantic/categories.ts"
      via: "import CategoryId pour typer les caps"
      pattern: "import.*CategoryId.*from.*categories"
---

<objective>
Creer le systeme anti-abus avec caps daily/weekly persistes dans SecureStore, empechant le spam d'effets.

Purpose: Chaque effet est limite par jour et par semaine (SEMANTIC-07). Le pattern est identique a `giftsSentToday` deja en production.
Output: `lib/semantic/caps.ts` avec load/save/check/increment, barrel mis a jour.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-moteur-d-effets-anti-abus/20-RESEARCH.md

<interfaces>
From lib/semantic/categories.ts:
```typescript
export type CategoryId =
  | 'menage_quotidien' | 'menage_hebdo' | 'courses'
  | 'enfants_routines' | 'enfants_devoirs' | 'rendez_vous'
  | 'gratitude_famille' | 'budget_admin' | 'bebe_soins' | 'cuisine_repas';
```

Pattern SecureStore existant (lib/semantic/flag.ts):
```typescript
import * as SecureStore from 'expo-secure-store';
export const SEMANTIC_COUPLING_KEY = 'semantic-coupling-enabled';
export async function isSemanticCouplingEnabled(): Promise<boolean> {
  try {
    const value = await SecureStore.getItemAsync(SEMANTIC_COUPLING_KEY);
    return value === 'true';
  } catch { return false; }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Creer lib/semantic/caps.ts — systeme anti-abus complet</name>
  <files>lib/semantic/caps.ts</files>
  <read_first>lib/semantic/flag.ts, lib/semantic/categories.ts</read_first>
  <action>
Creer `lib/semantic/caps.ts` avec le contenu suivant :

**Imports :**
```typescript
import * as SecureStore from 'expo-secure-store';
import type { CategoryId } from './categories';
```

**Types exportes :**
```typescript
export interface EffectCap {
  daily: number;    // compteur jour courant
  weekly: number;   // compteur semaine courante
  dayStart: string; // YYYY-MM-DD — reset si different de today
  weekStart: string; // YYYY-MM-DD (lundi) — reset si different de thisWeek
}

export type CouplingCaps = Partial<Record<CategoryId, EffectCap>>;
```

**Constantes de limites :**
```typescript
export const DAILY_CAPS: Record<CategoryId, number> = {
  menage_quotidien: 1,
  menage_hebdo: 1,
  courses: 1,
  enfants_routines: 2,
  enfants_devoirs: 1,
  rendez_vous: 1,
  gratitude_famille: 2,
  budget_admin: 1,
  bebe_soins: 1,
  cuisine_repas: 0,       // 0 = pas de cap daily (cap hebdo uniquement)
};

export const WEEKLY_CAPS: Record<CategoryId, number> = {
  menage_quotidien: 5,
  menage_hebdo: 3,
  courses: 3,
  enfants_routines: 10,
  enfants_devoirs: 5,
  rendez_vous: 3,
  gratitude_famille: 7,
  budget_admin: 3,
  bebe_soins: 5,
  cuisine_repas: 1,       // EFFECTS-10 : 1 recipe unlock / semaine
};
```

**Prefixe cle SecureStore :**
```typescript
const CAPS_KEY_PREFIX = 'coupling-caps-';
```

**Helper interne `getWeekStart(now)` :**
- Prend `now: Date = new Date()`.
- Retourne le lundi de la semaine courante en `YYYY-MM-DD`.
- Calcul : `const day = now.getDay(); const diff = now.getDate() - day + (day === 0 ? -6 : 1); const monday = new Date(now); monday.setDate(diff); monday.setHours(0,0,0,0); return monday.toISOString().slice(0,10);`
- Exporter cette fonction aussi (utile pour les tests).

**Fonction `loadCaps(profileId: string): Promise<CouplingCaps>` :**
```typescript
export async function loadCaps(profileId: string): Promise<CouplingCaps> {
  try {
    const raw = await SecureStore.getItemAsync(`${CAPS_KEY_PREFIX}${profileId}`);
    if (!raw) return {};
    return JSON.parse(raw) as CouplingCaps;
  } catch {
    return {};
  }
}
```

**Fonction `saveCaps(profileId: string, caps: CouplingCaps): Promise<void>` :**
```typescript
export async function saveCaps(profileId: string, caps: CouplingCaps): Promise<void> {
  try {
    await SecureStore.setItemAsync(
      `${CAPS_KEY_PREFIX}${profileId}`,
      JSON.stringify(caps),
    );
  } catch { /* non-critical */ }
}
```

**Fonction `isCapExceeded(categoryId: CategoryId, caps: CouplingCaps, now?: Date): boolean` :**
- Prend un `now` optionnel (defaut `new Date()`) pour la testabilite.
- `const today = (now ?? new Date()).toISOString().slice(0, 10);`
- `const thisWeek = getWeekStart(now);`
- `const cap = caps[categoryId];`
- Si `!cap` : return `false` (jamais utilise = pas cappe).
- Calculer `dailyCount = cap.dayStart === today ? cap.daily : 0;`
- Calculer `weeklyCount = cap.weekStart === thisWeek ? cap.weekly : 0;`
- `const dailyLimit = DAILY_CAPS[categoryId];`
- `const weeklyLimit = WEEKLY_CAPS[categoryId];`
- `if (dailyLimit > 0 && dailyCount >= dailyLimit) return true;`
- `if (weeklyLimit > 0 && weeklyCount >= weeklyLimit) return true;`
- `return false;`

**Fonction `incrementCap(caps: CouplingCaps, categoryId: CategoryId, now?: Date): CouplingCaps` :**
- Pure function, retourne un nouvel objet (pas de mutation).
- `const today = (now ?? new Date()).toISOString().slice(0, 10);`
- `const thisWeek = getWeekStart(now);`
- `const existing = caps[categoryId];`
- `const dailyCount = existing && existing.dayStart === today ? existing.daily : 0;`
- `const weeklyCount = existing && existing.weekStart === thisWeek ? existing.weekly : 0;`
- Retourner : `{ ...caps, [categoryId]: { daily: dailyCount + 1, weekly: weeklyCount + 1, dayStart: today, weekStart: thisWeek } }`.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep "caps\.ts" | head -10; echo "---"; grep -c "isCapExceeded\|incrementCap\|loadCaps\|saveCaps\|DAILY_CAPS\|WEEKLY_CAPS" lib/semantic/caps.ts</automated>
  </verify>
  <acceptance_criteria>
    - lib/semantic/caps.ts exporte `loadCaps`, `saveCaps`, `isCapExceeded`, `incrementCap`
    - lib/semantic/caps.ts exporte `CouplingCaps`, `EffectCap`
    - lib/semantic/caps.ts exporte `DAILY_CAPS` et `WEEKLY_CAPS` avec les 10 CategoryId comme cles
    - lib/semantic/caps.ts contient `coupling-caps-` comme prefixe SecureStore
    - lib/semantic/caps.ts contient `getWeekStart` (exportee)
    - `isCapExceeded` prend un parametre `now?: Date` pour la testabilite
    - `incrementCap` est une pure function (retourne un nouvel objet, pas de mutation)
    - `DAILY_CAPS.cuisine_repas` vaut `0` (pas de cap daily)
    - `WEEKLY_CAPS.cuisine_repas` vaut `1`
    - `npx tsc --noEmit` ne produit aucune erreur dans caps.ts
  </acceptance_criteria>
  <done>Systeme de caps anti-abus complet avec load/save SecureStore, verification et incrementation pures, caps daily/weekly pour les 10 categories.</done>
</task>

<task type="auto">
  <name>Task 2: Mettre a jour le barrel index pour exporter caps.ts</name>
  <files>lib/semantic/index.ts</files>
  <read_first>lib/semantic/index.ts</read_first>
  <action>
Ajouter les re-exports de caps.ts dans `lib/semantic/index.ts` (apres les exports existants de flag.ts) :

```typescript
// Phase 20 — caps anti-abus
export {
  loadCaps,
  saveCaps,
  isCapExceeded,
  incrementCap,
  getWeekStart,
  DAILY_CAPS,
  WEEKLY_CAPS,
} from './caps';
export type { CouplingCaps, EffectCap } from './caps';
```

Note : Plan 01 ajoute aussi des exports effects.ts dans le meme fichier. Si Plan 01 s'execute en parallele, verifier que les deux blocs d'exports sont presents. Le fichier final doit contenir les exports de : derive, flag, categories types, effects, caps.
  </action>
  <verify>
    <automated>grep -c "loadCaps\|saveCaps\|isCapExceeded\|incrementCap\|DAILY_CAPS\|WEEKLY_CAPS" lib/semantic/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - lib/semantic/index.ts contient `export { loadCaps` (re-export de caps.ts)
    - lib/semantic/index.ts contient `export { saveCaps` (re-export de caps.ts)
    - lib/semantic/index.ts contient `export { isCapExceeded` (re-export de caps.ts)
    - lib/semantic/index.ts contient `export type { CouplingCaps` (re-export type)
    - lib/semantic/index.ts contient `DAILY_CAPS` et `WEEKLY_CAPS`
  </acceptance_criteria>
  <done>Barrel index exporte toutes les fonctions et types du systeme de caps anti-abus.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` ne produit aucune nouvelle erreur
- `grep "coupling-caps-" lib/semantic/caps.ts` retourne au moins 1 ligne
- `grep "DAILY_CAPS" lib/semantic/caps.ts lib/semantic/index.ts` retourne au moins 2 lignes
</verification>

<success_criteria>
- Systeme de caps complet (load/save/check/increment)
- 10 categories avec caps daily et weekly definis
- SecureStore utilise comme persistance (cle `coupling-caps-{profileId}`)
- Reinitialisation automatique au changement de jour/semaine
- Pure functions pour check et increment (testables sans SecureStore)
- Barrel index mis a jour
</success_criteria>

<output>
After completion, create `.planning/phases/20-moteur-d-effets-anti-abus/20-02-SUMMARY.md`
</output>
