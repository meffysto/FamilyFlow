---
phase: 24-compagnon-tendu-seed-003-lite
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/mascot/companion-storage.ts
  - lib/mascot/companion-engine.ts
  - app/(tabs)/tree.tsx
autonomous: true
requirements: [COMPANION-03, COMPANION-06]

must_haves:
  truths:
    - "Les messages compagnon sont persistés dans SecureStore et survivent au restart"
    - "Au mount, les messages persistés alimentent l'anti-répétition IA (companionRecentMessagesRef)"
    - "Le celebration (streak%7) est commenté/dormant dans detectProactiveEvent"
  artifacts:
    - path: "lib/mascot/companion-storage.ts"
      provides: "loadCompanionMessages / saveCompanionMessages SecureStore I/O"
      exports: ["loadCompanionMessages", "saveCompanionMessages", "PersistedCompanionMessage"]
    - path: "lib/mascot/companion-engine.ts"
      provides: "detectProactiveEvent avec celebration commenté"
      contains: "// D-08: celebration"
    - path: "app/(tabs)/tree.tsx"
      provides: "saveToMemory étendu SecureStore + hydratation au mount"
  key_links:
    - from: "app/(tabs)/tree.tsx"
      to: "lib/mascot/companion-storage.ts"
      via: "import loadCompanionMessages, saveCompanionMessages"
      pattern: "import.*companion-storage"
    - from: "app/(tabs)/tree.tsx saveToMemory"
      to: "SecureStore via saveCompanionMessages"
      via: "fire-and-forget async call"
      pattern: "saveCompanionMessages"
---

<objective>
Persister les messages compagnon dans SecureStore (sortie RAM-only) et préparer le moteur en désactivant celebration.

Purpose: COMPANION-06 exige que les messages survivent au restart. COMPANION-03 est dormant per D-08.
Output: lib/mascot/companion-storage.ts (nouveau), tree.tsx modifié, companion-engine.ts modifié.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-compagnon-tendu-seed-003-lite/24-CONTEXT.md
@.planning/phases/24-compagnon-tendu-seed-003-lite/24-RESEARCH.md

@lib/mascot/companion-engine.ts
@lib/mascot/companion-types.ts
@lib/semantic/caps.ts
@app/(tabs)/tree.tsx

<interfaces>
<!-- Contracts from companion-types.ts -->
export type CompanionEvent =
  | 'task_completed' | 'loot_opened' | 'level_up' | 'greeting'
  | 'streak_milestone' | 'harvest' | 'craft'
  | 'routine_completed' | 'budget_alert' | 'meal_planned'
  | 'gratitude_written' | 'photo_added' | 'defi_completed'
  | 'family_milestone' | 'weekly_recap'
  | 'morning_greeting' | 'gentle_nudge' | 'comeback' | 'celebration'
  | 'building_full' | 'fence_broken' | 'roof_damaged'
  | 'weeds_growing' | 'pests_attacking' | 'wear_repaired';

<!-- From caps.ts — pattern to replicate -->
import * as SecureStore from 'expo-secure-store';
// Key pattern: `coupling-caps-${profileId}` → JSON string
// load: getItemAsync → JSON.parse → typed default
// save: setItemAsync → JSON.stringify

<!-- From tree.tsx — current saveToMemory (line ~594) -->
const saveToMemory = useCallback((msg: string) => {
  const recent = companionRecentMessagesRef.current;
  if (recent.length > 0 && recent[recent.length - 1] === msg) return;
  companionRecentMessagesRef.current = [...recent, msg].slice(-5);
}, []);

<!-- From tree.tsx — showCompanionMsg (line ~606) -->
const showCompanionMsg = useCallback((msg: string, context: any, duration = 8000) => {
  // ...sets companion message state, timer...
  saveToMemory(displayMsg);
}, [t, saveToMemory]);
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Créer companion-storage.ts + commenter celebration dans engine</name>
  <files>lib/mascot/companion-storage.ts, lib/mascot/companion-engine.ts</files>
  <read_first>
    - lib/semantic/caps.ts (pattern SecureStore load/save à répliquer)
    - lib/mascot/companion-engine.ts (detectProactiveEvent ligne ~564-593, celebration ligne ~580)
    - lib/mascot/companion-types.ts (CompanionEvent type)
  </read_first>
  <action>
1. Créer `lib/mascot/companion-storage.ts` — module pur SecureStore, zéro import vault/hook :

```typescript
import * as SecureStore from 'expo-secure-store';

export interface PersistedCompanionMessage {
  text: string;       // message affiché (traduit)
  event: string;      // CompanionEvent type (string pour flexibilité)
  timestamp: string;  // ISO datetime
}

const MESSAGES_KEY_PREFIX = 'companion_messages_';

export async function loadCompanionMessages(profileId: string): Promise<PersistedCompanionMessage[]> {
  try {
    const raw = await SecureStore.getItemAsync(`${MESSAGES_KEY_PREFIX}${profileId}`);
    if (!raw) return [];
    return JSON.parse(raw) as PersistedCompanionMessage[];
  } catch {
    return [];
  }
}

export async function saveCompanionMessages(
  profileId: string,
  messages: PersistedCompanionMessage[],
): Promise<void> {
  try {
    const toSave = messages.slice(-5); // D-02: garder les 5 derniers
    await SecureStore.setItemAsync(
      `${MESSAGES_KEY_PREFIX}${profileId}`,
      JSON.stringify(toSave),
    );
  } catch { /* non-critical */ }
}
```

2. Dans `lib/mascot/companion-engine.ts`, commenter la ligne celebration (~580) dans `detectProactiveEvent()` :
   - Remplacer `if (ctx.streak > 0 && ctx.streak % 7 === 0) return 'celebration';`
   - Par `// D-08: celebration désactivée Phase 24 — réactiver dans un futur milestone`
   - Ajouter `// if (ctx.streak > 0 && ctx.streak % 7 === 0) return 'celebration';`
   - NE PAS supprimer la ligne — la commenter seulement.
  </action>
  <verify>
    <automated>grep -q "PersistedCompanionMessage" lib/mascot/companion-storage.ts && grep -q "loadCompanionMessages" lib/mascot/companion-storage.ts && grep -q "saveCompanionMessages" lib/mascot/companion-storage.ts && grep -q "D-08" lib/mascot/companion-engine.ts && grep -q "// if (ctx.streak" lib/mascot/companion-engine.ts && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - lib/mascot/companion-storage.ts exporte `PersistedCompanionMessage`, `loadCompanionMessages`, `saveCompanionMessages`
    - companion-storage.ts contient `MESSAGES_KEY_PREFIX = 'companion_messages_'`
    - companion-storage.ts contient `.slice(-5)` (D-02)
    - companion-engine.ts contient `// D-08: celebration` sur la ligne commentée
    - companion-engine.ts contient `// if (ctx.streak > 0 && ctx.streak % 7 === 0)` (commenté, pas supprimé)
    - `npx tsc --noEmit` ne produit aucune nouvelle erreur
  </acceptance_criteria>
  <done>companion-storage.ts créé avec load/save SecureStore pattern. celebration commentée dans detectProactiveEvent.</done>
</task>

<task type="auto">
  <name>Task 2: Câbler persistance SecureStore dans tree.tsx (saveToMemory + hydratation mount)</name>
  <files>app/(tabs)/tree.tsx</files>
  <read_first>
    - app/(tabs)/tree.tsx (saveToMemory ~ligne 594, showCompanionMsg ~ligne 606, companionRecentMessagesRef ~ligne 380, useFocusEffect companion ~ligne 670-780)
    - lib/mascot/companion-storage.ts (créé par Task 1)
    - lib/mascot/companion-types.ts (CompanionEvent type)
  </read_first>
  <action>
1. Ajouter import en haut de tree.tsx :
   `import { loadCompanionMessages, saveCompanionMessages, type PersistedCompanionMessage } from '../../lib/mascot/companion-storage';`

2. Modifier `saveToMemory` pour accepter `event` optionnel et persister dans SecureStore :
   ```typescript
   const saveToMemory = useCallback((msg: string, event: CompanionEvent = 'greeting') => {
     if (!activeProfileRef.current) return;
     const recent = companionRecentMessagesRef.current;
     if (recent.length > 0 && recent[recent.length - 1] === msg) return;
     const updated = [...recent, msg].slice(-5);
     companionRecentMessagesRef.current = updated;
     // Persist — fire and forget (D-01, D-03)
     const profileId = activeProfileRef.current.id;
     loadCompanionMessages(profileId).then(existing => {
       const entry: PersistedCompanionMessage = {
         text: msg, event, timestamp: new Date().toISOString(),
       };
       saveCompanionMessages(profileId, [...existing, entry]);
     });
   }, []);
   ```

3. Modifier `showCompanionMsg` pour thread le `event` :
   - Ajouter paramètre `event?: CompanionEvent` après `duration` (default `'greeting'`)
   - Dans le corps, remplacer `saveToMemory(displayMsg)` par `saveToMemory(displayMsg, event ?? 'greeting')`

4. Mettre à jour les call sites de `showCompanionMsg` pour passer le bon event :
   - Greeting au mount (~ligne 661) : `showCompanionMsg(pickCompanionMessage('greeting', context), context, 4000, 'greeting')`
   - Proactive event (~ligne 722) : `showCompanionMsg(pickCompanionMessage(event, context), context, 5000, event)`
   - Action trigger (~ligne 817) : `showCompanionMsg(pickCompanionMessage(event, context), context, undefined, event)`
   - AI callback (~lignes 666, 727, 822) : passer le même event en 4eme paramètre

5. Ajouter hydratation au mount — dans le useEffect existant sur `[activeProfile?.id]` ou dans un nouveau useEffect :
   ```typescript
   useEffect(() => {
     if (!activeProfile?.id) return;
     // Pitfall 6: ne pas écraser les messages de la session en cours
     if (companionRecentMessagesRef.current.length > 0) return;
     loadCompanionMessages(activeProfile.id).then(msgs => {
       companionRecentMessagesRef.current = msgs.map(m => m.text).slice(-5);
     });
   }, [activeProfile?.id]);
   ```

IMPORTANT: `saveToMemory` est wrappé dans `useCallback` — la dépendance `[]` reste vide car il utilise `activeProfileRef.current` (ref, pas state).
  </action>
  <verify>
    <automated>grep -q "loadCompanionMessages" 'app/(tabs)/tree.tsx' && grep -q "saveCompanionMessages" 'app/(tabs)/tree.tsx' && grep -q "companion-storage" 'app/(tabs)/tree.tsx' && grep -q "PersistedCompanionMessage" 'app/(tabs)/tree.tsx' && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - tree.tsx importe `loadCompanionMessages`, `saveCompanionMessages`, `PersistedCompanionMessage` depuis `../../lib/mascot/companion-storage`
    - `saveToMemory` accepte un 2eme paramètre `event: CompanionEvent` (avec default `'greeting'`)
    - `saveToMemory` contient `loadCompanionMessages(profileId).then(` pour la persistence fire-and-forget
    - `saveToMemory` contient `new Date().toISOString()` pour le timestamp (D-03)
    - `showCompanionMsg` accepte un paramètre `event` et le passe à `saveToMemory`
    - Un useEffect sur `activeProfile?.id` contient `loadCompanionMessages` pour l'hydratation
    - L'hydratation contient le guard `companionRecentMessagesRef.current.length > 0` (Pitfall 6)
    - `npx tsc --noEmit` ne produit aucune nouvelle erreur
  </acceptance_criteria>
  <done>Messages compagnon persistés dans SecureStore via companion-storage.ts. Hydratation au mount. Backward-compatible (event optionnel).</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passe sans nouvelles erreurs
2. `grep -r "companion-storage" lib/ app/` montre les imports corrects
3. `grep "D-08" lib/mascot/companion-engine.ts` confirme celebration commentée
4. `grep "loadCompanionMessages\|saveCompanionMessages" app/(tabs)/tree.tsx` confirme le câblage
</verification>

<success_criteria>
- companion-storage.ts existe avec load/save SecureStore (clé `companion_messages_{profileId}`)
- saveToMemory dans tree.tsx persiste chaque message avec text/event/timestamp (D-03)
- Hydratation au mount remplit companionRecentMessagesRef depuis SecureStore (D-04)
- celebration commentée dans detectProactiveEvent (D-08)
- Zéro nouvelle erreur TypeScript
</success_criteria>

<output>
After completion, create `.planning/phases/24-compagnon-tendu-seed-003-lite/24-01-SUMMARY.md`
</output>
