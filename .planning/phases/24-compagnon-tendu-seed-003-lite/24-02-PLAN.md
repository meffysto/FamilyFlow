---
phase: 24-compagnon-tendu-seed-003-lite
plan: 02
type: execute
wave: 2
depends_on: ["24-01"]
files_modified:
  - lib/mascot/companion-engine.ts
  - lib/mascot/companion-storage.ts
  - components/dashboard/DashboardCompanion.tsx
  - components/dashboard/index.ts
  - app/(tabs)/index.tsx
autonomous: true
requirements: [COMPANION-01, COMPANION-02, COMPANION-04, COMPANION-05]

must_haves:
  truths:
    - "User voit un morning_greeting dans une bulle compagnon sur le dashboard a la premiere ouverture du jour (6h-11h)"
    - "User voit un weekly_recap dimanche soir (18h-21h) avec stats de couplage semantique"
    - "User voit un comeback message apres >24h d'absence (sur dashboard ou tree.tsx)"
    - "User voit un gentle_nudge apres-midi sans tache completee (sur tree.tsx, max 1/jour)"
  artifacts:
    - path: "components/dashboard/DashboardCompanion.tsx"
      provides: "Bulle inline compagnon sur le dashboard"
      min_lines: 60
    - path: "lib/mascot/companion-engine.ts"
      provides: "detectProactiveEvent avec weekly_recap condition"
      contains: "weekly_recap"
    - path: "lib/mascot/companion-storage.ts"
      provides: "nudge_shown_today flag load/save"
      exports: ["hasNudgeShownToday", "markNudgeShownToday"]
  key_links:
    - from: "components/dashboard/DashboardCompanion.tsx"
      to: "lib/mascot/companion-engine.ts"
      via: "detectProactiveEvent + pickCompanionMessage + generateCompanionAIMessage"
      pattern: "detectProactiveEvent|pickCompanionMessage"
    - from: "components/dashboard/DashboardCompanion.tsx"
      to: "lib/semantic/coupling-overrides.ts"
      via: "loadWeekStats() pour weekly_recap"
      pattern: "loadWeekStats"
    - from: "app/(tabs)/index.tsx"
      to: "components/dashboard/DashboardCompanion.tsx"
      via: "import et rendu en haut du dashboard"
      pattern: "DashboardCompanion"
    - from: "app/(tabs)/tree.tsx"
      to: "lib/mascot/companion-storage.ts"
      via: "hasNudgeShownToday + markNudgeShownToday"
      pattern: "NudgeShown"
---

<objective>
Câbler les 4 event types proactifs (morning_greeting, weekly_recap, comeback, gentle_nudge) et créer la bulle compagnon dashboard.

Purpose: COMPANION-01/02/04/05 — rendre le compagnon proactif au-delà de tree.tsx.
Output: DashboardCompanion.tsx (nouveau), engine modifié, dashboard câblé.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-compagnon-tendu-seed-003-lite/24-CONTEXT.md
@.planning/phases/24-compagnon-tendu-seed-003-lite/24-RESEARCH.md
@.planning/phases/24-compagnon-tendu-seed-003-lite/24-01-SUMMARY.md

@lib/mascot/companion-engine.ts
@lib/mascot/companion-types.ts
@lib/mascot/companion-storage.ts
@lib/semantic/coupling-overrides.ts
@components/dashboard/types.ts
@components/dashboard/index.ts
@components/dashboard/DashboardBilanSemaine.tsx
@components/mascot/CompanionAvatarMini.tsx
@app/(tabs)/index.tsx
@app/(tabs)/tree.tsx

<interfaces>
<!-- DashboardSectionProps from components/dashboard/types.ts -->
export interface DashboardSectionProps {
  isChildMode: boolean;
  vaultFileExists: Record<string, boolean>;
  activateCardTemplate: (cardId: string) => Promise<void>;
  insights?: Insight[];
}

<!-- detectProactiveEvent from companion-engine.ts -->
export interface ProactiveContext {
  hoursSinceLastVisit: number;
  currentHour: number;
  tasksToday: number;
  totalTasksToday: number;
  streak: number;
  hasGratitudeToday: boolean;
  hasMealsPlanned: boolean;
  isFirstVisitToday: boolean;
  familyMilestone?: string;
}
export function detectProactiveEvent(ctx: ProactiveContext): CompanionEvent | null;

<!-- loadWeekStats from coupling-overrides.ts -->
export async function loadWeekStats(): Promise<{ weekKey: string; counts: Record<string, number> }>;

<!-- companion-storage.ts (created Plan 01) -->
export interface PersistedCompanionMessage { text: string; event: string; timestamp: string; }
export async function loadCompanionMessages(profileId: string): Promise<PersistedCompanionMessage[]>;
export async function saveCompanionMessages(profileId: string, messages: PersistedCompanionMessage[]): Promise<void>;

<!-- CompanionAvatarMini props (approximate) -->
<CompanionAvatarMini species={species} stage={stage} size={number} />

<!-- useVault() provides -->
const { activeProfile, tasks, ... } = useVault();
// activeProfile.companion: CompanionData | null
// activeProfile.id: string

<!-- useAI() provides -->
const { aiCall } = useAI();
// aiCall: ((prompt: string) => Promise<string>) | null
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ajouter weekly_recap + nudge flag dans engine + storage</name>
  <files>lib/mascot/companion-engine.ts, lib/mascot/companion-storage.ts</files>
  <read_first>
    - lib/mascot/companion-engine.ts (detectProactiveEvent ~ligne 564-593, ProactiveContext ~ligne 548-558)
    - lib/mascot/companion-storage.ts (pattern SecureStore existant)
    - lib/semantic/coupling-overrides.ts (loadWeekStats signature ~ligne 81)
  </read_first>
  <action>
1. Dans `lib/mascot/companion-engine.ts` :
   a. Ajouter `isWeeklyRecapWindow?: boolean` au type `ProactiveContext` — champ optionnel, le call site calcule `new Date().getDay() === 0 && hour >= 18 && hour < 21` et le passe.
   b. Dans `detectProactiveEvent()`, APRES le bloc `morning_greeting` (après ligne ~573) et APRES `family_milestone` (après ligne ~577), AVANT le `gentle_nudge` (ligne ~583), insérer :
   ```typescript
   // Weekly recap dimanche soir (D-09)
   if (ctx.isWeeklyRecapWindow) return 'weekly_recap';
   ```
   c. La ligne celebration est déjà commentée par Plan 01. S'assurer que `weekly_recap` est inséré APRES l'ancien emplacement de celebration.

2. Dans `lib/mascot/companion-storage.ts`, ajouter les fonctions nudge_shown_today (D-10) :
   ```typescript
   const NUDGE_FLAG_KEY_PREFIX = 'companion_nudge_shown_';

   export async function hasNudgeShownToday(profileId: string): Promise<boolean> {
     try {
       const today = new Date().toISOString().slice(0, 10);
       const stored = await SecureStore.getItemAsync(`${NUDGE_FLAG_KEY_PREFIX}${profileId}`);
       return stored === today;
     } catch {
       return false;
     }
   }

   export async function markNudgeShownToday(profileId: string): Promise<void> {
     try {
       const today = new Date().toISOString().slice(0, 10);
       await SecureStore.setItemAsync(`${NUDGE_FLAG_KEY_PREFIX}${profileId}`, today);
     } catch { /* non-critical */ }
   }
   ```
  </action>
  <verify>
    <automated>grep -q "isWeeklyRecapWindow" lib/mascot/companion-engine.ts && grep -q "weekly_recap" lib/mascot/companion-engine.ts && grep -q "hasNudgeShownToday" lib/mascot/companion-storage.ts && grep -q "markNudgeShownToday" lib/mascot/companion-storage.ts && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `ProactiveContext` contient `isWeeklyRecapWindow?: boolean`
    - `detectProactiveEvent` contient `if (ctx.isWeeklyRecapWindow) return 'weekly_recap'` APRES morning_greeting et AVANT gentle_nudge
    - companion-storage.ts exporte `hasNudgeShownToday` et `markNudgeShownToday`
    - `NUDGE_FLAG_KEY_PREFIX = 'companion_nudge_shown_'`
    - `npx tsc --noEmit` ne produit aucune nouvelle erreur
  </acceptance_criteria>
  <done>detectProactiveEvent retourne weekly_recap dimanche 18h-21h. Nudge flag SecureStore disponible.</done>
</task>

<task type="auto">
  <name>Task 2: Créer DashboardCompanion.tsx + câbler dans index.tsx + nudge flag dans tree.tsx</name>
  <files>components/dashboard/DashboardCompanion.tsx, components/dashboard/index.ts, app/(tabs)/index.tsx, app/(tabs)/tree.tsx</files>
  <read_first>
    - components/dashboard/DashboardBilanSemaine.tsx (pattern section dashboard + Sunday gate)
    - components/dashboard/types.ts (DashboardSectionProps)
    - components/dashboard/index.ts (barrel exports)
    - components/mascot/CompanionAvatarMini.tsx (props du composant avatar)
    - app/(tabs)/index.tsx (structure dashboard, imports, rendering sections)
    - app/(tabs)/tree.tsx (useFocusEffect companion ~ligne 670-780, proactive event trigger ~ligne 700-730)
    - lib/mascot/companion-engine.ts (detectProactiveEvent, pickCompanionMessage, generateCompanionAIMessage)
    - lib/mascot/companion-storage.ts (loadCompanionMessages, saveCompanionMessages, hasNudgeShownToday, markNudgeShownToday)
    - lib/semantic/coupling-overrides.ts (loadWeekStats)
    - lib/mascot/companion-types.ts (CompanionMessageContext, CompanionEvent)
  </read_first>
  <action>
1. Créer `components/dashboard/DashboardCompanion.tsx` — bulle inline discrète (D-06) :
   - Import `useVault`, `useThemeColors`, `useAI`, `useTranslation` (hooks existants)
   - Import `CompanionAvatarMini` depuis `../mascot/CompanionAvatarMini`
   - Import `detectProactiveEvent`, `pickCompanionMessage`, `generateCompanionAIMessage` depuis `../../lib/mascot/companion-engine`
   - Import `getCompanionMood`, `getCompanionStage` depuis `../../lib/mascot/companion-engine`
   - Import `loadCompanionMessages`, `saveCompanionMessages` depuis `../../lib/mascot/companion-storage`
   - Import `loadWeekStats` depuis `../../lib/semantic/coupling-overrides`
   - Import `SectionErrorBoundary` depuis `../SectionErrorBoundary`
   - Import `* as SecureStore` depuis `expo-secure-store`
   - Import `Animated` layout animation `FadeIn` depuis `react-native-reanimated` pour l'entrée de la bulle
   - Props: `DashboardSectionProps` (standard)

   - Le composant :
     a. State: `message: string | null`, `eventType: CompanionEvent | null`
     b. Early return `null` si `!activeProfile?.companion`
     c. useEffect sur `[activeProfile?.id]` (une seule fois au mount par profil) :
        - Lire `companion_last_visit` depuis SecureStore
        - Calculer `isFirstVisitToday = stored !== today`
        - Calculer `hoursSinceLastVisit` via companion.lastEventAt ou 0
        - Calculer `isWeeklyRecapWindow = new Date().getDay() === 0 && currentHour >= 18 && currentHour < 21`
        - Construire `ProactiveContext` avec les données du vault (tasks, streak, etc.)
        - Appeler `detectProactiveEvent(ctx)` — si null, ne rien afficher
        - Si event === `'weekly_recap'` : charger `loadWeekStats()` et injecter dans le contexte IA
        - Les events `gentle_nudge` et `comeback` ne sont PAS déclenchés par le dashboard (D-05)
        - Seuls `morning_greeting` et `weekly_recap` sont affichés sur le dashboard
        - Si event est `'gentle_nudge'` ou `'comeback'` ou autre action event : return sans afficher
        - Construire `CompanionMessageContext` avec companionSpecies, companionName, profileName, tasksToday, streak, level, recentMessages (depuis companionRecentMessagesRef ou loadCompanionMessages)
        - Pour `weekly_recap` : ajouter dans le context les stats semaine (totalEffects, topCategories) — passer dans `recentTasks` ou un champ custom pour que le prompt IA les voie
        - Appeler `pickCompanionMessage(event, ctx)` immédiatement pour le fallback template
        - Mettre `setMessage(t(templateKey))` immédiatement
        - Appeler `generateCompanionAIMessage(event, ctx, aiCall)` en async, puis `setMessage(aiMsg)` si retourné
        - Persister le message via `saveCompanionMessages` (fire-and-forget)
     d. Rendu :
        - `View` horizontal (flexDirection row, alignItems center)
        - `CompanionAvatarMini` a gauche (size ~40)
        - `Animated.View` (entering={FadeIn.duration(400)}) avec style bulle :
          - backgroundColor: `colors.card` (ou `colors.surface` si disponible, sinon `colors.card`)
          - borderRadius: Spacing.md (12)
          - padding: Spacing.sm/Spacing.md
          - borderWidth: 1, borderColor: `primary + '20'` (subtle)
          - flex: 1, marginLeft: Spacing.sm
        - `Text` avec `colors.text`, fontSize Typography.sm ou body
        - Pas d'animation de dismiss — le message reste visible tant que le dashboard est affiché
        - Pas de swipe (conflit ScrollView — CLAUDE.md)

2. Ajouter export dans `components/dashboard/index.ts` :
   `export { DashboardCompanion } from './DashboardCompanion';`

3. Dans `app/(tabs)/index.tsx` :
   - Ajouter `import { DashboardCompanion } from '../../components/dashboard';` (déjà dans le barrel)
   - Placer `<DashboardCompanion {...dashboardProps} />` en HAUT des sections dashboard, après le header/greeting existant et avant les autres sections (position discrète en haut per D-06)
   - Le wrapping `SectionErrorBoundary` est fait DANS le composant DashboardCompanion

4. Dans `app/(tabs)/tree.tsx` — ajouter le guard nudge_shown_today (D-10) :
   - Import `hasNudgeShownToday`, `markNudgeShownToday` depuis `../../lib/mascot/companion-storage`
   - Dans le bloc ou `detectProactiveEvent` retourne `'gentle_nudge'` (~ligne 700-730), ajouter un check async :
     ```typescript
     if (event === 'gentle_nudge') {
       const alreadyShown = await hasNudgeShownToday(activeProfile.id);
       if (alreadyShown) return; // D-10: max 1 nudge/jour
       markNudgeShownToday(activeProfile.id); // fire-and-forget
     }
     ```
   - Le useFocusEffect qui trigger le proactive event doit devenir async-friendly (wrapper le corps dans une async IIFE si pas déjà le cas)
   - Passer `isWeeklyRecapWindow` dans le ProactiveContext construit par tree.tsx (meme si tree.tsx ne gere pas le weekly_recap, le champ est requis par le type)
  </action>
  <verify>
    <automated>test -f components/dashboard/DashboardCompanion.tsx && grep -q "DashboardCompanion" components/dashboard/index.ts && grep -q "DashboardCompanion" 'app/(tabs)/index.tsx' && grep -q "hasNudgeShownToday" 'app/(tabs)/tree.tsx' && grep -q "markNudgeShownToday" 'app/(tabs)/tree.tsx' && grep -q "isWeeklyRecapWindow" 'app/(tabs)/tree.tsx' && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - DashboardCompanion.tsx existe dans components/dashboard/
    - DashboardCompanion.tsx importe `detectProactiveEvent`, `pickCompanionMessage`, `generateCompanionAIMessage`
    - DashboardCompanion.tsx importe `loadWeekStats` depuis coupling-overrides.ts
    - DashboardCompanion.tsx importe `CompanionAvatarMini`
    - DashboardCompanion.tsx contient un guard qui ne declenche QUE `morning_greeting` et `weekly_recap` (pas gentle_nudge/comeback per D-05)
    - DashboardCompanion.tsx utilise `FadeIn` de react-native-reanimated (pas RN Animated)
    - DashboardCompanion.tsx utilise `useThemeColors()` pour toutes les couleurs (pas de hardcoded hex)
    - DashboardCompanion.tsx est wrappé dans `SectionErrorBoundary`
    - components/dashboard/index.ts contient `export { DashboardCompanion }`
    - app/(tabs)/index.tsx contient `<DashboardCompanion` rendu en haut des sections
    - tree.tsx contient `hasNudgeShownToday` et `markNudgeShownToday` dans le bloc gentle_nudge
    - tree.tsx passe `isWeeklyRecapWindow` dans ProactiveContext
    - `npx tsc --noEmit` ne produit aucune nouvelle erreur
  </acceptance_criteria>
  <done>Bulle compagnon dashboard affichant morning_greeting et weekly_recap. Nudge limité à 1/jour via flag SecureStore. 4 event types proactifs câblés.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passe sans nouvelles erreurs
2. DashboardCompanion.tsx existe et est exporté dans le barrel
3. Dashboard index.tsx rend `<DashboardCompanion>` en haut des sections
4. tree.tsx vérifie `hasNudgeShownToday` avant d'afficher un nudge
5. detectProactiveEvent retourne `weekly_recap` quand `isWeeklyRecapWindow` est true
</verification>

<success_criteria>
- morning_greeting s'affiche sur le dashboard a la premiere ouverture du jour 6h-11h (COMPANION-02)
- weekly_recap s'affiche sur le dashboard dimanche 18h-21h avec stats couplage (COMPANION-01)
- gentle_nudge reste sur tree.tsx, limité a 1/jour (COMPANION-04, D-10)
- comeback fonctionne sur tree.tsx pour >24h d'absence (COMPANION-05)
- Bulle inline discrète (avatar + texte) cohérente avec l'UI dashboard (D-06)
- Pas de provider global (D-07)
- Zéro nouvelle erreur TypeScript
</success_criteria>

<output>
After completion, create `.planning/phases/24-compagnon-tendu-seed-003-lite/24-02-SUMMARY.md`
</output>
