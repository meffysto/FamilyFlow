---
phase: 06-batiments-productifs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/mascot/types.ts
  - lib/mascot/building-engine.ts
  - lib/mascot/building-sprites.ts
  - lib/mascot/world-grid.ts
  - lib/types.ts
  - lib/parser.ts
  - hooks/useFarm.ts
  - locales/fr/common.json
  - locales/en/common.json
autonomous: true
requirements: [BAT-01, BAT-02, BAT-03]

must_haves:
  truths:
    - "PlacedBuilding avec buildingId/cellId/level/lastCollectAt remplace l'ancien farmBuildings string[]"
    - "buildingEngine exporte constructBuilding, upgradeBuilding, collectBuilding, getPendingResources"
    - "parseFamille() gere les deux formats (ancien string[] et nouveau PlacedBuilding CSV)"
    - "3 batiments dans BUILDING_CATALOG avec 3 tiers chacun"
    - "Slot b2 existe dans WORLD_GRID pour le moulin"
    - "farmInventory persiste oeuf/lait/farine dans famille.md"
  artifacts:
    - path: "lib/mascot/building-engine.ts"
      provides: "Logique batiments (construct, upgrade, collect, pending, serialize, parse)"
      exports: ["constructBuilding", "upgradeBuilding", "collectBuilding", "getPendingResources", "serializeBuildings", "parseBuildings", "serializeInventory", "parseInventory", "MAX_PENDING"]
    - path: "lib/mascot/building-sprites.ts"
      provides: "Mapping sprites batiments par niveau"
      exports: ["BUILDING_SPRITES"]
    - path: "lib/mascot/types.ts"
      provides: "PlacedBuilding, BuildingTier, ResourceType, FarmInventory, BUILDING_CATALOG etendu"
      contains: "interface PlacedBuilding"
    - path: "lib/types.ts"
      provides: "Profile.farmBuildings: PlacedBuilding[], Profile.farmInventory: FarmInventory"
      contains: "farmInventory"
    - path: "lib/mascot/world-grid.ts"
      provides: "Slot b2 pour le moulin"
      contains: "id: 'b2'"
  key_links:
    - from: "lib/mascot/building-engine.ts"
      to: "lib/mascot/types.ts"
      via: "imports PlacedBuilding, BuildingDefinition, FarmInventory"
      pattern: "import.*PlacedBuilding.*from.*types"
    - from: "lib/parser.ts"
      to: "lib/mascot/building-engine.ts"
      via: "parseBuildings() pour migration backward-compatible"
      pattern: "parseBuildings"
    - from: "hooks/useFarm.ts"
      to: "lib/mascot/building-engine.ts"
      via: "constructBuilding, upgradeBuilding, collectBuilding"
      pattern: "import.*from.*building-engine"
---

<objective>
Creer le systeme de donnees et la logique metier des batiments productifs : types etendus (PlacedBuilding, BuildingTier, FarmInventory), building-engine.ts avec toutes les operations, migration backward-compatible du parser, extension de useFarm, et sprites mapping.

Purpose: Fournir la couche donnees complete pour que le Plan 02 (UI) puisse directement consommer les fonctions sans logique metier.
Output: building-engine.ts, types etendus, parser migre, useFarm etendu, sprites mapping, i18n keys.
</objective>

<execution_context>
@/Users/gabrielwaltio/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gabrielwaltio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-batiments-productifs/06-CONTEXT.md
@.planning/phases/06-batiments-productifs/06-RESEARCH.md

<interfaces>
<!-- Types et contracts existants que l'executeur doit connaitre -->

From lib/mascot/types.ts (actuel — a etendre):
```typescript
export interface BuildingDefinition {
  id: string;
  labelKey: string;
  emoji: string;
  cost: number;
  dailyIncome: number;
  minTreeStage: TreeStage;
}

export const BUILDING_CATALOG: BuildingDefinition[] = [
  { id: 'poulailler', labelKey: 'farm.building.poulailler', emoji: '🏠', cost: 300, dailyIncome: 5, minTreeStage: 'arbuste' },
  { id: 'grange', labelKey: 'farm.building.grange', emoji: '🏚️', cost: 800, dailyIncome: 8, minTreeStage: 'arbre' },
];
```

From lib/types.ts (actuel — a modifier):
```typescript
export interface Profile {
  farmCrops: string;
  farmBuildings: string[];  // A migrer vers PlacedBuilding[]
  coins: number;
  // ...
}
```

From lib/mascot/farm-engine.ts (pattern a suivre):
```typescript
export function serializeCrops(crops: PlantedCrop[]): string
export function parseCrops(csv: string): PlantedCrop[]
export function plantCrop(crops, plotIndex, cropId): PlantedCrop[]
export function harvestCrop(crops, plotIndex): { crops; reward }
```

From hooks/useFarm.ts (pattern a etendre):
```typescript
export function useFarm() {
  const writeFarmCrops = useCallback(async (profileId, cropsCSV) => { ... })
  const deductCoins = useCallback(async (profileId, amount, note) => { ... })
  const addCoins = useCallback(async (profileId, amount, note) => { ... })
  const buyBuilding = useCallback(async (profileId, buildingId) => { ... })
  const collectPassiveIncome = useCallback(async (profileId) => { ... })
  return { plant, harvest, buyBuilding, collectPassiveIncome };
}
```

From lib/mascot/world-grid.ts:
```typescript
export const BUILDING_CELLS = WORLD_GRID
  .filter(c => c.cellType === 'building')
  .sort((a, b) => a.unlockOrder - b.unlockOrder);
// Actuellement: b0 (row 2, col 5) et b1 (row 3, col 5)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Types etendus + building-engine.ts + sprites mapping + world-grid b2</name>
  <files>
    lib/mascot/types.ts,
    lib/mascot/building-engine.ts,
    lib/mascot/building-sprites.ts,
    lib/mascot/world-grid.ts,
    lib/types.ts
  </files>
  <read_first>
    lib/mascot/types.ts,
    lib/mascot/farm-engine.ts,
    lib/mascot/world-grid.ts,
    lib/types.ts
  </read_first>
  <action>
**1. Etendre les types dans `lib/mascot/types.ts`:**

Ajouter apres la definition existante de `BuildingDefinition` (ligne ~331):

```typescript
export type ResourceType = 'oeuf' | 'lait' | 'farine';

export interface FarmInventory {
  oeuf: number;
  lait: number;
  farine: number;
}

export interface BuildingTier {
  level: number;
  productionRateHours: number;
  upgradeCoins: number;
  spriteSuffix: string;  // '_1', '_2', '_3' — pour BUILDING_SPRITES lookup
}

export interface PlacedBuilding {
  buildingId: string;
  cellId: string;
  level: number;
  lastCollectAt: string;  // ISO datetime YYYY-MM-DDTHH:mm
}
```

**Modifier** `BuildingDefinition` existante pour ajouter les champs `resourceType` et `tiers` tout en GARDANT `dailyIncome` pour compatibilite (deprecated):

```typescript
export interface BuildingDefinition {
  id: string;
  labelKey: string;
  emoji: string;
  cost: number;
  dailyIncome: number;       // @deprecated — utiliser tiers[0].productionRateHours
  minTreeStage: TreeStage;
  resourceType: ResourceType;
  tiers: BuildingTier[];
}
```

**Mettre a jour BUILDING_CATALOG** avec moulin + tiers pour les 3:

```typescript
export const BUILDING_CATALOG: BuildingDefinition[] = [
  {
    id: 'poulailler', labelKey: 'farm.building.poulailler', emoji: '🐔',
    cost: 300, dailyIncome: 5, minTreeStage: 'arbuste',
    resourceType: 'oeuf',
    tiers: [
      { level: 1, productionRateHours: 6, upgradeCoins: 0, spriteSuffix: '_1' },
      { level: 2, productionRateHours: 4, upgradeCoins: 500, spriteSuffix: '_2' },
      { level: 3, productionRateHours: 3, upgradeCoins: 1000, spriteSuffix: '_3' },
    ],
  },
  {
    id: 'grange', labelKey: 'farm.building.grange', emoji: '🐄',
    cost: 800, dailyIncome: 8, minTreeStage: 'arbre',
    resourceType: 'lait',
    tiers: [
      { level: 1, productionRateHours: 8, upgradeCoins: 0, spriteSuffix: '_1' },
      { level: 2, productionRateHours: 6, upgradeCoins: 1200, spriteSuffix: '_2' },
      { level: 3, productionRateHours: 4, upgradeCoins: 2000, spriteSuffix: '_3' },
    ],
  },
  {
    id: 'moulin', labelKey: 'farm.building.moulin', emoji: '🌾',
    cost: 1200, dailyIncome: 0, minTreeStage: 'majestueux',
    resourceType: 'farine',
    tiers: [
      { level: 1, productionRateHours: 8, upgradeCoins: 0, spriteSuffix: '_1' },
      { level: 2, productionRateHours: 6, upgradeCoins: 1800, spriteSuffix: '_2' },
      { level: 3, productionRateHours: 4, upgradeCoins: 3000, spriteSuffix: '_3' },
    ],
  },
];
```

**2. Modifier `lib/types.ts`:**

Ajouter les imports en haut:
```typescript
import type { PlacedBuilding, FarmInventory } from './mascot/types';
```

Modifier l'interface `Profile` (ligne ~84):
- `farmBuildings: string[]` → `farmBuildings: PlacedBuilding[]`
- Ajouter `farmInventory: FarmInventory;` apres `farmBuildings`

**3. Creer `lib/mascot/building-engine.ts`** sur le modele de farm-engine.ts:

```typescript
import { differenceInHours } from 'date-fns';
import { type PlacedBuilding, type FarmInventory, type ResourceType, BUILDING_CATALOG } from './types';

export const MAX_PENDING = 3;
const DEFAULT_INVENTORY: FarmInventory = { oeuf: 0, lait: 0, farine: 0 };

export function constructBuilding(
  buildings: PlacedBuilding[],
  buildingId: string,
  cellId: string,
): PlacedBuilding[] {
  if (buildings.some(b => b.cellId === cellId)) return buildings;
  if (!BUILDING_CATALOG.find(b => b.id === buildingId)) return buildings;
  return [...buildings, {
    buildingId,
    cellId,
    level: 1,
    lastCollectAt: new Date().toISOString().slice(0, 16),
  }];
}

export function upgradeBuilding(
  buildings: PlacedBuilding[],
  cellId: string,
): PlacedBuilding[] {
  return buildings.map(b => {
    if (b.cellId !== cellId) return b;
    const def = BUILDING_CATALOG.find(d => d.id === b.buildingId);
    if (!def || b.level >= def.tiers.length) return b;
    return { ...b, level: b.level + 1 };
  });
}

export function getPendingResources(
  building: PlacedBuilding,
  now: Date = new Date(),
): number {
  const def = BUILDING_CATALOG.find(d => d.id === building.buildingId);
  if (!def) return 0;
  const tier = def.tiers[building.level - 1];
  if (!tier) return 0;

  const lastCollect = building.lastCollectAt
    ? new Date(building.lastCollectAt.includes('T') ? building.lastCollectAt : `${building.lastCollectAt}T00:00`)
    : new Date(0);

  const hoursElapsed = differenceInHours(now, lastCollect);
  const units = Math.floor(hoursElapsed / tier.productionRateHours);
  return Math.min(units, MAX_PENDING);
}

export function collectBuilding(
  buildings: PlacedBuilding[],
  inventory: FarmInventory,
  cellId: string,
  now: Date = new Date(),
): { buildings: PlacedBuilding[]; inventory: FarmInventory; collected: number } {
  const building = buildings.find(b => b.cellId === cellId);
  if (!building) return { buildings, inventory, collected: 0 };

  const def = BUILDING_CATALOG.find(d => d.id === building.buildingId);
  if (!def) return { buildings, inventory, collected: 0 };

  const pending = getPendingResources(building, now);
  if (pending === 0) return { buildings, inventory, collected: 0 };

  const newInventory = { ...inventory };
  newInventory[def.resourceType] = (newInventory[def.resourceType] ?? 0) + pending;

  const newBuildings = buildings.map(b =>
    b.cellId === cellId
      ? { ...b, lastCollectAt: now.toISOString().slice(0, 16) }
      : b
  );

  return { buildings: newBuildings, inventory: newInventory, collected: pending };
}

export function getUpgradeCost(building: PlacedBuilding): number {
  const def = BUILDING_CATALOG.find(d => d.id === building.buildingId);
  if (!def) return 0;
  const nextTier = def.tiers[building.level]; // level is 1-indexed, tiers is 0-indexed — tier at [level] is the next
  return nextTier?.upgradeCoins ?? 0;
}

export function canUpgrade(building: PlacedBuilding): boolean {
  const def = BUILDING_CATALOG.find(d => d.id === building.buildingId);
  if (!def) return false;
  return building.level < def.tiers.length;
}

// ── Serialisation (pattern farm-engine) ──

export function serializeBuildings(buildings: PlacedBuilding[]): string {
  if (buildings.length === 0) return '';
  return buildings
    .map(b => `${b.buildingId}:${b.cellId}:${b.level}:${b.lastCollectAt}`)
    .join(',');
}

export function parseBuildings(csv: string): PlacedBuilding[] {
  if (!csv || csv.trim() === '') return [];
  const LEGACY_CELL_MAP: Record<string, string> = { poulailler: 'b0', grange: 'b1' };

  return csv.split(',').map(entry => {
    const parts = entry.trim().split(':');

    // Ancien format: "poulailler" (ID seul) — migration implicite
    if (parts.length === 1) {
      return {
        buildingId: parts[0].trim(),
        cellId: LEGACY_CELL_MAP[parts[0].trim()] ?? 'b0',
        level: 1,
        lastCollectAt: new Date().toISOString().slice(0, 16),
      };
    }

    // Nouveau format: "poulailler:b0:1:2026-03-28T10:00"
    const [buildingId, cellId, levelStr, ...dateParts] = parts;
    return {
      buildingId: buildingId.trim(),
      cellId: cellId.trim(),
      level: Math.max(1, Math.min(3, parseInt(levelStr, 10) || 1)),
      lastCollectAt: dateParts.join(':').trim() || new Date().toISOString().slice(0, 16),
    };
  }).filter(b => b.buildingId && b.cellId);
}

export function serializeInventory(inv: FarmInventory): string {
  return `oeuf:${inv.oeuf ?? 0},lait:${inv.lait ?? 0},farine:${inv.farine ?? 0}`;
}

export function parseInventory(csv: string): FarmInventory {
  const inv: FarmInventory = { oeuf: 0, lait: 0, farine: 0 };
  if (!csv || csv.trim() === '') return inv;
  csv.split(',').forEach(pair => {
    const [key, val] = pair.split(':');
    const k = key?.trim() as ResourceType;
    if (k in inv) inv[k] = parseInt(val?.trim() ?? '0', 10) || 0;
  });
  return inv;
}
```

**4. Creer `lib/mascot/building-sprites.ts`:**

Creer 9 placeholder sprites 32x32 dans `assets/buildings/` avant (couleur unie avec texte, generes programmatiquement n'est pas possible — creer des fichiers PNG placeholder simples OU copier un asset existant comme placeholder).

Puisque D-15 dit "Sprites generes par IA dans le style Mana Seed pixel 32x32", et qu'on ne peut pas generer d'images ici, creer le mapping avec require() vers des fichiers qui seront les sprites finaux. Utiliser les sprites existants `poulailler.png` et `grange.png` comme placeholder pour le niveau 1, et les memes fichiers pour les niveaux 2/3 temporairement.

```typescript
export const BUILDING_SPRITES: Record<string, Record<number, any>> = {
  poulailler: {
    1: require('../../assets/buildings/poulailler.png'),
    2: require('../../assets/buildings/poulailler.png'),  // placeholder — a remplacer par sprite niveau 2
    3: require('../../assets/buildings/poulailler.png'),  // placeholder — a remplacer par sprite niveau 3
  },
  grange: {
    1: require('../../assets/buildings/grange.png'),
    2: require('../../assets/buildings/grange.png'),
    3: require('../../assets/buildings/grange.png'),
  },
  moulin: {
    1: require('../../assets/buildings/grange.png'),  // placeholder — a remplacer par sprite moulin
    2: require('../../assets/buildings/grange.png'),
    3: require('../../assets/buildings/grange.png'),
  },
};
```

Note: les vrais sprites devront etre generes separement (D-15). Le mapping est pret.

**5. Ajouter slot b2 dans `lib/mascot/world-grid.ts`:**

Ajouter dans WORLD_GRID, dans la section Row 1 (apres c7):
```typescript
{ id: 'b2', col: 5, row: 1, x: 0.90, y: 0.32, cellType: 'building', unlockOrder: 19, size: 'large' },
```

Cela place le 3eme slot batiment au-dessus de b0 (row 2, col 5), symetrique.

**6. Modifier `Profile` dans `lib/types.ts`:**

Changer la ligne `farmBuildings: string[]` en:
```typescript
farmBuildings: PlacedBuilding[];
farmInventory: FarmInventory;
```

Ajouter l'import en haut du fichier:
```typescript
import type { PlacedBuilding, FarmInventory } from './mascot/types';
```
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep "interface PlacedBuilding" lib/mascot/types.ts retourne 1 resultat
    - grep "interface FarmInventory" lib/mascot/types.ts retourne 1 resultat
    - grep "interface BuildingTier" lib/mascot/types.ts retourne 1 resultat
    - grep "ResourceType" lib/mascot/types.ts retourne au moins 1 resultat
    - grep "id: 'moulin'" lib/mascot/types.ts retourne 1 resultat (3eme batiment ajoute)
    - grep "resourceType:" lib/mascot/types.ts retourne 3 resultats (un par batiment)
    - grep "tiers:" lib/mascot/types.ts retourne 3 resultats (un par batiment)
    - grep "constructBuilding" lib/mascot/building-engine.ts retourne au moins 1 resultat
    - grep "upgradeBuilding" lib/mascot/building-engine.ts retourne au moins 1 resultat
    - grep "collectBuilding" lib/mascot/building-engine.ts retourne au moins 1 resultat
    - grep "getPendingResources" lib/mascot/building-engine.ts retourne au moins 1 resultat
    - grep "parseBuildings" lib/mascot/building-engine.ts retourne au moins 1 resultat
    - grep "serializeBuildings" lib/mascot/building-engine.ts retourne au moins 1 resultat
    - grep "serializeInventory" lib/mascot/building-engine.ts retourne au moins 1 resultat
    - grep "parseInventory" lib/mascot/building-engine.ts retourne au moins 1 resultat
    - grep "MAX_PENDING" lib/mascot/building-engine.ts retourne au moins 1 resultat avec valeur 3
    - grep "BUILDING_SPRITES" lib/mascot/building-sprites.ts retourne au moins 1 resultat
    - grep "moulin" lib/mascot/building-sprites.ts retourne au moins 1 resultat
    - grep "id: 'b2'" lib/mascot/world-grid.ts retourne 1 resultat
    - grep "unlockOrder: 19" lib/mascot/world-grid.ts retourne 1 resultat
    - grep "farmBuildings: PlacedBuilding" lib/types.ts retourne 1 resultat
    - grep "farmInventory: FarmInventory" lib/types.ts retourne 1 resultat
  </acceptance_criteria>
  <done>
    - PlacedBuilding, BuildingTier, FarmInventory, ResourceType definis dans types.ts
    - BUILDING_CATALOG contient 3 batiments (poulailler, grange, moulin) avec 3 tiers chacun
    - building-engine.ts exporte toutes les fonctions (construct, upgrade, collect, getPendingResources, serialize/parse)
    - Migration backward-compatible: parseBuildings gere l'ancien format (string seul) et le nouveau (CSV avec colons)
    - Slot b2 ajoute dans WORLD_GRID (unlockOrder 19)
    - BUILDING_SPRITES mappe 3 batiments x 3 niveaux (placeholders pour niveaux 2/3)
    - Profile.farmBuildings type change de string[] a PlacedBuilding[]
    - Profile.farmInventory ajoute
  </done>
</task>

<task type="auto">
  <name>Task 2: Migration parser + useFarm etendu + cles i18n</name>
  <files>
    lib/parser.ts,
    hooks/useFarm.ts,
    locales/fr/common.json,
    locales/en/common.json
  </files>
  <read_first>
    lib/parser.ts,
    hooks/useFarm.ts,
    lib/mascot/building-engine.ts,
    lib/mascot/types.ts,
    lib/types.ts,
    locales/fr/common.json,
    locales/en/common.json
  </read_first>
  <action>
**1. Migrer `parseFamille()` dans `lib/parser.ts`:**

Ajouter l'import en haut de parser.ts:
```typescript
import { parseBuildings, parseInventory } from './mascot/building-engine';
```

Remplacer les lignes ~570-573 (section "Cultures ferme"):
```typescript
// AVANT:
// const farmBuildings = currentProps.farm_buildings
//   ? currentProps.farm_buildings.split(',').map((s: string) => s.trim()).filter(Boolean)
//   : [];

// APRES:
const farmBuildings = currentProps.farm_buildings
  ? parseBuildings(currentProps.farm_buildings)
  : [];
const farmInventory = currentProps.farm_inventory
  ? parseInventory(currentProps.farm_inventory)
  : { oeuf: 0, lait: 0, farine: 0 };
```

Dans le `profiles.push({...})` qui suit (~ligne 574-592), ajouter `farmInventory` apres `farmBuildings`:
```typescript
farmBuildings,
farmInventory,
```

**2. Refactorer `hooks/useFarm.ts`:**

Ajouter les imports:
```typescript
import {
  constructBuilding as constructBuildingFn,
  upgradeBuilding as upgradeBuildingFn,
  collectBuilding as collectBuildingFn,
  getPendingResources,
  serializeBuildings,
  parseBuildings,
  serializeInventory,
  parseInventory,
  getUpgradeCost,
  canUpgrade,
} from '../lib/mascot/building-engine';
import type { PlacedBuilding, FarmInventory } from '../lib/mascot/types';
```

Creer un helper `writeField` generique (refactorer le pattern existant de `writeFarmCrops`):
```typescript
const writeField = useCallback(async (profileId: string, fieldKey: string, value: string) => {
  if (!vault) return;
  const content = await vault.readFile(FAMILLE_FILE);
  const lines = content.split('\n');
  let inSection = false;
  let fieldLine = -1;
  let lastPropIdx = -1;

  for (let i = 0; i < lines.length; i++) {
    if (lines[i].startsWith('### ')) {
      if (inSection) break;
      if (lines[i].replace('### ', '').trim() === profileId) inSection = true;
    } else if (inSection && lines[i].includes(': ')) {
      lastPropIdx = i;
      if (lines[i].trim().startsWith(`${fieldKey}:`)) fieldLine = i;
    }
  }

  const newValue = `${fieldKey}: ${value}`;
  if (fieldLine >= 0) {
    lines[fieldLine] = newValue;
  } else if (lastPropIdx >= 0) {
    lines.splice(lastPropIdx + 1, 0, newValue);
  }
  await vault.writeFile(FAMILLE_FILE, lines.join('\n'));
}, [vault]);
```

Refactorer `writeFarmCrops` pour utiliser `writeField`:
```typescript
const writeFarmCrops = useCallback(async (profileId: string, cropsCSV: string) => {
  await writeField(profileId, 'farm_crops', cropsCSV);
}, [writeField]);
```

**Refactorer `buyBuilding`** pour le nouveau format PlacedBuilding:
```typescript
const buyBuilding = useCallback(async (profileId: string, buildingId: string, cellId: string) => {
  const profile = profiles?.find(p => p.id === profileId);
  if (!profile) return;

  const building = BUILDING_CATALOG.find(b => b.id === buildingId);
  if (!building) return;

  const currentBuildings = profile.farmBuildings ?? [];
  if (currentBuildings.some((b: PlacedBuilding) => b.cellId === cellId)) return;
  if ((profile.coins ?? 0) < building.cost) throw new Error('Pas assez de feuilles');

  const newBuildings = constructBuildingFn(currentBuildings, buildingId, cellId);
  await writeField(profileId, 'farm_buildings', serializeBuildings(newBuildings));
  await deductCoins(profileId, building.cost, `🏗️ Construction : ${buildingId}`);
  await refresh();
}, [profiles, writeField, deductCoins, refresh]);
```

**Ajouter `upgradeBuildingAction`:**
```typescript
const upgradeBuildingAction = useCallback(async (profileId: string, cellId: string) => {
  const profile = profiles?.find(p => p.id === profileId);
  if (!profile) return;

  const currentBuildings: PlacedBuilding[] = profile.farmBuildings ?? [];
  const building = currentBuildings.find(b => b.cellId === cellId);
  if (!building) return;

  const cost = getUpgradeCost(building);
  if (cost === 0 || !canUpgrade(building)) return;
  if ((profile.coins ?? 0) < cost) throw new Error('Pas assez de feuilles');

  const newBuildings = upgradeBuildingFn(currentBuildings, cellId);
  await writeField(profileId, 'farm_buildings', serializeBuildings(newBuildings));
  await deductCoins(profileId, cost, `⬆️ Amelioration : ${building.buildingId} Niv ${building.level + 1}`);
  await refresh();
}, [profiles, writeField, deductCoins, refresh]);
```

**Ajouter `collectBuildingResources`:**
```typescript
const collectBuildingResources = useCallback(async (profileId: string, cellId: string) => {
  const profile = profiles?.find(p => p.id === profileId);
  if (!profile || !vault) return 0;

  const currentBuildings: PlacedBuilding[] = profile.farmBuildings ?? [];
  const currentInventory: FarmInventory = profile.farmInventory ?? { oeuf: 0, lait: 0, farine: 0 };

  const result = collectBuildingFn(currentBuildings, currentInventory, cellId);
  if (result.collected === 0) return 0;

  // Ecrire les deux champs en sequence
  await writeField(profileId, 'farm_buildings', serializeBuildings(result.buildings));
  await writeField(profileId, 'farm_inventory', serializeInventory(result.inventory));
  await refresh();
  return result.collected;
}, [profiles, vault, writeField, refresh]);
```

**Refactorer `collectPassiveIncome`** pour garder la signature mais utiliser le nouveau systeme. Retourner le nombre total de ressources collectees (affichage toast):
```typescript
const collectPassiveIncome = useCallback(async (profileId: string): Promise<number> => {
  const profile = profiles?.find(p => p.id === profileId);
  if (!profile || !vault) return 0;

  const buildings: PlacedBuilding[] = profile.farmBuildings ?? [];
  if (buildings.length === 0) return 0;

  let totalCollected = 0;
  let currentBuildings = [...buildings];
  let currentInventory: FarmInventory = profile.farmInventory ?? { oeuf: 0, lait: 0, farine: 0 };

  for (const b of buildings) {
    const result = collectBuildingFn(currentBuildings, currentInventory, b.cellId);
    totalCollected += result.collected;
    currentBuildings = result.buildings;
    currentInventory = result.inventory;
  }

  if (totalCollected === 0) return 0;

  await writeField(profileId, 'farm_buildings', serializeBuildings(currentBuildings));
  await writeField(profileId, 'farm_inventory', serializeInventory(currentInventory));
  await refresh();
  return totalCollected;
}, [profiles, vault, writeField, refresh]);
```

**Mettre a jour le return:**
```typescript
return { plant, harvest, buyBuilding, upgradeBuildingAction, collectBuildingResources, collectPassiveIncome };
```

**3. Ajouter les cles i18n:**

Dans `locales/fr/common.json`, ajouter dans la section "farm":
```json
"farm.building.moulin": "Moulin",
"farm.building.poulailler": "Poulailler",
"farm.building.grange": "Grange",
"farm.building.level": "Niveau {{level}}",
"farm.building.production": "Produit : {{resource}}",
"farm.building.frequency": "Toutes les {{hours}}h",
"farm.building.pending": "{{count}} a collecter",
"farm.building.collect": "Collecter",
"farm.building.upgrade": "Ameliorer",
"farm.building.upgradeTo": "Ameliorer au Niv {{level}}",
"farm.building.cost": "{{cost}} 🍃",
"farm.building.maxLevel": "Niveau max",
"farm.building.build": "Construire",
"farm.building.empty": "Emplacement libre",
"farm.building.locked": "Debloquer au stade {{stage}}",
"farm.resource.oeuf": "oeuf",
"farm.resource.lait": "lait",
"farm.resource.farine": "farine",
"farm.inventory.title": "Inventaire"
```

Dans `locales/en/common.json`, ajouter l'equivalent anglais:
```json
"farm.building.moulin": "Mill",
"farm.building.poulailler": "Chicken Coop",
"farm.building.grange": "Barn",
"farm.building.level": "Level {{level}}",
"farm.building.production": "Produces: {{resource}}",
"farm.building.frequency": "Every {{hours}}h",
"farm.building.pending": "{{count}} to collect",
"farm.building.collect": "Collect",
"farm.building.upgrade": "Upgrade",
"farm.building.upgradeTo": "Upgrade to Lv {{level}}",
"farm.building.cost": "{{cost}} 🍃",
"farm.building.maxLevel": "Max level",
"farm.building.build": "Build",
"farm.building.empty": "Empty slot",
"farm.building.locked": "Unlock at {{stage}} stage",
"farm.resource.oeuf": "egg",
"farm.resource.lait": "milk",
"farm.resource.farine": "flour",
"farm.inventory.title": "Inventory"
```
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep "parseBuildings" lib/parser.ts retourne au moins 1 resultat (import utilise)
    - grep "parseInventory" lib/parser.ts retourne au moins 1 resultat
    - grep "farmInventory" lib/parser.ts retourne au moins 2 resultats (parse + push)
    - grep "upgradeBuildingAction" hooks/useFarm.ts retourne au moins 1 resultat
    - grep "collectBuildingResources" hooks/useFarm.ts retourne au moins 1 resultat
    - grep "writeField" hooks/useFarm.ts retourne au moins 3 resultats
    - grep "serializeBuildings" hooks/useFarm.ts retourne au moins 1 resultat
    - grep "serializeInventory" hooks/useFarm.ts retourne au moins 1 resultat
    - grep "farm.building.moulin" locales/fr/common.json retourne 1 resultat
    - grep "farm.resource.oeuf" locales/fr/common.json retourne 1 resultat
    - grep "farm.building.moulin" locales/en/common.json retourne 1 resultat
    - grep "farm.resource.oeuf" locales/en/common.json retourne 1 resultat
    - L'ancien buyBuilding dans useFarm accepte maintenant un 3eme parametre cellId
    - collectPassiveIncome garde sa signature (profileId: string): Promise<number>
  </acceptance_criteria>
  <done>
    - parseFamille() utilise parseBuildings() (gere ancien format string[] et nouveau PlacedBuilding CSV)
    - parseFamille() parse farmInventory depuis famille.md
    - useFarm exporte : plant, harvest, buyBuilding(+cellId), upgradeBuildingAction, collectBuildingResources, collectPassiveIncome
    - collectPassiveIncome refactore pour collecter les ressources typees de tous les batiments d'un coup
    - writeField helper generique remplace le pattern duplique
    - Cles i18n ajoutees en fr et en pour tous les textes batiments
    - npx tsc --noEmit passe sans nouvelles erreurs
  </done>
</task>

</tasks>

<verification>
```bash
# Verifier compilation
npx tsc --noEmit 2>&1 | head -30

# Verifier que building-engine est complet
grep "export function" lib/mascot/building-engine.ts | wc -l
# Attendu: >= 8 (construct, upgrade, collect, getPending, getUpgradeCost, canUpgrade, serialize x2, parse x2)

# Verifier migration backward-compatible
grep "parts.length === 1" lib/mascot/building-engine.ts
# Attendu: detecte l'ancien format

# Verifier slot b2
grep "b2" lib/mascot/world-grid.ts
```
</verification>

<success_criteria>
- building-engine.ts est un module autonome avec toutes les operations batiment
- Le parser gere la migration transparente de l'ancien format vers le nouveau
- useFarm expose les 3 nouvelles actions (buyBuilding avec cellId, upgradeBuildingAction, collectBuildingResources)
- Tous les types sont coherents et tsc passe
- Les cles i18n couvrent tous les textes UI necessaires pour Plan 02
</success_criteria>

<output>
After completion, create `.planning/phases/06-batiments-productifs/06-01-SUMMARY.md`
</output>
