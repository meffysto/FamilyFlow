---
phase: 06-batiments-productifs
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - components/mascot/WorldGridView.tsx
  - components/mascot/BuildingShopSheet.tsx
  - components/mascot/BuildingDetailSheet.tsx
  - app/(tabs)/tree.tsx
autonomous: false
requirements: [BAT-01, BAT-02, BAT-03]

must_haves:
  truths:
    - "Tap sur cellule building vide ouvre un bottom sheet avec les batiments constructibles"
    - "Tap sur batiment place ouvre un detail sheet avec collecte et amelioration"
    - "Le badge pending pulse quand des ressources sont pretes"
    - "La collecte met a jour l'inventaire et affiche un toast"
    - "L'amelioration change le sprite et la frequence de production"
    - "Les batiments sont visibles sur la grille avec le bon sprite selon le niveau"
  artifacts:
    - path: "components/mascot/WorldGridView.tsx"
      provides: "BuildingCell interactif avec badge pending + sprite par niveau"
      contains: "onBuildingCellPress"
    - path: "components/mascot/BuildingShopSheet.tsx"
      provides: "Bottom sheet construction batiment"
      contains: "BuildingShopSheet"
    - path: "components/mascot/BuildingDetailSheet.tsx"
      provides: "Bottom sheet detail/collecte/amelioration"
      contains: "BuildingDetailSheet"
    - path: "app/(tabs)/tree.tsx"
      provides: "Integration complete batiments dans l'ecran ferme"
      contains: "BuildingShopSheet"
  key_links:
    - from: "components/mascot/WorldGridView.tsx"
      to: "lib/mascot/building-engine.ts"
      via: "getPendingResources pour badge count"
      pattern: "getPendingResources"
    - from: "components/mascot/WorldGridView.tsx"
      to: "lib/mascot/building-sprites.ts"
      via: "BUILDING_SPRITES pour rendu sprite par niveau"
      pattern: "BUILDING_SPRITES"
    - from: "app/(tabs)/tree.tsx"
      to: "hooks/useFarm.ts"
      via: "buyBuilding, upgradeBuildingAction, collectBuildingResources"
      pattern: "upgradeBuildingAction|collectBuildingResources"
    - from: "components/mascot/BuildingDetailSheet.tsx"
      to: "app/(tabs)/tree.tsx"
      via: "callbacks onCollect et onUpgrade passes en props"
      pattern: "onCollect|onUpgrade"
---

<objective>
Implementer l'interface utilisateur des batiments productifs : BuildingCell interactif dans WorldGridView, bottom sheets de construction et detail/collecte/amelioration, integration complete dans tree.tsx.

Purpose: L'utilisateur peut construire, collecter, et ameliorer ses batiments directement sur la ferme.
Output: WorldGridView etendu, BuildingShopSheet, BuildingDetailSheet, tree.tsx integre.
</objective>

<execution_context>
@/Users/gabrielwaltio/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gabrielwaltio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-batiments-productifs/06-CONTEXT.md
@.planning/phases/06-batiments-productifs/06-RESEARCH.md
@.planning/phases/06-batiments-productifs/06-01-SUMMARY.md

<interfaces>
<!-- Interfaces creees par Plan 01 — consommees par ce plan -->

From lib/mascot/types.ts:
```typescript
export type ResourceType = 'oeuf' | 'lait' | 'farine';
export interface FarmInventory { oeuf: number; lait: number; farine: number; }
export interface PlacedBuilding { buildingId: string; cellId: string; level: number; lastCollectAt: string; }
export interface BuildingTier { level: number; productionRateHours: number; upgradeCoins: number; spriteSuffix: string; }
export interface BuildingDefinition {
  id: string; labelKey: string; emoji: string; cost: number;
  dailyIncome: number; minTreeStage: TreeStage; resourceType: ResourceType; tiers: BuildingTier[];
}
export const BUILDING_CATALOG: BuildingDefinition[];  // 3 entries: poulailler, grange, moulin
```

From lib/mascot/building-engine.ts:
```typescript
export const MAX_PENDING = 3;
export function constructBuilding(buildings, buildingId, cellId): PlacedBuilding[]
export function upgradeBuilding(buildings, cellId): PlacedBuilding[]
export function collectBuilding(buildings, inventory, cellId, now?): { buildings; inventory; collected }
export function getPendingResources(building, now?): number
export function getUpgradeCost(building): number
export function canUpgrade(building): boolean
```

From lib/mascot/building-sprites.ts:
```typescript
export const BUILDING_SPRITES: Record<string, Record<number, any>>;
// poulailler: { 1, 2, 3 }, grange: { 1, 2, 3 }, moulin: { 1, 2, 3 }
```

From hooks/useFarm.ts:
```typescript
export function useFarm() {
  // ...existing...
  return {
    plant, harvest,
    buyBuilding,                  // (profileId, buildingId, cellId) => Promise<void>
    upgradeBuildingAction,        // (profileId, cellId) => Promise<void>
    collectBuildingResources,     // (profileId, cellId) => Promise<number>
    collectPassiveIncome,         // (profileId) => Promise<number> — collecte tous les batiments
  };
}
```

From components/mascot/WorldGridView.tsx (actuel):
```typescript
interface WorldGridViewProps {
  treeStage: TreeStage;
  farmCropsCSV: string;
  ownedBuildings: string[];  // A changer vers PlacedBuilding[]
  containerWidth: number;
  containerHeight: number;
  onCropPlotPress?: (cellId: string, crop: PlantedCrop | null) => void;
}

function BuildingCell({ cell, buildingId, containerWidth, containerHeight })
// Actuellement: rendu statique, pas de tap, pas de badge, pas de sprite par niveau
```

From app/(tabs)/tree.tsx:
```typescript
const { plant, harvest, buyBuilding, collectPassiveIncome } = useFarm();
// L'ecran passe ownedBuildings={profile.farmBuildings} a WorldGridView
// collectPassiveIncome appele dans useEffect a l'ouverture
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: WorldGridView BuildingCell interactif + BuildingShopSheet + BuildingDetailSheet</name>
  <files>
    components/mascot/WorldGridView.tsx,
    components/mascot/BuildingShopSheet.tsx,
    components/mascot/BuildingDetailSheet.tsx
  </files>
  <read_first>
    components/mascot/WorldGridView.tsx,
    components/mascot/TreeShop.tsx,
    components/mascot/HarvestBurst.tsx,
    lib/mascot/building-engine.ts,
    lib/mascot/building-sprites.ts,
    lib/mascot/types.ts,
    lib/mascot/world-grid.ts,
    constants/spacing.ts,
    constants/typography.ts
  </read_first>
  <action>
**1. Refactorer WorldGridView.tsx — BuildingCell interactif:**

Modifier les imports en haut:
```typescript
import { BUILDING_SPRITES } from '../../lib/mascot/building-sprites';
import { getPendingResources } from '../../lib/mascot/building-engine';
import type { PlacedBuilding, FarmInventory } from '../../lib/mascot/types';
```

Supprimer l'ancien `BUILDING_SPRITES` en dur (lignes 29-32).

Modifier `WorldGridViewProps`:
```typescript
interface WorldGridViewProps {
  treeStage: TreeStage;
  farmCropsCSV: string;
  ownedBuildings: PlacedBuilding[];
  containerWidth: number;
  containerHeight: number;
  onCropPlotPress?: (cellId: string, crop: PlantedCrop | null) => void;
  onBuildingCellPress?: (cellId: string, building: PlacedBuilding | null) => void;
}
```

Refactorer `BuildingCell` completement:
```typescript
function BuildingCell({ cell, placedBuilding, pendingCount, containerWidth, containerHeight, onPress }: {
  cell: WorldCell;
  placedBuilding: PlacedBuilding | null;
  pendingCount: number;
  containerWidth: number;
  containerHeight: number;
  onPress: () => void;
}) {
  const pulse = useSharedValue(1);
  const reducedMotion = useReducedMotion();

  useEffect(() => {
    if (!reducedMotion && pendingCount > 0) {
      pulse.value = withRepeat(
        withSequence(
          withTiming(1.08, { duration: 800, easing: Easing.inOut(Easing.sin) }),
          withTiming(1, { duration: 800, easing: Easing.inOut(Easing.sin) }),
        ),
        -1, true,
      );
    } else {
      pulse.value = 1;
    }
  }, [reducedMotion, pendingCount > 0]);

  const animStyle = useAnimatedStyle(() => ({
    transform: [{ scale: pulse.value }],
  }));

  const size = CELL_SIZES[cell.size];
  const left = cell.x * containerWidth - size / 2;
  const top = cell.y * containerHeight - size / 2;

  return (
    <Animated.View style={[{ position: 'absolute', left, top, width: size, height: size }, animStyle]}>
      <TouchableOpacity onPress={onPress} activeOpacity={0.7} style={styles.buildingCell}>
        {placedBuilding ? (
          <>
            <Image
              source={BUILDING_SPRITES[placedBuilding.buildingId]?.[placedBuilding.level]}
              style={styles.buildingSprite}
            />
            {pendingCount > 0 && (
              <View style={styles.pendingBadge}>
                <Text style={styles.pendingBadgeText}>{pendingCount}</Text>
              </View>
            )}
          </>
        ) : (
          <Text style={styles.emptyBuildingPlus}>🏗️</Text>
        )}
      </TouchableOpacity>
    </Animated.View>
  );
}
```

Mettre a jour le rendu des buildings dans `WorldGridView`:
```typescript
{BUILDING_CELLS.map(cell => {
  const placedBuilding = ownedBuildings.find(b => b.cellId === cell.id) ?? null;
  const pendingCount = placedBuilding ? getPendingResources(placedBuilding) : 0;
  return (
    <BuildingCell
      key={cell.id}
      cell={cell}
      placedBuilding={placedBuilding}
      pendingCount={pendingCount}
      containerWidth={containerWidth}
      containerHeight={containerHeight}
      onPress={() => onBuildingCellPress?.(cell.id, placedBuilding)}
    />
  );
})}
```

Ajouter les styles manquants:
```typescript
pendingBadge: {
  position: 'absolute',
  top: -4,
  right: -4,
  backgroundColor: '#EF4444',
  borderRadius: 10,
  minWidth: 20,
  height: 20,
  justifyContent: 'center',
  alignItems: 'center',
  paddingHorizontal: 4,
},
pendingBadgeText: {
  color: '#FFFFFF',
  fontSize: 11,
  fontWeight: 'bold',
},
emptyBuildingPlus: {
  fontSize: 24,
  textAlign: 'center',
},
```

**2. Creer `components/mascot/BuildingShopSheet.tsx`:**

Bottom sheet modal pour construire un batiment sur une cellule vide. Presentation `pageSheet` + drag-to-dismiss.

Props:
```typescript
interface BuildingShopSheetProps {
  visible: boolean;
  cellId: string;
  treeStage: TreeStage;
  coins: number;
  ownedBuildings: PlacedBuilding[];
  onBuild: (buildingId: string) => void;
  onClose: () => void;
}
```

Contenu:
- Header avec titre "Construire" et bouton fermer
- Liste des batiments du BUILDING_CATALOG filtres par:
  - `minTreeStage` <= treeStage actuel (utiliser getStageIndex pour comparer)
  - Pas deja construit (ownedBuildings n'a pas ce buildingId)
- Pour chaque batiment afficher:
  - Sprite niveau 1 (BUILDING_SPRITES[id][1])
  - Nom (t(labelKey))
  - Ressource produite (t(`farm.resource.${resourceType}`))
  - Frequence (t('farm.building.frequency', { hours: tiers[0].productionRateHours }))
  - Cout (t('farm.building.cost', { cost }))
  - Bouton "Construire" (disabled si coins < cost, style primary/tint)
- Couleurs via `useThemeColors()` — JAMAIS de couleurs hardcodees pour les textes/backgrounds structurels
- Si aucun batiment disponible: message "Aucun batiment disponible" ou "Debloquer au stade X"

Utiliser `getStageIndex` de `lib/mascot/engine` pour comparer les stades.

**3. Creer `components/mascot/BuildingDetailSheet.tsx`:**

Bottom sheet modal pour un batiment place. Presentation `pageSheet` + drag-to-dismiss.

Props:
```typescript
interface BuildingDetailSheetProps {
  visible: boolean;
  building: PlacedBuilding;
  coins: number;
  onCollect: () => void;
  onUpgrade: () => void;
  onClose: () => void;
}
```

Contenu:
- Header avec nom du batiment + emoji + niveau actuel
- Sprite actuel (BUILDING_SPRITES[buildingId][level])
- Section production:
  - Ressource: t(`farm.resource.${def.resourceType}`)
  - Frequence: t('farm.building.frequency', { hours: tier.productionRateHours })
- Section collecte:
  - Ressources en attente (getPendingResources)
  - Bouton "Collecter X {ressource}" (disabled si pending === 0)
  - Style: bouton vert/accent quand des ressources sont pretes
  - Haptics: `Haptics.impactAsync(Haptics.ImpactFeedbackStyle.Medium)` a la collecte
- Section amelioration:
  - Si canUpgrade(building):
    - Prochain niveau + frequence amelioree
    - Cout: getUpgradeCost(building) feuilles
    - Bouton "Ameliorer au Niv X" (disabled si coins < cout)
  - Sinon: "Niveau max" avec badge dore
- Couleurs via `useThemeColors()` pour backgrounds, textes, bordures
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep "onBuildingCellPress" components/mascot/WorldGridView.tsx retourne au moins 2 resultats (props + usage)
    - grep "PlacedBuilding" components/mascot/WorldGridView.tsx retourne au moins 1 resultat
    - grep "getPendingResources" components/mascot/WorldGridView.tsx retourne au moins 1 resultat
    - grep "BUILDING_SPRITES" components/mascot/WorldGridView.tsx retourne au moins 1 resultat (import from building-sprites)
    - grep "pendingBadge" components/mascot/WorldGridView.tsx retourne au moins 1 resultat
    - grep "BuildingShopSheet" components/mascot/BuildingShopSheet.tsx retourne au moins 1 resultat (export)
    - grep "onBuild" components/mascot/BuildingShopSheet.tsx retourne au moins 1 resultat
    - grep "useThemeColors" components/mascot/BuildingShopSheet.tsx retourne au moins 1 resultat
    - grep "BUILDING_CATALOG" components/mascot/BuildingShopSheet.tsx retourne au moins 1 resultat
    - grep "BuildingDetailSheet" components/mascot/BuildingDetailSheet.tsx retourne au moins 1 resultat (export)
    - grep "onCollect" components/mascot/BuildingDetailSheet.tsx retourne au moins 1 resultat
    - grep "onUpgrade" components/mascot/BuildingDetailSheet.tsx retourne au moins 1 resultat
    - grep "getPendingResources" components/mascot/BuildingDetailSheet.tsx retourne au moins 1 resultat
    - grep "useThemeColors" components/mascot/BuildingDetailSheet.tsx retourne au moins 1 resultat
    - grep "Haptics" components/mascot/BuildingDetailSheet.tsx retourne au moins 1 resultat
  </acceptance_criteria>
  <done>
    - BuildingCell dans WorldGridView est interactif (tap, badge pending, sprite par niveau, pulse animation)
    - BuildingShopSheet affiche les batiments constructibles filtres par treeStage et coins
    - BuildingDetailSheet affiche le detail, permet la collecte et l'amelioration
    - Tous les textes utilisent les cles i18n ajoutees en Plan 01
    - Couleurs via useThemeColors() — aucune couleur hardcodee structurelle
    - Haptics sur la collecte
  </done>
</task>

<task type="auto">
  <name>Task 2: Integration tree.tsx — bottom sheets + collecte passive + toast</name>
  <files>
    app/(tabs)/tree.tsx
  </files>
  <read_first>
    app/(tabs)/tree.tsx,
    components/mascot/WorldGridView.tsx,
    components/mascot/BuildingShopSheet.tsx,
    components/mascot/BuildingDetailSheet.tsx,
    hooks/useFarm.ts,
    lib/mascot/types.ts,
    lib/mascot/building-engine.ts
  </read_first>
  <action>
**1. Ajouter les imports dans tree.tsx:**

```typescript
import { BuildingShopSheet } from '../../components/mascot/BuildingShopSheet';
import { BuildingDetailSheet } from '../../components/mascot/BuildingDetailSheet';
import type { PlacedBuilding, FarmInventory } from '../../lib/mascot/types';
import { BUILDING_CATALOG } from '../../lib/mascot/types';
import { getPendingResources } from '../../lib/mascot/building-engine';
```

**2. Mettre a jour le destructuring de useFarm:**

Changer:
```typescript
const { plant, harvest, buyBuilding, collectPassiveIncome } = useFarm();
```
En:
```typescript
const { plant, harvest, buyBuilding, upgradeBuildingAction, collectBuildingResources, collectPassiveIncome } = useFarm();
```

**3. Ajouter les state pour les bottom sheets batiments:**

Apres les states `showSeedPicker` et `selectedPlotIndex` (~ligne 165-168):
```typescript
const [showBuildingShop, setShowBuildingShop] = useState(false);
const [showBuildingDetail, setShowBuildingDetail] = useState(false);
const [selectedBuildingCellId, setSelectedBuildingCellId] = useState<string | null>(null);
const [selectedBuilding, setSelectedBuilding] = useState<PlacedBuilding | null>(null);
```

**4. Ajouter le handler onBuildingCellPress:**

```typescript
const handleBuildingCellPress = useCallback((cellId: string, building: PlacedBuilding | null) => {
  if (building) {
    setSelectedBuilding(building);
    setSelectedBuildingCellId(cellId);
    setShowBuildingDetail(true);
  } else {
    setSelectedBuildingCellId(cellId);
    setShowBuildingShop(true);
  }
}, []);
```

**5. Ajouter les handlers de construction, collecte, amelioration:**

```typescript
const handleBuildBuilding = useCallback(async (buildingId: string) => {
  if (!profile?.id || !selectedBuildingCellId) return;
  try {
    await buyBuilding(profile.id, buildingId, selectedBuildingCellId);
    const def = BUILDING_CATALOG.find(b => b.id === buildingId);
    showToast(`🏗️ ${def?.emoji ?? ''} Construit !`);
    setShowBuildingShop(false);
  } catch (e: any) {
    showToast(`❌ ${e.message}`);
  }
}, [profile?.id, selectedBuildingCellId, buyBuilding, showToast]);

const handleCollectBuilding = useCallback(async () => {
  if (!profile?.id || !selectedBuildingCellId) return;
  const collected = await collectBuildingResources(profile.id, selectedBuildingCellId);
  if (collected > 0) {
    const def = BUILDING_CATALOG.find(b => b.id === selectedBuilding?.buildingId);
    const resourceEmoji = def?.resourceType === 'oeuf' ? '🥚' : def?.resourceType === 'lait' ? '🥛' : '🌾';
    showToast(`${resourceEmoji} +${collected} ${def?.resourceType ?? ''}`);
    // Re-fetch le building a jour
    const updatedProfile = profiles?.find(p => p.id === profile.id);
    const updatedBuilding = updatedProfile?.farmBuildings?.find((b: PlacedBuilding) => b.cellId === selectedBuildingCellId) ?? null;
    setSelectedBuilding(updatedBuilding);
  }
}, [profile?.id, selectedBuildingCellId, selectedBuilding, collectBuildingResources, profiles, showToast]);

const handleUpgradeBuilding = useCallback(async () => {
  if (!profile?.id || !selectedBuildingCellId) return;
  try {
    await upgradeBuildingAction(profile.id, selectedBuildingCellId);
    const updatedProfile = profiles?.find(p => p.id === profile.id);
    const updatedBuilding = updatedProfile?.farmBuildings?.find((b: PlacedBuilding) => b.cellId === selectedBuildingCellId) ?? null;
    if (updatedBuilding) {
      setSelectedBuilding(updatedBuilding);
      showToast(`⬆️ Ameliore au Niv ${updatedBuilding.level} !`);
    }
  } catch (e: any) {
    showToast(`❌ ${e.message}`);
  }
}, [profile?.id, selectedBuildingCellId, upgradeBuildingAction, profiles, showToast]);
```

**6. Mettre a jour le useEffect collectPassiveIncome (~ligne 182-189):**

Changer le toast pour afficher les ressources au lieu des feuilles:
```typescript
useEffect(() => {
  if (!profile?.id) return;
  collectPassiveIncome(profile.id).then(totalCollected => {
    if (totalCollected > 0) {
      showToast(`🏠 +${totalCollected} ressources collectees`);
    }
  });
}, [profile?.id]);
```

**7. Mettre a jour le `<WorldGridView>` dans le JSX:**

Trouver le `<WorldGridView` existant et ajouter les props manquantes:
```typescript
<WorldGridView
  treeStage={stageInfo.stage}
  farmCropsCSV={profile?.farmCrops ?? ''}
  ownedBuildings={profile?.farmBuildings ?? []}
  containerWidth={...}
  containerHeight={...}
  onCropPlotPress={...}  // existant
  onBuildingCellPress={handleBuildingCellPress}
/>
```

Note: `ownedBuildings` passe maintenant `PlacedBuilding[]` au lieu de `string[]` (migration automatique par le parser).

**8. Ajouter les modals en fin de JSX (avant le `</View>` ou `</ScrollView>` de fermeture):**

```typescript
{/* Bottom sheet construction batiment */}
<BuildingShopSheet
  visible={showBuildingShop}
  cellId={selectedBuildingCellId ?? ''}
  treeStage={stageInfo.stage}
  coins={profile?.coins ?? 0}
  ownedBuildings={profile?.farmBuildings ?? []}
  onBuild={handleBuildBuilding}
  onClose={() => setShowBuildingShop(false)}
/>

{/* Bottom sheet detail batiment */}
{selectedBuilding && (
  <BuildingDetailSheet
    visible={showBuildingDetail}
    building={selectedBuilding}
    coins={profile?.coins ?? 0}
    onCollect={handleCollectBuilding}
    onUpgrade={handleUpgradeBuilding}
    onClose={() => { setShowBuildingDetail(false); setSelectedBuilding(null); }}
  />
)}
```
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep "BuildingShopSheet" "app/(tabs)/tree.tsx" retourne au moins 2 resultats (import + JSX)
    - grep "BuildingDetailSheet" "app/(tabs)/tree.tsx" retourne au moins 2 resultats (import + JSX)
    - grep "upgradeBuildingAction" "app/(tabs)/tree.tsx" retourne au moins 1 resultat
    - grep "collectBuildingResources" "app/(tabs)/tree.tsx" retourne au moins 1 resultat
    - grep "handleBuildingCellPress" "app/(tabs)/tree.tsx" retourne au moins 2 resultats (definition + usage)
    - grep "handleCollectBuilding" "app/(tabs)/tree.tsx" retourne au moins 2 resultats
    - grep "handleUpgradeBuilding" "app/(tabs)/tree.tsx" retourne au moins 2 resultats
    - grep "onBuildingCellPress" "app/(tabs)/tree.tsx" retourne au moins 1 resultat
    - grep "showBuildingShop" "app/(tabs)/tree.tsx" retourne au moins 2 resultats
    - grep "showBuildingDetail" "app/(tabs)/tree.tsx" retourne au moins 2 resultats
  </acceptance_criteria>
  <done>
    - tree.tsx integre les deux bottom sheets (construction + detail)
    - Tap cellule vide → BuildingShopSheet
    - Tap batiment place → BuildingDetailSheet avec collecte et amelioration
    - Toasts informatifs sur construction, collecte, amelioration, erreurs
    - collectPassiveIncome au lancement collecte les ressources accumulees
    - WorldGridView recoit les nouvelles props (PlacedBuilding[], onBuildingCellPress)
    - npx tsc --noEmit passe sans nouvelles erreurs
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verification visuelle batiments sur device</name>
  <files>app/(tabs)/tree.tsx</files>
  <action>
    Verification humaine du systeme complet de batiments productifs.
    Ce qui a ete construit : construction, production passive, collecte, amelioration 3 niveaux, persistance vault.
    Etapes de verification :
    1. Ouvrir l'app sur device/simulateur, naviguer vers l'ecran Arbre/Ferme
    2. Verifier que les cellules batiment existantes (b0, b1) affichent les batiments deja construits avec sprite
    3. Si aucun batiment construit: taper sur une cellule batiment vide - bottom sheet doit s'ouvrir avec la liste des batiments constructibles
    4. Construire un poulailler - verifier que le sprite apparait et les feuilles sont deduites
    5. Fermer et rouvrir l'app - verifier que le batiment est toujours la (persistance)
    6. Attendre ou avancer l'heure - taper sur le batiment - verifier le badge pending et la collecte
    7. Collecter les ressources - verifier le toast et que le badge disparait
    8. Ameliorer le batiment - verifier que le sprite change et que les feuilles sont deduites
    9. Verifier le mode nuit : aucune couleur structurelle hardcodee visible
  </action>
  <verify>Verification humaine — l'utilisateur approuve visuellement</verify>
  <done>L'utilisateur a approuve le fonctionnement des batiments sur device</done>
</task>

</tasks>

<verification>
```bash
# Compilation
npx tsc --noEmit 2>&1 | head -30

# Verifier les imports critiques
grep -r "building-engine" components/ app/ hooks/ --include="*.ts" --include="*.tsx" | head -10
grep -r "building-sprites" components/ --include="*.tsx" | head -5
grep -r "BuildingShopSheet\|BuildingDetailSheet" app/ --include="*.tsx" | head -5

# Verifier que l'ancien BUILDING_SPRITES inline est supprime de WorldGridView
grep "require.*buildings/poulailler" components/mascot/WorldGridView.tsx
# Attendu: 0 resultats (sprite mapping deplace dans building-sprites.ts)
```
</verification>

<success_criteria>
- L'utilisateur peut construire un batiment via tap cellule vide → bottom sheet → bouton construire
- Les batiments affichent un badge pending quand des ressources sont pretes (pulse animation)
- La collecte met a jour l'inventaire et affiche un toast
- L'amelioration fonctionne et deduit les feuilles
- Toutes les donnees persistent dans famille.md via le vault
- Le code compile sans erreur (tsc)
- Verification visuelle approuvee par l'utilisateur
</success_criteria>

<output>
After completion, create `.planning/phases/06-batiments-productifs/06-02-SUMMARY.md`
</output>
