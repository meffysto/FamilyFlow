---
phase: 10-compagnon-mascotte
plan: 02
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - lib/parser.ts
  - hooks/useVault.ts
  - hooks/useGamification.ts
  - lib/gamification/rewards.ts
autonomous: true
requirements: [COMP-05, COMP-06]

must_haves:
  truths:
    - "parseFamille() lit le champ companion: et retourne CompanionData sur le Profile"
    - "Un profil existant sans champ companion retourne companion: null sans crash"
    - "setCompanion() persiste les donnees compagnon dans famille.md"
    - "unlockCompanion() ajoute une espece a unlockedSpecies"
    - "Le bonus +5% XP est applique quand le profil a un compagnon actif"
    - "Les lootbox rare/epique peuvent dropper des compagnons"
  artifacts:
    - path: "lib/parser.ts"
      provides: "parseCompanion/serializeCompanion + integration parseFamille"
      contains: "parseCompanion"
    - path: "hooks/useVault.ts"
      provides: "setCompanion() et unlockCompanion() exposes"
      contains: "setCompanion"
    - path: "hooks/useGamification.ts"
      provides: "Bonus XP compagnon integre dans addPoints"
      contains: "companionBonus"
    - path: "lib/gamification/rewards.ts"
      provides: "Entrees compagnon dans les pools lootbox"
      contains: "companion"
  key_links:
    - from: "lib/parser.ts"
      to: "lib/mascot/companion-types.ts"
      via: "import parseCompanion types"
      pattern: "import.*CompanionData.*companion-types"
    - from: "hooks/useVault.ts"
      to: "lib/parser.ts"
      via: "parseCompanion dans parseFamille"
      pattern: "parseCompanion"
    - from: "hooks/useGamification.ts"
      to: "lib/mascot/companion-engine.ts"
      via: "getCompanionXpBonus()"
      pattern: "getCompanionXpBonus"
---

<objective>
Integrer le compagnon dans la couche donnees : parser famille.md, hooks useVault/useGamification, et lootbox rewards.

Purpose: Le compagnon doit etre persiste dans le vault, expose dans l'etat global, et droppable via lootbox. Sans cette couche donnees, aucune UI ne peut fonctionner.
Output: Parser companion, hooks mis a jour, rewards mis a jour
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-compagnon-mascotte/10-CONTEXT.md
@.planning/phases/10-compagnon-mascotte/10-RESEARCH.md
@.planning/phases/10-compagnon-mascotte/10-01-SUMMARY.md

@lib/parser.ts
@hooks/useVault.ts
@hooks/useGamification.ts
@lib/gamification/rewards.ts
@lib/mascot/companion-types.ts
@lib/mascot/companion-engine.ts

<interfaces>
<!-- From Plan 01 outputs -->

From lib/mascot/companion-types.ts (created in Plan 01):
```typescript
export type CompanionSpecies = 'chat' | 'chien' | 'lapin' | 'renard' | 'herisson';
export type CompanionMood = 'content' | 'endormi' | 'excite' | 'triste';
export interface CompanionData {
  activeSpecies: CompanionSpecies;
  name: string;
  unlockedSpecies: CompanionSpecies[];
  mood: CompanionMood;
}
```

From lib/mascot/companion-engine.ts (created in Plan 01):
```typescript
export function getCompanionXpBonus(companion: CompanionData | null | undefined): number;
```

From lib/types.ts (existing + modified in Plan 01):
```typescript
export interface Profile {
  // ... existing fields ...
  companion?: CompanionData | null;
  // ...
}
export type RewardType = /* ... */ | 'companion';
```

From lib/types.ts (existing):
```typescript
export interface RewardDefinition {
  emoji: string;
  reward: string;
  bonusPoints: number;
  requiresParent?: boolean;
  multiplier?: number;
  multiplierTasks?: number;
  rewardType: RewardType;
  mascotItemId?: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Parser companion + integration parseFamille</name>
  <files>lib/parser.ts</files>
  <read_first>lib/parser.ts, lib/mascot/companion-types.ts, lib/types.ts, hooks/useFarm.ts</read_first>
  <action>
    Ajouter dans `lib/parser.ts` deux fonctions :

    ```typescript
    export function parseCompanion(raw: string | undefined): CompanionData | null {
      if (!raw || !raw.trim()) return null;
      const parts = raw.trim().split(':');
      if (parts.length < 2) return null;
      const activeSpecies = parts[0] as CompanionSpecies;
      const name = parts[1] || '';
      const unlockedRaw = parts[2] || activeSpecies;
      const mood = (parts[3] as CompanionMood) || 'content';
      return {
        activeSpecies,
        name,
        unlockedSpecies: unlockedRaw.split('|') as CompanionSpecies[],
        mood,
      };
    }

    export function serializeCompanion(data: CompanionData): string {
      const unlocked = data.unlockedSpecies.join('|');
      return `${data.activeSpecies}:${data.name}:${unlocked}:${data.mood}`;
    }
    ```

    Ajouter l'import en haut de parser.ts :
    ```typescript
    import type { CompanionData, CompanionSpecies, CompanionMood } from './mascot/companion-types';
    ```

    Integrer dans `parseFamille()` : dans la boucle de parsing des proprietes de profil, ajouter la detection de `companion:` — quand la ligne match `companion:`, appeler `parseCompanion(value)` et assigner au profil. Pattern identique a la lecture de `farmCrops:` ou `treeSpecies:` dans la meme fonction.

    Chercher dans parseFamille() le switch/if qui traite les proprietes de profil (ex: `if (key === 'name')`, `if (key === 'role')`, etc.) et ajouter :
    ```typescript
    case 'companion':
      profile.companion = parseCompanion(value);
      break;
    ```
    Ou si c'est un if/else chain :
    ```typescript
    } else if (key === 'companion') {
      profile.companion = parseCompanion(value);
    }
    ```

    Backward compatibility : si le champ `companion` n'existe pas dans le fichier, `profile.companion` reste `undefined` (pas null) — c'est le comportement par defaut de TypeScript quand la propriete n'est pas assignee. Les composants doivent tester `profile.companion != null` (pas `!== undefined`).

    NE PAS modifier serializeFamille() ici — la serialisation du compagnon passe par writeCompanion() dans useVault (Task 2), meme pattern que writeFarmCrops() dans useFarm.ts.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -c "error" || echo "0 errors"</automated>
  </verify>
  <acceptance_criteria>
    - lib/parser.ts exporte `parseCompanion` et `serializeCompanion`
    - `grep "parseCompanion" lib/parser.ts` retourne au moins 3 lignes (definition + usage dans parseFamille)
    - `grep "serializeCompanion" lib/parser.ts` retourne au moins 1 ligne
    - `grep "companion-types" lib/parser.ts` retourne 1 ligne (import)
    - parseFamille integre la lecture du champ `companion:`
    - `npx tsc --noEmit` ne produit pas de nouvelles erreurs
  </acceptance_criteria>
  <done>parseCompanion/serializeCompanion existent et parseFamille() lit le champ companion des profils</done>
</task>

<task type="auto">
  <name>Task 2: Hooks useVault + useGamification + rewards lootbox</name>
  <files>hooks/useVault.ts, hooks/useGamification.ts, lib/gamification/rewards.ts</files>
  <read_first>hooks/useVault.ts, hooks/useGamification.ts, lib/gamification/rewards.ts, hooks/useFarm.ts, lib/mascot/companion-types.ts, lib/mascot/companion-engine.ts, lib/parser.ts</read_first>
  <action>
    **hooks/useVault.ts** — Ajouter deux fonctions au hook useVaultInternal() :

    1. `setCompanion(profileId: string, companion: CompanionData): Promise<void>` — ecrit le champ companion dans famille.md pour le profil donne. Pattern IDENTIQUE a `writeFarmCrops()` dans useFarm.ts :
       - Lire famille.md via vault.readFile
       - Parcourir les lignes pour trouver la section `### {profileId}`
       - Chercher la ligne `companion:` dans cette section
       - Si trouvee, remplacer la valeur : `lines[fieldLine] = 'companion: ' + serializeCompanion(companion)`
       - Si non trouvee, inserer apres la derniere propriete de la section : `lines.splice(lastPropIdx + 1, 0, 'companion: ' + serializeCompanion(companion))`
       - Ecrire le fichier, puis appeler refresh() pour mettre a jour l'etat
       - Import `serializeCompanion` from `../lib/parser`

    2. `unlockCompanion(profileId: string, speciesId: CompanionSpecies): Promise<void>` — ajoute une espece a unlockedSpecies du profil :
       - Lire le profil actuel
       - Si companion est null (pas encore de compagnon), ne rien faire (l'utilisateur doit d'abord choisir son compagnon initial)
       - Si l'espece est deja dans unlockedSpecies, ne rien faire
       - Sinon, ajouter l'espece a unlockedSpecies et appeler setCompanion()

    Exposer ces deux fonctions dans le retour du hook et dans le contexte VaultContext.

    **hooks/useGamification.ts** — Integrer le bonus XP compagnon :
    - Importer `getCompanionXpBonus` from `../lib/mascot/companion-engine`
    - Dans la fonction `addPoints()` (ou equivalent qui calcule les points XP a attribuer), appliquer le multiplicateur compagnon :
      - Trouver l'endroit ou les points sont calcules avant d'etre persistes
      - Multiplier par `getCompanionXpBonus(activeProfile?.companion)` — retourne 1.05 si compagnon actif, 1.0 sinon
      - S'assurer que le multiplicateur compagnon se cumule avec le multiplicateur existant (multiplier de lootbox) : `finalPoints = Math.round(basePoints * profile.multiplier * getCompanionXpBonus(profile.companion))`
      - Ajouter un commentaire `// Bonus compagnon +5% XP (per D-09)` pour traçabilite

    **lib/gamification/rewards.ts** — Ajouter des compagnons dans les pools lootbox :
    - Dans le pool `rare`, ajouter :
      ```typescript
      { emoji: '🐱', reward: 'Compagnon Chat !', bonusPoints: 0, rewardType: 'companion', mascotItemId: 'chat' },
      { emoji: '🐶', reward: 'Compagnon Chien !', bonusPoints: 0, rewardType: 'companion', mascotItemId: 'chien' },
      { emoji: '🐰', reward: 'Compagnon Lapin !', bonusPoints: 0, rewardType: 'companion', mascotItemId: 'lapin' },
      ```
    - Dans le pool `epique`, ajouter :
      ```typescript
      { emoji: '🦊', reward: 'Compagnon Renard !', bonusPoints: 0, rewardType: 'companion', mascotItemId: 'renard' },
      { emoji: '🦔', reward: 'Compagnon Herisson !', bonusPoints: 0, rewardType: 'companion', mascotItemId: 'herisson' },
      ```
    - Note: mascotItemId correspond a CompanionSpecies — le code qui traite l'ouverture de lootbox (dans useGamification) devra appeler unlockCompanion() quand rewardType === 'companion'. Ajouter ce traitement ici :
      - Dans la fonction qui traite les resultats de lootbox (chercher le switch/if sur rewardType dans useGamification.ts), ajouter :
        ```typescript
        case 'companion':
          if (result.mascotItemId) {
            await unlockCompanion(activeProfileId, result.mascotItemId as CompanionSpecies);
          }
          break;
        ```
      - Note: unlockCompanion est expose par useVault, donc useGamification doit y acceder — verifier comment les autres rewardType (mascot_deco, mascot_hab, farm_seed) appellent les fonctions useVault. Suivre le meme pattern.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -c "error" || echo "0 errors"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "setCompanion" hooks/useVault.ts` retourne au moins 3 lignes (definition + expose)
    - `grep "unlockCompanion" hooks/useVault.ts` retourne au moins 3 lignes
    - `grep "getCompanionXpBonus" hooks/useGamification.ts` retourne au moins 1 ligne
    - `grep "companion" lib/gamification/rewards.ts` retourne au moins 5 lignes (5 entrees lootbox)
    - `grep "rewardType.*companion" lib/gamification/rewards.ts` retourne au moins 2 lignes
    - Le traitement de rewardType 'companion' existe dans le handler de lootbox
    - `npx tsc --noEmit` ne produit pas de nouvelles erreurs
  </acceptance_criteria>
  <done>Le compagnon est persiste dans famille.md via setCompanion(), le bonus XP +5% est applique, et les lootbox rare/epique peuvent dropper des compagnons</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passe sans nouvelles erreurs
- `grep -r "parseCompanion\|serializeCompanion\|setCompanion\|unlockCompanion" lib/ hooks/ | wc -l` retourne >= 10
- `grep "companion" lib/gamification/rewards.ts | wc -l` >= 5
</verification>

<success_criteria>
La couche donnees du compagnon est complete : parsing bidirectionnel famille.md, hooks d'etat exposes, bonus XP integre, compagnons dans les lootbox. L'UI (Plans 03 et 04) peut maintenant s'appuyer sur ces fondations.
</success_criteria>

<output>
After completion, create `.planning/phases/10-compagnon-mascotte/10-02-SUMMARY.md`
</output>
