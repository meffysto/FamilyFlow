---
phase: 10-compagnon-mascotte
plan: 03
type: execute
wave: 3
depends_on: [10-01, 10-02]
files_modified:
  - components/mascot/CompanionSlot.tsx
  - components/mascot/CompanionPicker.tsx
  - components/mascot/TreeView.tsx
  - app/(tabs)/tree.tsx
autonomous: false
requirements: [COMP-01, COMP-02, COMP-03, COMP-08]

must_haves:
  truths:
    - "Le compagnon apparait visuellement sur l'ecran arbre dans un slot dedie distinct des inhabitants"
    - "Le compagnon affiche le sprite correspondant a son espece et stade d'evolution"
    - "Taper sur le compagnon declenche une animation de saut + haptic feedback"
    - "Le modal CompanionPicker s'ouvre quand le profil atteint level 5 sans compagnon"
    - "L'utilisateur peut nommer son compagnon dans le picker"
    - "Le compagnon affiche des bulles d'emotion reactives aux evenements"
  artifacts:
    - path: "components/mascot/CompanionSlot.tsx"
      provides: "Composant anime du compagnon dans la scene arbre"
      exports: ["CompanionSlot"]
      min_lines: 80
    - path: "components/mascot/CompanionPicker.tsx"
      provides: "Modal pageSheet de choix/switch compagnon"
      exports: ["CompanionPicker"]
      min_lines: 100
    - path: "components/mascot/TreeView.tsx"
      provides: "Integration du CompanionSlot dans la scene"
      contains: "CompanionSlot"
    - path: "app/(tabs)/tree.tsx"
      provides: "Orchestration CompanionPicker + passage companion a TreeView"
      contains: "CompanionPicker"
  key_links:
    - from: "components/mascot/CompanionSlot.tsx"
      to: "lib/mascot/companion-types.ts"
      via: "import types"
      pattern: "import.*companion-types"
    - from: "components/mascot/TreeView.tsx"
      to: "components/mascot/CompanionSlot.tsx"
      via: "rendu CompanionSlot dans overlay"
      pattern: "CompanionSlot"
    - from: "app/(tabs)/tree.tsx"
      to: "components/mascot/CompanionPicker.tsx"
      via: "modal CompanionPicker"
      pattern: "CompanionPicker"
    - from: "app/(tabs)/tree.tsx"
      to: "hooks/useVault.ts"
      via: "setCompanion appele depuis picker"
      pattern: "setCompanion"
---

<objective>
Creer les composants visuels du compagnon : CompanionSlot (rendu anime dans la scene), CompanionPicker (choix initial/switch), et les integrer dans TreeView + tree.tsx.

Purpose: C'est le coeur de l'experience utilisateur — le compagnon doit etre visible, interactif et attachant sur l'ecran arbre.
Output: CompanionSlot.tsx, CompanionPicker.tsx, TreeView modifie, tree.tsx modifie
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-compagnon-mascotte/10-CONTEXT.md
@.planning/phases/10-compagnon-mascotte/10-RESEARCH.md
@.planning/phases/10-compagnon-mascotte/10-01-SUMMARY.md
@.planning/phases/10-compagnon-mascotte/10-02-SUMMARY.md

@components/mascot/TreeView.tsx
@app/(tabs)/tree.tsx
@lib/mascot/companion-types.ts
@lib/mascot/companion-engine.ts
@lib/types.ts

<interfaces>
<!-- From Plan 01 outputs -->

From lib/mascot/companion-types.ts:
```typescript
export type CompanionSpecies = 'chat' | 'chien' | 'lapin' | 'renard' | 'herisson';
export type CompanionStage = 'bebe' | 'jeune' | 'adulte';
export type CompanionMood = 'content' | 'endormi' | 'excite' | 'triste';
export type CompanionEvent = 'task_completed' | 'loot_opened' | 'level_up' | 'greeting' | 'streak_milestone' | 'harvest' | 'craft';
export interface CompanionData {
  activeSpecies: CompanionSpecies;
  name: string;
  unlockedSpecies: CompanionSpecies[];
  mood: CompanionMood;
}
export interface CompanionSpeciesInfo {
  id: CompanionSpecies;
  nameKey: string;
  descriptionKey: string;
  rarity: 'initial' | 'rare' | 'epique';
}
export const COMPANION_UNLOCK_LEVEL: number;
export const COMPANION_SPECIES_CATALOG: CompanionSpeciesInfo[];
export const COMPANION_STAGES: CompanionStageInfo[];
```

From lib/mascot/companion-engine.ts:
```typescript
export function getCompanionStage(level: number): CompanionStage;
export function getCompanionMood(recentTasksCompleted: number, hoursSinceLastActivity: number): CompanionMood;
export function pickCompanionMessage(event: CompanionEvent, context: CompanionMessageContext): string;
```

From hooks/useVault.ts (modified in Plan 02):
```typescript
// Exposed functions:
setCompanion(profileId: string, companion: CompanionData): Promise<void>;
unlockCompanion(profileId: string, speciesId: CompanionSpecies): Promise<void>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CompanionSlot + CompanionPicker composants</name>
  <files>components/mascot/CompanionSlot.tsx, components/mascot/CompanionPicker.tsx</files>
  <read_first>components/mascot/TreeView.tsx, lib/mascot/companion-types.ts, lib/mascot/companion-engine.ts, app/(tabs)/tree.tsx, constants/spacing.ts, constants/typography.ts</read_first>
  <action>
    **CompanionSlot.tsx** — Composant anime du compagnon dans la scene arbre. Per D-04, D-08, D-10, D-11.

    Props :
    ```typescript
    interface CompanionSlotProps {
      species: CompanionSpecies;
      stage: CompanionStage;
      mood: CompanionMood;
      name: string;
      message?: string | null;  // bulle de message contextuel
      onTap: () => void;
      containerWidth: number;   // largeur du container TreeView pour le positionnement
      containerHeight: number;  // hauteur du container
    }
    ```

    Implementation :
    - Position : `Animated.View` avec `position: 'absolute'`. Calculer x/y depuis le viewbox SVG (200x240) vers le pixel space du container : `x = (cx / 200) * containerWidth`, `y = (cy / 240) * containerHeight`. Position dedie compagnon : `cx: 85, cy: 205` dans le viewbox — hors des HAB_SLOTS existants. Per D-04.
    - Sprites : Charger les idle frames via un mapping statique `COMPANION_SPRITES` :
      ```typescript
      const COMPANION_SPRITES: Record<CompanionSpecies, Record<CompanionStage, { idle_1: any; idle_2: any }>> = {
        chat: {
          bebe: { idle_1: require('@/assets/garden/animals/chat/bebe/idle_1.png'), idle_2: require('@/assets/garden/animals/chat/bebe/idle_2.png') },
          jeune: { idle_1: require('@/assets/garden/animals/chat/jeune/idle_1.png'), ... },
          adulte: { idle_1: require('@/assets/garden/animals/chat/adulte/idle_1.png'), ... },
        },
        // ... les 4 autres especes
      };
      ```
    - Animation idle : `setInterval` de 800ms qui alterne entre idle_1 et idle_2 via `useState(0)`. Meme pattern exactement que AnimatedAnimal dans TreeView.tsx (frame swap).
    - Animation tap (per D-08) : Au tap, `Haptics.impactAsync(ImpactFeedbackStyle.Medium)`. Animer `jumpY` via `withSequence(withSpring(-20, {damping:6, stiffness:400}), withSpring(0, {damping:10, stiffness:180}))` et `scale` via `withSequence(withSpring(1.3, {damping:6, stiffness:300}), withSpring(1.0, {damping:10, stiffness:180}))`. Utiliser `useSharedValue` + `useAnimatedStyle`.
    - Bulle de message (per D-10, D-12) : Si `message` non null, afficher un `Animated.View` au-dessus du compagnon avec le texte. Style : fond blanc arrondi, ombre legere, texte en FontSize.xs. Disparait apres 4 secondes via `withTiming(0, {duration: 500})` sur l'opacite. Pas de `perspective` dans les transforms.
    - Bulle humeur : Quand pas de message, afficher un petit emoji d'humeur au-dessus (content=😊, endormi=😴, excite=🤩, triste=😢). Style discret, plus petit que la bulle message.
    - `Pressable` wrapper pour le tap, PAS de `TouchableOpacity` (utiliser Pressable pour compatibilite Reanimated).
    - Taille du sprite : ~32x32 logical pixels (ajuster selon la taille reelle des sprites Mana Seed — verifier la taille des sprites dans assets/garden/animals/poussin/).
    - `React.memo` sur le composant.
    - Utiliser `useThemeColors()` pour les couleurs de la bulle (colors.surface, colors.text).
    - NE PAS utiliser `perspective` dans les transforms.

    **CompanionPicker.tsx** — Modal pageSheet de choix/switch compagnon. Per D-01, D-02, D-06.

    Props :
    ```typescript
    interface CompanionPickerProps {
      visible: boolean;
      onClose: () => void;
      onSelect: (species: CompanionSpecies, name: string) => void;
      unlockedSpecies: CompanionSpecies[];  // especes deja debloquees
      isInitialChoice: boolean;  // true = premier choix, false = switch
    }
    ```

    Implementation :
    - Modal avec `presentationStyle: 'pageSheet'` via react-native `Modal` composant (ou le pattern utilise dans le projet — verifier dans tree.tsx comment les modals sont presentes).
    - Grille des 5 especes : afficher les 5 especes de COMPANION_SPECIES_CATALOG. Pour chaque espece :
      - Sprite idle_1 du stade bebe (32x32)
      - Nom traduit via `t('companion.species.{id}')`
      - Description traduite via `t('companion.speciesDesc.{id}')`
      - Rarite affichee (initial = pas de badge, rare = badge "Rare", epique = badge "Epique")
      - Si l'espece n'est pas dans unlockedSpecies ET n'est pas 'initial' rarity : afficher en grise avec un cadenas + texte `t('companion.picker.locked')`
      - Les 3 especes initiales (chat, chien, lapin) sont toujours selectionnables
    - Champ de texte pour le nom : `TextInput` avec placeholder `t('companion.picker.namePlaceholder')`. Max 20 caracteres. Le nom est obligatoire — bouton confirmer desactive si nom vide.
    - Bouton confirmer : `t('companion.picker.confirm')` — appelle `onSelect(selectedSpecies, name)` et ferme le modal.
    - Pour le switch (isInitialChoice=false) : afficher les especes deja debloquees en priorite, permettre de changer l'espece active.
    - Utiliser `useThemeColors()` pour toutes les couleurs. Spacing et Typography des constants.
    - `ModalHeader` de `components/ui/` pour l'en-tete du modal (verifier si ce composant existe — sinon header simple avec titre + bouton fermer).
    - Haptics.selectionAsync() au tap sur une espece.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -c "error" || echo "0 errors"</automated>
  </verify>
  <acceptance_criteria>
    - components/mascot/CompanionSlot.tsx exporte `CompanionSlot`
    - `grep "useSharedValue" components/mascot/CompanionSlot.tsx` retourne au moins 2 lignes (jumpY + scale)
    - `grep "Haptics" components/mascot/CompanionSlot.tsx` retourne au moins 1 ligne
    - `grep "useThemeColors" components/mascot/CompanionSlot.tsx` retourne 1 ligne
    - `grep "setInterval" components/mascot/CompanionSlot.tsx` retourne 1 ligne (idle frame swap)
    - components/mascot/CompanionPicker.tsx exporte `CompanionPicker`
    - `grep "TextInput" components/mascot/CompanionPicker.tsx` retourne au moins 1 ligne (champ nom)
    - `grep "onSelect" components/mascot/CompanionPicker.tsx` retourne au moins 2 lignes
    - `npx tsc --noEmit` ne produit pas de nouvelles erreurs
  </acceptance_criteria>
  <done>CompanionSlot affiche le compagnon anime avec tap/haptic/bulle, CompanionPicker permet le choix initial et le switch</done>
</task>

<task type="auto">
  <name>Task 2: Integration TreeView + tree.tsx</name>
  <files>components/mascot/TreeView.tsx, app/(tabs)/tree.tsx</files>
  <read_first>components/mascot/TreeView.tsx, app/(tabs)/tree.tsx, components/mascot/CompanionSlot.tsx, components/mascot/CompanionPicker.tsx, lib/mascot/companion-types.ts, lib/mascot/companion-engine.ts, hooks/useVault.ts</read_first>
  <action>
    **TreeView.tsx** — Ajouter le CompanionSlot dans la scene.

    1. Ajouter des props optionnelles a TreeView :
       ```typescript
       companion?: CompanionData | null;
       companionStage?: CompanionStage;
       companionMood?: CompanionMood;
       companionMessage?: string | null;
       onCompanionTap?: () => void;
       ```
    2. Importer `CompanionSlot` depuis `./CompanionSlot`.
    3. Dans le rendu, apres les inhabitants overlay (chercher ou les AnimatedAnimal sont rendus — ils sont dans un `Animated.View` overlay au-dessus du SVG), ajouter conditionnellement le CompanionSlot :
       ```typescript
       {companion && (
         <CompanionSlot
           species={companion.activeSpecies}
           stage={companionStage || 'bebe'}
           mood={companionMood || 'content'}
           name={companion.name}
           message={companionMessage}
           onTap={onCompanionTap || (() => {})}
           containerWidth={containerWidth}
           containerHeight={containerHeight}
         />
       )}
       ```
    4. Le CompanionSlot doit etre rendu APRES les inhabitants pour apparaitre au premier plan (z-order). Per D-04 : slot dedie, distinct des inhabitants.
    5. Verifier que `containerWidth` et `containerHeight` sont accessibles dans le scope du rendu — ils sont probablement calcules via onLayout. Si pas directement disponibles, utiliser les dimensions existantes du container de la scene.

    **tree.tsx** — Orchestration du compagnon.

    1. Importer `CompanionPicker`, `COMPANION_UNLOCK_LEVEL`, `getCompanionStage`, `getCompanionMood`, `pickCompanionMessage`, types.
    2. State local :
       ```typescript
       const [showPicker, setShowPicker] = useState(false);
       const [companionMessage, setCompanionMessage] = useState<string | null>(null);
       ```
    3. Lire le compagnon depuis le profil actif : `const companion = activeProfile?.companion ?? null;`
    4. Calculer le stade et l'humeur :
       ```typescript
       const companionStage = companion ? getCompanionStage(activeProfile.level) : undefined;
       const companionMood = companion ? getCompanionMood(recentTasksCount, hoursSinceLastActivity) : undefined;
       ```
       Note: `recentTasksCount` = nombre de taches completees aujourd'hui (chercher dans le code existant comment les taches completees sont comptees — probablement via gamiData ou un calcul depuis les taches). `hoursSinceLastActivity` = heures depuis la derniere activite du profil (calculer depuis gamiData.lastActivityDate ou equivalent). Si ces donnees ne sont pas directement disponibles, utiliser des valeurs par defaut sensees (recentTasks=0, hoursSince=0) et ajouter un TODO pour les brancher correctement.
    5. Declenchement du picker (per D-01, pitfall 7) :
       ```typescript
       useEffect(() => {
         if (activeProfile && activeProfile.level >= COMPANION_UNLOCK_LEVEL && !activeProfile.companion) {
           // Verifier que le picker n'a pas ete deja propose (eviter annoyance)
           // Utiliser un ref ou une variable locale pour la session
           setShowPicker(true);
         }
       }, [activeProfile?.id]);
       ```
    6. Handler de selection :
       ```typescript
       const handleCompanionSelect = useCallback(async (species: CompanionSpecies, name: string) => {
         if (!activeProfile) return;
         const newCompanion: CompanionData = {
           activeSpecies: species,
           name,
           unlockedSpecies: [species],
           mood: 'content',
         };
         await setCompanion(activeProfile.id, newCompanion);
         setShowPicker(false);
       }, [activeProfile, setCompanion]);
       ```
    7. Handler tap compagnon (per D-08) :
       ```typescript
       const handleCompanionTap = useCallback(() => {
         if (!companion || !activeProfile) return;
         const context = {
           profileName: activeProfile.name,
           companionName: companion.name,
           companionSpecies: companion.activeSpecies,
           tasksToday: recentTasksCount,
           streak: activeProfile.streak,
           level: activeProfile.level,
         };
         const msgKey = pickCompanionMessage('greeting', context);
         setCompanionMessage(t(msgKey, context));
         // Effacer apres 4 secondes
         setTimeout(() => setCompanionMessage(null), 4000);
       }, [companion, activeProfile, t]);
       ```
    8. Message de bienvenue au focus (per D-12) : Quand l'ecran arbre regagne le focus, afficher un message de greeting. Utiliser `useFocusEffect` ou le pattern existant dans tree.tsx pour les actions au focus :
       ```typescript
       useFocusEffect(useCallback(() => {
         if (companion && activeProfile) {
           const context = { ... };
           const msgKey = pickCompanionMessage('greeting', context);
           setCompanionMessage(t(msgKey, context));
           const timer = setTimeout(() => setCompanionMessage(null), 4000);
           return () => clearTimeout(timer);
         }
       }, [companion?.activeSpecies, activeProfile?.id]));
       ```
    9. Passer les props au TreeView :
       ```typescript
       <TreeView
         {...existingProps}
         companion={companion}
         companionStage={companionStage}
         companionMood={companionMood}
         companionMessage={companionMessage}
         onCompanionTap={handleCompanionTap}
       />
       ```
    10. Rendre le CompanionPicker :
        ```typescript
        <CompanionPicker
          visible={showPicker}
          onClose={() => setShowPicker(false)}
          onSelect={handleCompanionSelect}
          unlockedSpecies={companion?.unlockedSpecies ?? []}
          isInitialChoice={!companion}
        />
        ```
    11. Ajouter un bouton discret pour ouvrir le picker en mode switch (per D-02) — par exemple un petit bouton icone pres du compagnon, ou dans un menu existant de l'ecran arbre. Verifier comment les autres interactions (shop, tech tree) sont declenchees dans tree.tsx et suivre le meme pattern.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -c "error" || echo "0 errors"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "CompanionSlot" components/mascot/TreeView.tsx` retourne au moins 2 lignes (import + rendu)
    - `grep "companion" components/mascot/TreeView.tsx` retourne au moins 5 lignes (props + rendu)
    - `grep "CompanionPicker" "app/(tabs)/tree.tsx"` retourne au moins 2 lignes (import + rendu)
    - `grep "setCompanion" "app/(tabs)/tree.tsx"` retourne au moins 1 ligne
    - `grep "handleCompanionTap" "app/(tabs)/tree.tsx"` retourne au moins 2 lignes
    - `grep "COMPANION_UNLOCK_LEVEL" "app/(tabs)/tree.tsx"` retourne au moins 1 ligne
    - `npx tsc --noEmit` ne produit pas de nouvelles erreurs
  </acceptance_criteria>
  <done>Le compagnon est visible et interactif sur l'ecran arbre, le picker se declenche au niveau 5, le message de bienvenue s'affiche au focus</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verification visuelle du compagnon sur l'ecran arbre</name>
  <files>components/mascot/CompanionSlot.tsx, components/mascot/CompanionPicker.tsx, components/mascot/TreeView.tsx, app/(tabs)/tree.tsx</files>
  <action>Verification humaine du rendu visuel et des interactions compagnon sur device physique. Pas de code a ecrire — verification uniquement.</action>
  <verify>
    <automated>npx tsc --noEmit 2>&amp;1 | grep -c "error" || echo "0 errors"</automated>
  </verify>
  <how-to-verify>
    1. Lancer l'app : `npx expo run:ios --device`
    2. Selectionner un profil de niveau >= 5 (ou modifier temporairement COMPANION_UNLOCK_LEVEL a 1 pour tester)
    3. Le modal CompanionPicker doit s'ouvrir automatiquement si le profil n'a pas de compagnon
    4. Choisir une espece, donner un nom, confirmer
    5. Verifier que le compagnon apparait sur l'ecran arbre, anime (idle frame swap)
    6. Taper sur le compagnon — verifier : animation de saut + haptic + bulle de message
    7. Quitter l'ecran arbre et y revenir — verifier : bulle de bienvenue s'affiche pendant ~4 secondes
    8. Verifier que le compagnon ne chevauche pas les inhabitants existants
    9. Verifier que la position du compagnon est coherente en mode clair et sombre
  </how-to-verify>
  <done>Le compagnon est visuellement correct, les animations fonctionnent, le picker permet le choix initial</done>
  <resume-signal>Type "approved" ou decrivez les problemes constates</resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passe sans nouvelles erreurs
- Le compagnon est visible et anime sur l'ecran arbre
- Le picker fonctionne pour le choix initial et le switch
- Le tap declenche une reaction visuelle + haptic
</verification>

<success_criteria>
Le compagnon est visuellement present et interactif sur l'ecran arbre. L'utilisateur peut choisir son compagnon au niveau 5, le nommer, et interagir avec lui. Les bulles de message et d'humeur fonctionnent.
</success_criteria>

<output>
After completion, create `.planning/phases/10-compagnon-mascotte/10-03-SUMMARY.md`
</output>
