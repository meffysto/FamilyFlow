---
phase: 10-compagnon-mascotte
plan: 04
type: execute
wave: 4
depends_on: [10-02, 10-03]
files_modified:
  - lib/mascot/companion-engine.ts
  - lib/ai-service.ts
  - app/(tabs)/tree.tsx
  - app/(tabs)/_layout.tsx
  - components/mascot/CompanionAvatarMini.tsx
autonomous: false
requirements: [COMP-04, COMP-07, COMP-08]

must_haves:
  truths:
    - "Le compagnon affiche des messages contextuels sur les dernieres actions quand on arrive sur l'ecran arbre"
    - "Les messages IA sont generes via Claude Haiku si la cle API est configuree, sinon fallback predefinies"
    - "Le compagnon sert d'avatar de profil dans la tab bar et le selecteur de profil"
    - "Les profils sans compagnon conservent leur emoji avatar"
    - "Les bulles d'emotion s'affichent sur les evenements app (tache completee, lootbox, level up)"
  artifacts:
    - path: "lib/mascot/companion-engine.ts"
      provides: "generateCompanionAIMessage() pour messages IA optionnels"
      contains: "generateCompanionAIMessage"
    - path: "components/mascot/CompanionAvatarMini.tsx"
      provides: "Composant avatar compagnon pour tab bar / profile picker"
      exports: ["CompanionAvatarMini"]
    - path: "app/(tabs)/_layout.tsx"
      provides: "Avatar compagnon dans profile picker et tab bar"
      contains: "CompanionAvatarMini"
  key_links:
    - from: "lib/mascot/companion-engine.ts"
      to: "lib/ai-service.ts"
      via: "appel Claude Haiku pour messages IA"
      pattern: "ai-service"
    - from: "app/(tabs)/_layout.tsx"
      to: "components/mascot/CompanionAvatarMini.tsx"
      via: "rendu avatar dans profile picker"
      pattern: "CompanionAvatarMini"
    - from: "app/(tabs)/tree.tsx"
      to: "lib/mascot/companion-engine.ts"
      via: "generateCompanionAIMessage au focus"
      pattern: "generateCompanionAIMessage"
---

<objective>
Finaliser le systeme compagnon : messages contextuels hybrides (predefinies + IA), avatar compagnon dans le profil, et bulles d'emotion reactives.

Purpose: Les messages contextuels rendent le compagnon "conscient" des actions, l'avatar dans le profil renforce l'attachement, et les bulles d'emotion completent la boucle emotionnelle.
Output: Messages IA, CompanionAvatarMini, integration _layout.tsx, bulles d'emotion evenements
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-compagnon-mascotte/10-CONTEXT.md
@.planning/phases/10-compagnon-mascotte/10-RESEARCH.md
@.planning/phases/10-compagnon-mascotte/10-01-SUMMARY.md
@.planning/phases/10-compagnon-mascotte/10-02-SUMMARY.md
@.planning/phases/10-compagnon-mascotte/10-03-SUMMARY.md

@lib/mascot/companion-engine.ts
@lib/mascot/companion-types.ts
@lib/ai-service.ts
@contexts/AIContext.tsx
@app/(tabs)/_layout.tsx
@app/(tabs)/tree.tsx
@components/mascot/CompanionSlot.tsx

<interfaces>
<!-- From Plan 01-03 outputs -->

From lib/mascot/companion-types.ts:
```typescript
export type CompanionSpecies = 'chat' | 'chien' | 'lapin' | 'renard' | 'herisson';
export type CompanionStage = 'bebe' | 'jeune' | 'adulte';
export type CompanionEvent = 'task_completed' | 'loot_opened' | 'level_up' | 'greeting' | 'streak_milestone' | 'harvest' | 'craft';
export interface CompanionData { activeSpecies: CompanionSpecies; name: string; unlockedSpecies: CompanionSpecies[]; mood: CompanionMood; }
export interface CompanionMessageContext { profileName: string; companionName: string; companionSpecies: CompanionSpecies; tasksToday: number; streak: number; level: number; lastAction?: string; }
```

From lib/mascot/companion-engine.ts (created in Plan 01):
```typescript
export function pickCompanionMessage(event: CompanionEvent, context: CompanionMessageContext): string;
export const MESSAGE_TEMPLATES: Record<CompanionEvent, string[]>;
```

From lib/ai-service.ts (existing):
```typescript
// Fetch direct Claude API — verifier les exports et la signature
```

From contexts/AIContext.tsx (existing):
```typescript
// useAI() hook — verifier comment acceder a la config IA (cle API, modele)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Messages IA contextuels + bulles d'emotion evenements</name>
  <files>lib/mascot/companion-engine.ts, app/(tabs)/tree.tsx</files>
  <read_first>lib/mascot/companion-engine.ts, lib/ai-service.ts, contexts/AIContext.tsx, app/(tabs)/tree.tsx, lib/mascot/companion-types.ts</read_first>
  <action>
    **lib/mascot/companion-engine.ts** — Ajouter la fonction generateCompanionAIMessage(). Per D-13.

    ```typescript
    export async function generateCompanionAIMessage(
      event: CompanionEvent,
      context: CompanionMessageContext,
      aiCall: ((prompt: string) => Promise<string>) | null,
    ): Promise<string> {
      // Fallback predefinies si pas d'IA
      const fallbackKey = pickCompanionMessage(event, context);
      if (!aiCall) return fallbackKey;

      try {
        const prompt = buildCompanionPrompt(event, context);
        const result = await Promise.race([
          aiCall(prompt),
          new Promise<never>((_, reject) => setTimeout(() => reject(new Error('timeout')), 3000)),
        ]);
        return result || fallbackKey;
      } catch {
        return fallbackKey;
      }
    }
    ```

    Fonction helper `buildCompanionPrompt()` (non exportee) :
    ```typescript
    function buildCompanionPrompt(event: CompanionEvent, ctx: CompanionMessageContext): string {
      // Prompt court < 200 tokens pour Claude Haiku
      // Instructions en francais car l'app est en francais
      const eventDescriptions: Record<CompanionEvent, string> = {
        task_completed: `${ctx.profileName} vient de completer une tache`,
        loot_opened: `${ctx.profileName} vient d'ouvrir un coffre a butin`,
        level_up: `${ctx.profileName} vient de monter au niveau ${ctx.level}`,
        greeting: `${ctx.profileName} vient d'arriver sur l'ecran de son arbre`,
        streak_milestone: `${ctx.profileName} a un streak de ${ctx.streak} jours`,
        harvest: `${ctx.profileName} vient de recolter sur sa ferme`,
        craft: `${ctx.profileName} vient de creer un objet dans son atelier`,
      };

      return `Tu es ${ctx.companionName}, un ${ctx.companionSpecies} mignon et attachant. ${eventDescriptions[event]}. Reponds en UNE phrase courte, encourageante et mignonne (max 80 caracteres). Pas d'emoji. Tutoie ${ctx.profileName}.`;
    }
    ```

    La fonction prend un `aiCall` callback au lieu d'importer directement ai-service.ts — cela evite un couplage direct entre lib/ (pure logic) et le service IA. L'appelant dans tree.tsx passera la fonction d'appel IA depuis AIContext.

    **app/(tabs)/tree.tsx** — Brancher les messages IA et les bulles d'emotion.

    1. Importer `generateCompanionAIMessage` depuis companion-engine.
    2. Acceder a l'IA via le hook AIContext existant (verifier le nom exact — probablement `useAI()` ou `useAIContext()`). Extraire la fonction d'appel Claude Haiku.
    3. Creer un callback `aiCall` compatible :
       ```typescript
       const aiCall = useMemo(() => {
         if (!aiConfig?.apiKey) return null;
         return async (prompt: string) => {
           // Utiliser le service IA existant pour appeler Claude Haiku
           // Verifier la signature de la fonction dans ai-service.ts
           // Probablement: callAI(prompt, { model: 'haiku', maxTokens: 100 })
           const response = await /* appel IA existant */;
           return response;
         };
       }, [aiConfig?.apiKey]);
       ```
    4. Modifier le message de bienvenue au focus (deja implemente en Plan 03) pour utiliser l'IA :
       ```typescript
       useFocusEffect(useCallback(() => {
         if (companion && activeProfile) {
           const context = { ... };
           // D'abord afficher un message predefini (instantane)
           const fallbackKey = pickCompanionMessage('greeting', context);
           setCompanionMessage(t(fallbackKey, context));

           // Puis tenter un message IA (async, remplace si reussi)
           if (aiCall) {
             generateCompanionAIMessage('greeting', context, aiCall)
               .then(msg => {
                 // Si c'est une cle i18n (fallback), traduire
                 // Si c'est un texte brut (IA), afficher directement
                 const isI18nKey = msg.startsWith('companion.msg.');
                 setCompanionMessage(isI18nKey ? t(msg, context) : msg);
               });
           }

           const timer = setTimeout(() => setCompanionMessage(null), 5000);
           return () => clearTimeout(timer);
         }
       }, [companion?.activeSpecies, activeProfile?.id]));
       ```
    5. Bulles d'emotion sur evenements app (per D-10) : Le compagnon doit reagir aux evenements qui se produisent AILLEURS dans l'app (tache completee, lootbox ouverte, level up). Comme le compagnon vit uniquement sur l'ecran arbre (D-11), ces reactions s'affichent au prochain focus de l'ecran arbre.
       - Stocker les evenements recents dans un `useRef<CompanionEvent[]>` ou dans le state :
         ```typescript
         const pendingEventsRef = useRef<CompanionEvent[]>([]);
         ```
       - Ecouter les evenements du vault/gamification qui indiquent une action recente. Alternatives :
         a) Comparer les donnees du profil (points, level, lootBoxesAvailable) entre deux focus pour detecter les changements
         b) Utiliser un callback onDataChange depuis useVault/useGamification
         c) Simplement afficher un message de greeting adapte a l'activite recente (plus simple)
       - Recommandation : option (c) — au focus, analyser les donnees recentes (taches aujourd'hui, dernier level up, etc.) et choisir l'evenement le plus pertinent pour le message. Cela evite un systeme d'evenements complexe.
       - Si `tasksToday > 0`, utiliser 'task_completed' au lieu de 'greeting'. Si level a change depuis la derniere visite, utiliser 'level_up'. Sinon, 'greeting'.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -c "error" || echo "0 errors"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "generateCompanionAIMessage" lib/mascot/companion-engine.ts` retourne au moins 2 lignes (export + definition)
    - `grep "buildCompanionPrompt" lib/mascot/companion-engine.ts` retourne au moins 1 ligne
    - `grep "generateCompanionAIMessage" "app/(tabs)/tree.tsx"` retourne au moins 1 ligne
    - `grep "aiCall" "app/(tabs)/tree.tsx"` retourne au moins 2 lignes
    - `grep "Promise.race" lib/mascot/companion-engine.ts` retourne 1 ligne (timeout 3s)
    - `npx tsc --noEmit` ne produit pas de nouvelles erreurs
  </acceptance_criteria>
  <done>Les messages contextuels hybrides (predefinies + IA Haiku) fonctionnent avec fallback automatique, et les evenements app declenchent des messages adaptes</done>
</task>

<task type="auto">
  <name>Task 2: CompanionAvatarMini + integration _layout.tsx</name>
  <files>components/mascot/CompanionAvatarMini.tsx, app/(tabs)/_layout.tsx</files>
  <read_first>app/(tabs)/_layout.tsx, lib/mascot/companion-types.ts, lib/types.ts, components/mascot/CompanionSlot.tsx</read_first>
  <action>
    **CompanionAvatarMini.tsx** — Composant avatar compagnon pour la tab bar et le profile picker. Per D-14.

    ```typescript
    import React from 'react';
    import { Image, Text, StyleSheet, type ImageSourcePropType } from 'react-native';
    import type { CompanionSpecies, CompanionStage } from '@/lib/mascot/companion-types';
    import type { CompanionData } from '@/lib/mascot/companion-types';
    import { getCompanionStage } from '@/lib/mascot/companion-engine';

    // Mapping sprites idle_1 par espece et stade (meme structure que CompanionSlot)
    const AVATAR_SPRITES: Record<CompanionSpecies, Record<CompanionStage, ImageSourcePropType>> = {
      chat: {
        bebe: require('@/assets/garden/animals/chat/bebe/idle_1.png'),
        jeune: require('@/assets/garden/animals/chat/jeune/idle_1.png'),
        adulte: require('@/assets/garden/animals/chat/adulte/idle_1.png'),
      },
      chien: {
        bebe: require('@/assets/garden/animals/chien/bebe/idle_1.png'),
        jeune: require('@/assets/garden/animals/chien/jeune/idle_1.png'),
        adulte: require('@/assets/garden/animals/chien/adulte/idle_1.png'),
      },
      lapin: {
        bebe: require('@/assets/garden/animals/lapin/bebe/idle_1.png'),
        jeune: require('@/assets/garden/animals/lapin/jeune/idle_1.png'),
        adulte: require('@/assets/garden/animals/lapin/adulte/idle_1.png'),
      },
      renard: {
        bebe: require('@/assets/garden/animals/renard/bebe/idle_1.png'),
        jeune: require('@/assets/garden/animals/renard/jeune/idle_1.png'),
        adulte: require('@/assets/garden/animals/renard/adulte/idle_1.png'),
      },
      herisson: {
        bebe: require('@/assets/garden/animals/herisson/bebe/idle_1.png'),
        jeune: require('@/assets/garden/animals/herisson/jeune/idle_1.png'),
        adulte: require('@/assets/garden/animals/herisson/adulte/idle_1.png'),
      },
    };

    interface CompanionAvatarMiniProps {
      companion: CompanionData | null | undefined;
      level: number;
      fallbackEmoji: string;
      size: number;
    }

    export const CompanionAvatarMini = React.memo(function CompanionAvatarMini({
      companion,
      level,
      fallbackEmoji,
      size,
    }: CompanionAvatarMiniProps) {
      if (!companion) {
        return <Text style={{ fontSize: size * 0.8 }}>{fallbackEmoji}</Text>;
      }
      const stage = getCompanionStage(level);
      const sprite = AVATAR_SPRITES[companion.activeSpecies]?.[stage];
      if (!sprite) {
        return <Text style={{ fontSize: size * 0.8 }}>{fallbackEmoji}</Text>;
      }
      return <Image source={sprite} style={{ width: size, height: size }} />;
    });
    ```

    **app/(tabs)/_layout.tsx** — Remplacer l'emoji avatar par le compagnon quand disponible. Per D-14, pitfall 6.

    1. Importer `CompanionAvatarMini` depuis `@/components/mascot/CompanionAvatarMini`.
    2. Chercher dans _layout.tsx l'endroit ou l'avatar du profil est rendu. Il y a au moins deux endroits :
       a) **Tab bar** : probablement un `<Text style={...}>{activeProfile.avatar}</Text>` dans la configuration des tabs
       b) **Profile picker modal** : probablement une liste de profils avec `<Text style={pickerStyles.avatar}>{p.avatar}</Text>`
    3. Pour chaque endroit, remplacer :
       ```typescript
       // AVANT:
       <Text style={pickerStyles.avatar}>{p.avatar}</Text>

       // APRES:
       <CompanionAvatarMini
         companion={p.companion}
         level={p.level}
         fallbackEmoji={p.avatar}
         size={40}
       />
       ```
       Ajuster `size` selon le contexte (40 pour le picker, peut-etre different pour la tab bar — verifier les styles existants).
    4. NE PAS casser le layout existant — le composant `CompanionAvatarMini` doit avoir les memes dimensions que l'emoji qu'il remplace. Si l'emoji est dans un container avec des styles specifiques (flex, padding, etc.), s'assurer que l'Image a les memes dimensions.
    5. Les profils sans compagnon (companion undefined/null) continuent d'afficher l'emoji — c'est le comportement par defaut du composant.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -c "error" || echo "0 errors"</automated>
  </verify>
  <acceptance_criteria>
    - components/mascot/CompanionAvatarMini.tsx exporte `CompanionAvatarMini`
    - `grep "React.memo" components/mascot/CompanionAvatarMini.tsx` retourne 1 ligne
    - `grep "CompanionAvatarMini" "app/(tabs)/_layout.tsx"` retourne au moins 2 lignes (import + usage)
    - `grep "fallbackEmoji" components/mascot/CompanionAvatarMini.tsx` retourne au moins 2 lignes
    - Le composant gere le cas companion=null (retourne l'emoji)
    - `npx tsc --noEmit` ne produit pas de nouvelles erreurs
  </acceptance_criteria>
  <done>L'avatar compagnon remplace l'emoji dans le profile picker et la tab bar quand un compagnon est actif</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verification complete du systeme compagnon</name>
  <files>app/(tabs)/tree.tsx, app/(tabs)/_layout.tsx, components/mascot/CompanionAvatarMini.tsx</files>
  <action>Verification humaine end-to-end du systeme compagnon complet sur device physique. Pas de code a ecrire — verification uniquement.</action>
  <verify>
    <automated>npx tsc --noEmit 2>&amp;1 | grep -c "error" || echo "0 errors"</automated>
  </verify>
  <how-to-verify>
    1. Lancer l'app : `npx expo run:ios --device`
    2. **Avatar profil** : Dans le selecteur de profil, verifier que le profil avec compagnon affiche le sprite au lieu de l'emoji. Les profils sans compagnon affichent toujours l'emoji.
    3. **Messages contextuels** : Completer une tache, puis naviguer vers l'ecran arbre. Le compagnon devrait afficher un message lie a la tache completee (pas juste un greeting generique).
    4. **Messages IA** (si cle API configuree) : Verifier que le message est genere par l'IA (texte naturel, pas un template predefinies). Si pas de cle API, le fallback predefinies doit s'afficher.
    5. **Humeur** : Le compagnon affiche l'humeur correcte (content si activite recente, endormi la nuit, excite si beaucoup de taches).
    6. **Lootbox** : Ouvrir un coffre a butin (si disponible). Si un compagnon est droppe, verifier qu'il apparait dans les especes debloquees.
    7. **Bonus XP** : Verifier dans les logs ou l'UI que le bonus +5% XP est visible quand un compagnon est actif.
    8. **Persistance** : Fermer et rouvrir l'app. Le compagnon, son nom, et ses donnees doivent persister.
  </how-to-verify>
  <done>Le systeme compagnon fonctionne de bout en bout : messages IA, avatar profil, bonus XP, persistance</done>
  <resume-signal>Type "approved" ou decrivez les problemes constates</resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passe sans nouvelles erreurs
- Le compagnon affiche des messages contextuels adaptes aux actions recentes
- L'avatar compagnon apparait dans le profile picker
- Les donnees compagnon persistent apres redemarrage
</verification>

<success_criteria>
Le systeme compagnon est fonctionnellement complet : le compagnon est conscient des actions, sert d'avatar de profil, et le systeme entier (choix, evolution, interactions, messages, lootbox, bonus XP) fonctionne de bout en bout.
</success_criteria>

<output>
After completion, create `.planning/phases/10-compagnon-mascotte/10-04-SUMMARY.md`
</output>
