---
phase: 14-parite-mobile-desktop
plan: 05
type: execute
wave: 2
depends_on: [14-01]
files_modified:
  - apps/desktop/src/pages/Pregnancy.tsx
  - apps/desktop/src/pages/Pregnancy.css
  - apps/desktop/src/pages/NightMode.tsx
  - apps/desktop/src/pages/NightMode.css
  - apps/desktop/src/pages/Compare.tsx
  - apps/desktop/src/pages/Compare.css
  - apps/desktop/src/pages/More.tsx
  - apps/desktop/src/pages/More.css
autonomous: true
requirements: [PAR-01]

must_haves:
  truths:
    - "L'ecran Pregnancy affiche la timeline semaine par semaine"
    - "L'ecran NightMode affiche l'interface sombre pour tetees nocturnes (sans brightness control)"
    - "L'ecran Compare affiche la comparaison photos cote a cote"
    - "L'ecran More affiche un menu de navigation organise vers tous les ecrans"
  artifacts:
    - path: "apps/desktop/src/pages/Pregnancy.tsx"
      provides: "Timeline grossesse semaine par semaine"
      min_lines: 100
    - path: "apps/desktop/src/pages/NightMode.tsx"
      provides: "Interface nuit pour tetees"
      min_lines: 80
    - path: "apps/desktop/src/pages/Compare.tsx"
      provides: "Comparaison photos cote a cote"
      min_lines: 80
    - path: "apps/desktop/src/pages/More.tsx"
      provides: "Menu navigation desktop"
      min_lines: 60
  key_links:
    - from: "apps/desktop/src/pages/Pregnancy.tsx"
      to: "apps/desktop/src/contexts/VaultContext.tsx"
      via: "useVault() pregnancyData"
      pattern: "pregnancyData"
---

<objective>
Creer les 4 ecrans simples restants : Pregnancy, NightMode, Compare, More.

Purpose: Ces ecrans sont moins complexes que les precedents — regroupes pour efficiency.
Output: 8 fichiers (4 .tsx + 4 .css).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-parit-mobile-desktop-analyser-et-combler-les-carts-fonctionnels-entre-app-mobile-et-vision-desktop-pour-atteindre-une-parit-quasi-identique/14-01-SUMMARY.md
@apps/desktop/src/pages/Budget.tsx
@apps/desktop/src/contexts/VaultContext.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ecrans Pregnancy + NightMode</name>
  <files>apps/desktop/src/pages/Pregnancy.tsx, apps/desktop/src/pages/Pregnancy.css, apps/desktop/src/pages/NightMode.tsx, apps/desktop/src/pages/NightMode.css</files>
  <read_first>
    - apps/desktop/src/pages/Budget.tsx (pattern page desktop)
    - apps/desktop/src/contexts/VaultContext.tsx (pregnancyData, donnees disponibles)
    - app/(tabs)/pregnancy.tsx (source de verite mobile si existe — sinon chercher dans app/ le fichier correspondant)
    - app/(tabs)/night-mode.tsx (source de verite mobile si existe)
  </read_first>
  <action>
**Pregnancy.tsx :**
- Import `useVault` pour `pregnancyData`, `addPregnancyEntry`
- Structure :
  1. Header "Grossesse" + indicateur semaine actuelle (si grossesse en cours)
  2. Timeline verticale : chaque semaine = une ligne avec numero, date, description du developpement bebe (texte informatif depuis les donnees ou constantes)
  3. Section "Mes notes" : liste des entrees (date, poids, symptomes, notes) dans des GlassCard
  4. Bouton "Ajouter une entree" ouvre un Modal avec champs : date, poids, tension, symptomes (textarea), notes
  5. Si pas de grossesse en cours : afficher un etat vide avec bouton "Demarrer le suivi"
- CSS : Timeline avec bordure gauche, points sur chaque semaine, responsive.

**NightMode.tsx :**
- Structure minimaliste a fond tres sombre :
  1. Fond noir/tres fonce (`background: #0a0a0a`)
  2. Texte en rouge attenute (`color: #661111` ou similaire) pour preserver la vision nocturne
  3. Horloge grande au centre (heure actuelle, `setInterval` 1s)
  4. Boutons grands : "Debut tetee", "Fin tetee", timer entre les deux
  5. Historique des tetees de la nuit en cours (debut, fin, duree, cote gauche/droite)
  6. Pas de brightness control (per D-01 — impossible sur desktop)
- IMPORTANT : NE PAS utiliser les CSS variables du theme — ce mode a son propre scheme de couleurs sombre
- CSS : Fond noir, texte rouge, gros boutons tactiles, min-height 100vh.
  </action>
  <verify>
    <automated>cd apps/desktop && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - Pregnancy.tsx existe et contient `export default function Pregnancy`
    - Pregnancy.tsx contient une timeline (element avec classe `timeline` ou `week` dans le JSX)
    - Pregnancy.css contient `border-left` ou equivalent pour la timeline
    - NightMode.tsx existe et contient `export default function NightMode`
    - NightMode.tsx contient `setInterval` pour l'horloge ou le timer
    - NightMode.css contient `background` avec une couleur tres sombre (#0a ou #000 ou rgb(10)
  </acceptance_criteria>
  <done>Pregnancy affiche une timeline + notes, NightMode affiche l'interface nuit avec timer tetees.</done>
</task>

<task type="auto">
  <name>Task 2: Ecrans Compare + More</name>
  <files>apps/desktop/src/pages/Compare.tsx, apps/desktop/src/pages/Compare.css, apps/desktop/src/pages/More.tsx, apps/desktop/src/pages/More.css</files>
  <read_first>
    - apps/desktop/src/pages/Photos.tsx (pattern page desktop avec images)
    - apps/desktop/src/pages/Settings.tsx (pattern page desktop avec navigation/liens)
    - apps/desktop/src/App.tsx (NAV_SECTIONS pour connaitre toutes les routes existantes)
    - app/(tabs)/compare.tsx (source de verite mobile si existe — sinon chercher le pattern)
  </read_first>
  <action>
**Compare.tsx :**
- Import `useVault` pour `files` (acceder aux photos du vault)
- Structure :
  1. Header "Comparer des photos"
  2. Layout 2 colonnes cote a cote (split view)
  3. Chaque colonne : selecteur de photo (dropdown/grid des photos disponibles dans le vault)
  4. Affichage des deux photos selectionnees cote a cote
  5. Slider de zoom desktop : boutons +/- et scroll wheel (per D-02 pinch-to-zoom equivalent)
  6. Si pas de photos : etat vide "Aucune photo dans le vault"
- Desktop enhancement (per D-02) : scroll wheel pour zoomer (onWheel event handler), boutons +/- visibles
- CSS : Grid 2 colonnes egales, gap central, images max-height avec object-fit contain.

**More.tsx :**
- Structure :
  1. Header "Plus"
  2. Grille de cartes de navigation vers tous les ecrans non visibles dans la sidebar principale
  3. Chaque carte : icone + label + description courte
  4. Liste des destinations : Rendez-vous, Notes, Statistiques, Competences, Sante, Routines, Grossesse, Mode nuit, Comparer
  5. Click sur une carte navigue via `useNavigate()` de react-router-dom
- CSS : Grille responsive (auto-fill, minmax(200px, 1fr)), cartes avec hover scale transition.

NOTE : Sur desktop, la sidebar affiche deja tous les liens, donc "More" est un hub supplementaire utile pour la decouverte. Si tous les ecrans sont deja dans la sidebar, More peut simplement afficher un dashboard des raccourcis avec descriptions.
  </action>
  <verify>
    <automated>cd apps/desktop && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - Compare.tsx existe et contient `export default function Compare`
    - Compare.tsx contient `onWheel` ou des boutons zoom (+/-)
    - Compare.css contient `grid-template-columns` 2 colonnes
    - More.tsx existe et contient `export default function More`
    - More.tsx contient `useNavigate` ou `NavLink` de react-router-dom
    - More.css contient une grille responsive
  </acceptance_criteria>
  <done>Compare affiche 2 photos cote a cote avec zoom, More affiche un hub de navigation vers tous les ecrans.</done>
</task>

</tasks>

<verification>
1. `cd apps/desktop && npx tsc --noEmit` passe
2. Les 8 fichiers existent
3. NightMode.tsx a un fond sombre
4. More.tsx a des liens vers les autres pages
</verification>

<success_criteria>
Les 4 ecrans simples sont fonctionnels, `tsc --noEmit` passe.
</success_criteria>

<output>
After completion, create `.planning/phases/14-parit-mobile-desktop-analyser-et-combler-les-carts-fonctionnels-entre-app-mobile-et-vision-desktop-pour-atteindre-une-parit-quasi-identique/14-05-SUMMARY.md`
</output>
