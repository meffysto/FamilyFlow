---
phase: 14-parite-mobile-desktop
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/desktop/package.json
  - apps/desktop/src/contexts/VaultContext.tsx
  - apps/desktop/src/App.tsx
autonomous: true
requirements: [PAR-01, PAR-03]

must_haves:
  truths:
    - "VaultContext desktop expose toutes les mutations CRUD (RDV, Notes, Defis, Health, Routines, Skills, Loot)"
    - "VaultContext desktop charge les données manquantes (gamiData, healthRecords, routines, skillTrees, pregnancyData)"
    - "App.tsx a des routes pour les 10 nouveaux ecrans (rdv, notes, stats, skills, health, routines, pregnancy, night-mode, compare, more)"
    - "framer-motion, recharts, canvas-confetti sont installes dans apps/desktop/package.json"
  artifacts:
    - path: "apps/desktop/src/contexts/VaultContext.tsx"
      provides: "All CRUD mutations + data loading for new screens"
      exports: ["useVault", "VaultProvider", "VaultContextValue"]
    - path: "apps/desktop/src/App.tsx"
      provides: "Routes for all 10 new screens"
  key_links:
    - from: "apps/desktop/src/contexts/VaultContext.tsx"
      to: "@family-vault/core"
      via: "import parsers and serializers"
      pattern: "import.*from '@family-vault/core'"
    - from: "apps/desktop/src/App.tsx"
      to: "apps/desktop/src/pages/*.tsx"
      via: "lazy imports and Route elements"
      pattern: "lazy\\(\\(\\) => import\\('./pages/"
---

<objective>
Fondation pour la parite mobile-desktop : installer les dependances, etendre VaultContext avec toutes les mutations CRUD manquantes et le chargement des donnees absentes, ajouter les routes pour les 10 ecrans manquants dans App.tsx.

Purpose: Tous les plans suivants (ecrans, loot revamp, OCR budget) dependent de VaultContext etendu et des routes.
Output: VaultContext complet, App.tsx avec routes, dependances installees.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@apps/desktop/src/contexts/VaultContext.tsx
@apps/desktop/src/App.tsx
@apps/desktop/package.json
@hooks/useVault.ts
@lib/parser.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Installer dependances + ajouter routes App.tsx</name>
  <files>apps/desktop/package.json, apps/desktop/src/App.tsx</files>
  <read_first>
    - apps/desktop/package.json (dependances actuelles)
    - apps/desktop/src/App.tsx (routes et NAV_SECTIONS actuels)
  </read_first>
  <action>
1. Dans `apps/desktop/`, executer :
   ```
   npm install framer-motion recharts canvas-confetti
   npm install -D @types/canvas-confetti
   ```

2. Dans `apps/desktop/src/App.tsx` :
   a. Ajouter les lazy imports pour les 10 nouvelles pages :
   ```typescript
   const RDV = lazy(() => import('./pages/RDV'));
   const Notes = lazy(() => import('./pages/Notes'));
   const Stats = lazy(() => import('./pages/Stats'));
   const Skills = lazy(() => import('./pages/Skills'));
   const Health = lazy(() => import('./pages/Health'));
   const Routines = lazy(() => import('./pages/Routines'));
   const Pregnancy = lazy(() => import('./pages/Pregnancy'));
   const NightMode = lazy(() => import('./pages/NightMode'));
   const Compare = lazy(() => import('./pages/Compare'));
   const More = lazy(() => import('./pages/More'));
   ```

   b. Remplacer la route redirect `/rdv` par la vraie route :
   ```typescript
   <Route path="/rdv" element={<RDV />} />
   ```

   c. Ajouter les 9 autres routes apres les routes existantes :
   ```typescript
   <Route path="/notes" element={<Notes />} />
   <Route path="/stats" element={<Stats />} />
   <Route path="/skills" element={<Skills />} />
   <Route path="/health" element={<Health />} />
   <Route path="/routines" element={<Routines />} />
   <Route path="/pregnancy" element={<Pregnancy />} />
   <Route path="/night-mode" element={<NightMode />} />
   <Route path="/compare" element={<Compare />} />
   <Route path="/more" element={<More />} />
   ```

   d. Ajouter dans NAV_SECTIONS une section "Plus" :
   ```typescript
   {
     label: 'Plus',
     items: [
       { path: '/rdv', label: 'Rendez-vous', icon: '📅' },
       { path: '/notes', label: 'Notes', icon: '📝' },
       { path: '/stats', label: 'Statistiques', icon: '📊' },
       { path: '/skills', label: 'Competences', icon: '🎯' },
       { path: '/health', label: 'Sante', icon: '🏥' },
       { path: '/routines', label: 'Routines', icon: '⏰' },
       { path: '/pregnancy', label: 'Grossesse', icon: '🤰' },
       { path: '/night-mode', label: 'Mode nuit', icon: '🌙' },
       { path: '/compare', label: 'Comparer', icon: '📷' },
     ],
   }
   ```

   e. Creer un fichier placeholder `apps/desktop/src/pages/Placeholder.tsx` exporte par defaut une div avec le nom de la page (les vrais ecrans seront crees dans les plans suivants). Chaque nouvelle page importera ce placeholder temporairement SAUF si la page est creee dans un plan parallele wave 2.

   IMPORTANT: Pour eviter les erreurs de build avant que les pages soient creees, creer des fichiers stub pour chacune des 10 pages (ex: `export default function RDV() { return <div className="page-container"><h1>Rendez-vous</h1><p>En cours de developpement...</p></div>; }`).
  </action>
  <verify>
    <automated>cd apps/desktop && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - apps/desktop/package.json contient "framer-motion", "recharts", "canvas-confetti" dans dependencies
    - apps/desktop/package.json contient "@types/canvas-confetti" dans devDependencies
    - apps/desktop/src/App.tsx contient `lazy(() => import('./pages/RDV'))`
    - apps/desktop/src/App.tsx contient `lazy(() => import('./pages/Notes'))`
    - apps/desktop/src/App.tsx contient `lazy(() => import('./pages/Stats'))`
    - apps/desktop/src/App.tsx contient `lazy(() => import('./pages/Skills'))`
    - apps/desktop/src/App.tsx contient `lazy(() => import('./pages/Health'))`
    - apps/desktop/src/App.tsx contient `lazy(() => import('./pages/Routines'))`
    - apps/desktop/src/App.tsx contient `<Route path="/rdv" element={<RDV />} />`
    - apps/desktop/src/App.tsx NE contient PAS `Navigate to="/calendar"` pour /rdv
    - Les 10 fichiers stub existent dans apps/desktop/src/pages/
  </acceptance_criteria>
  <done>Les 3 dependances sont installees, les 10 routes sont declarees dans App.tsx, les 10 stubs de pages existent, `tsc --noEmit` passe.</done>
</task>

<task type="auto">
  <name>Task 2: Etendre VaultContext — mutations CRUD + chargement donnees manquantes</name>
  <files>apps/desktop/src/contexts/VaultContext.tsx</files>
  <read_first>
    - apps/desktop/src/contexts/VaultContext.tsx (etat actuel complet — 606 lignes)
    - hooks/useVault.ts (source de verite mobile pour les signatures de mutations — lignes 1-100 pour les types, puis chercher chaque mutation par nom)
    - lib/parser.ts (fonctions serialize* disponibles)
  </read_first>
  <action>
Etendre VaultContext desktop en suivant le pattern existant de `toggleTask` et le conseil de RESEARCH.md Pitfall 1 (eviter le monolithe — grouper par domaine).

**A. Ajouter les types manquants dans VaultState :**
```typescript
export interface VaultState {
  // ... existants ...
  gamiData: GamificationData | null;  // gamification profil actif
  healthRecords: any[];               // suivi sante (HealthRecord[] si type existe dans core)
  routines: any[];                    // routines (Routine[] si type existe dans core)
  skillTrees: any[];                  // arbres competences
  pregnancyData: any | null;          // donnees grossesse
}
```
Verifier les types exacts dans `@family-vault/core` (imports disponibles). Si le type n'est pas exporte, utiliser `any` temporairement avec un commentaire TODO.

**B. Ajouter les states + setters :**
```typescript
const [gamiData, setGamiData] = useState<GamificationData | null>(null);
const [healthRecords, setHealthRecords] = useState<any[]>([]);
const [routines, setRoutines] = useState<any[]>([]);
const [skillTrees, setSkillTrees] = useState<any[]>([]);
const [pregnancyData, setPregnancyData] = useState<any | null>(null);
```

**C. Ajouter le chargement dans loadSecondaryData :**
Pour chaque nouveau type de donnee, ajouter un bloc async comme les existants :
- gamiData : lire `gami-{activeProfile.id}.md`, parser avec `parseGamification()`
- healthRecords : lire les fichiers dans `01 - Enfants/{Name}/Sante.md` (verifier le pattern mobile dans useVault.ts)
- routines : lire `05 - Famille/Routines.md` (verifier le pattern mobile)
- skillTrees : lire `gami-{id}.md` section skills (si integre dans gamiData)
- pregnancyData : lire `05 - Famille/Grossesse.md` (verifier le pattern mobile)

**D. Ajouter les mutations CRUD dans VaultContextValue :**

Suivre exactement le pattern `toggleTask` : useCallback, vaultPath guard, try/catch, serialize via core, writeVaultFile, update state local.

Mutations a ajouter (groupees par domaine) :

**RDV (per D-06) :**
- `addRDV(rdv)` : creer fichier `04 - Rendez-vous/{titre}.md`, serialiser avec `serializeRDV()` de core, setRdvs append
- `updateRDV(rdv)` : lire fichier, re-serialiser, ecrire, setRdvs update
- `deleteRDV(rdv)` : supprimer fichier (import deleteVaultFile from vault-service si dispo, sinon ecrire contenu vide), setRdvs filter

**Notes (per D-06) :**
- `addNote(note)` : creer `08 - Notes/{titre}.md`, serialiser avec `serializeNote()`, setNotes append
- `updateNote(note)` : re-serialiser et ecrire, setNotes update
- `deleteNote(note)` : supprimer fichier, setNotes filter

**Defis (per D-06) :**
- `addDefi(defi)` : ecrire dans `defis.md` (append au fichier), setDefis append
- `updateDefi(defi)` / `checkInDefi(defiId)` : lire, modifier, re-serialiser defis.md

**Loot (per D-06) :**
- `openLootBox()` : lire gami-{id}.md, appeler engine openLootBox de core, ecrire, setGamiData, retourner reward
- `markLootUsed(rewardId)` : marquer un reward comme utilise

**Skills (per D-06) :**
- `unlockSkill(skillId)` : lire gami-{id}.md, appeler engine, ecrire

**Health (per D-06) :**
- `saveHealthRecord(record)` : ecrire dans fichier sante du profil actif
- `addGrowthEntry(entry)`, `updateGrowthEntry(entry)`, `deleteGrowthEntry(id)` : CRUD croissance
- `addVaccineEntry(entry)` : append vaccin

**Routines (per D-06) :**
- `saveRoutines(routines)` : serialiser et ecrire routines.md
- `completeRoutineStep(routineId, stepIdx)` : marquer step complete

**Grossesse (per D-06) :**
- `addPregnancyEntry(entry)` : append dans grossesse.md

**Saga/Events (per D-06) :**
- `saveSagaProgress(progress)` : ecrire dans gami-{id}.md
- `saveEventProgress(progress)` : ecrire dans gami-{id}.md

**E. Mettre a jour VaultContextValue interface et useMemo value.**

IMPORTANT : Pour chaque mutation, verifier si la fonction serialize correspondante existe dans `@family-vault/core`. Si elle n'existe pas, consulter `lib/parser.ts` pour le format attendu et ecrire le markdown manuellement en suivant le pattern vault mobile.

IMPORTANT : Ajouter `deleteVaultFile` a `vault-service.ts` si absente (wrapper autour de `@tauri-apps/plugin-fs` remove).

IMPORTANT : Conserver le pattern anti-monolithe de RESEARCH.md — si VaultContext depasse 1200 lignes, extraire les mutations par domaine dans des hooks helpers (ex: `useRdvMutations.ts`, `useNotesMutations.ts`) importes dans VaultProvider.
  </action>
  <verify>
    <automated>cd apps/desktop && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - VaultContext.tsx exporte VaultContextValue avec les proprietes : addRDV, updateRDV, deleteRDV, addNote, updateNote, deleteNote, openLootBox, unlockSkill, saveHealthRecord, saveRoutines
    - VaultContext.tsx contient useState pour gamiData, healthRecords, routines
    - VaultContext.tsx contient loadSecondaryData avec chargement gamiData (pattern `gami-${`)
    - VaultContext.tsx importe des serializers depuis '@family-vault/core' (serializeRDV ou equivalent)
    - Le fichier ne depasse pas 1500 lignes (si depasse, les mutations sont extraites dans des hooks helpers)
    - `tsc --noEmit` dans apps/desktop passe sans nouvelles erreurs
  </acceptance_criteria>
  <done>VaultContext desktop expose toutes les mutations CRUD listees dans D-06, charge les donnees manquantes (gamiData, health, routines, skills, pregnancy), et passe tsc.</done>
</task>

</tasks>

<verification>
1. `cd apps/desktop && npx tsc --noEmit` passe
2. `grep -c "addRDV\|addNote\|openLootBox\|unlockSkill\|saveHealthRecord\|saveRoutines" apps/desktop/src/contexts/VaultContext.tsx` retourne >= 6
3. `grep -c "lazy.*import.*pages/" apps/desktop/src/App.tsx` retourne >= 28 (18 existants + 10 nouveaux)
4. `grep "framer-motion" apps/desktop/package.json` retourne une ligne
</verification>

<success_criteria>
Les dependances sont installees, VaultContext expose toutes les mutations CRUD per D-06, les 10 routes sont declarees, les stubs de pages existent, tsc passe.
</success_criteria>

<output>
After completion, create `.planning/phases/14-parit-mobile-desktop-analyser-et-combler-les-carts-fonctionnels-entre-app-mobile-et-version-desktop-pour-atteindre-une-parit-quasi-identique/14-01-SUMMARY.md`
</output>
