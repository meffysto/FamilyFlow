---
phase: 14-parite-mobile-desktop
plan: 08
type: execute
wave: 3
depends_on: [14-02, 14-03, 14-04, 14-05, 14-06, 14-07]
files_modified:
  - apps/desktop/src/pages/Tasks.tsx
  - apps/desktop/src/pages/Tasks.css
  - apps/desktop/src/pages/Challenges.tsx
  - apps/desktop/src/pages/Challenges.css
  - apps/desktop/src/pages/Wishlist.tsx
  - apps/desktop/src/pages/Wishlist.css
  - apps/desktop/src/pages/Birthdays.tsx
  - apps/desktop/src/pages/Birthdays.css
  - apps/desktop/src/pages/Moods.tsx
  - apps/desktop/src/pages/Gratitude.tsx
  - apps/desktop/src/pages/Quotes.tsx
autonomous: false
requirements: [PAR-02, PAR-03]

must_haves:
  truths:
    - "Toutes les pages existantes (Tasks, Challenges, Wishlist, Birthdays, Moods, Gratitude, Quotes) ont des interactions hover-to-reveal per D-02"
    - "Le raccourci Ctrl/Cmd+R declenche un refresh sur toutes les pages per D-02"
    - "Les donnees creees sur desktop sont lisibles sur mobile et vice-versa per PAR-03"
  artifacts:
    - path: "apps/desktop/src/pages/Tasks.tsx"
      provides: "Taches avec hover-to-reveal delete"
    - path: "apps/desktop/src/pages/Challenges.tsx"
      provides: "Defis avec mutations CRUD completes"
  key_links:
    - from: "apps/desktop/src/pages/Tasks.tsx"
      to: "apps/desktop/src/contexts/VaultContext.tsx"
      via: "useVault() refresh"
      pattern: "refresh"
---

<objective>
Polish des interactions desktop sur les 7+ pages existantes : ajouter hover-to-reveal delete, raccourcis clavier, verifier la parite des donnees entre desktop et mobile.

Purpose: Les pages existantes fonctionnent mais n'ont pas les interactions desktop specifiques (per D-02) ni la verification de parite (per PAR-03).
Output: Pages existantes mises a jour + checkpoint visuel final.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-parit-mobile-desktop-analyser-et-combler-les-carts-fonctionnels-entre-app-mobile-et-vision-desktop-pour-atteindre-une-parit-quasi-identique/14-01-SUMMARY.md
@apps/desktop/src/pages/Tasks.tsx
@apps/desktop/src/pages/Challenges.tsx
@apps/desktop/src/pages/Wishlist.tsx
@apps/desktop/src/contexts/VaultContext.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hover-to-reveal + raccourcis clavier sur pages existantes</name>
  <files>apps/desktop/src/pages/Tasks.tsx, apps/desktop/src/pages/Tasks.css, apps/desktop/src/pages/Challenges.tsx, apps/desktop/src/pages/Challenges.css, apps/desktop/src/pages/Wishlist.tsx, apps/desktop/src/pages/Wishlist.css, apps/desktop/src/pages/Birthdays.tsx, apps/desktop/src/pages/Birthdays.css, apps/desktop/src/pages/Moods.tsx, apps/desktop/src/pages/Gratitude.tsx, apps/desktop/src/pages/Quotes.tsx</files>
  <read_first>
    - apps/desktop/src/pages/Tasks.tsx (taches — verifier si hover-to-delete existe deja)
    - apps/desktop/src/pages/Tasks.css (styles existants)
    - apps/desktop/src/pages/Challenges.tsx (defis — verifier mutations CRUD)
    - apps/desktop/src/pages/Challenges.css
    - apps/desktop/src/pages/Wishlist.tsx (souhaits — verifier interactions)
    - apps/desktop/src/pages/Wishlist.css
    - apps/desktop/src/pages/Birthdays.tsx (anniversaires)
    - apps/desktop/src/pages/Moods.tsx (humeurs)
    - apps/desktop/src/pages/Gratitude.tsx (gratitude)
    - apps/desktop/src/pages/Quotes.tsx (citations)
  </read_first>
  <action>
Pour chaque page listee, appliquer les patterns d'interaction desktop per D-02 :

**Pattern hover-to-reveal (ajouter sur chaque page qui a des items avec actions) :**
- Wrapper CSS : `.item-row { position: relative; }` + `.item-actions { opacity: 0; transition: opacity 120ms ease; }` + `.item-row:hover .item-actions { opacity: 1; }`
- Boutons d'action : modifier (crayon), supprimer (corbeille) apparaissent au hover
- Verifier d'abord si le pattern existe deja — ne pas dupliquer

**Pattern raccourci clavier (ajouter sur chaque page) :**
```typescript
useEffect(() => {
  function onKeyDown(e: KeyboardEvent) {
    // Ctrl/Cmd+R = refresh
    if ((e.ctrlKey || e.metaKey) && e.key === 'r') {
      e.preventDefault();
      refresh();
    }
    // Delete = supprimer l'item selectionne
    if (e.key === 'Delete' && selectedId) {
      handleDelete(selectedId);
    }
  }
  window.addEventListener('keydown', onKeyDown);
  return () => window.removeEventListener('keydown', onKeyDown);
}, [selectedId, refresh]);
```

**Pages specifiques :**

- **Tasks.tsx** : Ajouter hover-to-reveal pour un bouton "Supprimer" sur chaque tache (si pas deja present). Ajouter raccourci Ctrl+R pour refresh.

- **Challenges.tsx** : Verifier que les mutations addDefi, checkInDefi, completeDefi sont utilisees. Ajouter hover-to-reveal pour modifier/supprimer un defi.

- **Wishlist.tsx** : Ajouter hover-to-reveal pour modifier/supprimer un souhait. Verifier mutations.

- **Birthdays.tsx** : Ajouter hover-to-reveal pour modifier/supprimer. Verifier mutations.

- **Moods.tsx** : Ajouter hover-to-reveal pour supprimer une entree humeur.

- **Gratitude.tsx** : Ajouter hover-to-reveal pour supprimer une entree gratitude.

- **Quotes.tsx** : Ajouter hover-to-reveal pour modifier/supprimer une citation.

**CSS updates :**
Ajouter dans chaque .css concerne les classes `.item-actions` hover-reveal si absentes.

IMPORTANT : Ne pas casser les fonctionnalites existantes. Lire chaque fichier avant modification. Si le hover-to-reveal existe deja, ne pas le dupliquer.
  </action>
  <verify>
    <automated>cd apps/desktop && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - Tasks.tsx contient `useEffect` avec `keydown` et soit `Ctrl` soit `metaKey`
    - Tasks.css contient `:hover` avec `.item-actions` ou pattern equivalent
    - Challenges.tsx contient hover-to-reveal pattern (`.item-actions` ou `opacity` toggle au hover)
    - Wishlist.css contient `:hover` rule pour les actions
    - Au moins 5 des 7 pages modifiees contiennent le pattern Ctrl/Cmd+R refresh
  </acceptance_criteria>
  <done>Toutes les pages existantes ont des interactions hover-to-reveal et raccourcis clavier per D-02.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verification visuelle parite mobile-desktop</name>
  <files>apps/desktop/src/pages/</files>
  <action>
Verification humaine de l'ensemble de la phase 14 : 10 nouveaux ecrans desktop, VaultContext etendu, Loot revampe, Budget OCR, interactions hover/clavier.

Lancer le desktop (`cd apps/desktop && npm run dev`) et verifier chaque ecran et interaction.
  </action>
  <what-built>
    Phase 14 complete : 10 nouveaux ecrans desktop, VaultContext etendu avec toutes les mutations CRUD, Loot revampe avec animation Framer Motion + confetti, Budget avec OCR drag & drop, interactions hover-to-reveal et raccourcis clavier sur toutes les pages.
  </what-built>
  <how-to-verify>
    1. Lancer le desktop : `cd apps/desktop && npm run dev`
    2. Verifier la sidebar — les nouvelles pages (RDV, Notes, Stats, Skills, Health, Routines, etc.) apparaissent
    3. Naviguer sur chaque nouvel ecran et verifier qu'il affiche du contenu (pas une page blanche)
    4. Sur l'ecran RDV : creer un RDV, verifier qu'il apparait, hover pour voir le bouton supprimer
    5. Sur l'ecran Notes : creer une note, verifier le layout master-detail
    6. Sur l'ecran Stats : verifier que les 6 graphiques recharts s'affichent
    7. Sur l'ecran Loot : ouvrir un coffre (si disponible), verifier l'animation card flip et le confetti
    8. Sur l'ecran Budget : drag & drop une image de recu, verifier le flow OCR (si cle API configuree)
    9. Sur l'ecran Tasks : hover sur une tache, verifier que le bouton supprimer apparait
    10. Appuyer Ctrl+R (ou Cmd+R sur Mac) sur n'importe quelle page — verifier que le refresh se declenche
    11. Creer une donnee sur desktop (ex: RDV) puis verifier sur le mobile qu'elle est visible (parite per PAR-03)
  </how-to-verify>
  <verify>Verification visuelle manuelle par l'utilisateur</verify>
  <done>L'utilisateur a approuve visuellement la parite mobile-desktop sur tous les ecrans.</done>
  <resume-signal>Type "approved" ou decrire les problemes trouves</resume-signal>
</task>

</tasks>

<verification>
1. `cd apps/desktop && npx tsc --noEmit` passe
2. Toutes les pages ont des interactions hover
3. Verification visuelle par l'utilisateur
</verification>

<success_criteria>
Toutes les pages desktop ont des interactions per D-02 (hover-to-reveal, raccourcis clavier), la parite des donnees est verifiee per PAR-03, et l'utilisateur approuve visuellement.
</success_criteria>

<output>
After completion, create `.planning/phases/14-parit-mobile-desktop-analyser-et-combler-les-carts-fonctionnels-entre-app-mobile-et-version-desktop-pour-atteindre-une-parit-quasi-identique/14-08-SUMMARY.md`
</output>
