---
phase: 14-parite-mobile-desktop
plan: 03
type: execute
wave: 2
depends_on: [14-01]
files_modified:
  - apps/desktop/src/pages/Health.tsx
  - apps/desktop/src/pages/Health.css
  - apps/desktop/src/pages/Routines.tsx
  - apps/desktop/src/pages/Routines.css
autonomous: true
requirements: [PAR-01, PAR-02]

must_haves:
  truths:
    - "L'ecran Health affiche le suivi croissance, vaccins et historique medical avec CRUD"
    - "L'ecran Routines affiche les sequences visuelles avec timers et permet de completer les etapes"
    - "Les deux ecrans utilisent hover-to-reveal et raccourcis clavier per D-02"
  artifacts:
    - path: "apps/desktop/src/pages/Health.tsx"
      provides: "Ecran sante avec onglets croissance/vaccins/historique"
      min_lines: 200
    - path: "apps/desktop/src/pages/Routines.tsx"
      provides: "Ecran routines avec timer visuel"
      min_lines: 150
  key_links:
    - from: "apps/desktop/src/pages/Health.tsx"
      to: "apps/desktop/src/contexts/VaultContext.tsx"
      via: "useVault() saveHealthRecord, addGrowthEntry, addVaccineEntry"
      pattern: "saveHealthRecord|addGrowthEntry|addVaccineEntry"
    - from: "apps/desktop/src/pages/Routines.tsx"
      to: "apps/desktop/src/contexts/VaultContext.tsx"
      via: "useVault() saveRoutines, completeRoutineStep"
      pattern: "saveRoutines|completeRoutineStep"
---

<objective>
Creer les ecrans Health (suivi sante) et Routines (sequences visuelles) sur desktop.

Purpose: Health et Routines sont des ecrans complexes avec plusieurs sous-sections — regroupes car meme niveau de complexite.
Output: 4 fichiers (2 .tsx + 2 .css), ecrans fonctionnels.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-parit-mobile-desktop-analyser-et-combler-les-carts-fonctionnels-entre-app-mobile-et-vision-desktop-pour-atteindre-une-parit-quasi-identique/14-01-SUMMARY.md
@apps/desktop/src/pages/Budget.tsx
@apps/desktop/src/contexts/VaultContext.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ecran Health desktop — croissance, vaccins, historique</name>
  <files>apps/desktop/src/pages/Health.tsx, apps/desktop/src/pages/Health.css</files>
  <read_first>
    - apps/desktop/src/pages/Challenges.tsx (pattern page desktop avec onglets/segments — reference SegmentedControl)
    - apps/desktop/src/pages/Challenges.css (pattern CSS avec segments)
    - apps/desktop/src/contexts/VaultContext.tsx (signatures mutations health)
    - app/(tabs)/health.tsx (source de verite mobile — 1123 lignes, structure des donnees sante, onglets, formulaires)
    - apps/desktop/src/components/ui/SegmentedControl.tsx (composant existant pour les onglets)
  </read_first>
  <action>
Creer `Health.tsx` et `Health.css` en suivant le pattern Challenges.tsx (page avec segments/onglets).

**Health.tsx :**
- Import `useVault` pour acceder a `healthRecords`, `activeProfile`, `profiles`, mutations health
- Import `SegmentedControl` pour les onglets
- Import types depuis `@family-vault/core`

**Structure de la page :**
1. Header avec titre "Sante" + selecteur de profil enfant (si plusieurs enfants)
2. SegmentedControl avec 3 onglets : "Croissance", "Vaccins", "Historique"

**Onglet Croissance :**
- Tableau des mesures (date, taille, poids) dans une GlassCard
- Bouton "Ajouter une mesure" ouvre un Modal avec champs : date, taille (cm), poids (kg)
- Hover-to-reveal supprimer sur chaque ligne per D-02

**Onglet Vaccins :**
- Liste des vaccins (nom, date, rappel) dans des GlassCard
- Bouton "Ajouter un vaccin" ouvre un Modal
- Hover-to-reveal supprimer per D-02

**Onglet Historique :**
- Liste chronologique des evenements medicaux (consultation, maladie, traitement)
- Bouton "Ajouter" ouvre un Modal avec champs : date, type, description, medecin
- Hover-to-reveal supprimer per D-02

**CSS :** Suivre le pattern Challenges.css — table responsive, segments, cards.
  </action>
  <verify>
    <automated>cd apps/desktop && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - apps/desktop/src/pages/Health.tsx existe et contient `export default function Health`
    - Health.tsx contient `SegmentedControl` avec au moins 3 segments
    - Health.tsx contient des Modal pour l'ajout de donnees
    - Health.tsx utilise des mutations health depuis useVault
    - Health.css existe et contient `.health-` classes
  </acceptance_criteria>
  <done>L'ecran Health affiche 3 onglets (croissance/vaccins/historique) avec CRUD et hover-to-reveal.</done>
</task>

<task type="auto">
  <name>Task 2: Ecran Routines desktop — sequences visuelles avec timers</name>
  <files>apps/desktop/src/pages/Routines.tsx, apps/desktop/src/pages/Routines.css</files>
  <read_first>
    - apps/desktop/src/pages/Tasks.tsx (pattern page desktop avec listes interactives — checkbox toggle)
    - apps/desktop/src/contexts/VaultContext.tsx (signatures mutations routines)
    - app/(tabs)/routines.tsx (source de verite mobile — 867 lignes, structure routines, timer, etapes)
  </read_first>
  <action>
Creer `Routines.tsx` et `Routines.css`.

**Routines.tsx :**
- Import `useVault` pour `routines`, `saveRoutines`, `completeRoutineStep`
- Import types depuis `@family-vault/core`

**Structure de la page :**
1. Header avec titre "Routines" + bouton "Creer une routine"
2. Liste des routines dans des GlassCard (ex: "Routine du matin", "Routine du soir")
3. Click sur une routine ouvre un panneau detail avec :
   - Liste des etapes ordonnees (icone + texte + timer duree)
   - Checkbox pour marquer chaque etape comme completee
   - Timer visuel par etape : barre de progression CSS qui s'anime sur la duree configuree (CSS transition width sur duree, pas Framer Motion)
   - Bouton play/pause pour le timer
4. Mode edition : reordonner les etapes (drag & drop HTML5 natif per D-02), ajouter/supprimer des etapes
5. Modal creation : nom routine, profil associe, liste d'etapes (nom + duree en minutes)

**Timer desktop :**
- Utiliser `setInterval` + state pour le countdown
- Barre de progression CSS : `transition: width {duree}s linear`
- Son optionnel : `new Audio()` pour le bip de fin (si le mobile en a un)

**CSS :** Progress bar animee, etapes avec padding et bordure gauche coloree, drag handle visible.
  </action>
  <verify>
    <automated>cd apps/desktop && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - apps/desktop/src/pages/Routines.tsx existe et contient `export default function Routines`
    - Routines.tsx contient `setInterval` ou `setTimeout` pour le timer
    - Routines.tsx contient gestion drag & drop (`onDragStart` ou `onDrop` ou `draggable`)
    - Routines.tsx utilise mutations routines depuis useVault
    - Routines.css existe et contient une animation/transition pour la barre de progression
  </acceptance_criteria>
  <done>L'ecran Routines affiche les sequences visuelles avec timers animes, drag-to-reorder, et CRUD.</done>
</task>

</tasks>

<verification>
1. `cd apps/desktop && npx tsc --noEmit` passe
2. Les 4 fichiers existent
3. Health.tsx contient SegmentedControl
4. Routines.tsx contient timer logic (setInterval)
</verification>

<success_criteria>
Les ecrans Health et Routines sont fonctionnels avec CRUD, interactions desktop per D-02, et `tsc --noEmit` passe.
</success_criteria>

<output>
After completion, create `.planning/phases/14-parit-mobile-desktop-analyser-et-combler-les-carts-fonctionnels-entre-app-mobile-et-vision-desktop-pour-atteindre-une-parit-quasi-identique/14-03-SUMMARY.md`
</output>
