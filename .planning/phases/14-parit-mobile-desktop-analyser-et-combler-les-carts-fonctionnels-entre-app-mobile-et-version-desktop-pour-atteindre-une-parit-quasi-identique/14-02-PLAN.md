---
phase: 14-parite-mobile-desktop
plan: 02
type: execute
wave: 2
depends_on: [14-01]
files_modified:
  - apps/desktop/src/pages/RDV.tsx
  - apps/desktop/src/pages/RDV.css
  - apps/desktop/src/pages/Notes.tsx
  - apps/desktop/src/pages/Notes.css
autonomous: true
requirements: [PAR-01, PAR-02]

must_haves:
  truths:
    - "L'ecran RDV desktop affiche la liste des rendez-vous, permet d'en creer, modifier et supprimer"
    - "L'ecran Notes desktop affiche la liste des notes, permet d'en creer, modifier et supprimer"
    - "Les interactions hover-to-delete et raccourci clavier Delete fonctionnent sur RDV et Notes"
  artifacts:
    - path: "apps/desktop/src/pages/RDV.tsx"
      provides: "Ecran RDV avec CRUD complet"
      min_lines: 150
    - path: "apps/desktop/src/pages/Notes.tsx"
      provides: "Ecran Notes avec CRUD complet"
      min_lines: 150
  key_links:
    - from: "apps/desktop/src/pages/RDV.tsx"
      to: "apps/desktop/src/contexts/VaultContext.tsx"
      via: "useVault() addRDV, updateRDV, deleteRDV"
      pattern: "addRDV|updateRDV|deleteRDV"
    - from: "apps/desktop/src/pages/Notes.tsx"
      to: "apps/desktop/src/contexts/VaultContext.tsx"
      via: "useVault() addNote, updateNote, deleteNote"
      pattern: "addNote|updateNote|deleteNote"
---

<objective>
Creer les ecrans RDV et Notes sur desktop avec CRUD complet, en utilisant les mutations VaultContext ajoutees dans le plan 01.

Purpose: RDV et Notes sont des ecrans CRUD classiques, regroupes car meme pattern (liste + editeur + suppression).
Output: 4 fichiers (2 .tsx + 2 .css), ecrans fonctionnels.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-parit-mobile-desktop-analyser-et-combler-les-carts-fonctionnels-entre-app-mobile-et-version-desktop-pour-atteindre-une-parit-quasi-identique/14-01-SUMMARY.md
@apps/desktop/src/pages/Budget.tsx
@apps/desktop/src/pages/Budget.css
@apps/desktop/src/contexts/VaultContext.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ecran RDV desktop — CRUD complet</name>
  <files>apps/desktop/src/pages/RDV.tsx, apps/desktop/src/pages/RDV.css</files>
  <read_first>
    - apps/desktop/src/pages/Budget.tsx (pattern de page desktop avec CRUD — reference layout, imports, GlassCard)
    - apps/desktop/src/pages/Budget.css (pattern CSS desktop)
    - apps/desktop/src/contexts/VaultContext.tsx (signatures addRDV, updateRDV, deleteRDV)
    - app/(tabs)/rdv.tsx (source de verite mobile — structure des donnees, logique metier, champs du formulaire)
  </read_first>
  <action>
Creer `RDV.tsx` et `RDV.css` en suivant le pattern de Budget.tsx (layout, imports, GlassCard).

**RDV.tsx :**
- Import `useVault` pour acceder a `rdvs`, `addRDV`, `updateRDV`, `deleteRDV`
- Import types `RDV` depuis `@family-vault/core`
- Import composants UI : `GlassCard`, `Modal`, `Button`, `SearchInput`

**Structure de la page :**
1. Header avec titre "Rendez-vous" + bouton "Ajouter" + SearchInput pour filtrer
2. Liste des RDV dans des GlassCard, groupes par mois (format JJ/MM/AAAA per convention)
3. Chaque item RDV affiche : titre, date, heure, lieu, notes, et un badge couleur par categorie
4. Hover sur un item revele un bouton modifier (icone crayon) et un bouton supprimer (icone corbeille) — per D-02
5. Raccourci clavier Delete sur item selectionne — per D-02
6. Click sur un item ouvre un Modal d'edition avec les champs : titre, date (input date), heure debut, heure fin, lieu, notes, categorie (select), recurrence
7. Bouton "Ajouter" ouvre le meme Modal en mode creation
8. Confirmation avant suppression (window.confirm)

**Interactions desktop per D-02 :**
- Hover-to-reveal actions (bouton modifier + supprimer apparaissent au hover via CSS)
- Raccourci Delete : `useEffect` avec `keydown` listener, supprime le RDV selectionne
- Click = tap (standard)

**CSS :** Suivre le pattern Budget.css — classes `.rdv-*`, hover transitions, responsive grid.
  </action>
  <verify>
    <automated>cd apps/desktop && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - apps/desktop/src/pages/RDV.tsx existe et contient `export default function RDV`
    - RDV.tsx contient `useVault()` et utilise `addRDV`, `updateRDV`, `deleteRDV`
    - RDV.tsx contient un `useEffect` avec `keydown` et `Delete` (raccourci clavier per D-02)
    - RDV.tsx contient un composant Modal pour l'edition
    - RDV.css existe et contient `.rdv-` classes
    - RDV.css contient une regle `:hover` pour les boutons d'action (per D-02)
  </acceptance_criteria>
  <done>L'ecran RDV desktop affiche les rendez-vous, permet CRUD, avec hover-to-reveal et raccourci Delete.</done>
</task>

<task type="auto">
  <name>Task 2: Ecran Notes desktop — CRUD complet</name>
  <files>apps/desktop/src/pages/Notes.tsx, apps/desktop/src/pages/Notes.css</files>
  <read_first>
    - apps/desktop/src/pages/RDV.tsx (pattern vient d'etre cree — reutiliser le meme layout)
    - apps/desktop/src/contexts/VaultContext.tsx (signatures addNote, updateNote, deleteNote)
    - app/(tabs)/notes.tsx (source de verite mobile — structure des notes, import web via defuddle si present)
  </read_first>
  <action>
Creer `Notes.tsx` et `Notes.css` en suivant le pattern de RDV.tsx (meme structure CRUD).

**Notes.tsx :**
- Import `useVault` pour acceder a `notes`, `addNote`, `updateNote`, `deleteNote`
- Import type `Note` depuis `@family-vault/core`

**Structure de la page :**
1. Header avec titre "Notes" + bouton "Nouvelle note" + SearchInput pour filtrer
2. Layout deux colonnes : liste des notes a gauche, editeur a droite (pattern master-detail adapte au desktop — plus ergonomique qu'une modale pour les notes)
3. Liste des notes dans des GlassCard : titre, extrait (2 premieres lignes du contenu), date de modification
4. Hover-to-reveal actions (modifier + supprimer) per D-02
5. Raccourci Delete sur note selectionnee per D-02
6. Editeur : textarea plein ecran avec titre editable en input text, contenu en textarea, bouton sauvegarder
7. Nouvelle note : cree un objet Note vide, ouvre l'editeur

**CSS :** Layout grid 2 colonnes (`grid-template-columns: 300px 1fr`), responsive (1 colonne sur petit ecran).
  </action>
  <verify>
    <automated>cd apps/desktop && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - apps/desktop/src/pages/Notes.tsx existe et contient `export default function Notes`
    - Notes.tsx contient `useVault()` et utilise `addNote`, `updateNote`, `deleteNote`
    - Notes.tsx contient un layout master-detail (grid ou flex avec 2 zones)
    - Notes.tsx contient textarea pour l'edition de contenu
    - Notes.css existe et contient `grid-template-columns` ou equivalent 2 colonnes
    - Notes.css contient une regle `:hover` pour les boutons d'action
  </acceptance_criteria>
  <done>L'ecran Notes desktop affiche les notes en master-detail, permet CRUD, avec hover-to-reveal et raccourci Delete.</done>
</task>

</tasks>

<verification>
1. `cd apps/desktop && npx tsc --noEmit` passe
2. Les 4 fichiers existent : RDV.tsx, RDV.css, Notes.tsx, Notes.css
3. `grep -c "addRDV\|deleteRDV" apps/desktop/src/pages/RDV.tsx` >= 2
4. `grep -c "addNote\|deleteNote" apps/desktop/src/pages/Notes.tsx` >= 2
</verification>

<success_criteria>
Les ecrans RDV et Notes sont fonctionnels avec CRUD complet, interactions hover/delete per D-02, et `tsc --noEmit` passe.
</success_criteria>

<output>
After completion, create `.planning/phases/14-parit-mobile-desktop-analyser-et-combler-les-carts-fonctionnels-entre-app-mobile-et-version-desktop-pour-atteindre-une-parit-quasi-identique/14-02-SUMMARY.md`
</output>
