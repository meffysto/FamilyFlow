---
phase: 14-parite-mobile-desktop
plan: 09
type: execute
wave: 2
depends_on: [14-01]
files_modified:
  - apps/desktop/src/components/companion/CompanionWidget.tsx
  - apps/desktop/src/components/companion/CompanionWidget.css
  - apps/desktop/src/components/companion/CompanionPicker.tsx
  - apps/desktop/src/components/companion/CompanionPicker.css
  - apps/desktop/src/pages/Tree.tsx
  - apps/desktop/src/pages/Tree.css
autonomous: true
requirements: [PAR-01, PAR-02]

must_haves:
  truths:
    - "Le companion system desktop affiche le compagnon actif avec son mood, ses messages contextuels et reactions au click (per D-05)"
    - "Le companion picker permet de choisir son compagnon parmi ceux debloques (per D-05)"
    - "Les sagas immersives (visiteur pixel, dialogues interactifs) sont jouables sur desktop (per D-05)"
    - "Les evenements saisonniers sont affiches avec le meme contenu que le mobile (per D-05)"
    - "Le tech tree / building upgrades est interactif sur desktop (per D-05)"
    - "Tous les textes UI utilisent useTranslation (per D-07)"
  artifacts:
    - path: "apps/desktop/src/components/companion/CompanionWidget.tsx"
      provides: "Widget compagnon avec mood, messages IA, avatar mini"
      min_lines: 100
    - path: "apps/desktop/src/components/companion/CompanionPicker.tsx"
      provides: "Selecteur de compagnon"
      min_lines: 80
    - path: "apps/desktop/src/pages/Tree.tsx"
      provides: "Page Tree etendue avec sagas, evenements saisonniers, tech tree"
  key_links:
    - from: "apps/desktop/src/components/companion/CompanionWidget.tsx"
      to: "@family-vault/core"
      via: "import companion engine functions"
      pattern: "import.*from.*mascot|companion"
    - from: "apps/desktop/src/pages/Tree.tsx"
      to: "apps/desktop/src/contexts/VaultContext.tsx"
      via: "useVault() saveSagaProgress, saveEventProgress, gamiData"
      pattern: "saveSagaProgress|saveEventProgress"
---

<objective>
Implementer les features gamification manquantes sur desktop per D-05 : companion system complet (picker, mood, messages IA, avatar mini), sagas immersives, evenements saisonniers, tech tree / building upgrades.

Purpose: D-05 exige la parite gamification complete. Le plan 06 couvre le loot box, ce plan couvre le reste.
Output: Companion widget reutilisable + extension de Tree.tsx avec sagas/events/tech tree.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-parit-mobile-desktop-analyser-et-combler-les-carts-fonctionnels-entre-app-mobile-et-version-desktop-pour-atteindre-une-parit-quasi-identique/14-01-SUMMARY.md
@apps/desktop/src/pages/Tree.tsx
@apps/desktop/src/pages/Tree.css
@apps/desktop/src/contexts/VaultContext.tsx
@lib/mascot/companion-engine.ts
@lib/mascot/sagas-engine.ts
@lib/mascot/farm-engine.ts
@lib/mascot/seasons.ts
@app/(tabs)/tree.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Companion system desktop — widget, picker, messages IA</name>
  <files>apps/desktop/src/components/companion/CompanionWidget.tsx, apps/desktop/src/components/companion/CompanionWidget.css, apps/desktop/src/components/companion/CompanionPicker.tsx, apps/desktop/src/components/companion/CompanionPicker.css</files>
  <read_first>
    - lib/mascot/companion-engine.ts (engine compagnon — fonctions pures disponibles)
    - app/(tabs)/tree.tsx (source de verite mobile — chercher les sections companion: picker, mood display, messages IA, reactions click)
    - apps/desktop/src/contexts/VaultContext.tsx (gamiData, mutations companion si presentes)
    - apps/desktop/src/pages/Tree.tsx (page Tree desktop actuelle — verifier ce qui existe deja pour le companion)
    - locales/fr/gamification.json (traductions gamification disponibles)
  </read_first>
  <action>
Creer le systeme companion desktop per D-05. Le companion est un widget reutilisable qui peut etre affiche sur Tree.tsx et potentiellement sur le Dashboard.

**CompanionWidget.tsx :**
- Import `useVault` pour `gamiData`, `activeProfile`
- Import `useTranslation` depuis `react-i18next` per D-07 : `const { t } = useTranslation('gamification');`
- Import les fonctions companion depuis `@family-vault/core` ou `lib/mascot/companion-engine.ts` (getCompanionMood, getContextualMessage, etc.)
- Structure :
  1. Avatar du compagnon actif (image/SVG ou pixel art placeholder)
  2. Indicateur de mood (content, neutre, triste) avec icone/couleur
  3. Bulle de message contextuel (le compagnon reagit au contexte : taches completees, streaks, etc.)
  4. Click sur le compagnon declenche une reaction animee (Framer Motion scale bounce + message aleatoire)
  5. Bonus XP affiche si le compagnon donne un bonus
- Animation Framer Motion per D-03 :
  - Click reaction : `whileTap={{ scale: 0.9 }}` + message bulle qui apparait avec `AnimatePresence`
  - Mood transition : `animate={{ ... }}` sur le changement de mood
- Textes via `t()` per D-07 — aucun texte hardcode

**CompanionPicker.tsx :**
- Modal ou panneau lateral pour choisir un compagnon parmi ceux debloques
- Import `useTranslation` per D-07
- Grille de compagnons disponibles : icone, nom, bonus, etat (debloque/verrouille)
- Click sur un compagnon debloque le selectionne comme actif
- Compagnons verrouilles affiches en grayscale avec condition de deblocage
- Sauvegarder le choix via mutation VaultContext (verifier si `setActiveCompanion` existe, sinon utiliser `saveSagaProgress` ou mutation generique gamiData)

**CSS :**
- CompanionWidget : position flottante ou integree, bulle de dialogue avec fleche CSS, avatar rond
- CompanionPicker : grille responsive, cartes avec bordure couleur, hover scale
  </action>
  <verify>
    <automated>cd apps/desktop && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - CompanionWidget.tsx existe et contient `export` (default ou named)
    - CompanionWidget.tsx importe depuis companion-engine ou @family-vault/core
    - CompanionWidget.tsx contient `useTranslation` (per D-07)
    - CompanionWidget.tsx contient `motion` ou `AnimatePresence` de framer-motion (per D-03)
    - CompanionPicker.tsx existe et contient une grille de compagnons
    - CompanionPicker.tsx contient `useTranslation` (per D-07)
    - CompanionWidget.css et CompanionPicker.css existent
  </acceptance_criteria>
  <done>Le companion widget affiche le compagnon avec mood, messages et reactions click. Le picker permet de choisir un compagnon.</done>
</task>

<task type="auto">
  <name>Task 2: Sagas immersives, evenements saisonniers, tech tree dans Tree.tsx</name>
  <files>apps/desktop/src/pages/Tree.tsx, apps/desktop/src/pages/Tree.css</files>
  <read_first>
    - apps/desktop/src/pages/Tree.tsx (version desktop actuelle — 82K, verifier ce qui existe deja)
    - apps/desktop/src/pages/Tree.css (styles actuels)
    - lib/mascot/sagas-engine.ts (engine sagas — fonctions pures)
    - lib/mascot/seasons.ts (engine saisons — evenements saisonniers)
    - lib/mascot/farm-engine.ts (engine ferme — buildings, upgrades)
    - app/(tabs)/tree.tsx (source de verite mobile — sections sagas, events, tech tree)
    - apps/desktop/src/contexts/VaultContext.tsx (saveSagaProgress, saveEventProgress, gamiData)
    - apps/desktop/src/components/companion/CompanionWidget.tsx (vient d'etre cree — integrer dans Tree)
  </read_first>
  <action>
Etendre Tree.tsx avec les features gamification manquantes per D-05. NE PAS reecrire le fichier — ajouter les sections manquantes.

**Import `useTranslation` per D-07** : `const { t } = useTranslation('gamification');` — Tous les nouveaux textes doivent utiliser `t()`.

**1. Integrer CompanionWidget :**
- Importer `CompanionWidget` depuis `../components/companion/CompanionWidget`
- L'afficher dans une position pertinente (coin de la ferme, ou section dediee)

**2. Section Sagas immersives :**
- Lire les sagas disponibles depuis `lib/mascot/sagas-engine.ts` (fonctions pures)
- Afficher les sagas actives et disponibles dans un panneau/section
- Chaque saga : titre, progression, description, bouton "Continuer" ou "Commencer"
- Dialogue interactif : quand une saga est active, afficher le dialogue du visiteur pixel avec choix de reponse
- Sauvegarder la progression via `saveSagaProgress()` de VaultContext
- Animations : Framer Motion pour l'apparition du visiteur (per D-03)

**3. Section Evenements saisonniers :**
- Lire l'evenement saisonnier actif depuis `lib/mascot/seasons.ts`
- Afficher banniere/section avec : nom de l'evenement, duree restante, progression, recompenses
- Quetes de l'evenement : liste avec checkboxes/progression
- Sauvegarder la progression via `saveEventProgress()` de VaultContext

**4. Section Tech tree / Building upgrades :**
- Lire l'arbre de buildings depuis farm-engine ou types associes
- Afficher une grille de buildings avec niveaux d'upgrade
- Chaque building : icone pixel, nom, niveau actuel, cout upgrade, bouton "Ameliorer"
- Buildings verrouilles : grayscale, condition affichee
- Click "Ameliorer" consomme des ressources et appelle la mutation appropriee
- Animations CSS : transition scale au hover, glow sur buildings max level

**CSS additions :**
- Section sagas : bulle de dialogue avec fleche, fond semi-transparent
- Section events : banniere avec gradient saisonnier, progress bar
- Section buildings : grille responsive, cartes avec bordure par type
  </action>
  <verify>
    <automated>cd apps/desktop && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - Tree.tsx importe CompanionWidget
    - Tree.tsx contient `saveSagaProgress` ou reference aux sagas
    - Tree.tsx contient `saveEventProgress` ou reference aux evenements saisonniers
    - Tree.tsx contient une section building/tech tree avec upgrades
    - Tree.tsx contient `useTranslation` (per D-07)
    - Tree.css contient de nouvelles classes pour sagas, events ou buildings
  </acceptance_criteria>
  <done>Tree.tsx integre le companion widget, les sagas immersives, les evenements saisonniers et le tech tree per D-05.</done>
</task>

</tasks>

<verification>
1. `cd apps/desktop && npx tsc --noEmit` passe
2. CompanionWidget et CompanionPicker existent dans components/companion/
3. Tree.tsx reference sagas, events et buildings
4. `grep -c "useTranslation" apps/desktop/src/components/companion/CompanionWidget.tsx apps/desktop/src/pages/Tree.tsx` retourne >= 1 pour chaque fichier
</verification>

<success_criteria>
Le companion system est complet (widget, picker, mood, messages IA), les sagas immersives sont jouables, les evenements saisonniers sont affiches, le tech tree est interactif — le tout per D-05 et D-07.
</success_criteria>

<output>
After completion, create `.planning/phases/14-parit-mobile-desktop-analyser-et-combler-les-carts-fonctionnels-entre-app-mobile-et-version-desktop-pour-atteindre-une-parit-quasi-identique/14-09-SUMMARY.md`
</output>
