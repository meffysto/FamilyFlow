---
phase: 14-parite-mobile-desktop
plan: 04
type: execute
wave: 2
depends_on: [14-01]
files_modified:
  - apps/desktop/src/pages/Skills.tsx
  - apps/desktop/src/pages/Skills.css
  - apps/desktop/src/pages/Stats.tsx
  - apps/desktop/src/pages/Stats.css
autonomous: true
requirements: [PAR-01]

must_haves:
  truths:
    - "L'ecran Skills affiche l'arbre RPG de competences enfants avec deblocage"
    - "L'ecran Stats affiche 6 visualisations de donnees avec recharts"
  artifacts:
    - path: "apps/desktop/src/pages/Skills.tsx"
      provides: "Arbre de competences RPG interactif"
      min_lines: 150
    - path: "apps/desktop/src/pages/Stats.tsx"
      provides: "6 graphiques statistiques avec recharts"
      min_lines: 200
  key_links:
    - from: "apps/desktop/src/pages/Stats.tsx"
      to: "recharts"
      via: "import BarChart, LineChart, ResponsiveContainer"
      pattern: "import.*from 'recharts'"
    - from: "apps/desktop/src/pages/Skills.tsx"
      to: "apps/desktop/src/contexts/VaultContext.tsx"
      via: "useVault() unlockSkill"
      pattern: "unlockSkill"
---

<objective>
Creer les ecrans Skills (arbre RPG competences) et Stats (visualisations donnees) sur desktop.

Purpose: Skills et Stats sont des ecrans principalement de lecture/visualisation — regroupes car meme complexite.
Output: 4 fichiers (2 .tsx + 2 .css), ecrans fonctionnels.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-parit-mobile-desktop-analyser-et-combler-les-carts-fonctionnels-entre-app-mobile-et-version-desktop-pour-atteindre-une-parit-quasi-identique/14-01-SUMMARY.md
@apps/desktop/src/pages/Budget.tsx
@apps/desktop/src/contexts/VaultContext.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ecran Skills desktop — arbre RPG competences</name>
  <files>apps/desktop/src/pages/Skills.tsx, apps/desktop/src/pages/Skills.css</files>
  <read_first>
    - app/(tabs)/skills.tsx (source de verite mobile — 576 lignes, structure arbre competences, categories, niveaux)
    - apps/desktop/src/contexts/VaultContext.tsx (signature unlockSkill)
    - apps/desktop/src/pages/Challenges.tsx (pattern page desktop avec segments)
    - locales/fr/skills.json (traductions competences)
  </read_first>
  <action>
Creer `Skills.tsx` et `Skills.css`.

**Skills.tsx :**
- Import `useVault` pour `activeProfile`, `profiles`, `unlockSkill`
- Import `SegmentedControl` pour les categories
- Import types et engine skills depuis `@family-vault/core` (SKILL_TREE, SkillNode, etc.)

**Structure de la page :**
1. Header avec titre "Competences" + selecteur de profil enfant
2. SegmentedControl pour les categories de competences (academique, artistique, sportif, social, etc. — lire les categories depuis SKILL_TREE de @family-vault/core)
3. Grille de competences dans des GlassCard :
   - Chaque competence = une carte avec icone, nom, niveau actuel (etoiles ou barre de progression)
   - Etat visuel : verrouille (grise, cadenas), debloque (couleur), max (dore)
   - Click sur une competence debloquee montre le detail : description, progression, bouton "Debloquer niveau suivant" si conditions remplies
4. Bouton debloquer consomme des ressources (XP/coins) via `unlockSkill(skillId)`
5. Animations CSS : transition scale au hover sur les cartes, opacity transition pour les etats

**CSS :** Grille responsive (auto-fill, minmax(180px, 1fr)), cartes avec bordures colorees par categorie, etat verrouille en grayscale + opacity 0.5.
  </action>
  <verify>
    <automated>cd apps/desktop && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - apps/desktop/src/pages/Skills.tsx existe et contient `export default function Skills`
    - Skills.tsx importe depuis `@family-vault/core` (SKILL_TREE ou equivalent)
    - Skills.tsx contient `unlockSkill` depuis useVault
    - Skills.tsx contient un selecteur de profil
    - Skills.css existe et contient `grid` ou `grid-template` pour la grille
    - Skills.css contient `grayscale` ou `opacity` pour l'etat verrouille
  </acceptance_criteria>
  <done>L'ecran Skills affiche l'arbre de competences RPG avec deblocage interactif et etats visuels.</done>
</task>

<task type="auto">
  <name>Task 2: Ecran Stats desktop — 6 visualisations recharts</name>
  <files>apps/desktop/src/pages/Stats.tsx, apps/desktop/src/pages/Stats.css</files>
  <read_first>
    - app/(tabs)/stats.tsx (source de verite mobile — 328 lignes, 6 types de stats, structure des donnees)
    - apps/desktop/src/contexts/VaultContext.tsx (donnees disponibles pour calcul stats)
    - apps/desktop/src/pages/Budget.tsx (pattern page desktop)
  </read_first>
  <action>
Creer `Stats.tsx` et `Stats.css` avec recharts.

**Stats.tsx :**
- Import depuis recharts : `BarChart, Bar, LineChart, Line, PieChart, Pie, Cell, XAxis, YAxis, Tooltip, Legend, ResponsiveContainer`
- Import `useVault` pour acceder a `tasks`, `defis`, `meals`, `moods`, `activeProfile`, `profiles`
- Import `GlassCard` pour le layout

**Structure de la page — 6 visualisations dans une grille 2x3 :**

1. **Taches completees par semaine** (BarChart) : agreger `tasks` par semaine, compter les completees
2. **Repartition des defis** (PieChart) : compter defis par statut (en cours, complete, abandonne)
3. **Humeurs sur 30 jours** (LineChart) : tracer `moods` en chronologique, valeur numerique par humeur
4. **Repas planifies par semaine** (BarChart) : compter `meals` non vides par jour
5. **XP gagne par jour** (LineChart) : si gamiData disponible, tracer l'evolution XP
6. **Streak de jours consecutifs** (BarChart/indicateur) : afficher le streak actuel et l'historique

Chaque visualisation dans une GlassCard avec titre et ResponsiveContainer (height 200).

**Couleurs :** Utiliser les CSS variables (`var(--primary)`, `var(--accent)`, etc.) pour les couleurs des graphiques. Passer via `getComputedStyle(document.documentElement).getPropertyValue('--primary')` ou hardcoder les couleurs du theme par defaut.

**CSS :** Grille 2 colonnes (`grid-template-columns: 1fr 1fr`), responsive 1 colonne sur petit ecran, GlassCard padding uniforme.
  </action>
  <verify>
    <automated>cd apps/desktop && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - apps/desktop/src/pages/Stats.tsx existe et contient `export default function Stats`
    - Stats.tsx contient `import { BarChart` ou `import { LineChart` depuis 'recharts'
    - Stats.tsx contient au moins 3 composants recharts differents (BarChart, LineChart, PieChart)
    - Stats.tsx contient `ResponsiveContainer` pour le responsive
    - Stats.tsx contient au moins 6 GlassCard (une par visualisation)
    - Stats.css existe et contient `grid-template-columns`
  </acceptance_criteria>
  <done>L'ecran Stats affiche 6 visualisations de donnees avec recharts dans une grille responsive.</done>
</task>

</tasks>

<verification>
1. `cd apps/desktop && npx tsc --noEmit` passe
2. Les 4 fichiers existent
3. Stats.tsx importe depuis 'recharts'
4. Skills.tsx utilise unlockSkill
</verification>

<success_criteria>
Les ecrans Skills et Stats sont fonctionnels avec toutes les visualisations et interactions, `tsc --noEmit` passe.
</success_criteria>

<output>
After completion, create `.planning/phases/14-parit-mobile-desktop-analyser-et-combler-les-carts-fonctionnels-entre-app-mobile-et-vision-desktop-pour-atteindre-une-parit-quasi-identique/14-04-SUMMARY.md`
</output>
