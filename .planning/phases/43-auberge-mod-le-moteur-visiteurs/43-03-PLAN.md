---
phase: 43-auberge-mod-le-moteur-visiteurs
plan: 03
type: execute
wave: 2
depends_on: [43-01, 43-02]
files_modified:
  - lib/mascot/auberge-engine.ts
  - lib/__tests__/auberge-engine.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "lib/mascot/auberge-engine.ts exporte 12 fonctions pures + helpers serialize/parse"
    - "shouldSpawnVisitor refuse spawn si lastSpawnAt < 6h ou cap actifs atteint"
    - "spawnVisitor instancie quantités random dans [min,max], snapshot rewardCoins"
    - "deliverVisitor retourne { state, deductedItems, reward, reputationDelta: 1 } sans muter"
    - "expireVisitors décrémente réputation avec floor 0"
    - "Round-trip serializeAuberge/parseAuberge lossless avec ISO timestamps"
    - "Test gating Comtesse : isVisitorUnlocked false si totalReputation < 15"
    - "Anti-spam : reputation.lastSeenAt + 24h respecté"
    - "Cap actifs : 1/2/3 selon stade pousse/arbuste/arbre+"
    - "npx jest lib/__tests__/auberge-engine.test.ts passe"
  artifacts:
    - path: "lib/mascot/auberge-engine.ts"
      provides: "Moteur pur Auberge (12 fonctions + serialize/parse)"
      contains: "spawnVisitor"
    - path: "lib/__tests__/auberge-engine.test.ts"
      provides: "Suite Jest exhaustive (spawn/deliver/dismiss/expire/round-trip/gating)"
  key_links:
    - from: "lib/mascot/auberge-engine.ts"
      to: "lib/mascot/visitor-catalog.ts (VISITOR_CATALOG)"
      via: "import { VISITOR_CATALOG }"
      pattern: "VISITOR_CATALOG"
    - from: "lib/mascot/auberge-engine.ts"
      to: "lib/mascot/types.ts (AubergeState, ActiveVisitor)"
      via: "import type"
      pattern: "AubergeState"
---

<objective>
Livrer `lib/mascot/auberge-engine.ts` : moteur 100% pur (sans React, sans I/O) qui implémente les 12 fonctions verrouillées par CONTEXT.md (spawn/deliver/dismiss/expire/canDeliver/getRemaining/getActive/getReputation/getTotal/isUnlocked/getEligible/shouldSpawn) + helpers de sérialisation `serializeAuberge` / `parseAuberge` (utilisés par Plan 04 pour persister l'AubergeState dans les chaînes opaques du Plan 02). Suite Jest exhaustive.

Purpose: Le cœur métier de l'Auberge. Une fois ce plan vert, toute la logique est prouvée par les tests — le hook (Plan 04) n'est qu'une couche d'orchestration React.

Output: 1 moteur pur testé, 1 suite Jest passante, 0 dépendance externe.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/43-auberge-mod-le-moteur-visiteurs/43-CONTEXT.md
@.planning/phases/43-auberge-mod-le-moteur-visiteurs/43-RESEARCH.md
@lib/mascot/types.ts
@lib/mascot/visitor-catalog.ts
@lib/mascot/expedition-engine.ts
@lib/mascot/farm-engine.ts
@lib/mascot/building-engine.ts
@lib/mascot/craft-engine.ts
@CLAUDE.md
</context>

<interfaces>
**Signatures verrouillées (CONTEXT.md) :**

```typescript
import type {
  AubergeState, ActiveVisitor, VisitorRequestItem, VisitorReputation,
  TreeStage, FarmInventory, HarvestInventory, CraftedItem,
} from './types';
import type { VisitorDefinition } from './visitor-catalog';
import { VISITOR_CATALOG } from './visitor-catalog';

// Helpers privés
function generateInstanceId(): string;
function pickWeighted<T>(items: T[], weights: number[]): T | null;
function estimatedSellValue(item: VisitorRequestItem): number;

// API publique
export function getEligibleVisitors(state: AubergeState, treeStage: TreeStage, totalReputation: number): VisitorDefinition[];
export function shouldSpawnVisitor(state: AubergeState, now: Date, treeStage: TreeStage): boolean;
export function spawnVisitor(state: AubergeState, treeStage: TreeStage, now: Date, totalReputation: number): { state: AubergeState; visitor: ActiveVisitor } | null;
export function canDeliver(visitor: ActiveVisitor, inventory: FarmInventory, harvestInv: HarvestInventory, craftedItems: CraftedItem[]): { ok: boolean; missing: VisitorRequestItem[] };
export function getRemainingMinutes(visitor: ActiveVisitor, now?: Date): number;
export function getActiveVisitors(state: AubergeState, now?: Date): ActiveVisitor[];
export function deliverVisitor(state: AubergeState, instanceId: string, inventories: { farm: FarmInventory; harvest: HarvestInventory; crafted: CraftedItem[] }, now: Date): { state: AubergeState; deductedItems: VisitorRequestItem[]; reward: { coins: number; loot?: string }; reputationDelta: 1 } | null;
export function dismissVisitor(state: AubergeState, instanceId: string, now: Date): { state: AubergeState };
export function expireVisitors(state: AubergeState, now: Date): { state: AubergeState; expired: ActiveVisitor[] };
export function getReputation(state: AubergeState, visitorId: string): number;
export function getTotalReputation(state: AubergeState): number;
export function isVisitorUnlocked(visitorId: string, state: AubergeState, treeStage: TreeStage): boolean;

// Persistance (pour Plan 04)
export function serializeAuberge(state: AubergeState): { visitors: string; reputations: string; lastSpawn?: string; totalDeliveries: number };
export function parseAuberge(serialized: { visitors?: string; reputations?: string; lastSpawn?: string; totalDeliveries?: number }): AubergeState;
```

**Constantes :**
- `SPAWN_COOLDOWN_HOURS = 6`
- `NPC_COOLDOWN_HOURS = 24`
- `CAP_BY_STAGE = { pousse: 1, arbuste: 2, arbre: 3, /* arbre+: 3 */ }`
- `RARITY_BONUS = { common: 1.0, uncommon: 1.15, rare: 1.4 }`
- `LOOT_CHANCE = { common: 0.08, uncommon: 0.18, rare: 0.35 }`
- `RARITY_WEIGHT = { common: 5, uncommon: 3, rare: 1 }` (inverse de rareté)
- `ARCHIVE_DAYS = 7`

**Format CSV (Claude's Discretion) :**

Choix recommandé (RESEARCH § Open Question 1) :
- `auberge_reputations` : CSV plat `visitorId:level:successCount:failureCount:lastSeenAt|...` — séparateur entre PNJ `|`, entre champs `:`, ISO reconstitué via `slice(...).join(':')` (pattern building-engine.ts:74).
- `auberge_visitors` : encoder chaque visiteur en JSON puis appliquer escape `,`→`|` / `:`→`§` (pattern `encodeModifiers` farm-engine.ts:228), séparateur entre visiteurs `||` (double pipe).

Le planner peut choisir une autre forme — tester le round-trip dans tous les cas.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implémenter auberge-engine.ts (12 fonctions + serialize/parse)</name>
  <files>lib/mascot/auberge-engine.ts</files>
  <read_first>
    - lib/mascot/expedition-engine.ts L570-637 (pattern time-bounded entity, isComplete/getRemaining)
    - lib/mascot/farm-engine.ts L228-274 (encodeModifiers escape pattern)
    - lib/mascot/farm-engine.ts L277-281 (getEffectiveHarvestReward)
    - lib/mascot/farm-engine.ts L310-317 (rollHarvestEvent — weighted random inline)
    - lib/mascot/building-engine.ts L33-98 (serializeBuildings + parseBuildings — CSV plat)
    - lib/mascot/craft-engine.ts L25-32 (BUILDING_RESOURCE_VALUE)
    - lib/mascot/craft-engine.ts L72+ (CRAFT_RECIPES.sellValue)
    - lib/wear-engine.ts L60 (UUID pattern)
    - .planning/phases/43-auberge-mod-le-moteur-visiteurs/43-RESEARCH.md (Pitfalls 1-9)
  </read_first>
  <behavior>
    Voir `<interfaces>` ci-dessus. Comportements clés :
    - **Pureté** : aucune fonction ne mute son argument `state` ; toutes retournent une nouvelle copie.
    - **Injection now** : toutes les fonctions time-bounded acceptent `now: Date = new Date()` (Pitfall 7).
    - **getActiveVisitors** : filtre `status === 'active' && now.getTime() < new Date(deadlineAt).getTime()` (Pitfall 5 — silencieux).
    - **spawnVisitor** : si aucun candidat éligible, retourne null. Sinon retourne `{ state: nouveauState, visitor: ActiveVisitor }` (la mise à jour de `state.visitors` + `state.lastSpawnAt` + `reputation.lastSeenAt` est faite ici).
    - **canDeliver** : vérifie chaque item du request dans l'inventaire approprié (`source: 'building'` → farmInventory ; `source: 'crop'` → harvestInv (somme des grades) ; `source: 'crafted'` → comptage des matchs dans craftedItems). Retourne items manquants.
    - **deliverVisitor** : si `canDeliver` ok → marque `status='delivered'`, incrémente réputation (+1 cap 5), incrémente `totalDeliveries`, roll loot selon rarity. Retourne `{ state, deductedItems, reward, reputationDelta: 1 }`. Si non livrable, retourne null.
    - **expireVisitors** : passe en `expired` les actifs dont la deadline est passée, applique -1 réputation (floor 0), incrémente `failureCount`.
    - **Archive** : `serializeAuberge` filtre les visiteurs `delivered`/`expired` plus vieux que 7 jours (compteur `totalDeliveries` les agrège déjà).
    - **Round-trip** : `parseAuberge(serializeAuberge(state)) === state` (modulo l'archive auto).
  </behavior>
  <action>
    Créer le fichier `lib/mascot/auberge-engine.ts` en suivant strictement le pattern des engines existants (expedition-engine.ts, building-engine.ts).

    Squelette :

    ```typescript
    /**
     * Moteur pur Auberge (Phase 43).
     * Sans React, sans I/O. Toutes les fonctions retournent un nouvel état (immutable).
     * Voir .planning/phases/43-auberge-mod-le-moteur-visiteurs/43-CONTEXT.md pour les règles.
     */
    import type {
      AubergeState, ActiveVisitor, VisitorRequestItem, VisitorReputation,
      TreeStage, FarmInventory, HarvestInventory, CraftedItem,
    } from './types';
    import type { VisitorDefinition } from './visitor-catalog';
    import { VISITOR_CATALOG } from './visitor-catalog';
    import { CROP_CATALOG } from './types';
    import { BUILDING_RESOURCE_VALUE, CRAFT_RECIPES } from './craft-engine';

    // Constantes module
    const SPAWN_COOLDOWN_MS = 6 * 60 * 60 * 1000;
    const NPC_COOLDOWN_MS = 24 * 60 * 60 * 1000;
    const ARCHIVE_DAYS_MS = 7 * 24 * 60 * 60 * 1000;
    const CAP_BY_STAGE: Record<TreeStage, number> = {
      graine: 0, pousse: 1, arbuste: 2, arbre: 3,
      // adapter aux noms réels de TreeStage observés dans types.ts
    };
    const RARITY_BONUS = { common: 1.0, uncommon: 1.15, rare: 1.4 } as const;
    const LOOT_CHANCE = { common: 0.08, uncommon: 0.18, rare: 0.35 } as const;
    const RARITY_WEIGHT = { common: 5, uncommon: 3, rare: 1 } as const;

    // Helpers privés
    function generateInstanceId(): string {
      return `vis_${Date.now()}_${Math.random().toString(36).slice(2, 6)}`;
    }

    function pickWeighted<T>(items: T[], weights: number[]): T | null {
      if (items.length === 0) return null;
      const total = weights.reduce((s, w) => s + w, 0);
      if (total <= 0) return null;
      let r = Math.random() * total;
      for (let i = 0; i < items.length; i++) {
        r -= weights[i];
        if (r < 0) return items[i];
      }
      return items[items.length - 1];
    }

    export function estimatedSellValue(item: VisitorRequestItem): number {
      switch (item.source) {
        case 'crop': {
          const c = CROP_CATALOG.find(x => x.id === item.itemId);
          return c?.harvestReward ?? 0;
        }
        case 'building':
          return (BUILDING_RESOURCE_VALUE as Record<string, number>)[item.itemId] ?? 0;
        case 'crafted':
          return CRAFT_RECIPES.find(r => r.id === item.itemId)?.sellValue ?? 0;
      }
    }

    // ... 12 fonctions publiques ...

    // ============================================================
    // Persistance — serialize / parse
    // ============================================================

    export function serializeAuberge(state: AubergeState): {
      visitors: string;
      reputations: string;
      lastSpawn?: string;
      totalDeliveries: number;
    } {
      // Filtre archive auto +7j
      // Encode visitors (JSON-escape pattern farm-engine.ts:228)
      // Encode reputations (CSV plat building-engine.ts:33)
      // ...
    }

    export function parseAuberge(serialized: {
      visitors?: string;
      reputations?: string;
      lastSpawn?: string;
      totalDeliveries?: number;
    }): AubergeState {
      // Decode tolérant — visiteurs avec visitorId catalogué inconnu = filtrés (Pitfall 6)
      // ...
    }
    ```

    **Implémenter chaque fonction publique selon CONTEXT.md** :

    1. `getEligibleVisitors` : filtre par `minTreeStage` (chronologie pousse < arbuste < arbre) + `unlockMinReputation ?? 0 ≤ totalReputation` + visiteur PAS déjà actif (Pitfall 4) + cooldown PNJ respecté.
    2. `shouldSpawnVisitor` : `(now - lastSpawnAt) >= 6h` ET `getActiveVisitors().length < CAP_BY_STAGE[treeStage]`.
    3. `spawnVisitor` : si `shouldSpawnVisitor` false → null. Sinon : `pickWeighted(eligible, eligible.map(v => RARITY_WEIGHT[v.rarity]))` → tirage template `pickWeighted(visitor.requestPool, requestPool.map(t => t.weight))` → résoudre `quantity = randInt(min, max)` → `baseValue = sum(qty × estimatedSellValue)` → `rewardCoins = round(baseValue × multiplier × RARITY_BONUS[rarity])`. Update state.visitors push, state.lastSpawnAt, reputation.lastSeenAt (créer reputation si absente).
    4. `canDeliver` : pour chaque item, vérifier l'inventaire correspondant (FarmInventory, HarvestInventory sommé sur grades, CraftedItem[] count). Cumuler items manquants.
    5. `deliverVisitor` : si !canDeliver.ok → null. Sinon : marquer status='delivered', +1 réputation cap 5, +1 totalDeliveries, roll loot (`Math.random() < LOOT_CHANCE[rarity]`) → si oui pick `preferredLoot` random ou skip. Retourner `deductedItems = visitor.request` pour que le hook applique la déduction.
    6. `dismissVisitor` : retire le visiteur du tableau (ou marque dismissed selon préférence — CONTEXT.md ne précise pas — choisir : retire du tableau pour ne pas polluer). Update reputation.lastSeenAt = now (cooldown).
    7. `expireVisitors` : itère actifs, marque expired ceux dont `now >= deadlineAt`, applique `level = max(0, level - 1)` + `failureCount++` + `lastSeenAt = now`.
    8. `getReputation` / `getTotalReputation` / `isVisitorUnlocked` / `getRemainingMinutes` / `getActiveVisitors` : trivial (cf. interfaces).

    **Pitfalls à respecter** :
    - Pitfall 1 : ISO `:` dans CSV → `slice(...).join(':')`.
    - Pitfall 2 : floor 0 réputation.
    - Pitfall 3 : rewardCoins snapshot au spawn, JAMAIS recalculé.
    - Pitfall 4 : getEligibleVisitors filtre les déjà-actifs.
    - Pitfall 5 : getActiveVisitors silencieux sur deadline passée.
    - Pitfall 6 : parser tolérant aux visitorId inconnus.
    - Pitfall 7 : injection now testable.
    - Pitfall 8 : totalDeliveries jamais dérivé du tableau.

    **Vérifier les noms réels de TreeStage** dans `lib/mascot/types.ts` (peut être `'pousse' | 'arbuste' | 'arbre' | 'arbre_majeur'` ou autre — adapter `CAP_BY_STAGE`).
  </action>
  <verify>
    <automated>test -f lib/mascot/auberge-engine.ts &amp;&amp; grep -q "export function spawnVisitor" lib/mascot/auberge-engine.ts &amp;&amp; grep -q "export function deliverVisitor" lib/mascot/auberge-engine.ts &amp;&amp; grep -q "export function expireVisitors" lib/mascot/auberge-engine.ts &amp;&amp; grep -q "export function dismissVisitor" lib/mascot/auberge-engine.ts &amp;&amp; grep -q "export function canDeliver" lib/mascot/auberge-engine.ts &amp;&amp; grep -q "export function getEligibleVisitors" lib/mascot/auberge-engine.ts &amp;&amp; grep -q "export function shouldSpawnVisitor" lib/mascot/auberge-engine.ts &amp;&amp; grep -q "export function getActiveVisitors" lib/mascot/auberge-engine.ts &amp;&amp; grep -q "export function getReputation" lib/mascot/auberge-engine.ts &amp;&amp; grep -q "export function getTotalReputation" lib/mascot/auberge-engine.ts &amp;&amp; grep -q "export function isVisitorUnlocked" lib/mascot/auberge-engine.ts &amp;&amp; grep -q "export function getRemainingMinutes" lib/mascot/auberge-engine.ts &amp;&amp; grep -q "export function serializeAuberge" lib/mascot/auberge-engine.ts &amp;&amp; grep -q "export function parseAuberge" lib/mascot/auberge-engine.ts &amp;&amp; npx tsc --noEmit 2>&amp;1 | grep -c "error TS" | awk '{ exit !($1 <= 5) }'</automated>
  </verify>
  <acceptance_criteria>
    - 12 fonctions publiques + serialize + parse exportées (grep).
    - `npx tsc --noEmit` ne dégrade pas la liste pré-existante d'erreurs.
    - Aucun import de React, expo, file system.
    - Constantes module en haut du fichier.
  </acceptance_criteria>
  <done>
    Moteur Auberge complet, pur, prêt à être testé en Task 2 et consommé par Plan 04.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Suite Jest auberge-engine.test.ts exhaustive</name>
  <files>lib/__tests__/auberge-engine.test.ts</files>
  <read_first>
    - lib/__tests__/expedition-engine.test.ts (pattern moteur pur time-bounded)
    - lib/__tests__/wager-engine.test.ts (pattern factories makeProfile/makeTask)
    - lib/__tests__/companion-feed.test.ts L38-45 (pattern injection now)
    - lib/mascot/auberge-engine.ts après Task 1
  </read_first>
  <behavior>
    Couverture obligatoire (CONTEXT.md §Tests Jest) :
    - **Spawn** : eligibilité par stade, anti-spam même PNJ (24h), cooldown global (6h), cap actifs simultanés (1/2/3).
    - **Deliver** : déduction correcte des items, reward calculé, réputation +1 cap 5, gestion items manquants.
    - **Dismiss** : pas de pénalité réputation, cooldown enregistré.
    - **Expire** : passe à 'expired', -1 réputation, floor 0.
    - **Round-trip** : parse(serialize(state)) === state, edge cases (état vide, multiples visiteurs, ISO timezone).
    - **Réputation totale** + gating Comtesse à 15.
    - **Pitfall 4** : getEligibleVisitors filtre les déjà-actifs.
    - **Pitfall 5** : getActiveVisitors silencieux sur deadline passée.
  </behavior>
  <action>
    Créer `lib/__tests__/auberge-engine.test.ts` avec ≥ 15 tests organisés par describe blocks. Squelette :

    ```typescript
    import {
      getEligibleVisitors, shouldSpawnVisitor, spawnVisitor, canDeliver,
      deliverVisitor, dismissVisitor, expireVisitors, getActiveVisitors,
      getReputation, getTotalReputation, isVisitorUnlocked, getRemainingMinutes,
      serializeAuberge, parseAuberge,
    } from '../mascot/auberge-engine';
    import type { AubergeState, ActiveVisitor, VisitorReputation } from '../mascot/types';

    const emptyState = (): AubergeState => ({
      visitors: [], reputations: [], totalDeliveries: 0,
    });

    const makeReputation = (visitorId: string, level: number, lastSeenISO: string): VisitorReputation => ({
      visitorId, level, successCount: level, failureCount: 0, lastSeenAt: lastSeenISO,
    });

    const FIXED_NOW = new Date('2026-04-29T12:00:00Z');

    describe('shouldSpawnVisitor — cooldown global 6h', () => {
      it('refuse si lastSpawnAt < 6h', () => {
        const state: AubergeState = { ...emptyState(), lastSpawnAt: '2026-04-29T07:00:00Z' };
        expect(shouldSpawnVisitor(state, FIXED_NOW, 'pousse')).toBe(false);
      });
      it('autorise si lastSpawnAt >= 6h', () => {
        const state: AubergeState = { ...emptyState(), lastSpawnAt: '2026-04-29T06:00:00Z' };
        expect(shouldSpawnVisitor(state, FIXED_NOW, 'pousse')).toBe(true);
      });
      it('autorise si pas de lastSpawnAt', () => {
        expect(shouldSpawnVisitor(emptyState(), FIXED_NOW, 'pousse')).toBe(true);
      });
    });

    describe('shouldSpawnVisitor — cap actifs', () => {
      it('cap 1 sur pousse', () => { /* state avec 1 visiteur actif → false */ });
      it('cap 3 sur arbre', () => { /* state avec 3 visiteurs actifs → false */ });
    });

    describe('getEligibleVisitors — anti-spam PNJ 24h', () => {
      it('exclut PNJ vu il y a < 24h', () => {
        const state: AubergeState = {
          ...emptyState(),
          reputations: [makeReputation('hugo_boulanger', 1, '2026-04-29T00:00:00Z')], // -12h
        };
        const eligible = getEligibleVisitors(state, 'pousse', 0);
        expect(eligible.find(v => v.id === 'hugo_boulanger')).toBeUndefined();
      });
    });

    describe('isVisitorUnlocked — gating Comtesse', () => {
      it('bloque Comtesse si totalReputation < 15', () => {
        const state: AubergeState = {
          ...emptyState(),
          reputations: [makeReputation('hugo_boulanger', 5, '2026-04-01T00:00:00Z')],
        };
        // totalRep = 5 < 15
        expect(isVisitorUnlocked('comtesse', state, 'arbre')).toBe(false);
      });
      it('autorise Comtesse si totalReputation >= 15', () => {
        const state: AubergeState = {
          ...emptyState(),
          reputations: [
            makeReputation('hugo_boulanger', 5, '2026-04-01T00:00:00Z'),
            makeReputation('meme_lucette', 5, '2026-04-01T00:00:00Z'),
            makeReputation('yann_apiculteur', 5, '2026-04-01T00:00:00Z'),
          ],
        };
        // totalRep = 15
        expect(isVisitorUnlocked('comtesse', state, 'arbre')).toBe(true);
      });
    });

    describe('deliverVisitor', () => {
      it('items insuffisants → retourne null', () => { /* canDeliver false → null */ });
      it('items suffisants → state.totalDeliveries +1, reputation +1', () => { /* ... */ });
      it('réputation cap 5', () => { /* 5e livraison → reste 5 */ });
    });

    describe('dismissVisitor', () => {
      it('pas de pénalité réputation', () => { /* level inchangé */ });
      it('lastSeenAt mis à now', () => { /* cooldown */ });
    });

    describe('expireVisitors', () => {
      it('marque expired les actifs dont deadline passée', () => { /* ... */ });
      it('-1 réputation avec floor 0', () => {
        const state: AubergeState = {
          visitors: [{
            visitorId: 'hugo_boulanger', instanceId: 'vis_1', arrivedAt: '2026-04-26T12:00:00Z',
            deadlineAt: '2026-04-28T12:00:00Z', request: [], status: 'active', rewardCoins: 100,
          }],
          reputations: [makeReputation('hugo_boulanger', 0, '2026-04-26T12:00:00Z')],
          totalDeliveries: 0,
        };
        const result = expireVisitors(state, FIXED_NOW);
        expect(result.expired).toHaveLength(1);
        expect(result.state.reputations[0].level).toBe(0); // floor 0
      });
    });

    describe('getActiveVisitors — Pitfall 5 silencieux', () => {
      it('exclut visiteurs dont deadline passée mais status active', () => {
        const expiredButNotTicked: ActiveVisitor = {
          visitorId: 'hugo_boulanger', instanceId: 'vis_x', arrivedAt: '2026-04-26T12:00:00Z',
          deadlineAt: '2026-04-28T12:00:00Z', request: [], status: 'active', rewardCoins: 100,
        };
        const state: AubergeState = { ...emptyState(), visitors: [expiredButNotTicked] };
        expect(getActiveVisitors(state, FIXED_NOW)).toHaveLength(0);
      });
    });

    describe('Round-trip serializeAuberge / parseAuberge', () => {
      it('état vide round-trip', () => {
        const empty = emptyState();
        const s = serializeAuberge(empty);
        const parsed = parseAuberge(s);
        expect(parsed).toEqual(empty);
      });
      it('multiples visiteurs round-trip', () => { /* ... avec ISO timestamps complets ... */ });
      it('visitorId catalogue inconnu → filtré silencieusement (Pitfall 6)', () => {
        const s = { visitors: '...payload avec id inconnu...', reputations: '', totalDeliveries: 0 };
        expect(() => parseAuberge(s)).not.toThrow();
      });
    });
    ```

    **Compléter les tests indiqués `/* ... */`** avec des fixtures réalistes. Chaque `it` doit avoir au moins 1 expect.

    Lancer `npx jest lib/__tests__/auberge-engine.test.ts --no-coverage` — tous verts.
  </action>
  <verify>
    <automated>test -f lib/__tests__/auberge-engine.test.ts &amp;&amp; grep -c "it(" lib/__tests__/auberge-engine.test.ts | awk '{ exit !($1 >= 15) }' &amp;&amp; npx jest lib/__tests__/auberge-engine.test.ts --no-coverage 2>&amp;1 | tail -10 | grep -q "passed"</automated>
  </verify>
  <acceptance_criteria>
    - Fichier existe avec ≥ 15 `it(` blocks.
    - `npx jest lib/__tests__/auberge-engine.test.ts --no-coverage` exit 0.
    - Couverture des 8 axes obligatoires (spawn cooldown, spawn cap, anti-spam PNJ, gating Comtesse, deliver, dismiss, expire, round-trip).
  </acceptance_criteria>
  <done>
    Moteur Auberge prouvé par Jest. Plan 04 (hook) peut s'appuyer sur la stabilité de l'API.
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` ne dégrade pas la liste pré-existante d'erreurs.
- `npx jest lib/__tests__/auberge-engine.test.ts --no-coverage` vert.
- Aucun import React/Expo/FS dans auberge-engine.ts.
</verification>

<success_criteria>
- 12 fonctions + serialize/parse implémentées et testées.
- 15+ tests Jest passants.
- Commit FR atomique : `feat(43): moteur pur Auberge (12 fonctions + serialize/parse + 15 tests Jest)`.
</success_criteria>

<output>
.planning/phases/43-auberge-mod-le-moteur-visiteurs/43-03-SUMMARY.md avec :
- Liste exacte des 12 fonctions exposées + signatures finales.
- Format CSV final retenu pour visitors et reputations (avec rationale).
- Confirmation des constantes (cooldowns, caps, weights, bonuses).
- Liste des Pitfalls couverts par tests (1-9).
- Output `npx jest auberge-engine.test.ts` (résumé).
</output>
