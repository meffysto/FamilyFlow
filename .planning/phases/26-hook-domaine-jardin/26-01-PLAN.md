---
phase: 26-hook-domaine-jardin
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - hooks/useVault.ts
  - lib/types.ts
  - lib/parser.ts
autonomous: true
requirements: [DATA-03, OBJ-05]

must_haves:
  truths:
    - "useVault.ts expose gardenRaw et setGardenRaw dans VaultState"
    - "jardin-familial.md est lu au chargement du vault via VILLAGE_FILE"
    - "FarmProfileData accepte village_claimed_week en lecture et ecriture"
    - "useVault.ts grandit de 15 lignes maximum"
  artifacts:
    - path: "hooks/useVault.ts"
      provides: "gardenRaw state + lecture fichier village dans loadVaultData"
      contains: "gardenRaw"
    - path: "lib/types.ts"
      provides: "village_claimed_week dans FarmProfileData"
      contains: "village_claimed_week"
    - path: "lib/parser.ts"
      provides: "parse et serialize village_claimed_week dans gami-{id}.md"
      contains: "village_claimed_week"
  key_links:
    - from: "hooks/useVault.ts"
      to: "lib/village/parser.ts"
      via: "import VILLAGE_FILE"
      pattern: "VILLAGE_FILE"
    - from: "lib/parser.ts"
      to: "lib/types.ts"
      via: "FarmProfileData.village_claimed_week"
      pattern: "village_claimed_week"
---

<objective>
Cablage minimal de useVault.ts pour exposer gardenRaw (contenu brut de jardin-familial.md) et extension de FarmProfileData pour le flag anti-double-claim village_claimed_week.

Purpose: Fournir l'infrastructure que useGarden.ts (Plan 02) consommera — gardenRaw pour parser les donnees village, et village_claimed_week pour empecher le double-claim de recompense.
Output: useVault.ts expose gardenRaw/setGardenRaw, FarmProfileData accepte village_claimed_week, parseFarmProfile/serializeFarmProfile gerent le nouveau champ.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-hook-domaine-jardin/26-CONTEXT.md
@.planning/phases/26-hook-domaine-jardin/26-RESEARCH.md

<interfaces>
<!-- useVault.ts VaultState interface (ligne 128+) — ajouter gardenRaw/setGardenRaw -->
<!-- Promise.allSettled array: indices [0]-[20], secretMissions est [20] -->
<!-- Apres results: val(results[20], []) pour secretMissions a ligne 1143 -->
<!-- Return useMemo a ligne 1497 — ajouter gardenRaw, setGardenRaw -->
<!-- Deps array du useMemo a ligne 1630+ — ajouter gardenRaw -->

From lib/types.ts (ligne 571):
```typescript
export interface FarmProfileData {
  treeSpecies?: import('../lib/mascot/types').TreeSpecies;
  mascotDecorations: string[];
  // ... (19 champs existants)
  unlockedEffectRecipes?: string[];
  effectGoldenMultiplier?: number;
}
```

From lib/parser.ts (ligne 571-630):
```typescript
export function parseFarmProfile(content: string): FarmProfileData {
  // key-value parser: props[key] = val
  return { ...existingFields };
  // village_claimed_week manquant — a ajouter
}
```

From lib/parser.ts (ligne 637-674):
```typescript
export function serializeFarmProfile(profileName: string, data: FarmProfileData): string {
  // if (data.field) lines.push(`field_name: ${data.field}`)
  // village_claimed_week manquant — a ajouter
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Etendre FarmProfileData + parseFarmProfile + serializeFarmProfile pour village_claimed_week</name>
  <files>lib/types.ts, lib/parser.ts</files>
  <read_first>lib/types.ts, lib/parser.ts</read_first>
  <action>
1. Dans `lib/types.ts`, interface `FarmProfileData` (ligne ~594, apres `effectGoldenMultiplier?: number;`), ajouter :
   ```typescript
   village_claimed_week?: string; // ISO 'YYYY-MM-DD' — semaine village la plus recente claimee (per D-08)
   ```

2. Dans `lib/parser.ts`, fonction `parseFarmProfile` (ligne ~606-630), dans l'objet retourne, ajouter apres la ligne `effectGoldenMultiplier` :
   ```typescript
   village_claimed_week: props.village_claimed_week || undefined,
   ```

3. Dans `lib/parser.ts`, fonction `serializeFarmProfile` (ligne ~637-674), ajouter avant le `return lines.join('\n') + '\n';` (apres la derniere condition `if (data.unlockedEffectRecipes...)`) :
   ```typescript
   if (data.village_claimed_week) lines.push(`village_claimed_week: ${data.village_claimed_week}`);
   ```
  </action>
  <verify>
    <automated>cd /Users/gabrielwaltio/Documents/family-vault && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - lib/types.ts contient `village_claimed_week?: string;` dans FarmProfileData
    - lib/parser.ts parseFarmProfile retourne `village_claimed_week: props.village_claimed_week || undefined`
    - lib/parser.ts serializeFarmProfile contient `if (data.village_claimed_week) lines.push(`
    - `npx tsc --noEmit` passe sans nouvelles erreurs
  </acceptance_criteria>
  <done>FarmProfileData, parseFarmProfile et serializeFarmProfile gerent village_claimed_week — backward compatible (champ optionnel, ignoré si absent)</done>
</task>

<task type="auto">
  <name>Task 2: Cabler gardenRaw dans useVault.ts — lecture fichier village + exposition VaultState</name>
  <files>hooks/useVault.ts</files>
  <read_first>hooks/useVault.ts, lib/village/index.ts</read_first>
  <action>
Modifier `hooks/useVault.ts` en 5 endroits precis (per D-02, D-03). Total : ~12-15 lignes ajoutees.

1. **Import VILLAGE_FILE** — en haut du fichier, dans les imports existants de `../lib/...`, ajouter :
   ```typescript
   import { VILLAGE_FILE } from '../lib/village';
   ```

2. **Interface VaultState** (ligne ~128) — ajouter apres `dietary: ...;` (dernier champ actuel) :
   ```typescript
   gardenRaw: string;
   setGardenRaw: React.Dispatch<React.SetStateAction<string>>;
   ```

3. **useState dans useVaultInternal** — ajouter pres des autres useState simples (ex: apres les useState de quotes/moods vers la ligne ~230-250) :
   ```typescript
   const [gardenRaw, setGardenRaw] = useState<string>('');
   ```

4. **loadVaultData, bloc Promise.allSettled** — apres le dernier element `[20] Missions secretes` (ligne ~1069) et avant le `]);` fermant, ajouter :
   ```typescript
   // [21] Village garden — fichier partage
   vault.readFile(VILLAGE_FILE).catch(() => ''),
   ```
   Puis dans le traitement des resultats, apres `missionsHook.setSecretMissions(val(results[20], []));` (ligne ~1143), ajouter :
   ```typescript
   setGardenRaw(val(results[21], '') as string);
   ```

5. **Return useMemo** (ligne ~1497) — ajouter dans l'objet retourne, apres `dietary: dietaryHook,` :
   ```typescript
   gardenRaw,
   setGardenRaw,
   ```
   Et dans le deps array du useMemo (ligne ~1632), ajouter `gardenRaw` dans la liste des state values :
   ```typescript
   // Ajouter gardenRaw a la fin de la premiere ligne de state values
   ..., secretMissions, gardenRaw,
   ```
  </action>
  <verify>
    <automated>cd /Users/gabrielwaltio/Documents/family-vault && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - hooks/useVault.ts contient `import { VILLAGE_FILE } from '../lib/village'`
    - hooks/useVault.ts interface VaultState contient `gardenRaw: string;`
    - hooks/useVault.ts interface VaultState contient `setGardenRaw:`
    - hooks/useVault.ts contient `useState<string>('')` pour gardenRaw
    - hooks/useVault.ts contient `vault.readFile(VILLAGE_FILE).catch(() => '')`
    - hooks/useVault.ts contient `setGardenRaw(val(results[21]`
    - hooks/useVault.ts return useMemo contient `gardenRaw,` et `setGardenRaw,`
    - `npx tsc --noEmit` passe sans nouvelles erreurs
    - Nombre de lignes ajoutees dans useVault.ts <= 15
  </acceptance_criteria>
  <done>useVault.ts expose gardenRaw/setGardenRaw dans VaultState, lit jardin-familial.md dans le bloc parallel loading, respecte le boundary de 15 lignes max</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passe sans nouvelles erreurs
- `grep -c 'gardenRaw\|setGardenRaw\|VILLAGE_FILE' hooks/useVault.ts` retourne >= 7
- `grep 'village_claimed_week' lib/types.ts lib/parser.ts` retourne 3 lignes (1 type + 1 parse + 1 serialize)
- Diff useVault.ts montre <= 15 lignes ajoutees
</verification>

<success_criteria>
- VaultState expose gardenRaw: string et setGardenRaw
- jardin-familial.md est lu dans loadVaultData via VILLAGE_FILE
- FarmProfileData accepte village_claimed_week optionnel
- parseFarmProfile lit village_claimed_week depuis le fichier
- serializeFarmProfile ecrit village_claimed_week si present
- useVault.ts grandit de 15 lignes maximum (boundary god hook respectee)
- tsc --noEmit passe
</success_criteria>

<output>
After completion, create `.planning/phases/26-hook-domaine-jardin/26-01-SUMMARY.md`
</output>
