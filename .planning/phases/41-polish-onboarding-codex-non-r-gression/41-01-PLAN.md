---
phase: 41-polish-onboarding-codex-non-r-gression
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/types.ts
  - hooks/useFarm.ts
  - lib/__tests__/useFarm-wager.test.ts
autonomous: true
requirements: [SPOR-10]
must_haves:
  truths:
    - "User voit `wagerMarathonWins: number` (default 0) présent dans FarmProfileData et parsé depuis le champ markdown `wager_marathon_wins`"
    - "User voit son compteur `wagerMarathonWins` incrémenté de +1 exactement à chaque pari Sporée gagné (branche harvest wager.won === true) — ni sur défaite, ni sur récolte sans wager"
    - "User voit le compteur persister dans le vault via le même writeFile atomique que la harvest wager (pas de double I/O)"
    - "User voit la suite Jest passer : round-trip parse/serialize `wager_marathon_wins`, increment on won, no-op on lost/no-wager"
  artifacts:
    - path: "lib/types.ts"
      provides: "Champ optionnel `wagerMarathonWins?: number` sur FarmProfileData (après sporeeCount cluster)"
      contains: "wagerMarathonWins?: number"
    - path: "hooks/useFarm.ts"
      provides: "Case parser `wager_marathon_wins` dans setFarmField + incrément dans branche harvest wager.won + persistance via fieldsToWrite/updatedProfile spread"
      contains: "wager_marathon_wins"
    - path: "lib/__tests__/useFarm-wager.test.ts"
      provides: "Tests marathon wins increment (extension de la suite existante Phase 40-04)"
      min_lines: 40
  key_links:
    - from: "hooks/useFarm.ts harvest wager won branch"
      to: "FarmProfileData.wagerMarathonWins"
      via: "profile.wagerMarathonWins = (profile.wagerMarathonWins ?? 0) + 1 + persist via fieldsToWrite.wager_marathon_wins OU spread updatedProfile"
      pattern: "wager_marathon_wins.*String"
---

<objective>
Ajouter le compteur vanité `wagerMarathonWins` (SPOR-10 partie codex) à la donnée ferme : shape `FarmProfileData`, parser markdown, incrément dans `useFarm.harvest` sur pari gagné, persistance atomique dans le même writeFile que la récolte wager. Zéro UI — consommé par Plan 03 pour l'affichage dans FarmCodexModal.

Purpose: Préparer la fondation data du compteur long-terme vanité (récompense de régularité) avant le câblage UI.
Output: Champ persisté dans vault + logic incrémentale + tests Jest.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@lib/types.ts
@hooks/useFarm.ts

<interfaces>
<!-- Shape existante FarmProfileData cluster Sporée (lib/types.ts:654-660) -->
```typescript
// Phase 38 — économie Sporée per-profil (MOD/SPOR v1.7)
sporeeCount?: number;                    // inventaire 0-10, default undefined
sporeeShopBoughtToday?: number;          // achats shop du jour 0-2
sporeeShopLastResetDate?: string;        // YYYY-MM-DD local — dernier reset cap quotidien
sporeeOnboardingGiftClaimed?: boolean;   // true = cadeau stade 3 déjà donné (anti-rejeu)
// Phase 40 — Bootstrap maybeRecompute
wagerLastRecomputeDate?: string;         // ISO YYYY-MM-DD
```

<!-- Pattern parser existant hooks/useFarm.ts:155-162 -->
```typescript
case 'sporee_count': {
  const n = parseInt(value, 10);
  data.sporeeCount = isNaN(n) ? undefined : n;
  break;
}
case 'wager_last_recompute_date':
  data.wagerLastRecomputeDate = value || undefined;
  break;
```

<!-- Branche harvest wager won existante hooks/useFarm.ts:370-390 -->
```typescript
if (wager) {
  const validation = validateWagerOnHarvest(wager.cumulCurrent ?? 0, wager.cumulTarget ?? 0);
  if (validation.won) {
    wagerWon = true;
    finalQty = Math.round(finalQty * wager.multiplier);
    if (rollWagerDropBack()) { ... profile.sporeeCount = inc.newCount; wagerDropBack = true; ... }
  }
}
```

<!-- Persistance: deux branches (golden spread + fieldsToWrite non-golden) hooks/useFarm.ts:392-437 -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Ajouter wagerMarathonWins à FarmProfileData + parser field</name>
  <files>lib/types.ts, hooks/useFarm.ts, lib/__tests__/useFarm-wager.test.ts</files>
  <read_first>
    - lib/types.ts (voir FarmProfileData lignes 654-661 cluster Sporée — ajouter après wagerLastRecomputeDate)
    - hooks/useFarm.ts (voir setFarmField lignes 145-163 pour pattern case parsing)
    - lib/__tests__/useFarm-wager.test.ts (suite existante Phase 40-04 — référence pour style et mocks)
  </read_first>
  <behavior>
    - Test 1: `parseFarmProfile` d'un fichier contenant `wager_marathon_wins: 7` → `data.wagerMarathonWins === 7`
    - Test 2: `parseFarmProfile` sans le champ → `data.wagerMarathonWins === undefined`
    - Test 3: `parseFarmProfile` avec `wager_marathon_wins: abc` (NaN) → `data.wagerMarathonWins === undefined`
    - Test 4: `parseFarmProfile` avec `wager_marathon_wins: 0` → `data.wagerMarathonWins === 0`
  </behavior>
  <action>
    1. Dans `lib/types.ts`, interface `FarmProfileData`, ajouter APRÈS la ligne `wagerLastRecomputeDate?: string;` (ligne ~660) :
       ```typescript
       // Phase 41 — Compteur codex vanité long terme (SPOR-10)
       wagerMarathonWins?: number;              // total paris Sporée gagnés (vanité, jamais reset)
       ```

    2. Dans `hooks/useFarm.ts`, fonction `setFarmField` (vers ligne 145-163), ajouter un nouveau case APRÈS le case `'wager_last_recompute_date'` :
       ```typescript
       case 'wager_marathon_wins': {
         const n = parseInt(value, 10);
         data.wagerMarathonWins = isNaN(n) ? undefined : n;
         break;
       }
       ```

    3. Vérifier que `serializeFarmProfile` (dans `lib/parser.ts`) écrit automatiquement les champs définis sur `FarmProfileData` — si la sérialisation est explicite par champ, ajouter la ligne correspondante `wager_marathon_wins: ${data.wagerMarathonWins}` (conditionnel si défini). Lire lib/parser.ts `serializeFarmProfile` pour confirmer le pattern (chercher `sporee_count` comme référence — même comportement attendu).

    4. Étendre `lib/__tests__/useFarm-wager.test.ts` avec bloc `describe('wagerMarathonWins parsing', ...)` exécutant les 4 behaviors ci-dessus via `parseFarmProfile` direct (mocks vault inutiles pour parser pur).
  </action>
  <verify>
    <automated>npx jest --no-coverage lib/__tests__/useFarm-wager.test.ts -t "wagerMarathonWins parsing"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "wagerMarathonWins" lib/types.ts` renvoie exactement 1 ligne contenant `wagerMarathonWins?: number;`
    - `grep -n "wager_marathon_wins" hooks/useFarm.ts` renvoie ≥1 ligne (case parser)
    - `grep -n "wager_marathon_wins" lib/parser.ts` renvoie ≥1 ligne (sérialisation) OU la fonction sérialise génériquement les champs data.
    - `npx jest --no-coverage lib/__tests__/useFarm-wager.test.ts -t "wagerMarathonWins parsing"` affiche ≥4 tests passing et 0 failing.
    - `npx tsc --noEmit` ne produit AUCUNE nouvelle erreur (hors les 3 pré-existantes : MemoryEditor.tsx, cooklang.ts, useVault.ts).
  </acceptance_criteria>
  <done>Champ présent dans le type, parser/serializer cohérent, 4 tests round-trip verts, tsc inchangé.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Incrémenter wagerMarathonWins dans la branche harvest wager.won</name>
  <files>hooks/useFarm.ts, lib/__tests__/useFarm-wager.test.ts</files>
  <read_first>
    - hooks/useFarm.ts (branche harvest lignes 362-451 — voir `if (validation.won)` ligne ~372 et les deux branches de persistance : golden spread `updatedProfile` ligne 394-401 et fieldsToWrite ligne 418-434)
    - lib/__tests__/useFarm-wager.test.ts (suite Plan 40-04 — voir tests `rollWagerDropBack` pour pattern injection RNG)
  </read_first>
  <behavior>
    - Test 1: Plant scellé récolté, cumul atteint (won=true) → `profile.wagerMarathonWins` passe de 0 à 1 (default undefined traité comme 0)
    - Test 2: Plant scellé récolté, cumul NON atteint (won=false) → `profile.wagerMarathonWins` reste inchangé (0 ou undefined)
    - Test 3: Plant non scellé (pas de wager) → `wagerMarathonWins` jamais touché
    - Test 4: Plant scellé won avec wagerMarathonWins initial = 5 → devient 6
    - Test 5: La persistance écrit `wager_marathon_wins` dans fieldsToWrite (branche non-golden) OU dans updatedProfile (branche golden) SEULEMENT si incrémenté
  </behavior>
  <action>
    1. Dans `hooks/useFarm.ts`, harvest callback, DANS le bloc `if (validation.won) { ... }` (vers ligne 372), APRÈS `wagerWon = true;` et AVANT le bloc `rollWagerDropBack` :
       ```typescript
       // Phase 41 (SPOR-10) — Compteur vanité marathon wins
       profile.wagerMarathonWins = (profile.wagerMarathonWins ?? 0) + 1;
       const marathonIncremented = true;
       ```
       Déclarer `let marathonIncremented = false;` EN DEHORS du `if (wager)` initial (au niveau de `sporeeDropped` / `wagerDropBack`) pour pouvoir le lire dans les deux branches de persistance.

    2. Branche golden (ligne 394-401 `updatedProfile = { ... }`) : ajouter au spread conditionnel :
       ```typescript
       ...(marathonIncremented ? { wagerMarathonWins: profile.wagerMarathonWins } : {}),
       ```

    3. Branche non-golden (ligne 418 `fieldsToWrite`) : après le bloc `sporee_count`, ajouter :
       ```typescript
       // Phase 41 (SPOR-10) — Persister compteur vanité marathon wins
       if (marathonIncremented && typeof profile.wagerMarathonWins === 'number') {
         fieldsToWrite.wager_marathon_wins = String(profile.wagerMarathonWins);
       }
       ```

    4. Étendre `lib/__tests__/useFarm-wager.test.ts` avec bloc `describe('wagerMarathonWins increment on harvest', ...)` exécutant les 5 behaviors. Utiliser l'injection RNG déjà en place dans la suite (patron `rollWagerDropBack` du Plan 40-04) et mocker vault.writeFile pour capturer `fieldsToWrite.wager_marathon_wins`.
  </action>
  <verify>
    <automated>npx jest --no-coverage lib/__tests__/useFarm-wager.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "wagerMarathonWins" hooks/useFarm.ts` renvoie ≥3 lignes (décl. let, incrément dans validation.won, persistance branche golden).
    - `grep -n "wager_marathon_wins" hooks/useFarm.ts` renvoie ≥2 lignes (case parser + fieldsToWrite).
    - `grep -n "marathonIncremented" hooks/useFarm.ts` renvoie ≥3 lignes (déclaration + set true + 2 lectures de persistance).
    - `npx jest --no-coverage lib/__tests__/useFarm-wager.test.ts` affiche 0 failing, les 5 nouveaux tests passent.
    - `npx tsc --noEmit` ne produit AUCUNE nouvelle erreur.
    - Aucun appel à `vault.writeFile` ajouté hors des branches existantes (grep `vault.writeFile` dans `hooks/useFarm.ts` n'augmente pas vs avant ce plan).
  </acceptance_criteria>
  <done>Incrément atomique dans writeFile existant, 5 tests verts, aucune régression tsc/jest.</done>
</task>

</tasks>

<verification>
- `npx jest --no-coverage lib/__tests__/useFarm-wager.test.ts` all green
- `npx tsc --noEmit` only pre-existing errors
- Vault field persisté après un harvest wager gagné (vérif manuelle post-dev via re-boot app)
</verification>

<success_criteria>
- wagerMarathonWins parsé/sérialisé round-trip sans perte
- Incrément +1 exactement sur pari gagné, 0 sinon
- Persistance atomique (pas de double writeFile)
- Suite Jest existante Phase 40-04 étendue, tous tests verts
- SPOR-10 partie data/logic livrée (UI codex en Plan 03)
</success_criteria>

<output>
After completion, create `.planning/phases/41-polish-onboarding-codex-non-r-gression/41-01-SUMMARY.md`
</output>
