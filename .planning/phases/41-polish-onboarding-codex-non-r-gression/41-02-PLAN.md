---
phase: 41-polish-onboarding-codex-non-r-gression
plan: 02
type: execute
wave: 2
depends_on: [41-01]
files_modified:
  - components/mascot/SporeeOnboardingTooltip.tsx
  - hooks/useFarm.ts
  - hooks/useExpeditions.ts
  - hooks/useGamification.ts
  - app/(tabs)/tree.tsx
autonomous: false
requirements: [SPOR-10]
must_haves:
  truths:
    - "User voit un tooltip/alert one-shot s'afficher au tout premier drop ou cadeau de Sporée (3 sources : harvest drop, expedition drop, cadeau onboarding stade 3)"
    - "User voit le tooltip contenir 1-2 phrases expliquant la mécanique Sporée (pari bienveillant sur le cumul de tâches pendant que le plant pousse)"
    - "User peut dismiss le tooltip (bouton OK / tap outside) ; une fois vu le flag `sporee_tooltip` est persisté via HelpContext.markScreenSeen"
    - "User qui obtient une 2e Sporée ne voit JAMAIS retrigger le tooltip (hasSeenScreen('sporee_tooltip') === true bloque)"
    - "Le flag est device-global (SecureStore via HelpContext — pattern TUTO-02 existant) — pas per-profil"
  artifacts:
    - path: "components/mascot/SporeeOnboardingTooltip.tsx"
      provides: "Composant Modal transparent one-shot avec cachet Sporée + texte FR + bouton OK + haptic"
      min_lines: 60
    - path: "hooks/useFarm.ts"
      provides: "Émission signal sporee_first_obtained quand sporeeDropped OU wagerDropBack et hasSeenScreen('sporee_tooltip') === false"
    - path: "hooks/useExpeditions.ts"
      provides: "Émission signal sporee_first_obtained quand drop Sporée post-expedition"
    - path: "hooks/useGamification.ts"
      provides: "Émission signal sporee_first_obtained quand cadeau onboarding stage 3"
    - path: "app/(tabs)/tree.tsx"
      provides: "Render conditionnel SporeeOnboardingTooltip écoutant signal"
  key_links:
    - from: "hooks/useFarm.ts harvest sporee drop / wager drop-back"
      to: "callback onSporeeObtained passé via useVault subscribe"
      via: "Pattern event-driven (useState local dans tree.tsx + prop callback)"
      pattern: "onSporeeObtained|setShowSporeeTooltip"
    - from: "components/mascot/SporeeOnboardingTooltip.tsx OK press"
      to: "HelpContext.markScreenSeen('sporee_tooltip')"
      via: "Persistance SecureStore device-global"
      pattern: "markScreenSeen\\(['\"]sporee_tooltip"
---

<objective>
Implémenter le tooltip one-shot au premier drop/obtention de Sporée (SPOR-10 partie onboarding) — modal discret expliquant la mécanique en 1-2 phrases, dismissable, jamais re-triggeré (flag device-global SecureStore via HelpContext). Couvrir les 3 sources d'obtention : harvest drop, expedition drop, cadeau onboarding stage 3.

Purpose: Onboarding discret mais complet (pattern TUTO-02) qui évite la friction "qu'est-ce que cette icône ?" au premier contact Sporée.
Output: Tooltip modal + wiring 3 sources + persistance flag device-global.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@contexts/HelpContext.tsx
@hooks/useFarm.ts
@hooks/useExpeditions.ts
@hooks/useGamification.ts
@app/(tabs)/tree.tsx

<interfaces>
<!-- HelpContext API existante (contexts/HelpContext.tsx) -->
```typescript
interface HelpContextValue {
  hasSeenScreen: (screenId: string) => boolean;   // synchrone, backed by SecureStore + mémoire
  markScreenSeen: (screenId: string) => Promise<void>;  // persiste device-global
  resetScreen: (screenId: string) => Promise<void>;
  isLoaded: boolean;
}
export function useHelp(): HelpContextValue;
```

<!-- Pattern existant TUTO-02 : flag 'farm_tutorial' persisté via markScreenSeen -->

<!-- Signaux d'émission existants — 3 sources de Sporée : -->
<!-- 1. hooks/useFarm.ts:351-360 — rollSporeeDropOnHarvest + inc.accepted → sporeeDropped = true -->
<!-- 2. hooks/useFarm.ts:377-386 — rollWagerDropBack + inc.accepted → wagerDropBack = true -->
<!-- 3. hooks/useExpeditions.ts:253-258 — rollSporeeDropOnExpedition + inc.accepted -->
<!-- 4. hooks/useGamification.ts:326-332 — shouldGiftOnboardingSporee + inc.accepted -->

<!-- Pattern tree.tsx écoute useFarm.harvest().then(result => ...) — result peut être enrichi -->
```typescript
// tree.tsx:1287-1319 harvest().then(result => { if (result) { ... showHarvestCard ... if (result.wager) { ... } } })
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Créer SporeeOnboardingTooltip composant Modal transparent one-shot</name>
  <files>components/mascot/SporeeOnboardingTooltip.tsx</files>
  <read_first>
    - contexts/HelpContext.tsx (API hasSeenScreen/markScreenSeen, useHelp hook — lignes 36-265)
    - components/mascot/FarmCodexModal.tsx (style pageSheet + parchment existant pour cohérence visuelle — lignes 182-321)
    - constants/spacing.ts, constants/typography.ts (tokens Spacing/FontSize/FontWeight/Radius)
    - contexts/ThemeContext.tsx (useThemeColors API)
  </read_first>
  <action>
    Créer `components/mascot/SporeeOnboardingTooltip.tsx` :

    ```typescript
    /**
     * SporeeOnboardingTooltip.tsx — Phase 41 (SPOR-10)
     *
     * Tooltip one-shot au premier drop/obtention d'une Sporée.
     * Flag 'sporee_tooltip' persisté device-global via HelpContext (pattern TUTO-02).
     * Jamais retriggeré une fois dismissé.
     */
    import React, { useCallback } from 'react';
    import { Modal, View, Text, Pressable, StyleSheet } from 'react-native';
    import * as Haptics from 'expo-haptics';
    import { useThemeColors } from '../../contexts/ThemeContext';
    import { useHelp } from '../../contexts/HelpContext';
    import { Spacing, Radius } from '../../constants/spacing';
    import { FontSize, FontWeight } from '../../constants/typography';

    interface Props {
      visible: boolean;
      onDismiss: () => void;
    }

    export function SporeeOnboardingTooltip({ visible, onDismiss }: Props) {
      const { colors } = useThemeColors();
      const { markScreenSeen } = useHelp();

      const handleOk = useCallback(async () => {
        Haptics.selectionAsync();
        await markScreenSeen('sporee_tooltip');
        onDismiss();
      }, [markScreenSeen, onDismiss]);

      return (
        <Modal visible={visible} transparent animationType="fade" onRequestClose={handleOk}>
          <Pressable style={styles.backdrop} onPress={handleOk}>
            <Pressable style={[styles.card, { backgroundColor: colors.card, borderColor: colors.border }]} onPress={(e) => e.stopPropagation()}>
              <Text style={styles.emoji}>🍄</Text>
              <Text style={[styles.title, { color: colors.text }]}>Une Sporée !</Text>
              <Text style={[styles.body, { color: colors.textSub }]}>
                Applique-la à un plant pour parier sur ta régularité : si tu complètes assez de tâches pendant que le plant pousse, tu gagnes un multiplicateur de récompense. Pari bienveillant — jamais de pénalité.
              </Text>
              <Pressable style={[styles.okBtn, { backgroundColor: colors.primary }]} onPress={handleOk}>
                <Text style={[styles.okLabel, { color: colors.bg }]}>Compris</Text>
              </Pressable>
            </Pressable>
          </Pressable>
        </Modal>
      );
    }

    const styles = StyleSheet.create({
      backdrop: { flex: 1, backgroundColor: 'rgba(0,0,0,0.55)', justifyContent: 'center', alignItems: 'center', padding: Spacing['2xl'] },
      card: { padding: Spacing['2xl'], borderRadius: Radius.lg, borderWidth: 1, alignItems: 'center', maxWidth: 340 },
      emoji: { fontSize: 48, marginBottom: Spacing.md },
      title: { fontSize: FontSize.lg, fontWeight: FontWeight.bold, marginBottom: Spacing.md },
      body: { fontSize: FontSize.body, textAlign: 'center', lineHeight: 22, marginBottom: Spacing.xl },
      okBtn: { paddingHorizontal: Spacing['2xl'], paddingVertical: Spacing.md, borderRadius: Radius.md },
      okLabel: { fontSize: FontSize.body, fontWeight: FontWeight.semibold },
    });
    ```

    Noms de tokens Spacing/FontSize/Radius : utiliser EXACTEMENT ceux du projet (vérifier via `grep "Radius.lg\\|FontSize.lg" constants/`). Si absent, fallback sur valeurs présentes.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -v "MemoryEditor.tsx\|cooklang.ts\|useVault.ts" | grep -i "SporeeOnboardingTooltip\|error TS" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - Fichier `components/mascot/SporeeOnboardingTooltip.tsx` existe et exporte `SporeeOnboardingTooltip`.
    - `grep -n "markScreenSeen('sporee_tooltip')" components/mascot/SporeeOnboardingTooltip.tsx` renvoie exactement 1 ligne.
    - `grep -n "useThemeColors\\|useHelp" components/mascot/SporeeOnboardingTooltip.tsx` renvoie ≥2 lignes.
    - `grep -n "rgba\\|#[0-9a-fA-F]\\{3,6\\}" components/mascot/SporeeOnboardingTooltip.tsx` : AUCUN hardcoded sauf le backdrop `rgba(0,0,0,0.55)` (exception documentée — overlay neutre standard).
    - `npx tsc --noEmit` ne signale aucune nouvelle erreur sur ce fichier.
  </acceptance_criteria>
  <done>Composant créé, conforme conventions projet (useThemeColors, tokens, haptics), typé clean.</done>
</task>

<task type="auto">
  <name>Task 2: Wiring 3 sources d'obtention → signal sporee_first_obtained + tooltip render dans tree.tsx</name>
  <files>hooks/useFarm.ts, hooks/useExpeditions.ts, hooks/useGamification.ts, app/(tabs)/tree.tsx</files>
  <read_first>
    - hooks/useFarm.ts (lignes 311-452 harvest callback — points d'émission aux lignes 356 sporeeDropped, 382 wagerDropBack)
    - hooks/useExpeditions.ts (lignes 249-310 — point d'émission ligne 256 après tryIncrementSporeeCount)
    - hooks/useGamification.ts (lignes 317-340 — point d'émission ligne 329 après tryIncrementSporeeCount cadeau onboarding)
    - app/(tabs)/tree.tsx (lignes 1287-1319 harvest().then() — point de render tooltip)
    - contexts/HelpContext.tsx (hasSeenScreen API)
  </read_first>
  <action>
    **Approche : extension type du retour harvest/loot pour signaler "première obtention", rendu tooltip au niveau tree.tsx.**

    1. Dans `hooks/useFarm.ts`, type de retour de `harvest` (ligne 311) : ajouter champ `sporeeFirstObtained?: boolean`.
       Dans le corps, AVANT `return { ... }` (2 branches golden et non-golden), calculer :
       ```typescript
       // Phase 41 (SPOR-10) — signal premier obtention Sporée (tooltip one-shot)
       const sporeeFirstObtained = (sporeeDropped || wagerDropBack);
       ```
       Ajouter `sporeeFirstObtained` aux 2 returns (golden ligne 414 + non-golden ligne 451).

    2. Dans `hooks/useExpeditions.ts`, fonction `collectExpedition` (ou équivalent retournant le résultat expedition) :
       - Chercher le bloc où `farm.sporeeCount = inc.newCount;` est set (ligne ~256).
       - Ajouter variable locale `let sporeeDroppedFromExpedition = false;` et la mettre à `true` après `farm.sporeeCount = inc.newCount;`.
       - Enrichir le return du callback avec `sporeeFirstObtained: sporeeDroppedFromExpedition`.
       - Si le type de retour ne propage pas déjà vers UI, l'ajouter au type.

    3. Dans `hooks/useGamification.ts`, fonction contenant la logique cadeau onboarding (lignes 317-340) :
       - Après `farmData.sporeeCount = inc.newCount; farmData.sporeeOnboardingGiftClaimed = true;`, ajouter :
         ```typescript
         // Phase 41 (SPOR-10) — signal tooltip premier obtention (cadeau onboarding)
         sporeeFirstObtainedViaOnboarding = true;
         ```
       - Déclarer `let sporeeFirstObtainedViaOnboarding = false;` en début de scope.
       - Propager dans le retour de la fonction concernée (ou via un callback `onSporeeFirstObtained?: () => void` passé en paramètre du hook — préférer callback si propagation de retour casse trop d'appelants).

    4. Dans `app/(tabs)/tree.tsx` :
       - Importer `SporeeOnboardingTooltip` et `useHelp` (si pas déjà fait).
       - Ajouter state local : `const [showSporeeTooltip, setShowSporeeTooltip] = useState(false);`
       - Dans le bloc `harvest().then(result => { if (result) { ... })` (ligne 1287-1319) : ajouter APRÈS `triggerActionMsg('harvest')` :
         ```typescript
         if (result.sporeeFirstObtained && !hasSeenScreen('sporee_tooltip')) {
           setTimeout(() => setShowSporeeTooltip(true), 800);
         }
         ```
       - Destructurer `hasSeenScreen` depuis `useHelp()`.
       - Pour la source expedition : localiser le `.then()` du `collectExpedition` dans tree.tsx (ou fichier ExpeditionsSheet — vérifier où `useExpeditions().collectExpedition` est consommé) et appliquer la même logique `result.sporeeFirstObtained && !hasSeenScreen → setShowSporeeTooltip(true)`.
       - Pour la source cadeau onboarding (useGamification) : si callback injection retenu, passer `() => { if (!hasSeenScreen('sporee_tooltip')) setShowSporeeTooltip(true); }` au hook useGamification via son provider (useVault). Sinon, s'assurer que le retour propage et wire au niveau task complete dans tree.tsx.
       - Rendre le composant en fin de JSX (même niveau que les autres modals existants) :
         ```tsx
         <SporeeOnboardingTooltip visible={showSporeeTooltip} onDismiss={() => setShowSporeeTooltip(false)} />
         ```
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -v "MemoryEditor.tsx\|cooklang.ts\|useVault.ts" | grep "error TS" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "sporeeFirstObtained" hooks/useFarm.ts` renvoie ≥3 lignes (variable + 2 returns golden/non-golden).
    - `grep -n "sporeeFirstObtained\\|sporee_tooltip" hooks/useExpeditions.ts` renvoie ≥1 ligne.
    - `grep -n "sporeeFirstObtained\\|sporee_tooltip\\|SporeeOnboardingTooltip" app/(tabs)/tree.tsx` renvoie ≥3 lignes (import + state + render + check).
    - `grep -n "SporeeOnboardingTooltip" app/(tabs)/tree.tsx` renvoie ≥2 lignes (import + render JSX).
    - `grep -n "hasSeenScreen('sporee_tooltip')" app/(tabs)/tree.tsx` renvoie ≥1 ligne.
    - `npx tsc --noEmit` ne signale aucune nouvelle erreur (hors les 3 pré-existantes).
    - Aucune nouvelle dépendance npm : `diff package.json` inchangé sur la section dependencies/devDependencies.
  </acceptance_criteria>
  <done>Signal propagé depuis 3 sources, tooltip render conditionnel avec guard hasSeenScreen, flag persisté au dismiss, tsc clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Vérifier tooltip one-shot sur device</name>
  <files>N/A (vérification device manuelle)</files>
  <action>Suivre la procédure how-to-verify ci-dessous sur un build iOS dev-client.</action>
  <verify>Signal "approved" utilisateur après exécution des 6 étapes how-to-verify.</verify>
  <done>Tooltip visible 1x au premier drop, jamais au second, flag persiste après kill/relaunch.</done>
  <what-built>
    Tooltip SporeeOnboardingTooltip one-shot branché sur 3 sources (harvest drop, expedition drop, cadeau onboarding stade 3 arbre), flag `sporee_tooltip` persisté device-global via HelpContext.markScreenSeen.
  </what-built>
  <how-to-verify>
    1. Depuis un état fresh (aucune Sporée encore obtenue OU via `npx expo start -c` + éventuel reset manuel du flag via `HelpContext.resetScreen('sporee_tooltip')` exposé dans un dev tool si existant) :
       - Compléter des tâches jusqu'à forcer un drop Sporée à la récolte (ou expedition Pousse+).
       - OU atteindre stade 3 arbre pour déclencher le cadeau onboarding.
    2. **Attendu** : Au premier drop, le tooltip apparaît en overlay après ~800ms avec texte FR "Applique-la à un plant pour parier sur ta régularité…" + bouton "Compris".
    3. Tap "Compris" ou tap outside → tooltip se ferme avec haptic selection.
    4. Récupérer une 2e Sporée (drop suivant) : **tooltip NE doit PAS réapparaître**.
    5. Kill app + relaunch + obtenir encore une Sporée : tooltip NE doit PAS réapparaître (persistance device-global OK).
    6. Facultatif : reset complet app/simulateur → premier drop réaffiche le tooltip.
  </how-to-verify>
  <resume-signal>Répondre "approved" ou décrire les problèmes observés</resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` clean (hors pré-existantes)
- Tooltip visible 1x exactement par install device (vérifié checkpoint)
- 3 sources couvertes : harvest, expedition, onboarding
</verification>

<success_criteria>
- SporeeOnboardingTooltip composant créé et branché
- Flag 'sporee_tooltip' persisté via HelpContext (pattern TUTO-02 reconduit)
- Zéro nouvelle dépendance npm
- Aucune régression TS
- User voit le tooltip exactement 1x au premier contact Sporée
</success_criteria>

<output>
After completion, create `.planning/phases/41-polish-onboarding-codex-non-r-gression/41-02-SUMMARY.md`
</output>
