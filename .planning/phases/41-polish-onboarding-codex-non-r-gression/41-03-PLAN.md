---
phase: 41-polish-onboarding-codex-non-r-gression
plan: 03
type: execute
wave: 3
depends_on: [41-01, 41-02]
files_modified:
  - components/mascot/FarmCodexModal.tsx
autonomous: false
requirements: [SPOR-10, SPOR-12]
must_haves:
  truths:
    - "User voit son compteur `wagerMarathonWins` affiché dans FarmCodexModal (footer ou zone dédiée) au format `🍄 Paris gagnés : N` (N = valeur actuelle, 0 si absent)"
    - "User qui ouvre le codex sans aucun pari gagné voit `🍄 Paris gagnés : 0` (pas d'affichage fantôme, pas de NaN)"
    - "User voit la valeur refléter live la dernière valeur persistée dans farm profile (prop `profile` déjà passée à FarmCodexModal)"
    - "User ne voit AUCUNE régression TypeScript (`npx tsc --noEmit` clean hors les 3 pré-existantes MemoryEditor.tsx / cooklang.ts / useVault.ts)"
    - "User ne voit AUCUNE régression Jest (`npx jest --no-coverage` clean — 100% tests existants + nouveaux passent)"
    - "User voit tous les commits v1.7 (Phase 38-41) respecter la privacy policy : seuls noms génériques Lucas/Emma/Dupont ou aucun nom réel"
  artifacts:
    - path: "components/mascot/FarmCodexModal.tsx"
      provides: "Affichage compteur wagerMarathonWins dans le footer existant (au-dessus ou à côté du bouton replay tutoriel)"
      contains: "wagerMarathonWins"
  key_links:
    - from: "components/mascot/FarmCodexModal.tsx footer"
      to: "profile.wagerMarathonWins (passé via prop DiscoverySource)"
      via: "Destructuring depuis profile + render conditionnel"
      pattern: "profile\\??\\.wagerMarathonWins"
---

<objective>
Clôturer Phase 41 par :
1. L'affichage du compteur `wagerMarathonWins` (livré en Plan 01) dans le FarmCodexModal existant — récompense vanité long terme visible (SPOR-10).
2. La vérification finale de non-régression milestone v1.7 : `npx tsc --noEmit` clean hors pré-existantes, `npx jest --no-coverage` clean, privacy check des commits Phase 38-41 (SPOR-12).

Purpose: Livrer la touche finale UX et garantir une sortie de milestone propre.
Output: Affichage codex + rapport de non-régression + milestone v1.7 shippable.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@components/mascot/FarmCodexModal.tsx
@lib/types.ts

<interfaces>
<!-- FarmCodexModalProps existant (components/mascot/FarmCodexModal.tsx:65-69) -->
```typescript
interface FarmCodexModalProps {
  visible: boolean;
  onClose: () => void;
  profile: DiscoverySource | null;  // type importé de lib/codex/discovery — contient déjà un champ Profile-like
}
```

<!-- Footer existant (lignes 292-310) : un seul bouton Pressable "replay tutoriel" -->
<!-- Champ ajouté en Plan 41-01 : profile.wagerMarathonWins?: number -->

<!-- Profile type (lib/types.ts:70-126) est la base de DiscoverySource via merge useVault runtime -->
```typescript
// Profile ligne 101-104 (cluster Sporée) — wagerMarathonWins merge via spread farmData
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Afficher wagerMarathonWins dans le footer FarmCodexModal</name>
  <files>components/mascot/FarmCodexModal.tsx</files>
  <read_first>
    - components/mascot/FarmCodexModal.tsx (intégral — structure + footer lignes 292-310)
    - lib/codex/discovery.ts (définition DiscoverySource — confirmer qu'elle inclut les champs profile étendus OU si cast nécessaire)
    - lib/types.ts (Profile + FarmProfileData wagerMarathonWins défini Plan 41-01)
  </read_first>
  <action>
    Dans `components/mascot/FarmCodexModal.tsx` :

    1. Localiser le footer (lignes ~292-310) :
       ```tsx
       <View style={[styles.footer, { borderTopColor: colors.text + '22' }]}>
         <TouchableOpacity onPress={handleReplayTutorial} style={[styles.replayBtn, ...]}>
           <Text style={[styles.replayLabel, { color: colors.bg }]}>{t('codex:tutorial.replay')}</Text>
         </TouchableOpacity>
       </View>
       ```

    2. Ajouter AVANT le TouchableOpacity un Text affichant le compteur :
       ```tsx
       <Text style={[styles.marathonCounter, { color: colors.textSub }]}>
         🍄 Paris gagnés : {(profile as any)?.wagerMarathonWins ?? 0}
       </Text>
       ```
       Si le type `DiscoverySource` ne propage pas `wagerMarathonWins`, soit :
       - Option A (PRÉFÉRÉE) : étendre `DiscoverySource` dans `lib/codex/discovery.ts` pour inclure `wagerMarathonWins?: number` (alignement avec Profile).
       - Option B : cast local `(profile as Profile | null)?.wagerMarathonWins`.

       Choisir A si la modif reste triviale (1 ligne), sinon B.

    3. Ajouter au StyleSheet à la fin du fichier :
       ```typescript
       marathonCounter: {
         fontSize: FontSize.sm,
         fontWeight: FontWeight.semibold,
         textAlign: 'center',
         marginBottom: Spacing.md,
       },
       ```
       Utiliser EXACTEMENT les tokens déjà importés (FontSize/FontWeight/Spacing — déjà présents dans le fichier).

    4. Ajuster le styles.footer si nécessaire pour contenir 2 éléments verticalement (le footer actuel contient 1 bouton — si `flexDirection` est `'row'`, ajouter un wrapper `<View>` colonne, sinon laisser en colonne par défaut).

    5. **NE PAS ajouter de traduction i18n pour ce texte** — Phase 41 minimaliste, texte FR inline conforme convention projet (comme "Inventaire Sporée plein" en Phase 38). Confirmer par grep existant que les toasts FR inline sont acceptés (déjà prouvé par `useFarm.ts`).
  </action>
  <verify>
    <automated>grep -c "wagerMarathonWins" components/mascot/FarmCodexModal.tsx && npx tsc --noEmit 2>&1 | grep -v "MemoryEditor.tsx\|cooklang.ts\|useVault.ts" | grep "FarmCodexModal\|error TS" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "wagerMarathonWins" components/mascot/FarmCodexModal.tsx` renvoie ≥1 ligne.
    - `grep -n "Paris gagnés" components/mascot/FarmCodexModal.tsx` renvoie exactement 1 ligne (libellé FR).
    - `grep -n "marathonCounter" components/mascot/FarmCodexModal.tsx` renvoie ≥2 lignes (usage + style).
    - `npx tsc --noEmit` ne signale aucune nouvelle erreur sur FarmCodexModal.tsx.
    - Aucun hardcoded color dans les lignes ajoutées (grep `#[0-9a-fA-F]\{3,6\}` dans la section touchée = 0 match nouveau).
  </acceptance_criteria>
  <done>Compteur affiché, typé clean, style cohérent, i18n inline FR.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Non-régression finale milestone v1.7 (SPOR-12) + privacy check</name>
  <files>N/A (exécution commandes shell)</files>
  <action>Exécuter les 5 commandes listées dans how-to-verify et coller les résultats.</action>
  <verify>Les 5 commandes renvoient les résultats attendus (0 nouvelle erreur TS, 0 failing Jest, 0 match privacy).</verify>
  <done>tsc clean, jest clean, privacy clean sur commits + docs + sources v1.7.</done>
  <what-built>
    Milestone v1.7 complète. Cette vérification clôt Phase 41 + la milestone : TypeScript clean, Jest clean, commits/docs v1.7 privacy-compliant.
  </what-built>
  <how-to-verify>
    Exécuter les commandes suivantes dans l'ordre et confirmer les résultats attendus :

    1. **TypeScript non-régression** :
       ```bash
       npx tsc --noEmit 2>&1 | tee /tmp/tsc-v17.log
       grep "error TS" /tmp/tsc-v17.log | grep -vE "MemoryEditor\.tsx|cooklang\.ts|useVault\.ts"
       ```
       **Attendu** : La dernière commande renvoie 0 ligne (aucune nouvelle erreur TS hors les 3 pré-existantes).

    2. **Jest non-régression** :
       ```bash
       npx jest --no-coverage 2>&1 | tail -30
       ```
       **Attendu** : `Tests: XXX passed, 0 failed`. Si des suites pré-existantes cassent, les identifier ; les suites touchées par Phases 38-41 (parser-modifiers, sporee-economy, wager-engine, wager-ui-helpers, useFarm-wager) doivent être 100% passing.

    3. **Privacy check commits v1.7** :
       ```bash
       git log 120d3a6^..HEAD --pretty=format:"%H %s%n%b" | grep -iE "\\b(gabriel|meffysto|waltio)\\b"
       ```
       Remplacer `120d3a6` par le premier commit Phase 38 si différent (récupérable via `git log --oneline --grep "Phase 38"` ou `git log --oneline --grep "38-01"`).
       **Attendu** : 0 match (aucun nom réel dans subject/body commits v1.7).

    4. **Privacy check docs phases** :
       ```bash
       grep -rniE "\\b(gabriel|meffysto|waltio)\\b" .planning/phases/38-* .planning/phases/39-* .planning/phases/40-* .planning/phases/41-*
       ```
       **Attendu** : 0 match.

    5. **Privacy check fichiers sources modifiés v1.7** :
       ```bash
       git diff 120d3a6^..HEAD -- 'hooks/' 'components/' 'lib/' 'app/' | grep -iE "\\b(gabriel|meffysto|waltio)\\b"
       ```
       **Attendu** : 0 match.

    Coller les 5 résultats (ou un résumé "all clean") comme signal de reprise.

    Si TS/Jest rouge → NE PAS approuver, retour en Plan 03 pour fix avant ship milestone.
  </how-to-verify>
  <resume-signal>Répondre "approved · tsc clean · jest clean · privacy clean" ou lister les régressions trouvées</resume-signal>
</task>

</tasks>

<verification>
- Compteur vanité affiché dans codex
- `npx tsc --noEmit` clean hors 3 pré-existantes
- `npx jest --no-coverage` 100% passing
- Aucun nom réel dans commits/docs/sources v1.7 (Phases 38-41)
</verification>

<success_criteria>
- SPOR-10 entièrement livré (tooltip Plan 02 + counter Plan 01 + affichage Plan 03)
- SPOR-12 validé (non-régression TS/Jest + privacy check)
- Milestone v1.7 shippable
- Zéro nouvelle dépendance npm sur la milestone (6e consécutive)
</success_criteria>

<output>
After completion, create `.planning/phases/41-polish-onboarding-codex-non-r-gression/41-03-SUMMARY.md`
</output>
