---
phase: 03-gamification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/mascot/SeasonalParticles.tsx
  - app/(tabs)/tree.tsx
autonomous: true
requirements: [GAME-02]

must_haves:
  truths:
    - "Le diorama ferme affiche des particules saisonnieres correspondant a la saison reelle sans action manuelle"
    - "Les particules saisonnieres coexistent avec les particules horaires sans surcharge visuelle"
    - "Les particules respectent useReducedMotion et se desactivent si Reduce Motion est active"
  artifacts:
    - path: "components/mascot/SeasonalParticles.tsx"
      provides: "Composant particules saisonnieres anime avec Reanimated"
      min_lines: 80
      exports: ["SeasonalParticles"]
    - path: "app/(tabs)/tree.tsx"
      provides: "Integration SeasonalParticles dans le diorama"
      contains: "SeasonalParticles"
  key_links:
    - from: "components/mascot/SeasonalParticles.tsx"
      to: "lib/mascot/seasons.ts"
      via: "import SEASONAL_PARTICLES, getCurrentSeason"
      pattern: "SEASONAL_PARTICLES\\[season\\]"
    - from: "app/(tabs)/tree.tsx"
      to: "components/mascot/SeasonalParticles.tsx"
      via: "import et rendu dans le diorama"
      pattern: "<SeasonalParticles"
---

<objective>
Afficher des particules saisonnieres animees dans le diorama ferme, correspondant a la saison reelle (fleurs de cerisier au printemps, feuilles mortes en automne, flocons en hiver, etoiles scintillantes en ete).

Purpose: GAME-02 — Les visuels saisonniers du diorama reagissent au calendrier reel sans action manuelle, rendant la ferme vivante et immersive.
Output: Composant SeasonalParticles.tsx integre dans tree.tsx, cohabitant avec AmbientParticles.
</objective>

<execution_context>
@/Users/gabrielwaltio/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gabrielwaltio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-gamification/03-RESEARCH.md

<interfaces>
<!-- Systeme saisonnier existant dans lib/mascot/seasons.ts -->

```typescript
export type Season = 'printemps' | 'ete' | 'automne' | 'hiver';

export interface SeasonalParticle {
  emoji: string;
  color: string;
  count: number;
  speed: 'slow' | 'normal' | 'fast';
  direction: 'up' | 'down' | 'float';
}

export const SEASONAL_PARTICLES: Record<Season, SeasonalParticle> = {
  printemps: { emoji: '🌸', color: '#FFB7C5', count: 6, speed: 'slow', direction: 'down' },
  ete:       { emoji: '✨', color: '#FFE082', count: 4, speed: 'slow', direction: 'float' },
  automne:   { emoji: '🍂', color: '#D4A373', count: 8, speed: 'normal', direction: 'down' },
  hiver:     { emoji: '❄️', color: '#E3F2FD', count: 10, speed: 'slow', direction: 'down' },
};

export function getCurrentSeason(date?: Date): Season;
```

<!-- AmbientParticles existant (pattern a suivre) -->

```typescript
// components/mascot/AmbientParticles.tsx
interface AmbientParticlesProps {
  containerHeight: number;
}
export function AmbientParticles({ containerHeight }: AmbientParticlesProps): JSX.Element | null;
// Utilise useReducedMotion, useSharedValue, withRepeat, withSequence, withTiming, withDelay
// containerWidth hardcode a 390
```

<!-- Integration dans tree.tsx (lignes 580-583) -->

```typescript
// app/(tabs)/tree.tsx — zone diorama
// Ligne 248: const season = getCurrentSeason();
// Ligne 580-583:
{/* Couche ambiance : particules horaires + tint */}
<View style={{ ...StyleSheet.absoluteFillObject, zIndex: 5 }} pointerEvents="none">
  <AmbientParticles containerHeight={DIORAMA_HEIGHT_BY_STAGE[stageIdx] ?? SCREEN_H * 0.60} />
</View>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tache 1 : Creer le composant SeasonalParticles</name>
  <files>components/mascot/SeasonalParticles.tsx</files>
  <read_first>
    - components/mascot/AmbientParticles.tsx
    - lib/mascot/seasons.ts
  </read_first>
  <action>
Creer `components/mascot/SeasonalParticles.tsx` — un composant autonome qui affiche les particules saisonnieres au-dessus du diorama.

**Interface :**
```typescript
interface SeasonalParticlesProps {
  season: Season;
  containerHeight: number;
}
```

**Implementation :**
1. Importer `SEASONAL_PARTICLES` et `Season` depuis `../../lib/mascot/seasons`
2. Importer les memes outils Reanimated que AmbientParticles : `useSharedValue`, `useAnimatedStyle`, `withRepeat`, `withSequence`, `withTiming`, `withDelay`, `Easing`, `useReducedMotion`
3. Creer un sous-composant `SeasonalParticleItem` avec le meme pattern que `AmbientParticle` dans AmbientParticles.tsx :
   - Props: `{ config: SeasonalParticle; index: number; containerHeight: number; containerWidth: number }`
   - `startX` = `(index * 47 + 23) % containerWidth` (meme formule)
   - `startY` : si `direction === 'down'`, demarrer a `-20` ; si `direction === 'float'`, distribuer entre 10% et 70% de la hauteur
   - Animation Y : `direction === 'down'` → `withTiming(containerHeight + 20, { duration })` avec `withRepeat(-1, false)` ; `direction === 'float'` → `withRepeat(withSequence(withTiming(-15, ...), withTiming(15, ...)), -1, true)`
   - Animation X : wobble horizontal leger `+/- 12px` avec `withRepeat(withSequence(...), -1, true)`
   - Opacity : pulse entre `0.7` et `0.2` avec `withRepeat(withSequence(...), -1, true)`
   - Duree : `speed === 'slow'` → 5000ms, `speed === 'normal'` → 3500ms, `speed === 'fast'` → 2000ms
   - Delay par particule : `index * 500`
   - Rendre un `<Text>` avec l'emoji `config.emoji` (pas un `<View>` rond — les particules saisonnieres sont des emoji)
   - Taille emoji : `fontSize: 16` pour tous
   - Guard `useReducedMotion` : si active, retourner null dans le composant particule

4. Composant principal `SeasonalParticles` :
   - Guard `useReducedMotion` au top level aussi
   - `containerWidth = 390` (meme convention que AmbientParticles)
   - Lire config depuis `SEASONAL_PARTICLES[season]`
   - Rendre `config.count` instances de `SeasonalParticleItem` dans un `<View style={StyleSheet.absoluteFill} pointerEvents="none">`

5. **Reduction de count quand particules horaires actives** (Pitfall 1 de la recherche) :
   - Importer `getTimeSlot` et `AMBIENT_CONFIGS` depuis `../../lib/mascot/ambiance`
   - Si `AMBIENT_CONFIGS[getTimeSlot()]` n'est pas `null` (particules horaires actives), diviser `config.count` par 2 avec `Math.ceil`
   - Cela evite la surcharge visuelle quand les lucioles de nuit coexistent avec les flocons d'hiver

6. Export nomme : `export function SeasonalParticles(...)`
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -c "error" || echo "0 errors"</automated>
  </verify>
  <acceptance_criteria>
    - Le fichier `components/mascot/SeasonalParticles.tsx` existe et exporte `SeasonalParticles`
    - Le composant importe `SEASONAL_PARTICLES` depuis `../../lib/mascot/seasons`
    - Le composant utilise `useReducedMotion` de react-native-reanimated
    - Le composant utilise `getTimeSlot` et `AMBIENT_CONFIGS` pour reduire le count si particules horaires actives
    - Aucune couleur hardcodee — les couleurs viennent de `SEASONAL_PARTICLES[season].color`
    - `npx tsc --noEmit` ne rapporte pas de nouvelles erreurs
  </acceptance_criteria>
  <done>SeasonalParticles.tsx cree avec animations Reanimated, emoji saisonniers, reduction de densite quand particules horaires actives, et respect de useReducedMotion</done>
</task>

<task type="auto">
  <name>Tache 2 : Integrer SeasonalParticles dans le diorama tree.tsx</name>
  <files>app/(tabs)/tree.tsx</files>
  <read_first>
    - app/(tabs)/tree.tsx
    - components/mascot/SeasonalParticles.tsx
  </read_first>
  <action>
Modifier `app/(tabs)/tree.tsx` pour ajouter la couche de particules saisonnieres au diorama.

1. Ajouter l'import en haut du fichier :
   ```typescript
   import { SeasonalParticles } from '../../components/mascot/SeasonalParticles';
   ```

2. Dans le bloc diorama (apres la couche AmbientParticles, lignes ~580-583), ajouter une nouvelle couche a `zIndex: 4` (sous les particules horaires qui sont a `zIndex: 5`) :
   ```typescript
   {/* Couche saisonnieres : particules emoji selon la saison */}
   <View style={{ ...StyleSheet.absoluteFillObject, zIndex: 4 }} pointerEvents="none">
     <SeasonalParticles
       season={season}
       containerHeight={DIORAMA_HEIGHT_BY_STAGE[stageIdx] ?? SCREEN_H * 0.60}
     />
   </View>
   ```

3. Placer ce bloc AVANT le bloc AmbientParticles existant (lignes 580-583) pour que les particules saisonnieres soient en dessous des particules horaires dans le z-order visuel.

**NE PAS :**
- Modifier `AmbientParticles.tsx` — c'est un composant horaire, on ne melange pas les concerns
- Supprimer ou modifier le bloc AmbientParticles existant
- Modifier la variable `season` existante (ligne 248) — elle est deja correcte
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -c "error" || echo "0 errors"</automated>
  </verify>
  <acceptance_criteria>
    - `app/(tabs)/tree.tsx` importe `SeasonalParticles` depuis `../../components/mascot/SeasonalParticles`
    - Le JSX contient `<SeasonalParticles season={season}` dans le bloc diorama
    - La prop `season` utilise la variable `season` deja calculee a la ligne 248 (`const season = getCurrentSeason()`)
    - Le zIndex de la couche saisonniere (4) est inferieur a celui de la couche ambiance (5)
    - Le bloc AmbientParticles existant est intact
    - `npx tsc --noEmit` ne rapporte pas de nouvelles erreurs
  </acceptance_criteria>
  <done>Le diorama affiche automatiquement des particules saisonnieres (cerisiers au printemps, etoiles en ete, feuilles en automne, flocons en hiver) correspondant a la saison reelle, superposees au sol et sous les particules horaires</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passe sans nouvelles erreurs
2. `grep -r "SeasonalParticles" app/ components/` confirme l'integration
3. `grep "SEASONAL_PARTICLES" components/mascot/SeasonalParticles.tsx` confirme l'utilisation des configs existantes
4. `grep "useReducedMotion" components/mascot/SeasonalParticles.tsx` confirme le respect de l'accessibilite
</verification>

<success_criteria>
- Le diorama ferme affiche des particules emoji saisonnieres (cerisiers, etoiles, feuilles, flocons) correspondant a `getCurrentSeason()`
- Les particules saisonnieres et horaires coexistent sans surcharge (count reduit de 50% quand les deux sont actives)
- useReducedMotion desactive les animations si Reduce Motion est active
- Aucune regression sur les particules horaires existantes (AmbientParticles intact)
- `npx tsc --noEmit` passe
</success_criteria>

<output>
After completion, create `.planning/phases/03-gamification/03-01-SUMMARY.md`
</output>
