---
phase: 09-cadeaux-familiaux
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - hooks/useFarm.ts
  - components/mascot/GiftSenderSheet.tsx
  - components/mascot/GiftReceiptModal.tsx
  - components/mascot/CraftSheet.tsx
  - app/(tabs)/tree.tsx
  - hooks/useVault.ts
autonomous: false

requirements: [SOC-01, SOC-02]

must_haves:
  truths:
    - "Un long-press sur un item d'inventaire dans CraftSheet ouvre le GiftSenderSheet"
    - "Le GiftSenderSheet affiche les avatars des profils familiaux et un selecteur de quantite"
    - "Envoyer un cadeau retire l'item de l'inventaire expediteur et depose un fichier gifts-pending-{recipientId}.md"
    - "Au refresh, si un fichier gifts-pending-{profileId}.md existe, les items sont ajoutes a l'inventaire et la GiftReceiptModal s'affiche"
    - "La GiftReceiptModal anime un paquet qui tombe avec spring bounce, confetti, haptic, et message expediteur"
    - "L'historique des 10 derniers cadeaux est visible dans le CraftSheet onglet inventaire"
    - "Le compteur anti-abus bloque apres 5 envois/jour avec message d'erreur"
  artifacts:
    - path: "components/mascot/GiftSenderSheet.tsx"
      provides: "Bottom sheet selection destinataire + quantite + envoi"
      min_lines: 80
    - path: "components/mascot/GiftReceiptModal.tsx"
      provides: "Modal animee reception cadeau avec spring + confetti + haptic"
      min_lines: 100
    - path: "hooks/useFarm.ts"
      provides: "sendGift et receiveGifts callbacks"
      contains: "sendGift"
  key_links:
    - from: "components/mascot/CraftSheet.tsx"
      to: "components/mascot/GiftSenderSheet.tsx"
      via: "onLongPress -> onOfferItem prop -> ouvre GiftSenderSheet"
      pattern: "onLongPress.*onOfferItem"
    - from: "hooks/useFarm.ts"
      to: "lib/mascot/gift-engine.ts"
      via: "import removeFromInventory, addGiftToInventory, parsePendingGifts"
      pattern: "import.*gift-engine"
    - from: "hooks/useVault.ts"
      to: "hooks/useFarm.ts"
      via: "refreshFarm detecte et consomme gifts-pending-{id}.md"
      pattern: "gifts-pending"
    - from: "app/(tabs)/tree.tsx"
      to: "components/mascot/GiftReceiptModal.tsx"
      via: "pendingGiftsToShow state declenche GiftReceiptModal"
      pattern: "GiftReceiptModal"
---

<objective>
Integrer les cadeaux dans l'UI : long-press inventaire pour offrir (GiftSenderSheet), reception animee au refresh (GiftReceiptModal), sendGift/receiveGifts dans useFarm, detection du pending dans refreshFarm, historique dans CraftSheet, et cablage dans tree.tsx.

Purpose: Livrer la fonctionnalite complete de cadeaux familiaux — de l'envoi a la reception avec animation et persistance.
Output: 2 nouveaux composants (GiftSenderSheet, GiftReceiptModal), useFarm etendu, CraftSheet avec long-press, tree.tsx cable.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-cadeaux-familiaux/09-CONTEXT.md
@.planning/phases/09-cadeaux-familiaux/09-RESEARCH.md
@.planning/phases/09-cadeaux-familiaux/09-01-SUMMARY.md
@hooks/useFarm.ts
@hooks/useVault.ts
@components/mascot/CraftSheet.tsx
@components/mascot/CompanionPicker.tsx
@app/(tabs)/tree.tsx
@lib/mascot/gift-engine.ts
@lib/parser.ts
@lib/types.ts

<interfaces>
<!-- Depuis Plan 01 — gift-engine.ts -->
```typescript
export interface GiftEntry {
  sender_id: string;
  sender_name: string;
  sender_avatar: string;
  item_type: 'harvest' | 'rare_seed' | 'crafted' | 'building_resource';
  item_id: string;
  quantity: number;
  sent_at: string;
}
export interface PendingGifts { gifts: GiftEntry[]; }
export interface GiftHistoryEntry {
  date: string; direction: 'sent' | 'received';
  fromId: string; toId: string;
  itemType: string; itemId: string; quantity: number;
}
export const MAX_GIFTS_PER_DAY = 5;
export function parsePendingGifts(content: string): PendingGifts;
export function serializePendingGifts(gifts: GiftEntry[]): string;
export function canSendGiftToday(field: string | undefined, now?: Date): boolean;
export function incrementGiftsSent(field: string | undefined, now?: Date): string;
export function addGiftToInventory(farmData: FarmProfileData, entry: GiftEntry): FarmProfileData;
export function removeFromInventory(farmData: FarmProfileData, itemType: string, itemId: string, qty: number): { success: boolean; updated: FarmProfileData };
export function buildGiftHistoryEntry(direction: 'sent'|'received', fromId: string, toId: string, itemType: string, itemId: string, qty: number, now?: Date): string;
export function parseGiftHistory(csv: string | undefined): GiftHistoryEntry[];
```

<!-- FarmProfileData etendu (Plan 01) -->
```typescript
export interface FarmProfileData {
  // ... champs existants ...
  giftHistory?: string;      // CSV pipe-separe des 10 derniers echanges
  giftsSentToday?: string;   // format "count|YYYY-MM-DD" anti-abus
}
```

<!-- useFarm retour existant -->
```typescript
return {
  plant, harvest, buyBuilding, upgradeBuildingAction,
  collectBuildingResources, collectPassiveIncome,
  craft, sellHarvest, sellCrafted, unlockTech,
  checkWear, repairWear, getWearEffects, getWearEvents,
};
```

<!-- CraftSheet props existantes -->
```typescript
interface CraftSheetProps {
  visible: boolean; onClose: () => void; profileId: string;
  coins: number; harvestInventory: HarvestInventory;
  farmInventory: FarmInventory; craftedItems: CraftedItem[];
  treeStage: TreeStage;
  onCraft: (recipeId: string) => Promise<CraftedItem | null>;
  onSellHarvest: (cropId: string) => Promise<number>;
  // ... autres handlers
}
```

<!-- refreshFarm dans useVault.ts -->
```typescript
const refreshFarm = useCallback(async (profileId: string) => {
  if (!vaultRef.current) return;
  const content = await vaultRef.current.readFile(farmFile(profileId));
  const farmData = parseFarmProfile(content);
  setProfiles(prev => prev.map(p =>
    p.id === profileId ? { ...p, ...farmData } : p
  ));
}, []);
```

<!-- CompanionPicker pattern (Modal pageSheet) -->
```typescript
<Modal visible={visible} animationType="slide" presentationStyle="pageSheet"
  onRequestClose={onClose}>
  <ModalHeader title="..." onClose={onClose} />
  {/* content */}
</Modal>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: sendGift et receiveGifts dans useFarm + detection pending dans refreshFarm</name>
  <files>hooks/useFarm.ts, hooks/useVault.ts</files>
  <read_first>
    - hooks/useFarm.ts (pattern writeProfileFields, deductCoins, gamiFile, farmFile, retour du hook)
    - hooks/useVault.ts (refreshFarm implementation ligne ~2484, pattern readFile/writeFile)
    - lib/mascot/gift-engine.ts (toutes les fonctions a importer — crees dans Plan 01)
    - lib/parser.ts (parseFarmProfile, serializeFarmProfile — structure complete)
    - lib/gamification/engine.ts (addPoints signature)
    - lib/notifications.ts (dispatchNotificationAsync signature)
  </read_first>
  <action>
**hooks/useFarm.ts** — Ajouter deux nouveaux callbacks :

1. `sendGift` callback :
```typescript
const sendGift = useCallback(async (
  senderId: string,
  recipientId: string,
  recipientName: string,
  itemType: 'harvest' | 'rare_seed' | 'crafted' | 'building_resource',
  itemId: string,
  quantity: number,
): Promise<{ success: boolean; error?: string }> => {
  if (!vault) return { success: false, error: 'no_vault' };

  // 1. Lire farm-{senderId}.md
  const senderContent = await vault.readFile(farmFile(senderId)).catch(() => '');
  const senderFarm = parseFarmProfile(senderContent);

  // 2. Verifier anti-abus
  if (!canSendGiftToday(senderFarm.giftsSentToday)) {
    return { success: false, error: 'daily_limit' };
  }

  // 3. Retirer de l'inventaire expediteur
  const { success, updated } = removeFromInventory(senderFarm, itemType, itemId, quantity);
  if (!success) return { success: false, error: 'not_enough' };

  // 4. Construire le GiftEntry
  const senderProfile = profiles?.find(p => p.id === senderId);
  const gift: GiftEntry = {
    sender_id: senderId,
    sender_name: senderProfile?.name ?? senderId,
    sender_avatar: senderProfile?.avatar ?? '🎁',
    item_type: itemType,
    item_id: itemId,
    quantity,
    sent_at: new Date().toISOString(),
  };

  // 5. Lire pending existant, merger, ecrire
  const pendingFile = `gifts-pending-${recipientId}.md`;
  const pendingContent = await vault.readFile(pendingFile).catch(() => '');
  const pending = parsePendingGifts(pendingContent);
  pending.gifts.push(gift);
  await vault.writeFile(pendingFile, serializePendingGifts(pending.gifts));

  // 6. Ecrire farm expediteur avec inventaire reduit + historique + compteur
  const historyEntry = buildGiftHistoryEntry('sent', senderId, recipientId, itemType, itemId, quantity);
  const existingHistory = updated.giftHistory ?? '';
  const allHistory = existingHistory ? `${historyEntry}, ${existingHistory}` : historyEntry;
  // Garder max 10 entrees
  const trimmedHistory = allHistory.split(', ').slice(0, 10).join(', ');

  const fieldsToWrite: Record<string, string> = {};
  // Serialiser tous les champs inventaire modifies selon itemType
  // (utiliser serializeHarvestInventory, serializeCraftedItems, serializeRareSeeds, serializeInventory selon le type)
  // + gift_history et gifts_sent_today
  fieldsToWrite['gift_history'] = trimmedHistory;
  fieldsToWrite['gifts_sent_today'] = incrementGiftsSent(senderFarm.giftsSentToday);
  // Ajouter le champ inventaire modifie (harvest_inventory, crafted_items, rare_seeds, ou farm_inventory)
  await writeProfileFields(senderId, fieldsToWrite);

  // 7. +5 XP expediteur (pattern identique a completeSagaChapter)
  const gamiContent = await vault.readFile(gamiFile(senderId)).catch(() => '');
  const gamiData = parseGamification(gamiContent);
  if (senderProfile) {
    const { profile: updatedProfile, entry } = addPoints(senderProfile, 5, '🎁 Cadeau envoye');
    const updatedGami = { ...gamiData, points: updatedProfile.points, level: updatedProfile.level, coins: updatedProfile.coins };
    await vault.writeFile(gamiFile(senderId), serializeGamification(senderProfile.name, updatedGami));
    await refreshGamification(senderId);
  }

  // 8. Notification Telegram (fire-and-forget)
  // dispatchNotificationAsync('gift_received', { sender: senderProfile?.name ?? '', item: itemId, quantity: String(quantity) }, notifPrefs);

  await refreshFarm(senderId);
  return { success: true };
}, [vault, profiles, writeProfileFields, refreshFarm, refreshGamification]);
```

Importer depuis gift-engine : `parsePendingGifts`, `serializePendingGifts`, `canSendGiftToday`, `incrementGiftsSent`, `removeFromInventory`, `buildGiftHistoryEntry`, `GiftEntry`.
Importer depuis craft-engine les serializers selon besoin.
Importer `addPoints` depuis `../lib/gamification/engine`.
Importer `parseGamification`, `serializeGamification` depuis `../lib/parser`.

2. `receiveGifts` callback :
```typescript
const receiveGifts = useCallback(async (
  recipientId: string,
): Promise<GiftEntry[]> => {
  if (!vault) return [];

  const pendingFile = `gifts-pending-${recipientId}.md`;
  const pendingContent = await vault.readFile(pendingFile).catch(() => null);
  if (!pendingContent) return [];

  const pending = parsePendingGifts(pendingContent);
  if (pending.gifts.length === 0) return [];

  // 1. CLAIM FIRST — supprimer le fichier pending AVANT d'appliquer (evite double-consommation)
  await vault.deleteFile(pendingFile);

  // 2. Lire farm destinataire
  const content = await vault.readFile(farmFile(recipientId)).catch(() => '');
  let farmData = parseFarmProfile(content);

  // 3. Appliquer chaque cadeau
  for (const gift of pending.gifts) {
    farmData = addGiftToInventory(farmData, gift);
  }

  // 4. Ajouter a l'historique
  const recipientProfile = profiles?.find(p => p.id === recipientId);
  const newEntries = pending.gifts.map(g =>
    buildGiftHistoryEntry('received', g.sender_id, recipientId, g.item_type, g.item_id, g.quantity)
  );
  const existingHistory = farmData.giftHistory ?? '';
  const allHistory = [...newEntries, ...existingHistory.split(', ').filter(Boolean)].slice(0, 10).join(', ');

  // 5. Ecrire farm destinataire complet via serializeFarmProfile
  const profileName = recipientProfile?.name ?? recipientId;
  await vault.writeFile(farmFile(recipientId), serializeFarmProfile(profileName, { ...farmData, giftHistory: allHistory }));

  await refreshFarm(recipientId);
  return pending.gifts;
}, [vault, profiles, refreshFarm]);
```

Importer `addGiftToInventory` depuis gift-engine.

3. Ajouter `sendGift` et `receiveGifts` au retour du hook :
```typescript
return {
  plant, harvest, buyBuilding, upgradeBuildingAction,
  collectBuildingResources, collectPassiveIncome,
  craft, sellHarvest, sellCrafted, unlockTech,
  checkWear, repairWear, getWearEffects, getWearEvents,
  sendGift, receiveGifts,  // NOUVEAU
};
```

**hooks/useVault.ts** — NE PAS modifier refreshFarm directement. La detection du pending sera faite dans tree.tsx au moment du refresh (appeler `receiveGifts` depuis useFarm apres le refresh). Cela evite de coupler useVault.ts au systeme de cadeaux.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "useFarm|gift" | head -10; grep "sendGift" hooks/useFarm.ts | head -3; grep "receiveGifts" hooks/useFarm.ts | head -3</automated>
  </verify>
  <acceptance_criteria>
    - hooks/useFarm.ts contient "const sendGift = useCallback"
    - hooks/useFarm.ts contient "const receiveGifts = useCallback"
    - hooks/useFarm.ts retourne sendGift et receiveGifts dans l'objet de retour
    - hooks/useFarm.ts importe parsePendingGifts, serializePendingGifts, canSendGiftToday, incrementGiftsSent, removeFromInventory, addGiftToInventory, buildGiftHistoryEntry, GiftEntry depuis gift-engine
    - hooks/useFarm.ts contient "await vault.deleteFile(pendingFile)" AVANT "addGiftToInventory" dans receiveGifts (claim first)
    - npx tsc --noEmit ne produit aucune nouvelle erreur liee a useFarm
  </acceptance_criteria>
  <done>sendGift retire l'item de l'expediteur, depose le pending, ajoute XP. receiveGifts claim-first le pending, ajoute les items au destinataire. Les deux callbacks sont exposes par useFarm.</done>
</task>

<task type="auto">
  <name>Task 2: GiftSenderSheet + GiftReceiptModal + long-press CraftSheet + cablage tree.tsx</name>
  <files>components/mascot/GiftSenderSheet.tsx, components/mascot/GiftReceiptModal.tsx, components/mascot/CraftSheet.tsx, app/(tabs)/tree.tsx</files>
  <read_first>
    - components/mascot/CompanionPicker.tsx (pattern Modal pageSheet + ModalHeader + avatars)
    - components/mascot/CraftSheet.tsx (onglet inventaire, props, TouchableOpacity items)
    - components/mascot/EvolutionOverlay.tsx (pattern ConfettiCannon usage)
    - app/(tabs)/tree.tsx (state management, overlays, refresh pattern, useFarm usage)
    - constants/spacing.ts (Spacing tokens)
    - constants/typography.ts (FontSize tokens)
    - contexts/ThemeContext.tsx (useThemeColors pattern)
    - hooks/useFarm.ts (sendGift, receiveGifts — crees Task 1)
  </read_first>
  <action>
**components/mascot/GiftSenderSheet.tsx** — Nouveau composant :

Props :
```typescript
interface GiftSenderSheetProps {
  visible: boolean;
  onClose: () => void;
  itemType: 'harvest' | 'rare_seed' | 'crafted' | 'building_resource';
  itemId: string;
  itemName: string;
  maxQuantity: number;
  profiles: Profile[];  // tous les profils sauf l'expediteur
  onSend: (recipientId: string, quantity: number) => Promise<{ success: boolean; error?: string }>;
}
```

Implementation :
- `Modal` avec `presentationStyle="pageSheet"` et `animationType="slide"` (pattern CompanionPicker)
- `ModalHeader` avec titre `t('gamification:gift_send_title')` et onClose
- Afficher l'item selectionne en haut (nom + emoji/icone selon type)
- Grille de profils (avatars circulaires 56x56 avec nom en dessous), `selectedRecipient` state
- Selecteur quantite : boutons `-` et `+` autour d'un Text affichant la quantite, min 1, max `maxQuantity`
- Bouton "Envoyer" (components/ui Button ou TouchableOpacity styled), disabled si pas de destinataire selectionne
- `onPress` du bouton appelle `onSend(selectedRecipient, quantity)`, affiche toast success ou erreur via le retour
- Couleurs via `useThemeColors()`, Spacing et FontSize tokens, StyleSheet.create pour styles statiques
- Haptic `Haptics.selectionAsync()` au tap sur un avatar

**components/mascot/GiftReceiptModal.tsx** — Nouveau composant :

Props :
```typescript
interface GiftReceiptModalProps {
  visible: boolean;
  gifts: GiftEntry[];
  onDone: () => void;
}
```

Implementation :
- `Modal` transparent, `animationType="none"` (on anime manuellement)
- Fond semi-transparent `rgba(0,0,0,0.5)` avec Animated.View
- Paquet cadeau anime :
  - `useSharedValue` pour translateY (demarre a -300), scale (demarre a 0.3), opacity (demarre a 0)
  - `useEffect` au mount : `translateY.value = withSpring(0, { damping: 12, stiffness: 180 })`, `scale.value = withSpring(1, ...)`, `opacity.value = withTiming(1, { duration: 300 })`
  - Emoji cadeau `🎁` en 64px puis apres atterrissage, pulse scale 1 -> 1.2 -> 1 via `withSequence(withSpring(1.2, { damping: 8, stiffness: 200 }), withSpring(1, { damping: 12, stiffness: 180 }))`
- Apres le pulse (300ms delay via `withDelay`), afficher :
  - Avatar expediteur + texte `t('gamification:gift_received_message', { sender, quantity, item })` 
  - Texte pour chaque gift (si multiple, lister)
- `ConfettiCannon` (ref, `count={50}`, `origin={{ x: -10, y: 0 }}`, `fadeOut`, meme pattern que loot box)
  - Declencher le confetti apres le pulse (`confettiRef.current?.start()`)
- `Haptics.impactAsync(Haptics.ImpactFeedbackStyle.Medium)` au moment du bounce
- Bouton "Super !" en bas pour appeler `onDone`
- Couleurs via `useThemeColors()`, pas de couleurs hardcodees

**components/mascot/CraftSheet.tsx** — Modifications :

1. Ajouter prop `onOfferItem?: (itemType: string, itemId: string, maxQty: number, itemName: string) => void` a `CraftSheetProps`
2. Sur chaque `TouchableOpacity` d'item dans l'onglet "inventaire" (recoltes, crafts, graines rares, ressources batiments), ajouter `onLongPress={() => onOfferItem?.(type, id, qty, name)}` avec `delayLongPress={400}`
3. Ajouter une section "Cadeaux recents" en bas de l'onglet inventaire :
   - Prop `giftHistory?: string` dans CraftSheetProps
   - Parser avec `parseGiftHistory(giftHistory)` depuis gift-engine
   - Afficher les 10 derniers en liste simple : icone direction (fleche), nom, item, date (format JJ/MM/AAAA via date-fns `format(parseISO(date), 'dd/MM/yyyy')`)
   - Titre section : `t('gamification:gift_history_title')`

**app/(tabs)/tree.tsx** — Cablage :

1. Importer `GiftSenderSheet` et `GiftReceiptModal`
2. State pour le gift sender :
   ```typescript
   const [giftOffer, setGiftOffer] = useState<{ itemType: string; itemId: string; maxQty: number; itemName: string } | null>(null);
   ```
3. State pour la reception :
   ```typescript
   const [pendingGiftsToShow, setPendingGiftsToShow] = useState<GiftEntry[]>([]);
   ```
4. Passer `onOfferItem={setGiftOffer}` a CraftSheet
5. Rendre `GiftSenderSheet` :
   ```typescript
   <GiftSenderSheet
     visible={!!giftOffer}
     onClose={() => setGiftOffer(null)}
     itemType={giftOffer?.itemType}
     itemId={giftOffer?.itemId}
     itemName={giftOffer?.itemName}
     maxQuantity={giftOffer?.maxQty ?? 1}
     profiles={profiles.filter(p => p.id !== activeProfileId)}
     onSend={async (recipientId, qty) => {
       const result = await sendGift(activeProfileId, recipientId, '', giftOffer!.itemType, giftOffer!.itemId, qty);
       if (result.success) setGiftOffer(null);
       return result;
     }}
   />
   ```
6. Detection des cadeaux au refresh/mount — dans le useEffect existant de refresh ou apres `refreshFarm`, appeler :
   ```typescript
   const received = await receiveGifts(activeProfileId);
   if (received.length > 0) setPendingGiftsToShow(received);
   ```
   Placer cet appel apres le refresh initial de l'arbre (pas a chaque re-render, une seule fois au mount ou au changement de profil).
7. Rendre `GiftReceiptModal` :
   ```typescript
   <GiftReceiptModal
     visible={pendingGiftsToShow.length > 0}
     gifts={pendingGiftsToShow}
     onDone={() => setPendingGiftsToShow([])}
   />
   ```
8. Passer `giftHistory` a CraftSheet depuis le profil actif : `giftHistory={activeProfile?.giftHistory}`
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "Gift|CraftSheet|tree" | head -10; grep "onLongPress" components/mascot/CraftSheet.tsx | head -3; grep "GiftReceiptModal" "app/(tabs)/tree.tsx" | head -3; grep "GiftSenderSheet" "app/(tabs)/tree.tsx" | head -3</automated>
  </verify>
  <acceptance_criteria>
    - components/mascot/GiftSenderSheet.tsx existe avec export function GiftSenderSheet
    - components/mascot/GiftSenderSheet.tsx contient "presentationStyle" et "ModalHeader"
    - components/mascot/GiftReceiptModal.tsx existe avec export function GiftReceiptModal
    - components/mascot/GiftReceiptModal.tsx contient "withSpring" et "ConfettiCannon" et "Haptics.impactAsync"
    - components/mascot/GiftReceiptModal.tsx ne contient PAS "perspective" dans les transforms
    - components/mascot/CraftSheet.tsx contient "onLongPress" avec "delayLongPress"
    - components/mascot/CraftSheet.tsx contient "onOfferItem" dans les props
    - components/mascot/CraftSheet.tsx contient "gift_history_title" (section historique)
    - app/(tabs)/tree.tsx contient "GiftSenderSheet" et "GiftReceiptModal"
    - app/(tabs)/tree.tsx contient "receiveGifts" et "setPendingGiftsToShow"
    - app/(tabs)/tree.tsx contient "giftOffer" state
    - npx tsc --noEmit ne produit aucune nouvelle erreur liee aux modifications
  </acceptance_criteria>
  <done>Le long-press inventaire ouvre le GiftSenderSheet, l'envoi depose le pending et retire l'item, la reception anime la GiftReceiptModal avec confetti et haptic, l'historique est visible dans CraftSheet</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verification visuelle et fonctionnelle des cadeaux familiaux</name>
  <files>app/(tabs)/tree.tsx</files>
  <action>
Verification humaine du systeme complet de cadeaux familiaux.

Etapes de test :
1. Ouvrir l'app, aller sur l'ecran arbre, ouvrir l'Atelier (CraftSheet)
2. Onglet "Inventaire" — verifier qu'un long-press (maintenir 400ms) sur un item ouvre le bottom sheet "Offrir un cadeau"
3. Verifier que les avatars des autres profils s'affichent, selectionner un destinataire
4. Ajuster la quantite avec +/-, puis envoyer
5. Verifier que l'item est retire de l'inventaire de l'expediteur
6. Changer de profil vers le destinataire, rafraichir (pull-to-refresh ou recharger l'ecran arbre)
7. Verifier que la modal de reception s'affiche : animation paquet qui tombe, confetti, haptic feedback, message "X t'a envoye Y !"
8. Fermer la modal, verifier que l'item est dans l'inventaire du destinataire
9. Verifier la section "Cadeaux recents" en bas de l'onglet inventaire — l'echange doit y apparaitre
10. Tester l'anti-abus : envoyer 5 cadeaux puis verifier le message de limite
  </action>
  <verify>Verification humaine sur device</verify>
  <done>L'utilisateur confirme que le flow complet fonctionne : envoi, reception animee, historique, anti-abus</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passe sans nouvelles erreurs
- L'onglet inventaire de CraftSheet a `onLongPress` sur tous les items
- GiftSenderSheet utilise `useThemeColors()` — aucune couleur hardcodee
- GiftReceiptModal utilise `react-native-reanimated` (withSpring) — pas RN Animated
- GiftReceiptModal n'utilise pas `perspective` dans les transforms
- tree.tsx detecte les pending au refresh et affiche GiftReceiptModal
- L'item est retire de l'expediteur AVANT le depot du pending (pas de duplication)
- Le fichier pending est supprime AVANT l'ajout a l'inventaire (claim first, pas de double)
</verification>

<success_criteria>
Un membre peut long-presser un item d'inventaire, choisir un destinataire, ajuster la quantite, et envoyer. Le destinataire voit une animation cadeau au prochain refresh avec confetti et haptic. L'item est transfere sans duplication. L'historique des 10 derniers echanges est visible. Le compteur anti-abus limite a 5/jour.
</success_criteria>

<output>
After completion, create `.planning/phases/09-cadeaux-familiaux/09-02-SUMMARY.md`
</output>
