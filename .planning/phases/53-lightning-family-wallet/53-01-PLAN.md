---
phase: 53-lightning-family-wallet
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/lightning/types.ts
  - lib/lightning/family-credentials.ts
  - lib/lightning/lnbits-client.ts
  - lib/lightning/resolve-recipient.ts
  - lib/lightning/audit-log.ts
  - lib/lightning/daily-cap.ts
  - lib/lightning/trigger-mode.ts
  - lib/lightning/payout-queue.ts
  - lib/lightning/parent-notif.ts
  - lib/lightning/migration.ts
  - lib/lightning/lightning-events.ts
  - lib/lightning/index.ts
  - lib/lightning/__tests__/resolve-recipient.test.ts
  - lib/lightning/__tests__/audit-log.test.ts
  - lib/lightning/__tests__/daily-cap.test.ts
  - lib/lightning/__tests__/trigger-mode.test.ts
  - lib/lightning/__tests__/payout-queue.test.ts
  - lib/lightning/__tests__/idempotence.test.ts
  - lib/lightning/__tests__/migration.test.ts
  - lib/lightning/__tests__/__mocks__/secure-store.ts
  - package.json
  - app.json
autonomous: true
requirements: [REQ-2, REQ-3, REQ-4, REQ-5, REQ-6, REQ-7, REQ-11, REQ-12]
requirements_addressed: [REQ-2, REQ-3, REQ-4, REQ-5, REQ-6, REQ-7, REQ-11, REQ-12]
tags: [lightning, lnbits, secure-store, jest, idempotence, audit-log]

user_setup:
  - service: async-storage
    why: "Audit log Lightning ~160 KB en régime — dépasse limite SecureStore 2 KB. Décision RESEARCH Q1 RESOLVED → AsyncStorage. Dépendance NEUVE (vault-cache.ts utilise expo-file-system, pas AsyncStorage)."
    env_vars: []
    dashboard_config:
      - task: "Étape 1 : `npx expo install @react-native-async-storage/async-storage` (ajoute la dépendance au package.json). Étape 2 : rebuild dev-client : `npx expo run:ios --device`. Étape 3 : vérifier installation : `grep -q '\"@react-native-async-storage/async-storage\"' package.json && echo OK`."
        location: "Terminal local + iOS dev-client"
  - service: expo-camera
    why: "Scanner QR bolt11 (REQ-9 — utilisé Plan 03b). Installation incluse dans ce plan pour préparer la wave UI."
    env_vars: []
    dashboard_config:
      - task: "Aucun — `npx expo install expo-camera` ajouté à package.json puis `npx expo run:ios --device` requis pour rebuild dev-client APRÈS ce plan (avant Plan 03b)."
        location: "Terminal local"

must_haves:
  truths:
    - "Le module `lib/lightning` expose `processTaskCompletionForLightning({task, deps})` et un set de fonctions pures (resolveRecipient, checkDailyCap, dispatchTrigger, enqueuePayout) consommables par Plan 02 — vérifiable via signature Jest test + import test exécutable"
    - "La fonction pure `resolveRecipient(task, profiles, memberWallets, activeProfileId)` couvre les 6 cas SPEC acceptance REQ-2 (mention unique configurée, mention non configurée, no mention + active configuré, no mention + active non configuré, 2 mentions ambiguës, mention adulte)"
    - "Le type `MemberWalletMapping` (anciennement `ChildWalletMapping`) est défini avec champ `adminKey?: string` optionnel — REQ-10 + REQ-12"
    - "Le type `FamilyLightningConfig` contient `triggerMode: 'instant'|'daily-review'|'hybrid'` (default `instant`) ET `dailyCapPerMember: number` (default 1000, range 100-10000) — REQ-3 + REQ-4 + SPEC Constraint #6 (plafonnage par construction)"
    - "`loadFamilyConfig` accepte rétro-compat `children` et `members` à la lecture, `saveFamilyConfig` écrit toujours `members` — REQ-12"
    - "La fonction `dispatchTrigger(cumulSatsToday, mode, satsAmount)` retourne `'instant'` | `'queue'` selon REQ-3 (hybrid = instant si cumul < 100 else queue)"
    - "La fonction `checkDailyCap(profileId, satsAmount, audit, capPerMember)` retourne `'allowed'` | `'capped'` atomic avant tout call réseau — REQ-4"
    - "L'audit log expose `loadAudit/appendAudit/clearAudit/purgeOlderThan(90)` avec entrées `{ts, profileId, taskId, sats, status, paymentHash?, error?}` et purge > 90 j idempotente au load — REQ-7"
    - "Le statut `'already_paid_today'` est retourné quand un `taskId+date` est déjà `paid` dans l'audit — REQ-6"
    - "La queue offline expose `enqueuePayout/loadQueue/flushQueue/markFailed` avec items `{taskId, profileId, sats, attemptCount, lastError?, queuedAt, reason}` — REQ-5"
    - "La migration single→family est idempotente (3 cas testés : single seul → migré ; family seul → no-op ; les deux → cleanup single sans modifier family) — REQ-11 + Pitfall #9"
    - "`createInvoice(amountSats, memo, extra?)` accepte un paramètre `extra` (Record<string,string|number>) injecté dans le body LNbits — REQ-6 idempotency tag"
    - "Le bus d'événements `lightning-events.ts` expose `onPayoutSuccess/emitPayoutSuccess/onPayoutFailed/emitPayoutFailed` pour découpler hook ↔ UI"
    - "D-10 : `lib/lightning/parent-notif.ts` expose `maybeNotifyParent({events, cumulSatsToday, pending, failed})` qui agrège max 1 notif locale/jour via expo-notifications, silencieuse entre 9h et 16h (heures école), wording chaleureux/factuel sans promotion sats, timestamp dernière notif persisté en SecureStore — règles D-10 verbatim (cap 1/j, fenêtre silencieuse 9-16h, agrégation événements de la journée)"
    - "`@react-native-async-storage/async-storage` est listé dans `package.json` (`npx expo install @react-native-async-storage/async-storage`) — dépendance NEUVE requise par `lib/lightning/audit-log.ts` (RESEARCH Q1 RESOLVED)"
    - "`expo-camera` est listé dans `package.json` (`npx expo install expo-camera`) et `app.json` contient `NSCameraUsageDescription` en français — préparation Plan 03b"
    - "Tous les tests Jest passent : `npx jest --no-coverage lib/lightning/__tests__/` exits 0"
  artifacts:
    - path: "lib/lightning/types.ts"
      provides: "Types étendus : MemberWalletMapping (rename Child→Member, adminKey? optionnel), FamilyLightningConfig (+ triggerMode, dailyCapPerMember, members: [])"
      contains: "export interface MemberWalletMapping"
    - path: "lib/lightning/family-credentials.ts"
      provides: "load/save/clear + backward-compat children→members + parse triggerMode + parse dailyCapPerMember (clamped 100-10000)"
      contains: "members: membersArray.filter"
    - path: "lib/lightning/resolve-recipient.ts"
      provides: "Pure function resolveRecipient — 6 cas REQ-2"
      contains: "export function resolveRecipient"
    - path: "lib/lightning/audit-log.ts"
      provides: "AsyncStorage-based audit log 90j glissants (Pitfall #1 — AsyncStorage retenu, pas SecureStore)"
      contains: "export async function loadAudit"
    - path: "lib/lightning/daily-cap.ts"
      provides: "checkDailyCap atomic + getCumulSatsToday(profileId, audit)"
      contains: "export function checkDailyCap"
    - path: "lib/lightning/trigger-mode.ts"
      provides: "dispatchTrigger(cumul, mode, sats) → 'instant' | 'queue'"
      contains: "export function dispatchTrigger"
    - path: "lib/lightning/payout-queue.ts"
      provides: "Queue SecureStore + flushQueue + cap 5 attempts"
      contains: "export async function enqueuePayout"
    - path: "lib/lightning/parent-notif.ts"
      provides: "Agrégation 1/jour + silencieuse 9-16h + timestamp SecureStore"
      contains: "export async function maybeSendParentNotif"
    - path: "lib/lightning/migration.ts"
      provides: "migrateSingleToFamily idempotent (Pitfall #9)"
      contains: "export async function migrateSingleToFamily"
    - path: "lib/lightning/lightning-events.ts"
      provides: "Bus d'événements module-level (Pattern 3 RESEARCH)"
      contains: "export function onPayoutSuccess"
    - path: "lib/lightning/lnbits-client.ts"
      provides: "createInvoice étendu avec param `extra?` pour idempotency tag (Pitfall #4 fallback `bolt11 ?? payment_request` conservé)"
      contains: "extra?: Record<string"
    - path: "lib/lightning/index.ts"
      provides: "Barrel — nouveaux exports + retrait exports legacy single-wallet"
      contains: "export { resolveRecipient }"
    - path: "lib/lightning/__tests__/resolve-recipient.test.ts"
      provides: "6 cas REQ-2 SPEC acceptance"
      contains: "describe('resolveRecipient'"
    - path: "lib/lightning/__tests__/audit-log.test.ts"
      provides: "Purge > 90 j via fake timers + idempotence load"
      contains: "jest.useFakeTimers"
    - path: "lib/lightning/__tests__/daily-cap.test.ts"
      provides: "Cumul atomic + cap dépassé"
      contains: "describe('checkDailyCap'"
    - path: "lib/lightning/__tests__/trigger-mode.test.ts"
      provides: "Dispatch instant/daily-review/hybrid"
      contains: "describe('dispatchTrigger'"
    - path: "lib/lightning/__tests__/payout-queue.test.ts"
      provides: "Enqueue/flush/cap 5 attempts"
      contains: "describe('payout-queue'"
    - path: "lib/lightning/__tests__/idempotence.test.ts"
      provides: "REQ-6 already_paid_today"
      contains: "already_paid_today"
    - path: "lib/lightning/__tests__/migration.test.ts"
      provides: "3 cas Pitfall #9"
      contains: "describe('migrateSingleToFamily'"
    - path: "package.json"
      provides: "expo-camera ajouté (préparation Plan 03)"
      contains: "expo-camera"
    - path: "app.json"
      provides: "NSCameraUsageDescription FR + plugin expo-camera"
      contains: "NSCameraUsageDescription"
  key_links:
    - from: "lib/lightning/resolve-recipient.ts"
      to: "lib/types.ts (Task, Profile)"
      via: "import type"
      pattern: "import type \\{.*Task.*Profile.*\\} from"
    - from: "lib/lightning/audit-log.ts"
      to: "@react-native-async-storage/async-storage (NEW dependency — installed via `npx expo install` per RESEARCH Q1 RESOLVED ; vault-cache.ts uses expo-file-system, not AsyncStorage)"
      via: "AsyncStorage.getItem/setItem"
      pattern: "AsyncStorage\\.(getItem|setItem)"
    - from: "lib/lightning/payout-queue.ts"
      to: "expo-secure-store"
      via: "SecureStore.getItemAsync/setItemAsync"
      pattern: "SecureStore\\.(getItemAsync|setItemAsync)"
    - from: "lib/lightning/lnbits-client.ts"
      to: "POST /api/v1/payments LNbits"
      via: "body.extra = {taskId, completedDate, profileId}"
      pattern: "body\\.extra"
    - from: "lib/lightning/index.ts"
      to: "lib/lightning/* (tous les nouveaux modules)"
      via: "export { ... }"
      pattern: "export \\{"
---

<objective>
Construire le **module pur Lightning** — toutes les fonctions/types/persistance/tests nécessaires aux Plans 02, 03, 04. Ce plan ne touche AUCUNE UI ni hook React : il livre uniquement le moteur testable Jest. Inclut le renommage `Child*` → `Member*` (REQ-12 partiel — l'app continue de fonctionner via backward-compat read), l'extension `lnbits-client.ts` avec `extra`, et l'installation de `expo-camera` + `NSCameraUsageDescription` FR pour préparer la wave UI.

Purpose: Stabiliser les API contracts (`resolveRecipient`, `checkDailyCap`, `dispatchTrigger`, `enqueuePayout`, `loadAudit`, `migrateSingleToFamily`, bus d'événements) et leurs tests AVANT toute intégration hooks ou UI. C'est la fondation que Plan 02 (listener) et Plan 03 (UI) consommeront sans pouvoir dévier.

Output: 11 nouveaux modules `lib/lightning/*.ts` + 7 fichiers tests Jest + types étendus + lnbits-client étendu + barrel à jour + expo-camera installé + Info.plist permission FR.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/53-lightning-family-wallet/53-SPEC.md
@.planning/phases/53-lightning-family-wallet/53-CONTEXT.md
@.planning/phases/53-lightning-family-wallet/53-RESEARCH.md
@.planning/phases/53-lightning-family-wallet/53-PATTERNS.md
@CLAUDE.md
@lib/lightning/types.ts
@lib/lightning/family-credentials.ts
@lib/lightning/lnbits-client.ts
@lib/lightning/feature-flag.ts
@lib/lightning/biometric-gate.ts
@lib/lightning/index.ts
@lib/types.ts
@hooks/useVaultTasks.ts
@package.json
@app.json

<interfaces>
<!-- Contracts à respecter / produire. Aucun "scavenger hunt" — tout est ici. -->

Types existants à étendre (lib/lightning/types.ts) :
```typescript
// AVANT (à modifier) :
export interface FamilyLightningConfig {
  baseUrl: string;
  family: { name: string; invoiceKey: string; adminKey: string; };
  children: ChildWalletMapping[];  // ← rename
}

export interface ChildWalletMapping {
  profileId: string;
  displayName: string;
  invoiceKey: string;
}

// APRÈS (cible) :
export interface FamilyLightningConfig {
  baseUrl: string;
  family: { name: string; invoiceKey: string; adminKey: string; };
  members: MemberWalletMapping[];  // ← rename
  triggerMode: 'instant' | 'daily-review' | 'hybrid';  // NEW REQ-3
  dailyCapPerMember: number;  // NEW REQ-4, clamp 100-10000
}

export interface MemberWalletMapping {
  profileId: string;
  displayName: string;
  invoiceKey: string;
  adminKey?: string;  // NEW REQ-10
}
```

Types existants à RÉUTILISER tel quel :
```typescript
// lib/types.ts
export interface Task {
  id: string;
  text: string;
  completed: boolean;
  completedDate?: string;
  mentions: string[];  // extrait des @user
  targetProfileId?: string;
  // ... autres champs non utilisés Phase 53
}

export interface Profile {
  id: string;
  name: string;
  // ...
}
```

Bus d'événements (NEW lib/lightning/lightning-events.ts) :
```typescript
export type PayoutSuccessEvent = {
  profileId: string;
  profileName: string;
  sats: number;
  taskId: string;
};

export type PayoutFailedEvent = {
  profileId: string;
  taskId: string;
  reason: 'network' | 'capped' | 'attribution' | 'biometric' | 'lnbits_error';
  message?: string;
};

export function onPayoutSuccess(listener: (e: PayoutSuccessEvent) => void): () => void;
export function emitPayoutSuccess(event: PayoutSuccessEvent): void;
export function onPayoutFailed(listener: (e: PayoutFailedEvent) => void): () => void;
export function emitPayoutFailed(event: PayoutFailedEvent): void;
```

Audit log (NEW lib/lightning/audit-log.ts — AsyncStorage cf. Pitfall #1) :
```typescript
export type AuditStatus =
  | 'paid' | 'queued' | 'capped' | 'failed' | 'undone'
  | 'already_paid_today' | 'cash_out' | 'attribution_failed';

export interface AuditEntry {
  ts: string;             // ISO
  profileId: string;
  taskId: string;
  sats: number;
  status: AuditStatus;
  paymentHash?: string;
  error?: string;
}

export async function loadAudit(): Promise<AuditEntry[]>;
export async function appendAudit(entry: AuditEntry): Promise<void>;
export async function clearAudit(): Promise<void>;
export function purgeOlderThan(entries: AuditEntry[], days: number): AuditEntry[];
```

DailyCap (NEW lib/lightning/daily-cap.ts) :
```typescript
export function getCumulSatsToday(profileId: string, audit: AuditEntry[]): number;
export function checkDailyCap(
  profileId: string,
  satsAmount: number,
  audit: AuditEntry[],
  capPerMember: number,
): 'allowed' | 'capped';
```

TriggerMode (NEW lib/lightning/trigger-mode.ts) :
```typescript
export function dispatchTrigger(
  cumulSatsToday: number,
  mode: 'instant' | 'daily-review' | 'hybrid',
  satsAmount: number,
): 'instant' | 'queue';
// instant → 'instant'
// daily-review → 'queue'
// hybrid → cumul < 100 ? 'instant' : 'queue'  (SPEC REQ-3 seuil 100)
```

Queue (NEW lib/lightning/payout-queue.ts — SecureStore) :
```typescript
export interface PayoutQueueItem {
  taskId: string;
  profileId: string;
  sats: number;
  attemptCount: number;
  lastError?: string;
  queuedAt: string;       // ISO
  reason: 'offline' | 'review';  // A6 RESEARCH — 1 clé, champ reason
}

export async function loadQueue(): Promise<PayoutQueueItem[]>;
export async function enqueuePayout(item: Omit<PayoutQueueItem,'attemptCount'|'queuedAt'>, reason: 'offline'|'review'): Promise<void>;
export async function removeFromQueue(taskId: string, profileId: string): Promise<void>;
export async function incrementAttempt(taskId: string, profileId: string, lastError: string): Promise<PayoutQueueItem | null>;
// MAX_ATTEMPTS = 5 ; au-delà, item retiré + audit 'failed' (caller's responsibility).
```

Migration (NEW lib/lightning/migration.ts — REQ-11, Pitfall #9) :
```typescript
export async function migrateSingleToFamily(): Promise<{ migrated: boolean; reason: 'no_single' | 'family_exists' | 'migrated' }>;
// Logic :
// 1. loadFamilyConfig() → si présent : juste clearLnbitsConfig() (cleanup single sans toucher family) → return 'family_exists'
// 2. loadLnbitsConfig() → si null : return 'no_single'
// 3. single trouvé : créer family { baseUrl, family: { name: 'Famille', invoiceKey: single.invoiceKey, adminKey: '' }, members: [], triggerMode: 'instant', dailyCapPerMember: 1000 } + saveFamilyConfig + clearLnbitsConfig → return 'migrated'
```

ResolveRecipient (NEW lib/lightning/resolve-recipient.ts — REQ-2) :
```typescript
export function resolveRecipient(
  task: Task,
  profiles: Profile[],
  memberWallets: MemberWalletMapping[],
  activeProfileId: string | null,
): { profileId: string; profile: Profile; wallet: MemberWalletMapping } | null;
// Voir RESEARCH.md Example A pour les 6 cas SPEC REQ-2.
```

ParentNotif (NEW lib/lightning/parent-notif.ts — D-10) :
```typescript
export interface DailySummary {
  paidCount: number;
  cappedProfileNames: string[];
  failedCount: number;
  pendingCount: number;
}
export async function maybeSendParentNotif(summary: DailySummary, now?: Date): Promise<{ sent: boolean; reason?: string }>;
// Règles D-10 :
// - Silent 09h00–16h00 locale → return { sent: false, reason: 'school_hours' }
// - Cap 1/jour via SecureStore key 'lightning_last_parent_notif_v1' (date ISO YYYY-MM-DD)
// - Si déjà notifié aujourd'hui → return { sent: false, reason: 'already_sent_today' }
// - Sinon : Notifications.scheduleNotificationAsync({ content: { title: 'Lumière Lightning', body: buildBody(summary), sound: false }, trigger: null })
// - buildBody : "{N} pay-out{N>1?'s':''} validé{N>1?'s':''}" + virgule + "plafond atteint pour {Lucas, Emma}" si len>0 + virgule + "{pending} en attente" si pending>0
```

Bus subscribe pattern source (à imiter — lib/lightning/lightning-events.ts) :
```typescript
// Inspiré de hooks/useVaultTasks.ts:76-82 mais module-level (singleton)
const successListeners = new Set<(e: PayoutSuccessEvent) => void>();
const failedListeners = new Set<(e: PayoutFailedEvent) => void>();

export function onPayoutSuccess(listener: (e: PayoutSuccessEvent) => void): () => void {
  successListeners.add(listener);
  return () => { successListeners.delete(listener); };
}

export function emitPayoutSuccess(event: PayoutSuccessEvent): void {
  for (const l of Array.from(successListeners)) {
    try { l(event); } catch (e) { if (__DEV__) console.warn('[lightning] success listener error:', e); }
  }
}
// Idem pour failed.
```

LNbits client — extension `createInvoice` (lib/lightning/lnbits-client.ts:117-148) :
```typescript
async createInvoice(
  amountSats: number,
  memo: string,
  extra?: Record<string, string | number>,  // NEW
): Promise<CreateInvoiceResult> {
  if (!Number.isInteger(amountSats) || amountSats <= 0) {
    throw new LnbitsError('amountSats doit être un entier positif');
  }
  const body: { out: false; amount: number; unit: 'sat'; memo: string; extra?: Record<string, string|number> } = {
    out: false, amount: amountSats, unit: 'sat', memo,
  };
  if (extra) body.extra = extra;
  const raw = await this.request<{ payment_hash?: string; bolt11?: string; payment_request?: string; checking_id?: string }>(
    '/api/v1/payments',
    { method: 'POST', body: JSON.stringify(body) },
  );
  // CONSERVER le double fallback Pitfall #4 :
  const bolt11 = raw.bolt11 ?? raw.payment_request;
  if (!raw.payment_hash || !bolt11) {
    throw new LnbitsError('Réponse LNbits incomplète (payment_hash/bolt11 manquant)');
  }
  return { paymentHash: raw.payment_hash, bolt11, checkingId: raw.checking_id };
}
```

Family credentials backward-compat (lib/lightning/family-credentials.ts loadFamilyConfig) :
```typescript
const membersArray = Array.isArray(parsed.members)
  ? parsed.members
  : Array.isArray((parsed as any).children)
    ? (parsed as any).children
    : [];

return {
  baseUrl: parsed.baseUrl,
  family: { name: parsed.family.name, invoiceKey: parsed.family.invoiceKey, adminKey: parsed.family.adminKey },
  members: membersArray.filter(
    (m: any): m is MemberWalletMapping =>
      !!m &&
      typeof m.profileId === 'string' &&
      typeof m.displayName === 'string' &&
      typeof m.invoiceKey === 'string',
  ).map((m: any) => ({
    profileId: m.profileId,
    displayName: m.displayName,
    invoiceKey: m.invoiceKey,
    adminKey: typeof m.adminKey === 'string' ? m.adminKey : undefined,  // REQ-10
  })),
  triggerMode: (parsed as any).triggerMode === 'daily-review' || (parsed as any).triggerMode === 'hybrid'
    ? (parsed as any).triggerMode
    : 'instant',
  dailyCapPerMember: typeof (parsed as any).dailyCapPerMember === 'number'
    ? Math.max(100, Math.min(10000, (parsed as any).dailyCapPerMember))
    : 1000,
};
```

SecureStore keys (conventions) :
- `lightning_lnbits_config_v1` (legacy single — à supprimer après migration)
- `lightning_family_config_v1` (existant, étendre)
- `lightning_enabled_v1` (existant)
- `lightning_payout_queue_v1` (NEW)
- `lightning_last_parent_notif_v1` (NEW — date ISO `YYYY-MM-DD`)

AsyncStorage key (NEW, audit log — Pitfall #1 décision) :
- `@lightning_audit_v1` (préfixe `@` convention AsyncStorage projet — vérifier dans lib/vault-cache.ts:6 le pattern existant)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tâche 1 : Types étendus + family-credentials backward-compat + lnbits-client extra + barrel</name>
  <read_first>
    - .planning/phases/53-lightning-family-wallet/53-RESEARCH.md (section "Architectural Responsibility Map", "Pattern 1", "Code Examples", "Common Pitfalls #4, #9")
    - .planning/phases/53-lightning-family-wallet/53-PATTERNS.md (section "lib/lightning/types.ts", "family-credentials.ts backward-compat", "lnbits-client.ts extra")
    - .planning/phases/53-lightning-family-wallet/53-SPEC.md (REQ-3, REQ-4, REQ-6, REQ-10, REQ-11, REQ-12)
    - lib/lightning/types.ts (état actuel à étendre)
    - lib/lightning/family-credentials.ts (état actuel — backward-compat à ajouter)
    - lib/lightning/lnbits-client.ts (méthode createInvoice ligne 117-148 — paramètre `extra` à ajouter, double fallback Pitfall #4 à conserver)
    - lib/lightning/index.ts (barrel — retirer exports legacy `ChildWalletMapping`, ajouter alias rétro-compat type pendant la transition)
    - CLAUDE.md (conventions FR, `__DEV__` only, errors silencieuses)
  </read_first>
  <behavior>
    - Test types.ts : `MemberWalletMapping` exporté avec `adminKey?: string` optionnel, `FamilyLightningConfig` contient `members`, `triggerMode`, `dailyCapPerMember`
    - Test family-credentials : loadFamilyConfig accepte JSON contenant `children: [...]` ET renvoie un objet avec `members: [...]` (rétro-compat lecture)
    - Test family-credentials : saveFamilyConfig écrit `members: [...]` dans le JSON SecureStore (jamais `children`)
    - Test family-credentials : triggerMode défaut `'instant'` si absent ou invalide ; dailyCapPerMember clampé 100-10000 (default 1000 si absent ou invalide type)
    - Test lnbits-client : createInvoice avec `extra: { taskId: 'T1', completedDate: '2026-05-18' }` envoie `body.extra` dans la requête POST `/api/v1/payments` (vérifié via mock fetch)
    - Test lnbits-client : double fallback `bolt11 ?? payment_request` toujours présent (Pitfall #4)
    - Test barrel : `MemberWalletMapping` exporté depuis `lib/lightning` ; `ChildWalletMapping` SUPPRIMÉ du barrel
  </behavior>
  <action>
    1. **lib/lightning/types.ts** — Renommer interface `ChildWalletMapping` → `MemberWalletMapping`, ajouter `adminKey?: string` optionnel. Renommer dans `FamilyLightningConfig` le champ `children: ChildWalletMapping[]` → `members: MemberWalletMapping[]`. Ajouter `triggerMode: 'instant' | 'daily-review' | 'hybrid'` et `dailyCapPerMember: number`. Conserver le commentaire JSDoc complet de `FamilyLightningConfig` (préciser dans le doc que `triggerMode` et `dailyCapPerMember` sont REQ-3/REQ-4). Ne PAS exporter d'alias rétro-compat `ChildWalletMapping` — la rétro-compat se fait au niveau du PARSER (family-credentials), pas du type (REQ-12 cleanup).

    2. **lib/lightning/family-credentials.ts** — Mettre à jour TOUS les usages `children` → `members` (lignes 15, 29, 40, 62). Étendre `loadFamilyConfig` pour le pattern backward-compat verbatim de `<interfaces>` ci-dessus : (a) lire `parsed.members` SINON `parsed.children` SINON `[]`, (b) filter sur shape valide, (c) mapper en `MemberWalletMapping` avec `adminKey: typeof m.adminKey === 'string' ? m.adminKey : undefined`, (d) parser `triggerMode` avec default `'instant'`, (e) parser `dailyCapPerMember` avec clamp 100-10000 et default 1000. Étendre `saveFamilyConfig` pour normaliser : toujours écrire `members: [...]`, `triggerMode`, `dailyCapPerMember` ; ne jamais écrire `children`. Conserver le log `__DEV__` only `'[lightning] loadFamilyConfig failed:'`.

    3. **lib/lightning/lnbits-client.ts** — Étendre la signature de `createInvoice` avec un 3ᵉ paramètre optionnel `extra?: Record<string, string | number>`. Si `extra` fourni, l'injecter dans `body.extra` AVANT le `JSON.stringify`. CONSERVER **inchangé** le double fallback ligne 139 `const bolt11 = raw.bolt11 ?? raw.payment_request` (Pitfall #4 — ne PAS simplifier). Conserver le check `payment_hash + bolt11` qui throw `LnbitsError`. JSDoc en FR : "Idempotency tag REQ-6 (taskId+completedDate)".

    4. **lib/lightning/index.ts** — Barrel : RETIRER l'export `ChildWalletMapping` ; AJOUTER l'export `MemberWalletMapping`. Conserver tous les autres exports existants (`LnbitsClient`, `FamilyLightningConfig`, `LnbitsError`, etc.). Les nouveaux exports (resolveRecipient, audit-log, etc.) seront ajoutés dans les tâches suivantes. Ce sous-pas ne RETIRE PAS encore `loadLnbitsConfig`/`saveLnbitsConfig`/`clearLnbitsConfig` (REQ-12 cleanup final → Plan 04). Ces 3 fonctions restent exposées le temps que la migration tourne en Plan 02.

    5. **lib/lightning/__tests__/__mocks__/secure-store.ts** — Créer un mock SecureStore en mémoire (Map<string,string>) qui implémente `getItemAsync(key)`, `setItemAsync(key,value)`, `deleteItemAsync(key)`. Exporter aussi `__resetMock()` et `__seedMock(key,value)` pour les tests. Aucune dépendance externe. Référence : pattern A8/A9 RESEARCH.md.

    6. **package.json (2 nouvelles dépendances)** :
       - (a) **AsyncStorage** : `npx expo install @react-native-async-storage/async-storage` — dépendance NEUVE (vérifié : `package.json` ne contient PAS `@react-native-async-storage/async-storage` aujourd'hui ; `lib/vault-cache.ts` utilise `expo-file-system`, pas AsyncStorage). Cette dépendance est requise par `lib/lightning/audit-log.ts` (RESEARCH Q1 RESOLVED — audit ~160 KB > limite SecureStore 2 KB).
       - (b) **expo-camera** : `npx expo install expo-camera` pour ajouter la dépendance (laisser npm/expo choisir la version SDK 54). Mettre à jour `app.json` : (i) ajouter `expo-camera` dans `plugins` avec `cameraPermission: "Permet à FamilyFlow de scanner les QR codes des invoices Lightning."` (FR strict, A8 RESEARCH) ; (ii) si `infoPlist` section existe pour iOS dans `app.json`, ajouter `NSCameraUsageDescription: "Permet à FamilyFlow de scanner les QR codes des invoices Lightning."`. Ne PAS rebuilder le dev-client dans ce plan — laisser cette étape au développeur après ce plan (note dans le SUMMARY). REQ-12 — ces installs préparent uniquement Plan 03b, pas de UI ici.
       - **Vérification post-install** : `grep -q '"@react-native-async-storage/async-storage"' package.json && echo OK-async` ET `grep -q '"expo-camera"' package.json && echo OK-camera` doivent tous deux passer.

    7. **Type check final** : `npx tsc --noEmit` doit être clean (hors erreurs pré-existantes MemoryEditor.tsx, cooklang.ts, useVault.ts cf. CLAUDE.md Testing). Si une nouvelle erreur apparaît à cause du rename `Child*` → `Member*`, corriger immédiatement (le code Lightning existant — `lnbits-client.ts`, `family-credentials.ts`, barrel — doit compiler ; le playground `app/lightning-family-spike.tsx` doit également compiler car il référence `ChildWalletMapping` — utiliser un type alias **local** dans le playground ou updater ses imports vers `MemberWalletMapping` ; suppression effective du playground reste Plan 04).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "lib/lightning|app/lightning-(family-)?spike" | grep -v "MemoryEditor\|cooklang\|useVault.ts" ; test ${PIPESTATUS[0]} -eq 0 || echo "TSC failed"</automated>
    <automated>grep -c "MemberWalletMapping" lib/lightning/types.ts # >= 2</automated>
    <automated>grep -c "triggerMode" lib/lightning/types.ts # >= 1</automated>
    <automated>grep -c "dailyCapPerMember" lib/lightning/types.ts # >= 1</automated>
    <automated>grep -c "adminKey?" lib/lightning/types.ts # >= 1</automated>
    <automated>grep -c "Array.isArray(parsed.members)" lib/lightning/family-credentials.ts # >= 1 (backward-compat read)</automated>
    <automated>grep -c "body.extra" lib/lightning/lnbits-client.ts # >= 1</automated>
    <automated>grep -c "raw.bolt11 ?? raw.payment_request" lib/lightning/lnbits-client.ts # >= 1 (Pitfall #4 conservé)</automated>
    <automated>grep -c "ChildWalletMapping" lib/lightning/index.ts # == 0</automated>
    <automated>grep -c "MemberWalletMapping" lib/lightning/index.ts # >= 1</automated>
    <automated>grep -c "@react-native-async-storage/async-storage" package.json # >= 1 (BLOCKER #2 — nouvelle dépendance)</automated>
    <automated>grep -c "expo-camera" package.json # >= 1</automated>
    <automated>grep -c "NSCameraUsageDescription\|cameraPermission" app.json # >= 1</automated>
    <automated>test -f lib/lightning/__tests__/__mocks__/secure-store.ts</automated>
  </verify>
  <done>
    Types étendus, rétro-compat lecture en place, lnbits-client supporte `extra`, barrel à jour, `@react-native-async-storage/async-storage` ET `expo-camera` installés (2 nouvelles dépendances visibles dans `package.json`), mock SecureStore prêt. TSC clean sur le module Lightning (hors fichiers pré-existants).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Tâche 2 : Modules purs (resolveRecipient, audit-log, daily-cap, trigger-mode, payout-queue, parent-notif, migration, lightning-events) + 7 suites Jest</name>
  <read_first>
    - .planning/phases/53-lightning-family-wallet/53-RESEARCH.md (sections "Code Examples", "Common Pitfalls #1, #2, #3, #9, #10", "Validation Architecture", "Architecture Patterns Pattern 2, Pattern 3")
    - .planning/phases/53-lightning-family-wallet/53-PATTERNS.md (sections "lib/lightning/audit-log.ts", "lib/lightning/lightning-events.ts", "__tests__/resolve-recipient.test.ts")
    - .planning/phases/53-lightning-family-wallet/53-SPEC.md (REQ-2 6 cas, REQ-4 cap, REQ-5 queue, REQ-6 idempotence, REQ-7 audit 90j, REQ-11 migration)
    - .planning/phases/53-lightning-family-wallet/53-CONTEXT.md (D-10 parent notif règles 9-16h)
    - lib/lightning/family-credentials.ts (pattern load/save/clear à imiter)
    - lib/lightning/feature-flag.ts (pattern cache + SecureStore)
    - hooks/useVaultTasks.ts:76-124 (pattern Set<Listener> à imiter pour lightning-events)
    - lib/types.ts (Task, Profile — pour resolveRecipient)
    - lib/scheduled-notifications.ts (pattern expo-notifications à imiter pour parent-notif)
    - lib/vault-cache.ts (pattern AsyncStorage déjà utilisé — vérifier le préfixe de clé utilisé : `@vault_cache_v1` ou similaire, et appliquer convention identique pour `@lightning_audit_v1`)
    - CLAUDE.md (errors silencieuses, FR strict, __DEV__ only)
  </read_first>
  <behavior>
    - **resolve-recipient.test.ts (REQ-2, 6 cas)** :
      1. `task.mentions = ['lucas']` + profile Lucas configuré → retourne `{ profileId: 'lucas-id', profile, wallet }`
      2. `task.mentions = ['inconnu']` (profil non configuré) → retourne `null`
      3. `task.mentions = []` + activeProfileId='lucas-id' configuré → retourne Lucas
      4. `task.mentions = []` + activeProfileId='lucas-id' NON configuré → retourne `null`
      5. `task.mentions = ['lucas','emma']` (2 mentions) → retourne `null` (ambiguïté)
      6. `task.mentions = ['gabriel']` (adulte avec wallet) → retourne Gabriel
    - **audit-log.test.ts (REQ-7)** :
      - append → load round-trip preserve les entrées
      - purgeOlderThan(90) avec fake timers : 3 entrées (today, 80j ago, 100j ago) → load ne retourne que 2
      - clearAudit → loadAudit retourne `[]`
      - load avec JSON corrompu → retourne `[]` silencieusement + log __DEV__
    - **daily-cap.test.ts (REQ-4)** :
      - getCumulSatsToday filtre par profileId + date locale today + status='paid'
      - checkDailyCap retourne 'allowed' si cumul + amount <= cap
      - checkDailyCap retourne 'capped' si cumul + amount > cap
      - 11 entrées paid 100 sats today + cap 1000 → 11ᵉ pay-out 'capped'
    - **trigger-mode.test.ts (REQ-3)** :
      - mode 'instant' → toujours 'instant'
      - mode 'daily-review' → toujours 'queue'
      - mode 'hybrid' + cumul=0 + amount=100 → 'instant' (cumul < 100 STRICT — cf. SPEC #3 acceptance "hybrid jusqu'à 100 instant puis bascule en queue")
      - mode 'hybrid' + cumul=100 + amount=100 → 'queue'
      - mode 'hybrid' + cumul=99 + amount=100 → 'instant' (seuil strict <)
    - **payout-queue.test.ts (REQ-5)** :
      - enqueue → load retrouve l'item avec attemptCount=0
      - incrementAttempt → load retourne attemptCount=1 + lastError
      - 5 incrementAttempt successifs → item toujours présent (le caller décide du removal au cap 5)
      - removeFromQueue → load ne retourne plus l'item
      - 2 items avec reason 'offline' et 'review' → load retourne les 2
    - **idempotence.test.ts (REQ-6)** :
      - check `findPaidEntry(audit, taskId, dateISO)` retourne true si une entrée `status:'paid', taskId, ts.slice(0,10)===dateISO` existe
      - 1 paid + 1 undone même taskId+date → findPaidEntry retourne true (le undone n'efface pas le paid)
    - **migration.test.ts (REQ-11, Pitfall #9)** :
      - Cas A : seed single + clear family → migration retourne `'migrated'`, family contient baseUrl+invoiceKey hérités, adminKey vide, members=[], single supprimé
      - Cas B : seed family + clear single → migration retourne `'family_exists'`, family inchangée, single supprimé (cleanup)
      - Cas C : single+family présents → migration retourne `'family_exists'`, family inchangée, single supprimé
      - Cas D : ni single ni family → migration retourne `'no_single'`, aucune écriture
    - **lightning-events** (sans test dédié — testé indirectement Plan 02) :
      - `onPayoutSuccess` + `emitPayoutSuccess({...})` → listener appelé synchroneusement
      - `unsub()` retiré du Set → listener PLUS appelé
  </behavior>
  <action>
    1. **lib/lightning/lightning-events.ts** — Créer le module-level event bus (singleton Set<Listener>). Implémenter exactement les 4 fonctions listées dans `<interfaces>` : `onPayoutSuccess`, `emitPayoutSuccess`, `onPayoutFailed`, `emitPayoutFailed`. Pattern Set + try/catch __DEV__ only verbatim de hooks/useVaultTasks.ts:111-124. Cf. RESEARCH.md Pattern 3.

    2. **lib/lightning/resolve-recipient.ts** — Implémenter la fonction pure selon RESEARCH.md Example A verbatim. Imports : `import type { Task, Profile } from '../types';` + `import type { MemberWalletMapping } from './types';`. Logique :
       - Cas 1 : `task.mentions.length === 1` → find par `p.name.toLowerCase() === mentionName.toLowerCase()` OU `p.id.toLowerCase() === mentionName.toLowerCase()` → check wallet existence → return or null
       - Cas 2 : `task.mentions.length > 1` → return null
       - Cas 3 : `!activeProfileId` → return null ; sinon find activeProfile + check wallet → return or null

    3. **lib/lightning/audit-log.ts** — Implémenter avec `@react-native-async-storage/async-storage` (déjà installé via cache vault). Clé `@lightning_audit_v1`. Fonctions : `loadAudit`, `appendAudit`, `clearAudit`, `purgeOlderThan(entries, days)`. `loadAudit` appelle automatiquement `purgeOlderThan(parsed, RETENTION_DAYS=90)`. `appendAudit` lit, push, write. Erreurs silencieuses + log `__DEV__` only. Validation défensive shape : filter sur `e && typeof e.ts==='string' && typeof e.profileId==='string' && typeof e.taskId==='string' && typeof e.sats==='number' && typeof e.status==='string'`. Helpers exportés : `findPaidEntry(audit, taskId, dateISO)` retourne boolean (REQ-6).

    4. **lib/lightning/daily-cap.ts** — Implémenter `getCumulSatsToday(profileId, audit)` qui filtre `e.profileId===profileId && e.ts.slice(0,10)===todayLocalISO() && e.status==='paid'` et sum `e.sats`. Helper local `todayLocalISO()` qui retourne `new Date()` formaté `YYYY-MM-DD` en TZ LOCALE (pas UTC — cf. STATE.md Phase 38 `getLocalDateKey`). Implémenter `checkDailyCap(profileId, satsAmount, audit, capPerMember)` : `(getCumulSatsToday(profileId, audit) + satsAmount) <= capPerMember ? 'allowed' : 'capped'`.

    5. **lib/lightning/trigger-mode.ts** — Implémenter `dispatchTrigger(cumulSatsToday, mode, satsAmount)` :
       - `mode === 'instant'` → return `'instant'`
       - `mode === 'daily-review'` → return `'queue'`
       - `mode === 'hybrid'` → return `cumulSatsToday < 100 ? 'instant' : 'queue'` (SPEC REQ-3 — seuil STRICT 100, exclusif)

    6. **lib/lightning/payout-queue.ts** — Implémenter avec SecureStore (queue petite). Clé `lightning_payout_queue_v1`. Constante `MAX_ATTEMPTS = 5`. Fonctions exactes listées dans `<interfaces>`. `enqueuePayout` génère `attemptCount: 0` et `queuedAt: new Date().toISOString()`. Validation défensive au load (shape). Si JSON corrompu → return `[]`.

    7. **lib/lightning/parent-notif.ts** — Implémenter `maybeSendParentNotif(summary, now=new Date())`. Logique :
       - Lire `lightning_last_parent_notif_v1` (SecureStore string `YYYY-MM-DD`)
       - todayISO = `now` formaté YYYY-MM-DD locale
       - Heure locale = `now.getHours()` → si `9 <= heure < 16` → return `{ sent: false, reason: 'school_hours' }`
       - Si lastNotifDate === todayISO → return `{ sent: false, reason: 'already_sent_today' }`
       - Sinon : `await Notifications.scheduleNotificationAsync({ content: { title: 'Lumière Lightning', body: buildBody(summary), sound: false, sticky: false }, trigger: null })`
       - SecureStore.setItemAsync('lightning_last_parent_notif_v1', todayISO)
       - return `{ sent: true }`
       - `buildBody(s)` construit la string FR cible : `"{paidCount} pay-out{paidCount>1?'s':''} validé{paidCount>1?'s':''}"` + virgule + `"plafond atteint pour {cappedProfileNames.join(', ')}"` si len>0 + virgule + `"{pendingCount} en attente"` si pending>0 (joindre les segments avec `' · '` pour cohérence UI-SPEC D-10).
       - Errors silencieuses + log `__DEV__` only.

    8. **lib/lightning/migration.ts** — Implémenter `migrateSingleToFamily()` selon `<interfaces>` ci-dessus. Imports : `loadLnbitsConfig, clearLnbitsConfig` depuis `./credentials` (legacy — sera supprimé Plan 04), `loadFamilyConfig, saveFamilyConfig` depuis `./family-credentials`. Logique exacte des 3 cas (Pitfall #9 — check family FIRST). Erreurs silencieuses + log `__DEV__`.

    9. **lib/lightning/index.ts** — Étendre le barrel avec les nouveaux exports : `resolveRecipient`, `loadAudit, appendAudit, clearAudit, purgeOlderThan, findPaidEntry, AuditEntry, AuditStatus`, `checkDailyCap, getCumulSatsToday`, `dispatchTrigger`, `enqueuePayout, loadQueue, removeFromQueue, incrementAttempt, PayoutQueueItem`, `maybeSendParentNotif, DailySummary`, `migrateSingleToFamily`, `onPayoutSuccess, emitPayoutSuccess, onPayoutFailed, emitPayoutFailed, PayoutSuccessEvent, PayoutFailedEvent`. Conserver les exports existants.

    10. **7 fichiers tests Jest** dans `lib/lightning/__tests__/` :
       - `resolve-recipient.test.ts` (6 cas REQ-2)
       - `audit-log.test.ts` (purge 90j via `jest.useFakeTimers()` + `jest.setSystemTime(new Date('2026-08-16'))` pour simuler +91j)
       - `daily-cap.test.ts` (cumul + cap dépassé)
       - `trigger-mode.test.ts` (3 modes + seuil 100 strict)
       - `payout-queue.test.ts` (enqueue/incrementAttempt/remove)
       - `idempotence.test.ts` (findPaidEntry true même avec undone)
       - `migration.test.ts` (4 cas A/B/C/D Pitfall #9)
       Chaque fichier mock `expo-secure-store` via `jest.mock('expo-secure-store', () => require('./__mocks__/secure-store'))` (et idem pour `@react-native-async-storage/async-storage` si audit-log.test.ts). Reset mock dans `beforeEach`.

    11. **Vérification finale** : `npx jest --no-coverage lib/lightning/__tests__/` exits 0. `npx tsc --noEmit` clean sur `lib/lightning/`.
  </action>
  <verify>
    <automated>test -f lib/lightning/resolve-recipient.ts && test -f lib/lightning/audit-log.ts && test -f lib/lightning/daily-cap.ts && test -f lib/lightning/trigger-mode.ts && test -f lib/lightning/payout-queue.ts && test -f lib/lightning/parent-notif.ts && test -f lib/lightning/migration.ts && test -f lib/lightning/lightning-events.ts</automated>
    <automated>test -f lib/lightning/__tests__/resolve-recipient.test.ts && test -f lib/lightning/__tests__/audit-log.test.ts && test -f lib/lightning/__tests__/daily-cap.test.ts && test -f lib/lightning/__tests__/trigger-mode.test.ts && test -f lib/lightning/__tests__/payout-queue.test.ts && test -f lib/lightning/__tests__/idempotence.test.ts && test -f lib/lightning/__tests__/migration.test.ts</automated>
    <automated>grep -c "export function resolveRecipient" lib/lightning/resolve-recipient.ts # >= 1</automated>
    <automated>grep -c "export async function loadAudit" lib/lightning/audit-log.ts # >= 1</automated>
    <automated>grep -c "RETENTION_DAYS" lib/lightning/audit-log.ts # >= 1</automated>
    <automated>grep -c "export function checkDailyCap" lib/lightning/daily-cap.ts # >= 1</automated>
    <automated>grep -c "export function dispatchTrigger" lib/lightning/trigger-mode.ts # >= 1</automated>
    <automated>grep -c "MAX_ATTEMPTS" lib/lightning/payout-queue.ts # >= 1</automated>
    <automated>grep -c "school_hours" lib/lightning/parent-notif.ts # >= 1</automated>
    <automated>grep -c "family_exists\|no_single\|migrated" lib/lightning/migration.ts # >= 3</automated>
    <automated>grep -c "emitPayoutSuccess\|onPayoutSuccess" lib/lightning/lightning-events.ts # >= 2</automated>
    <automated>grep -c "resolveRecipient\|checkDailyCap\|dispatchTrigger\|enqueuePayout\|migrateSingleToFamily" lib/lightning/index.ts # >= 5</automated>
    <automated>npx jest --no-coverage lib/lightning/__tests__/ 2>&1 | tail -5</automated>
    <automated>npx tsc --noEmit 2>&1 | grep -E "lib/lightning/" | grep -v "MemoryEditor\|cooklang\|useVault.ts" | wc -l # == 0</automated>
  </verify>
  <done>
    11 nouveaux modules `lib/lightning/*.ts` créés (8 modules + barrel update + 7 tests + 1 mock). Tous les tests Jest passent (`npx jest lib/lightning/__tests__/` exits 0). TSC clean sur le module Lightning. Le moteur est prêt à être consommé par Plan 02 (listener) et Plan 03 (UI).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Code Lightning → SecureStore | Lecture/écriture creds, queue, flag, parent-notif timestamp. SecureStore = iOS Keychain `kSecClassGenericPassword`, chiffrement OS-level. |
| Code Lightning → AsyncStorage (audit log) | Lecture/écriture audit log. AsyncStorage = stockage non chiffré ; audit n'est pas un secret (paymentHash retrouvable sur blockchain LN). |
| Code Lightning → LNbits REST API | Requêtes HTTPS sortantes. Le module ne reçoit AUCUNE entrée externe non sollicitée (pas de webhook). `extra` field utilisé pour idempotency tag (envoyé par nous, pas reçu). |
| Tests Jest → SecureStore/AsyncStorage | Tests utilisent mocks en mémoire (`__mocks__/secure-store.ts`). Aucun appel système réel. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-01-01 | Spoofing | resolveRecipient (mentions match) | mitigate | Comparaison strict `toLowerCase()` côté name ET id ; absence de wallet → return null. Test : 6 cas SPEC dont mention adulte (REQ-2). Pas de fuzzy match (un typo mention → no payout, pas mauvais destinataire). |
| T-53-01-02 | Tampering | audit-log (AsyncStorage non chiffré) | accept | L'audit log est consultable publiquement sur la blockchain LN via paymentHash. Le risque "user altère son propre audit" est documenté dans le README utilisateur comme "audit log informationnel". Pas d'investissement en signature/HMAC pour Phase 53. |
| T-53-01-03 | Repudiation | payout-queue / audit-log | mitigate | Chaque entrée audit contient `{ts, profileId, taskId, sats, paymentHash?, status}`. `paymentHash` retourné par LNbits prouve la transaction côté serveur LN. Re-cocher tâche → status `already_paid_today` (REQ-6 idempotence). Test : `idempotence.test.ts` couvre la non-répudiation par paymentHash. |
| T-53-01-04 | Information Disclosure | family-credentials (adminKey en lecture) | mitigate | `MemberWalletMapping.adminKey` optionnel — n'apparaît PAS dans les logs (vérifier `__DEV__` only ne logge JAMAIS le contenu de `parsed.family.adminKey` ni `m.adminKey`). Test : grep `console.log.*adminKey` doit retourner 0. SecureStore = chiffrement OS Keychain. |
| T-53-01-05 | Denial of Service | dailyCap atomic check | mitigate | `checkDailyCap` calcule cumul depuis audit avant tout call réseau. Atomic in-process (Plan 02 ajoute un lock). Test `daily-cap.test.ts` : 11ᵉ pay-out 'capped' avec cap 1000 + 10×100 paid. |
| T-53-01-06 | Elevation of Privilege | migration.ts (Pitfall #9) | mitigate | check `loadFamilyConfig() !== null` AVANT migration → si family existe, JAMAIS d'écriture (juste cleanup single). Test : cas C (single+family) → family inchangée. Empêche un attaquant qui réinjecterait la legacy single de retirer les members de la family existante. |
| T-53-01-07 | Tampering | LNbits idempotency via `extra` | mitigate | `createInvoice(amount, memo, extra={ taskId, completedDate, profileId })` permet à un audit Lightning serveur de croiser un éventuel double pay-out. Côté client, l'audit log local fait le check final (`findPaidEntry`). Combinaison ceinture+bretelles. |
| T-53-01-08 | Information Disclosure | parent-notif body | mitigate | Le body de notif contient `paidCount`, `cappedProfileNames`, `pendingCount` — pas de sats individuels ni invoice. Conforme UI-SPEC D-10 wording. Visible sur lock screen → acceptable (info de récap familial, pas de secret). |
| T-53-01-09 | Tampering | package.json / app.json modifs | accept | Modifs ciblées (expo-camera + NSCameraUsageDescription) revues par le développeur via diff git. Pas de pre-commit hook à ce stade. |

**Block-on severity:** high. Aucune disposition `accept` n'est jugée high (T-53-01-02 audit tampering : ACCEPTABLE car audit = info publique post-paiement ; T-53-01-09 build files : ACCEPTABLE car diff git revu humainement).
</threat_model>

<verification>
- `npx tsc --noEmit` clean sur `lib/lightning/` (hors fichiers pré-existants MemoryEditor/cooklang/useVault.ts)
- `npx jest --no-coverage lib/lightning/__tests__/` exits 0 — 7 suites + tests passent
- Aucun `console.log`/`console.warn`/`console.error` non encadré par `if (__DEV__)` dans les nouveaux fichiers : `grep -nE "console\\.(log|warn|error)" lib/lightning/{resolve-recipient,audit-log,daily-cap,trigger-mode,payout-queue,parent-notif,migration,lightning-events}.ts | grep -v "__DEV__"` retourne vide
- Aucune référence à `adminKey` dans les logs `__DEV__` (vérifier que le contenu jamais loggé) : `grep -nE "console\\.(log|warn).*adminKey" lib/lightning/` retourne vide
- Le barrel `lib/lightning/index.ts` ne contient plus `ChildWalletMapping` mais contient `MemberWalletMapping` et les nouveaux exports
- `@react-native-async-storage/async-storage` listé dans `package.json` (dépendance NEUVE — BLOCKER #2 résolu)
- `expo-camera` listé dans `package.json` ; `NSCameraUsageDescription` ou `cameraPermission` présent dans `app.json` avec wording FR
</verification>

<success_criteria>
- Tous les types étendus compilent (`MemberWalletMapping`, `FamilyLightningConfig.{triggerMode, dailyCapPerMember, members}`)
- `loadFamilyConfig` lit `children` ET `members` (backward-compat), `saveFamilyConfig` écrit toujours `members`
- `createInvoice(amount, memo, extra?)` envoie `body.extra` quand fourni, double fallback `bolt11 ?? payment_request` conservé
- 8 nouveaux modules `lib/lightning/*.ts` exportent les fonctions/types listés dans `<interfaces>`
- 7 suites Jest passent (`resolve-recipient`, `audit-log`, `daily-cap`, `trigger-mode`, `payout-queue`, `idempotence`, `migration`)
- `@react-native-async-storage/async-storage` installé (BLOCKER #2 résolu — vérifié : pas dans package.json avant Phase 53)
- `expo-camera` installé + `NSCameraUsageDescription` FR dans `app.json`
- `npx tsc --noEmit` clean sur la phase
- Audit log utilise AsyncStorage (décision Pitfall #1) ; queue + flag + parent-notif timestamp utilisent SecureStore
- Migration `single → family` idempotente (3 cas testés)
- Bus d'événements `lightning-events.ts` prêt à être consommé par Plan 02 (emit) et Plan 03 (subscribe)
- Aucune référence à `adminKey` dans les logs même `__DEV__`
</success_criteria>

<output>
After completion, create `.planning/phases/53-lightning-family-wallet/53-01-SUMMARY.md` listing :
- Tous les nouveaux fichiers `lib/lightning/*.ts` créés + leurs API exportées
- Les 7 suites Jest et leur statut (PASS)
- La décision audit-log AsyncStorage vs SecureStore (justifier Pitfall #1)
- Note pour le développeur : `npx expo run:ios --device` requis avant Plan 03b pour intégrer `expo-camera` ET `@react-native-async-storage/async-storage` natively (2 nouvelles dépendances installées dans ce plan)
- Tout ajustement par rapport au plan (interfaces affinées, dépendances découvertes)
</output>
