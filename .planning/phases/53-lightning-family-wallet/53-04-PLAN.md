---
phase: 53-lightning-family-wallet
plan: 04
type: execute
wave: 5
depends_on: [53-03b]
files_modified:
  - lib/lightning/credentials.ts
  - app/lightning-spike.tsx
  - app/lightning-family-spike.tsx
  - lib/lightning/index.ts
  - lib/lightning/migration.ts
  - hooks/useVaultTasks.ts
  - hooks/useVault.ts
autonomous: false
requirements: [REQ-12, REQ-6]
requirements_addressed: [REQ-12, REQ-6]
tags: [lightning, cleanup, non-regression, app-store-posture, undone-audit]

user_setup: []

must_haves:
  truths:
    - "Le fichier `app/lightning-spike.tsx` est SUPPRIMÉ (449 lignes playground single-wallet) — REQ-12"
    - "Le fichier `app/lightning-family-spike.tsx` est SUPPRIMÉ (751 lignes playground family) — REQ-12"
    - "Le fichier `lib/lightning/credentials.ts` est SUPPRIMÉ (50 lignes single-wallet legacy) — REQ-12 + SPEC requirement 11 acceptance"
    - "Le barrel `lib/lightning/index.ts` ne référence plus `loadLnbitsConfig`, `saveLnbitsConfig`, `clearLnbitsConfig`, ni `LnbitsConfig` (le type est interne à lnbits-client.ts désormais — `LnbitsClient` continue d'accepter sa propre config interne)"
    - "La migration single→family (Plan 02) doit avoir tourné AU MOINS UNE FOIS avant ce cleanup — un Membre dev qui aurait un single legacy doit voir sa data migrée et la clé `lightning_lnbits_config_v1` effacée avant qu'on supprime `credentials.ts`"
    - "`grep -rE 'ChildWalletMapping|loadLnbitsConfig|saveLnbitsConfig|clearLnbitsConfig|lightning-spike|lightning-family-spike' lib components app --include='*.ts' --include='*.tsx'` retourne 0 résultat (REQ-12 acceptance verbatim)"
    - "`find app lib -name '*lightning-spike*'` retourne 0 résultat (REQ-12 acceptance verbatim)"
    - "`npx tsc --noEmit` est clean — aucune référence cassée par les suppressions"
    - "`npx jest --no-coverage` est clean (toutes suites Lightning + projet vert) — non-régression complète"
    - "La ferme, l'Auberge, le widget, la gamification fonctionnent comme avant Phase 53 — checkpoint device humain confirme"
    - "La branche `feat/lightning-farm` reste isolée — aucun commit vers `main` sans décision explicite (spike 003 App Store posture)"
    - "Aucun nom personnel réel dans les commits/docs/sources Phase 53 — générique Lucas/Emma/Dupont uniquement"
    - "REQ-6 'undone' audit entry (deferred depuis Plan 02 — WARNING #7) : `subscribeTaskUncomplete(listener)` exposé par `hooks/useVaultTasks.ts` (mirror de `subscribeTaskComplete`) ; 2ᵉ `useEffect` dans `hooks/useVault.ts` souscrit à `subscribeTaskUncomplete` et appelle `appendAudit({status:'undone', taskId, profileId, ts})` SI une entrée `paid` existe pour ce taskId+date dans l'audit"
    - "Le SPEC #6 acceptance \"compléter la même tâche après dé-coche → audit log montre `undone` puis `already_paid_today`\" est désormais entièrement couvert (Plan 02 = `already_paid_today` via findPaidEntry, Plan 04 = `undone` via subscribeTaskUncomplete)"
  artifacts:
    - path: "lib/lightning/credentials.ts (SUPPRIMÉ)"
      provides: "(plus rien — fichier supprimé)"
      contains: "n/a"
    - path: "app/lightning-spike.tsx (SUPPRIMÉ)"
      provides: "(plus rien — playground retiré)"
      contains: "n/a"
    - path: "app/lightning-family-spike.tsx (SUPPRIMÉ)"
      provides: "(plus rien — playground retiré)"
      contains: "n/a"
    - path: "lib/lightning/index.ts"
      provides: "Barrel final — sans exports legacy single-wallet"
      contains: "MemberWalletMapping"
    - path: "hooks/useVaultTasks.ts"
      provides: "Nouveau `subscribeTaskUncomplete(listener)` — mirror de `subscribeTaskComplete`, fire sur transition `true→false`. Pattern Set<Listener> identique."
      contains: "subscribeTaskUncomplete"
    - path: "hooks/useVault.ts"
      provides: "2ᵉ useEffect Lightning souscrit à `subscribeTaskUncomplete`, appelle `appendAudit({status:'undone', ...})` SI `findPaidEntry(audit, taskId, dateISO) === true`"
      contains: "subscribeTaskUncomplete"
  key_links:
    - from: "Aucun fichier production"
      to: "credentials.ts | lightning-spike | lightning-family-spike"
      via: "grep verification 0 résultat"
      pattern: "loadLnbitsConfig|lightning-spike"
---

<objective>
**Cleanup atomique final + REQ-6 'undone' audit entry** — (1) supprimer les 3 fichiers legacy (`lib/lightning/credentials.ts`, `app/lightning-spike.tsx`, `app/lightning-family-spike.tsx`), (2) mettre à jour le barrel `lib/lightning/index.ts` pour retirer les exports legacy, (3) vérifier par grep/find qu'aucune référence ne subsiste, (4) **ajouter `subscribeTaskUncomplete` dans `hooks/useVaultTasks.ts` + 2ᵉ useEffect Lightning dans `hooks/useVault.ts` pour enregistrer l'audit 'undone' (REQ-6 deferred depuis Plan 02, WARNING #7)**, (5) valider la non-régression complète (TSC + Jest + checkpoint device sur la ferme/Auberge/widget). Confirmer également la posture branche : `feat/lightning-farm` reste isolée, pas de merge `main`.

Purpose: Sortir de la phase avec une codebase production-grade — pas de "double rail" entre la legacy single-wallet et la family multi-wallet, pas de playgrounds spike qui pourraient bug-attire un dev futur. Le tout APRÈS que tout le reste fonctionne (Pitfall #10 — ordre cleanup critique : on retire d'abord les CONSOMMATEURS des fichiers à supprimer, puis on supprime les fichiers eux-mêmes).

Output: 3 fichiers supprimés + 1 fichier mis à jour (`lib/lightning/index.ts`) + vérifications grep/find/TSC/Jest + checkpoint développeur sur la non-régression ferme.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/53-lightning-family-wallet/53-SPEC.md
@.planning/phases/53-lightning-family-wallet/53-CONTEXT.md
@.planning/phases/53-lightning-family-wallet/53-RESEARCH.md
@.planning/phases/53-lightning-family-wallet/53-PATTERNS.md
@.planning/phases/53-lightning-family-wallet/53-03-SUMMARY.md
@CLAUDE.md
@lib/lightning/index.ts
@lib/lightning/credentials.ts
@lib/lightning/migration.ts
@app/lightning-spike.tsx
@app/lightning-family-spike.tsx
@components/settings/SettingsLightning.tsx

<interfaces>
<!-- État cible barrel après cleanup -->

```typescript
// lib/lightning/index.ts (FINAL Phase 53)
// Family configuration (creds + triggerMode + dailyCap)
export {
  loadFamilyConfig, saveFamilyConfig, clearFamilyConfig,
} from './family-credentials';
export type {
  FamilyLightningConfig,
  MemberWalletMapping,
  // (PAS d'export ChildWalletMapping — supprimé)
} from './types';

// LNbits REST client + types
export { LnbitsClient, LnbitsError } from './lnbits-client';
export type { CreateInvoiceResult, PaymentStatus, WalletInfo, PaymentStatusValue } from './types';
// Note : LnbitsConfig type interne au constructor de LnbitsClient — exposé pour ceux qui instancient le client.
export type { LnbitsConfig } from './types';

// Feature flag
export { isLightningEnabled, setLightningEnabled } from './feature-flag';

// Biometric gate
// (réexporté depuis biometric-gate.ts si pattern existant — sinon import direct chez les consommateurs)

// Pure functions / Persistence Plan 01
export { resolveRecipient } from './resolve-recipient';
export {
  loadAudit, appendAudit, clearAudit, purgeOlderThan, findPaidEntry,
} from './audit-log';
export type { AuditEntry, AuditStatus } from './audit-log';
export { checkDailyCap, getCumulSatsToday } from './daily-cap';
export { dispatchTrigger } from './trigger-mode';
export {
  enqueuePayout, loadQueue, removeFromQueue, incrementAttempt,
} from './payout-queue';
export type { PayoutQueueItem } from './payout-queue';
export { maybeSendParentNotif } from './parent-notif';
export type { DailySummary } from './parent-notif';
export { migrateSingleToFamily } from './migration';

// Bus d'événements
export {
  onPayoutSuccess, emitPayoutSuccess,
  onPayoutFailed, emitPayoutFailed,
} from './lightning-events';
export type { PayoutSuccessEvent, PayoutFailedEvent } from './lightning-events';

// Orchestrateurs Plan 02
export { processTaskCompletionForLightning } from './process-task-completion';
export { executePayout, isNetworkError } from './payout-executor';
export { flushOfflineQueue } from './flush-queue';

// ─── SUPPRIMÉ Plan 04 ───
// export { loadLnbitsConfig, saveLnbitsConfig, clearLnbitsConfig } from './credentials';  // SUPPRIMÉ
// export type { ChildWalletMapping } from './types';                                       // SUPPRIMÉ
```

Précautions :
- `lib/lightning/migration.ts` (Plan 01) importe `loadLnbitsConfig` / `clearLnbitsConfig` depuis `./credentials`. CES IMPORTS doivent être RETIRÉS / remplacés par un accès direct à SecureStore.

**Refactor migration.ts** : remplacer les imports `./credentials` par un accès direct à `expo-secure-store` :
```typescript
// AVANT (utilise credentials.ts) :
import { loadLnbitsConfig, clearLnbitsConfig } from './credentials';

// APRÈS (accès direct, ne dépend plus de credentials.ts) :
import * as SecureStore from 'expo-secure-store';
const SINGLE_KEY = 'lightning_lnbits_config_v1';
async function readSingleLegacy(): Promise<{ baseUrl: string; invoiceKey: string } | null> {
  try {
    const raw = await SecureStore.getItemAsync(SINGLE_KEY);
    if (!raw) return null;
    const parsed = JSON.parse(raw);
    if (typeof parsed.baseUrl !== 'string' || typeof parsed.invoiceKey !== 'string') return null;
    return { baseUrl: parsed.baseUrl, invoiceKey: parsed.invoiceKey };
  } catch { return null; }
}
async function clearSingleLegacy(): Promise<void> {
  await SecureStore.deleteItemAsync(SINGLE_KEY);
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tâche 1 : REQ-6 'undone' audit entry — `subscribeTaskUncomplete` + 2ᵉ useEffect Lightning</name>
  <read_first>
    - hooks/useVaultTasks.ts (lignes 54, 76-125 — pattern `subscribeTaskComplete` à mirror)
    - hooks/useVault.ts (lignes 845-870 — emplacement du listener Lightning Phase 53)
    - lib/lightning/audit-log.ts (Plan 01 — `findPaidEntry` et `appendAudit` signatures)
    - lib/lightning/index.ts (barrel — `findPaidEntry`, `appendAudit` exports)
    - .planning/phases/53-lightning-family-wallet/53-SPEC.md (REQ-6 acceptance verbatim)
    - .planning/phases/53-lightning-family-wallet/53-02-SUMMARY.md (note "REQ-6 undone deferred")
    - CLAUDE.md (errors silencieuses pattern)
  </read_first>
  <behavior>
    - `subscribeTaskUncomplete` est exporté par `hooks/useVaultTasks.ts` (mirror de subscribeTaskComplete)
    - Toggle tâche `true→false` après un paid same-day → audit log contient `{status:'undone', taskId, profileId, ts}`
    - Toggle tâche `true→false` SANS paid same-day → AUCUNE entrée audit ajoutée (no-op)
    - Toggle tâche `false→true` ne déclenche PAS le listener uncomplete (séparation stricte des 2 events)
    - Tests Jest : `subscribeTaskUncomplete` fire/unsub/cleanup
  </behavior>
  <action>
    1. **hooks/useVaultTasks.ts** — Ajouter `subscribeTaskUncomplete(listener)` mirror exact de `subscribeTaskComplete`. Pattern :
       ```typescript
       // Module-level Set similaire à taskCompleteListenersRef
       const taskUncompleteListenersRef: Set<(task: Task) => void> = new Set();
       export function subscribeTaskUncomplete(listener: (task: Task) => void): () => void {
         taskUncompleteListenersRef.add(listener);
         return () => { taskUncompleteListenersRef.delete(listener); };
       }
       // Dans le toggle handler existant, détecter true→false transition et fire :
       function emitTaskUncomplete(task: Task) {
         for (const l of Array.from(taskUncompleteListenersRef)) {
           try { l(task); } catch (e) { if (__DEV__) console.warn('[useVaultTasks] uncomplete listener error:', e); }
         }
       }
       ```
       Brancher `emitTaskUncomplete(updatedTask)` dans le handler `toggleTask`/`updateTask` où la transition `prev.completed === true && next.completed === false` est détectée. Lire le code source actuel pour identifier précisément l'endroit (ligne ~76-125 par symétrie avec `subscribeTaskComplete`).

    2. **hooks/useVault.ts** — Ajouter un 2ᵉ useEffect Lightning juste après le useEffect listener pay-out (Phase 53 Plan 02) :
       ```typescript
       // Phase 53 Plan 04 — REQ-6 'undone' audit entry
       useEffect(() => {
         const unsub = tasksHook.subscribeTaskUncomplete((task) => {
           (async () => {
             try {
               const audit = await loadAudit();
               const completedDate = task.completedDate ?? new Date().toISOString().slice(0, 10);
               if (findPaidEntry(audit, task.id, completedDate)) {
                 await appendAudit({
                   ts: new Date().toISOString(),
                   profileId: '',  // resolution à postériori si possible — sinon string vide
                   taskId: task.id,
                   sats: 0,
                   status: 'undone',
                 });
               }
             } catch (e) {
               if (__DEV__) console.warn('[lightning] undone audit failed:', e);
               /* Lightning — non-critical, vault domain unaffected */
             }
           })();
         });
         return unsub;
       }, [tasksHook]);
       ```
       Imports requis : `loadAudit, findPaidEntry, appendAudit` depuis `../lib/lightning`.

    3. **Test Jest** — Ajouter `lib/lightning/__tests__/undone-audit.test.ts` (ou étendre `process-task-completion.test.ts`) :
       - Cas A : seed audit `paid` taskId='T1' date='2026-05-18' ; fire uncomplete T1 → audit contient nouvelle entrée `undone` taskId='T1'
       - Cas B : seed audit vide ; fire uncomplete T2 → audit reste vide (no-op, pas de paid prior)
       - Cas C : seed audit `paid` T3 mais date différente '2026-05-17' ; fire uncomplete T3 today '2026-05-18' → audit reste sans undone (findPaidEntry retourne false pour date mismatch)

    4. **Vérification** : `npx tsc --noEmit` clean. `npx jest --no-coverage lib/lightning/__tests__/` passes. `grep -c "subscribeTaskUncomplete" hooks/useVaultTasks.ts` >= 1 ET `grep -c "subscribeTaskUncomplete" hooks/useVault.ts` >= 1.
  </action>
  <verify>
    <automated>grep -c "subscribeTaskUncomplete" hooks/useVaultTasks.ts # >= 2 (export + emit)</automated>
    <automated>grep -c "subscribeTaskUncomplete" hooks/useVault.ts # >= 1</automated>
    <automated>grep -c "status: 'undone'" hooks/useVault.ts # >= 1 (audit entry)</automated>
    <automated>grep -c "findPaidEntry" hooks/useVault.ts # >= 1 (gate sur paid prior)</automated>
    <automated>npx tsc --noEmit 2>&1 | grep -E "hooks/useVault" | grep -v "MemoryEditor\|cooklang" | wc -l # tolère pré-existants mais pas de NOUVELLES erreurs</automated>
    <automated>npx jest --no-coverage lib/lightning/__tests__/ 2>&1 | tail -10</automated>
  </verify>
  <done>
    `subscribeTaskUncomplete` exposé par useVaultTasks.ts. 2ᵉ useEffect Lightning dans useVault.ts enregistre audit `undone` SI paid prior pour le couple taskId+date. Tests Jest passent. SPEC #6 acceptance entièrement couvert (Plans 02 + 04 ensemble).
  </done>
</task>

<task type="auto">
  <name>Tâche 2 : Refactor `lib/lightning/migration.ts` pour ne plus dépendre de credentials.ts + cleanup barrel + suppression atomique des 3 fichiers</name>
  <read_first>
    - lib/lightning/migration.ts (vérifier exactement quels imports de `./credentials` sont utilisés)
    - lib/lightning/credentials.ts (vérifier la clé SecureStore et le shape du single config — `lightning_lnbits_config_v1`, `{ baseUrl, invoiceKey }`)
    - lib/lightning/index.ts (état actuel après Plans 01-03 — vérifier exhaustivement quels exports legacy sont encore présents)
    - app/lightning-spike.tsx (vérifier qu'aucun composant ne l'importe — `grep -r 'lightning-spike' app components hooks lib --include='*.ts' --include='*.tsx'` doit déjà être vide après Plan 03)
    - app/lightning-family-spike.tsx (idem)
    - components/settings/SettingsLightning.tsx (Plan 03 a déjà retiré les handlers `handleOpenSpike`/`handleOpenFamily` et les TouchableOpacity vers les playgrounds — vérifier)
    - .planning/phases/53-lightning-family-wallet/53-02-SUMMARY.md (la migration single→family doit avoir été exécutée 1× par le runtime — confirmer)
  </read_first>
  <behavior>
    - migration.ts ne dépend plus de credentials.ts — fonctionne via SecureStore direct
    - Les 3 fichiers sont supprimés en UN SEUL commit
    - barrel ne référence plus loadLnbitsConfig / saveLnbitsConfig / clearLnbitsConfig / ChildWalletMapping
    - TSC clean : aucune référence cassée
    - Jest clean : toutes les suites passent
    - grep/find tests acceptance REQ-12 retournent 0
  </behavior>
  <action>
    1. **Refactor `lib/lightning/migration.ts`** — Remplacer les imports `loadLnbitsConfig, clearLnbitsConfig` de `./credentials` par 2 fonctions locales privées `readSingleLegacy()` et `clearSingleLegacy()` qui accèdent directement à `expo-secure-store` avec la clé `lightning_lnbits_config_v1`. Implémentation verbatim de `<interfaces>` ci-dessus. La logique de `migrateSingleToFamily` reste identique (3 cas Pitfall #9 : family_exists / no_single / migrated), seule la dépendance change. Vérifier que les tests `lib/lightning/__tests__/migration.test.ts` (Plan 01) continuent de passer — ils doivent fonctionner car ils mockent `expo-secure-store` qui inclut maintenant la clé `lightning_lnbits_config_v1` aussi. Si nécessaire, ajuster les tests pour seed directement la clé `lightning_lnbits_config_v1` au lieu d'appeler `saveLnbitsConfig`.

    2. **Mise à jour `lib/lightning/index.ts`** — Retirer du barrel les exports legacy :
       - Retirer `export { ... } from './credentials';` (la ligne entière)
       - Retirer `export type { ChildWalletMapping } from './types';` (si encore présente — Plan 01 a déjà fait ça, mais double-check)
       - CONSERVER `LnbitsConfig` type export (utilisé par les consommateurs pour instancier `new LnbitsClient({ baseUrl, invoiceKey })`)
       - État cible verbatim de `<interfaces>` ci-dessus.

    3. **Suppression atomique en 1 commit** des 3 fichiers :
       - `rm app/lightning-spike.tsx`
       - `rm app/lightning-family-spike.tsx`
       - `rm lib/lightning/credentials.ts`

    4. **Vérification grep/find acceptance REQ-12** (ces commandes DOIVENT retourner 0) :
       - `find app lib -name '*lightning-spike*' 2>/dev/null | wc -l` → 0
       - `grep -rE 'ChildWalletMapping|loadLnbitsConfig|saveLnbitsConfig|clearLnbitsConfig' lib components app --include='*.ts' --include='*.tsx' 2>/dev/null | wc -l` → 0
       - `grep -rE 'lightning-spike|lightning-family-spike' lib components app --include='*.ts' --include='*.tsx' 2>/dev/null | wc -l` → 0

    5. **TSC final** : `npx tsc --noEmit` clean. Si erreur, c'est qu'un consommateur référence encore une fonction supprimée — corriger immédiatement.

    6. **Jest final** : `npx jest --no-coverage` clean (suite complète projet). Les tests Lightning Plan 01 et Plan 02 doivent toujours passer. Aucune autre suite ne doit régresser. Si `migration.test.ts` casse à cause du refactor → fix immédiat en seedant SecureStore directement.

    7. **Vérification privacy CLAUDE.md** : `git log --oneline feat/lightning-farm --since='2026-05-17' | head -20` et inspecter les messages pour confirmer qu'aucun nom personnel réel n'apparaît. Idem `git diff main..feat/lightning-farm -- '.planning/phases/53-lightning-family-wallet/*'` ne doit contenir que Lucas/Emma/Dupont.

    8. **Branche posture finale** : `git branch --show-current` doit retourner `feat/lightning-farm`. PAS de merge sur main dans ce plan. PAS de `git push --force`. Si CI configure un check de release plus tard pour empêcher le merge accidentel, c'est hors scope Phase 53 (Open Q4 RESEARCH).
  </action>
  <verify>
    <automated>test ! -f app/lightning-spike.tsx</automated>
    <automated>test ! -f app/lightning-family-spike.tsx</automated>
    <automated>test ! -f lib/lightning/credentials.ts</automated>
    <automated>find app lib -name '*lightning-spike*' 2>/dev/null | wc -l # == 0</automated>
    <automated>grep -rE 'ChildWalletMapping|loadLnbitsConfig|saveLnbitsConfig|clearLnbitsConfig' lib components app --include='*.ts' --include='*.tsx' 2>/dev/null | grep -v '^#' | wc -l # == 0</automated>
    <automated>grep -rE 'lightning-spike|lightning-family-spike' lib components app --include='*.ts' --include='*.tsx' 2>/dev/null | grep -v '^#' | wc -l # == 0</automated>
    <automated>grep -c "from './credentials'" lib/lightning/migration.ts # == 0</automated>
    <automated>grep -c "lightning_lnbits_config_v1" lib/lightning/migration.ts # >= 1 (clé legacy lue directement)</automated>
    <automated>grep -c "ChildWalletMapping\|loadLnbitsConfig" lib/lightning/index.ts # == 0</automated>
    <automated>grep -c "MemberWalletMapping" lib/lightning/index.ts # >= 1</automated>
    <automated>npx tsc --noEmit 2>&1 | grep -E "lib/lightning|components/lightning|app/lightning-wallet|app/\(tabs\)/tree|components/settings/SettingsLightning" | grep -v "MemoryEditor\|cooklang\|useVault.ts" | wc -l # == 0</automated>
    <automated>npx jest --no-coverage lib/lightning/__tests__/ 2>&1 | tail -10</automated>
    <automated>git branch --show-current # feat/lightning-farm</automated>
  </verify>
  <done>
    3 fichiers legacy supprimés. migration.ts refactorée pour ne plus dépendre de credentials.ts. Barrel à jour. grep/find acceptance REQ-12 verts. TSC + Jest clean. Branche `feat/lightning-farm` toujours active (pas de merge sur main).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Tâche 3 — Checkpoint device final : non-régression ferme + posture branche + App Store metadata</name>
  <what-built>
    - 3 fichiers legacy supprimés (credentials.ts + 2 playgrounds spike)
    - Barrel `lib/lightning/index.ts` finalisé (sans exports single-wallet)
    - migration.ts refactorée (lecture directe SecureStore au lieu de credentials.ts)
    - L'application complète Phase 53 prête à itérer
  </what-built>
  <how-to-verify>
    **Flow 1 : Non-régression ferme (flag OFF)**
    1. Désactiver `LIGHTNING_ENABLED` dans Settings → Labo → Lightning
    2. Quitter complètement l'app (swipe up) et la rouvrir
    3. **Vérifier** : la ferme s'ouvre normalement — arbre, mascotte, plantes, animaux, jardin
    4. **Vérifier** : aucun bouton ⚡ visible dans le HUD
    5. Cocher 2-3 tâches
    6. **Vérifier** : aucun toast Lightning, aucun audit log entry (le listener fait early return sur flag OFF)
    7. **Vérifier** : XP gagné normalement (gamification préservée)
    8. **Vérifier** : Auberge fonctionne (un visiteur peut spawner — Phase 46 préservée)
    9. **Vérifier** : compteur tâches du jour à jour (Phase 40 widget préservée)

    **Flow 2 : Non-régression vault + cache**
    1. Forcer une régénération du cache (réinstall app ou clearCache via debug)
    2. Au reboot froid, **vérifier** : aucun crash, aucune erreur console __DEV__ référençant `loadLnbitsConfig` / `lightning-spike` (les fichiers n'existent plus)
    3. Vérifier les domaines vault : profils, tâches, recettes, courses, RDV, anniversaires fonctionnent

    **Flow 3 : Migration legacy (si applicable)**
    1. Si tu avais une config single-wallet legacy (clé `lightning_lnbits_config_v1`) :
    2. Au boot, **vérifier** : log __DEV__ "[lightning] migration result: migrated" OU "family_exists"
    3. Settings → Labo → Lightning → **vérifier** : la family config existe, members possiblement vide
    4. Inspect SecureStore (debug iOS via Xcode si possible) : la clé `lightning_lnbits_config_v1` est SUPPRIMÉE
    5. Si tu n'as jamais eu de single legacy : log __DEV__ "[lightning] migration result: no_single" — comportement attendu

    **Flow 4 : Settings Labo final**
    1. Settings → Labo → Lightning
    2. **Vérifier** : aucun bouton "Ouvrir l'écran de test" / "Mode famille (multi-wallet)" — ils ont été retirés par Plan 03
    3. **Vérifier** : la section Lightning contient bien : toggle ENABLED, form config family, TriggerModeSelector, dailyCap input, (et entrée "Pay-outs en attente (N)" si queue non vide)
    4. Tap "Enregistrer" sur le form de config → **vérifier** : pas de crash, pas d'alert "fichier introuvable"

    **Flow 5 : Branche & App Store posture**
    1. Confirmer par `git branch --show-current` que tu es sur `feat/lightning-farm`
    2. **Vérifier que tu NE FUSIONNES PAS sur `main`** — la décision de merge nécessite (a) une validation juridique App Store (spike 003 reste PARTIAL), (b) une décision explicite produit
    3. Vérifier App Store metadata (App Store Connect) :
       - Aucune mention de "Lightning", "sats", "Bitcoin" dans la description
       - Aucun screenshot ne montre le bouton ⚡ HUD ni l'écran wallet
       - Si tu as déjà uploadé un build avec Phase 53 sur TestFlight : note que la feature est invisible (flag OFF par défaut) — pas de risque de rejet immédiat
    4. **Optionnel** : créer un tag git `lightning-phase-53-ready` pour marquer le point de stabilité, sans push vers le remote

    **Flow 6 : Audit Privacy**
    1. `git log --oneline feat/lightning-farm --since='2026-05-17' | head -20`
    2. **Vérifier** : aucun nom personnel réel dans les messages de commit (générique Lucas/Emma/Dupont)
    3. `grep -r 'GabriEl\|Florian\|Maël' .planning/phases/53-lightning-family-wallet/ lib/lightning/ components/lightning/ app/lightning-wallet.tsx 2>/dev/null` → 0 résultat (remplacer les prénoms par ceux qui te concernent)

    **Flow 7 : Quality gate final**
    1. `npx tsc --noEmit` clean (hors erreurs pré-existantes documentées CLAUDE.md)
    2. `npx jest --no-coverage` clean (toutes les suites projet)
    3. **Vérifier** : aucun warning Metro bundler au démarrage du dev-client
  </how-to-verify>
  <resume-signal>
    Réponds :
    - "approved" si tous les flows sont OK et la feature est prête à itérer (mais PAS à merger sur main)
    - "issues: [description]" si tu vois une régression (crash boot, ferme blanche, tâche cassée, etc.)
    - "merge-decision-pending" pour indiquer que tu valides Phase 53 sans toucher à la posture App Store — décision merge reportée à un autre moment
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Suppression de fichiers `lib/lightning/credentials.ts` + `app/lightning-spike.tsx` + `app/lightning-family-spike.tsx` | Modification du file system Git. Diff revu par le développeur lors du commit. |
| Modification barrel `lib/lightning/index.ts` | API publique du module — risque de casser un consommateur si un export oublié. |
| Refactor `lib/lightning/migration.ts` | Accès SecureStore direct au lieu d'un helper — doit lire/écrire la même clé `lightning_lnbits_config_v1` qu'avant. |
| Branche `feat/lightning-farm` vs `main` | Posture App Store : pas de merge sans décision. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-04-01 | Tampering | Suppression fichiers casse imports | mitigate | grep verification AVANT suppression (Plan 03 a déjà retiré les TouchableOpacity de SettingsLightning, donc aucun consommateur de `lightning-spike` ne reste). TSC final + Jest final garantissent le clean. |
| T-53-04-02 | Information Disclosure | Données vault dans playgrounds spike | accept | Les playgrounds étaient en `__DEV__` only et ne sont pas distribués en prod release. Suppression = aucune fuite. |
| T-53-04-03 | Repudiation | Cleanup non documenté | mitigate | Le commit final doit avoir un message clair : "chore(53): cleanup spike playgrounds + retire single-wallet legacy (REQ-12)". SUMMARY.md liste exhaustivement les fichiers supprimés. |
| T-53-04-04 | Spoofing | Migration lit la mauvaise clé après refactor | mitigate | Tests `migration.test.ts` (Plan 01) doivent toujours passer après le refactor. Verification : `npx jest --no-coverage lib/lightning/__tests__/migration.test.ts` exits 0. Si test échoue, le refactor a un bug. |
| T-53-04-05 | Elevation of Privilege | Merge accidentel sur main | mitigate | Checkpoint device explicite "ne PAS merger sur main". Git branch protection à configurer côté Github si déployé (hors scope Phase 53, Open Q4 RESEARCH). |
| T-53-04-06 | Information Disclosure | App Store screenshots révèlent feature | mitigate | Checkpoint flow 5 demande au développeur de vérifier les screenshots App Store Connect. Flag OFF par défaut = invisible end-user en prod. |
| T-53-04-07 | Tampering | Privacy leak (noms réels) dans commits/sources | mitigate | Checkpoint flow 6 : grep manuel des prénoms personnels. CLAUDE.md règle. |

**Block-on severity:** high. T-53-04-05 (merge main accidentel) bloque la phase tant que le développeur ne confirme pas explicitement la non-fusion. T-53-04-06 (App Store screenshots) reste sous responsabilité humaine (impossible à automatiser depuis le repo).
</threat_model>

<verification>
- 3 fichiers supprimés : `app/lightning-spike.tsx`, `app/lightning-family-spike.tsx`, `lib/lightning/credentials.ts`
- `find app lib -name '*lightning-spike*'` → 0
- `grep -rE 'ChildWalletMapping|loadLnbitsConfig|saveLnbitsConfig|clearLnbitsConfig|lightning-spike|lightning-family-spike' lib components app --include='*.ts' --include='*.tsx'` → 0
- `lib/lightning/index.ts` ne référence plus les exports legacy
- `lib/lightning/migration.ts` n'importe plus depuis `./credentials` — lecture directe SecureStore
- `npx tsc --noEmit` clean
- `npx jest --no-coverage` clean — toutes les suites projet vertes (tests Lightning Plans 01-02 + autres)
- Branche `feat/lightning-farm` toujours active (`git branch --show-current` retourne `feat/lightning-farm`)
- Aucun nom personnel réel dans les commits Phase 53 (privacy CLAUDE.md)
- Checkpoint device approved : ferme/Auberge/widget/gamification non régressés (flag OFF)
- Migration legacy testée si applicable (single → family transparente)
</verification>

<success_criteria>
- Cleanup atomique exécuté : 3 fichiers supprimés en 1 commit + barrel à jour + migration.ts refactorée
- `grep`/`find` acceptance REQ-12 retournent 0
- TSC clean + Jest clean — pas de régression
- Ferme/Auberge/widget/gamification non régressés (verified device)
- Branche `feat/lightning-farm` isolée — aucun merge `main`
- App Store metadata : Lightning invisible (description, screenshots)
- Privacy : aucun nom personnel réel dans repo Phase 53
- Phase 53 prête à itérer/évoluer mais pas à shipper end-user sans décision juridique (cf. spike 003)
</success_criteria>

<output>
After completion, create `.planning/phases/53-lightning-family-wallet/53-04-SUMMARY.md` listing :
- Liste exhaustive des fichiers supprimés
- Confirmation de migration.ts refactorée (extraits before/after)
- État final du barrel `lib/lightning/index.ts` (exports listés)
- **REQ-6 'undone' audit entry** (deferred depuis Plan 02 — WARNING #7) : confirmation `subscribeTaskUncomplete` exposé + 2ᵉ useEffect Lightning branché + tests Jest verts
- Résultats grep/find/TSC/Jest
- Statut checkpoint device (approved / merge-decision-pending / issues)
- **Note `/gsd-execute-phase 53` checkpoint gate** : Plan 04 a bloqué sur `checkpoint:human-verify` (Tâche 3) jusqu'à `resume-signal`. Pas de modifs irréversibles si checkpoint refusé.
- Note finale : décision merge `main` REPORTÉE à un autre moment, posture spike 003 PARTIAL maintenue
- Recommendation post-phase : créer une issue GitHub pour la décision juridique App Store (LNbits BYO + sats only = pas de 3.1.5(iii) exchange rule a priori, mais validation tier requise)
</output>
