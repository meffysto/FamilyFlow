---
phase: 15-quetes-cooperatives-ferme
plan: 03
type: execute
wave: 3
depends_on: [15-01, 15-02]
files_modified:
  - hooks/useVault.ts
  - app/(tabs)/tree.tsx
  - app/(tabs)/tasks.tsx
  - app/(tabs)/index.tsx
autonomous: true
gap_closure: true
requirements: [QUEST-01]

must_haves:
  truths:
    - "La progression d'une quete augmente quand un membre complete une tache, recolte, ou defi"
  artifacts:
    - path: "hooks/useVault.ts"
      provides: "questsHook initialise avant defisHook, contribute passe comme 5e param a useVaultDefis"
      contains: "useVaultDefis(vaultRef, gamiDataRef, setGamiData, setProfiles, questsHook.contribute)"
    - path: "app/(tabs)/tree.tsx"
      provides: "useFarm() appele avec contributeFamilyQuest comme onQuestProgress"
      contains: "useFarm(contributeFamilyQuest)"
    - path: "app/(tabs)/tasks.tsx"
      provides: "useGamification appele avec onQuestProgress: contributeFamilyQuest"
      contains: "onQuestProgress: contributeFamilyQuest"
    - path: "app/(tabs)/index.tsx"
      provides: "useGamification appele avec onQuestProgress: contributeFamilyQuest"
      contains: "onQuestProgress: contributeFamilyQuest"
  key_links:
    - from: "hooks/useVaultDefis.ts"
      to: "hooks/useVaultFamilyQuests.ts"
      via: "onQuestProgress param (5e argument)"
      pattern: "questsHook\\.contribute"
    - from: "app/(tabs)/tree.tsx"
      to: "hooks/useFarm.ts"
      via: "useFarm(contributeFamilyQuest)"
      pattern: "useFarm\\(contributeFamilyQuest\\)"
    - from: "app/(tabs)/tasks.tsx"
      to: "hooks/useGamification.ts"
      via: "onQuestProgress: contributeFamilyQuest"
      pattern: "onQuestProgress.*contributeFamilyQuest"
    - from: "app/(tabs)/index.tsx"
      to: "hooks/useGamification.ts"
      via: "onQuestProgress: contributeFamilyQuest"
      pattern: "onQuestProgress.*contributeFamilyQuest"
---

<objective>
Cabrer les 4 call sites manquants pour que la progression de quete soit automatiquement declenchee lors de completions de taches, recoltes, crafts, et defis.

Purpose: Les signatures de hook onQuestProgress existent et sont correctement implementees — seuls les call sites n'ont jamais transmis la fonction. Ce plan ferme le gap Truth 4 (progression automatique non chablee) identifie dans 15-VERIFICATION.md.
Output: 4 fichiers mis a jour, progression quete chablee de bout en bout.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-quetes-cooperatives-ferme/15-VERIFICATION.md

<interfaces>
<!-- Signatures existantes — ne pas modifier, juste passer le bon argument -->

hooks/useVaultDefis.ts (l.44-50):
```typescript
export function useVaultDefis(
  vaultRef: React.MutableRefObject<VaultManager | null>,
  gamiDataRef: React.MutableRefObject<GamificationData | null>,
  setGamiData: React.Dispatch<React.SetStateAction<GamificationData | null>>,
  setProfiles: React.Dispatch<React.SetStateAction<Profile[]>>,
  onQuestProgress?: (profileId: string, type: string, amount: number) => Promise<void>,
): UseVaultDefisResult
```

hooks/useFarm.ts (l.137):
```typescript
export function useFarm(
  onQuestProgress?: (profileId: string, type: string, amount: number) => Promise<void>
)
```

hooks/useGamification.ts (l.37-42):
```typescript
interface UseGamificationArgs {
  vault: VaultManager | null;
  notifPrefs: NotificationPreferences;
  onDataChange?: (profiles: Profile[]) => void;
  onQuestProgress?: (profileId: string, type: string, amount: number) => Promise<void>;
}
```

hooks/useVault.ts (l.1571) — contributeFamilyQuest est deja expose dans VaultState:
```typescript
contributeFamilyQuest: questsHook.contribute,
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tache 1 : Cabrer les 4 call sites onQuestProgress</name>
  <files>hooks/useVault.ts, app/(tabs)/tree.tsx, app/(tabs)/tasks.tsx, app/(tabs)/index.tsx</files>
  <action>
    Quatre modifications chirurgicales — aucune nouvelle logique, seulement passer les arguments manquants.

    **1. hooks/useVault.ts — inverser l'ordre questsHook/defisHook**

    Actuellement (lignes 508-515) :
    ```
    // Domaine Défis délégué à useVaultDefis
    const gamiDataRef = useRef(gamiData);
    gamiDataRef.current = gamiData;
    const defisHook = useVaultDefis(vaultRef, gamiDataRef, setGamiData, setProfiles);
    const { defis } = defisHook;

    // Domaine Quêtes coopératives délégué à useVaultFamilyQuests
    const questsHook = useVaultFamilyQuests(vaultRef, gamiDataRef, setGamiData, setProfiles);
    ```

    Remplacer par :
    ```typescript
    // Domaine Quêtes coopératives — initialise EN PREMIER pour que contribute soit disponible pour defisHook
    const gamiDataRef = useRef(gamiData);
    gamiDataRef.current = gamiData;
    const questsHook = useVaultFamilyQuests(vaultRef, gamiDataRef, setGamiData, setProfiles);

    // Domaine Défis délégué à useVaultDefis — reçoit questsHook.contribute comme onQuestProgress
    const defisHook = useVaultDefis(vaultRef, gamiDataRef, setGamiData, setProfiles, questsHook.contribute);
    const { defis } = defisHook;
    ```

    Note : `gamiDataRef` etait initialise juste avant `defisHook` — le deplacer avec `questsHook` (meme bloc). Verifier qu'aucune autre variable entre les deux blocs ne cree une dependance (il n'y en a pas — recipesHook et profilesHook sont au-dessus).

    **2. app/(tabs)/tree.tsx — passer contributeFamilyQuest a useFarm()**

    `contributeFamilyQuest` est DEJA destructure depuis `useVault()` a la ligne 289. Seule la ligne 311 doit changer.

    Ligne 311 actuelle :
    ```typescript
    const { plant, harvest, buyBuilding, upgradeBuildingAction, collectBuildingResources, collectPassiveIncome, craft, sellHarvest, sellCrafted, unlockTech, checkWear, repairWear, getWearEffects, getWearEvents, sendGift, receiveGifts } = useFarm();
    ```

    Remplacer par :
    ```typescript
    const { plant, harvest, buyBuilding, upgradeBuildingAction, collectBuildingResources, collectPassiveIncome, craft, sellHarvest, sellCrafted, unlockTech, checkWear, repairWear, getWearEffects, getWearEvents, sendGift, receiveGifts } = useFarm(contributeFamilyQuest);
    ```

    **3. app/(tabs)/tasks.tsx — ajouter onQuestProgress a useGamification**

    Ligne 193 actuelle (destructure useVault) :
    ```typescript
    const { tasks, vault, profiles, activeProfile, notifPrefs, toggleTask, skipTask, addTask, editTask, deleteTask, refresh, isLoading, vacationTasks, vacationConfig, isVacationActive, refreshGamification, secretMissions, completeSecretMission, validateSecretMission } = useVault();
    ```

    Ajouter `contributeFamilyQuest` a cette destructuration :
    ```typescript
    const { tasks, vault, profiles, activeProfile, notifPrefs, toggleTask, skipTask, addTask, editTask, deleteTask, refresh, isLoading, vacationTasks, vacationConfig, isVacationActive, refreshGamification, secretMissions, completeSecretMission, validateSecretMission, contributeFamilyQuest } = useVault();
    ```

    Ligne 194 actuelle :
    ```typescript
    const { completeTask } = useGamification({ vault, notifPrefs });
    ```

    Remplacer par :
    ```typescript
    const { completeTask } = useGamification({ vault, notifPrefs, onQuestProgress: contributeFamilyQuest });
    ```

    **4. app/(tabs)/index.tsx — ajouter onQuestProgress a useGamification**

    La destructuration de `useVault()` dans index.tsx (multi-ligne, se termine a la ligne 213) ne contient pas `contributeFamilyQuest`. Ajouter `contributeFamilyQuest` dans le bloc de destructuration existant (juste avant le `} = useVault();` final).

    Ligne 218 actuelle :
    ```typescript
    const { completeTask } = useGamification({ vault, notifPrefs });
    ```

    Remplacer par :
    ```typescript
    const { completeTask } = useGamification({ vault, notifPrefs, onQuestProgress: contributeFamilyQuest });
    ```
  </action>
  <verify>
    <automated>cd /Users/gabrielwaltio/Documents/family-vault && npx tsc --noEmit 2>&1 | grep -v "MemoryEditor\|cooklang\|useVault.ts" | head -20</automated>
  </verify>
  <done>
    - grep -c "questsHook.contribute" hooks/useVault.ts retourne au moins 2 (arg defisHook + VaultState expose)
    - grep -c "useFarm(contributeFamilyQuest)" "app/(tabs)/tree.tsx" retourne 1
    - grep -c "onQuestProgress: contributeFamilyQuest" "app/(tabs)/tasks.tsx" retourne 1
    - grep -c "onQuestProgress: contributeFamilyQuest" "app/(tabs)/index.tsx" retourne 1
    - tsc passe sans nouvelles erreurs (hors erreurs pre-existantes MemoryEditor/cooklang)
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passe (hors erreurs pre-existantes MemoryEditor/cooklang/useVault.ts)
2. `grep -c "useFarm(contributeFamilyQuest)" "app/(tabs)/tree.tsx"` retourne 1
3. `grep -c "onQuestProgress: contributeFamilyQuest" "app/(tabs)/tasks.tsx"` retourne 1
4. `grep -c "onQuestProgress: contributeFamilyQuest" "app/(tabs)/index.tsx"` retourne 1
5. `grep "useVaultDefis.*questsHook.contribute\|questsHook.contribute.*useVaultDefis" hooks/useVault.ts` trouve la ligne
6. Dans hooks/useVault.ts, questsHook est initialise AVANT defisHook (chercher l'ordre dans le fichier)
</verification>

<success_criteria>
- La ligne `useVaultDefis(vaultRef, gamiDataRef, setGamiData, setProfiles, questsHook.contribute)` existe dans hooks/useVault.ts
- questsHook est initialise avant defisHook dans hooks/useVault.ts
- useFarm recoit contributeFamilyQuest dans tree.tsx
- useGamification recoit onQuestProgress: contributeFamilyQuest dans tasks.tsx et index.tsx
- tsc passe sans nouvelles erreurs
- Truth 4 du VERIFICATION.md est clos : completer une tache, une recolte, ou un defi trigge automatiquement la progression de la quete active correspondante
</success_criteria>

<output>
After completion, create `.planning/phases/15-quetes-cooperatives-ferme/15-03-SUMMARY.md`
</output>
