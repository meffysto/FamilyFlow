---
phase: 15-quetes-cooperatives-ferme
plan: 02
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - components/mascot/FamilyQuestBanner.tsx
  - components/mascot/FamilyQuestDetailSheet.tsx
  - components/mascot/FamilyQuestPickerSheet.tsx
  - app/(tabs)/tree.tsx
autonomous: true
requirements: [QUEST-01, QUEST-02, QUEST-03]

must_haves:
  truths:
    - "L'ecran ferme affiche un widget banniere de la quete active avec barre de progression et contributions par avatar"
    - "Taper la banniere ouvre un bottom sheet avec details complets de la quete"
    - "L'utilisateur peut demarrer une nouvelle quete depuis un picker avec les templates disponibles"
    - "La recompense de la quete est clairement visible sur la banniere et dans le detail"
  artifacts:
    - path: "components/mascot/FamilyQuestBanner.tsx"
      provides: "Widget banniere quete active — barre progression animee + avatars contributions"
      exports: ["FamilyQuestBanner"]
    - path: "components/mascot/FamilyQuestDetailSheet.tsx"
      provides: "Bottom sheet detail quete — contributions, reward preview, bouton completer"
      exports: ["FamilyQuestDetailSheet"]
    - path: "components/mascot/FamilyQuestPickerSheet.tsx"
      provides: "Bottom sheet picker templates quetes — grille de templates avec preview"
      exports: ["FamilyQuestPickerSheet"]
  key_links:
    - from: "app/(tabs)/tree.tsx"
      to: "components/mascot/FamilyQuestBanner.tsx"
      via: "rendu conditionnel avant WeeklyGoal"
      pattern: "FamilyQuestBanner"
    - from: "components/mascot/FamilyQuestBanner.tsx"
      to: "components/mascot/FamilyQuestDetailSheet.tsx"
      via: "onPress -> setShowQuestDetail(true)"
      pattern: "showQuestDetail"
    - from: "components/mascot/FamilyQuestPickerSheet.tsx"
      to: "hooks/useVaultFamilyQuests.ts"
      via: "createFamilyQuest(templateId)"
      pattern: "createFamilyQuest"
---

<objective>
Creer les composants UI des quetes cooperatives — banniere widget sur l'ecran ferme, bottom sheet detail, picker de templates — et les integrer dans tree.tsx.

Purpose: Rendre les quetes cooperatives visibles et interactives pour la famille sur l'ecran principal de la ferme.
Output: 3 composants (FamilyQuestBanner, FamilyQuestDetailSheet, FamilyQuestPickerSheet) + integration tree.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-quetes-cooperatives-ferme/15-RESEARCH.md
@.planning/phases/15-quetes-cooperatives-ferme/15-01-SUMMARY.md

@components/mascot/WeeklyGoal.tsx (pattern exact pour FamilyQuestBanner)
@app/(tabs)/tree.tsx (point d'insertion ~ligne 1935 avant WeeklyGoal)
@lib/quest-engine.ts (types FamilyQuest, FamilyFarmReward — cree plan 01)
@hooks/useVaultFamilyQuests.ts (API createQuest/contribute/completeQuest — cree plan 01)
@constants/questTemplates.ts (QUEST_TEMPLATES — cree plan 01)
@constants/spacing.ts (Spacing, Radius tokens)
@constants/shadows.ts (Shadows)

<interfaces>
<!-- Types crees par Plan 01 — l'executeur les utilise directement -->

From lib/quest-engine.ts:
```typescript
export type FamilyQuestType = 'tasks' | 'defis' | 'checkins' | 'harvest' | 'craft' | 'production' | 'streak' | 'golden_harvest' | 'composite';

export type FamilyFarmReward =
  | { type: 'unlock_plot' }
  | { type: 'rare_seeds'; quantity: number }
  | { type: 'building'; buildingId: string }
  | { type: 'rain_bonus'; durationHours: number }
  | { type: 'golden_rain'; durationHours: number }
  | { type: 'production_boost'; durationHours: number }
  | { type: 'loot_legendary'; count: number }
  | { type: 'crafting_recipe'; recipeId: string }
  | { type: 'tech_unlock'; nodeId: string }
  | { type: 'family_trophy'; trophyId: string }
  | { type: 'seasonal_decoration'; id: string };

export interface FamilyQuest {
  id: string;
  title: string;
  description: string;
  emoji: string;
  type: FamilyQuestType;
  target: number;
  current: number;
  contributions: Record<string, number>;
  farmReward: FamilyFarmReward;
  status: 'active' | 'completed' | 'expired';
  startDate: string;
  endDate: string;
  completedDate?: string;
}
```

From hooks/useVault.ts (expose par Plan 01):
```typescript
familyQuests: FamilyQuest[];
createFamilyQuest: (templateId: string) => Promise<void>;
contributeFamilyQuest: (profileId: string, type: FamilyQuestType, amount: number) => Promise<void>;
completeFamilyQuest: (questId: string) => Promise<void>;
deleteFamilyQuest: (questId: string) => Promise<void>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tache 1 : FamilyQuestBanner + FamilyQuestDetailSheet</name>
  <files>components/mascot/FamilyQuestBanner.tsx, components/mascot/FamilyQuestDetailSheet.tsx</files>
  <read_first>
    - components/mascot/WeeklyGoal.tsx (pattern complet — structure, animations, styles)
    - lib/quest-engine.ts (types FamilyQuest, FamilyFarmReward)
    - constants/spacing.ts (Spacing, Radius)
    - constants/shadows.ts (Shadows)
    - components/ui/ModalHeader.tsx (pattern bottom sheet header)
  </read_first>
  <action>
    **Creer `components/mascot/FamilyQuestBanner.tsx` :**

    Pattern exact de `WeeklyGoal.tsx` — meme structure, memes animations, meme placement.

    Props :
    ```typescript
    interface FamilyQuestBannerProps {
      quest: FamilyQuest;
      profiles: Profile[];
      colors: ReturnType<typeof useThemeColors>['colors'];
      t: (key: string, opts?: any) => string;
      onPress: () => void;
    }
    ```

    Layout (comme dans la phase_design) :
    ```
    +-------------------------------------+
    | {emoji} {title}          {jours}j   |
    | [====progress bar====]  {current}/{target} |
    | {avatar1} +{n}  {avatar2} +{n}     |
    | Recompense : {reward_label}         |
    +-------------------------------------+
    ```

    - Wrapper `Animated.View entering={FadeInDown}` (import depuis `react-native-reanimated`)
    - Barre de progression : `View` avec `width: ${progress * 100}%` et couleur `colors.primary`
    - Hauteur barre : `Spacing.xs` (4px)
    - Background barre : `colors.border` (track), `colors.primary` (fill)
    - Calcul jours restants : `differenceInDays(parseISO(quest.endDate), new Date())` avec `date-fns`
    - Contributions : mapper `Object.entries(quest.contributions)` et afficher pour chaque profil un emoji avatar (premier caractere du nom) + `+{count}`. Limiter a 4 profils affiches, `+N autres` si plus.
    - Recompense : fonction locale `getRewardLabel(reward: FamilyFarmReward, t): string` qui retourne un label lisible :
      - `loot_legendary` → "Coffre Legendaire x{count}"
      - `rare_seeds` → "Graines Rares x{quantity}"
      - `rain_bonus` → "Pluie Magique {durationHours}h"
      - `golden_rain` → "Pluie Doree {durationHours}h"
      - `production_boost` → "Boost Production {durationHours}h"
      - `building` → "Batiment : {buildingId}"
      - `tech_unlock` → "Technologie : {nodeId}"
      - `family_trophy` → "Trophee Familial"
      - `unlock_plot` → "Nouvelle Parcelle"
      - `crafting_recipe` → "Recette Secrete"
      - `seasonal_decoration` → "Decoration Saisonniere"
    - `TouchableOpacity onPress={onPress}` pour ouvrir le detail
    - `React.memo()` sur le composant
    - Padding : `Spacing.md` partout, `Radius.lg` pour le border radius
    - Background : `colors.card`, shadow : `Shadows.sm`
    - Toutes les couleurs via `colors.*` (jamais hardcodees)

    **Creer `components/mascot/FamilyQuestDetailSheet.tsx` :**

    Bottom sheet modal (pattern `pageSheet` + drag-to-dismiss).

    Props :
    ```typescript
    interface FamilyQuestDetailSheetProps {
      quest: FamilyQuest;
      profiles: Profile[];
      colors: ReturnType<typeof useThemeColors>['colors'];
      t: (key: string, opts?: any) => string;
      visible: boolean;
      onClose: () => void;
      onComplete: () => void;
      onDelete: () => void;
    }
    ```

    Contenu :
    - `ModalHeader` avec titre `{emoji} {quest.title}`
    - Section progression : grande barre de progression (hauteur `Spacing.sm`) + texte `{current} / {target}`
    - Section contributions : liste FlatList des profils avec pour chacun :
      - Avatar (premier emoji ou initiale du nom du profil)
      - Nom du profil
      - Barre individuelle proportionnelle a `contributions[profileId] / quest.target`
      - Texte `+{count}`
    - Section recompense : icone + label (reutiliser `getRewardLabel`)
    - Section dates : `Debut: {startDate} — Fin: {endDate}`
    - Si `quest.current >= quest.target` : bouton "Completer la quete" (couleur `colors.primary`, haptic `impactAsync`) qui appelle `onComplete`
    - Si status active : bouton "Supprimer" discret en bas qui appelle `onDelete` avec `Alert.alert` de confirmation en francais
    - Utiliser `Modal` RN avec `presentationStyle="pageSheet"` et `animationType="slide"` sur iOS
    - Exporter `getRewardLabel` depuis ce fichier pour reutilisation dans le banner (ou le definir dans quest-engine.ts et importer)
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -v "MemoryEditor\|cooklang\|useVault.ts" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "export.*FamilyQuestBanner" components/mascot/FamilyQuestBanner.tsx retourne 1
    - grep -c "React.memo" components/mascot/FamilyQuestBanner.tsx retourne 1
    - grep -c "FadeInDown" components/mascot/FamilyQuestBanner.tsx retourne 1
    - grep -c "colors\." components/mascot/FamilyQuestBanner.tsx retourne au moins 3
    - grep -c "export.*FamilyQuestDetailSheet" components/mascot/FamilyQuestDetailSheet.tsx retourne 1
    - grep -c "pageSheet\|presentationStyle" components/mascot/FamilyQuestDetailSheet.tsx retourne au moins 1
    - grep -c "onComplete\|onDelete" components/mascot/FamilyQuestDetailSheet.tsx retourne au moins 2
    - grep -c "getRewardLabel" components/mascot/FamilyQuestBanner.tsx retourne au moins 1
    - Aucune couleur hardcodee (#[0-9a-fA-F]{3,8}) dans FamilyQuestBanner.tsx ou FamilyQuestDetailSheet.tsx
  </acceptance_criteria>
  <done>FamilyQuestBanner affiche la quete active avec barre de progression animee, contributions par avatar, et label de recompense. FamilyQuestDetailSheet ouvre un bottom sheet pageSheet avec details complets, contributions individuelles, et boutons completer/supprimer. tsc passe.</done>
</task>

<task type="auto">
  <name>Tache 2 : FamilyQuestPickerSheet + integration tree.tsx</name>
  <files>components/mascot/FamilyQuestPickerSheet.tsx, app/(tabs)/tree.tsx</files>
  <read_first>
    - app/(tabs)/tree.tsx (structure complete — emplacement WeeklyGoal ~ligne 1935, state hooks, imports)
    - components/mascot/WeeklyGoal.tsx (pattern d'insertion)
    - components/mascot/FamilyQuestBanner.tsx (cree tache 1)
    - components/mascot/FamilyQuestDetailSheet.tsx (cree tache 1)
    - constants/questTemplates.ts (QUEST_TEMPLATES — cree plan 01)
    - lib/quest-engine.ts (FamilyQuestTemplate — cree plan 01)
    - constants/defiTemplates.ts (pattern picker)
  </read_first>
  <action>
    **Creer `components/mascot/FamilyQuestPickerSheet.tsx` :**

    Bottom sheet pour demarrer une nouvelle quete depuis un template.

    Props :
    ```typescript
    interface FamilyQuestPickerSheetProps {
      visible: boolean;
      onClose: () => void;
      onSelect: (templateId: string) => void;
      colors: ReturnType<typeof useThemeColors>['colors'];
      t: (key: string, opts?: any) => string;
    }
    ```

    Contenu :
    - `Modal` avec `presentationStyle="pageSheet"` + `animationType="slide"`
    - `ModalHeader` avec titre "Nouvelle Quete Familiale"
    - `ScrollView` avec grille de templates (2 colonnes, `flexWrap: 'wrap'`)
    - Chaque template affiche :
      - Emoji + titre
      - Type (en francais : "Recoltes", "Taches", "Defis", etc.)
      - Target : "{target} {type} en {durationDays}j"
      - Recompense : `getRewardLabel(template.reward)`
    - `TouchableOpacity` sur chaque template → `onSelect(template.id)` + `onClose()`
    - Import `QUEST_TEMPLATES` depuis `../../constants/questTemplates`
    - Import `getRewardLabel` depuis le fichier ou quest-engine ou on l'a defini en tache 1
    - `React.memo()` sur le composant
    - Haptic `selectionAsync` au tap
    - Couleurs via `colors.*`, padding `Spacing.md`, radius `Radius.lg`

    Fonction locale `getTypeLabel(type: FamilyQuestType): string` :
    - tasks → "Taches"
    - defis → "Defis"
    - harvest → "Recoltes"
    - golden_harvest → "Recoltes Dorees"
    - craft → "Creations"
    - production → "Productions"
    - streak → "Streak"
    - checkins → "Check-ins"
    - composite → "Mixte"

    **Modifier `app/(tabs)/tree.tsx` :**

    1. Importer les 3 composants :
       ```
       import { FamilyQuestBanner } from '../../components/mascot/FamilyQuestBanner';
       import { FamilyQuestDetailSheet } from '../../components/mascot/FamilyQuestDetailSheet';
       import { FamilyQuestPickerSheet } from '../../components/mascot/FamilyQuestPickerSheet';
       ```

    2. Recuperer depuis `useVault()` :
       ```
       const { familyQuests, createFamilyQuest, completeFamilyQuest, deleteFamilyQuest } = useVault();
       ```

    3. Ajouter les states :
       ```
       const [showQuestDetail, setShowQuestDetail] = useState(false);
       const [showQuestPicker, setShowQuestPicker] = useState(false);
       ```

    4. Calculer la quete active :
       ```
       const activeQuest = useMemo(() => familyQuests?.find(q => q.status === 'active') ?? null, [familyQuests]);
       ```

    5. Handlers (useCallback) :
       ```
       const handleCompleteQuest = useCallback(async () => {
         if (activeQuest) {
           await completeFamilyQuest(activeQuest.id);
           setShowQuestDetail(false);
           Haptics.notificationAsync(Haptics.NotificationFeedbackType.Success);
         }
       }, [activeQuest, completeFamilyQuest]);

       const handleDeleteQuest = useCallback(async () => {
         if (activeQuest) {
           await deleteFamilyQuest(activeQuest.id);
           setShowQuestDetail(false);
         }
       }, [activeQuest, deleteFamilyQuest]);

       const handleCreateQuest = useCallback(async (templateId: string) => {
         await createFamilyQuest(templateId);
         Haptics.impactAsync(Haptics.ImpactFeedbackStyle.Medium);
       }, [createFamilyQuest]);
       ```

    6. Inserer dans le JSX juste AVANT `<WeeklyGoal>` (~ligne 1935) :
       ```jsx
       {activeQuest ? (
         <FamilyQuestBanner
           quest={activeQuest}
           profiles={profiles}
           colors={colors}
           t={t}
           onPress={() => setShowQuestDetail(true)}
         />
       ) : (
         <TouchableOpacity
           onPress={() => setShowQuestPicker(true)}
           style={{ padding: Spacing.sm, alignItems: 'center' }}
         >
           <Text style={{ color: colors.primary, fontSize: 14 }}>
             + Nouvelle quete familiale
           </Text>
         </TouchableOpacity>
       )}
       ```

    7. Ajouter les modals en bas du JSX (avant la derniere fermeture de View/SafeArea) :
       ```jsx
       {activeQuest && (
         <FamilyQuestDetailSheet
           quest={activeQuest}
           profiles={profiles}
           colors={colors}
           t={t}
           visible={showQuestDetail}
           onClose={() => setShowQuestDetail(false)}
           onComplete={handleCompleteQuest}
           onDelete={handleDeleteQuest}
         />
       )}
       <FamilyQuestPickerSheet
         visible={showQuestPicker}
         onClose={() => setShowQuestPicker(false)}
         onSelect={handleCreateQuest}
         colors={colors}
         t={t}
       />
       ```
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -v "MemoryEditor\|cooklang\|useVault.ts" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "export.*FamilyQuestPickerSheet" components/mascot/FamilyQuestPickerSheet.tsx retourne 1
    - grep -c "QUEST_TEMPLATES" components/mascot/FamilyQuestPickerSheet.tsx retourne au moins 1
    - grep -c "pageSheet\|presentationStyle" components/mascot/FamilyQuestPickerSheet.tsx retourne au moins 1
    - grep -c "FamilyQuestBanner" "app/(tabs)/tree.tsx" retourne au moins 2
    - grep -c "FamilyQuestDetailSheet" "app/(tabs)/tree.tsx" retourne au moins 2
    - grep -c "FamilyQuestPickerSheet" "app/(tabs)/tree.tsx" retourne au moins 2
    - grep -c "showQuestDetail\|showQuestPicker" "app/(tabs)/tree.tsx" retourne au moins 4
    - grep -c "familyQuests\|createFamilyQuest\|completeFamilyQuest" "app/(tabs)/tree.tsx" retourne au moins 3
    - grep -c "activeQuest" "app/(tabs)/tree.tsx" retourne au moins 3
  </acceptance_criteria>
  <done>FamilyQuestPickerSheet affiche les 8 templates en grille avec preview. tree.tsx affiche FamilyQuestBanner si quete active, sinon bouton "+ Nouvelle quete". Tap banniere → detail sheet. Tap "Completer" → applyQuestReward + statut completed. tsc passe.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passe (hors erreurs pre-existantes)
2. L'ecran ferme (tree.tsx) affiche la banniere quete active avec progression et contributions
3. Taper la banniere ouvre le detail sheet avec contributions individuelles et bouton completer
4. Le picker permet de demarrer une nouvelle quete depuis les templates
5. Aucune couleur hardcodee dans les 3 composants — tout via `colors.*`
6. Animations via react-native-reanimated (FadeInDown) — pas RN Animated
</verification>

<success_criteria>
- Le widget banniere s'affiche sur l'ecran ferme avec barre de progression et contributions
- Le detail sheet montre les contributions par membre et la recompense
- L'utilisateur peut demarrer une nouvelle quete depuis le picker de templates
- L'utilisateur peut completer une quete quand la cible est atteinte
- Toutes les couleurs via useThemeColors, toutes les animations via reanimated
- tsc passe sans nouvelles erreurs
</success_criteria>

<output>
After completion, create `.planning/phases/15-quetes-cooperatives-ferme/15-02-SUMMARY.md`
</output>
