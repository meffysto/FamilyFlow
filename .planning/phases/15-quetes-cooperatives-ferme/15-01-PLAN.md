---
phase: 15-quetes-cooperatives-ferme
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/quest-engine.ts
  - constants/questTemplates.ts
  - lib/parser.ts
  - hooks/useVaultFamilyQuests.ts
  - hooks/useVault.ts
  - hooks/useGamification.ts
  - hooks/useFarm.ts
  - hooks/useVaultDefis.ts
autonomous: true
requirements: [QUEST-01, QUEST-03]

must_haves:
  truths:
    - "Une quete familiale peut etre creee depuis un template avec target, reward, et dates"
    - "Une seule quete active a la fois — startQuest refuse si une quete est deja active"
    - "Seuls les adultes et ados peuvent demarrer une quete (role check)"
    - "La progression d'une quete augmente quand un membre complete une tache, recolte, ou defi"
    - "Quand current >= target, la quete peut etre completee et les recompenses ferme s'appliquent a tous les profils"
    - "Les quetes et leur progression sont persistees dans family-quests.md et rechargees au demarrage"
    - "Une quete expiree (endDate < today && status active) est detectee au loadVault, passe en expired, et declenche une notification locale"
  artifacts:
    - path: "lib/quest-engine.ts"
      provides: "Types FamilyQuest, FamilyFarmReward, FamilyQuestType + applyQuestReward() + serializeReward/parseReward"
      exports: ["FamilyQuest", "FamilyFarmReward", "FamilyQuestType", "FamilyQuestTemplate", "applyQuestReward", "serializeReward", "parseReward"]
    - path: "constants/questTemplates.ts"
      provides: "8 templates de quetes cooperatives"
      exports: ["QUEST_TEMPLATES"]
    - path: "lib/parser.ts"
      provides: "parseFamilyQuests() + serializeFamilyQuests()"
      exports: ["parseFamilyQuests", "serializeFamilyQuests"]
    - path: "hooks/useVaultFamilyQuests.ts"
      provides: "Hook domaine CRUD quetes familiales avec contrainte 1 quete active + role check"
      exports: ["useVaultFamilyQuests", "UseVaultFamilyQuestsResult"]
  key_links:
    - from: "hooks/useVault.ts"
      to: "hooks/useVaultFamilyQuests.ts"
      via: "questsHook = useVaultFamilyQuests(...)"
      pattern: "useVaultFamilyQuests"
    - from: "hooks/useGamification.ts"
      to: "hooks/useVaultFamilyQuests.ts"
      via: "onQuestProgress callback"
      pattern: "onQuestProgress"
    - from: "hooks/useFarm.ts"
      to: "hooks/useVaultFamilyQuests.ts"
      via: "onQuestProgress callback"
      pattern: "onQuestProgress"
    - from: "hooks/useVault.ts"
      to: "lib/scheduled-notifications.ts"
      via: "notification locale a l'expiration d'une quete"
      pattern: "scheduleNotificationAsync"
---

<objective>
Creer le systeme de quetes cooperatives familiales — types, engine, parser, templates, hook domaine, et integration progression dans les hooks existants (tasks, recoltes, defis).

Purpose: Fournir toute la couche donnees/logique pour que la famille puisse creer, progresser, et completer des quetes partagees avec recompenses ferme. Contrainte : 1 seule quete active a la fois, demarrage reserve aux adultes/ados, detection expiration avec notification.
Output: quest-engine.ts, questTemplates.ts, parser extensions, useVaultFamilyQuests.ts, integration hooks existants.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-quetes-cooperatives-ferme/15-RESEARCH.md

@lib/parser.ts (parseDefis/serializeDefis pattern — lignes 940-1040)
@hooks/useVaultDefis.ts (pattern hook domaine exact a reproduire)
@hooks/useVault.ts (point d'integration questsHook)
@hooks/useGamification.ts (point d'integration completeTask)
@hooks/useFarm.ts (point d'integration harvest)
@lib/types.ts (FarmProfileData, Profile — noter Profile.role: 'enfant' | 'ado' | 'adulte')
@lib/mascot/farm-engine.ts (advanceFarmCrops, harvestCrop)
@lib/mascot/building-engine.ts (constructBuilding)
@lib/mascot/tech-engine.ts (unlockTechNode)
@constants/defiTemplates.ts (pattern templates)
@lib/scheduled-notifications.ts (pattern expo-notifications — scheduleNotificationAsync)
</context>

<tasks>

<task type="auto">
  <name>Tache 1 : Types quest-engine + parser + templates</name>
  <files>lib/quest-engine.ts, constants/questTemplates.ts, lib/parser.ts</files>
  <read_first>
    - lib/parser.ts (lignes 940-1040 — pattern parseDefis/serializeDefis)
    - constants/defiTemplates.ts (pattern templates)
    - lib/types.ts (FarmProfileData, Profile, GamificationData)
    - lib/mascot/farm-engine.ts (exports advanceFarmCrops)
    - lib/mascot/building-engine.ts (exports constructBuilding)
    - lib/mascot/tech-engine.ts (exports unlockTechNode)
    - hooks/useVaultDefis.ts (pattern completeDefi — distribution multi-profil rewards)
  </read_first>
  <action>
    **Creer `lib/quest-engine.ts` :**

    1. Exporter les types depuis la phase_design :
       - `FamilyQuestType` = `'tasks' | 'defis' | 'checkins' | 'harvest' | 'craft' | 'production' | 'golden_harvest' | 'composite'`
       - `FamilyFarmReward` (discriminated union avec 11 variantes : unlock_plot, rare_seeds, building, rain_bonus, golden_rain, production_boost, loot_legendary, crafting_recipe, tech_unlock, family_trophy, seasonal_decoration)
       - `FamilyQuest` interface (id, title, description, emoji, type, target, current, contributions: Record<string, number>, farmReward, status, startDate, endDate, completedDate?)
       - `FamilyQuestTemplate` interface (id, title, emoji, type, target, durationDays, reward: FamilyFarmReward, description)

    2. Exporter `serializeReward(r: FamilyFarmReward): string` — format compact `type:param` (ex: `loot_legendary:2`, `unlock_plot` sans param). Pattern identique au format CSV colon-separe de building-engine.

    3. Exporter `parseReward(s: string): FamilyFarmReward` — inverse de serializeReward. Gerer les cas sans param (unlock_plot) et avec param numerique ou string.

    4. Exporter `applyQuestReward(vault: VaultManager, profileIds: string[], reward: FamilyFarmReward): Promise<void>` — pattern reward-first :
       - `loot_legendary` : pour chaque profil, lire `gami-{id}.md`, incrementer `lootBoxesAvailable`, reecrire
       - `rare_seeds` : pour chaque profil, lire `farm-{id}.md`, ajouter au `farmRareSeeds`, reecrire
       - `rain_bonus` / `golden_rain` / `production_boost` : ecrire un champ `activeEffect: type:expiresAt` dans `family-quests.md` (passe en argument). Calculer expiresAt = now + durationHours
       - `building` : pour chaque profil, ajouter `offeredBuilding: buildingId` dans `farm-{id}.md`
       - `tech_unlock` : pour chaque profil, ajouter nodeId a `farm_tech` dans `farm-{id}.md`
       - `unlock_plot` : pour chaque profil, ajouter `unlock_extra_plot` a `farm_tech` dans `farm-{id}.md`
       - `family_trophy` : ajouter `trophyId` a un champ `trophies` dans `family-quests.md`
       - `crafting_recipe` / `seasonal_decoration` : stocker dans `family-quests.md` champ `unlockedRecipes` / `unlockedDecorations`

       Import `VaultManager` depuis `../lib/vault`. Import `parseGamification`/`serializeGamification` depuis `../lib/parser`. Utiliser les parsers farm existants.

    5. Exporter `createQuestFromTemplate(template: FamilyQuestTemplate): FamilyQuest` — genere id `quest_${Date.now()}_${Math.random().toString(36).slice(2,5)}`, status 'active', current 0, contributions {}, startDate today, endDate = today + durationDays.

    **Creer `constants/questTemplates.ts` :**

    Exporter `QUEST_TEMPLATES: FamilyQuestTemplate[]` avec les 7 templates :
    - moisson_collective (harvest, 25, 7j, loot_legendary:1)
    - grand_defrichage (tasks, 50, 7j, unlock_plot)
    - champions_defi (defis, 3, 14j, rare_seeds:10)
    - artisans_familiaux (craft, 5, 10j, crafting_recipe:galette_royale)
    - graines_dorees (golden_harvest, 3, 14j, golden_rain:48)
    - semaine_production (production, 30, 7j, production_boost:48)
    - pluie_magique (tasks, 30, 5j, rain_bonus:24)

    Import `FamilyQuestTemplate` depuis `../lib/quest-engine`.

    **Etendre `lib/parser.ts` :**

    Ajouter apres `serializeDefis` (~ligne 1040) :
    - `export function parseFamilyQuests(content: string): FamilyQuest[]` — meme pattern que parseDefis : H2 = titre quete, cles/valeurs ligne par ligne, `contributions: emma:5,lucas:4` parse en Record. Importer `FamilyQuest`, `parseReward` depuis `../lib/quest-engine`.
    - `export function serializeFamilyQuests(quests: FamilyQuest[], meta?: { activeEffect?: string; trophies?: string[]; unlockedRecipes?: string[]; unlockedDecorations?: string[] }): string` — meme pattern que serializeDefis. Frontmatter `tags: [quests]`, H1 `# Quetes familiales`. Chaque quete en H2 avec cles/valeurs. Sauf `contributions` serialise comme `emma:5,lucas:4`. Le meta optionnel ajoute des champs apres le H1 (avant les H2).
    - Exporter `FAMILY_QUESTS_FILE = 'family-quests.md'`
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -v "MemoryEditor\|cooklang\|useVault.ts" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "export type FamilyQuestType" lib/quest-engine.ts retourne 1
    - grep -c "export type FamilyFarmReward" lib/quest-engine.ts retourne 1
    - grep -c "export interface FamilyQuest" lib/quest-engine.ts retourne 1
    - grep -c "export function applyQuestReward" lib/quest-engine.ts retourne 1
    - grep -c "export function serializeReward" lib/quest-engine.ts retourne 1
    - grep -c "export function parseReward" lib/quest-engine.ts retourne 1
    - grep -c "export function createQuestFromTemplate" lib/quest-engine.ts retourne 1
    - grep -c "QUEST_TEMPLATES" constants/questTemplates.ts retourne au moins 1
    - grep -c "moisson_collective" constants/questTemplates.ts retourne 1
    - grep -c "export function parseFamilyQuests" lib/parser.ts retourne 1
    - grep -c "export function serializeFamilyQuests" lib/parser.ts retourne 1
    - grep -c "FAMILY_QUESTS_FILE" lib/parser.ts retourne 1
  </acceptance_criteria>
  <done>Types FamilyQuest/FamilyFarmReward exportes, quest-engine avec applyQuestReward/serialize/parse/createFromTemplate, 8 templates, parser extensions parseFamilyQuests/serializeFamilyQuests dans parser.ts. tsc passe.</done>
</task>

<task type="auto">
  <name>Tache 2 : Hook useVaultFamilyQuests + cablage useVault + callbacks progression + expiration</name>
  <files>hooks/useVaultFamilyQuests.ts, hooks/useVault.ts, hooks/useGamification.ts, hooks/useFarm.ts, hooks/useVaultDefis.ts</files>
  <read_first>
    - hooks/useVaultDefis.ts (pattern complet — signature, state, CRUD, completeDefi multi-profil)
    - hooks/useVault.ts (pattern d'integration defisHook, loadVault, VaultState expose)
    - hooks/useGamification.ts (completeTask — point d'insertion onQuestProgress)
    - hooks/useFarm.ts (harvest — point d'insertion onQuestProgress)
    - lib/quest-engine.ts (cree en tache 1 — types, applyQuestReward, createQuestFromTemplate)
    - lib/parser.ts (parseFamilyQuests, serializeFamilyQuests, FAMILY_QUESTS_FILE — crees en tache 1)
    - lib/types.ts (Profile.role — 'enfant' | 'ado' | 'adulte')
    - lib/scheduled-notifications.ts (pattern Notifications.scheduleNotificationAsync)
  </read_first>
  <action>
    **Creer `hooks/useVaultFamilyQuests.ts` :**

    Pattern exact de `useVaultDefis.ts`. Signature :
    ```typescript
    export function useVaultFamilyQuests(
      vaultRef: React.MutableRefObject<VaultManager | null>,
      gamiDataRef: React.MutableRefObject<GamificationData | null>,
      setGamiData: React.Dispatch<React.SetStateAction<GamificationData | null>>,
      setProfiles: React.Dispatch<React.SetStateAction<Profile[]>>,
    ): UseVaultFamilyQuestsResult
    ```

    State : `const [familyQuests, setFamilyQuests] = useState<FamilyQuest[]>([]);`

    Fonctions (toutes `useCallback`) :

    - `startQuest(templateId: string, profileId: string, profiles: Profile[]): Promise<void>` :
      1. **Contrainte 1 quete active** : verifier `familyQuests.some(q => q.status === 'active')`. Si oui, `Alert.alert('Quete en cours', 'La famille a deja une quete active. Terminez-la ou attendez son expiration avant d en lancer une nouvelle.')` et return sans creer.
      2. **Contrainte role** : trouver le profil dans `profiles` par `profileId`, verifier `profile.role === 'adulte' || profile.role === 'ado'`. Si non, `Alert.alert('Acces restreint', 'Seuls les adultes et ados peuvent demarrer une quete familiale.')` et return.
      3. Si les deux checks passent : trouver template dans QUEST_TEMPLATES, appeler `createQuestFromTemplate`, append a la liste, `vault.writeFile(FAMILY_QUESTS_FILE, serializeFamilyQuests(updated))`

    - `contribute(profileId: string, type: FamilyQuestType, amount: number): Promise<void>` — pattern read-modify-write : lire `family-quests.md` frais (pas depuis state React, cf pitfall 1), trouver la quete active dont `quest.type === type`, incrementer `current += amount` et `contributions[profileId] += amount`. Si `current >= target` ne PAS auto-completer (l'utilisateur choisit quand). Reecrire le fichier.

    - `completeQuest(questId: string): Promise<void>` — pattern reward-first : lire les profils depuis `famille.md`, appeler `applyQuestReward(vault, profileIds, quest.farmReward)` AVANT de marquer `status: 'completed'` + `completedDate`. Puis reecrire `family-quests.md`.

    - `checkAndExpireQuests(quests: FamilyQuest[]): FamilyQuest[]` — fonction helper (pas useCallback, appelee depuis loadVault) :
      1. Pour chaque quete avec `status === 'active'` et `endDate < today` (comparer avec `new Date().toISOString().slice(0, 10)`), passer `status` a `'expired'`.
      2. Pour chaque quete nouvellement expiree, declencher une notification locale :
         ```typescript
         import * as Notifications from 'expo-notifications';
         await Notifications.scheduleNotificationAsync({
           content: {
             title: 'Quete expiree',
             body: `⏰ La quete "${quest.title}" a expire ! La famille n'a pas atteint l'objectif — on retente ?`,
           },
           trigger: null, // immediate
         });
         ```
      3. Retourner le tableau mis a jour (avec les status changes).
      4. Si des quetes ont ete expirees, reecrire `family-quests.md` via vault.

    - `deleteQuest(questId: string): Promise<void>` — filtrer la quete, reecrire.
    - `resetQuests()` — `setFamilyQuests([])`

    Export `UseVaultFamilyQuestsResult` interface avec : familyQuests, setFamilyQuests, startQuest, contribute, completeQuest, deleteQuest, resetQuests, checkAndExpireQuests.

    `gamiFile(profileId)` defini localement (pattern etabli — evite dependance circulaire).

    **Modifier `hooks/useVault.ts` :**

    1. Importer `useVaultFamilyQuests` depuis `./useVaultFamilyQuests`
    2. Importer `parseFamilyQuests, FAMILY_QUESTS_FILE` depuis `../lib/parser`
    3. Dans `useVaultInternal()`, apres la ligne `const defisHook = useVaultDefis(...)`, ajouter :
       ```
       const questsHook = useVaultFamilyQuests(vaultRef, gamiDataRef, setGamiData, setProfiles);
       ```
    4. Dans `loadVault()`, apres le chargement de `newDefis`, ajouter :
       ```
       const questsContent = await vault.readFile(FAMILY_QUESTS_FILE).catch(() => '');
       let loadedQuests = parseFamilyQuests(questsContent);
       // Detection expiration au chargement
       loadedQuests = await questsHook.checkAndExpireQuests(loadedQuests);
       questsHook.setFamilyQuests(loadedQuests);
       ```
    5. Passer `questsHook.contribute` comme `onQuestProgress` au gamiHook et farmHook (via les callbacks existants ou en ajoutant le parametre).
    6. Dans le return/VaultState, exposer :
       - `familyQuests: questsHook.familyQuests`
       - `startFamilyQuest: questsHook.startQuest`
       - `contributeFamilyQuest: questsHook.contribute`
       - `completeFamilyQuest: questsHook.completeQuest`
       - `deleteFamilyQuest: questsHook.deleteQuest`

    **Modifier `hooks/useGamification.ts` :**

    1. Ajouter `onQuestProgress?: (profileId: string, type: string, amount: number) => Promise<void>` au type des arguments du hook (ou dans les parametres de `completeTask`).
    2. Dans `completeTask`, apres l'ecriture du fichier gami, ajouter :
       ```
       if (onQuestProgress) {
         try { await onQuestProgress(profile.id, 'tasks', 1); } catch { /* Quest — non-critical */ }
       }
       ```

    **Modifier `hooks/useFarm.ts` :**

    1. Ajouter `onQuestProgress?: (profileId: string, type: string, amount: number) => Promise<void>` aux parametres.
    2. Dans la fonction harvest, apres l'ecriture du profil ferme, ajouter :
       ```
       if (onQuestProgress) {
         try {
           const isGolden = result.isGolden;
           await onQuestProgress(profileId, isGolden ? 'golden_harvest' : 'harvest', 1);
         } catch { /* Quest — non-critical */ }
       }
       ```
    3. Dans collectBuilding (si existe), apres collection :
       ```
       if (onQuestProgress) {
         try { await onQuestProgress(profileId, 'production', 1); } catch { /* Quest — non-critical */ }
       }
       ```
    4. Dans craftItem (si existe dans useFarm), apres craft :
       ```
       if (onQuestProgress) {
         try { await onQuestProgress(profileId, 'craft', 1); } catch { /* Quest — non-critical */ }
       }
       ```

    **Modifier `hooks/useVaultDefis.ts` :**

    1. Ajouter `onQuestProgress?: (profileId: string, type: string, amount: number) => Promise<void>` aux parametres.
    2. Dans `checkInDefi`, apres la section XP (dans le bloc `if (isNewCheckIn && completed)`), ajouter :
       ```
       if (onQuestProgress) {
         try { await onQuestProgress(profileId, 'defis', 1); } catch { /* Quest — non-critical */ }
       }
       ```
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -v "MemoryEditor\|cooklang\|useVault.ts" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "export function useVaultFamilyQuests" hooks/useVaultFamilyQuests.ts retourne 1
    - grep -c "familyQuests" hooks/useVault.ts retourne au moins 2
    - grep -c "questsHook" hooks/useVault.ts retourne au moins 3
    - grep -c "FAMILY_QUESTS_FILE" hooks/useVault.ts retourne au moins 1
    - grep -c "onQuestProgress" hooks/useGamification.ts retourne au moins 2
    - grep -c "onQuestProgress" hooks/useFarm.ts retourne au moins 2
    - grep -c "onQuestProgress" hooks/useVaultDefis.ts retourne au moins 2
    - grep "startQuest\|contribute\|completeQuest\|deleteQuest" hooks/useVaultFamilyQuests.ts
    - grep "reward-first\|applyQuestReward" hooks/useVaultFamilyQuests.ts retourne au moins 1
    - grep -c "role.*adulte\|role.*ado" hooks/useVaultFamilyQuests.ts retourne au moins 1
    - grep -c "status.*active.*some\|some.*active" hooks/useVaultFamilyQuests.ts retourne au moins 1
    - grep -c "scheduleNotificationAsync\|expo-notifications" hooks/useVaultFamilyQuests.ts retourne au moins 1
    - grep -c "checkAndExpireQuests" hooks/useVaultFamilyQuests.ts retourne au moins 1
    - grep -c "startFamilyQuest" hooks/useVault.ts retourne au moins 1
  </acceptance_criteria>
  <done>Hook useVaultFamilyQuests fonctionnel avec CRUD + progression. Contrainte 1 quete active a la fois (startQuest refuse si active). Role check adulte/ado sur startQuest. Detection expiration dans loadVault avec notification locale. Cable dans useVault.ts (load + expire check + expose). Callbacks onQuestProgress dans useGamification (tasks), useFarm (harvest/production/craft), useVaultDefis (defis). tsc passe.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passe (hors erreurs pre-existantes MemoryEditor/cooklang/useVault.ts)
2. Les types FamilyQuest et FamilyFarmReward sont exportes depuis quest-engine.ts
3. parseFamilyQuests/serializeFamilyQuests font un roundtrip correct (serialise puis re-parse une quete = memes donnees)
4. useVault.ts expose familyQuests, startFamilyQuest, contributeFamilyQuest, completeFamilyQuest dans son return
5. Les 3 hooks existants (useGamification, useFarm, useVaultDefis) appellent onQuestProgress apres leurs actions respectives
6. startQuest refuse si une quete est deja active (Alert.alert)
7. startQuest refuse si le profil n'est pas adulte ou ado (Alert.alert)
8. loadVault detecte les quetes expirees et declenche une notification locale
</verification>

<success_criteria>
- Une quete peut etre creee depuis un template, persistee dans family-quests.md, et rechargee au demarrage
- Une seule quete active a la fois — startQuest refuse avec Alert si une quete est deja active
- Seuls les adultes/ados peuvent demarrer une quete — role check dans startQuest
- Completer une tache, recolter, ou faire un defi incremente la progression de la quete active correspondante
- Completer une quete applique la recompense ferme a tous les profils (reward-first pattern)
- Les quetes expirees sont detectees au loadVault et declenchent une notification locale
- tsc passe sans nouvelles erreurs
</success_criteria>

<output>
After completion, create `.planning/phases/15-quetes-cooperatives-ferme/15-01-SUMMARY.md`
</output>
