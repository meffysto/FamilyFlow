---
phase: 40-ui-spor-e-seed-picker-badge-validation
plan: 04
type: execute
wave: 3
depends_on: [40-01, 40-02, 40-03]
files_modified:
  - hooks/useFarm.ts
  - lib/__tests__/useFarm-wager.test.ts
autonomous: false
requirements: [SPOR-07]
must_haves:
  truths:
    - "User récolte un plant scellé avec cumul atteint → reward × multiplier + toast FR 'Victoire ! +X 🍃 (×M)' affiché"
    - "User récolte un plant scellé cumul atteint avec RNG 15% favorable → toast inclut '· Sporée retrouvée 🎁', sporeeCount += 1 (respectant cap 10)"
    - "User récolte un plant scellé cumul NON atteint → reward normale + toast neutre FR 'Plant récolté · Sporée consommée' (jamais punitif)"
    - "User voit le modifier.wager disparaître après récolte (plant entier nettoyé, zéro ghost data vault)"
    - "Injection RNG testable : `rollWagerDropBack(() => 0.1)` → drop-back TRUE / `rollWagerDropBack(() => 0.5)` → FALSE"
  artifacts:
    - path: "hooks/useFarm.ts"
      provides: "Branche harvest wager finalisée (toast + drop-back + multiplier après golden)"
    - path: "lib/__tests__/useFarm-wager.test.ts"
      provides: "Smoke test validation + drop-back injection"
      min_lines: 80
  key_links:
    - from: "hooks/useFarm.ts harvest wager won"
      to: "contexts/ToastContext.showToast"
      via: "message inline incluant drop-back conditionnel"
      pattern: "showToast.*Victoire.*Sporée retrouvée"
    - from: "hooks/useFarm.ts harvest wager lost"
      to: "contexts/ToastContext.showToast"
      via: "message neutre type='info'"
      pattern: "showToast.*Plant récolté.*consommée"
---

<objective>
Finaliser la boucle end-to-end Sporée : vérifier que la branche wager de `useFarm.harvest` (livrée Plan 01) produit les 3 variantes de toast correctes (victoire / victoire+drop-back / défaite) et que le drop-back RNG 15% est effectivement câblé avec `tryIncrementSporeeCount` respectant le cap inventaire. Ajouter une suite Jest dédiée testant les cas critiques via injection RNG.

Inclure un **checkpoint human-verify** pour valider le flow complet sur device (sceller → compléter tâches → récolter victoire/défaite) — Plan 02 + Plan 03 livrent l'UI, ce plan clôt la boucle.

Purpose : garantir que SPOR-07 est pleinement satisfait (reward × multiplier + toast + drop-back 15%) et qu'aucun toast punitif n'apparaît sur défaite (Core Value bien-être familial).

Output : useFarm finalisé (ajustements éventuels post-Plan 01) + suite Jest dédiée + checkpoint UX device.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/40-ui-spor-e-seed-picker-badge-validation/40-01-SUMMARY.md
@.planning/phases/40-ui-spor-e-seed-picker-badge-validation/40-02-SUMMARY.md
@.planning/phases/40-ui-spor-e-seed-picker-badge-validation/40-03-SUMMARY.md

@hooks/useFarm.ts
@contexts/ToastContext.tsx
@lib/mascot/sporee-economy.ts
@lib/mascot/wager-engine.ts

<interfaces>
From Plan 40-01 (hooks/useFarm.ts) :
```typescript
// branche wager dans harvest (déjà livrée) — ce plan vérifie + complète.
// rollWagerDropBack(random?) : boolean  — injection testable.
// tryIncrementSporeeCount(current, delta) : { accepted, newCount } — cap 10.
// validateWagerOnHarvest(cumulCurrent, cumulTarget) : { won, cumulCurrent, cumulTarget }.
```

From contexts/ToastContext.tsx :
```typescript
showToast(message: string, type: 'success' | 'info' | 'error' | 'warn', action?, options?): void;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 : Suite Jest useFarm-wager — validation + drop-back injection + extraction buildWagerHarvestToast</name>
  <files>lib/__tests__/useFarm-wager.test.ts, lib/mascot/wager-ui-helpers.ts, hooks/useFarm.ts</files>
  <behavior>
    Tests unitaires sur les fonctions pures consommées par la branche wager de harvest — sans monter le hook React (trop lourd, dépendances vault).

    **Describe 1 : `validateWagerOnHarvest` × `tryIncrementSporeeCount` integration**
    - cumul atteint (current=10, target=10) → won=true, `Math.round(30 * 2.5) = 75` feuilles.
    - cumul non atteint (current=5, target=10) → won=false, reward normale (30).
    - cumul atteint + drop-back true (RNG 0.1) + sporeeCount=5 → accepted=true, newCount=6.
    - cumul atteint + drop-back false (RNG 0.5) → accepted stays n/a (pas d'appel increment).
    - cumul atteint + drop-back true + sporeeCount=10 (cap) → tryIncrementSporeeCount refuse, newCount=10, flag overflow.

    **Describe 2 : `buildWagerHarvestToast` (helper pur FR)**
    - won=false, tous params → retourne "Plant récolté · Sporée consommée".
    - won=true, dropBack=false, finalQty=75, multiplier=2.5 → "Victoire ! +75 🍃 (×2.5)".
    - won=true, dropBack=true, finalQty=75, multiplier=2.5 → "Victoire ! +75 🍃 (×2.5) · Sporée retrouvée 🎁".
    - won=true, multiplier=1.3 → message contient "×1.3".
    - Tous messages en FR, emojis 🍃 et 🎁 corrects.

    ≥8 tests total, tous verts.
  </behavior>
  <action>
    1. **Extraire `buildWagerHarvestToast` dans `lib/mascot/wager-ui-helpers.ts`** :
       ```typescript
       export function buildWagerHarvestToast(opts: {
         won: boolean;
         finalQty: number;
         multiplier: number;
         dropBack: boolean;
       }): string {
         if (!opts.won) return 'Plant récolté · Sporée consommée';
         const suffix = opts.dropBack ? ' · Sporée retrouvée 🎁' : '';
         return `Victoire ! +${opts.finalQty} 🍃 (×${opts.multiplier})${suffix}`;
       }
       ```

    2. **Mettre à jour `hooks/useFarm.ts`** :
       - Importer `buildWagerHarvestToast` depuis `lib/mascot/wager-ui-helpers`.
       - Remplacer les templates inline dans la branche wager de `harvest` (livrée Plan 01) par `showToast(buildWagerHarvestToast({ won, finalQty, multiplier, dropBack }), won ? 'success' : 'info')`.
       - Conserver la logique vault/drop-back inchangée, seul le builder de message change.

    3. **Créer `lib/__tests__/useFarm-wager.test.ts`** :
       - Importer les primitives pures : `validateWagerOnHarvest`, `rollWagerDropBack`, `tryIncrementSporeeCount`, `buildWagerHarvestToast`.
       - 2 describe blocs (intégration validation/drop-back + format toast FR).
       - Injection RNG explicite (`() => 0.1`, `() => 0.5`) — pattern Phase 38.
       - ≥8 tests total.
       - Aucun mock vault nécessaire — tout est pur.

    Convention FR commentaires + noms tests FR acceptés (pattern Phase 39).
  </action>
  <verify>
    <automated>npx jest --no-coverage lib/__tests__/useFarm-wager.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>
    - `buildWagerHarvestToast` exporté depuis `wager-ui-helpers.ts`.
    - `useFarm.harvest` branche wager consomme le helper (plus d'inline template).
    - Suite Jest `useFarm-wager.test.ts` verte avec ≥8 tests.
    - Tous les cas won/lost × dropBack true/false × cap inventaire couverts.
    - Format toast FR validé par test (jamais punitif sur défaite).
    - `npx tsc --noEmit` clean.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2 : Checkpoint UX device — boucle Sporée end-to-end</name>
  <files>(aucun fichier modifié — validation visuelle uniquement)</files>
  <action>
    Build dev-client iOS et valider manuellement les 5 scénarios ci-dessous. Ce checkpoint clôt Phase 40 : si un scénario échoue, créer un node-repair ciblé ou rouvrir le plan concerné.

    ```bash
    npx expo run:ios --device
    ```

    **Scénario 1 — Sceller + victoire + drop-back (RNG dépendant)** :
    1. S'assurer d'avoir ≥1 Sporée en inventaire (drop récolte ou cadeau onboarding arbuste→arbre Phase 38).
    2. Aller sur l'onglet Ferme (arbre), taper sur un slot vide → seed picker apparaît.
    3. Choisir une graine peu coûteuse (ex. maïs, 1 tasksPerStage).
    4. **ATTENDU** : pageSheet secondaire "Sceller cette plantation ?" apparaît après ~300ms.
    5. Vérifier les 3 options affichées avec multipliers (×1.3 / ×1.7 / ×2.5) + preview durée + cumul cible.
    6. Taper "Sprint ×2.5".
    7. **ATTENDU** : Sporée -1, plant apparaît scellé avec badge visible (X/Y · Z/N).
    8. Compléter plusieurs tâches du domaine Tasks jusqu'à atteindre `cumulTarget`.
    9. **ATTENDU** : badge pace passe orange → jaune → vert, puis anneau vert apparaît quand plant mûr + cumul atteint.
    10. Récolter le plant.
    11. **ATTENDU** : toast "Victoire ! +X 🍃 (×2.5)" OU "Victoire ! +X 🍃 (×2.5) · Sporée retrouvée 🎁" (RNG 15%).
    12. Vérifier sporeeCount inventaire reflète drop-back si applicable.

    **Scénario 2 — Sceller + défaite** :
    1. Sceller un plant (durée courte Sprint).
    2. Ne PAS compléter assez de tâches (laisser cumul < target).
    3. Laisser le plant mûrir ou forcer via data manip.
    4. Récolter.
    5. **ATTENDU** : toast neutre "Plant récolté · Sporée consommée". Aucun message punitif, aucune perte de feuilles supplémentaire.

    **Scénario 3 — Skip + plantation normale** :
    1. Avec ≥1 Sporée, ouvrir seed picker, choisir une graine.
    2. Sealer apparaît → taper "Pas cette fois — planter normalement".
    3. **ATTENDU** : plant apparaît sans badge, Sporée inchangée.

    **Scénario 4 — Zéro Sporée = comportement historique** :
    1. Consommer toutes les Sporées ou partir d'un profil à 0.
    2. Ouvrir seed picker, choisir graine.
    3. **ATTENDU** : plantation directe, aucun sealer affiché.

    **Scénario 5 — Plusieurs wagers simultanés** :
    1. Sceller 2-3 plants différents (consomme 2-3 Sporées).
    2. Compléter 1 tâche Tasks.
    3. **ATTENDU** : TOUS les badges des plants scellés incrémentent leur `cumulCurrent` de 1.

    **Privacy check avant commit final** : vérifier aucun nom réel en console.log ou commit messages Phase 40.
  </action>
  <verify>
    <automated>MISSING — checkpoint humain : validation visuelle des 5 scénarios sur device iOS, non automatisable.</automated>
  </verify>
  <done>
    - Les 5 scénarios device passent sans régression.
    - Aucun toast punitif sur défaite (Core Value respectée).
    - Badge + anneau affichés selon les règles (visible dès stage 0, anneau double-gate).
    - Drop-back 15% observé au moins une fois (ou testé via data manip + RNG injectée).
    - User tape "approved" OU liste précise des bugs à corriger.
  </done>
  <resume-signal>Type "approved" si tous scénarios OK, ou décris précisément ce qui échoue (scénario + comportement observé vs attendu).</resume-signal>
</task>

</tasks>

<verification>
**Automated (post-checkpoint) :**
- `npx tsc --noEmit` clean hors pré-existant (MemoryEditor, cooklang, useVault.ts).
- `npx jest --no-coverage` : suites wager-engine (65) + wager-ui-helpers (≥10) + sporee-economy (+3 drop-back) + useFarm-wager (≥8) toutes vertes.
- Privacy check : grep noms réels dans commits phase 40 → 0 match.

**Goal-backward (must_haves.truths) :**
- Toast victoire FR avec multiplier affiché ✓
- Drop-back inline dans toast ✓ (pas de notif séparée)
- Toast défaite neutre/bienveillant FR ✓
- Modifier wager nettoyé post-harvest ✓ (harvestCrop existant efface le plant entier)
- RNG testable via injection ✓
</verification>

<success_criteria>
- [ ] Suite Jest `useFarm-wager.test.ts` verte (≥8 tests).
- [ ] `buildWagerHarvestToast` extrait dans `wager-ui-helpers.ts` et consommé par `useFarm.harvest`.
- [ ] Checkpoint device validé sur 5 scénarios (victoire, défaite, skip, zéro Sporée, multi-wagers).
- [ ] Toast FR correct dans les 3 variantes (victoire / victoire+drop-back / défaite).
- [ ] Cap inventaire Sporée respecté lors de drop-back (pas de dépassement 10).
- [ ] Zéro régression suite Jest globale.
- [ ] Commits FR, zéro nom réel.
</success_criteria>

<output>
Créer `.planning/phases/40-ui-spor-e-seed-picker-badge-validation/40-04-SUMMARY.md` :
- Résultats des 5 scénarios device + éventuels bugs découverts et fix-ups appliqués.
- Confirmation de l'extraction `buildWagerHarvestToast` + usage dans `useFarm`.
- Récap Phase 40 complète : 4 plans, requirements MOD-03/SPOR-01/SPOR-02/SPOR-07/SPOR-11 couverts.
- Pointeur vers Phase 41 (SPOR-10 onboarding tooltip + SPOR-12 codex marathonWins + non-régression finale).
</output>
