---
phase: 40-ui-spor-e-seed-picker-badge-validation
plan: 03
type: execute
wave: 2
depends_on: [40-01]
files_modified:
  - components/mascot/PlantWagerBadge.tsx
  - components/mascot/WagerReadyRing.tsx
  - components/mascot/WorldGridView.tsx
autonomous: true
requirements: [SPOR-02, SPOR-11]
must_haves:
  truths:
    - "User voit un badge overlay 2-LIGNES sur chaque plant scellé : ligne 1 'X/Y tâches aujourd'hui' (consommé depuis wager.tasksCompletedToday) + ligne 2 'cumul Z/N' (cumulCurrent/cumulTarget) avec code couleur pace (vert/jaune/orange)"
    - "User voit le badge visible dès la plantation (stage 0) pas seulement à maturité — c'est le feedback progression quotidienne (G6)"
    - "User voit un anneau vert 'prêt à valider' overlay sur le plant uniquement quand plant mûr (stage ≥ 4) ET cumul atteint (cumulCurrent ≥ cumulTarget) — double gate"
    - "User ne voit AUCUNE animation continue lourde sur le badge (zéro reanimated) — anneau optionnel breathing très lent honorant reduceMotion"
    - "User avec plusieurs plants scellés simultanés voit N badges indépendants sans impact CPU (React.memo + zéro shared clock)"
    - "Le badge consomme `wager.totalDays` (persisté Plan 01) — ZÉRO magic number '7' ni fallback `/7` côté UI (B2)"
  artifacts:
    - path: "components/mascot/PlantWagerBadge.tsx"
      provides: "Badge overlay pure View + Text memoïsé, 2 lignes, zéro animation"
      min_lines: 60
    - path: "components/mascot/WagerReadyRing.tsx"
      provides: "Anneau vert statique + breathing optionnel reanimated (reduceMotion aware)"
      min_lines: 50
    - path: "components/mascot/WorldGridView.tsx"
      provides: "CropCell étendu avec <PlantWagerBadge> + <WagerReadyRing> conditionnels, paceLevel via computePaceLevel, totalDays lu depuis wager.totalDays"
  key_links:
    - from: "components/mascot/WorldGridView.tsx CropCell"
      to: "components/mascot/PlantWagerBadge"
      via: "render conditionnel si crop.modifiers?.wager présent, props nourries directement par wager.tasksCompletedToday / wager.totalDays / wager.cumulCurrent / wager.cumulTarget"
      pattern: "PlantWagerBadge"
    - from: "components/mascot/WorldGridView.tsx CropCell"
      to: "components/mascot/WagerReadyRing"
      via: "render conditionnel double gate (mûr + cumul atteint)"
      pattern: "WagerReadyRing"
    - from: "components/mascot/PlantWagerBadge"
      to: "lib/mascot/wager-ui-helpers.computePaceLevel"
      via: "useMemo → paceLevel → couleur de fond (calculé dans le parent CropCell, passé en prop)"
      pattern: "computePaceLevel"
    - from: "components/mascot/WorldGridView.tsx CropCell"
      to: "lib/mascot/wager-ui-helpers.daysBetween"
      via: "import depuis wager-ui-helpers (Plan 01 Task 1) pour calculer daysElapsed"
      pattern: "daysBetween"
---

<objective>
Livrer les 2 composants d'overlay visuel sur les plants scellés : `PlantWagerBadge` (badge **2 LIGNES** `X/Y tâches aujourd'hui` + `cumul Z/N`, zéro animation, React.memo) et `WagerReadyRing` (anneau vert statique avec breathing optionnel reanimated très subtil, gate double mûr + cumul atteint). Injecter les deux dans `CropCell` de `WorldGridView.tsx` en overlay absolu au-dessus du sprite, sans conflit layout.

**Consommation data** : toutes les valeurs du badge (`tasksCompletedToday`, `totalDays`) sont consommées DIRECTEMENT depuis `wager` (persisté Plan 01). **Zéro magic number**, **zéro fallback `/7`**, aucun recalcul côté UI.

Purpose : feedback visuel permanent de l'engagement pari + moment critique "prêt à valider". C'est ce qui transforme le plant en objet stratégique visible chaque jour. SPOR-02 livré en 2 lignes comme verrouillé en CONTEXT.md décision 3.

Output : 2 nouveaux composants + modification ciblée de `WorldGridView.tsx` (injection dans CropCell existant).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/40-ui-spor-e-seed-picker-badge-validation/40-CONTEXT.md
@.planning/phases/40-ui-spor-e-seed-picker-badge-validation/40-RESEARCH.md
@.planning/phases/40-ui-spor-e-seed-picker-badge-validation/40-01-SUMMARY.md

@components/mascot/WorldGridView.tsx
@contexts/ThemeContext.tsx
@constants/colors.ts
@constants/spacing.ts
@constants/typography.ts
@components/ui/Badge.tsx

<interfaces>
From Plan 40-01 (lib/mascot/wager-ui-helpers.ts) :
```typescript
export type PaceLevel = 'green' | 'yellow' | 'orange';
export function computePaceLevel(
  cumulCurrent: number,
  cumulTarget: number,
  daysElapsed: number,
  totalDays: number,
): PaceLevel;
export function daysBetween(startISO: string, endISO: string): number;
// computeWagerTotalDays exporté aussi mais NON utilisé dans ce plan
// (la valeur est persistée sur wager.totalDays au startWager — Plan 01).
```

From lib/mascot/types.ts (SHAPE POST-Plan 40-01) :
```typescript
export interface WagerModifier {
  sporeeId: string;
  duration: WagerDuration;
  multiplier: WagerMultiplier;
  appliedAt: string;
  sealerProfileId: string;
  cumulTarget?: number;
  cumulCurrent?: number;
  tasksCompletedToday?: number;   // Phase 40 — B1
  lastDailyResetDate?: string;    // Phase 40 — B1
  totalDays?: number;             // Phase 40 — B2
}
export interface FarmCrop {
  cropId: string;
  currentStage: number; // 0-4, 4 = mûr
  modifiers?: { wager?: WagerModifier };
  // ...
}
```

Pattern CropCell existant (WorldGridView.tsx lignes 88-254) :
- `stageRow` (dots progression) = pure View memoïsée, aucun re-render parent.
- Overlay absolu pattern déjà utilisé pour whispers.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1 : PlantWagerBadge.tsx (badge 2-lignes pure, zéro animation)</name>
  <files>components/mascot/PlantWagerBadge.tsx</files>
  <action>
    Composant pure View + Text memoïsé. **Zéro reanimated, zéro useSharedValue.**

    **Props** (SPOR-02 : 2 lignes verrouillées CONTEXT.md) :
    ```typescript
    interface PlantWagerBadgeProps {
      cumulCurrent: number;
      cumulTarget: number;
      tasksToday: number;         // consommé depuis wager.tasksCompletedToday (Plan 01)
      tasksTargetToday: number;   // Math.max(1, Math.ceil(cumulTarget / max(1, daysRemaining))) — calculé parent
      paceLevel: PaceLevel;       // 'green' | 'yellow' | 'orange'
    }
    ```

    **Rendu** (position absolue, taille ~48x30px pour 2 lignes compactes) :
    - `<View style={{ position: 'absolute', top: -Spacing.xs, right: -Spacing.xs, … }}>`.
    - Couleur de fond dérivée de `paceLevel` :
      - green → `colors.successBg` (ou proche).
      - yellow → `colors.warningBg`.
      - orange → `colors.errorBg`.
      - (Si tokens exacts absents, choisir le plus proche existant dans `constants/colors.ts` — **jamais hardcoded**.)
    - Border : `borderWidth: 1, borderColor: colors.border`.
    - Radius : `Radius.sm` ou `Radius.md`.
    - Padding : `Spacing.xs` horizontal, `2` vertical (via token minimal).
    - **2 lignes text compactes** (SPOR-02 verrouillé) :
      - Ligne 1 : `${tasksToday}/${tasksTargetToday}` — `FontSize.caption`, `FontWeight.semibold`, `colors.textPrimary`. AccessibilityLabel complet : "X tâches complétées sur Y aujourd'hui".
      - Ligne 2 : `${cumulCurrent}/${cumulTarget}` — `FontSize.microCaption` (ou plus petit disponible), `colors.textMuted`. Préfixe visuel discret (emoji ∑ ou juste les chiffres).
    - Fallback si `cumulTarget === 0` (P2, pari auto-gagné ou pas encore recompute) : afficher ligne 2 `'✓'` au lieu de `0/0`, ligne 1 `'—/—'`.

    **Perf critique** :
    - `export const PlantWagerBadge = React.memo(function PlantWagerBadge({ … }: PlantWagerBadgeProps) { … })`.
    - Hook `useThemeColors()` (cheap).
    - `useMemo` pour la palette `paceBg` dérivée du `paceLevel`.
    - Aucune animation, aucun timer, aucun shared clock.

    **Accessibilité** :
    - `accessible={true}`, `accessibilityRole="text"`, `accessibilityLabel={\`Pari : \${tasksToday} sur \${tasksTargetToday} tâches aujourd'hui, cumul \${cumulCurrent} sur \${cumulTarget}\`}`.

    **Commentaires FR** : documenter la gate "badge visible dès stage 0" (G6) et la règle "2 lignes obligatoires CONTEXT.md décision 3".
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>
    - `PlantWagerBadge.tsx` créé, ~60-100 lignes.
    - React.memo appliqué, zéro reanimated.
    - **2 lignes rendues** (jamais 1 ligne — SPOR-02 verrouillé).
    - 3 couleurs pace via tokens theme, zéro hardcoded.
    - Fallback cumulTarget=0 géré (P2).
    - Accessibilité FR.
    - TS clean.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2 : WagerReadyRing.tsx (anneau vert double-gate + breathing optionnel)</name>
  <files>components/mascot/WagerReadyRing.tsx</files>
  <action>
    Composant overlay anneau vert statique **ou** breathing très subtil.

    **Props** :
    ```typescript
    interface WagerReadyRingProps {
      size: number;       // diamètre px (matché au sprite CropCell)
      borderRadius?: number; // default size / 2
    }
    ```

    **Rendu statique (fallback reduceMotion)** :
    ```tsx
    <View style={{
      position: 'absolute',
      top: 0, left: 0,
      width: size, height: size,
      borderWidth: 2,
      borderColor: colors.successText,
      borderRadius: borderRadius ?? size / 2,
      pointerEvents: 'none',
    }} />
    ```

    **Rendu breathing (optionnel, si reduceMotion === false)** :
    - `const opacity = useSharedValue(0.7);`
    - Au mount : `opacity.value = withRepeat(withTiming(1.0, { duration: 2000 }), -1, true);`
    - `useAnimatedStyle(() => ({ opacity: opacity.value }))`.
    - Appliquer sur `Animated.View` (reanimated).
    - **Constante module** : `const BREATHING_CONFIG = { duration: 2000 } as const;`.

    **Check reduceMotion** :
    - Utiliser `useReducedMotion()` de `react-native-reanimated` si disponible, sinon `AccessibilityInfo.isReduceMotionEnabled()` via effect.
    - Si reduceMotion → `<View>` statique (pas d'animation démarrée).
    - Si reduceMotion=false → `<Animated.View>` avec opacity breathing.

    **Perf** :
    - Ce composant n'est rendu que pour plants **mûrs ET cumul atteint** — cas rare (0-2 plants simultanés max typique).
    - `useSharedValue` par instance acceptable.
    - Pas de cleanup manuel : reanimated dispose au unmount.

    **Pas de child** : overlay visuel pur, `pointerEvents: 'none'`.

    **Couleur** : `colors.successText` (ou `colors.success` selon tokens existants — vérifier `constants/colors.ts`). **Jamais hardcoded**.

    **Commentaires FR** : documenter "double gate mûr + cumul atteint appliqué par le parent CropCell".
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>
    - `WagerReadyRing.tsx` créé, ~50-80 lignes.
    - Breathing reanimated optionnel + fallback statique si reduceMotion.
    - `pointerEvents: 'none'`.
    - Couleur via token theme.
    - TS clean.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3 : Injection dans WorldGridView.tsx CropCell — consommation directe wager.totalDays + wager.tasksCompletedToday</name>
  <files>components/mascot/WorldGridView.tsx</files>
  <action>
    Modifications ciblées dans `CropCell` existant (lignes ~88-254). **Zéro magic number, zéro fallback `/7`.**

    1. **Imports** :
       ```typescript
       import { PlantWagerBadge } from './PlantWagerBadge';
       import { WagerReadyRing } from './WagerReadyRing';
       import { computePaceLevel, daysBetween } from '../../lib/mascot/wager-ui-helpers';
       import { getLocalDateKey } from '../../lib/mascot/sporee-economy'; // ou location actuelle
       ```

    2. **Dans `CropCell`**, après avoir parsé `crop` :
       ```typescript
       const wager = crop.modifiers?.wager;
       const showBadge = !!wager; // G6 : visible dès stage 0
       const isMature = crop.currentStage >= 4;
       const cumulTarget = wager?.cumulTarget ?? 0;
       const cumulCurrent = wager?.cumulCurrent ?? 0;
       const cumulReached = !!wager && cumulTarget > 0 && cumulCurrent >= cumulTarget;
       const showReadyRing = isMature && !!wager && cumulReached;

       // Pace level — B2 : totalDays consommé DIRECTEMENT depuis wager.totalDays (persisté Plan 01)
       const paceLevel = useMemo(() => {
         if (!wager) return 'green' as const;
         const today = getLocalDateKey(new Date());
         const daysElapsed = daysBetween(wager.appliedAt, today);
         const totalDays = wager.totalDays ?? 1; // fallback minimal : 1 jour (jamais magic number 7)
         return computePaceLevel(cumulCurrent, cumulTarget, daysElapsed, totalDays);
       }, [wager, cumulCurrent, cumulTarget]);

       // tasksToday — B1 : consommé DIRECTEMENT depuis wager.tasksCompletedToday (persisté Plan 01)
       const tasksToday = wager?.tasksCompletedToday ?? 0;

       // tasksTargetToday — cadence requise basée sur totalDays persisté (pas magic 7)
       const tasksTargetToday = useMemo(() => {
         if (!wager) return 1;
         const today = getLocalDateKey(new Date());
         const daysElapsed = daysBetween(wager.appliedAt, today);
         const totalDays = wager.totalDays ?? 1;
         const daysRemaining = Math.max(1, totalDays - daysElapsed);
         const cumulRemaining = Math.max(0, cumulTarget - cumulCurrent);
         return Math.max(1, Math.ceil(cumulRemaining / daysRemaining));
       }, [wager, cumulCurrent, cumulTarget]);
       ```

       **Note B1/B2** :
       - `wager.tasksCompletedToday` (B1) et `wager.totalDays` (B2) sont PERSISTÉS par Plan 01 au `startWager` et maintenus par `incrementWagerCumul` (reset quotidien).
       - **ZÉRO fallback `/7`**, **zéro magic number**. Les fallback `?? 0` / `?? 1` servent uniquement à la rétro-compatibilité avec paris créés AVANT Plan 40-01 (cas inexistant en pratique puisque Phase 40 est la première à exposer l'UI).

    3. **Injection dans le JSX de CropCell** (au-dessus du sprite, dans le container existant) :
       ```tsx
       <View style={cropContainerStyle}>
         {showReadyRing && <WagerReadyRing size={spriteSize} />}
         {/* sprite existant */}
         {showBadge && wager && (
           <PlantWagerBadge
             cumulCurrent={cumulCurrent}
             cumulTarget={cumulTarget}
             tasksToday={tasksToday}
             tasksTargetToday={tasksTargetToday}
             paceLevel={paceLevel}
           />
         )}
       </View>
       ```

    4. **Vérifier que l'overlay absolu ne casse pas le layout existant** (whispers, stageRow, sprite frame swap). Le badge en `top: -Spacing.xs, right: -Spacing.xs` doit sortir légèrement du cropContainer sans clipping — ajouter `overflow: 'visible'` au container si nécessaire.

    **Commentaires FR** au-dessus du bloc wager documentant : "Consommation directe wager.totalDays et wager.tasksCompletedToday — persistés par Plan 40-01, aucun recalcul ici (B1/B2 plan-checker)".

    **Perf** : `useMemo` pour paceLevel + tasksTargetToday avec deps minimales. `React.memo` sur CropCell déjà existant.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>
    - CropCell injecte `<PlantWagerBadge>` conditionnellement si `modifiers.wager` présent.
    - CropCell injecte `<WagerReadyRing>` uniquement si double gate (mûr + cumul atteint).
    - Badge visible dès stage 0 (G6 honoré).
    - `paceLevel` calculé via `computePaceLevel` consommant `wager.totalDays` persisté (B2).
    - `tasksToday` lu directement depuis `wager.tasksCompletedToday` (B1).
    - **Zéro literal `7` dans le fichier** (grep `/ 7` et `\* 7` → 0 match sur bloc wager).
    - **Zéro commentaire "fallback 7 jours"** ou équivalent.
    - `daysBetween` importé depuis `wager-ui-helpers` (pas inline).
    - Fallback gracieux si `cumulTarget === 0` ou données manquantes.
    - Layout existant (whispers, stageRow) intact.
    - TS clean, zéro régression visuelle attendue sur plants non scellés.
  </done>
</task>

</tasks>

<verification>
**Code review :**
- Grep `PlantWagerBadge` dans WorldGridView.tsx → 1 import + 1 usage conditionnel.
- Grep `WagerReadyRing` → 1 import + 1 usage conditionnel.
- Grep `useSharedValue` dans PlantWagerBadge.tsx → 0 match (garantie zéro animation).
- Grep `computePaceLevel` → 1 appel dans useMemo CropCell.
- Grep `daysBetween` dans WorldGridView.tsx → 2 usages (paceLevel + tasksTargetToday), import depuis wager-ui-helpers.
- **Grep `/ 7` ou `\* 7` dans bloc wager CropCell → 0 match (B2 honoré)**.
- Grep `wager.totalDays` dans WorldGridView.tsx → ≥1 usage.
- Grep `wager.tasksCompletedToday` ou `wager?.tasksCompletedToday` → ≥1 usage.

**Smoke visuel (checkpoint implicite Plan 04)** :
- Plant non scellé → aucun badge ni anneau (pas de régression).
- Plant scellé stage 0 → badge 2 lignes visible, pas d'anneau.
- Plant scellé stage 4 cumul < target → badge 2 lignes visible, pas d'anneau.
- Plant scellé stage 4 cumul ≥ target → badge + anneau vert visibles.
- Ligne 1 badge (`X/Y aujourd'hui`) évolue quand user complète une tâche. Après changement de jour, reset à 1/Y.
</verification>

<success_criteria>
- [ ] 2 nouveaux composants livrés, pattern React.memo + useCallback.
- [ ] Badge **2 lignes** (SPOR-02 verrouillé CONTEXT.md D-03).
- [ ] Zéro hardcoded couleurs (tokens theme uniquement).
- [ ] Zéro animation continue sur badge.
- [ ] Anneau breathing très subtil + fallback reduceMotion.
- [ ] Double gate anneau vert respecté (SPOR-11).
- [ ] Badge visible dès plantation (G6).
- [ ] **Zéro magic number `7`** côté UI — `wager.totalDays` consommé directement (B2).
- [ ] **Zéro fallback ad-hoc pour tasksToday** — `wager.tasksCompletedToday` consommé directement (B1).
- [ ] TS clean, zéro régression layout plants non scellés.
</success_criteria>

<output>
Créer `.planning/phases/40-ui-spor-e-seed-picker-badge-validation/40-03-SUMMARY.md` :
- Confirmation badge 2 lignes livré (jamais dégradé à 1 ligne).
- Approche breathing finale (reanimated + useReducedMotion vs AccessibilityInfo).
- Tokens couleur exacts utilisés pour pace green/yellow/orange + ring success.
- Confirmation consommation directe `wager.totalDays` + `wager.tasksCompletedToday` (B2/B1).
</output>
</content>
</invoke>