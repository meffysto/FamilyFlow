---
phase: 40-ui-spor-e-seed-picker-badge-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - hooks/useFarm.ts
  - lib/mascot/sporee-economy.ts
  - lib/mascot/wager-ui-helpers.ts
  - lib/mascot/index.ts
  - lib/mascot/types.ts
  - lib/parser.ts
  - lib/types.ts
  - lib/vault-cache.ts
  - lib/__tests__/wager-ui-helpers.test.ts
  - lib/__tests__/sporee-economy.test.ts
  - hooks/useTasks.ts
  - contexts/VaultContext.tsx
autonomous: true
requirements: [MOD-03, SPOR-01, SPOR-07, SPOR-11]
must_haves:
  truths:
    - "User voit les cumul target/current d'un pari mis à jour quand il scelle une plantation et quand il complète une tâche du domaine Tasks"
    - "User voit `tasksCompletedToday` incrémenté à chaque tâche complétée, et reset à 0 (puis 1) au passage de jour (changement `lastDailyResetDate`)"
    - "User voit le cumul recompute au boot de l'app si le jour a changé (maybeRecompute), avec `wagerLastRecomputeDate` persisté dans farm-{profileId}.md frontmatter"
    - "User voit son inventaire Sporée décrémenté de 1 à la création d'un pari, et potentiellement ré-incrémenté de 1 à la récolte victorieuse (drop-back 15%)"
    - "User voit les helpers UI (durées × taille plant + pace level + totalDays) produire des valeurs déterministes cohérentes avec wager-engine"
    - "WagerModifier persiste `totalDays` au `startWager` — aucun consommateur UI ne recalcule cette valeur avec un magic number"
  artifacts:
    - path: "lib/mascot/wager-ui-helpers.ts"
      provides: "computeWagerDurations + computePaceLevel + computeWagerTotalDays + daysBetween (pures, testables)"
      exports: ["computeWagerDurations", "computePaceLevel", "computeWagerTotalDays", "daysBetween", "WagerDurationOption", "PaceLevel"]
    - path: "lib/mascot/sporee-economy.ts"
      provides: "rollWagerDropBack injectable"
      contains: "rollWagerDropBack"
    - path: "lib/mascot/types.ts"
      provides: "WagerModifier étendu avec tasksCompletedToday + lastDailyResetDate + totalDays (tous optionnels pour rétro-compat Phase 38/39)"
      contains: "tasksCompletedToday"
    - path: "lib/parser.ts"
      provides: "Round-trip WagerModifier avec nouveaux champs (encodage JSON existant préservé) + frontmatter wager_last_recompute_date"
      contains: "wager_last_recompute_date"
    - path: "lib/vault-cache.ts"
      provides: "CACHE_VERSION bumpé à 5 (shape WagerModifier + FarmProfileData.wagerLastRecomputeDate changent)"
      contains: "CACHE_VERSION = 5"
    - path: "hooks/useFarm.ts"
      provides: "startWager + incrementWagerCumul (reset quotidien) + harvest branche wager + bootstrap maybeRecompute"
      contains: "startWager"
    - path: "lib/__tests__/wager-ui-helpers.test.ts"
      provides: "Suite Jest helpers UI (durations + pace + totalDays + daysBetween)"
    - path: "hooks/useTasks.ts"
      provides: "Callback onTaskComplete event-driven (transition false→true)"
    - path: "contexts/VaultContext.tsx"
      provides: "Câblage onTaskComplete → incrementWagerCumul"
  key_links:
    - from: "hooks/useTasks.ts"
      to: "contexts/VaultContext.tsx"
      via: "callback onTaskComplete émis sur transition false→true (pattern onQuestProgress useFarm.ts:297)"
      pattern: "onTaskComplete"
    - from: "contexts/VaultContext.tsx"
      to: "hooks/useFarm.ts"
      via: "VaultContext relaie l'event au callback incrementWagerCumul exposé par useFarm"
      pattern: "incrementWagerCumul"
    - from: "hooks/useFarm.ts harvest"
      to: "lib/mascot/wager-engine.validateWagerOnHarvest"
      via: "import + appel après bloc golden"
      pattern: "validateWagerOnHarvest"
    - from: "hooks/useFarm.ts startWager"
      to: "lib/mascot/wager-engine.computeCumulTarget"
      via: "snapshot live tâches pending"
      pattern: "computeCumulTarget"
    - from: "hooks/useFarm.ts startWager"
      to: "lib/mascot/wager-ui-helpers.computeWagerTotalDays"
      via: "persistance totalDays sur WagerModifier au moment du sceau"
      pattern: "computeWagerTotalDays"
---

<objective>
Étendre `useFarm` avec les 3 callbacks Sporée (`startWager`, `incrementWagerCumul`, extension `harvest`), ajouter le helper pur `rollWagerDropBack` dans `sporee-economy.ts`, créer le module `wager-ui-helpers.ts` (durées × taille plant + pace level + `computeWagerTotalDays` + `daysBetween`) + sa suite Jest, étendre le shape `WagerModifier` avec `tasksCompletedToday` / `lastDailyResetDate` / `totalDays` (tous optionnels, rétro-compat), persister `wagerLastRecomputeDate` dans `FarmProfileData` (frontmatter `wager_last_recompute_date`), bumper `CACHE_VERSION` à 5, et câbler le callback `onTaskComplete` event-driven depuis `useTasks` vers `incrementWagerCumul` (via VaultContext, pattern `onQuestProgress`).

Bootstrap `maybeRecompute` dans un `useEffect` d'init de `useFarm` (boot) pour garantir que les badges lisent un `cumulTarget` frais dès le premier render.

Purpose : fondation data/hook de toute la Phase 40 — aucun plan UI suivant ne peut fonctionner sans ces primitives ni ces nouveaux champs persistés (B1 badge 2-lignes + B2 élimination magic number 7).

Output : hook useFarm étendu + moteur sporee-economy enrichi + module wager-ui-helpers + types/parser étendus + cache bumpé + 2 suites Jest enrichies + câblage task→wager event-driven.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-ui-spor-e-seed-picker-badge-validation/40-CONTEXT.md
@.planning/phases/40-ui-spor-e-seed-picker-badge-validation/40-RESEARCH.md
@.planning/phases/39-moteur-prorata-calcul-famille/39-02-SUMMARY.md
@.planning/phases/38-fondation-modifiers-conomie-spor-e/38-03-SUMMARY.md

@hooks/useFarm.ts
@hooks/useTasks.ts
@contexts/VaultContext.tsx
@lib/mascot/sporee-economy.ts
@lib/mascot/wager-engine.ts
@lib/mascot/types.ts
@lib/mascot/farm-engine.ts
@lib/parser.ts
@lib/types.ts
@lib/vault-cache.ts

<interfaces>
From lib/mascot/wager-engine.ts (Phase 39):
```typescript
export function computeCumulTarget(opts: {
  sealerProfileId: string;
  allProfiles: Profile[];
  tasks: Task[];
  today: string;
  pendingCount: number;
}): FamilyWeightResult;

export function canSealWager(opts: {
  sealerProfileId: string;
  allProfiles: Profile[];
  today: string;
}): CanSealResult;

export function maybeRecompute(opts: {
  now: Date;
  lastRecomputeDate: string;
  snapshot: FamilySnapshot | null;
  sealerProfileId: string;
  allProfiles: Profile[];
  tasks: Task[];
}): MaybeRecomputeResult;

export function validateWagerOnHarvest(
  cumulCurrent: number,
  cumulTarget: number
): WagerHarvestResult;

export function filterTasksForWager(tasks: Task[]): Task[];
```

From lib/mascot/sporee-economy.ts (Phase 38):
```typescript
export function tryIncrementSporeeCount(
  current: number,
  delta: number
): { accepted: boolean; newCount: number };
// Cap 10, refus pur au-delà.
```

From lib/mascot/types.ts (AVANT Phase 40) :
```typescript
export type WagerDuration = 'chill' | 'engage' | 'sprint';
export type WagerMultiplier = 1.3 | 1.7 | 2.5;
export interface WagerModifier {
  sporeeId: string;
  duration: WagerDuration;
  multiplier: WagerMultiplier;
  appliedAt: string;           // ISO YYYY-MM-DD local
  sealerProfileId: string;
  cumulTarget?: number;        // Phase 39
  cumulCurrent?: number;       // Phase 39
}
```

From lib/mascot/farm-engine.ts :
```typescript
// encodeModifiers / decodeModifiers : JSON.stringify avec ',' → '|' et ':' → '§'.
// Nouveaux champs optionnels du WagerModifier round-trippent automatiquement — pas de modification encodage nécessaire.
```

From lib/vault-cache.ts :
```typescript
const CACHE_VERSION = 4; // Phase 38
const CACHE_FILE_URI = FileSystem.documentDirectory + 'vault-cache-v4.json';
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 : Helpers purs wager-ui-helpers.ts (durées + pace + totalDays + daysBetween) + rollWagerDropBack + suites Jest</name>
  <files>lib/mascot/wager-ui-helpers.ts, lib/mascot/sporee-economy.ts, lib/mascot/index.ts, lib/__tests__/wager-ui-helpers.test.ts, lib/__tests__/sporee-economy.test.ts</files>
  <behavior>
    **computeWagerDurations(tasksPerStage, computeCumulTargetFn, ctx) → WagerDurationOption[]**
    - Retourne toujours exactement 3 options ordonnées : chill (×1.3, facteur 1.0), engage (×1.7, facteur 0.7), sprint (×2.5, facteur 0.5).
    - absoluteTasks = `Math.max(1, Math.ceil(tasksPerStage * 4 * facteur))`.
    - estimatedHours = `absoluteTasks * 6` (projection UI, 6h/tâche).
    - targetTasks vient de `computeCumulTargetFn(ctx).cumulTarget` (consommé depuis wager-engine, pas recalculé).
    - Tests : 3 options pour tasksPerStage=1 (petit plant) et tasksPerStage=6 (plant géant), ordre stable chill/engage/sprint, absoluteTasks ≥ 1 même avec tasksPerStage=0 (garde-fou), multipliers strictement 1.3/1.7/2.5.

    **computePaceLevel(cumulCurrent, cumulTarget, daysElapsed, totalDays) → 'green' | 'yellow' | 'orange'**
    - ratio = (cumulCurrent / max(1, cumulTarget)) / max(0.01, daysElapsed / max(1, totalDays)).
    - ratio ≥ 1.0 → 'green'.
    - ratio ≥ 0.7 → 'yellow'.
    - ratio < 0.7 → 'orange'.
    - Tests : pace pile 100% → green, pace 70% → yellow, pace 69% → orange, cumulTarget=0 → green (pari auto-gagné, fallback D-04), daysElapsed=0 → green (premier jour jamais punitif).

    **computeWagerTotalDays(duration: WagerDuration, tasksPerStage: number): number** (NOUVEAU — B2)
    - Constante module `DURATION_FACTORS = { chill: 1.0, engage: 0.7, sprint: 0.5 } as const`.
    - `absoluteTasks = Math.max(1, Math.ceil(tasksPerStage * 4 * DURATION_FACTORS[duration]))`.
    - `estimatedHours = absoluteTasks * 6`.
    - `totalDays = Math.max(1, Math.ceil(estimatedHours / 24))`.
    - Tests : chill/tasksPerStage=3 → cohérent (≥4 jours), sprint/tasksPerStage=1 → ≥1, garde-fou tasksPerStage=0 → 1.

    **daysBetween(startISO: string, endISO: string): number** (NOUVEAU — W4/I4, déclaré ici)
    - Compare 2 dates locales ISO (`YYYY-MM-DD`), retourne nombre de jours (Math.max(0, …)).
    - Tests : `daysBetween('2026-04-18', '2026-04-18')` → 0, `'2026-04-18' → '2026-04-19'` → 1, ordre inversé → 0 (pas de négatif).

    **rollWagerDropBack(random?: () => number): boolean** dans sporee-economy.ts
    - Constante `DROP_BACK_CHANCE = 0.15`.
    - Retourne `random() < 0.15`, default `Math.random`.
    - Tests : injection `() => 0.1` → true, `() => 0.15` → false (strictement <), `() => 0.5` → false.
  </behavior>
  <action>
    1. Créer `lib/mascot/wager-ui-helpers.ts` :
       - Export type `PaceLevel = 'green' | 'yellow' | 'orange'`.
       - Export type `WagerDurationOption = { duration: WagerDuration; multiplier: WagerMultiplier; targetTasks: number; absoluteTasks: number; estimatedHours: number }`.
       - Constante module `DURATION_FACTORS = { chill: 1.0, engage: 0.7, sprint: 0.5 } as const` (partagée par `computeWagerDurations` et `computeWagerTotalDays`).
       - Constante module `MULTIPLIERS = { chill: 1.3, engage: 1.7, sprint: 2.5 } as const`.
       - `computeWagerDurations` prend un paramètre `computeCumulTarget` (callback injecté) pour rester pur-testable sans dépendre de wager-engine à l'import — le hook injectera `computeCumulTarget` de wager-engine au moment de l'appel.
       - `computePaceLevel` pure, zéro I/O.
       - `computeWagerTotalDays(duration, tasksPerStage)` pure — **source unique de vérité** pour le mapping durée → totalDays (B2).
       - `daysBetween(startISO, endISO)` pure — exploité par PlantWagerBadge (Plan 03).

    2. Ajouter `rollWagerDropBack` dans `lib/mascot/sporee-economy.ts` — exporté après `rollSporeeDropOnExpedition` (pattern cohérent avec injection random).

    3. Étendre barrel `lib/mascot/index.ts` :
       ```typescript
       export * from './wager-ui-helpers';
       export { rollWagerDropBack } from './sporee-economy';
       ```
       (garder exports existants inchangés)

    4. Créer `lib/__tests__/wager-ui-helpers.test.ts` avec 4 describe blocs (computeWagerDurations, computePaceLevel, computeWagerTotalDays, daysBetween), ≥14 tests total couvrant les cas ci-dessus.

    5. Étendre `lib/__tests__/sporee-economy.test.ts` : ajouter describe `rollWagerDropBack` (3 tests injection random).

    Convention CLAUDE.md : fonctions en français dans les commentaires, noms d'API en anglais (pattern Phase 38/39). Zéro nouvelle dépendance npm.
  </action>
  <verify>
    <automated>npx jest --no-coverage lib/__tests__/wager-ui-helpers.test.ts lib/__tests__/sporee-economy.test.ts</automated>
  </verify>
  <done>
    - `wager-ui-helpers.ts` existe avec 4 fonctions pures + 2 types exportés.
    - `rollWagerDropBack` existe dans `sporee-economy.ts` avec injection random.
    - Barrel `lib/mascot/index.ts` exporte les nouveaux symboles.
    - Suites Jest ≥14 tests helpers + 3 tests drop-back, 100% vertes.
    - `npx tsc --noEmit` clean hors pré-existant.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2 : Extension types + parser + cache + useFarm + câblage onTaskComplete event-driven</name>
  <files>lib/mascot/types.ts, lib/parser.ts, lib/types.ts, lib/vault-cache.ts, hooks/useFarm.ts, hooks/useTasks.ts, contexts/VaultContext.tsx</files>
  <action>
    ## Sous-étape 0 — Investigation obligatoire (NE PAS ÉCRIRE DE CODE AVANT)

    **But** : décider la stratégie de câblage `onTaskComplete → incrementWagerCumul` et la consigner en commentaire decision dans `contexts/VaultContext.tsx` avant toute autre modification.

    Lectures obligatoires en amont :
    1. `hooks/useTasks.ts` en entier — localiser la fonction `toggleTask` / `completeTask` (détection transition `completed: false → true`).
    2. `contexts/VaultContext.tsx` — identifier comment les hooks de domaine sont assemblés et comment les callbacks cross-domaine sont actuellement câblés.
    3. `hooks/useFarm.ts` autour de la ligne ~297 — relire le pattern `onQuestProgress` (callback exposé + consommateur) qui sert de référence pour Phase 40.

    Décision à consigner (commentaire FR au-dessus du câblage dans `VaultContext.tsx`) :
    ```
    // Phase 40 — Décision : Option A (callback injecté via VaultContext, pattern onQuestProgress)
    //                      OU Option B (subscribe via ref partagée exposée par useFarm).
    // Retenu : {A|B} parce que {raison concrète observée dans le code}.
    ```

    **Si Option A impraticable** (ex: cycle de dépendance useTasks↔useFarm via VaultContext) : fallback documenté sur scan boot (tracking `tasksCompletedToday` reconstruit au boot depuis snapshot du jour). Ne pas livrer sans câblage fonctionnel.

    ## A. Extension `lib/mascot/types.ts` — shape WagerModifier (B1 + B2)

    ```typescript
    export interface WagerModifier {
      sporeeId: string;
      duration: WagerDuration;
      multiplier: WagerMultiplier;
      appliedAt: string;              // ISO YYYY-MM-DD local
      sealerProfileId: string;
      cumulTarget?: number;           // Phase 39
      cumulCurrent?: number;          // Phase 39

      // Phase 40 — B1 badge 2-lignes (X/Y aujourd'hui)
      tasksCompletedToday?: number;   // compteur du jour courant
      lastDailyResetDate?: string;    // ISO YYYY-MM-DD — date du dernier reset de tasksCompletedToday

      // Phase 40 — B2 totalDays persisté (élimine magic number côté UI)
      totalDays?: number;             // calculé via computeWagerTotalDays(duration, tasksPerStage) au startWager
    }
    ```

    Tous les nouveaux champs sont **optionnels** pour garder la rétro-compatibilité avec les WagerModifier sérialisés en Phase 38/39. Les consommateurs (Plan 03) fallback `?? 0` et `?? 1` uniquement quand le champ est absent pour un pari historique.

    ## B. Extension `lib/types.ts` — `FarmProfileData`

    Ajouter le champ optionnel :
    ```typescript
    export interface FarmProfileData {
      // ... champs existants
      wagerLastRecomputeDate?: string; // Phase 40 — ISO YYYY-MM-DD, dernier passage de maybeRecompute pour ce profil
    }
    ```

    ## C. Extension `lib/parser.ts` — parseFarmProfile + serializeFarmProfile

    Dans `parseFarmProfile` (retour à la fin) :
    ```typescript
    wagerLastRecomputeDate: props.wager_last_recompute_date || undefined,
    ```

    Dans `serializeFarmProfile` (pattern `if (data.X) lines.push(...)`) :
    ```typescript
    if (data.wagerLastRecomputeDate) lines.push(`wager_last_recompute_date: ${data.wagerLastRecomputeDate}`);
    ```

    **Note** : les nouveaux champs `tasksCompletedToday` / `lastDailyResetDate` / `totalDays` du `WagerModifier` sont encodés dans le JSON des modifiers (`encodeModifiers` farm-engine.ts:228) — round-trip automatique, **aucune modification parser/encoder nécessaire**. Vérifier par un test manuel de round-trip après implémentation (sérialiser un FarmCrop avec wager complet, désérialiser, assert shape identique).

    ## D. Bump `lib/vault-cache.ts` — CACHE_VERSION

    ```typescript
    // v5 : Phase 40 — shape WagerModifier étendu (tasksCompletedToday, lastDailyResetDate, totalDays)
    //               + FarmProfileData.wagerLastRecomputeDate frontmatter
    const CACHE_VERSION = 5;
    const CACHE_FILE_URI = FileSystem.documentDirectory + 'vault-cache-v5.json';
    ```

    ## E. Extension `hooks/useFarm.ts`

    1. Imports :
       ```typescript
       import {
         computeCumulTarget,
         validateWagerOnHarvest,
         filterTasksForWager,
         maybeRecompute,
       } from '../lib/mascot/wager-engine';
       import { rollWagerDropBack, tryIncrementSporeeCount } from '../lib/mascot/sporee-economy';
       import { computeWagerTotalDays } from '../lib/mascot/wager-ui-helpers';
       import type { WagerDuration, WagerMultiplier, WagerModifier } from '../lib/mascot/types';
       ```
       (Ne pas dupliquer `tryIncrementSporeeCount` ou `useToast` si déjà importés Phase 38.)

    2. Constante module :
       ```typescript
       const WAGER_MULTIPLIERS: Record<WagerDuration, WagerMultiplier> = {
         chill: 1.3, engage: 1.7, sprint: 2.5,
       };
       ```

    3. **Nouveau callback `startWager(profileId, plotIndex, cropId, duration)`** :
       - Lire farm file via `parseFarmProfile`.
       - Guard `currentSporee < 1` → throw Error FR "Pas de Sporée disponible".
       - Planter via `plantCrop` (pattern existant).
       - `today = getLocalDateKey(new Date())`.
       - `cumulTarget = computeCumulTarget({ sealerProfileId, allProfiles: profiles, tasks: filterTasksForWager(allTasks), today, pendingCount: allTasks.filter(t => !t.completed).length }).cumulTarget`.
       - `tasksPerStage = CROP_CATALOG.find(c => c.id === cropId)!.tasksPerStage`.
       - `totalDays = computeWagerTotalDays(duration, tasksPerStage)` — **B2 : totalDays PERSISTÉ**, jamais recalculé côté UI avec magic number 7.
       - Construire `WagerModifier` :
         ```typescript
         const wager: WagerModifier = {
           sporeeId: `sporee-${Date.now()}`,
           duration,
           multiplier: WAGER_MULTIPLIERS[duration],
           appliedAt: today,
           sealerProfileId: profileId,
           cumulTarget,
           cumulCurrent: 0,
           tasksCompletedToday: 0,      // B1
           lastDailyResetDate: today,   // B1
           totalDays,                    // B2
         };
         ```
       - Assigner `newPlant.modifiers = { wager }`.
       - `farmData.sporeeCount = currentSporee - 1`.
       - **1 unique writeFile** (mutation in-place farmData + serializeFarmProfile).
       - `deductCoins(profileId, cost, '🌱 Graine scellée : ...')`.
       - `await refreshFarm(profileId)`.

    4. **Nouveau callback `incrementWagerCumul(profileId, taskSourceFile)`** — reset quotidien B1 :
       - Lire farm, parser crops.
       - Filtre source : early return si `filterTasksForWager([shimTask]).length === 0`.
       - `today = getLocalDateKey(new Date())`.
       - Boucle sur TOUS les crops dont `modifiers.wager?.sealerProfileId === profileId` :
         ```typescript
         const w = crop.modifiers.wager;
         // Reset quotidien B1
         if (w.lastDailyResetDate !== today) {
           w.tasksCompletedToday = 1;           // première tâche du jour
           w.lastDailyResetDate = today;
         } else {
           w.tasksCompletedToday = (w.tasksCompletedToday ?? 0) + 1;
         }
         w.cumulCurrent = (w.cumulCurrent ?? 0) + 1;
         ```
       - Si aucun wager actif → early return sans I/O.
       - Unique writeFile + refreshFarm.

    5. **Extension `harvest` branche wager** (après golden) :
       - `wager = crop.modifiers?.wager`.
       - Si présent : `validation = validateWagerOnHarvest(wager.cumulCurrent ?? 0, wager.cumulTarget ?? 0)`.
       - Si `validation.won` :
         - `finalQty = Math.round(finalQty * wager.multiplier)` (APRÈS golden, pitfall P3).
         - Roll `rollWagerDropBack()` → si true, `tryIncrementSporeeCount(profile.sporeeCount ?? 0, 1)` → accepted → `profile.sporeeCount = inc.newCount` + `dropBack = true`.
         - `showToast('Victoire ! +... 🍃 (×...)' + (dropBack ? ' · Sporée retrouvée 🎁' : ''), 'success')`.
       - Sinon : `showToast('Plant récolté · Sporée consommée', 'info')`.
       - Le modifier disparaît via `harvestCrop` existant (P5 vérifié).

    6. **Bootstrap `maybeRecompute`** dans `useFarm` :
       - `useEffect` deps `[profile?.id]` :
         - Pour chaque crop avec `modifiers.wager` du profil actif, appeler `maybeRecompute({ now: new Date(), lastRecomputeDate: farmData.wagerLastRecomputeDate ?? '', snapshot: null, sealerProfileId: wager.sealerProfileId, allProfiles: profiles, tasks: filterTasksForWager(allTasks) })`.
         - Si `recomputed: true` → persister `wager.cumulTarget = result.cumulTarget` + `farmData.wagerLastRecomputeDate = getLocalDateKey(new Date())` en 1 writeFile (évite badge 0/0 au premier boot, P2).
       - **W3 résolu** : `wagerLastRecomputeDate` PERSISTÉ dans `farm-{profileId}.md` frontmatter (pas SecureStore, pas ref memory-only) → cohérent pattern vault-first, durable entre sessions, visible dans parser/serializer.

    7. Exposer `startWager` et `incrementWagerCumul` dans le return de `useFarm`.

    ## F. `hooks/useTasks.ts` — émission event `onTaskComplete`

    1. Dans la fonction `toggleTask` / `completeTask`, détecter la transition `completed: false → true` après persistence vault succès.
    2. Appeler `onTaskComplete?.(profileId, taskSourceFile)` — callback optionnel passé via props/context.
    3. Ne PAS émettre sur transition `true → false` (un-check d'une tâche ne doit pas incrémenter).

    ## G. `contexts/VaultContext.tsx` — câblage cross-domaine

    Selon décision Option A/B consignée en sous-étape 0 :
    - **Option A** (pattern `onQuestProgress`) : passer `incrementWagerCumul` (exposé par `useFarm`) comme prop/param à `useTasks` au moment de l'assemblage du contexte.
    - **Option B** : subscribe pattern avec ref partagée.

    Consigner la décision en commentaire FR au-dessus du câblage.

    **Privacy** : commentaires FR, `__DEV__` guards pour console.warn, pas de noms réels.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx jest --no-coverage lib/__tests__/wager-engine.test.ts lib/__tests__/sporee-economy.test.ts lib/__tests__/wager-ui-helpers.test.ts</automated>
  </verify>
  <done>
    - `WagerModifier` étendu avec `tasksCompletedToday` / `lastDailyResetDate` / `totalDays` (optionnels).
    - `FarmProfileData.wagerLastRecomputeDate` ajouté + parser/serializer couvrent le frontmatter `wager_last_recompute_date`.
    - `CACHE_VERSION` bumpé à 5 + nouveau URI cache.
    - `useFarm.startWager` exposé, consomme 1 Sporée + set modifier COMPLET (incluant totalDays + tasksCompletedToday=0 + lastDailyResetDate=today) + 1 writeFile.
    - `useFarm.incrementWagerCumul` gère reset quotidien B1 (détection changement jour → reset à 1, sinon +1) + cumulCurrent +1.
    - `useFarm.harvest` applique multiplier après golden + toast victoire/défaite + drop-back 15% inline.
    - `useEffect` init appelle `maybeRecompute` au boot, persiste `wagerLastRecomputeDate` (W3 résolu).
    - `useTasks` émet `onTaskComplete` event-driven (transition false→true uniquement).
    - VaultContext câble `incrementWagerCumul` → `useTasks.onTaskComplete` (Option A ou B, décision consignée).
    - `npx tsc --noEmit` clean hors pré-existant (MemoryEditor, cooklang, useVault.ts).
    - Aucune régression suite Jest Phase 38/39 (sporee-economy + wager-engine verts).
  </done>
</task>

</tasks>

<verification>
**Code review :**
- Grep `startWager` dans useFarm.ts → 1 déclaration + return.
- Grep `incrementWagerCumul` → 1 déclaration + 1 usage VaultContext.
- Grep `validateWagerOnHarvest` dans useFarm.ts → 1 appel dans `harvest`.
- Grep `maybeRecompute` dans useFarm.ts → 1 appel dans useEffect boot.
- Grep `rollWagerDropBack` dans useFarm.ts → 1 appel dans branche won.
- Grep `computeWagerTotalDays` dans useFarm.ts → 1 appel dans `startWager` (totalDays persisté).
- Grep `tasksCompletedToday` dans useFarm.ts → initialisé dans startWager + reset logic dans incrementWagerCumul.
- Grep `wager_last_recompute_date` dans parser.ts → 1 parse + 1 serialize.
- `CACHE_VERSION = 5` dans vault-cache.ts (ligne ~46).
- `Math.round(finalQty * wager.multiplier)` après bloc golden (P3).

**Round-trip manuel (non bloquant)** : serialize un FarmCrop avec wager complet → parse → assert shape identique sur nouveaux champs.
</verification>

<success_criteria>
- [ ] Types `WagerModifier` étendus (tasksCompletedToday + lastDailyResetDate + totalDays) + FarmProfileData.wagerLastRecomputeDate.
- [ ] Parser/serializer parse/serialize `wager_last_recompute_date` frontmatter (round-trip OK).
- [ ] `CACHE_VERSION = 5` + nouveau URI cache.
- [ ] 2 nouveaux callbacks `useFarm` (startWager, incrementWagerCumul) + harvest étendu + bootstrap useEffect.
- [ ] `rollWagerDropBack` + `wager-ui-helpers` livrés, ≥17 tests Jest verts (14 helpers + 3 drop-back).
- [ ] Câblage event-driven `useTasks.onTaskComplete → useFarm.incrementWagerCumul` avec décision A/B consignée.
- [ ] Zéro régression TS/Jest (baseline Phase 38/39 préservé).
- [ ] Zéro nouvelle dépendance npm.
- [ ] Commentaires FR, `__DEV__` guards respectés.
</success_criteria>

<output>
Créer `.planning/phases/40-ui-spor-e-seed-picker-badge-validation/40-01-SUMMARY.md` documentant :
- Exports finaux de `wager-ui-helpers.ts` (4 fonctions + 2 types) + signature `rollWagerDropBack`.
- Shape final `WagerModifier` (champs Phase 40 ajoutés) + `FarmProfileData.wagerLastRecomputeDate`.
- Signature finale `startWager` / `incrementWagerCumul` (pour consommation Plan 02/03).
- Stratégie câblage `onTaskComplete` retenue (Option A/B + raison).
- Bump cache version confirmé (v4 → v5).
- Éventuels écarts vs RESEARCH.
</output>
</content>
</invoke>