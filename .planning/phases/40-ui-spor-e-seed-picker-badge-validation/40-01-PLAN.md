---
phase: 40-ui-spor-e-seed-picker-badge-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - hooks/useFarm.ts
  - lib/mascot/sporee-economy.ts
  - lib/mascot/wager-ui-helpers.ts
  - lib/mascot/index.ts
  - lib/__tests__/wager-ui-helpers.test.ts
  - lib/__tests__/sporee-economy.test.ts
  - hooks/useTasks.ts
autonomous: true
requirements: [MOD-03, SPOR-01, SPOR-07, SPOR-11]
must_haves:
  truths:
    - "User voit les cumul target/current d'un pari mis à jour quand il scelle une plantation et quand il complète une tâche du domaine Tasks"
    - "User voit le cumul recompute au boot de l'app si le jour a changé (maybeRecompute)"
    - "User voit son inventaire Sporée décrémenté de 1 à la création d'un pari, et potentiellement ré-incrémenté de 1 à la récolte victorieuse (drop-back 15%)"
    - "User voit les helpers UI (durées × taille plant + pace level) produire des valeurs déterministes cohérentes avec wager-engine"
  artifacts:
    - path: "lib/mascot/wager-ui-helpers.ts"
      provides: "computeWagerDurations + computePaceLevel (pures, testables)"
      exports: ["computeWagerDurations", "computePaceLevel", "WagerDurationOption", "PaceLevel"]
    - path: "lib/mascot/sporee-economy.ts"
      provides: "rollWagerDropBack injectable"
      contains: "rollWagerDropBack"
    - path: "hooks/useFarm.ts"
      provides: "startWager + incrementWagerCumul + harvest branche wager + bootstrap maybeRecompute"
      contains: "startWager"
    - path: "lib/__tests__/wager-ui-helpers.test.ts"
      provides: "Suite Jest helpers UI"
    - path: "hooks/useTasks.ts"
      provides: "Callback onTaskComplete event-driven (transition false→true)"
  key_links:
    - from: "hooks/useTasks.ts"
      to: "hooks/useFarm.incrementWagerCumul"
      via: "callback onTaskComplete exposé via VaultContext"
      pattern: "onTaskComplete.*incrementWagerCumul"
    - from: "hooks/useFarm.ts harvest"
      to: "lib/mascot/wager-engine.validateWagerOnHarvest"
      via: "import + appel après bloc golden"
      pattern: "validateWagerOnHarvest"
    - from: "hooks/useFarm.ts startWager"
      to: "lib/mascot/wager-engine.computeCumulTarget"
      via: "snapshot live tâches pending"
      pattern: "computeCumulTarget"
---

<objective>
Étendre `useFarm` avec les 3 callbacks Sporée (`startWager`, `incrementWagerCumul`, extension `harvest`), ajouter le helper pur `rollWagerDropBack` dans `sporee-economy.ts`, créer le module `wager-ui-helpers.ts` (durées × taille plant + pace level) + sa suite Jest, et câbler le callback `onTaskComplete` event-driven depuis `useTasks` vers `incrementWagerCumul` pour tous les wagers actifs du sealeur.

Bootstrap `maybeRecompute` dans un `useEffect` d'init de `useFarm` (boot) pour garantir que les badges lisent un `cumulTarget` frais dès le premier render.

Purpose : fondation data/hook de toute la Phase 40 — aucun plan UI suivant ne peut fonctionner sans ces primitives. Zéro I/O spéculatif, mutation in-place + unique writeFile.

Output : hook useFarm étendu + moteur sporee-economy enrichi + module wager-ui-helpers + 2 suites Jest enrichies + câblage task→wager event-driven.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-ui-spor-e-seed-picker-badge-validation/40-CONTEXT.md
@.planning/phases/40-ui-spor-e-seed-picker-badge-validation/40-RESEARCH.md
@.planning/phases/39-moteur-prorata-calcul-famille/39-02-SUMMARY.md
@.planning/phases/38-fondation-modifiers-conomie-spor-e/38-03-SUMMARY.md

@hooks/useFarm.ts
@lib/mascot/sporee-economy.ts
@lib/mascot/wager-engine.ts
@lib/mascot/types.ts
@hooks/useTasks.ts
@contexts/VaultContext.tsx

<interfaces>
From lib/mascot/wager-engine.ts (Phase 39):
```typescript
export function computeCumulTarget(opts: {
  sealerProfileId: string;
  allProfiles: Profile[];
  tasks: Task[];
  today: string;
  pendingCount: number;
}): FamilyWeightResult;

export function canSealWager(opts: {
  sealerProfileId: string;
  allProfiles: Profile[];
  today: string;
}): CanSealResult;

export function maybeRecompute(opts: {
  now: Date;
  lastRecomputeDate: string;
  snapshot: FamilySnapshot | null;
  sealerProfileId: string;
  allProfiles: Profile[];
  tasks: Task[];
}): MaybeRecomputeResult;

export function validateWagerOnHarvest(
  cumulCurrent: number,
  cumulTarget: number
): WagerHarvestResult;

export function filterTasksForWager(tasks: Task[]): Task[];
```

From lib/mascot/sporee-economy.ts (Phase 38):
```typescript
export function tryIncrementSporeeCount(
  current: number,
  delta: number
): { accepted: boolean; newCount: number };
// Cap 10, refus pur au-delà.
```

From lib/mascot/types.ts:
```typescript
export type WagerDuration = 'chill' | 'engage' | 'sprint';
export type WagerMultiplier = 1.3 | 1.7 | 2.5;
export interface WagerModifier {
  sporeeId: string;
  duration: WagerDuration;
  multiplier: WagerMultiplier;
  appliedAt: string;        // YYYY-MM-DD local
  sealerProfileId: string;
  cumulTarget: number;
  cumulCurrent: number;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 : Helper pur wager-ui-helpers.ts + rollWagerDropBack + suites Jest</name>
  <files>lib/mascot/wager-ui-helpers.ts, lib/mascot/sporee-economy.ts, lib/mascot/index.ts, lib/__tests__/wager-ui-helpers.test.ts, lib/__tests__/sporee-economy.test.ts</files>
  <behavior>
    **computeWagerDurations(tasksPerStage, computeCumulTargetFn, ctx) → WagerDurationOption[]**
    - Retourne toujours exactement 3 options ordonnées : chill (×1.3, facteur 1.0), engage (×1.7, facteur 0.7), sprint (×2.5, facteur 0.5).
    - absoluteTasks = `Math.max(1, Math.ceil(tasksPerStage * 4 * facteur))`.
    - estimatedHours = `absoluteTasks * 6` (projection UI, 6h/tâche).
    - targetTasks vient de `computeCumulTargetFn(ctx).cumulTarget` (consommé depuis wager-engine, pas recalculé).
    - Tests : 3 options pour tasksPerStage=1 (petit plant) et tasksPerStage=6 (plant géant), ordre stable chill/engage/sprint, absoluteTasks ≥ 1 même avec tasksPerStage=0 (garde-fou), multipliers strictement 1.3/1.7/2.5.

    **computePaceLevel(cumulCurrent, cumulTarget, daysElapsed, totalDays) → 'green' | 'yellow' | 'orange'**
    - ratio = (cumulCurrent / max(1, cumulTarget)) / max(0.01, daysElapsed / max(1, totalDays)).
    - ratio ≥ 1.0 → 'green'.
    - ratio ≥ 0.7 → 'yellow'.
    - ratio < 0.7 → 'orange'.
    - Tests : pace pile 100% → green, pace 70% → yellow, pace 69% → orange, cumulTarget=0 → green (pari auto-gagné, fallback D-04), daysElapsed=0 → green (premier jour jamais punitif).

    **rollWagerDropBack(random?: () => number): boolean** dans sporee-economy.ts
    - Constante `DROP_BACK_CHANCE = 0.15`.
    - Retourne `random() < 0.15`, default `Math.random`.
    - Tests : injection `() => 0.1` → true, `() => 0.15` → false (strictement <), `() => 0.5` → false.
  </behavior>
  <action>
    1. Créer `lib/mascot/wager-ui-helpers.ts` :
       - Export type `PaceLevel = 'green' | 'yellow' | 'orange'`.
       - Export type `WagerDurationOption = { duration: WagerDuration; multiplier: WagerMultiplier; targetTasks: number; absoluteTasks: number; estimatedHours: number }`.
       - Constante module `DURATION_FACTORS = { chill: 1.0, engage: 0.7, sprint: 0.5 } as const`.
       - Constante module `MULTIPLIERS = { chill: 1.3, engage: 1.7, sprint: 2.5 } as const`.
       - `computeWagerDurations` prend un paramètre `computeCumulTarget` (callback injecté) pour rester pur-testable sans dépendre de wager-engine à l'import — le hook injectera `computeCumulTarget` de wager-engine au moment de l'appel.
       - `computePaceLevel` pure, zéro I/O.

    2. Ajouter `rollWagerDropBack` dans `lib/mascot/sporee-economy.ts` — exporté après `rollSporeeDropOnExpedition` (pattern cohérent avec injection random).

    3. Étendre barrel `lib/mascot/index.ts` :
       ```typescript
       export * from './wager-ui-helpers';
       export { rollWagerDropBack } from './sporee-economy';
       ```
       (garder exports existants inchangés)

    4. Créer `lib/__tests__/wager-ui-helpers.test.ts` avec 2 describe blocs (computeWagerDurations, computePaceLevel), ≥10 tests total couvrant les cas ci-dessus.

    5. Étendre `lib/__tests__/sporee-economy.test.ts` : ajouter describe `rollWagerDropBack` (3 tests injection random).

    Convention CLAUDE.md : fonctions en français dans les commentaires, noms d'API en anglais (pattern Phase 38/39). Zéro nouvelle dépendance npm.
  </action>
  <verify>
    <automated>npx jest --no-coverage lib/__tests__/wager-ui-helpers.test.ts lib/__tests__/sporee-economy.test.ts</automated>
  </verify>
  <done>
    - `wager-ui-helpers.ts` existe avec 2 fonctions pures + 2 types exportés.
    - `rollWagerDropBack` existe dans `sporee-economy.ts` avec injection random.
    - Barrel `lib/mascot/index.ts` exporte les nouveaux symboles.
    - Suites Jest ≥10 tests + 3 tests drop-back, 100% vertes.
    - `npx tsc --noEmit` clean hors pré-existant.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2 : Extension useFarm — startWager + incrementWagerCumul + harvest branche wager + bootstrap maybeRecompute</name>
  <files>hooks/useFarm.ts, hooks/useTasks.ts</files>
  <action>
    **A. Dans `hooks/useFarm.ts` :**

    1. Imports à ajouter en haut :
       ```typescript
       import {
         computeCumulTarget,
         validateWagerOnHarvest,
         filterTasksForWager,
         maybeRecompute,
       } from '../lib/mascot/wager-engine';
       import { rollWagerDropBack, tryIncrementSporeeCount } from '../lib/mascot/sporee-economy';
       import type { WagerDuration, WagerMultiplier, WagerModifier } from '../lib/mascot/types';
       ```
       (Note : `tryIncrementSporeeCount` et `useToast` déjà importés depuis Phase 38 Plan 03 — vérifier et ne pas dupliquer.)

    2. Ajouter constante module :
       ```typescript
       const WAGER_MULTIPLIERS: Record<WagerDuration, WagerMultiplier> = {
         chill: 1.3, engage: 1.7, sprint: 2.5,
       };
       ```

    3. **Nouveau callback `startWager`** (après `plant`, avant `harvest`) :
       - Signature : `(profileId, plotIndex, cropId, duration: WagerDuration) => Promise<void>`.
       - Lire farm file via pattern Phase 38 (`parseFarmProfile`).
       - Guard `currentSporee < 1` → throw Error FR "Pas de Sporée disponible".
       - Planter via `plantCrop` (pattern existant de `plant`).
       - Calculer `cumulTarget` via `computeCumulTarget({ sealerProfileId, allProfiles: profiles, tasks: filterTasksForWager(allTasks), today: getLocalDateKey(new Date()), pendingCount: allTasks.filter(t => !t.completed).length })`.
       - Construire `WagerModifier` : sporeeId=`sporee-${Date.now()}`, duration, multiplier=WAGER_MULTIPLIERS[duration], appliedAt=today, sealerProfileId=profileId, cumulTarget, cumulCurrent=0.
       - Assigner `newPlant.modifiers = { wager }` avant sérialisation.
       - Consommer 1 Sporée : `farmData.sporeeCount = currentSporee - 1`.
       - Écrire vault en **1 seul writeFile** (mutation in-place farmData + serializeFarmProfile, pattern Phase 38 Plan 03).
       - Déduire coût graine via `deductCoins(profileId, CROP_CATALOG.find(...).cost, '🌱 Graine scellée : ...')`.
       - `await refreshFarm(profileId)`.
       - Deps useCallback : `[vault, profiles, allTasks, deductCoins, refreshFarm]` (allTasks exposé via VaultContext).

    4. **Nouveau callback `incrementWagerCumul`** :
       - Signature : `(profileId: string, taskSourceFile: string) => Promise<void>`.
       - Lire farm file, parser crops, **boucler sur TOUS les crops** dont `modifiers.wager?.sealerProfileId === profileId` (gotcha G2 — multi-wagers).
       - Pour chaque wager trouvé : `wager.cumulCurrent = (wager.cumulCurrent ?? 0) + 1`.
       - Filtre source : appeler uniquement si `filterTasksForWager([{ sourceFile: taskSourceFile } as Task]).length === 1` (passer un shim Task minimal pour réutiliser le filtre pur).
       - Écrire unique writeFile + refreshFarm.
       - Si aucun wager actif pour ce profil → early return sans I/O.

    5. **Extension `harvest` branche wager** (après le bloc golden, lignes ~330 existantes) :
       - Parser crop récolté, lire `modifiers.wager`.
       - Si présent : `validation = validateWagerOnHarvest(wager.cumulCurrent ?? 0, wager.cumulTarget ?? 0)`.
       - Si `validation.won` :
         - `finalQty = Math.round(finalQty * wager.multiplier)` (appliquer APRÈS golden, pitfall P3).
         - Roll `rollWagerDropBack()` → si true, `tryIncrementSporeeCount(profile.sporeeCount ?? 0, 1)` → si accepted, `profile.sporeeCount = inc.newCount` + `dropBack = true`.
         - `showToast(\`Victoire ! +\${finalQty} 🍃 (×\${wager.multiplier})\${dropBack ? ' · Sporée retrouvée 🎁' : ''}\`, 'success')`.
       - Si `!validation.won` : `showToast('Plant récolté · Sporée consommée', 'info')`.
       - Le `modifier.wager` disparaît naturellement avec le plant via `harvestCrop` existant (pitfall P5 vérifié).
       - Note FR commentaire : `// Sporée — validation pari + drop-back 15%`.

    6. **Bootstrap `maybeRecompute`** dans `useFarm` :
       - Ajouter `useEffect` d'init (deps `[profile?.id]`) qui, pour chaque crop avec `modifiers.wager` du profil actif :
         - Appelle `maybeRecompute({ now: new Date(), lastRecomputeDate: farmData.wagerLastRecomputeDate ?? '', snapshot: null /* Phase 40 n'utilise pas snapshot matinal, live only */, sealerProfileId: wager.sealerProfileId, allProfiles: profiles, tasks: filterTasksForWager(allTasks) })`.
         - Si `recomputed: true` → persister `wager.cumulTarget = result.cumulTarget` + `farmData.wagerLastRecomputeDate = newRecomputeDate` en 1 writeFile (évite pitfall P2 badge 0/0 au premier boot).
       - Note : schedule minuit non requis Phase 40 (boot recatch suffit, deferred).

    7. Exposer `startWager` et `incrementWagerCumul` dans le return de `useFarm`.

    **B. Dans `hooks/useTasks.ts` :**

    1. Inspecter la fonction de validation/toggle tâche existante (probablement `toggleTask` ou `completeTask`).
    2. Détecter la transition `completed: false → true` (pattern Pitfall P6).
    3. Après persistence vault succès, appeler `onTaskComplete?.(profileId, taskSourceFile)` (nouveau callback optionnel passé en prop par VaultContext, pattern `onQuestProgress` existant ligne ~297 useFarm.ts).
    4. **Câblage VaultContext** : passer `incrementWagerCumul` (exposé par useFarm) à `useTasks` via le même pattern que `onQuestProgress` (prop callback).

    **Si le pattern exact n'est pas évident** : ajouter un `TODO: wire onTaskComplete` explicite + fallback temporaire (scan au boot) pour ne pas bloquer Plan 02/03 — mais l'objectif est de câbler event-driven ici.

    Deps useCallback propres. Commentaires FR. `__DEV__` guard pour console.warn.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx jest --no-coverage lib/__tests__/wager-engine.test.ts lib/__tests__/sporee-economy.test.ts lib/__tests__/wager-ui-helpers.test.ts</automated>
  </verify>
  <done>
    - `useFarm.startWager` exposé, consomme 1 Sporée + set modifier + 1 writeFile.
    - `useFarm.incrementWagerCumul` boucle sur tous wagers actifs du sealeur.
    - `useFarm.harvest` applique multiplier après golden + toast victoire/défaite + drop-back 15% inline.
    - `useEffect` init appelle `maybeRecompute` au boot pour éviter badge 0/0.
    - `useTasks` déclenche `onTaskComplete` event-driven (transition false→true uniquement).
    - VaultContext câble `incrementWagerCumul` → `useTasks.onTaskComplete`.
    - `npx tsc --noEmit` clean hors pré-existant (MemoryEditor, cooklang, useVault.ts).
    - Aucune régression suite Jest Phase 38/39 (sporee-economy + wager-engine verts).
  </done>
</task>

</tasks>

<verification>
**Code review :**
- Grep `startWager` dans useFarm.ts → 1 déclaration + return.
- Grep `incrementWagerCumul` → 1 déclaration + 1 usage dans useTasks/VaultContext.
- Grep `validateWagerOnHarvest` dans useFarm.ts → 1 appel dans `harvest`.
- Grep `maybeRecompute` dans useFarm.ts → 1 appel dans useEffect boot.
- Grep `rollWagerDropBack` dans useFarm.ts → 1 appel dans branche won.
- `Math.round(finalQty * wager.multiplier)` après bloc golden (pitfall P3 honoré).

**Runtime smoke test (non bloquant, Plan 02 livre l'UI) :**
- `console.log` conditionnel `__DEV__` dans startWager pour vérifier sporeeCount décrémenté lors d'un test manuel Plan 02.
</verification>

<success_criteria>
- [ ] 2 nouveaux callbacks `useFarm` (startWager, incrementWagerCumul) + harvest étendu + bootstrap useEffect.
- [ ] `rollWagerDropBack` + `wager-ui-helpers` livrés, ≥13 tests Jest verts (10 helpers + 3 drop-back).
- [ ] Câblage event-driven `useTasks.onTaskComplete → useFarm.incrementWagerCumul`.
- [ ] Zéro régression TS/Jest (baseline Phase 38/39 préservé).
- [ ] Zéro nouvelle dépendance npm.
- [ ] Commentaires FR, `useThemeColors()` non applicable ici (data layer pur), `__DEV__` guards respectés.
</success_criteria>

<output>
Créer `.planning/phases/40-ui-spor-e-seed-picker-badge-validation/40-01-SUMMARY.md` documentant :
- Exports finaux de `wager-ui-helpers.ts` + signature `rollWagerDropBack`.
- Signature finale `startWager` / `incrementWagerCumul` (pour consommation Plan 02).
- Stratégie câblage `onTaskComplete` retenue (pattern `onQuestProgress` ou autre).
- Éventuels écarts vs RESEARCH (ex: si bootstrap `maybeRecompute` placé ailleurs qu'init useFarm).
</output>
