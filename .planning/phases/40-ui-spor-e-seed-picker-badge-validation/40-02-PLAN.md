---
phase: 40-ui-spor-e-seed-picker-badge-validation
plan: 02
type: execute
wave: 2
depends_on: [40-01]
files_modified:
  - components/mascot/WagerSealerSheet.tsx
  - app/(tabs)/tree.tsx
autonomous: true
requirements: [MOD-03, SPOR-01]
must_haves:
  truths:
    - "User qui choisit une graine avec ≥1 Sporée en stock voit apparaître un pageSheet secondaire 'Sceller cette plantation ?' proposant 3 durées (Chill ×1.3 / Engagé ×1.7 / Sprint ×2.5) + option 'Pas cette fois'"
    - "User voit pour chaque durée : multiplier, durée absolue (ex 24h), cumul cible prorata (ex '5/10 tâches'), cadence requise (ex '~3 tâches/jour')"
    - "User qui clique une durée → pari scellé, Sporée -1, plant planté avec modifier wager, pageSheet dismiss"
    - "User qui clique 'Pas cette fois' ou dismiss → plant planté SANS modifier (plantation normale), zéro Sporée consommée"
    - "User avec 0 Sporée OU canSealWager ok=false → pageSheet ne s'ouvre jamais, comportement plantation directe inchangé"
  artifacts:
    - path: "components/mascot/WagerSealerSheet.tsx"
      provides: "Modal pageSheet secondaire choix 3 durées + preview prorata"
      min_lines: 200
    - path: "app/(tabs)/tree.tsx"
      provides: "handleSeedSelect étendu + state showWagerSealer + pendingPlant"
  key_links:
    - from: "app/(tabs)/tree.tsx handleSeedSelect"
      to: "components/mascot/WagerSealerSheet"
      via: "state setShowWagerSealer + setTimeout(300ms) après close seed picker"
      pattern: "setShowWagerSealer.*setTimeout"
    - from: "components/mascot/WagerSealerSheet"
      to: "hooks/useFarm.startWager"
      via: "prop onConfirm(duration) consommé dans handler"
      pattern: "startWager\\(.*duration"
    - from: "components/mascot/WagerSealerSheet"
      to: "lib/mascot/wager-ui-helpers.computeWagerDurations"
      via: "useMemo au mount, consommé pour afficher 3 options"
      pattern: "computeWagerDurations"
---

<objective>
Créer le modal `WagerSealerSheet` (pageSheet secondaire empilé) qui apparaît après `handleSeedSelect` **uniquement si** `sporeeCount ≥ 1` ET `canSealWager().ok === true`. Le modal affiche 3 options de durée (Chill/Engagé/Sprint) avec preview prorata (multiplier, durée absolue, cadence requise, cumul cible), un bouton "Pas cette fois — planter normalement", et gère les 3 paths de sortie (confirm sceller / confirm skip / dismiss) pour garantir zéro plant fantôme (pitfall P1).

Purpose : livrer le flow UX décisionnel au cœur de la mécanique Sporée — c'est l'écran qui transforme la graine en pari stratégique.

Output : 1 nouveau composant + modifications localisées dans `tree.tsx` (handleSeedSelect + 2 state hooks).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/40-ui-spor-e-seed-picker-badge-validation/40-CONTEXT.md
@.planning/phases/40-ui-spor-e-seed-picker-badge-validation/40-RESEARCH.md
@.planning/phases/40-ui-spor-e-seed-picker-badge-validation/40-01-SUMMARY.md

@app/(tabs)/tree.tsx
@components/ui/ModalHeader.tsx
@components/ui/Button.tsx
@contexts/ThemeContext.tsx
@constants/colors.ts
@constants/spacing.ts
@constants/typography.ts

<interfaces>
From Plan 40-01 (lib/mascot/wager-ui-helpers.ts) :
```typescript
export type PaceLevel = 'green' | 'yellow' | 'orange';
export type WagerDurationOption = {
  duration: WagerDuration;
  multiplier: WagerMultiplier;
  targetTasks: number;
  absoluteTasks: number;
  estimatedHours: number;
};
export function computeWagerDurations(
  tasksPerStage: number,
  computeCumulTarget: (...) => FamilyWeightResult,
  ctx: { sealerProfileId, allProfiles, tasks, today, pendingCount }
): WagerDurationOption[];
```

From Plan 40-01 (hooks/useFarm.ts) :
```typescript
startWager(profileId: string, plotIndex: number, cropId: string, duration: WagerDuration): Promise<void>;
plant(profileId: string, plotIndex: number, cropId: string): Promise<void>; // existant
```

From lib/mascot/wager-engine (Phase 39) :
```typescript
canSealWager({ sealerProfileId, allProfiles, today }): CanSealResult;
```

Pattern pageSheet existant (tree.tsx lignes 1256-1258) :
```typescript
setShowSeedPicker(false);
setTimeout(() => setShowOther(true), 400); // delay anti-iOS collision
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1 : Composant WagerSealerSheet.tsx (pageSheet 3 options + preview prorata)</name>
  <files>components/mascot/WagerSealerSheet.tsx</files>
  <action>
    Créer le composant modal pageSheet autonome, testable en isolation.

    **Structure** :
    ```typescript
    interface WagerSealerSheetProps {
      visible: boolean;
      onClose: () => void;
      onConfirmSeal: (duration: WagerDuration) => Promise<void>;
      onConfirmSkip: () => Promise<void>;
      cropId: string;           // pour nom plant dans header
      tasksPerStage: number;    // pour computeWagerDurations
      sealerProfileId: string;  // pour computeCumulTarget
      allProfiles: Profile[];
      allTasks: Task[];
      sporeeCount: number;      // affiché dans header ("2 🍄")
    }
    ```

    **Layout** (ModalHeader + ScrollView) :
    - `<Modal presentationStyle="pageSheet" onRequestClose={onClose}>`.
    - `ModalHeader` title="Sceller cette plantation ?" + bouton close qui **déclenche onConfirmSkip** (P1 : dismiss = planter normalement).
    - Sous-titre explicatif FR : "Consomme 1 Sporée 🍄 · reward × multiplier si cumul atteint".
    - `useMemo` pour `durations = computeWagerDurations(tasksPerStage, computeCumulTarget, ctx)` au mount (stabilité).
    - 3 cartes durée empilées (pattern `<Pressable>` + `useThemeColors()` inline) :
      - **Ligne 1** : Nom mode + multiplier (ex "Chill ×1.3"), `FontSize.h3`, `FontWeight.semibold`.
      - **Ligne 2** : durée absolue · cadence requise · cumul cible (ex "24h · ~3 tâches/jour · 5/10"), `FontSize.caption`, `colors.textMuted`.
      - Calcul cadence : `Math.ceil(option.targetTasks / Math.max(1, option.estimatedHours / 24))`.
      - Emoji/pictogramme cohérent par mode (suggestion : 🌱 Chill / 🔥 Engagé / ⚡ Sprint — cosmétique, pas requis).
      - Pressable onPress → `await onConfirmSeal(option.duration)` puis `onClose()`.
      - Haptic feedback : `Haptics.impactAsync(Haptics.ImpactFeedbackStyle.Medium)` au press.
    - **Bouton "Pas cette fois — planter normalement"** en bas (variant secondary) → `await onConfirmSkip()` puis `onClose()`.

    **Tokens design obligatoires** :
    - Couleurs : `useThemeColors()` → `colors.surface`, `colors.surfaceRaised`, `colors.textPrimary`, `colors.textMuted`, `colors.primary`, `colors.border`.
    - Spacing : `Spacing.sm / Spacing.md / Spacing.lg / Spacing['2xl']`.
    - Radius : `Radius.md / Radius.lg`.
    - FontSize : `FontSize.h3 / FontSize.body / FontSize.caption`.
    - **Zéro hardcoded** — pas de `#FFFFFF`, pas de `16`, pas de `padding: 12`.

    **Styles** :
    - `StyleSheet.create({})` pour layout statique (padding, gaps, flex).
    - Couleurs inline dans JSX avec `useThemeColors()`.
    - Pattern cohérent avec `TreeShop.tsx` ou `CraftSheet.tsx` (composants mascot existants).

    **Accessibilité** :
    - `accessibilityRole="button"` sur les 3 Pressables + skip.
    - `accessibilityLabel` explicite FR (ex: "Sceller avec Chill, multiplicateur 1.3, durée 24 heures, cible 5 tâches sur 10").

    **Perf** :
    - `React.memo` sur le composant racine (props stables sauf `allTasks` — passer memoïsé depuis tree.tsx).
    - `useCallback` sur les 4 handlers (3 durées + skip).

    **Commentaires FR** au-dessus des blocs clés.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>
    - Fichier `WagerSealerSheet.tsx` existe, ~200-300 lignes.
    - Props typées strictement (WagerSealerSheetProps).
    - 3 options dynamiques calculées via `computeWagerDurations` (pas hardcodées).
    - Bouton skip présent + ModalHeader close → onConfirmSkip (P1).
    - Zéro couleur hardcoded, zéro magic number.
    - `React.memo` + `useCallback` appliqués.
    - Haptics au press confirm.
    - TypeScript clean hors pré-existant.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2 : Câblage tree.tsx — handleSeedSelect étendu + state sealer + stacking pageSheets</name>
  <files>app/(tabs)/tree.tsx</files>
  <action>
    Modifications localisées dans `tree.tsx` — ne pas refondre le seed picker existant.

    1. **Imports** :
       ```typescript
       import { WagerSealerSheet } from '../../components/mascot/WagerSealerSheet';
       import { canSealWager } from '../../lib/mascot/wager-engine';
       import { getLocalDateKey } from '../../lib/mascot/sporee-economy'; // ou location actuelle
       import type { WagerDuration } from '../../lib/mascot/types';
       ```

    2. **State local** (après `showSeedPicker` existant) :
       ```typescript
       const [showWagerSealer, setShowWagerSealer] = useState(false);
       const [pendingPlant, setPendingPlant] = useState<{
         plotIndex: number;
         cropId: string;
         tasksPerStage: number;
       } | null>(null);
       ```

    3. **Consommer `startWager`** depuis `useFarm()` (déjà exposé Plan 40-01).

    4. **Modifier `handleSeedSelect`** existant :
       - Après validation graine + lecture `sporeeCount = profile.sporeeCount ?? 0` :
       - Gate : si `sporeeCount >= 1` :
         - `const check = canSealWager({ sealerProfileId: profile.id, allProfiles: profiles, today: getLocalDateKey(new Date()) })`.
         - Si `check.ok === true` :
           - `setPendingPlant({ plotIndex: selectedPlotIndex, cropId, tasksPerStage: CROP_CATALOG.find(c => c.id === cropId)!.tasksPerStage })`.
           - `setShowSeedPicker(false)`.
           - `setTimeout(() => setShowWagerSealer(true), 300)` — gotcha G1 stacking pageSheets iOS.
           - `return` (suspendre plantation directe).
       - Sinon (zero Sporée OU canSealWager.ok=false) : plantation directe comportement actuel (pas de refonte).

    5. **Handlers pour WagerSealerSheet** :
       ```typescript
       const handleWagerConfirm = useCallback(async (duration: WagerDuration) => {
         if (!pendingPlant || !profile) return;
         try {
           await startWager(profile.id, pendingPlant.plotIndex, pendingPlant.cropId, duration);
         } catch (e) {
           Alert.alert('Erreur', e instanceof Error ? e.message : 'Impossible de sceller');
         } finally {
           setPendingPlant(null);
         }
       }, [pendingPlant, profile, startWager]);

       const handleWagerSkip = useCallback(async () => {
         if (!pendingPlant || !profile) return;
         try {
           await plant(profile.id, pendingPlant.plotIndex, pendingPlant.cropId);
         } catch (e) {
           Alert.alert('Erreur', e instanceof Error ? e.message : 'Impossible de planter');
         } finally {
           setPendingPlant(null);
         }
       }, [pendingPlant, profile, plant]);

       const handleWagerClose = useCallback(() => {
         setShowWagerSealer(false);
         // Note : si pendingPlant toujours présent après close (dismiss sans bouton),
         //       handleWagerSkip déjà consommé par ModalHeader close OU confirm déjà fait.
         //       Le useEffect cleanup ci-dessous sert de garde-fou.
       }, []);
       ```

    6. **Garde-fou anti-plant-fantôme (P1)** :
       ```typescript
       useEffect(() => {
         // Si sealer fermé mais pendingPlant orphelin → planter normalement
         if (!showWagerSealer && pendingPlant && profile) {
           const { plotIndex, cropId } = pendingPlant;
           plant(profile.id, plotIndex, cropId).catch(/* silent */);
           setPendingPlant(null);
         }
       }, [showWagerSealer, pendingPlant, profile, plant]);
       ```
       Alternative plus simple : s'assurer que `onConfirmSkip` est TOUJOURS appelé par ModalHeader close (Task 1) — dans ce cas ce useEffect devient inutile. **Retenir la plus simple qui fonctionne** — l'objectif est zéro plant fantôme.

    7. **Rendu WagerSealerSheet** (après le Modal seed picker existant) :
       ```tsx
       <WagerSealerSheet
         visible={showWagerSealer}
         onClose={handleWagerClose}
         onConfirmSeal={handleWagerConfirm}
         onConfirmSkip={handleWagerSkip}
         cropId={pendingPlant?.cropId ?? ''}
         tasksPerStage={pendingPlant?.tasksPerStage ?? 1}
         sealerProfileId={profile?.id ?? ''}
         allProfiles={profiles}
         allTasks={allTasks /* exposé via useVault() */}
         sporeeCount={profile?.sporeeCount ?? 0}
       />
       ```

    **Privacy** : commits FR, zéro nom réel. Commentaires FR.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>
    - `handleSeedSelect` étendu avec gate sporeeCount + canSealWager.
    - `setTimeout(300ms)` respecté pour stacking pageSheets (G1).
    - 3 handlers (confirm / skip / close) gérant tous les paths de sortie.
    - WagerSealerSheet rendu dans tree.tsx avec props correctes.
    - Garde-fou anti-plant-fantôme (P1) en place.
    - Comportement plantation normale inchangé si 0 Sporée.
    - TS clean hors pré-existant.
  </done>
</task>

</tasks>

<verification>
**Smoke manual (non bloquant, checkpoint humain implicite Plan 04)** :
- Avec 0 Sporée : seed picker → choix → plante direct (pas de sealer). OK.
- Avec ≥1 Sporée : seed picker → choix → sealer apparaît avec 3 options. OK.
- Click Chill → Sporée -1, plant avec wager modifier. OK.
- Click "Pas cette fois" → plant sans wager, Sporée inchangée. OK.
- Dismiss swipe down → plant sans wager (P1). OK.

**TS** : `npx tsc --noEmit` clean hors pré-existant.
</verification>

<success_criteria>
- [ ] WagerSealerSheet.tsx livré avec 3 options + skip + preview prorata dynamique.
- [ ] tree.tsx étendu avec gate + state + handlers + rendu.
- [ ] 3 paths sortie testés (confirm seal / confirm skip / dismiss).
- [ ] Zéro régression seed picker existant pour users sans Sporée.
- [ ] TS clean, zéro hardcoded couleurs/magic numbers.
- [ ] FR partout (UI + commentaires + toasts).
</success_criteria>

<output>
Créer `.planning/phases/40-ui-spor-e-seed-picker-badge-validation/40-02-SUMMARY.md` :
- Pattern stacking pageSheets finalisé (delay, gotchas iOS rencontrés).
- Handlers tree.tsx exposés pour doc future.
- Toute déviation vs RESEARCH (ex: handler skip via useEffect vs ModalHeader close).
</output>
