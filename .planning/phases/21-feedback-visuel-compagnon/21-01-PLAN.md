---
phase: 21-feedback-visuel-compagnon
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/semantic/effect-toasts.ts
  - lib/mascot/haptics.ts
  - components/mascot/HarvestBurst.tsx
autonomous: true
requirements: [FEEDBACK-01, FEEDBACK-02, FEEDBACK-03, FEEDBACK-05]

must_haves:
  truths:
    - "EFFECT_TOASTS dictionnaire contient 10 entrées avec icon, fr, en, subtitle_fr, subtitle_en, type"
    - "4 fonctions haptic exportées couvrent les 10 catégories par intensité"
    - "HarvestBurst accepte variant prop et adapte particules/couleur/durée"
  artifacts:
    - path: "lib/semantic/effect-toasts.ts"
      provides: "Dictionnaire EFFECT_TOASTS + mapping CATEGORY_VARIANT + mapping CATEGORY_HAPTIC_FN"
      exports: ["EFFECT_TOASTS", "EffectToastDef", "CATEGORY_VARIANT", "CATEGORY_HAPTIC_FN"]
    - path: "lib/mascot/haptics.ts"
      provides: "4 fonctions haptic par intensité"
      exports: ["hapticsEffectLight", "hapticsEffectMedium", "hapticsEffectStrong", "hapticsEffectGolden"]
    - path: "components/mascot/HarvestBurst.tsx"
      provides: "Composant particules avec variant golden/rare/ambient"
      exports: ["HarvestBurst", "HarvestBurstVariant", "VARIANT_CONFIG"]
  key_links:
    - from: "lib/semantic/effect-toasts.ts"
      to: "lib/semantic/categories.ts"
      via: "import type CategoryId"
      pattern: "Record<CategoryId"
    - from: "lib/semantic/effect-toasts.ts"
      to: "lib/mascot/haptics.ts"
      via: "CATEGORY_HAPTIC_FN mapping"
      pattern: "hapticsEffect"
---

<objective>
Créer les constantes et composants de feedback visuel/haptique pour les 10 effets sémantiques.

Purpose: Fournir les briques (dictionnaire toasts, haptic patterns, HarvestBurst variants) que le Plan 02 câblera dans completeTask.
Output: 3 fichiers (1 nouveau + 2 étendus) avec toutes les données de feedback prêtes à consommer.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-feedback-visuel-compagnon/21-CONTEXT.md
@.planning/phases/21-feedback-visuel-compagnon/21-RESEARCH.md
@.planning/phases/21-feedback-visuel-compagnon/21-UI-SPEC.md

@lib/semantic/categories.ts
@lib/semantic/effects.ts
@lib/mascot/haptics.ts
@components/mascot/HarvestBurst.tsx
@constants/spacing.ts
@constants/typography.ts

<interfaces>
From lib/semantic/categories.ts:
```typescript
export type CategoryId =
  | 'menage_quotidien' | 'menage_hebdo' | 'courses'
  | 'enfants_routines' | 'enfants_devoirs' | 'rendez_vous'
  | 'gratitude_famille' | 'budget_admin' | 'bebe_soins' | 'cuisine_repas';
```

From lib/mascot/haptics.ts:
```typescript
// Existing exports: hapticsTreeTap, hapticsSpeciesChange, hapticsShopBuy, hapticsShopError, hapticsEvolution, hapticsLootOpen, hapticsLootResult, hapticsStageScroll
// Internal helper: function delay(ms: number): Promise<void>
// Guard pattern: const isWeb = Platform.OS === 'web';
```

From components/mascot/HarvestBurst.tsx:
```typescript
interface HarvestBurstProps {
  x: number; y: number; reward: number; cropColor: string; onComplete: () => void;
}
// Module constants: PARTICLE_COUNT = 8, PARTICLE_SIZE = 6
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Créer lib/semantic/effect-toasts.ts + étendre haptics.ts</name>
  <files>lib/semantic/effect-toasts.ts, lib/mascot/haptics.ts, lib/semantic/index.ts</files>
  <read_first>
    - lib/semantic/categories.ts (CategoryId type — les 10 identifiants canoniques)
    - lib/mascot/haptics.ts (pattern existant : isWeb guard, delay() helper, async function, format des exports)
    - lib/semantic/index.ts (barrel — ajouter les nouveaux re-exports)
    - lib/semantic/effects.ts (EffectId type pour référence)
  </read_first>
  <action>
**Fichier 1 — lib/semantic/effect-toasts.ts (NOUVEAU)**

Créer un module pur (zéro import vault/hook) avec :

1. Interface `EffectToastDef` :
```typescript
export interface EffectToastDef {
  icon: string;
  fr: string;
  en: string;
  subtitle_fr: string;
  subtitle_en: string;
  type: 'success' | 'info';
}
```

2. Constante `EFFECT_TOASTS: Record<CategoryId, EffectToastDef>` — 10 entrées exactes (per D-01, UI-SPEC Toast Design Contract) :

| CategoryId | icon | fr | en | subtitle_fr | subtitle_en | type |
|---|---|---|---|---|---|---|
| menage_quotidien | 🌿 | Ménage : mauvaises herbes retirées ! | Housework: weeds removed! | Ferme plus propre | Cleaner farm | success |
| menage_hebdo | 🔧 | Grand ménage : usure réparée ! | Deep clean: wear repaired! | Ferme comme neuve | Farm like new | success |
| courses | 🚀 | Courses faites : turbo bâtiments 24h ! | Shopping done: building turbo 24h! | Production ×2 | Production ×2 | success |
| enfants_routines | 💛 | Routines enfants : compagnon heureux ! | Child routines: happy companion! | Humeur au max | Mood maxed | success |
| enfants_devoirs | 📚 | Devoirs : sprint de croissance activé ! | Homework: growth sprint active! | Stade plus vite | Faster growth | info |
| rendez_vous | 💎 | Rendez-vous : graine rare obtenue ! | Appointment: rare seed earned! | Récolte précieuse | Precious harvest | success |
| gratitude_famille | ✨ | Gratitude : trait de saga boosté ! | Gratitude: saga trait boosted! | Voyageur renforcé | Traveler empowered | info |
| budget_admin | 🏦 | Budget réglé : capacité ×2 pendant 24h ! | Budget done: capacity ×2 for 24h! | Bâtiments pleins vite | Buildings fill fast | success |
| bebe_soins | 🌟 | Soins bébé : récolte dorée ×3 ! | Baby care: golden harvest ×3! | Prochaine récolte épique | Next harvest epic | success |
| cuisine_repas | 🍳 | Repas préparé : recette rare débloquée ! | Meal done: rare recipe unlocked! | Nouveau craft disponible | New craft available | info |

3. Type et constante `HarvestBurstVariant` + mapping `CATEGORY_VARIANT: Record<CategoryId, HarvestBurstVariant>` :
```typescript
export type HarvestBurstVariant = 'ambient' | 'rare' | 'golden';
export const CATEGORY_VARIANT: Record<CategoryId, HarvestBurstVariant> = {
  bebe_soins: 'golden', rendez_vous: 'golden',
  cuisine_repas: 'rare', gratitude_famille: 'rare', budget_admin: 'rare',
  menage_quotidien: 'ambient', menage_hebdo: 'ambient', courses: 'ambient',
  enfants_routines: 'ambient', enfants_devoirs: 'ambient',
};
```

4. Mapping `CATEGORY_HAPTIC_FN: Record<CategoryId, () => void | Promise<void>>` importé depuis `../mascot/haptics` :
```typescript
import { hapticsEffectLight, hapticsEffectMedium, hapticsEffectStrong, hapticsEffectGolden } from '../mascot/haptics';
export const CATEGORY_HAPTIC_FN: Record<CategoryId, () => void | Promise<void>> = {
  menage_quotidien: hapticsEffectLight,
  menage_hebdo: hapticsEffectLight,
  courses: hapticsEffectMedium,
  enfants_routines: hapticsEffectMedium,
  enfants_devoirs: hapticsEffectMedium,
  budget_admin: hapticsEffectMedium,
  cuisine_repas: hapticsEffectStrong,
  gratitude_famille: hapticsEffectStrong,
  rendez_vous: hapticsEffectGolden,
  bebe_soins: hapticsEffectGolden,
};
```

**Fichier 2 — lib/mascot/haptics.ts (ÉTENDRE)**

Ajouter 4 fonctions APRÈS les exports existants, AVANT la fonction `delay()` interne (qui reste en bas du fichier). Chaque fonction suit le pattern existant exactement (`if (isWeb) return;` guard, `async` pour séquences, `delay()` pour espacement) :

```typescript
/** Effet léger — ménage quotidien et hebdomadaire */
export function hapticsEffectLight() {
  if (isWeb) return;
  Haptics.impactAsync(Haptics.ImpactFeedbackStyle.Light);
}

/** Effet modéré — courses, routines enfants, devoirs, budget */
export async function hapticsEffectMedium() {
  if (isWeb) return;
  Haptics.impactAsync(Haptics.ImpactFeedbackStyle.Light);
  await delay(80);
  Haptics.impactAsync(Haptics.ImpactFeedbackStyle.Medium);
}

/** Effet chaleureux — cuisine, gratitude */
export async function hapticsEffectStrong() {
  if (isWeb) return;
  Haptics.impactAsync(Haptics.ImpactFeedbackStyle.Medium);
  await delay(100);
  Haptics.impactAsync(Haptics.ImpactFeedbackStyle.Medium);
  await delay(80);
  Haptics.notificationAsync(Haptics.NotificationFeedbackType.Success);
}

/** Effet épique — soins bébé, rendez-vous (mirrors hapticsEvolution cadence) */
export async function hapticsEffectGolden() {
  if (isWeb) return;
  Haptics.impactAsync(Haptics.ImpactFeedbackStyle.Light);
  await delay(100);
  Haptics.impactAsync(Haptics.ImpactFeedbackStyle.Medium);
  await delay(80);
  Haptics.impactAsync(Haptics.ImpactFeedbackStyle.Heavy);
  await delay(150);
  Haptics.notificationAsync(Haptics.NotificationFeedbackType.Success);
}
```

IMPORTANT : NE PAS déplacer la fonction `delay()` — elle est utilisée par les fonctions existantes ET les nouvelles. Les 4 nouvelles fonctions doivent être placées AVANT `delay()` (avant la ligne `// ── Utilitaire ───────────────────────────────`).

**Fichier 3 — lib/semantic/index.ts (ÉTENDRE)**

Ajouter les re-exports suivants :
```typescript
export { EFFECT_TOASTS, CATEGORY_VARIANT, CATEGORY_HAPTIC_FN } from './effect-toasts';
export type { EffectToastDef, HarvestBurstVariant } from './effect-toasts';
```
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - lib/semantic/effect-toasts.ts existe et contient `export const EFFECT_TOASTS: Record<CategoryId, EffectToastDef>`
    - lib/semantic/effect-toasts.ts contient exactement 10 entrées (menage_quotidien, menage_hebdo, courses, enfants_routines, enfants_devoirs, rendez_vous, gratitude_famille, budget_admin, bebe_soins, cuisine_repas)
    - lib/semantic/effect-toasts.ts contient `export const CATEGORY_VARIANT: Record<CategoryId, HarvestBurstVariant>`
    - lib/semantic/effect-toasts.ts contient `export const CATEGORY_HAPTIC_FN: Record<CategoryId`
    - lib/mascot/haptics.ts contient `export function hapticsEffectLight`
    - lib/mascot/haptics.ts contient `export async function hapticsEffectMedium`
    - lib/mascot/haptics.ts contient `export async function hapticsEffectStrong`
    - lib/mascot/haptics.ts contient `export async function hapticsEffectGolden`
    - lib/semantic/index.ts contient `export { EFFECT_TOASTS, CATEGORY_VARIANT, CATEGORY_HAPTIC_FN }`
    - `npx tsc --noEmit` passe sans erreur dans ces fichiers
  </acceptance_criteria>
  <done>3 fichiers créés/étendus, EFFECT_TOASTS 10 entrées, 4 fonctions haptic, 2 mappings catégorie→variant et catégorie→haptic, barrel mis à jour, TypeScript passe</done>
</task>

<task type="auto">
  <name>Task 2: Étendre HarvestBurst avec prop variant</name>
  <files>components/mascot/HarvestBurst.tsx</files>
  <read_first>
    - components/mascot/HarvestBurst.tsx (composant complet — comprendre PARTICLE_COUNT, PARTICLE_SIZE, Particle sub-component, rewardLabel style, animation config existante)
    - lib/semantic/effect-toasts.ts (HarvestBurstVariant type — créé en Task 1)
  </read_first>
  <action>
Modifier `components/mascot/HarvestBurst.tsx` pour supporter un prop `variant` optionnel qui contrôle la taille, le nombre et la couleur des particules.

1. Importer le type :
```typescript
import type { HarvestBurstVariant } from '../../lib/semantic/effect-toasts';
```

2. Ajouter `VARIANT_CONFIG` en constante module APRÈS `CROP_COLORS` :
```typescript
const VARIANT_CONFIG: Record<HarvestBurstVariant, {
  particleCount: number;
  particleSize: number;
  travelMin: number;
  travelMax: number;
  labelTravelY: number;
  labelDuration: number;
  labelColor: string;
  particleColor: string;
}> = {
  ambient: { particleCount: 8, particleSize: 6, travelMin: 25, travelMax: 45, labelTravelY: -45, labelDuration: 800, labelColor: '#A7F3D0', particleColor: '#34D399' },
  rare:    { particleCount: 10, particleSize: 6, travelMin: 30, travelMax: 50, labelTravelY: -50, labelDuration: 900, labelColor: '#C4B5FD', particleColor: '#A78BFA' },
  golden:  { particleCount: 12, particleSize: 8, travelMin: 35, travelMax: 60, labelTravelY: -60, labelDuration: 1000, labelColor: '#FFD700', particleColor: '#FFD700' },
};
```

3. Étendre `HarvestBurstProps` :
```typescript
interface HarvestBurstProps {
  x: number;
  y: number;
  reward: number;
  cropColor: string;
  onComplete: () => void;
  variant?: HarvestBurstVariant;  // NOUVEAU — Phase 21
}
```

4. Modifier le composant `Particle` pour accepter `particleSize`, `travelMin`, `travelMax` en props :
```typescript
function Particle({ color, index, startX, startY, particleSize, particleCount, travelMin, travelMax }: {
  color: string; index: number; startX: number; startY: number;
  particleSize: number; particleCount: number; travelMin: number; travelMax: number;
}) {
  const angle = (index / particleCount) * Math.PI * 2 + (Math.random() - 0.5) * 0.8;
  const distance = travelMin + Math.random() * (travelMax - travelMin);
  // ... reste identique, utiliser particleSize au lieu de PARTICLE_SIZE
```

5. Dans `HarvestBurst`, dériver la config du variant :
```typescript
export function HarvestBurst({ x, y, reward, cropColor, onComplete, variant }: HarvestBurstProps) {
  const cfg = variant ? VARIANT_CONFIG[variant] : null;
  const effectiveParticleCount = cfg?.particleCount ?? PARTICLE_COUNT;
  const effectiveParticleSize = cfg?.particleSize ?? PARTICLE_SIZE;
  const effectiveColor = cfg?.particleColor ?? cropColor;
  const effectiveTravelMin = cfg?.travelMin ?? 25;
  const effectiveTravelMax = cfg?.travelMax ?? 45;
  const effectiveLabelTravelY = cfg?.labelTravelY ?? -45;
  const effectiveLabelDuration = cfg?.labelDuration ?? 800;
  const effectiveLabelColor = cfg?.labelColor ?? '#FFD700';
```

6. Mettre à jour le `labelY.value` pour utiliser `effectiveLabelTravelY` et `effectiveLabelDuration` :
```typescript
labelY.value = withTiming(effectiveLabelTravelY, { duration: effectiveLabelDuration, easing: Easing.out(Easing.quad) });
```

7. Mettre à jour le `Array.from` pour utiliser `effectiveParticleCount` et passer les props au `Particle` :
```typescript
{Array.from({ length: effectiveParticleCount }).map((_, i) => (
  <Particle key={i} color={effectiveColor} index={i} startX={0} startY={0}
    particleSize={effectiveParticleSize} particleCount={effectiveParticleCount}
    travelMin={effectiveTravelMin} travelMax={effectiveTravelMax} />
))}
```

8. Mettre à jour le style du rewardLabel pour utiliser `effectiveLabelColor` (inline, pas dans StyleSheet) :
```typescript
<Text style={[styles.rewardLabel, { color: effectiveLabelColor }]}>
```

9. Mettre à jour le `setTimeout` callback pour utiliser `effectiveLabelDuration` (ajouter 100ms de marge) :
```typescript
setTimeout(() => runOnJS(triggerComplete)(), (cfg?.labelDuration ?? 800) + 100);
```

IMPORTANT : Le comportement par défaut (sans variant prop) doit rester 100% identique — les valeurs par défaut reprennent exactement PARTICLE_COUNT=8, PARTICLE_SIZE=6, travel 25-45, labelTravelY=-45, labelDuration=800.

Exporter aussi `VARIANT_CONFIG` et `HarvestBurstVariant` (re-export du type) pour que d'autres composants puissent les utiliser.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - components/mascot/HarvestBurst.tsx contient `variant?: HarvestBurstVariant` dans HarvestBurstProps
    - components/mascot/HarvestBurst.tsx contient `const VARIANT_CONFIG` avec 3 entrées (ambient, rare, golden)
    - components/mascot/HarvestBurst.tsx contient `particleCount: 12` (golden), `particleCount: 10` (rare), `particleCount: 8` (ambient)
    - components/mascot/HarvestBurst.tsx contient `particleColor: '#FFD700'` (golden)
    - Le composant Particle accepte `particleSize` et `travelMin` et `travelMax` en props
    - `npx tsc --noEmit` passe sans erreur
  </acceptance_criteria>
  <done>HarvestBurst supporte 3 variants visuels (golden 12 particules dorées, rare 10 violettes, ambient 8 vertes), comportement par défaut inchangé sans variant prop</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passe sans erreur
- `lib/semantic/effect-toasts.ts` existe avec 10 entrées EFFECT_TOASTS + CATEGORY_VARIANT + CATEGORY_HAPTIC_FN
- `lib/mascot/haptics.ts` a 4 nouvelles fonctions hapticsEffect*
- `components/mascot/HarvestBurst.tsx` accepte variant prop avec 3 configs
</verification>

<success_criteria>
- Les 10 définitions de toast sont disponibles en FR+EN (FEEDBACK-01, FEEDBACK-05)
- Les 4 patterns haptiques couvrent les 10 catégories par intensité (FEEDBACK-02)
- HarvestBurst variant adapte particules/couleur/durée (FEEDBACK-03)
- TypeScript compile sans erreur
</success_criteria>

<output>
After completion, create `.planning/phases/21-feedback-visuel-compagnon/21-01-SUMMARY.md`
</output>
