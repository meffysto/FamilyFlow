---
phase: 21-feedback-visuel-compagnon
plan: 02
type: execute
wave: 2
depends_on: ["21-01"]
files_modified:
  - hooks/useGamification.ts
  - app/(tabs)/tasks.tsx
  - app/(tabs)/tree.tsx
  - lib/mascot/companion-types.ts
  - lib/mascot/companion-engine.ts
  - locales/fr/common.json
  - locales/en/common.json
autonomous: true
requirements: [FEEDBACK-01, FEEDBACK-02, FEEDBACK-03, FEEDBACK-04, FEEDBACK-05]

must_haves:
  truths:
    - "User voit un toast specifique quand un effet semantique est declenche"
    - "User sent un haptic pattern distinct par categorie"
    - "User voit un HarvestBurst variant (golden/rare/ambient) au centre de l'ecran tasks apres completion"
    - "User lit un message compagnon contextuel par categorie dans la bulle arbre"
    - "User retrouve la parite FR+EN stricte sur tous les strings"
    - "Pas de toast quand le cap est atteint (D-03)"
  artifacts:
    - path: "hooks/useGamification.ts"
      provides: "Dispatch feedback (toast + haptic) apres applyTaskEffect(), retourne effectCategoryId"
      contains: "EFFECT_TOASTS"
    - path: "app/(tabs)/tasks.tsx"
      provides: "Passage de taskMeta a completeTask + HarvestBurst overlay centre-ecran avec variant"
      contains: "HarvestBurst"
    - path: "app/(tabs)/tree.tsx"
      provides: "Lecture de lastSemanticCategoryId depuis SecureStore, injection dans CompanionMessageContext.subType"
      contains: "subType"
    - path: "lib/mascot/companion-types.ts"
      provides: "subType?: string dans CompanionMessageContext"
      contains: "subType"
    - path: "lib/mascot/companion-engine.ts"
      provides: "SUB_TYPE_TEMPLATES + pickCompanionMessage etendu"
      contains: "SUB_TYPE_TEMPLATES"
    - path: "locales/fr/common.json"
      provides: "20 cles i18n companion sub-type FR"
      contains: "taskDone_menage_quotidien"
    - path: "locales/en/common.json"
      provides: "20 cles i18n companion sub-type EN"
      contains: "taskDone_menage_quotidien"
  key_links:
    - from: "hooks/useGamification.ts"
      to: "lib/semantic/effect-toasts.ts"
      via: "import EFFECT_TOASTS, CATEGORY_HAPTIC_FN"
      pattern: "EFFECT_TOASTS\\[catId\\]"
    - from: "hooks/useGamification.ts"
      to: "expo-secure-store"
      via: "SecureStore.setItemAsync('last_semantic_category', catId)"
      pattern: "last_semantic_category"
    - from: "app/(tabs)/tasks.tsx"
      to: "components/mascot/HarvestBurst.tsx"
      via: "import HarvestBurst + CATEGORY_VARIANT render"
      pattern: "HarvestBurst.*variant"
    - from: "app/(tabs)/tasks.tsx"
      to: "hooks/useGamification.ts"
      via: "completeTask(activeProfile, task.text, taskMeta)"
      pattern: "tags: task\\.tags"
    - from: "app/(tabs)/tree.tsx"
      to: "expo-secure-store"
      via: "SecureStore.getItemAsync('last_semantic_category') -> subType"
      pattern: "last_semantic_category"
    - from: "lib/mascot/companion-engine.ts"
      to: "lib/mascot/companion-types.ts"
      via: "context.subType"
      pattern: "context\\.subType"
---

<objective>
Cabler le feedback complet dans completeTask : toast + haptic dispatch, passer taskMeta depuis tasks.tsx, afficher HarvestBurst variant au centre-ecran, etendre le compagnon avec messages sub-type par categorie via SecureStore bridge, ajouter les 40 cles i18n FR+EN.

Purpose: Rendre les effets semantiques visibles et tangibles a l'utilisateur a chaque completion de tache.
Output: Feedback complet wired end-to-end (tasks.tsx -> useGamification -> toast/haptic/burst/companion).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-feedback-visuel-compagnon/21-CONTEXT.md
@.planning/phases/21-feedback-visuel-compagnon/21-RESEARCH.md
@.planning/phases/21-feedback-visuel-compagnon/21-UI-SPEC.md
@.planning/phases/21-feedback-visuel-compagnon/21-01-SUMMARY.md

@hooks/useGamification.ts
@app/(tabs)/tasks.tsx
@app/(tabs)/tree.tsx
@lib/mascot/companion-types.ts
@lib/mascot/companion-engine.ts
@lib/semantic/effect-toasts.ts
@lib/semantic/categories.ts
@contexts/ToastContext.tsx
@lib/i18n.ts
@locales/fr/common.json
@locales/en/common.json
@components/mascot/HarvestBurst.tsx

<interfaces>
From lib/semantic/effect-toasts.ts (cree Plan 01):
```typescript
export interface EffectToastDef { icon: string; fr: string; en: string; subtitle_fr: string; subtitle_en: string; type: 'success' | 'info'; }
export const EFFECT_TOASTS: Record<CategoryId, EffectToastDef>;
export type HarvestBurstVariant = 'ambient' | 'rare' | 'golden';
export const CATEGORY_VARIANT: Record<CategoryId, HarvestBurstVariant>;
export const CATEGORY_HAPTIC_FN: Record<CategoryId, () => void | Promise<void>>;
```

From hooks/useGamification.ts:
```typescript
completeTask: (profile: Profile, taskText: string, taskMeta?: { tags?: string[]; section?: string; sourceFile?: string }) => Promise<{
  updatedProfile: Profile; lootAwarded: boolean; pointsGained: number; cropsMatured: string[]; effectResult?: EffectResult | null;
}>
```

From contexts/ToastContext.tsx:
```typescript
showToast: (message: string, type?: ToastType, action?: ToastAction, options?: ToastOptions) => void;
// ToastOptions: { icon?: string; subtitle?: string; }
```

From lib/mascot/companion-types.ts:
```typescript
export interface CompanionMessageContext {
  profileName: string; companionName: string; companionSpecies: CompanionSpecies;
  tasksToday: number; streak: number; level: number; /* ... etc */
}
export type CompanionEvent = 'task_completed' | 'loot_opened' | /* ... */;
```

From lib/mascot/companion-engine.ts:
```typescript
export const MESSAGE_TEMPLATES: Record<CompanionEvent, string[]>;
export function pickCompanionMessage(event: CompanionEvent, _context: CompanionMessageContext): string;
```

From components/mascot/HarvestBurst.tsx (etendu Plan 01):
```typescript
interface HarvestBurstProps {
  x: number; y: number; reward: number; cropColor: string; onComplete: () => void;
  variant?: HarvestBurstVariant;
}
export function HarvestBurst(props: HarvestBurstProps): JSX.Element;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dispatch toast + haptic + retourner effectCategoryId dans useGamification + cabler taskMeta et HarvestBurst overlay dans tasks.tsx</name>
  <files>hooks/useGamification.ts, app/(tabs)/tasks.tsx</files>
  <read_first>
    - hooks/useGamification.ts (COMPLET — comprendre le flux completeTask, le bloc semantic coupling lignes ~204-254, le return statement ligne ~274, les imports existants)
    - app/(tabs)/tasks.tsx (trouver l'appel `completeTask(activeProfile, task.text)` — environ ligne 298 — et comprendre l'objet `task` disponible pour extraire tags/section/sourceFile. Trouver les imports existants et le JSX return pour placer le HarvestBurst overlay)
    - contexts/ToastContext.tsx (API showToast — premier 60 lignes pour comprendre ToastOptions interface avec icon + subtitle)
    - lib/semantic/effect-toasts.ts (cree Plan 01 — EFFECT_TOASTS, CATEGORY_HAPTIC_FN, CATEGORY_VARIANT)
    - lib/i18n.ts (comment acceder a i18n.language directement — import du singleton)
    - components/mascot/HarvestBurst.tsx (props interface apres Plan 01 — variant prop)
  </read_first>
  <action>
**Fichier 1 — hooks/useGamification.ts (ETENDRE)**

1. Ajouter les imports suivants en haut du fichier (apres les imports existants de `lib/semantic`) :
```typescript
import { EFFECT_TOASTS, CATEGORY_HAPTIC_FN } from '../lib/semantic/effect-toasts';
import { useToast } from '../contexts/ToastContext';
import i18n from '../lib/i18n';
import * as SecureStore from 'expo-secure-store';
```

2. Dans la fonction `useGamification`, ajouter `const { showToast } = useToast();` APRES la ligne `const [isProcessing, setIsProcessing] = useState(false);` et AVANT le `useEffect` pour `loadGamiConfig()`.

3. Declarer `let derivedCategory: CategoryMatch | null = null;` juste APRES `let effectResult: EffectResult | null = null;` (vers ligne 175).

4. Dans le bloc semantic coupling, APRES `const category = deriveTaskCategory(task as Task);` (vers ligne 213), ajouter `derivedCategory = category;`.

5. Ajouter le bloc de dispatch feedback APRES le bloc semantic coupling (apres le catch vers ligne ~252-254) et AVANT le commentaire `// 3. Ecrire farm-{id}.md` (ligne ~256) :

```typescript
        // Phase 21 : Feedback visuel + haptique (FEEDBACK-01, FEEDBACK-02)
        // Toast + haptic immediat en parallele (D-02), silencieux si cappe (D-03)
        if (effectResult?.effectApplied && derivedCategory) {
          try {
            const catId = derivedCategory.id;
            const toastDef = EFFECT_TOASTS[catId];
            if (toastDef) {
              const lang = i18n.language?.startsWith('en') ? 'en' : 'fr';
              showToast(
                lang === 'en' ? toastDef.en : toastDef.fr,
                toastDef.type,
                undefined,
                { icon: toastDef.icon, subtitle: lang === 'en' ? toastDef.subtitle_en : toastDef.subtitle_fr }
              );
            }
            // Haptic fire-and-forget (non-critical)
            try { CATEGORY_HAPTIC_FN[catId]?.(); } catch { /* haptic — non-critical */ }
            // Phase 21 FEEDBACK-04 : Bridge SecureStore pour subType compagnon (tree.tsx le lira)
            SecureStore.setItemAsync('last_semantic_category', catId).catch(() => {});
          } catch {
            if (__DEV__) console.warn('Effect feedback — non-critical');
          }
        }
```

6. Modifier le return statement (vers ligne 274-280) pour inclure `effectCategoryId` :

```typescript
        return {
          updatedProfile,
          lootAwarded,
          pointsGained: entry.points,
          cropsMatured,
          effectResult,  // Phase 20
          effectCategoryId: derivedCategory?.id ?? null,  // Phase 21 — pour HarvestBurst variant
        };
```

7. Mettre a jour le type de retour de completeTask (vers ligne 60-65) pour inclure `effectCategoryId` :

```typescript
  completeTask: (profile: Profile, taskText: string, taskMeta?: { tags?: string[]; section?: string; sourceFile?: string }) => Promise<{
    updatedProfile: Profile;
    lootAwarded: boolean;
    pointsGained: number;
    cropsMatured: string[];
    effectResult?: EffectResult | null;
    effectCategoryId?: CategoryId | null;  // Phase 21
  }>;
```

8. Ajouter `import type { CategoryMatch } from '../lib/semantic';` et `import type { CategoryId } from '../lib/semantic/categories';` si pas deja importes.

9. Ajouter `showToast` a la dependance array du `useCallback` pour `completeTask` (vers ligne ~285) : `[vault, notifPrefs, onDataChange, onQuestProgress, showToast]`.

**Fichier 2 — app/(tabs)/tasks.tsx (FIX + HARVESTBURST OVERLAY)**

A. **Fix taskMeta (critique)** — Trouver l'appel `completeTask(activeProfile, task.text)` (vers ligne 298). Remplacer par :
```typescript
const { lootAwarded, pointsGained, effectCategoryId } = await completeTask(
  activeProfile,
  task.text,
  { tags: task.tags, section: task.section, sourceFile: task.sourceFile }
);
```

B. **Ajouter imports** en haut du fichier :
```typescript
import { Dimensions } from 'react-native';
import { HarvestBurst } from '../../components/mascot/HarvestBurst';
import { CATEGORY_VARIANT } from '../../lib/semantic/effect-toasts';
import type { HarvestBurstVariant } from '../../lib/semantic/effect-toasts';
```

C. **Ajouter state pour le burst** dans le composant `TasksScreen` (apres les autres useState) :
```typescript
const [effectBurst, setEffectBurst] = useState<{
  variant: HarvestBurstVariant;
  key: number;
} | null>(null);
```

D. **Declencher le burst apres completeTask** — dans le handleTaskToggle, APRES la ligne `await refreshGamification();` et AVANT les conditionnelles sur `lootAwarded` / toast (vers ligne 300-305) :
```typescript
            // Phase 21 — HarvestBurst variant au centre-ecran (FEEDBACK-03)
            if (effectCategoryId) {
              const variant = CATEGORY_VARIANT[effectCategoryId];
              if (variant) {
                setEffectBurst({ variant, key: Date.now() });
              }
            }
```

E. **Rendre le HarvestBurst overlay** — dans le JSX return, JUSTE AVANT le `</View>` de fermeture du container principal (ou apres le dernier composant avant la fermeture). Utiliser une position fixe au centre-ecran :
```tsx
{effectBurst && (
  <HarvestBurst
    key={effectBurst.key}
    x={Dimensions.get('window').width / 2}
    y={Dimensions.get('window').height / 3}
    reward={0}
    cropColor={effectBurst.variant === 'golden' ? '#FFD700' : effectBurst.variant === 'rare' ? '#A78BFA' : '#34D399'}
    variant={effectBurst.variant}
    onComplete={() => setEffectBurst(null)}
  />
)}
```

Note : `reward={0}` masque le label de recompense (le composant HarvestBurst n'affiche pas le label quand reward=0 — verifier dans le code ; sinon utiliser reward={1} et laisser le label s'afficher brievement). Le `key={Date.now()}` force un re-mount a chaque declenchement.

F. **Ajouter effectBurst dependencies** au useCallback si necessaire.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - hooks/useGamification.ts contient `import { EFFECT_TOASTS, CATEGORY_HAPTIC_FN } from '../lib/semantic/effect-toasts'`
    - hooks/useGamification.ts contient `import { useToast } from '../contexts/ToastContext'`
    - hooks/useGamification.ts contient `const { showToast } = useToast()`
    - hooks/useGamification.ts contient `let derivedCategory` variable
    - hooks/useGamification.ts contient `EFFECT_TOASTS[catId]`
    - hooks/useGamification.ts contient `CATEGORY_HAPTIC_FN[catId]`
    - hooks/useGamification.ts contient `SecureStore.setItemAsync('last_semantic_category', catId)`
    - hooks/useGamification.ts retourne `effectCategoryId` dans le return de completeTask
    - hooks/useGamification.ts contient `showToast` dans le useCallback dependency array
    - app/(tabs)/tasks.tsx contient `tags: task.tags, section: task.section, sourceFile: task.sourceFile`
    - app/(tabs)/tasks.tsx contient `import { HarvestBurst }` et `import { CATEGORY_VARIANT }`
    - app/(tabs)/tasks.tsx contient `const [effectBurst, setEffectBurst]` state
    - app/(tabs)/tasks.tsx contient `<HarvestBurst` dans le JSX avec `variant={effectBurst.variant}`
    - app/(tabs)/tasks.tsx contient `CATEGORY_VARIANT[effectCategoryId]` pour deriver le variant
    - `npx tsc --noEmit` passe sans erreur
  </acceptance_criteria>
  <done>Toast + haptic dispatch cable dans completeTask, taskMeta passe depuis tasks.tsx, HarvestBurst variant rendu au centre-ecran dans tasks.tsx apres completion, SecureStore bridge ecrit pour subType compagnon, feedback silencieux si cap exceeded (D-03)</done>
</task>

<task type="auto">
  <name>Task 2: Messages compagnon sub-type + cles i18n FR+EN + bridge subType dans tree.tsx</name>
  <files>lib/mascot/companion-types.ts, lib/mascot/companion-engine.ts, app/(tabs)/tree.tsx, locales/fr/common.json, locales/en/common.json</files>
  <read_first>
    - lib/mascot/companion-types.ts (CompanionMessageContext interface — comprendre les champs existants pour ajouter subType)
    - lib/mascot/companion-engine.ts (MESSAGE_TEMPLATES Record type, pickCompanionMessage function — comprendre la logique de selection et le type Record)
    - app/(tabs)/tree.tsx (COMPLET — trouver les 3-4 endroits ou CompanionMessageContext est construit, comprendre le pattern useFocusEffect et showCompanionMsg, trouver les imports SecureStore existants)
    - lib/semantic/categories.ts (CategoryId type — les 10 identifiants)
    - locales/fr/common.json (structure companion.msg.* existante — verifier le format exact des cles taskDone)
    - locales/en/common.json (pareil — verifier la parite existante)
  </read_first>
  <action>
**Fichier 1 — lib/mascot/companion-types.ts (ETENDRE)**

Ajouter `subType?: string;` dans l'interface `CompanionMessageContext` (apres le champ `timeOfDay`). Utiliser `string` et non `CategoryId` pour eviter d'importer un type de `lib/semantic` dans `lib/mascot` (pas de couplage ascendant). Le commentaire indique la semantique :

```typescript
  subType?: string;  // Phase 21 — CategoryId de la tache semantique, passe a pickCompanionMessage pour templates sub-type
```

**Fichier 2 — lib/mascot/companion-engine.ts (ETENDRE)**

1. Ajouter un dictionnaire separe `SUB_TYPE_TEMPLATES` APRES le `MESSAGE_TEMPLATES` existant (apres la ligne `};` qui ferme MESSAGE_TEMPLATES, vers ligne 221). Ne PAS modifier le type de `MESSAGE_TEMPLATES` (reste `Record<CompanionEvent, string[]>`) :

```typescript
/**
 * Templates sub-type pour task_completed par categorie semantique (Phase 21).
 * Cles = 'task_completed_${categoryId}' — 2 templates par categorie (D-05).
 * Dictionnaire separe pour ne pas elargir le type Record<CompanionEvent, ...>.
 */
export const SUB_TYPE_TEMPLATES: Record<string, string[]> = {
  task_completed_menage_quotidien: [
    'companion.msg.taskDone_menage_quotidien.1',
    'companion.msg.taskDone_menage_quotidien.2',
  ],
  task_completed_menage_hebdo: [
    'companion.msg.taskDone_menage_hebdo.1',
    'companion.msg.taskDone_menage_hebdo.2',
  ],
  task_completed_courses: [
    'companion.msg.taskDone_courses.1',
    'companion.msg.taskDone_courses.2',
  ],
  task_completed_enfants_routines: [
    'companion.msg.taskDone_enfants_routines.1',
    'companion.msg.taskDone_enfants_routines.2',
  ],
  task_completed_enfants_devoirs: [
    'companion.msg.taskDone_enfants_devoirs.1',
    'companion.msg.taskDone_enfants_devoirs.2',
  ],
  task_completed_rendez_vous: [
    'companion.msg.taskDone_rendez_vous.1',
    'companion.msg.taskDone_rendez_vous.2',
  ],
  task_completed_gratitude_famille: [
    'companion.msg.taskDone_gratitude_famille.1',
    'companion.msg.taskDone_gratitude_famille.2',
  ],
  task_completed_budget_admin: [
    'companion.msg.taskDone_budget_admin.1',
    'companion.msg.taskDone_budget_admin.2',
  ],
  task_completed_bebe_soins: [
    'companion.msg.taskDone_bebe_soins.1',
    'companion.msg.taskDone_bebe_soins.2',
  ],
  task_completed_cuisine_repas: [
    'companion.msg.taskDone_cuisine_repas.1',
    'companion.msg.taskDone_cuisine_repas.2',
  ],
};
```

2. Modifier `pickCompanionMessage` pour resoudre les sub-types AVANT le fallback generique :

```typescript
export function pickCompanionMessage(
  event: CompanionEvent,
  context: CompanionMessageContext,
): string {
  // Phase 21 : Sub-type lookup pour task_completed (D-04)
  if (event === 'task_completed' && context.subType) {
    const subKey = `task_completed_${context.subType}`;
    const subTemplates = SUB_TYPE_TEMPLATES[subKey];
    if (subTemplates && subTemplates.length > 0) {
      const idx = Math.floor(Math.random() * subTemplates.length);
      return subTemplates[idx];
    }
  }
  // Fallback — comportement existant
  const templates = MESSAGE_TEMPLATES[event];
  if (!templates || templates.length === 0) {
    return MESSAGE_TEMPLATES.greeting[0];
  }
  const idx = Math.floor(Math.random() * templates.length);
  return templates[idx];
}
```

Changer `_context` en `context` dans la signature (enlever le underscore) puisqu'on l'utilise maintenant.

**Fichier 3 — app/(tabs)/tree.tsx (ETENDRE — BRIDGE SUBTYPE)**

Ce fichier est le maillon manquant qui connecte le SecureStore bridge (ecrit par useGamification en Task 1) au CompanionMessageContext.subType.

1. SecureStore est deja importe dans tree.tsx (utilise pour `companion_last_visit`). Pas de nouvel import necessaire.

2. Trouver la callback `showActionMessage` (vers ligne 680-721) qui est appelee quand un evenement compagnon se produit (task_completed, etc.). C'est la que le subType doit etre lu.

3. DANS la callback `showActionMessage`, AVANT la construction de `context: CompanionMessageContext` (vers ligne 699), ajouter la lecture du SecureStore :

```typescript
    // Phase 21 : Lire le subType semantique depuis le bridge SecureStore
    let subType: string | undefined;
    try {
      const stored = await SecureStore.getItemAsync('last_semantic_category');
      if (stored) {
        subType = stored;
        // Nettoyer immediatement pour eviter de reutiliser un vieux subType
        SecureStore.deleteItemAsync('last_semantic_category').catch(() => {});
      }
    } catch { /* SecureStore — non-critical */ }
```

NOTE IMPORTANTE : `showActionMessage` est possiblement synchrone (useCallback). Si c'est un useCallback synchrone, il faut le rendre `async` ou utiliser un pattern `.then()`. Verifier la signature exacte. Si la callback n'est pas async, utiliser le pattern suivant a la place :

```typescript
    // Alternative si showActionMessage ne peut pas etre async :
    // Lire de maniere synchrone via un ref qui est mis a jour par un useEffect
```

En realite, la meilleure approche est d'utiliser un ref `lastSemanticCategoryRef` qui est mis a jour par un listener. Plus simple : ajouter un `useEffect` qui lit le SecureStore a intervalle regulier (polled) ou mieux, lire le SecureStore dans le `useFocusEffect` qui construit deja les messages compagnon.

**APPROCHE RECOMMANDEE** — Injecter dans le useFocusEffect de bienvenue (vers ligne 724-800) :

Le useFocusEffect qui affiche un message au focus de l'onglet arbre construit un `context` et appelle `pickCompanionMessage(event, context)`. C'est l'endroit ideal car :
- Il est deja `async`-compatible (le callback interne fait des `await`)
- Il est appele quand l'user revient sur l'onglet arbre apres avoir complete des taches
- C'est exactement le moment ou le subType est pertinent

Dans ce useFocusEffect (vers ligne 724), APRES la construction de `context` (vers ligne 744-758) et AVANT l'appel `showCompanionMsg(pickCompanionMessage(event, context), context)` (vers ligne 795), ajouter :

```typescript
    // Phase 21 : Injecter subType depuis le bridge SecureStore (FEEDBACK-04, D-06)
    try {
      const storedCategory = await SecureStore.getItemAsync('last_semantic_category');
      if (storedCategory) {
        context.subType = storedCategory;
        // Nettoyer pour eviter reutilisation
        SecureStore.deleteItemAsync('last_semantic_category').catch(() => {});
      }
    } catch { /* SecureStore — non-critical */ }
```

4. EGALEMENT injecter le subType dans la callback `showActionMessage` (vers ligne 699-710) si elle est appelee pour `task_completed`. Meme pattern : lire SecureStore, injecter dans context.subType, nettoyer. Si `showActionMessage` n'est pas async, wrapper la lecture dans un `.then()` ou convertir en async.

5. Verifier que les endroits dans tree.tsx ou `pickCompanionMessage('task_completed', context)` est appele incluent bien le subType dans le context. Il y a ~3 endroits ou CompanionMessageContext est construit — seuls ceux lies a `task_completed` ont besoin du subType.

**Fichier 4 — locales/fr/common.json (ETENDRE)**

Ajouter 20 cles i18n APRES le bloc `taskDone` existant (dans `companion.msg`). Format : `"taskDone_${categoryId}": { "1": "...", "2": "..." }` :

```json
"taskDone_menage_quotidien": {
  "1": "Le menage est fait, les mauvaises herbes ont disparu de la parcelle !",
  "2": "Bien joue pour le menage quotidien, la ferme respire mieux sans ces herbes !"
},
"taskDone_menage_hebdo": {
  "1": "Le grand nettoyage a paye, une usure reparee sur la ferme !",
  "2": "Grace au menage en profondeur, la ferme est comme neuve !"
},
"taskDone_courses": {
  "1": "Les courses sont rangees et les batiments tournent a plein regime !",
  "2": "Bien joue pour les courses, la production des batiments est boostee !"
},
"taskDone_enfants_routines": {
  "1": "Les routines sont faites, {{companionName}} deborde de joie !",
  "2": "Grace aux routines des enfants, {{companionName}} est au top de sa forme !"
},
"taskDone_enfants_devoirs": {
  "1": "Les devoirs sont faits, les cultures poussent plus vite maintenant !",
  "2": "Bien joue pour les devoirs, le sprint de croissance est lance !"
},
"taskDone_rendez_vous": {
  "1": "Le rendez-vous est passe et une graine rare est apparue dans le jardin !",
  "2": "Bravo pour le rendez-vous, une recolte precieuse attend dans la ferme !"
},
"taskDone_gratitude_famille": {
  "1": "Ce moment de gratitude a renforce le voyageur de la saga !",
  "2": "Grace a cette attention familiale, le trait de saga a grandi !"
},
"taskDone_budget_admin": {
  "1": "Le budget est regle et les batiments ont double leur capacite !",
  "2": "Bien joue pour l'admin, les batiments se remplissent deux fois plus vite !"
},
"taskDone_bebe_soins": {
  "1": "Les soins sont donnes et la prochaine recolte sera doree !",
  "2": "Grace aux soins du bebe, une pluie doree attend a la recolte !"
},
"taskDone_cuisine_repas": {
  "1": "Le repas est pret et une recette rare a ete debloquee !",
  "2": "Bien joue en cuisine, un nouveau craft est disponible !"
}
```

NOTE : Les accents doivent etre presents dans le fichier final (e accent aigu, a accent grave, etc.). Les exemples ci-dessus sont sans accents pour la lisibilite du plan — l'executeur DOIT utiliser les accents corrects en francais.

**Fichier 5 — locales/en/common.json (ETENDRE)**

Ajouter les 20 cles equivalentes EN PARITE STRICTE (FEEDBACK-05) :

```json
"taskDone_menage_quotidien": {
  "1": "Housework done, the weeds have vanished from the plot!",
  "2": "Nice daily cleaning, the farm breathes better without those weeds!"
},
"taskDone_menage_hebdo": {
  "1": "Deep cleaning paid off, a wear event is repaired on the farm!",
  "2": "Thanks to the thorough cleaning, the farm is good as new!"
},
"taskDone_courses": {
  "1": "Shopping is done and the buildings are running at full speed!",
  "2": "Great job on the shopping, building production is boosted!"
},
"taskDone_enfants_routines": {
  "1": "Routines are done, {{companionName}} is bursting with joy!",
  "2": "Thanks to the kids' routines, {{companionName}} is in top shape!"
},
"taskDone_enfants_devoirs": {
  "1": "Homework is done, crops are growing faster now!",
  "2": "Great job on homework, the growth sprint is on!"
},
"taskDone_rendez_vous": {
  "1": "Appointment done and a rare seed appeared in the garden!",
  "2": "Well done on the appointment, a precious harvest awaits on the farm!"
},
"taskDone_gratitude_famille": {
  "1": "This moment of gratitude empowered the saga traveler!",
  "2": "Thanks to this family attention, the saga trait has grown!"
},
"taskDone_budget_admin": {
  "1": "Budget is settled and buildings doubled their capacity!",
  "2": "Great admin work, buildings fill up twice as fast!"
},
"taskDone_bebe_soins": {
  "1": "Baby care done and the next harvest will be golden!",
  "2": "Thanks to baby care, a golden rain awaits at harvest!"
},
"taskDone_cuisine_repas": {
  "1": "Meal is ready and a rare recipe has been unlocked!",
  "2": "Great cooking, a new craft is available!"
}
```
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - lib/mascot/companion-types.ts contient `subType?: string` dans CompanionMessageContext
    - lib/mascot/companion-engine.ts contient `export const SUB_TYPE_TEMPLATES: Record<string, string[]>`
    - lib/mascot/companion-engine.ts contient `task_completed_menage_quotidien` dans SUB_TYPE_TEMPLATES
    - lib/mascot/companion-engine.ts pickCompanionMessage contient `context.subType` (pas `_context`)
    - lib/mascot/companion-engine.ts pickCompanionMessage contient `SUB_TYPE_TEMPLATES[subKey]`
    - app/(tabs)/tree.tsx contient `SecureStore.getItemAsync('last_semantic_category')` dans le useFocusEffect
    - app/(tabs)/tree.tsx contient `context.subType = storedCategory`
    - app/(tabs)/tree.tsx contient `SecureStore.deleteItemAsync('last_semantic_category')` (nettoyage)
    - locales/fr/common.json contient `taskDone_menage_quotidien` avec cles "1" et "2"
    - locales/fr/common.json contient `taskDone_cuisine_repas` avec cles "1" et "2"
    - locales/en/common.json contient `taskDone_menage_quotidien` avec cles "1" et "2"
    - locales/en/common.json contient `taskDone_cuisine_repas` avec cles "1" et "2"
    - Nombre exact de 10 entrees dans SUB_TYPE_TEMPLATES (10 task_completed_* keys)
    - FR et EN ont le meme nombre de cles taskDone_* (parite stricte FEEDBACK-05)
    - `npx tsc --noEmit` passe sans erreur
  </acceptance_criteria>
  <done>Companion sub-type messages cables end-to-end (useGamification ecrit catId dans SecureStore, tree.tsx le lit et l'injecte dans CompanionMessageContext.subType, pickCompanionMessage resout SUB_TYPE_TEMPLATES), 20 templates i18n FR+EN ajoutes, HarvestBurst variant visible dans tasks.tsx</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passe sans erreur
- `npx jest --no-coverage` passe (tests existants non casses)
- tasks.tsx passe taskMeta a completeTask
- tasks.tsx rend `<HarvestBurst variant={...}>` au centre-ecran quand effectCategoryId est retourne
- useGamification dispatch toast + haptic apres effectApplied
- useGamification ecrit `last_semantic_category` dans SecureStore
- tree.tsx lit `last_semantic_category` depuis SecureStore et l'injecte dans context.subType
- tree.tsx nettoie `last_semantic_category` apres lecture
- companion-engine resout sub-type templates avant fallback
- Parite FR+EN stricte : 10 cles taskDone_* dans chaque locale
</verification>

<success_criteria>
- FEEDBACK-01 : Toast specifique par effet via EFFECT_TOASTS[catId] + showToast
- FEEDBACK-02 : Haptic distinct par categorie via CATEGORY_HAPTIC_FN[catId]
- FEEDBACK-03 : HarvestBurst variant rendu au centre-ecran dans tasks.tsx via CATEGORY_VARIANT[effectCategoryId] (corrige — Plan 01 fournit les donnees, Plan 02 les rend visibles)
- FEEDBACK-04 : Message compagnon contextuel via SecureStore bridge -> subType dans CompanionMessageContext -> SUB_TYPE_TEMPLATES (corrige — subType est peuple a runtime par useGamification et lu par tree.tsx)
- FEEDBACK-05 : Parite FR+EN — 20 cles companion + 10 toast definitions bilingues
- D-02 : Toast + burst en parallele (pas de sequencement)
- D-03 : Silencieux si cappe (effectApplied null -> pas de toast/burst/haptic)
- D-04 : Templates fixes avec sub_type fallback generique
- D-05 : 2 templates par categorie (20 total)
- D-06 : Bulle arbre uniquement (pas de toast compagnon cross-ecran)
</success_criteria>

<output>
After completion, create `.planning/phases/21-feedback-visuel-compagnon/21-02-SUMMARY.md`
</output>
