---
phase: 21-feedback-visuel-compagnon
plan: 02
type: execute
wave: 2
depends_on: ["21-01"]
files_modified:
  - hooks/useGamification.ts
  - app/(tabs)/tasks.tsx
  - lib/mascot/companion-types.ts
  - lib/mascot/companion-engine.ts
  - locales/fr/common.json
  - locales/en/common.json
autonomous: true
requirements: [FEEDBACK-01, FEEDBACK-02, FEEDBACK-03, FEEDBACK-04, FEEDBACK-05]

must_haves:
  truths:
    - "User voit un toast spécifique quand un effet sémantique est déclenché"
    - "User sent un haptic pattern distinct par catégorie"
    - "User lit un message compagnon contextuel par catégorie dans la bulle arbre"
    - "User retrouve la parité FR+EN stricte sur tous les strings"
    - "Pas de toast quand le cap est atteint (D-03)"
  artifacts:
    - path: "hooks/useGamification.ts"
      provides: "Dispatch feedback (toast + haptic) après applyTaskEffect()"
      contains: "EFFECT_TOASTS"
    - path: "app/(tabs)/tasks.tsx"
      provides: "Passage de taskMeta à completeTask"
      contains: "tags: task.tags"
    - path: "lib/mascot/companion-types.ts"
      provides: "subType?: CategoryId dans CompanionMessageContext"
      contains: "subType"
    - path: "lib/mascot/companion-engine.ts"
      provides: "SUB_TYPE_TEMPLATES + pickCompanionMessage étendu"
      contains: "SUB_TYPE_TEMPLATES"
    - path: "locales/fr/common.json"
      provides: "20 clés i18n companion sub-type FR"
      contains: "taskDone_menage_quotidien"
    - path: "locales/en/common.json"
      provides: "20 clés i18n companion sub-type EN"
      contains: "taskDone_menage_quotidien"
  key_links:
    - from: "hooks/useGamification.ts"
      to: "lib/semantic/effect-toasts.ts"
      via: "import EFFECT_TOASTS, CATEGORY_HAPTIC_FN"
      pattern: "EFFECT_TOASTS\\[catId\\]"
    - from: "app/(tabs)/tasks.tsx"
      to: "hooks/useGamification.ts"
      via: "completeTask(activeProfile, task.text, taskMeta)"
      pattern: "tags: task\\.tags"
    - from: "lib/mascot/companion-engine.ts"
      to: "lib/mascot/companion-types.ts"
      via: "context.subType"
      pattern: "context\\.subType"
---

<objective>
Câbler le feedback complet dans completeTask : toast + haptic dispatch, passer taskMeta depuis tasks.tsx, étendre le compagnon avec messages sub-type par catégorie, ajouter les 40 clés i18n FR+EN.

Purpose: Rendre les effets sémantiques visibles et tangibles à l'utilisateur à chaque complétion de tâche.
Output: Feedback complet wired end-to-end (tasks.tsx → useGamification → toast/haptic/companion).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-feedback-visuel-compagnon/21-CONTEXT.md
@.planning/phases/21-feedback-visuel-compagnon/21-RESEARCH.md
@.planning/phases/21-feedback-visuel-compagnon/21-UI-SPEC.md
@.planning/phases/21-feedback-visuel-compagnon/21-01-SUMMARY.md

@hooks/useGamification.ts
@app/(tabs)/tasks.tsx
@lib/mascot/companion-types.ts
@lib/mascot/companion-engine.ts
@lib/semantic/effect-toasts.ts
@lib/semantic/categories.ts
@contexts/ToastContext.tsx
@lib/i18n.ts
@locales/fr/common.json
@locales/en/common.json

<interfaces>
From lib/semantic/effect-toasts.ts (créé Plan 01):
```typescript
export interface EffectToastDef { icon: string; fr: string; en: string; subtitle_fr: string; subtitle_en: string; type: 'success' | 'info'; }
export const EFFECT_TOASTS: Record<CategoryId, EffectToastDef>;
export type HarvestBurstVariant = 'ambient' | 'rare' | 'golden';
export const CATEGORY_VARIANT: Record<CategoryId, HarvestBurstVariant>;
export const CATEGORY_HAPTIC_FN: Record<CategoryId, () => void | Promise<void>>;
```

From hooks/useGamification.ts:
```typescript
completeTask: (profile: Profile, taskText: string, taskMeta?: { tags?: string[]; section?: string; sourceFile?: string }) => Promise<{
  updatedProfile: Profile; lootAwarded: boolean; pointsGained: number; cropsMatured: string[]; effectResult?: EffectResult | null;
}>
```

From contexts/ToastContext.tsx:
```typescript
showToast: (message: string, type?: ToastType, action?: ToastAction, options?: ToastOptions) => void;
// ToastOptions: { icon?: string; subtitle?: string; }
```

From lib/mascot/companion-types.ts:
```typescript
export interface CompanionMessageContext {
  profileName: string; companionName: string; companionSpecies: CompanionSpecies;
  tasksToday: number; streak: number; level: number; /* ... etc */
}
export type CompanionEvent = 'task_completed' | 'loot_opened' | /* ... */;
```

From lib/mascot/companion-engine.ts:
```typescript
export const MESSAGE_TEMPLATES: Record<CompanionEvent, string[]>;
export function pickCompanionMessage(event: CompanionEvent, _context: CompanionMessageContext): string;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dispatch toast + haptic dans useGamification + fix taskMeta dans tasks.tsx</name>
  <files>hooks/useGamification.ts, app/(tabs)/tasks.tsx</files>
  <read_first>
    - hooks/useGamification.ts (COMPLET — comprendre le flux completeTask, le bloc semantic coupling lignes ~204-254, le return statement ligne ~274, les imports existants)
    - app/(tabs)/tasks.tsx (trouver l'appel `completeTask(activeProfile, task.text)` — environ ligne 298 — et comprendre l'objet `task` disponible pour extraire tags/section/sourceFile)
    - contexts/ToastContext.tsx (API showToast — premier 60 lignes pour comprendre ToastOptions interface avec icon + subtitle)
    - lib/semantic/effect-toasts.ts (créé Plan 01 — EFFECT_TOASTS, CATEGORY_HAPTIC_FN)
    - lib/i18n.ts (comment accéder à i18n.language directement — import du singleton)
  </read_first>
  <action>
**Fichier 1 — hooks/useGamification.ts (ÉTENDRE)**

1. Ajouter les imports suivants en haut du fichier (après les imports existants de `lib/semantic`) :
```typescript
import { EFFECT_TOASTS, CATEGORY_HAPTIC_FN } from '../lib/semantic/effect-toasts';
import { useToast } from '../contexts/ToastContext';
import i18n from '../lib/i18n';
```

2. Dans la fonction `useGamification`, ajouter `const { showToast } = useToast();` APRÈS la ligne `const [isProcessing, setIsProcessing] = useState(false);` et AVANT le `useEffect` pour `loadGamiConfig()`. C'est un hook appelant un hook — c'est valide.

3. Ajouter le bloc de dispatch feedback dans `completeTask`, APRÈS le bloc semantic coupling (après la ligne `} catch { if (__DEV__) console.warn('Semantic coupling — non-critical error'); }` vers ligne ~252-254) et AVANT le commentaire `// 3. Ecrire farm-{id}.md` (ligne ~256). Le code de dispatch :

```typescript
        // Phase 21 : Feedback visuel + haptique (FEEDBACK-01, FEEDBACK-02)
        // Toast + haptic immédiat en parallèle (D-02), silencieux si cappé (D-03)
        if (effectResult?.effectApplied) {
          try {
            const category = deriveTaskCategory({
              text: taskText,
              tags: taskMeta?.tags ?? [],
              section: taskMeta?.section,
              sourceFile: taskMeta?.sourceFile ?? '',
            } as Task);
            if (category) {
              const catId = category.id;
              const toastDef = EFFECT_TOASTS[catId];
              if (toastDef) {
                const lang = i18n.language?.startsWith('en') ? 'en' : 'fr';
                showToast(
                  lang === 'en' ? toastDef.en : toastDef.fr,
                  toastDef.type,
                  undefined,
                  { icon: toastDef.icon, subtitle: lang === 'en' ? toastDef.subtitle_en : toastDef.subtitle_fr }
                );
              }
              // Haptic fire-and-forget (non-critical)
              try { CATEGORY_HAPTIC_FN[catId]?.(); } catch { /* haptic — non-critical */ }
            }
          } catch {
            if (__DEV__) console.warn('Effect feedback — non-critical');
          }
        }
```

NOTE IMPORTANTE : `category` est déjà dérivée dans le bloc semantic coupling au-dessus mais elle est dans un scope `try` inaccessible. Au lieu de dupliquer `deriveTaskCategory`, capturer `category` dans une variable extérieure au try. Concrètement :
- Déclarer `let derivedCategory: CategoryMatch | null = null;` juste APRÈS `let effectResult: EffectResult | null = null;` (vers ligne 175)
- Dans le bloc semantic coupling, APRÈS `const category = deriveTaskCategory(task as Task);` (vers ligne 214), ajouter `derivedCategory = category;`
- Utiliser `derivedCategory` au lieu de re-dériver dans le bloc feedback :

```typescript
        if (effectResult?.effectApplied && derivedCategory) {
          try {
            const catId = derivedCategory.id;
            const toastDef = EFFECT_TOASTS[catId];
            if (toastDef) {
              const lang = i18n.language?.startsWith('en') ? 'en' : 'fr';
              showToast(
                lang === 'en' ? toastDef.en : toastDef.fr,
                toastDef.type,
                undefined,
                { icon: toastDef.icon, subtitle: lang === 'en' ? toastDef.subtitle_en : toastDef.subtitle_fr }
              );
            }
            try { CATEGORY_HAPTIC_FN[catId]?.(); } catch { /* haptic — non-critical */ }
          } catch {
            if (__DEV__) console.warn('Effect feedback — non-critical');
          }
        }
```

4. Ajouter `import type { CategoryMatch } from '../lib/semantic';` si pas déjà importé.

5. Ajouter `showToast` à la dépendance array du `useCallback` pour `completeTask` (vers ligne ~285). Ajouter à la fin de la liste existante : `[vault, notifPrefs, onDataChange, onQuestProgress, showToast]`.

**Fichier 2 — app/(tabs)/tasks.tsx (FIX CRITIQUE)**

Trouver l'appel `completeTask(activeProfile, task.text)` (vers ligne 298). Remplacer par :
```typescript
const { lootAwarded, pointsGained } = await completeTask(
  activeProfile,
  task.text,
  { tags: task.tags, section: task.section, sourceFile: task.sourceFile }
);
```

Ceci est le fix critique qui permet au semantic coupling de fonctionner — sans ce taskMeta, `deriveTaskCategory` retourne toujours null.

Vérifier que l'objet `task` a bien les propriétés `tags`, `section`, `sourceFile` (ils sont définis dans le type `Task` de `lib/types.ts`).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - hooks/useGamification.ts contient `import { EFFECT_TOASTS, CATEGORY_HAPTIC_FN } from '../lib/semantic/effect-toasts'`
    - hooks/useGamification.ts contient `import { useToast } from '../contexts/ToastContext'`
    - hooks/useGamification.ts contient `const { showToast } = useToast()`
    - hooks/useGamification.ts contient `let derivedCategory` variable
    - hooks/useGamification.ts contient `EFFECT_TOASTS[catId]`
    - hooks/useGamification.ts contient `CATEGORY_HAPTIC_FN[catId]`
    - hooks/useGamification.ts contient `showToast` dans le useCallback dependency array
    - app/(tabs)/tasks.tsx contient `tags: task.tags, section: task.section, sourceFile: task.sourceFile`
    - `npx tsc --noEmit` passe sans erreur
  </acceptance_criteria>
  <done>Toast + haptic dispatch câblé dans completeTask, taskMeta passé depuis tasks.tsx, feedback silencieux si cap exceeded (D-03)</done>
</task>

<task type="auto">
  <name>Task 2: Messages compagnon sub-type + clés i18n FR+EN</name>
  <files>lib/mascot/companion-types.ts, lib/mascot/companion-engine.ts, locales/fr/common.json, locales/en/common.json</files>
  <read_first>
    - lib/mascot/companion-types.ts (CompanionMessageContext interface — comprendre les champs existants pour ajouter subType)
    - lib/mascot/companion-engine.ts (MESSAGE_TEMPLATES Record type, pickCompanionMessage function — comprendre la logique de sélection et le type Record<CompanionEvent, string[]>)
    - lib/semantic/categories.ts (CategoryId type — les 10 identifiants)
    - locales/fr/common.json (structure companion.msg.* existante — vérifier le format exact des clés taskDone)
    - locales/en/common.json (pareil — vérifier la parité existante)
  </read_first>
  <action>
**Fichier 1 — lib/mascot/companion-types.ts (ÉTENDRE)**

Ajouter `subType?: string;` dans l'interface `CompanionMessageContext` (après le champ `timeOfDay`). Utiliser `string` et non `CategoryId` pour éviter d'importer un type de `lib/semantic` dans `lib/mascot` (pas de couplage ascendant). Le commentaire indique la sémantique :

```typescript
  subType?: string;  // Phase 21 — CategoryId de la tâche sémantique, passé à pickCompanionMessage pour templates sub-type
```

**Fichier 2 — lib/mascot/companion-engine.ts (ÉTENDRE)**

1. Ajouter un dictionnaire séparé `SUB_TYPE_TEMPLATES` APRÈS le `MESSAGE_TEMPLATES` existant (après la ligne `};` qui ferme MESSAGE_TEMPLATES, vers ligne 221). Ne PAS modifier le type de `MESSAGE_TEMPLATES` (reste `Record<CompanionEvent, string[]>`) :

```typescript
/**
 * Templates sub-type pour task_completed par catégorie sémantique (Phase 21).
 * Clés = 'task_completed_${categoryId}' — 2 templates par catégorie (D-05).
 * Dictionnaire séparé pour ne pas élargir le type Record<CompanionEvent, ...>.
 */
export const SUB_TYPE_TEMPLATES: Record<string, string[]> = {
  task_completed_menage_quotidien: [
    'companion.msg.taskDone_menage_quotidien.1',
    'companion.msg.taskDone_menage_quotidien.2',
  ],
  task_completed_menage_hebdo: [
    'companion.msg.taskDone_menage_hebdo.1',
    'companion.msg.taskDone_menage_hebdo.2',
  ],
  task_completed_courses: [
    'companion.msg.taskDone_courses.1',
    'companion.msg.taskDone_courses.2',
  ],
  task_completed_enfants_routines: [
    'companion.msg.taskDone_enfants_routines.1',
    'companion.msg.taskDone_enfants_routines.2',
  ],
  task_completed_enfants_devoirs: [
    'companion.msg.taskDone_enfants_devoirs.1',
    'companion.msg.taskDone_enfants_devoirs.2',
  ],
  task_completed_rendez_vous: [
    'companion.msg.taskDone_rendez_vous.1',
    'companion.msg.taskDone_rendez_vous.2',
  ],
  task_completed_gratitude_famille: [
    'companion.msg.taskDone_gratitude_famille.1',
    'companion.msg.taskDone_gratitude_famille.2',
  ],
  task_completed_budget_admin: [
    'companion.msg.taskDone_budget_admin.1',
    'companion.msg.taskDone_budget_admin.2',
  ],
  task_completed_bebe_soins: [
    'companion.msg.taskDone_bebe_soins.1',
    'companion.msg.taskDone_bebe_soins.2',
  ],
  task_completed_cuisine_repas: [
    'companion.msg.taskDone_cuisine_repas.1',
    'companion.msg.taskDone_cuisine_repas.2',
  ],
};
```

2. Modifier `pickCompanionMessage` pour résoudre les sub-types AVANT le fallback générique :

```typescript
export function pickCompanionMessage(
  event: CompanionEvent,
  context: CompanionMessageContext,
): string {
  // Phase 21 : Sub-type lookup pour task_completed (D-04)
  if (event === 'task_completed' && context.subType) {
    const subKey = `task_completed_${context.subType}`;
    const subTemplates = SUB_TYPE_TEMPLATES[subKey];
    if (subTemplates && subTemplates.length > 0) {
      const idx = Math.floor(Math.random() * subTemplates.length);
      return subTemplates[idx];
    }
  }
  // Fallback — comportement existant
  const templates = MESSAGE_TEMPLATES[event];
  if (!templates || templates.length === 0) {
    return MESSAGE_TEMPLATES.greeting[0];
  }
  const idx = Math.floor(Math.random() * templates.length);
  return templates[idx];
}
```

Changer `_context` en `context` dans la signature (enlever le underscore) puisqu'on l'utilise maintenant.

3. Dans `eventDescriptions` (vers ligne 276), mettre à jour la description pour `task_completed` pour inclure le subType si présent :

```typescript
task_completed: context.subType
  ? `${ctx.profileName} vient de compléter une tâche (catégorie: ${context.subType}, ${ctx.tasksToday} aujourd'hui)`
  : `${ctx.profileName} vient de compléter des tâches (${ctx.tasksToday} aujourd'hui)`,
```

Note : la variable s'appelle `ctx` dans `eventDescriptions` (pas `context`). Vérifier le nommage exact dans le code.

**Fichier 3 — locales/fr/common.json (ÉTENDRE)**

Ajouter 20 clés i18n APRÈS le bloc `taskDone` existant (dans `companion.msg`). Format : `"taskDone_${categoryId}": { "1": "...", "2": "..." }`. Toutes les phrases suivent les contraintes du companion : 1 phrase courte naturelle, pas d'emoji, tutoie, pas de guillemets, référence la catégorie ET l'effet :

```json
"taskDone_menage_quotidien": {
  "1": "Le ménage est fait, les mauvaises herbes ont disparu de la parcelle !",
  "2": "Bien joué pour le ménage quotidien, la ferme respire mieux sans ces herbes !"
},
"taskDone_menage_hebdo": {
  "1": "Le grand nettoyage a payé, une usure réparée sur la ferme !",
  "2": "Grâce au ménage en profondeur, la ferme est comme neuve !"
},
"taskDone_courses": {
  "1": "Les courses sont rangées et les bâtiments tournent à plein régime !",
  "2": "Bien joué pour les courses, la production des bâtiments est boostée !"
},
"taskDone_enfants_routines": {
  "1": "Les routines sont faites, {{companionName}} déborde de joie !",
  "2": "Grâce aux routines des enfants, {{companionName}} est au top de sa forme !"
},
"taskDone_enfants_devoirs": {
  "1": "Les devoirs sont faits, les cultures poussent plus vite maintenant !",
  "2": "Bien joué pour les devoirs, le sprint de croissance est lancé !"
},
"taskDone_rendez_vous": {
  "1": "Le rendez-vous est passé et une graine rare est apparue dans le jardin !",
  "2": "Bravo pour le rendez-vous, une récolte précieuse attend dans la ferme !"
},
"taskDone_gratitude_famille": {
  "1": "Ce moment de gratitude a renforcé le voyageur de la saga !",
  "2": "Grâce à cette attention familiale, le trait de saga a grandi !"
},
"taskDone_budget_admin": {
  "1": "Le budget est réglé et les bâtiments ont doublé leur capacité !",
  "2": "Bien joué pour l'admin, les bâtiments se remplissent deux fois plus vite !"
},
"taskDone_bebe_soins": {
  "1": "Les soins sont donnés et la prochaine récolte sera dorée !",
  "2": "Grâce aux soins du bébé, une pluie dorée attend à la récolte !"
},
"taskDone_cuisine_repas": {
  "1": "Le repas est prêt et une recette rare a été débloquée !",
  "2": "Bien joué en cuisine, un nouveau craft est disponible !"
}
```

**Fichier 4 — locales/en/common.json (ÉTENDRE)**

Ajouter les 20 clés équivalentes EN PARITÉ STRICTE (FEEDBACK-05) dans le même emplacement :

```json
"taskDone_menage_quotidien": {
  "1": "Housework done, the weeds have vanished from the plot!",
  "2": "Nice daily cleaning, the farm breathes better without those weeds!"
},
"taskDone_menage_hebdo": {
  "1": "Deep cleaning paid off, a wear event is repaired on the farm!",
  "2": "Thanks to the thorough cleaning, the farm is good as new!"
},
"taskDone_courses": {
  "1": "Shopping is done and the buildings are running at full speed!",
  "2": "Great job on the shopping, building production is boosted!"
},
"taskDone_enfants_routines": {
  "1": "Routines are done, {{companionName}} is bursting with joy!",
  "2": "Thanks to the kids' routines, {{companionName}} is in top shape!"
},
"taskDone_enfants_devoirs": {
  "1": "Homework is done, crops are growing faster now!",
  "2": "Great job on homework, the growth sprint is on!"
},
"taskDone_rendez_vous": {
  "1": "Appointment done and a rare seed appeared in the garden!",
  "2": "Well done on the appointment, a precious harvest awaits on the farm!"
},
"taskDone_gratitude_famille": {
  "1": "This moment of gratitude empowered the saga traveler!",
  "2": "Thanks to this family attention, the saga trait has grown!"
},
"taskDone_budget_admin": {
  "1": "Budget is settled and buildings doubled their capacity!",
  "2": "Great admin work, buildings fill up twice as fast!"
},
"taskDone_bebe_soins": {
  "1": "Baby care done and the next harvest will be golden!",
  "2": "Thanks to baby care, a golden rain awaits at harvest!"
},
"taskDone_cuisine_repas": {
  "1": "Meal is ready and a rare recipe has been unlocked!",
  "2": "Great cooking, a new craft is available!"
}
```
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - lib/mascot/companion-types.ts contient `subType?: string` dans CompanionMessageContext
    - lib/mascot/companion-engine.ts contient `export const SUB_TYPE_TEMPLATES: Record<string, string[]>`
    - lib/mascot/companion-engine.ts contient `task_completed_menage_quotidien` dans SUB_TYPE_TEMPLATES
    - lib/mascot/companion-engine.ts pickCompanionMessage contient `context.subType` (pas `_context`)
    - lib/mascot/companion-engine.ts pickCompanionMessage contient `SUB_TYPE_TEMPLATES[subKey]`
    - locales/fr/common.json contient `taskDone_menage_quotidien` avec clés "1" et "2"
    - locales/fr/common.json contient `taskDone_cuisine_repas` avec clés "1" et "2"
    - locales/en/common.json contient `taskDone_menage_quotidien` avec clés "1" et "2"
    - locales/en/common.json contient `taskDone_cuisine_repas` avec clés "1" et "2"
    - Nombre exact de 10 entrées dans SUB_TYPE_TEMPLATES (10 task_completed_* keys)
    - FR et EN ont le même nombre de clés taskDone_* (parité stricte FEEDBACK-05)
    - `npx tsc --noEmit` passe sans erreur
  </acceptance_criteria>
  <done>Companion sub-type messages câblés (20 templates i18n FR+EN), pickCompanionMessage résout sub-type avant fallback, taskMeta passé depuis tasks.tsx, toast+haptic dispatch fonctionnel</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passe sans erreur
- `npx jest --no-coverage` passe (tests existants non cassés)
- tasks.tsx passe taskMeta à completeTask
- useGamification dispatch toast + haptic après effectApplied
- companion-engine résout sub-type templates avant fallback
- Parité FR+EN stricte : 10 clés taskDone_* dans chaque locale
</verification>

<success_criteria>
- FEEDBACK-01 : Toast spécifique par effet via EFFECT_TOASTS[catId] + showToast
- FEEDBACK-02 : Haptic distinct par catégorie via CATEGORY_HAPTIC_FN[catId]
- FEEDBACK-03 : HarvestBurst variant retourné dans effectResult (wired Plan 01)
- FEEDBACK-04 : Message compagnon contextuel via subType dans CompanionMessageContext + SUB_TYPE_TEMPLATES
- FEEDBACK-05 : Parité FR+EN — 20 clés companion + 10 toast definitions bilingues
- D-02 : Toast + burst en parallèle (pas de séquencement)
- D-03 : Silencieux si cappé (effectApplied null → pas de toast)
- D-04 : Templates fixes avec sub_type fallback générique
- D-05 : 2 templates par catégorie (20 total)
- D-06 : Bulle arbre uniquement (pas de toast compagnon cross-écran)
</success_criteria>

<output>
After completion, create `.planning/phases/21-feedback-visuel-compagnon/21-02-SUMMARY.md`
</output>
