---
phase: 44-auberge-b-timent-branche-tech-social
plan: 04
type: execute
wave: 2
depends_on:
  - 44-01
files_modified:
  - components/mascot/BuildingDetailSheet.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "BuildingDetailSheet lit def.producesResource au début du render"
    - "Si def.producesResource === false : le hero card production est caché (pas de pendingCount, pas de progress bar, pas de timer)"
    - "Si def.producesResource === false : le CTA 'Collecter' est caché"
    - "Si def.producesResource === false : un placeholder est affiché à la place : icône + texte 'Ce bâtiment ne produit pas de ressources passives. Voir l'intérieur prochainement.'"
    - "Si def.producesResource === false : le panneau d'upgrade reste affiché (l'auberge est upgradable)"
    - "Pour les 4 bâtiments existants (poulailler/grange/moulin/ruche), l'affichage est strictement inchangé (rétrocompat UI)"
    - "Aucun calcul (getPendingResources, getMinutesUntilNext) n'est invoqué pour un bâtiment non-productif (les gardes Plan 44-01 garantissent 0 mais on n'appelle même pas)"
    - "npx tsc --noEmit ne dégrade pas la liste d'erreurs"
  artifacts:
    - path: "components/mascot/BuildingDetailSheet.tsx"
      provides: "Branchement non-productif gracieux"
      contains: "producesResource"
  key_links:
    - from: "BuildingDetailSheet.tsx"
      to: "BuildingDefinition.producesResource (Plan 44-01)"
      via: "lecture directe via def.producesResource === false"
      pattern: "producesResource"
---

<objective>
Adapter `components/mascot/BuildingDetailSheet.tsx` pour gérer gracieusement un bâtiment avec `producesResource: false` : cacher le hero card production + le CTA Collecte, afficher un placeholder informatif "Voir l'intérieur prochainement" (texte FR direct, sans i18n — la phase 45 routera vers `AubergeSheet`). Le panneau d'upgrade reste affiché.

Purpose: Sans cette adaptation, l'utilisateur qui tape sur le bâtiment Auberge construit Phase 44-02 verrait un panneau de production vide / 0 ressource / timer figé. L'expérience doit rester propre même si l'UI Auberge n'arrive qu'en Phase 45.

Output: Un seul fichier modifié, ajout d'un branchement conditionnel + un nouveau bloc placeholder.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/44-auberge-b-timent-branche-tech-social/44-CONTEXT.md
@.planning/phases/44-auberge-b-timent-branche-tech-social/44-01-PLAN.md
@components/mascot/BuildingDetailSheet.tsx
@CLAUDE.md
</context>

<interfaces>
**Modification logique (BuildingDetailSheet.tsx) :**

```typescript
const def = BUILDING_CATALOG.find(d => d.id === building.buildingId);
if (!def) return null;

// Phase 44 — Branchement non-productif
const isProductive = def.producesResource !== false; // undefined ou true = productif (rétrocompat)

// ...

return (
  <Modal ...>
    <View style={styles.overlay}>
      ...
      <ScrollView ...>
        {/* Banner toit endommagé — uniquement si productif */}
        {isProductive && isDamaged && onRepairRoof && (...) }

        {isProductive ? (
          <>
            {/* Hero card production existante */}
            {/* CTA collecte existante */}
          </>
        ) : (
          <View style={styles.nonProductiveCard}>
            <Text style={styles.nonProductiveIcon}>🛖</Text>
            <Text style={styles.nonProductiveTitle}>Bâtiment social</Text>
            <Text style={styles.nonProductiveBody}>
              Ce bâtiment ne produit pas de ressources passives. Voir l'intérieur prochainement.
            </Text>
          </View>
        )}

        {/* Section upgrade — toujours affichée */}
        ...
      </ScrollView>
    </View>
  </Modal>
);
```

**Notes :**
- Les calculs `pendingCount`, `minutesUntilNext`, `progressRatio`, `currentCycleHours` peuvent rester avant le `return` — Plan 44-01 garantit qu'ils retournent 0 pour un non-productif. Mais idéalement on les wrap dans un `if (isProductive)` pour éviter du calcul inutile. Les variables `nextTier`, `upgradable`, `upgradeCost`, `nextCycleHours`, `nextMaxPending` etc. doivent rester (le panneau d'upgrade les utilise).
- Le toit endommagé (`isDamaged`) ne s'applique pas à un bâtiment non-productif — cacher la banner.
- Garder `t(def.labelKey)` pour le titre (l'auberge a sa clé i18n via Plan 44-02).
- `labels.subtitle` lit `RESOURCE_LABELS[def.resourceType]?.subtitle` ; pour l'auberge `resourceType: 'oeuf'` retournerait "Production d'œufs" → INCORRECT. Override : si non-productif, override `subtitle` à "Bâtiment social".

**Styles à ajouter (en bas du StyleSheet) :**

```typescript
nonProductiveCard: {
  backgroundColor: T.surface2,
  borderRadius: Radius.lg,
  borderWidth: 1,
  borderColor: T.accentLine,
  padding: Spacing.xl,
  alignItems: 'center',
  ...Shadows.sm,
},
nonProductiveIcon: {
  fontSize: 32,
  marginBottom: Spacing.sm,
},
nonProductiveTitle: {
  fontSize: 15,
  fontWeight: '800',
  color: T.ink,
  marginBottom: 4,
},
nonProductiveBody: {
  fontSize: 13,
  color: T.ink3,
  textAlign: 'center',
  fontWeight: '500',
  lineHeight: 18,
},
```
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Brancher l'affichage gracieux non-productif dans BuildingDetailSheet</name>
  <files>components/mascot/BuildingDetailSheet.tsx</files>
  <read_first>
    - components/mascot/BuildingDetailSheet.tsx (intégral — 1010 lignes : repérer le bloc render lignes 285-553 et le StyleSheet 555-1010)
    - lib/mascot/types.ts (BuildingDefinition.producesResource — Plan 44-01)
    - .planning/phases/44-auberge-b-timent-branche-tech-social/44-CONTEXT.md (section "BuildingDetailSheet — gestion gracieuse")
  </read_first>
  <behavior>
    - Une variable `isProductive = def.producesResource !== false` est calculée juste après le check `if (!def) return null;`.
    - Tous les calculs liés à la production (`pendingCount`, `minutesUntilNext`, `progressRatio`, `currentCycleHours`, etc.) sont conservés tels quels (Plan 44-01 retourne 0 — pas de NaN ni crash).
    - Le subtitle est overridé à "Bâtiment social" si `!isProductive` (au lieu de "Production d'œufs" qui serait erroné).
    - La banner "Toit endommagé" est cachée si `!isProductive` (un bâtiment non-productif n'a pas de wear pertinent).
    - Le `<>` fragment hero card + CTA collecte est wrappé dans `{isProductive ? (...) : (<placeholder />)}`.
    - Le panneau upgrade (lignes 489-547 actuellement) reste affiché dans tous les cas.
    - Aucune modification des styles existants — seulement 4 nouveaux styles ajoutés en fin de StyleSheet.
    - Texte FR direct dans le placeholder (pas d'i18n key — choix discrétionnaire confirmé par CONTEXT.md).
  </behavior>
  <action>
    Dans `components/mascot/BuildingDetailSheet.tsx` :

    1. **Après la ligne 288** (`if (!def) return null;`), ajouter :
       ```typescript
       // Phase 44 — Bâtiment non-productif (Auberge & co.) : affichage gracieux
       const isProductive = def.producesResource !== false;
       ```

    2. **Ligne 322 (subtitle override)** : remplacer le bloc :
       ```typescript
       const labels = RESOURCE_LABELS[def.resourceType] ?? { ... };
       ```
       par :
       ```typescript
       const labels = RESOURCE_LABELS[def.resourceType] ?? {
         name: def.resourceType,
         produced: def.resourceType,
         nextLabel: 'Prochaine ressource dans',
         cycleLabel: '1 unité toutes les',
         subtitle: 'Production',
       };
       // Phase 44 — override subtitle pour les bâtiments non-productifs
       const titleSubtitle = isProductive ? labels.subtitle : 'Bâtiment social';
       ```
       Puis dans le JSX (ligne ≈ 391), remplacer `<Text style={styles.titleSub}>{labels.subtitle}</Text>` par `<Text style={styles.titleSub}>{titleSubtitle}</Text>`.

    3. **Banner toit endommagé (lignes 401-427)** : wrapper la condition avec `isProductive` :
       ```typescript
       {isProductive && isDamaged && onRepairRoof && (
         <Animated.View ...>
           ...
         </Animated.View>
       )}
       ```

    4. **Hero card + CTA collecte (lignes 430-486)** : remplacer le bloc actuel par un ternaire :
       ```typescript
       {isProductive ? (
         <>
           {/* Hero card — production actuelle */}
           <Animated.View
             entering={FadeIn.delay(100).springify().damping(12).stiffness(180)}
             style={styles.heroCard}
           >
             {/* ...contenu existant inchangé... */}
           </Animated.View>

           {/* CTA collecte */}
           <Animated.View
             entering={FadeIn.delay(160).springify().damping(12).stiffness(180)}
           >
             <CTA ... />
           </Animated.View>
         </>
       ) : (
         <Animated.View
           entering={FadeIn.delay(100).springify().damping(12).stiffness(180)}
           style={styles.nonProductiveCard}
         >
           <Text style={styles.nonProductiveIcon}>🛖</Text>
           <Text style={styles.nonProductiveTitle}>Bâtiment social</Text>
           <Text style={styles.nonProductiveBody}>
             Ce bâtiment ne produit pas de ressources passives. Voir l'intérieur prochainement.
           </Text>
         </Animated.View>
       )}
       ```

    5. **Section upgrade (lignes 488-547)** : ne PAS modifier — reste identique, affichée dans tous les cas.

    6. **StyleSheet (avant la `}` finale, ≈ ligne 1010)** : ajouter les 4 nouveaux styles :
       ```typescript
       // ── Non-productif (Phase 44) ─────────────────────
       nonProductiveCard: {
         backgroundColor: T.surface2,
         borderRadius: Radius.lg,
         borderWidth: 1,
         borderColor: T.accentLine,
         padding: Spacing.xl,
         alignItems: 'center',
         ...Shadows.sm,
       },
       nonProductiveIcon: {
         fontSize: 32,
         marginBottom: Spacing.sm,
       },
       nonProductiveTitle: {
         fontSize: 15,
         fontWeight: '800',
         color: T.ink,
         marginBottom: 4,
       },
       nonProductiveBody: {
         fontSize: 13,
         color: T.ink3,
         textAlign: 'center',
         fontWeight: '500',
         lineHeight: 18,
       },
       ```

    **Vérifications post-write :**
    - `npx tsc --noEmit` clean.
    - Grep `isProductive` dans le fichier : ≥ 3 occurrences (déclaration + 3 usages : subtitle, banner, hero/CTA).
  </action>
  <verify>
    <automated>grep -q "const isProductive = def.producesResource !== false" components/mascot/BuildingDetailSheet.tsx &amp;&amp; grep -q "Bâtiment social" components/mascot/BuildingDetailSheet.tsx &amp;&amp; grep -q "nonProductiveCard" components/mascot/BuildingDetailSheet.tsx &amp;&amp; grep -q "Voir l'intérieur prochainement" components/mascot/BuildingDetailSheet.tsx &amp;&amp; npx tsc --noEmit 2>&amp;1 | grep -c "error TS" | awk '{ exit !($1 <= 5) }'</automated>
  </verify>
  <acceptance_criteria>
    - Variable `isProductive` déclarée.
    - Texte FR placeholder présent ("Bâtiment social", "Voir l'intérieur prochainement").
    - 4 styles `nonProductive*` ajoutés.
    - `npx tsc --noEmit` clean.
    - Aucune modification du panneau upgrade (grep `upgradeSection`, `upgradeFooter` montre les styles intacts).
  </acceptance_criteria>
  <done>
    Quand l'utilisateur ouvrira le `BuildingDetailSheet` sur un Auberge construit (Phase 44-02), il verra : header normal + sprite emoji 🛖 + subtitle "Bâtiment social" + carte placeholder "Voir l'intérieur prochainement" + panneau upgrade (1500 → niveau 2). Aucune erreur, aucun timer cassé.
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` clean.
- Test mental sur les 4 bâtiments existants : `producesResource` undefined → `isProductive = true` → comportement intégralement préservé.
- Test mental sur auberge (post-Plan 44-02) : `producesResource: false` → `isProductive = false` → placeholder affiché, panneau upgrade affiché.
- Vérification visuelle dev-client : optionnelle pour cette phase (pas de bloquant — type-check + cohérence logique suffisent en Phase 44 ; Phase 45 livrera la vraie UI).
</verification>

<success_criteria>
- Build TypeScript OK.
- BuildingDetailSheet gère 2 modes : productif (existant) + non-productif (nouveau).
- Aucune régression UI sur poulailler/grange/moulin/ruche.
- Commit FR atomique : `feat(44): BuildingDetailSheet — affichage gracieux non-productif (auberge)`.
</success_criteria>

<output>
.planning/phases/44-auberge-b-timent-branche-tech-social/44-04-SUMMARY.md avec :
- Confirmation des 4 modifications du fichier (variable + subtitle override + banner wrap + hero/CTA ternaire + styles).
- Note sur la vérification visuelle effectuée ou non.
- Confirmation tsc clean.
</output>
