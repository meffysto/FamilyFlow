---
phase: 44-auberge-b-timent-branche-tech-social
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/mascot/types.ts
  - lib/mascot/building-engine.ts
  - lib/__tests__/building-engine-non-productive.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "BuildingDefinition expose un champ optionnel `producesResource?: boolean` (default-treat undefined === true)"
    - "getPendingResources retourne 0 si def.producesResource === false (court-circuit avant tout calcul)"
    - "getMinutesUntilNext retourne 0 si def.producesResource === false"
    - "collectBuilding retourne { buildings, inventory, collected: 0 } sans muter rien si def.producesResource === false"
    - "Les 4 bâtiments existants (poulailler, grange, moulin, ruche) continuent de produire normalement (rétrocompat — pas de champ producesResource ajouté à leurs entrées)"
    - "getUpgradeCost et canUpgrade restent fonctionnels même pour un bâtiment non-productif (l'auberge est upgradable)"
    - "npx tsc --noEmit ne dégrade pas la liste pré-existante d'erreurs"
    - "npx jest --no-coverage lib/__tests__/building-engine-non-productive.test.ts passe (3+ tests verts)"
  artifacts:
    - path: "lib/mascot/types.ts"
      provides: "Champ producesResource? sur BuildingDefinition"
      contains: "producesResource"
    - path: "lib/mascot/building-engine.ts"
      provides: "Court-circuit non-productif dans getPendingResources / getMinutesUntilNext / collectBuilding"
      contains: "producesResource === false"
    - path: "lib/__tests__/building-engine-non-productive.test.ts"
      provides: "Tests Jest non-régression du court-circuit"
      contains: "producesResource"
  key_links:
    - from: "lib/mascot/building-engine.ts"
      to: "BuildingDefinition.producesResource"
      via: "lecture du champ via def.producesResource === false"
      pattern: "producesResource === false"
---

<objective>
Refacto rétrocompatible de `BuildingDefinition` : ajout d'un champ optionnel `producesResource?: boolean` (default-treat `undefined === true`) puis court-circuit dans les 3 fonctions productrices de `lib/mascot/building-engine.ts` (`getPendingResources`, `getMinutesUntilNext`, `collectBuilding`). Tests Jest dédiés couvrant le no-op pour un fake bâtiment non-productif.

Purpose: Plan « interface-first » qui débloque le Plan 02 (ajout entrée auberge dans BUILDING_CATALOG) et le Plan 04 (BuildingDetailSheet gracieux). La rétrocompat est non-négociable — les 4 bâtiments existants (poulailler/grange/moulin/ruche) ne doivent pas régresser.

Output: 1 type étendu + 3 fonctions modifiées (3 lignes de garde) + 1 fichier de tests neuf.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/44-auberge-b-timent-branche-tech-social/44-CONTEXT.md
@lib/mascot/types.ts
@lib/mascot/building-engine.ts
@CLAUDE.md
</context>

<interfaces>
**Champ à ajouter à `BuildingDefinition` (lib/mascot/types.ts:422-432) :**

```typescript
export interface BuildingDefinition {
  id: string;
  labelKey: string;
  emoji: string;
  cost: number;
  dailyIncome: number;
  minTreeStage: TreeStage;
  resourceType: ResourceType;
  tiers: BuildingTier[];
  techRequired?: string;
  /** Phase 44 — false = bâtiment décoratif/social (Auberge), aucune ressource passive.
   *  undefined ou true = comportement historique (production active). */
  producesResource?: boolean;
}
```

**Pattern de court-circuit à appliquer (3 fonctions de `lib/mascot/building-engine.ts`) :**

```typescript
export function getPendingResources(building, now, ...): number {
  const def = BUILDING_CATALOG.find(d => d.id === building.buildingId);
  if (!def) return 0;
  if (def.producesResource === false) return 0; // ← Phase 44
  // ...reste inchangé
}

export function getMinutesUntilNext(building, now, ...): number {
  const def = BUILDING_CATALOG.find(d => d.id === building.buildingId);
  if (!def) return 0;
  if (def.producesResource === false) return 0; // ← Phase 44
  // ...reste inchangé
}

export function collectBuilding(buildings, inventory, cellId, now, ...) {
  const building = buildings.find(b => b.cellId === cellId);
  if (!building) return { buildings, inventory, collected: 0 };
  const def = BUILDING_CATALOG.find(d => d.id === building.buildingId);
  if (!def) return { buildings, inventory, collected: 0 };
  if (def.producesResource === false) return { buildings, inventory, collected: 0 }; // ← Phase 44
  // ...reste inchangé
}
```

**Note :** `getUpgradeCost` et `canUpgrade` ne sont PAS modifiés — l'Auberge doit rester upgradable.
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Ajouter le flag producesResource à BuildingDefinition + court-circuit dans building-engine</name>
  <files>lib/mascot/types.ts, lib/mascot/building-engine.ts</files>
  <read_first>
    - lib/mascot/types.ts:422-432 (interface BuildingDefinition)
    - lib/mascot/building-engine.ts:140-235 (getPendingResources, getMinutesUntilNext, collectBuilding)
    - .planning/phases/44-auberge-b-timent-branche-tech-social/44-CONTEXT.md (section "Refacto producesResource")
  </read_first>
  <behavior>
    - `BuildingDefinition.producesResource?: boolean` ajouté avec JSDoc FR.
    - Le default-treat `undefined === true` est implicite : aucun changement aux 4 entrées du `BUILDING_CATALOG` existant — elles n'ont pas le champ et continuent à produire.
    - Les 3 fonctions productrices ajoutent une garde `if (def.producesResource === false)` JUSTE APRÈS le check `if (!def)`, AVANT tout autre calcul (tier, wear, multiplicateurs).
    - `getUpgradeCost` et `canUpgrade` ne sont pas touchés.
    - `npx tsc --noEmit` reste clean.
  </behavior>
  <action>
    1. **types.ts** : dans l'interface `BuildingDefinition` (lib/mascot/types.ts:422-432), ajouter le champ optionnel à la fin (après `techRequired?: string;`) :
       ```typescript
       /** Phase 44 — false = bâtiment décoratif/social (Auberge), aucune ressource passive.
        *  undefined ou true = comportement historique (production active). */
       producesResource?: boolean;
       ```
       NE PAS modifier les 4 entrées du `BUILDING_CATALOG` (poulailler, grange, moulin, ruche) — leur absence du champ vaut `true` (rétrocompat).

    2. **building-engine.ts** : ajouter la garde dans 3 fonctions :
       - `getPendingResources` (≈ ligne 148) : APRÈS `if (!def) return 0;`, ajouter `if (def.producesResource === false) return 0;`
       - `getMinutesUntilNext` (≈ ligne 174) : APRÈS `if (!def) return 0;`, ajouter `if (def.producesResource === false) return 0;`
       - `collectBuilding` (≈ ligne 208) : APRÈS le second `if (!def) return { buildings, inventory, collected: 0 };`, ajouter `if (def.producesResource === false) return { buildings, inventory, collected: 0 };`

    3. **NE PAS toucher** `getUpgradeCost` (ligne 239) ni `canUpgrade` (ligne 247) — l'Auberge restera upgradable.

    4. Vérifier : `npx tsc --noEmit` clean, `npx jest --no-coverage lib/__tests__/farm-engine.test.ts` reste vert (non-régression poulailler/grange/moulin/ruche déjà couverts par farm-engine.test.ts si tests existants).
  </action>
  <verify>
    <automated>grep -q "producesResource?: boolean" lib/mascot/types.ts &amp;&amp; grep -c "def.producesResource === false" lib/mascot/building-engine.ts | awk '{ exit !($1 == 3) }' &amp;&amp; npx tsc --noEmit 2>&amp;1 | grep -c "error TS" | awk '{ exit !($1 <= 5) }'</automated>
  </verify>
  <acceptance_criteria>
    - `producesResource?: boolean` exporté dans BuildingDefinition (grep).
    - 3 occurrences exactes de `def.producesResource === false` dans building-engine.ts (une par fonction).
    - Aucune entrée existante du BUILDING_CATALOG ne contient `producesResource:` (rétrocompat strict).
    - `npx tsc --noEmit` ne dégrade pas le baseline.
  </acceptance_criteria>
  <done>
    Le contrat est en place : un futur bâtiment avec `producesResource: false` sera silencieusement ignoré par les 3 fonctions productrices. Plan 02 peut ajouter l'entrée auberge.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tests Jest non-régression — court-circuit non-productif</name>
  <files>lib/__tests__/building-engine-non-productive.test.ts</files>
  <read_first>
    - lib/__tests__/farm-engine.test.ts (pattern de tests existant — repérer comment un PlacedBuilding fake est construit, comment BUILDING_CATALOG est étendu in-test ou comment un mock est injecté)
    - lib/mascot/types.ts (PlacedBuilding shape : buildingId, cellId, level, lastCollectAt)
    - lib/mascot/building-engine.ts (signatures des 3 fonctions)
  </read_first>
  <behavior>
    - Test 1 : sur `poulailler` (def existant, sans flag → défaut true), avec `lastCollectAt` ancien (24h dans le passé), `getPendingResources` retourne > 0 (rétrocompat OK).
    - Test 2 : court-circuit `getPendingResources` — pour un buildingId dont `BUILDING_CATALOG` a `producesResource: false`, retourne 0 même avec lastCollectAt très ancien.
    - Test 3 : court-circuit `getMinutesUntilNext` — retourne 0 pour un bâtiment non-productif.
    - Test 4 : court-circuit `collectBuilding` — retourne `{ buildings: <inchangé>, inventory: <inchangé>, collected: 0 }`. La référence ne doit PAS être identique (le moteur peut retourner une copie shallow), mais le contenu doit être strictement égal au input.

    **Pattern d'injection du fake :** comme BUILDING_CATALOG est exporté en `const`, il faut soit (a) push une entrée fake en hook beforeAll/afterAll avec restore (push + splice), soit (b) ne tester que sur l'entrée auberge AJOUTÉE par Plan 02 — mais Plan 02 dépend de ce plan. **Choix retenu** : option (a) — push/restore en setup/teardown.
  </behavior>
  <action>
    Créer `lib/__tests__/building-engine-non-productive.test.ts`. Pattern :

    ```typescript
    import {
      getPendingResources,
      getMinutesUntilNext,
      collectBuilding,
    } from '../mascot/building-engine';
    import { BUILDING_CATALOG, type PlacedBuilding, type FarmInventory } from '../mascot/types';

    const FAKE_ID = 'auberge_test_fixture';
    const FAKE_DEF = {
      id: FAKE_ID,
      labelKey: 'farm.building.fake',
      emoji: '🛖',
      cost: 0,
      dailyIncome: 0,
      minTreeStage: 'pousse' as const,
      resourceType: 'oeuf' as const, // non-utilisé mais requis par le type
      producesResource: false,
      tiers: Array.from({ length: 10 }, (_, i) => ({
        level: i + 1,
        productionRateHours: 8,
        upgradeCoins: i === 0 ? 0 : 100 * (i + 1),
        spriteSuffix: '',
      })),
    };

    describe('building-engine — court-circuit producesResource: false', () => {
      beforeAll(() => {
        BUILDING_CATALOG.push(FAKE_DEF as any);
      });
      afterAll(() => {
        const idx = BUILDING_CATALOG.findIndex(b => b.id === FAKE_ID);
        if (idx >= 0) BUILDING_CATALOG.splice(idx, 1);
      });

      const fakePlaced: PlacedBuilding = {
        buildingId: FAKE_ID,
        cellId: 'b0',
        level: 1,
        lastCollectAt: new Date(Date.now() - 1000 * 60 * 60 * 48).toISOString(), // -48h
      };

      it('getPendingResources retourne 0 pour un bâtiment non-productif (même avec 48h écoulées)', () => {
        expect(getPendingResources(fakePlaced)).toBe(0);
      });

      it('getMinutesUntilNext retourne 0 pour un bâtiment non-productif', () => {
        expect(getMinutesUntilNext(fakePlaced)).toBe(0);
      });

      it('collectBuilding ne mute rien et retourne collected: 0', () => {
        const inventory: FarmInventory = { oeuf: 5, lait: 2, farine: 0, miel: 1 };
        const buildings: PlacedBuilding[] = [fakePlaced];
        const result = collectBuilding(buildings, inventory, 'b0');
        expect(result.collected).toBe(0);
        expect(result.inventory).toEqual(inventory);
        expect(result.buildings).toEqual(buildings);
      });

      it('rétrocompat : poulailler (sans flag producesResource) produit toujours après 24h', () => {
        const placed: PlacedBuilding = {
          buildingId: 'poulailler',
          cellId: 'b1',
          level: 1,
          lastCollectAt: new Date(Date.now() - 1000 * 60 * 60 * 24).toISOString(),
        };
        expect(getPendingResources(placed)).toBeGreaterThan(0);
      });
    });
    ```

    **Convention :** tous les noms de tests en français (pattern existant). Pas d'import de `BUILDING_CATALOG` en `as const` — on push/splice en runtime.
  </action>
  <verify>
    <automated>npx jest --no-coverage lib/__tests__/building-engine-non-productive.test.ts 2>&amp;1 | grep -q "PASS\|passed"</automated>
  </verify>
  <acceptance_criteria>
    - Fichier `lib/__tests__/building-engine-non-productive.test.ts` créé.
    - 4 tests verts (3 court-circuit + 1 rétrocompat poulailler).
    - `npx jest --no-coverage` global ne dégrade aucun test pré-existant (vérification post-run).
  </acceptance_criteria>
  <done>
    Le contrat de court-circuit est verrouillé par tests. Tout futur refacto qui casserait la garde sera détecté immédiatement.
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` clean (pas de nouvelles erreurs).
- `npx jest --no-coverage lib/__tests__/building-engine-non-productive.test.ts` : 4/4 verts.
- `npx jest --no-coverage lib/__tests__/farm-engine.test.ts` (s'il existe) : aucun test régressé.
- Grep `producesResource: false` dans BUILDING_CATALOG : 0 occurrences (rétrocompat préservée — l'auberge sera ajoutée Plan 02).
</verification>

<success_criteria>
- Build TypeScript OK.
- Tests Jest dédiés verts.
- Plan 02 peut ajouter l'entrée `auberge` à BUILDING_CATALOG sans risque de régression.
- Plan 04 peut lire `def.producesResource` côté UI pour basculer en mode "voir l'auberge".
- Commit FR atomique : `feat(44): flag producesResource + court-circuit building-engine + tests`.
</success_criteria>

<output>
.planning/phases/44-auberge-b-timent-branche-tech-social/44-01-SUMMARY.md avec :
- Confirmation des 3 gardes ajoutées (lignes exactes).
- Résultat des 4 tests Jest.
- Confirmation rétrocompat (BUILDING_CATALOG existant non modifié).
</output>
