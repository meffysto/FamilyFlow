---
phase: 44-auberge-b-timent-branche-tech-social
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/mascot/tech-engine.ts
  - locales/fr/common.json
  - lib/__tests__/tech-engine-social.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "TechBranchId inclut 'social' (union étendue)"
    - "TECH_TREE contient 3 nouveaux nœuds : social-1 (cost 300, requires null), social-2 (cost 1500, requires social-1), social-3 (cost 4000, requires social-2)"
    - "Tous les social-* ont branch: 'social' et order: 1/2/3"
    - "TechBonuses expose 2 nouveaux champs : aubergeMaxActiveBonus: number (default 0), aubergeRewardMultiplier: number (default 1.0)"
    - "getTechBonuses agrège : social-2 → aubergeMaxActiveBonus = 1, social-3 → aubergeRewardMultiplier = 1.2"
    - "social-1 n'affecte aucun champ TechBonuses (gating pur — la construction de l'auberge est filtrée par BUILDING_CATALOG.techRequired === 'social-1')"
    - "Les bonus existants (culture/elevage/expansion) sont préservés intégralement"
    - "Les clés i18n tech.social-1/2/3 + descriptions + branch_social existent"
    - "npx tsc --noEmit ne dégrade pas la liste d'erreurs"
    - "npx jest --no-coverage lib/__tests__/tech-engine-social.test.ts passe"
  artifacts:
    - path: "lib/mascot/tech-engine.ts"
      provides: "Branche tech social (3 nœuds) + 2 nouveaux champs TechBonuses"
      contains: "social-1"
    - path: "lib/__tests__/tech-engine-social.test.ts"
      provides: "Tests Jest branche tech social"
      contains: "social-1"
    - path: "locales/fr/common.json"
      provides: "i18n branche social (labelKey + descriptionKey + branch label)"
      contains: "social-1"
  key_links:
    - from: "TECH_TREE.social-2.requires"
      to: "social-1"
      via: "string ref"
      pattern: "requires: 'social-1'"
    - from: "getTechBonuses(['social-2'])"
      to: "TechBonuses.aubergeMaxActiveBonus = 1"
      via: "switch case"
      pattern: "aubergeMaxActiveBonus"
    - from: "BUILDING_CATALOG.auberge.techRequired"
      to: "social-1"
      via: "gating de construction (consommé Phase 44-02)"
      pattern: "social-1"
---

<objective>
Créer la branche tech `social` (3 nœuds) dans `lib/mascot/tech-engine.ts` : `social-1` (cost 300, gate construction Auberge), `social-2` (cost 1500, +1 visiteur actif), `social-3` (cost 4000, ×1.2 reward). Étendre `TechBonuses` avec 2 nouveaux champs (`aubergeMaxActiveBonus`, `aubergeRewardMultiplier`) avec defaults sensibles. Câbler `getTechBonuses` pour les agréger. Ajouter les clés i18n et un fichier de tests Jest dédié.

Purpose: Plan parallèle au Plan 44-01 (aucune dépendance file-level avec les autres). Le Plan 44-02 dépend de la string `'social-1'` côté `TECH_TREE` pour gater la construction Auberge. Les bonus 2 et 3 sont câblés dans `TechBonuses` mais non-consommés en Phase 44 — Phase 45 (UI) ou 46 (spawn auto) les lira.

Output: 3 nœuds + 2 champs TechBonuses + i18n + tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/44-auberge-b-timent-branche-tech-social/44-CONTEXT.md
@lib/mascot/tech-engine.ts
@CLAUDE.md
</context>

<interfaces>
**Modifications de `lib/mascot/tech-engine.ts` :**

```typescript
// 1. Étendre TechBranchId
export type TechBranchId = 'culture' | 'elevage' | 'expansion' | 'social';

// 2. Étendre TechBonuses (ajout en fin d'interface, defaults sensibles)
export interface TechBonuses {
  // ...champs existants inchangés...
  /** Phase 44 — Bonus +N visiteurs actifs simultanés à l'Auberge (default 0). Lu par auberge-engine en Phase 45+. */
  aubergeMaxActiveBonus: number;
  /** Phase 44 — Multiplicateur reward livraison Auberge (default 1.0, social-3 → 1.2). Lu par auberge-engine en Phase 45+. */
  aubergeRewardMultiplier: number;
}

// 3. Ajouter 3 entrées à TECH_TREE après expansion-3
{ id: 'social-1', branch: 'social', order: 1, labelKey: 'tech.social-1', descriptionKey: 'tech.social-1_desc', emoji: '🛖', cost: 300, requires: null },
{ id: 'social-2', branch: 'social', order: 2, labelKey: 'tech.social-2', descriptionKey: 'tech.social-2_desc', emoji: '🍻', cost: 1500, requires: 'social-1' },
{ id: 'social-3', branch: 'social', order: 3, labelKey: 'tech.social-3', descriptionKey: 'tech.social-3_desc', emoji: '✨', cost: 4000, requires: 'social-2' },

// 4. Étendre les defaults dans getTechBonuses
const bonuses: TechBonuses = {
  // ...existants inchangés...
  aubergeMaxActiveBonus: 0,
  aubergeRewardMultiplier: 1.0,
};

// 5. Ajouter les cases dans le switch
case 'social-1':
  // gating pur — pas de bonus agrégé
  break;
case 'social-2':
  bonuses.aubergeMaxActiveBonus = 1;
  break;
case 'social-3':
  bonuses.aubergeRewardMultiplier = 1.2;
  break;
```

**Clés i18n à ajouter dans `locales/fr/common.json`, section `tech` (≈ ligne 4493) :**

```json
"branch_social": "Social",
"social-1": "Auberge ouverte",
"social-1_desc": "Débloque la construction de l'Auberge (1800 🍃)",
"social-2": "Réputation grandissante",
"social-2_desc": "+1 visiteur actif simultané à l'Auberge",
"social-3": "Hôte de prestige",
"social-3_desc": "+20% sur les récompenses des livraisons Auberge",
```
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Étendre TechBranchId, TechBonuses + ajouter 3 nœuds social-* à TECH_TREE</name>
  <files>lib/mascot/tech-engine.ts</files>
  <read_first>
    - lib/mascot/tech-engine.ts (intégral — 207 lignes)
    - .planning/phases/44-auberge-b-timent-branche-tech-social/44-CONTEXT.md (section "Branche tech social")
  </read_first>
  <behavior>
    - `TechBranchId` devient `'culture' | 'elevage' | 'expansion' | 'social'`.
    - `TechBonuses` gagne 2 champs : `aubergeMaxActiveBonus: number`, `aubergeRewardMultiplier: number`.
    - `TECH_TREE` gagne 3 entrées (social-1/2/3) après `expansion-3`.
    - `getTechBonuses` initialise les 2 nouveaux champs aux defaults (0 et 1.0) et ajoute 3 cases dans le switch (social-1 = no-op gating, social-2 = bonus capacité, social-3 = bonus reward).
    - Aucun comportement existant ne change pour les techs déjà débloquables.
  </behavior>
  <action>
    Modifier `lib/mascot/tech-engine.ts` :

    1. **Ligne 6** : étendre l'union `TechBranchId` :
       ```typescript
       export type TechBranchId = 'culture' | 'elevage' | 'expansion' | 'social';
       ```

    2. **Interface `TechBonuses` (lignes 21-31)** : ajouter les 2 champs en fin d'interface, juste avant la `}` de fermeture :
       ```typescript
       /** Phase 44 — Bonus +N visiteurs actifs simultanés à l'Auberge (default 0, social-2 → 1). */
       aubergeMaxActiveBonus: number;
       /** Phase 44 — Multiplicateur reward livraison Auberge (default 1.0, social-3 → 1.2). */
       aubergeRewardMultiplier: number;
       ```

    3. **TECH_TREE (avant la `]` de fermeture, après expansion-3 ligne 97)** : ajouter 3 entrées :
       ```typescript
         // Branche Social — Auberge & visiteurs (Phase 44)
         {
           id: 'social-1', branch: 'social', order: 1,
           labelKey: 'tech.social-1', descriptionKey: 'tech.social-1_desc',
           emoji: '🛖', cost: 300, requires: null,
         },
         {
           id: 'social-2', branch: 'social', order: 2,
           labelKey: 'tech.social-2', descriptionKey: 'tech.social-2_desc',
           emoji: '🍻', cost: 1500, requires: 'social-1',
         },
         {
           id: 'social-3', branch: 'social', order: 3,
           labelKey: 'tech.social-3', descriptionKey: 'tech.social-3_desc',
           emoji: '✨', cost: 4000, requires: 'social-2',
         },
       ```

    4. **`getTechBonuses` (lignes 153-207)** :
       - Dans l'objet `bonuses` initial (lignes 154-164), ajouter les 2 defaults :
         ```typescript
         aubergeMaxActiveBonus: 0,
         aubergeRewardMultiplier: 1.0,
         ```
       - Dans le `switch (techId)` (ligne 167), ajouter à la fin (après `case 'expansion-3'`) :
         ```typescript
         // Social (Phase 44)
         case 'social-1':
           // Gating pur — débloque la construction de l'Auberge via BUILDING_CATALOG.techRequired
           break;
         case 'social-2':
           bonuses.aubergeMaxActiveBonus = 1;
           break;
         case 'social-3':
           bonuses.aubergeRewardMultiplier = 1.2;
           break;
         ```

    Vérifier `npx tsc --noEmit` clean.

    **Note :** ne PAS importer `BUILDING_CATALOG` ni introduire de dépendance circulaire — `social-1` ne fait rien dans `getTechBonuses`, c'est juste une string consommée par `BuildingShopSheet` qui filtre `def.techRequired`.
  </action>
  <verify>
    <automated>grep -q "'culture' | 'elevage' | 'expansion' | 'social'" lib/mascot/tech-engine.ts &amp;&amp; grep -q "aubergeMaxActiveBonus" lib/mascot/tech-engine.ts &amp;&amp; grep -q "aubergeRewardMultiplier" lib/mascot/tech-engine.ts &amp;&amp; grep -q "id: 'social-1'" lib/mascot/tech-engine.ts &amp;&amp; grep -q "id: 'social-2'" lib/mascot/tech-engine.ts &amp;&amp; grep -q "id: 'social-3'" lib/mascot/tech-engine.ts &amp;&amp; grep -q "case 'social-2'" lib/mascot/tech-engine.ts &amp;&amp; npx tsc --noEmit 2>&amp;1 | grep -c "error TS" | awk '{ exit !($1 <= 5) }'</automated>
  </verify>
  <acceptance_criteria>
    - `TechBranchId` étendu à 4 valeurs.
    - `TechBonuses` a les 2 nouveaux champs (grep confirmé).
    - 3 nœuds social-* présents dans TECH_TREE avec costs 300/1500/4000 et chaîne de prereqs correcte.
    - `getTechBonuses({})` retourne `aubergeMaxActiveBonus: 0` et `aubergeRewardMultiplier: 1.0` par défaut (vérifié par tests Task 2).
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>
    La branche social est câblée. Plan 44-02 peut référencer `'social-1'` dans `BUILDING_CATALOG.auberge.techRequired`. Phase 45+ pourra lire `aubergeMaxActiveBonus` et `aubergeRewardMultiplier` dans `auberge-engine` pour appliquer les bonus.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Tests Jest — branche tech social (gating + agrégation bonus)</name>
  <files>lib/__tests__/tech-engine-social.test.ts</files>
  <read_first>
    - lib/mascot/tech-engine.ts (post-Task 1 : signatures + TECH_TREE complet)
    - lib/__tests__/auberge-engine.test.ts (pattern de tests Jest existant — describe/it FR)
  </read_first>
  <behavior>
    - Test 1 : `social-1` peut être débloqué dès le départ (`requires: null`, coût 300, sans prereq) via `canUnlockTech('social-1', [], 300).canUnlock === true`.
    - Test 2 : `social-2` ne peut PAS être débloqué sans `social-1` même avec coins suffisants : `canUnlockTech('social-2', [], 1500).canUnlock === false`.
    - Test 3 : `social-3` requiert `social-2` (chaîne de prereqs) : `canUnlockTech('social-3', ['social-1'], 4000).canUnlock === false` (manque social-2).
    - Test 4 : `getTechBonuses([])` retourne defaults `aubergeMaxActiveBonus: 0` + `aubergeRewardMultiplier: 1.0`.
    - Test 5 : `getTechBonuses(['social-2'])` retourne `aubergeMaxActiveBonus: 1, aubergeRewardMultiplier: 1.0`.
    - Test 6 : `getTechBonuses(['social-3'])` retourne `aubergeMaxActiveBonus: 0, aubergeRewardMultiplier: 1.2`.
    - Test 7 : `getTechBonuses(['social-1', 'social-2', 'social-3'])` agrège les deux : `aubergeMaxActiveBonus: 1, aubergeRewardMultiplier: 1.2`.
    - Test 8 : non-régression — `getTechBonuses(['culture-1', 'elevage-2'])` n'altère pas les nouveaux champs (defaults préservés).
  </behavior>
  <action>
    Créer `lib/__tests__/tech-engine-social.test.ts` :

    ```typescript
    import { canUnlockTech, getTechBonuses, TECH_TREE } from '../mascot/tech-engine';

    describe('tech-engine — branche social (Phase 44)', () => {
      describe('TECH_TREE structure', () => {
        it('contient les 3 nœuds social-1/2/3', () => {
          const ids = TECH_TREE.map(n => n.id);
          expect(ids).toContain('social-1');
          expect(ids).toContain('social-2');
          expect(ids).toContain('social-3');
        });

        it('chaîne de prereqs : social-2 → social-1, social-3 → social-2', () => {
          const s2 = TECH_TREE.find(n => n.id === 'social-2')!;
          const s3 = TECH_TREE.find(n => n.id === 'social-3')!;
          expect(s2.requires).toBe('social-1');
          expect(s3.requires).toBe('social-2');
        });

        it('coûts : 300 / 1500 / 4000', () => {
          expect(TECH_TREE.find(n => n.id === 'social-1')!.cost).toBe(300);
          expect(TECH_TREE.find(n => n.id === 'social-2')!.cost).toBe(1500);
          expect(TECH_TREE.find(n => n.id === 'social-3')!.cost).toBe(4000);
        });
      });

      describe('canUnlockTech — gating', () => {
        it('social-1 débloquable d\'emblée avec 300 feuilles', () => {
          expect(canUnlockTech('social-1', [], 300).canUnlock).toBe(true);
        });

        it('social-2 bloqué tant que social-1 non débloqué', () => {
          expect(canUnlockTech('social-2', [], 5000).canUnlock).toBe(false);
        });

        it('social-3 bloqué si social-2 manquant (même avec social-1)', () => {
          expect(canUnlockTech('social-3', ['social-1'], 5000).canUnlock).toBe(false);
        });

        it('social-3 débloquable après social-1 + social-2 + 4000 feuilles', () => {
          expect(canUnlockTech('social-3', ['social-1', 'social-2'], 4000).canUnlock).toBe(true);
        });
      });

      describe('getTechBonuses — agrégation', () => {
        it('defaults : aubergeMaxActiveBonus=0, aubergeRewardMultiplier=1.0', () => {
          const b = getTechBonuses([]);
          expect(b.aubergeMaxActiveBonus).toBe(0);
          expect(b.aubergeRewardMultiplier).toBe(1.0);
        });

        it('social-2 → aubergeMaxActiveBonus=1', () => {
          const b = getTechBonuses(['social-2']);
          expect(b.aubergeMaxActiveBonus).toBe(1);
          expect(b.aubergeRewardMultiplier).toBe(1.0);
        });

        it('social-3 → aubergeRewardMultiplier=1.2', () => {
          const b = getTechBonuses(['social-3']);
          expect(b.aubergeMaxActiveBonus).toBe(0);
          expect(b.aubergeRewardMultiplier).toBe(1.2);
        });

        it('social-1 + social-2 + social-3 → +1 cap, ×1.2 reward', () => {
          const b = getTechBonuses(['social-1', 'social-2', 'social-3']);
          expect(b.aubergeMaxActiveBonus).toBe(1);
          expect(b.aubergeRewardMultiplier).toBe(1.2);
        });

        it('non-régression : techs non-social ne touchent pas aux champs auberge', () => {
          const b = getTechBonuses(['culture-1', 'elevage-2', 'expansion-1']);
          expect(b.aubergeMaxActiveBonus).toBe(0);
          expect(b.aubergeRewardMultiplier).toBe(1.0);
          // sanity-check des bonus historiques
          expect(b.tasksPerStageReduction).toBe(1);
          expect(b.buildingCapacityMultiplier).toBe(2);
          expect(b.extraBuildingCells).toBe(1);
        });
      });
    });
    ```
  </action>
  <verify>
    <automated>npx jest --no-coverage lib/__tests__/tech-engine-social.test.ts 2>&amp;1 | grep -q "PASS\|passed"</automated>
  </verify>
  <acceptance_criteria>
    - Fichier de tests créé.
    - Tous les tests verts (≥ 11 tests dans 3 describe).
    - `npx jest --no-coverage` global ne dégrade aucun test pré-existant.
  </acceptance_criteria>
  <done>
    Branche tech social verrouillée par tests. Tout futur refacto qui casserait l'agrégation ou la chaîne de prereqs sera détecté.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: i18n FR — branche social + 3 techs</name>
  <files>locales/fr/common.json</files>
  <read_first>
    - locales/fr/common.json:4493-4527 (section tech existante : branch_culture/elevage/expansion + culture-N / elevage-N / expansion-N)
  </read_first>
  <behavior>
    - Ajout de `branch_social` après `branch_expansion` (ligne 4497).
    - Ajout des 6 clés `social-1` / `social-1_desc` / `social-2` / `social-2_desc` / `social-3` / `social-3_desc` après `expansion-3_desc` (ligne 4519), avant `unlock`.
    - JSON reste valide.
  </behavior>
  <action>
    Dans `locales/fr/common.json`, section `tech` :

    1. Après la ligne `"branch_expansion": "Expansion",` (≈ ligne 4497), ajouter :
       ```json
       "branch_social": "Social",
       ```

    2. Après la ligne `"expansion-3_desc": "Parcelle géante (double récolte)",` (≈ ligne 4519), avant la ligne `"unlock":`, ajouter :
       ```json
       "social-1": "Auberge ouverte",
       "social-1_desc": "Débloque la construction de l'Auberge (1800 🍃)",
       "social-2": "Réputation grandissante",
       "social-2_desc": "+1 visiteur actif simultané à l'Auberge",
       "social-3": "Hôte de prestige",
       "social-3_desc": "+20% sur les récompenses des livraisons Auberge",
       ```

    Vérifier validité JSON.
  </action>
  <verify>
    <automated>grep -q '"branch_social"' locales/fr/common.json &amp;&amp; grep -q '"social-1":' locales/fr/common.json &amp;&amp; grep -q '"social-2_desc"' locales/fr/common.json &amp;&amp; grep -q '"social-3":' locales/fr/common.json &amp;&amp; node -e "JSON.parse(require('fs').readFileSync('locales/fr/common.json','utf8'))"</automated>
  </verify>
  <acceptance_criteria>
    - Les 7 clés (`branch_social` + 6 keys social-N/social-N_desc) sont présentes.
    - JSON valide.
  </acceptance_criteria>
  <done>
    L'arbre tech affichera correctement les 3 nœuds sociaux dans l'UI existante (composant TechTreeSheet ou équivalent).
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` clean.
- `npx jest --no-coverage lib/__tests__/tech-engine-social.test.ts` : tous verts.
- `node -e "JSON.parse(...)"` : pas d'erreur.
- Grep TECH_TREE : 3 entrées social-* présentes.
</verification>

<success_criteria>
- Branche tech `social` complète et testée.
- Plan 44-02 peut consommer la string `'social-1'` (gating BUILDING_CATALOG.auberge.techRequired).
- Phase 45+ peut lire `aubergeMaxActiveBonus` / `aubergeRewardMultiplier` dans `getTechBonuses` pour câbler les effets côté `auberge-engine`.
- Commit FR atomique : `feat(44): branche tech social (3 nœuds) + bonus TechBonuses + i18n + tests`.
</success_criteria>

<output>
.planning/phases/44-auberge-b-timent-branche-tech-social/44-03-SUMMARY.md avec :
- Confirmation des 3 nœuds ajoutés (id, cost, requires).
- Confirmation des 2 nouveaux champs TechBonuses + leur agrégation.
- Résultat des tests Jest (nombre de tests verts).
- Confirmation des 7 clés i18n.
</output>
