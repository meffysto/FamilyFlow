---
phase: 44-auberge-b-timent-branche-tech-social
plan: 02
type: execute
wave: 2
depends_on:
  - 44-01
  - 44-03
files_modified:
  - lib/mascot/types.ts
  - locales/fr/common.json
autonomous: true
requirements: []
must_haves:
  truths:
    - "BUILDING_CATALOG contient une 5e entrée id: 'auberge' avec producesResource: false"
    - "L'entrée auberge a techRequired: 'social-1' (gating tech-tree)"
    - "L'entrée auberge a minTreeStage: 'arbuste', cost: 1800, dailyIncome: 0, resourceType: 'oeuf' (placeholder unused)"
    - "Les paliers de l'auberge sont générés via generateTiers(0, 1500) — baseHours=0 court-circuité par Plan 44-01"
    - "Les clés i18n farm.building.auberge / farm.building.auberge_desc existent dans locales/fr/common.json"
    - "labelKey du catalogue = 'farm.building.auberge' (pattern miroir poulailler/grange)"
    - "npx tsc --noEmit ne dégrade pas la liste d'erreurs"
  artifacts:
    - path: "lib/mascot/types.ts"
      provides: "Entrée auberge dans BUILDING_CATALOG"
      contains: "id: 'auberge'"
    - path: "locales/fr/common.json"
      provides: "Clés i18n auberge / auberge_desc dans farm.building"
      contains: "auberge"
  key_links:
    - from: "BUILDING_CATALOG.auberge"
      to: "techRequired: 'social-1'"
      via: "string ref vers TECH_TREE"
      pattern: "techRequired: 'social-1'"
    - from: "BUILDING_CATALOG.auberge.labelKey"
      to: "locales/fr/common.json farm.building.auberge"
      via: "i18n key"
      pattern: "farm.building.auberge"
---

<objective>
Ajouter le bâtiment `auberge` au `BUILDING_CATALOG` (lib/mascot/types.ts) avec `producesResource: false` (Plan 44-01) et `techRequired: 'social-1'` (Plan 44-03), plus les clés i18n minimales dans `locales/fr/common.json`. Aucune logique — seulement une 5e entrée déclarative.

Purpose: Une fois `social-1` débloqué et le stade arbre `arbuste` atteint, l'utilisateur verra l'Auberge dans `BuildingShopSheet` (filtrage par `techRequired` + `minTreeStage` géré par le composant existant — vérifier mais ne pas adapter). La construction crédite un PlacedBuilding standard (1800 feuilles).

Output: 1 entrée ajoutée au catalogue + 2 clés i18n.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/44-auberge-b-timent-branche-tech-social/44-CONTEXT.md
@.planning/phases/44-auberge-b-timent-branche-tech-social/44-01-PLAN.md
@.planning/phases/44-auberge-b-timent-branche-tech-social/44-03-PLAN.md
@lib/mascot/types.ts
@CLAUDE.md
</context>

<interfaces>
**Entrée à ajouter au `BUILDING_CATALOG` (lib/mascot/types.ts:514-556) :**

```typescript
{
  id: 'auberge',
  labelKey: 'farm.building.auberge',
  emoji: '🛖',
  cost: 1800,
  dailyIncome: 0,
  minTreeStage: 'arbuste',
  resourceType: 'oeuf', // unused — Plan 44-01 court-circuite via producesResource: false
  producesResource: false,
  techRequired: 'social-1',
  tiers: generateTiers(0, 1500), // baseHours=0 court-circuité — coût upgrade utilisé pour la progression visuelle
},
```

**Clés i18n à ajouter (locales/fr/common.json, section `farm.building`, ≈ ligne 4336 après `ruche_desc`) :**

```json
"auberge": "Auberge",
"auberge_desc": "Un refuge chaleureux pour les voyageurs. Accueille des visiteurs qui apportent des requêtes contre récompense.",
```
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Ajouter l'entrée auberge à BUILDING_CATALOG</name>
  <files>lib/mascot/types.ts</files>
  <read_first>
    - lib/mascot/types.ts:514-556 (BUILDING_CATALOG existant — pattern poulailler/grange/moulin/ruche)
    - lib/mascot/types.ts:447 (helper generateTiers)
    - .planning/phases/44-auberge-b-timent-branche-tech-social/44-CONTEXT.md (section "Bâtiment Auberge dans BUILDING_CATALOG")
  </read_first>
  <behavior>
    - Une 5e entrée est ajoutée à `BUILDING_CATALOG`, après `ruche`.
    - Tous les champs du `BuildingDefinition` sont renseignés (TS strict).
    - `producesResource: false` (consommé par Plan 44-01).
    - `techRequired: 'social-1'` (consommé par BuildingShopSheet existant qui filtre déjà par techRequired).
    - `tiers: generateTiers(0, 1500)` — baseHours=0 ne pose pas problème car le court-circuit `producesResource === false` est appliqué AVANT lecture du tier.productionRateHours.
    - Les 4 entrées existantes ne sont PAS modifiées.
  </behavior>
  <action>
    Dans `lib/mascot/types.ts`, ajouter une 5e entrée à la fin de l'array `BUILDING_CATALOG` (après l'entrée `ruche`, avant `];`) :

    ```typescript
    // Phase 44 — Auberge : bâtiment social non-productif (gating social-1, débloque visiteurs PNJ Phase 43)
    {
      id: 'auberge',
      labelKey: 'farm.building.auberge',
      emoji: '🛖',
      cost: 1800,
      dailyIncome: 0,
      minTreeStage: 'arbuste',
      resourceType: 'oeuf', // placeholder requis par le type — non-lu (producesResource: false)
      producesResource: false,
      techRequired: 'social-1',
      tiers: generateTiers(0, 1500),
    },
    ```

    Vérifier que `generateTiers(0, 1500)` ne lance pas d'erreur runtime — l'examen visuel de `generateTiers` (ligne 447) confirme : `productionRateHours: 0 * Math.pow(0.92, level - 1) = 0` (légal), `upgradeCoins: level === 1 ? 0 : 1500 * 1.6^(level-2)` (légal).
  </action>
  <verify>
    <automated>grep -q "id: 'auberge'" lib/mascot/types.ts &amp;&amp; grep -q "techRequired: 'social-1'" lib/mascot/types.ts &amp;&amp; grep -q "producesResource: false" lib/mascot/types.ts &amp;&amp; npx tsc --noEmit 2>&amp;1 | grep -c "error TS" | awk '{ exit !($1 <= 5) }'</automated>
  </verify>
  <acceptance_criteria>
    - Entrée `auberge` présente dans BUILDING_CATALOG (grep).
    - `producesResource: false` et `techRequired: 'social-1'` présents.
    - Les 4 entrées existantes inchangées (poulailler, grange, moulin, ruche).
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>
    L'auberge est déclarée. Une fois `social-1` débloqué et stade `arbuste`+ atteint, elle apparaîtra dans BuildingShopSheet et coûtera 1800 feuilles.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Ajouter les clés i18n auberge dans locales/fr/common.json</name>
  <files>locales/fr/common.json</files>
  <read_first>
    - locales/fr/common.json:4328-4337 (section farm.building avec poulailler / grange / moulin / ruche)
  </read_first>
  <behavior>
    - 2 clés ajoutées juste après `ruche_desc` : `auberge` et `auberge_desc`.
    - JSON reste valide (pas de virgule traînante, pas de duplication).
    - Le label suit le ton des autres bâtiments (description courte, ≤ 1 phrase).
  </behavior>
  <action>
    Dans `locales/fr/common.json`, dans le bloc `farm.building`, juste après la ligne `"ruche_desc": "Une ruche bourdonnante. Produit du miel automatiquement.",` (≈ ligne 4336), ajouter :

    ```json
    "auberge": "Auberge",
    "auberge_desc": "Un refuge chaleureux pour les voyageurs. Bientôt habitée…",
    ```

    Vérifier la validité JSON après modification.
  </action>
  <verify>
    <automated>grep -q '"auberge": "Auberge"' locales/fr/common.json &amp;&amp; grep -q '"auberge_desc"' locales/fr/common.json &amp;&amp; node -e "JSON.parse(require('fs').readFileSync('locales/fr/common.json','utf8'))"</automated>
  </verify>
  <acceptance_criteria>
    - Les 2 clés `farm.building.auberge` et `farm.building.auberge_desc` existent.
    - Le JSON parse sans erreur (`node -e "JSON.parse(...)"`).
    - Aucune autre clé i18n modifiée.
  </acceptance_criteria>
  <done>
    Les libellés FR sont en place. Le bâtiment auberge s'affichera correctement dans `BuildingShopSheet` et `BuildingDetailSheet` (titre via `t(def.labelKey)`).
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` clean.
- `node -e "JSON.parse(require('fs').readFileSync('locales/fr/common.json','utf8'))"` ne lance pas d'erreur.
- `grep "auberge" lib/mascot/types.ts` retourne au moins 1 ligne (entrée catalogue).
</verification>

<success_criteria>
- Le bâtiment auberge est constructible dès que `social-1` est débloqué et stade `arbuste`+.
- Aucune régression sur les 4 bâtiments existants.
- Commit FR atomique : `feat(44): bâtiment auberge dans BUILDING_CATALOG + i18n FR`.
</success_criteria>

<output>
.planning/phases/44-auberge-b-timent-branche-tech-social/44-02-SUMMARY.md avec :
- Confirmation entrée auberge ajoutée (grep id, techRequired, producesResource).
- Confirmation 2 clés i18n FR ajoutées.
- Note si BuildingShopSheet a été testé visuellement ou non (optionnel — Plan 44-04 fait la vérif).
</output>
