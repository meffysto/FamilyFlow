---
phase: 27-cran-village-composants
plan: 02
type: execute
wave: 2
depends_on: ["27-01"]
files_modified:
  - app/(tabs)/village.tsx
autonomous: false
requirements: [MAP-01, COOP-03, COOP-04, OBJ-02, HIST-01, HIST-02]

must_haves:
  truths:
    - "L'ecran village affiche une carte tilemap cobblestone via TileMapRenderer mode='village'"
    - "Un feed affiche qui a contribue quoi cette semaine (nom, type, montant, heure relative)"
    - "Une barre de progression montre l'avancement collectif vers la cible (ex: 47/100)"
    - "Chaque membre a un indicateur de sa contribution hebdomadaire visible"
    - "Un panneau historique liste les semaines passees avec cible, total, statut recompense"
    - "Quand l'objectif est atteint, un bouton 'Reclamer la recompense' apparait"
  artifacts:
    - path: "app/(tabs)/village.tsx"
      provides: "Ecran village complet avec toutes les sections"
      min_lines: 200
  key_links:
    - from: "app/(tabs)/village.tsx"
      to: "hooks/useGarden.ts"
      via: "useGarden() hook"
      pattern: "useGarden"
    - from: "app/(tabs)/village.tsx"
      to: "components/mascot/TileMapRenderer.tsx"
      via: "TileMapRenderer mode='village'"
      pattern: "mode.*village"
    - from: "app/(tabs)/village.tsx"
      to: "components/ui/LiquidXPBar.tsx"
      via: "LiquidXPBar color=colors.success"
      pattern: "LiquidXPBar"
    - from: "app/(tabs)/village.tsx"
      to: "components/ui/CollapsibleSection.tsx"
      via: "CollapsibleSection pour historique"
      pattern: "CollapsibleSection"
    - from: "app/(tabs)/village.tsx"
      to: "components/ui/ReactiveAvatar.tsx"
      via: "ReactiveAvatar pour indicateurs membres"
      pattern: "ReactiveAvatar"
---

<objective>
Creer l'ecran village complet avec carte tilemap, barre de progression objectif, feed de contributions, indicateurs par membre, et panneau historique.

Purpose: C'est l'ecran principal du Jardin Familial — rend visible la cooperation familiale. Couvre MAP-01 (rendu carte), COOP-03 (feed), COOP-04 (indicateurs membres), OBJ-02 (barre progression), HIST-01 (panneau historique), HIST-02 (detail semaines).
Output: `app/(tabs)/village.tsx` — ecran navigable complet.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-cran-village-composants/27-CONTEXT.md
@.planning/phases/27-cran-village-composants/27-RESEARCH.md
@.planning/phases/27-cran-village-composants/27-UI-SPEC.md
@.planning/phases/27-cran-village-composants/27-01-SUMMARY.md

@hooks/useGarden.ts
@contexts/VaultContext.tsx
@components/mascot/TileMapRenderer.tsx
@components/ui/LiquidXPBar.tsx
@components/ui/CollapsibleSection.tsx
@components/ui/ReactiveAvatar.tsx
@lib/mascot/seasons.ts
@lib/village/types.ts
@lib/village/templates.ts
@constants/spacing.ts
@constants/colors.ts

<interfaces>
<!-- Key types and contracts the executor needs -->

From hooks/useGarden.ts:
```typescript
export interface UseGardenReturn {
  gardenData: VillageData;
  currentTarget: number;
  progress: number;
  isGoalReached: boolean;
  currentTemplate: ObjectiveTemplate;
  addContribution: (type: ContributionType, profileId: string) => Promise<void>;
  claimReward: (profileId: string) => Promise<boolean>;
  weekHistory: VillageWeekRecord[];
  isLoading: boolean;
}
```

From lib/village/types.ts:
```typescript
export interface VillageData {
  version: number;
  createdAt: string;
  currentWeekStart: string;
  currentThemeIndex: number;
  rewardClaimed: boolean;
  contributions: VillageContribution[];
  pastWeeks: VillageWeekRecord[];
}
export interface VillageContribution {
  timestamp: string;    // ISO 8601 sans Z
  profileId: string;
  type: ContributionType;
  amount: number;
}
export type ContributionType = 'harvest' | 'task';
export interface VillageWeekRecord {
  weekStart: string;    // YYYY-MM-DD
  target: number;
  total: number;
  claimed: boolean;
}
export interface ObjectiveTemplate {
  id: string;
  name: string;
  icon: string;
  description: string;
}
```

From contexts/VaultContext.tsx (ce dont village.tsx a besoin):
```typescript
// useVault() expose:
// activeProfile: Profile | null — profil actif courant
// profiles: Profile[] — tous les profils
```

From components/ui/LiquidXPBar.tsx:
```typescript
interface LiquidXPBarProps {
  current: number;
  total: number;
  label?: string;
  color?: string;
  height?: number;  // default 22
}
export function LiquidXPBar({ current, total, label, color, height = 22 }: LiquidXPBarProps)
```

From components/ui/CollapsibleSection.tsx:
```typescript
interface CollapsibleSectionProps {
  id: string;              // Identifiant unique pour persistance SecureStore
  title: string;
  children: React.ReactNode;
  defaultCollapsed?: boolean;  // default false
}
export function CollapsibleSection({ id, title, children, defaultCollapsed = false }: CollapsibleSectionProps)
```

From components/ui/ReactiveAvatar.tsx:
```typescript
type AvatarMood = 'idle' | 'night' | 'loot' | 'allDone' | 'overdue' | 'morning';
interface ReactiveAvatarProps {
  emoji: string;
  mood: AvatarMood;
  style?: ViewStyle;
}
export function ReactiveAvatar({ emoji, mood, style }: ReactiveAvatarProps)
```

From components/mascot/TileMapRenderer.tsx (apres Plan 01):
```typescript
interface TileMapRendererProps {
  treeStage: TreeStage;
  containerWidth: number;
  containerHeight: number;
  season: Season;
  mode?: 'farm' | 'village';
}
```

From 27-UI-SPEC.md — Couleurs:
```
Accent village progression: colors.success (#10B981 light / #34D399 dark)
Accent village objectif atteint: colors.warning (#F59E0B light / #FBBF24 dark)
Constante cosmetique doree: #FFD700 (per Phase 4)
```

From 27-UI-SPEC.md — Copywriting:
```
Titre ecran: "Place du Village"
Titre section objectif: "Objectif de la semaine"
Label barre normale: "Progression collective"
Label barre atteint: "Objectif atteint ! 🎉"
CTA atteint: "Reclamer la recompense"
CTA deja claim: "Recompense reclamee" (desactive, opacity 0.5)
Titre section contributions: "Contributions cette semaine"
Lien depliement: "Voir tout (N)"
Lien repliement: "Reduire"
Titre section membres: "Membres actifs"
Titre section historique: "Semaines precedentes"
Etat vide feed: "Pas encore de contributions" / "Les recoltes et taches completees cette semaine apparaitront ici."
Etat vide historique: "L'historique des semaines accomplies s'affichera ici."
Type harvest: "a recolte"
Type task: "a complete une tache"
Statut atteint: "Objectif atteint"
Statut en cours: "En cours"
Statut non atteint: "Non atteint"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Creer l'ecran village.tsx complet</name>
  <files>app/(tabs)/village.tsx</files>
  <read_first>
    - hooks/useGarden.ts (UseGardenReturn complet — gardenData, progress, currentTarget, isGoalReached, currentTemplate, weekHistory, claimReward, isLoading)
    - contexts/VaultContext.tsx (useVault — activeProfile, profiles)
    - app/(tabs)/tree.tsx (pattern de reference: layout carte fixe + ScrollView, onLayout, Dimensions, useSafeAreaInsets)
    - components/mascot/TileMapRenderer.tsx (props interface avec mode)
    - components/ui/LiquidXPBar.tsx (props interface)
    - components/ui/CollapsibleSection.tsx (props interface, id namespace)
    - components/ui/ReactiveAvatar.tsx (props interface, AvatarMood)
    - lib/village/types.ts (VillageContribution, VillageWeekRecord, ContributionType)
    - lib/mascot/seasons.ts (getCurrentSeason export)
    - constants/spacing.ts (Spacing tokens)
    - constants/colors.ts (colors.success, colors.warning, colors.catJeux)
    - .planning/phases/27-cran-village-composants/27-UI-SPEC.md (layout complet, specs exactes)
  </read_first>
  <action>
Creer `app/(tabs)/village.tsx` — ecran complet du village.

**Structure generale (per D-02, UI-SPEC):**
```
SafeAreaView (flex:1, bg: colors.bg)
  Header fixe (44px) — bouton retour + titre "Place du Village"
  Vue carte TileMapRenderer mode='village' (hauteur: 42% ecran)
  ScrollView (flex:1) contenant:
    Section Objectif (LiquidXPBar + bouton claim conditionnel)
    Section Contributions (feed 5 items + "Voir tout")
    Section Membres (rangee horizontale avatars + totaux)
    Section Historique (CollapsibleSection par semaine)
```

**Imports requis:**
```typescript
import { useCallback, useMemo, useState } from 'react';
import { View, Text, ScrollView, StyleSheet, TouchableOpacity, Dimensions, ActivityIndicator } from 'react-native';
import { useRouter } from 'expo-router';
import { useSafeAreaInsets } from 'react-native-safe-area-context';
import Animated, { FadeInDown } from 'react-native-reanimated';
import * as Haptics from 'expo-haptics';
import { useVault } from '../../contexts/VaultContext';
import { useThemeColors } from '../../contexts/ThemeContext';
import { useGarden } from '../../hooks/useGarden';
import { TileMapRenderer } from '../../components/mascot/TileMapRenderer';
import { LiquidXPBar } from '../../components/ui/LiquidXPBar';
import { CollapsibleSection } from '../../components/ui/CollapsibleSection';
import { ReactiveAvatar } from '../../components/ui/ReactiveAvatar';
import { getCurrentSeason } from '../../lib/mascot/seasons';
import { Spacing } from '../../constants/spacing';
import type { VillageContribution } from '../../lib/village/types';
```

**Constantes en haut du fichier:**
```typescript
const { width: SCREEN_W, height: SCREEN_H } = Dimensions.get('window');
const MAP_HEIGHT = Math.round(SCREEN_H * 0.42);
const GOLD = '#FFD700';
const SPRING_FEED = { damping: 20, stiffness: 200 } as const;
const FEED_LIMIT = 5;
```

**Composant principal `VillageScreen` (export default):**

1. **Hooks:**
   - `const router = useRouter()`
   - `const insets = useSafeAreaInsets()`
   - `const { colors } = useThemeColors()`
   - `const { activeProfile, profiles } = useVault()`
   - `const { gardenData, progress, currentTarget, isGoalReached, currentTemplate, weekHistory, claimReward, isLoading } = useGarden()`
   - `const [showAllFeed, setShowAllFeed] = useState(false)`
   - `const [claimedThisSession, setClaimedThisSession] = useState(false)` — Pitfall 4 (RESEARCH.md)
   - `const [mapSize, setMapSize] = useState({ width: SCREEN_W, height: MAP_HEIGHT })`
   - `const season = getCurrentSeason()`

2. **Memos (per CLAUDE.md — useMemo dans providers/composants complexes):**

   Feed items — plus recent en haut, limite a 5 par defaut (per D-05):
   ```typescript
   const feedItems = useMemo(
     () => [...(gardenData?.contributions ?? [])].reverse().slice(0, showAllFeed ? undefined : FEED_LIMIT),
     [gardenData?.contributions, showAllFeed],
   );
   const totalContribs = gardenData?.contributions?.length ?? 0;
   ```

   Contributions par membre (per COOP-04 / D-06):
   ```typescript
   const memberContribs = useMemo(() => {
     const map: Record<string, number> = {};
     for (const c of gardenData?.contributions ?? []) {
       map[c.profileId] = (map[c.profileId] ?? 0) + c.amount;
     }
     return map;
   }, [gardenData?.contributions]);
   ```

   Profils actifs (filtrer grossesse per Phase 26 decision):
   ```typescript
   const activeProfiles = useMemo(
     () => profiles.filter(p => p.statut !== 'grossesse'),
     [profiles],
   );
   ```

3. **Couleur barre progression (per D-07, D-08):**
   ```typescript
   const barColor = isGoalReached ? colors.warning : colors.success;
   const barLabel = isGoalReached ? 'Objectif atteint ! 🎉' : 'Progression collective';
   const canClaim = isGoalReached && !gardenData?.rewardClaimed && !claimedThisSession;
   const alreadyClaimed = isGoalReached && (gardenData?.rewardClaimed || claimedThisSession);
   ```

4. **Handler claim (per D-08, Pitfall 4, Pitfall 7):**
   ```typescript
   const handleClaim = useCallback(async () => {
     if (!activeProfile) return;
     Haptics.notificationAsync(Haptics.NotificationFeedbackType.Success);
     const success = await claimReward(activeProfile.id);
     if (success) setClaimedThisSession(true);
   }, [activeProfile, claimReward]);
   ```

5. **Fonction utilitaire heure relative (per D-04 — "il y a 2h", "hier", "lun."):**
   ```typescript
   function formatRelativeTime(isoTimestamp: string): string {
     const now = Date.now();
     const then = new Date(isoTimestamp).getTime();
     const diffMs = now - then;
     const diffH = diffMs / 3_600_000;
     const diffD = diffMs / 86_400_000;
     if (diffH < 1) return 'a l\'instant';
     if (diffH < 24) return `il y a ${Math.floor(diffH)}h`;
     if (diffD < 2) return 'hier';
     return new Date(isoTimestamp).toLocaleDateString('fr-FR', { weekday: 'short' });
   }
   ```

6. **Fonction utilitaire format date JJ/MM (per CLAUDE.md — format date affiche JJ/MM/AAAA):**
   ```typescript
   function formatDateFR(isoDate: string): string {
     const [y, m, d] = isoDate.split('-');
     return `${d}/${m}/${y}`;
   }
   ```

7. **Composant FeedItem memoise (per CLAUDE.md — React.memo sur list items):**
   Definir en dehors du composant principal:
   ```typescript
   const FeedItem = React.memo(function FeedItem({
     contribution, profileName, profileEmoji, colors,
   }: {
     contribution: VillageContribution;
     profileName: string;
     profileEmoji: string;
     colors: ReturnType<typeof useThemeColors>['colors'];
   }) {
     const typeLabel = contribution.type === 'harvest' ? 'a recolte' : 'a complete une tache';
     return (
       <View style={[styles.feedItem, { borderBottomColor: colors.borderLight }]}>
         <Text style={styles.feedEmoji}>{profileEmoji}</Text>
         <Text style={[styles.feedName, { color: colors.text }]}>{profileName}</Text>
         <Text style={[styles.feedType, { color: colors.textMuted }]}>{typeLabel}</Text>
         <View style={{ flex: 1 }} />
         <Text style={[styles.feedAmount, { color: colors.success }]}>+{contribution.amount}</Text>
         <Text style={[styles.feedTime, { color: colors.textFaint ?? colors.textMuted }]}>
           {formatRelativeTime(contribution.timestamp)}
         </Text>
       </View>
     );
   });
   ```

8. **JSX principal — structure exacte:**

   **Header fixe (44px):**
   - TouchableOpacity retour avec `router.back()` a gauche
   - Texte "Place du Village" centre, FontSize.heading (18px), semibold

   **Carte tilemap fixe:**
   - View avec `height: MAP_HEIGHT`, `onLayout` pour mesurer
   - `<TileMapRenderer treeStage="arbre" containerWidth={mapSize.width} containerHeight={mapSize.height} season={season} mode="village" />`

   **ScrollView contenant 4 sections:**

   **Section Objectif (OBJ-02):**
   - Titre "Objectif de la semaine" (FontSize.heading, semibold)
   - Sous-titre avec icone + nom du template: `{currentTemplate.icon} {currentTemplate.name}`
   - `<LiquidXPBar current={progress} total={currentTarget} label={barLabel} color={barColor} height={24} />`
   - Texte `{progress} / {currentTarget}` sous la barre
   - Si `canClaim`: bouton `Animated.View entering={FadeInDown.duration(400)}` contenant TouchableOpacity "Reclamer la recompense" (backgroundColor: colors.warning, borderRadius: 12, paddingVertical: 12)
   - Si `alreadyClaimed`: bouton desactive "Recompense reclamee" (opacity: 0.5)

   **Section Contributions (COOP-03, D-04, D-05):**
   - Titre "Contributions cette semaine" (FontSize.heading, semibold)
   - Si `totalContribs === 0`: etat vide — emoji "🌱" (48px) + "Pas encore de contributions" + "Les recoltes et taches completees cette semaine apparaitront ici."
   - Sinon: mapper `feedItems` avec `<FeedItem>` pour chaque contribution. Resoudre profileName et profileEmoji depuis `profiles.find(p => p.id === c.profileId)` (fallback: `profileId` et `👤`).
   - Si `totalContribs > FEED_LIMIT`: lien toggle — "Voir tout ({totalContribs})" ou "Reduire" selon `showAllFeed`

   **Section Membres (COOP-04, D-06):**
   - Titre "Membres actifs" (FontSize.heading, semibold)
   - ScrollView horizontal avec pour chaque profil actif:
     - `<ReactiveAvatar emoji={profile.emoji ?? '👤'} mood="idle" style={{ width: 36, height: 36 }} />`
     - Texte total contribution (FontSize.label 13px, semibold, colors.success): `memberContribs[profile.id] ?? 0`
     - Texte nom profil (FontSize.caption 12px, normal, colors.textMuted): `profile.name`
     - Chaque item: largeur 64px, alignItems center

   **Section Historique (HIST-01, HIST-02, D-09, D-10):**
   - Titre "Semaines precedentes" (FontSize.heading, semibold)
   - Si `weekHistory.length === 0`: texte "L'historique des semaines accomplies s'affichera ici." (colors.textMuted)
   - Sinon: mapper `weekHistory` (plus recente en haut — `[...weekHistory].reverse()`) avec:
     ```tsx
     <CollapsibleSection
       id={`village_week_${week.weekStart}`}
       title={`Semaine du ${formatDateFR(week.weekStart)} — ${week.total}/${week.target}`}
       defaultCollapsed={true}
     >
       <View>
         <Text>Statut: {week.claimed ? 'Objectif atteint' : week.total >= week.target ? 'Objectif atteint' : 'Non atteint'}</Text>
         {/* Note HIST-02 limitation: VillageWeekRecord ne stocke pas le detail par membre — uniquement total/cible/claimed */}
       </View>
     </CollapsibleSection>
     ```
   - **IMPORTANT — Pitfall 2 (RESEARCH.md):** Les ids de CollapsibleSection DOIVENT etre prefixes `village_week_` pour eviter collision avec tree.tsx.
   - **IMPORTANT — Pitfall 5 (RESEARCH.md):** L'historique n'affiche PAS le detail par membre (VillageWeekRecord ne le contient pas). Afficher uniquement cible/total/statut.

9. **Etat loading:**
   Si `isLoading` ou `!gardenData`: afficher `<ActivityIndicator>` centre dans la zone ScrollView.

10. **Styles — StyleSheet.create() en bas du fichier (per CLAUDE.md):**
    Styles statiques. Styles dynamiques (couleurs theme) inline via `useThemeColors()`.
    Tokens de spacing: utiliser `Spacing.xs` (4), `Spacing.md` (8), `Spacing.xl` (12), `Spacing['2xl']` (16), `Spacing['3xl']` (20), `Spacing['4xl']` (24), `Spacing['5xl']` (32).
    Touch targets minimum 44px hauteur (per UI-SPEC — indicateurs membres).

**Pitfall 7 (RESEARCH.md):** village.tsx importe DEUX hooks — `useVault()` pour `activeProfile`/`profiles` ET `useGarden()` pour toutes les donnees village. Ne PAS essayer d'obtenir profileId depuis useGarden.
  </action>
  <verify>
    <automated>cd /Users/gabrielwaltio/Documents/family-vault && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - app/(tabs)/village.tsx existe et exporte un composant default `VillageScreen` (ou nom similaire)
    - village.tsx contient `useGarden()` avec destructuring de gardenData, progress, currentTarget, isGoalReached, currentTemplate, weekHistory, claimReward
    - village.tsx contient `useVault()` avec destructuring de activeProfile, profiles
    - village.tsx contient `<TileMapRenderer` avec `mode="village"`
    - village.tsx contient `<LiquidXPBar` avec `color={barColor}` ou equivalent utilisant colors.success/colors.warning
    - village.tsx contient `<CollapsibleSection` avec id prefixe `village_week_`
    - village.tsx contient `<ReactiveAvatar` avec `mood="idle"`
    - village.tsx contient `formatRelativeTime` pour les heures relatives du feed
    - village.tsx contient `React.memo` sur le composant FeedItem
    - village.tsx contient `claimedThisSession` pour le guard post-claim (Pitfall 4)
    - village.tsx contient les textes FR: "Place du Village", "Objectif de la semaine", "Contributions cette semaine", "Membres actifs", "Semaines precedentes"
    - village.tsx contient `Haptics.notificationAsync` dans handleClaim
    - village.tsx contient `router.back()` dans le header
    - village.tsx contient `FadeInDown` pour l'animation du bouton claim
    - `npx tsc --noEmit` passe sans nouvelles erreurs
  </acceptance_criteria>
  <done>L'ecran village affiche la carte tilemap cobblestone, la barre de progression objectif, le feed de contributions avec heure relative, les indicateurs par membre, et le panneau historique avec CollapsibleSections — tout cable sur useGarden et tsc passe</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verification visuelle ecran village</name>
  <files>app/(tabs)/village.tsx</files>
  <action>Verification humaine de l'ecran village complet. L'executeur lance l'app sur device et guide l'utilisateur pour verifier visuellement la carte tilemap cobblestone, le feed de contributions, la barre de progression, les indicateurs par membre, et le panneau historique.</action>
  <verify>
    <automated>cd /Users/gabrielwaltio/Documents/family-vault && npx tsc --noEmit 2>&1 | head -5</automated>
  </verify>
  <what-built>Ecran village complet avec carte tilemap cobblestone, barre de progression objectif, feed contributions, indicateurs par membre, et panneau historique</what-built>
  <how-to-verify>
    1. Lancer l'app sur device: `npx expo run:ios --device`
    2. Ouvrir l'ecran ferme (onglet arbre)
    3. Verifier qu'un FAB avec emoji village est visible en bas-droite de la carte
    4. Taper sur le FAB — l'ecran village s'ouvre
    5. Verifier que la carte tilemap en haut est visuellement differente de la ferme (cobblestone dominant, pas d'arbres fruitiers ni poissons)
    6. Verifier les sections sous la carte: Objectif de la semaine (barre verte), Contributions cette semaine, Membres actifs, Semaines precedentes
    7. Verifier le bouton retour en haut a gauche fonctionne
    8. Si des contributions existent: verifier le feed affiche nom + type + heure relative
    9. Si l'objectif est atteint: verifier le bouton "Reclamer la recompense" doree
  </how-to-verify>
  <resume-signal>Taper "approved" ou decrire les problemes</resume-signal>
  <done>L'utilisateur a verifie visuellement l'ecran village et approuve le rendu</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passe sans nouvelles erreurs
- L'ecran village est accessible via FAB depuis la ferme
- La carte tilemap est cobblestone dominant (pas herbe)
- Le feed de contributions affiche les noms de profils avec heures relatives
- La barre LiquidXPBar montre la progression collective
- Les indicateurs par membre montrent la contribution de chaque profil
- Le panneau historique utilise CollapsibleSection avec ids prefixes `village_`
- Le bouton "Reclamer la recompense" apparait quand l'objectif est atteint
</verification>

<success_criteria>
- L'ecran village est navigable, visuellement distinct de la ferme
- Toutes les 6 requirements (MAP-01, COOP-03, COOP-04, OBJ-02, HIST-01, HIST-02) sont couvertes
- Le feed, la barre, les indicateurs et l'historique sont cables sur useGarden
- tsc --noEmit passe
- Verification visuelle humaine approuvee
</success_criteria>

<output>
After completion, create `.planning/phases/27-cran-village-composants/27-02-SUMMARY.md`
</output>
