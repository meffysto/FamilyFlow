---
phase: 27-cran-village-composants
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/mascot/farm-map.ts
  - components/mascot/TileMapRenderer.tsx
  - app/(tabs)/tree.tsx
autonomous: true
requirements: [MAP-01]

must_haves:
  truths:
    - "La carte village utilise un terrain cobblestone dominant distinct de la ferme (herbe)"
    - "TileMapRenderer accepte un prop mode='village' pour conditionner la map et les decorations"
    - "Un bouton FAB sur l'ecran ferme navigue vers /(tabs)/village"
  artifacts:
    - path: "lib/mascot/farm-map.ts"
      provides: "buildVillageMap() export"
      exports: ["buildVillageMap"]
    - path: "components/mascot/TileMapRenderer.tsx"
      provides: "mode prop dans TileMapRendererProps"
      contains: "mode?: 'farm' | 'village'"
    - path: "app/(tabs)/tree.tsx"
      provides: "FAB village navigation"
      contains: "router.push"
  key_links:
    - from: "components/mascot/TileMapRenderer.tsx"
      to: "lib/mascot/farm-map.ts"
      via: "buildVillageMap() import"
      pattern: "buildVillageMap"
    - from: "app/(tabs)/tree.tsx"
      to: "/(tabs)/village"
      via: "router.push"
      pattern: "village"
---

<objective>
Adapter le moteur tilemap pour le terrain village et ajouter le FAB de navigation vers le village sur l'ecran ferme.

Purpose: MAP-01 exige une carte "Place du Village" avec terrain cobblestone distinct. Le FAB (D-03) permet d'acceder au village depuis la ferme. Ces fondations sont requises avant la creation de l'ecran village (Plan 02).
Output: `buildVillageMap()` dans farm-map.ts, prop `mode` sur TileMapRenderer, FAB village sur tree.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-cran-village-composants/27-CONTEXT.md
@.planning/phases/27-cran-village-composants/27-RESEARCH.md
@.planning/phases/27-cran-village-composants/27-UI-SPEC.md

@lib/mascot/farm-map.ts
@components/mascot/TileMapRenderer.tsx
@app/(tabs)/tree.tsx
@lib/village/grid.ts

<interfaces>
<!-- Key types and contracts the executor needs -->

From lib/mascot/farm-map.ts:
```typescript
export interface FarmMapData {
  cols: number;
  rows: number;
  layers: Record<TerrainType, boolean[][]>;
}
export const FARM_MAP_COLS = 12;
export const FARM_MAP_ROWS = 20;
export function buildFarmMap(treeStage: TreeStage): FarmMapData;
// Internal helpers (not exported but available in same file):
// function emptyVertices(cols: number, rows: number): boolean[][]
// function fillRect(grid: boolean[][], x1: number, y1: number, x2: number, y2: number): void
```

From components/mascot/TileMapRenderer.tsx:
```typescript
interface TileMapRendererProps {
  treeStage: TreeStage;
  containerWidth: number;
  containerHeight: number;
  season: Season;
}
export function TileMapRenderer({ treeStage, containerWidth, containerHeight, season }: TileMapRendererProps)
// Internal: getFarmDecos(season, stageIdx) returns FarmDeco[]
// Internal: farmMap = useMemo(() => buildFarmMap(treeStage), [treeStage])
// Internal: decos = useMemo(() => { const allDecos = getFarmDecos(season, stageIdx); ... })
```

From 27-UI-SPEC.md — FAB specs:
```
Position: absolute, bottom: Spacing['4xl'] (24px), right: Spacing['2xl'] (16px)
Taille: 56x56px, Radius.full
Icone: emoji "🏘️"
Background: colors.catJeux (#16A34A)
Ombre: { shadowColor: '#000', shadowOffset: {width:0, height:2}, shadowOpacity:0.15, shadowRadius:4, elevation:4 }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ajouter buildVillageMap() et prop mode sur TileMapRenderer</name>
  <files>lib/mascot/farm-map.ts, components/mascot/TileMapRenderer.tsx</files>
  <read_first>
    - lib/mascot/farm-map.ts (voir buildFarmMap, emptyVertices, fillRect, FarmMapData, FARM_MAP_COLS, FARM_MAP_ROWS)
    - components/mascot/TileMapRenderer.tsx (voir TileMapRendererProps, useMemo farmMap, getFarmDecos, decos filtering)
    - lib/village/grid.ts (positions VILLAGE_GRID des elements interactifs)
  </read_first>
  <action>
**Dans lib/mascot/farm-map.ts:**

Ajouter un nouvel export `buildVillageMap()` apres `buildFarmMap()`. La fonction genere une carte cobblestone dominante pour la place du village:

```typescript
export function buildVillageMap(): FarmMapData {
  const cols = FARM_MAP_COLS;  // 12
  const rows = FARM_MAP_ROWS;  // 20
  const cobblestone = emptyVertices(cols, rows);
  const dirt = emptyVertices(cols, rows);
  const water = emptyVertices(cols, rows);
  const farmland = emptyVertices(cols, rows);

  // Place centrale cobblestone (~60% surface)
  fillRect(cobblestone, 2, 4, 10, 16);

  // Chemins d'entree en terre (bordures haut et bas)
  fillRect(dirt, 4, 0, 8, 3);
  fillRect(dirt, 4, 17, 8, 20);

  // Fontaine: petit espace eau au centre (VILLAGE_GRID fountain x=0.50 y=0.45 -> cols 5-7, rows 8-10)
  fillRect(water, 5, 8, 7, 10);

  return {
    cols,
    rows,
    layers: { grass: emptyVertices(cols, rows), farmland, dirt, water, cobblestone },
  };
}
```

**Dans components/mascot/TileMapRenderer.tsx:**

1. Ajouter le prop `mode` dans `TileMapRendererProps`:
```typescript
interface TileMapRendererProps {
  treeStage: TreeStage;
  containerWidth: number;
  containerHeight: number;
  season: Season;
  mode?: 'farm' | 'village';  // NOUVEAU — 'farm' par defaut
}
```

2. Dans le destructuring de `TileMapRenderer`, extraire `mode = 'farm'`:
```typescript
export function TileMapRenderer({
  treeStage, containerWidth, containerHeight, season, mode = 'farm',
}: TileMapRendererProps) {
```

3. Importer `buildVillageMap` depuis farm-map.ts (buildFarmMap est deja importe).

4. Modifier le useMemo de farmMap pour conditionner sur le mode:
```typescript
const farmMap = useMemo(
  () => mode === 'village' ? buildVillageMap() : buildFarmMap(treeStage),
  [treeStage, mode],
);
```

5. Modifier le useMemo des decos pour retourner un tableau vide en mode village (pas de decorations ferme — arbres fruitiers, poissons koi, etc. — sur la carte village). Rechercher le bloc `const decos = useMemo(() => { const allDecos = getFarmDecos(...)` et ajouter en debut de ce useMemo:
```typescript
if (mode === 'village') return [];
```
Ajouter `mode` dans le tableau de dependances du useMemo decos.

**IMPORTANT — Pitfall 1 (RESEARCH.md):** Ne PAS afficher les decorations ferme sur la carte village. Le `if (mode === 'village') return [];` dans le useMemo decos est le guard necessaire.
  </action>
  <verify>
    <automated>cd /Users/gabrielwaltio/Documents/family-vault && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - lib/mascot/farm-map.ts contient `export function buildVillageMap(): FarmMapData`
    - lib/mascot/farm-map.ts buildVillageMap appelle `fillRect(cobblestone, 2, 4, 10, 16)`
    - components/mascot/TileMapRenderer.tsx contient `mode?: 'farm' | 'village'`
    - components/mascot/TileMapRenderer.tsx contient `mode === 'village' ? buildVillageMap() : buildFarmMap(treeStage)`
    - components/mascot/TileMapRenderer.tsx decos useMemo contient `if (mode === 'village') return []`
    - `npx tsc --noEmit` passe sans nouvelles erreurs
  </acceptance_criteria>
  <done>buildVillageMap() genere une carte cobblestone dominante, TileMapRenderer accepte mode='village' qui utilise cette carte et masque les decorations ferme, tsc passe</done>
</task>

<task type="auto">
  <name>Task 2: Ajouter le FAB village sur l'ecran ferme tree.tsx</name>
  <files>app/(tabs)/tree.tsx</files>
  <read_first>
    - app/(tabs)/tree.tsx (structure JSX existante, imports, styles, utilisation de useRouter, zone carte)
    - constants/spacing.ts (Spacing tokens)
    - constants/colors.ts (catJeux color)
    - .planning/phases/27-cran-village-composants/27-UI-SPEC.md (FAB specs exactes)
  </read_first>
  <action>
**Dans app/(tabs)/tree.tsx:**

1. Verifier que `useRouter` est deja importe depuis `expo-router` (tree.tsx l'utilise probablement deja — sinon l'ajouter).

2. Verifier que `Haptics` est deja importe depuis `expo-haptics` (deja utilise dans tree.tsx — sinon ajouter).

3. Ajouter le bouton FAB comme dernier enfant dans le conteneur de la carte (le `View` qui contient `TileMapRenderer`, identifie par le style `mapContainer` ou similaire). Le FAB doit etre EN SUPERPOSITION sur la carte (position absolute), PAS dans le ScrollView.

JSX a ajouter dans le conteneur carte, apres TileMapRenderer:
```tsx
<TouchableOpacity
  style={[styles.villageFAB, { backgroundColor: colors.catJeux }]}
  onPress={() => {
    Haptics.selectionAsync();
    router.push('/(tabs)/village');
  }}
  activeOpacity={0.8}
>
  <Text style={styles.villageFABText}>🏘️</Text>
</TouchableOpacity>
```

4. Ajouter les styles dans `StyleSheet.create()` en bas du fichier (per UI-SPEC):
```typescript
villageFAB: {
  position: 'absolute',
  bottom: 24,    // Spacing['4xl']
  right: 16,     // Spacing['2xl']
  width: 56,
  height: 56,
  borderRadius: 28,  // Radius.full = 9999 ou moitie de la taille
  alignItems: 'center',
  justifyContent: 'center',
  zIndex: 10,
  shadowColor: '#000',
  shadowOffset: { width: 0, height: 2 },
  shadowOpacity: 0.15,
  shadowRadius: 4,
  elevation: 4,
},
villageFABText: {
  fontSize: 24,
},
```

**IMPORTANT — D-03:** Ce FAB est temporaire — il sera remplace par le portail anime en Phase 28. Le placer en position absolute bottom-right de la zone carte pour ne pas couvrir les controles existants.

**IMPORTANT — D-01:** Ne PAS ajouter village comme onglet dans la tab bar (_layout.tsx). La navigation est uniquement via ce FAB.
  </action>
  <verify>
    <automated>cd /Users/gabrielwaltio/Documents/family-vault && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - app/(tabs)/tree.tsx contient `router.push('/(tabs)/village')`
    - app/(tabs)/tree.tsx contient `villageFAB` dans StyleSheet.create
    - app/(tabs)/tree.tsx contient `backgroundColor: colors.catJeux` (ou equivalent) dans le FAB
    - app/(tabs)/tree.tsx contient `Haptics.selectionAsync()` dans le handler du FAB
    - app/(tabs)/tree.tsx contient `🏘️` comme emoji du FAB
    - `npx tsc --noEmit` passe sans nouvelles erreurs
  </acceptance_criteria>
  <done>Un bouton FAB 56x56 avec emoji village est visible en bas-droite de la carte ferme, navigue vers /(tabs)/village au tap avec haptic feedback</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passe sans nouvelles erreurs
- `buildVillageMap` est exporte depuis `lib/mascot/farm-map.ts`
- `TileMapRenderer` accepte `mode='village'` et utilise `buildVillageMap()` dans ce mode
- Les decorations ferme ne sont PAS rendues en mode village (guard `if (mode === 'village') return []`)
- tree.tsx contient un FAB qui navigue vers `/(tabs)/village`
</verification>

<success_criteria>
- Le moteur tilemap supporte un mode village avec terrain cobblestone dominant
- Le FAB sur l'ecran ferme permet de naviguer vers le village
- tsc --noEmit passe
- Aucune regression sur l'ecran ferme existant (mode='farm' par defaut)
</success_criteria>

<output>
After completion, create `.planning/phases/27-cran-village-composants/27-01-SUMMARY.md`
</output>
