---
phase: 12-templates-onboarding-vivants-contenu-personnalis-et-complet
plan: 02
type: execute
wave: 2
depends_on:
  - 12-01
files_modified:
  - lib/vault-templates.ts
  - locales/fr/common.json
  - locales/en/common.json
  - app/setup.tsx
autonomous: true
requirements:
  - TMPL-06
  - TMPL-07
  - TMPL-08
  - TMPL-09

must_haves:
  truths:
    - "stock-initial pack generates a pantry/fridge stock file with 10+ household items"
    - "stock-initial pack adds baby items section when family has a bebe"
    - "defis-lancement pack picks 3 facile defis from DEFI_TEMPLATES and serializes them into defis.md"
    - "defis-lancement pack does NOT define custom defi content — it reuses constants/defiTemplates.ts"
    - "Both new packs are added to TEMPLATE_PACKS export array"
    - "All new strings use t() with keys in both FR and EN locale files"
    - "Onboarding step 4 shows a mini farm scene with real pixel art sprites instead of emoji row"
    - "Onboarding step 4 title is 'Vos taches accomplies prennent vie'"
    - "Onboarding step 4 features mention potager, batiments, and sagas/compagnon"
  artifacts:
    - path: "lib/vault-templates.ts"
      provides: "2 new template packs: stockInitial and defisLancement"
      contains: "stock-initial"
    - path: "app/setup.tsx"
      provides: "Redesigned farm onboarding slide replacing tree/emoji slide"
      contains: "farmScene"
    - path: "locales/fr/common.json"
      provides: "French i18n keys for stock-initial, defis-lancement, and farm slide"
      contains: "stockInitial"
    - path: "locales/en/common.json"
      provides: "English i18n keys for stock-initial, defis-lancement, and farm slide"
      contains: "stockInitial"
  key_links:
    - from: "lib/vault-templates.ts"
      to: "locales/fr/common.json"
      via: "t() calls with setup.templateContent.stockInitial keys"
      pattern: "stockInitial"
    - from: "lib/vault-templates.ts"
      to: "constants/defiTemplates.ts"
      via: "import DEFI_TEMPLATES to pick starter defis"
      pattern: "DEFI_TEMPLATES"
    - from: "app/setup.tsx"
      to: "locales/fr/common.json"
      via: "t() calls with setup.farm.* keys"
      pattern: "setup.farm"
---

<objective>
Add 2 new template packs (stock-initial, defis-lancement) and redesign the onboarding step 4 from "arbre" to "ferme familiale" with real pixel art sprites.

Purpose: Complete the template enrichment with pantry stock and starter challenges, and make the onboarding visually showcase the farm instead of abstract tree emojis.
Output: 2 new TemplatePack objects, redesigned renderMascotSlide in setup.tsx, ~40 new i18n keys.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-templates-onboarding-vivants-contenu-personnalis-et-complet/12-RESEARCH.md
@.planning/phases/12-templates-onboarding-vivants-contenu-personnalis-et-complet/12-01-SUMMARY.md
</context>

<interfaces>
Stock file format (from _stockContent() in vault.ts):

| Produit | Detail | Qte restante | Seuil | Qte/achat |
| ------- | ------ | ------------ | ----- | --------- |
| Pates   |        | 3            | 1     | 3         |

File must have frontmatter with tags: [stock, maison] and cssclasses: [stock].
Must have backlink: > <- [[00 - Dashboard/Dashboard|Dashboard]]

Defis file format (from parseDefis() in parser.ts):
Each defi starts with ## Title, then key-value lines (id, type, emoji, difficulty, startDate, endDate, targetDays, participants, status, rewardPoints, rewardLootBoxes, description), ends with ### Progression.

DEFI_TEMPLATES available in constants/defiTemplates.ts:
- 20 templates with id, title, description, type, emoji, difficulty, targetDays, category
- DEFI_REWARDS maps difficulty to {points, lootBoxes}
- Facile defis include: lecture_histoire, nature_sortie, famille_compliment, menage_ranger, coop_gentillesse, coop_menage_express, coop_photo_jour

Profile ID slug formula:
name.toLowerCase().normalize('NFD').replace(/[accents]/g, '').replace(/spaces/g, '-')
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add i18n keys for stock-initial, defis-lancement, and farm onboarding slide</name>
  <files>locales/fr/common.json, locales/en/common.json</files>
  <read_first>locales/fr/common.json (lines 193-390 for templateContent block, lines 122-129 for setup.mascot block), locales/en/common.json (same ranges)</read_first>
  <action>
Add new key blocks inside `setup.templateContent` in both FR and EN locale files.

**1. stock-initial keys** -- add `"stockInitial"` block inside `setup.templateContent`:

FR values / EN values:
- "title": "Stock maison" / "Household stock"
- "epicerie": "Placards — Epicerie" / "Pantry — Dry goods"
- "frigo": "Frigo" / "Fridge"
- "congelo": "Congelateur" / "Freezer"
- "pates": "Pates" / "Pasta"
- "riz": "Riz" / "Rice"
- "huile": "Huile d'olive" / "Olive oil"
- "farine": "Farine" / "Flour"
- "sucre": "Sucre" / "Sugar"
- "sel": "Sel" / "Salt"
- "conservesTomates": "Conserves de tomates" / "Canned tomatoes"
- "cafe": "Cafe" / "Coffee"
- "the": "The" / "Tea"
- "cereales": "Cereales" / "Cereal"
- "lait": "Lait" / "Milk"
- "beurre": "Beurre" / "Butter"
- "oeufs": "Oeufs" / "Eggs"
- "fromage": "Fromage" / "Cheese"
- "yaourts": "Yaourts" / "Yogurts"
- "legumes": "Legumes frais" / "Fresh vegetables"
- "fruits": "Fruits" / "Fruits"
- "painCongele": "Pain congele" / "Frozen bread"
- "legumesC": "Legumes congeles" / "Frozen vegetables"
- "glaces": "Glaces" / "Ice cream"
- "couches": "Couches" / "Diapers"
- "lingettes": "Lingettes" / "Wipes"
- "petitsPots": "Petits pots / compotes" / "Baby food / purees"
- "laitInfantile": "Lait infantile" / "Baby formula"
- "bebeHeader": "Produits bebe" / "Baby products"

**2. defis-lancement keys** -- add `"defisLancement"` block inside `setup.templateContent`:

FR values / EN values:
- "title": "Defis familiaux" / "Family challenges"
- "description": "3 defis faciles pour demarrer la gamification" / "3 easy challenges to start gamification"

(No content keys needed — titles/descriptions come from DEFI_TEMPLATES and gamification i18n)

**3. Farm onboarding slide keys** -- REPLACE the existing `setup.mascot` block (lines 122-129) with new `setup.farm` block:

FR values / EN values:
- "title": "Vos taches accomplies prennent vie" / "Your completed tasks come to life"
- "subtitle": "Chaque tache terminee fait pousser votre ferme familiale. Cultivez, construisez et vivez des aventures ensemble." / "Every completed task grows your family farm. Grow crops, build structures and go on adventures together."
- "features": {
    "crops": "Plantez et recoltez dans votre potager familial" / "Plant and harvest in your family garden",
    "buildings": "Debloquez moulin, grange et poulailler au fil des niveaux" / "Unlock mill, barn and coop as you level up",
    "adventures": "Adoptez un compagnon et vivez des sagas narratives" / "Adopt a companion and experience narrative sagas"
  }

IMPORTANT: Keep the old `setup.mascot` block (lines 122-129) but ADD the new `setup.farm` block right after it. The old mascot keys are still used elsewhere in the app (mascot stages display). Only setup.tsx step 4 will switch to using `setup.farm.*` keys.

IMPORTANT: Use proper French accents in actual JSON values. Match the accent style of existing keys.
Ensure both FR and EN files have EXACTLY the same key structure.
  </action>
  <verify>
    <automated>node -e "const fr=require('./locales/fr/common.json'); const en=require('./locales/en/common.json'); const tc=fr.setup.templateContent; const tce=en.setup.templateContent; const checks=['stockInitial.pates','stockInitial.lait','stockInitial.bebeHeader','defisLancement.title']; let ok=true; for(const c of checks){const parts=c.split('.'); let vf=tc,ve=tce; for(const p of parts){vf=vf?.[p];ve=ve?.[p]} if(!vf||!ve){console.log('MISSING:',c,'fr=',!!vf,'en=',!!ve);ok=false}} if(!fr.setup.farm?.title){console.log('MISSING: setup.farm.title');ok=false} if(!en.setup.farm?.title){console.log('MISSING: setup.farm.title (EN)');ok=false} if(!fr.setup.farm?.features?.crops){console.log('MISSING: setup.farm.features.crops');ok=false} if(ok)console.log('ALL_KEYS_PRESENT'); else process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
- `locales/fr/common.json` contains key `setup.templateContent.stockInitial.pates` with non-empty FR value
- `locales/en/common.json` contains key `setup.templateContent.stockInitial.pates` with value "Pasta"
- `locales/fr/common.json` contains key `setup.templateContent.defisLancement.title` with non-empty FR value
- `locales/fr/common.json` contains key `setup.farm.title` with value containing "taches accomplies prennent vie"
- `locales/fr/common.json` contains key `setup.farm.features.crops` with non-empty value
- `locales/fr/common.json` contains key `setup.farm.features.buildings` with non-empty value
- `locales/fr/common.json` contains key `setup.farm.features.adventures` with non-empty value
- `locales/en/common.json` contains key `setup.farm.title` with non-empty EN value
- The existing `setup.mascot` block is PRESERVED (not deleted)
- Both FR and EN files are valid JSON
  </acceptance_criteria>
  <done>~40 new i18n keys exist in both FR and EN for stock-initial, defis-lancement, and farm onboarding slide.</done>
</task>

<task type="auto">
  <name>Task 2: Create stock-initial template pack</name>
  <files>lib/vault-templates.ts</files>
  <read_first>lib/vault-templates.ts (full file, will be ~850+ lines after Plan 01), lib/vault.ts (search for _stockContent to see stock file format pattern)</read_first>
  <action>
Add the stockInitial TemplatePack object in `lib/vault-templates.ts` BEFORE the export section.

Create a `const stockInitial: TemplatePack` with:
- id: 'stock-initial'
- name: 'Stock maison'
- emoji: the convenience store emoji
- description: 'Inventaire de depart pour placards, frigo et congelateur'
- generate(ctx): computes `hasBebe = ctx.children.some(c => c.ageCategory === 'bebe')`
- Uses `const p = 'setup.templateContent.stockInitial'`
- Generates a stock markdown file at path `02 - Maison/Stock maison.md`
- File has frontmatter: tags [stock, maison], cssclasses [stock]
- Has backlink: `> <- [[00 - Dashboard/Dashboard|Dashboard]]`
- 3 sections using table format:
  - Epicerie header `## 🗄️ ${t(`${p}.epicerie`)}` with table: 10 items (pates, riz, huile, farine, sucre, sel, conservesTomates, cafe, the, cereales) -- each row: `| ${t(`${p}.pates`)} |  | 3 | 1 | 3 |`
  - Frigo header `## 🧊 ${t(`${p}.frigo`)}` with table: 7 items (lait, beurre, oeufs, fromage, yaourts, legumes, fruits) -- reasonable quantities
  - Congelateur header `## ❄️ ${t(`${p}.congelo`)}` with table: 3 items (painCongele, legumesC, glaces) -- reasonable quantities
- Each section has the table header: `| Produit | Detail | Qte restante | Seuil | Qte/achat |` followed by `| ------- | ------ | ------------ | ----- | --------- |`
- Conditional 4th section: `## 👶 ${t(`${p}.bebeHeader`)}` with 4 items (couches, lingettes, petitsPots, laitInfantile) when hasBebe is true
- All product names use t() with stockInitial.* keys
- Returns single file, NOT append mode (new file)

Then add `stockInitial` to the `TEMPLATE_PACKS` array (after vieDeFamille). Do NOT add to DEFAULT_SELECTED_PACKS.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const src=fs.readFileSync('lib/vault-templates.ts','utf8'); const checks=[\"id: 'stock-initial'\", 'stockInitial', 'hasBebe', 'bebeHeader', 'Produit', 'Qte restante']; let ok=true; for(const c of checks){if(!src.includes(c)){console.log('MISSING:',c);ok=false}} if(ok)console.log('STOCK_PACK_OK'); else process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
- `lib/vault-templates.ts` contains `id: 'stock-initial'` for the new stock pack
- `lib/vault-templates.ts` stockInitial.generate checks `hasBebe` for conditional baby section
- `lib/vault-templates.ts` stock file has frontmatter with tags [stock, maison] and cssclasses [stock]
- `lib/vault-templates.ts` stock file has backlink `> <- [[00 - Dashboard/Dashboard|Dashboard]]`
- `lib/vault-templates.ts` stock tables use `| Produit | Detail | Qte restante | Seuil | Qte/achat |` header
- `lib/vault-templates.ts` TEMPLATE_PACKS array contains `stockInitial`
- `npx tsc --noEmit` reports 0 new errors in vault-templates.ts
  </acceptance_criteria>
  <done>stock-initial template pack generates pantry/fridge/freezer stock file with 20+ items and conditional baby section. Registered in TEMPLATE_PACKS.</done>
</task>

<task type="auto">
  <name>Task 3: Create defis-lancement template pack using existing DEFI_TEMPLATES</name>
  <files>lib/vault-templates.ts</files>
  <read_first>lib/vault-templates.ts (the stockInitial pack just added), constants/defiTemplates.ts (DEFI_TEMPLATES array and DEFI_REWARDS map), lib/parser.ts (search for parseDefis to see exact format)</read_first>
  <action>
Add the defisLancement TemplatePack object in `lib/vault-templates.ts` AFTER stockInitial, BEFORE the export section.

CRITICAL: This pack does NOT define custom defi content. It imports and reuses DEFI_TEMPLATES from constants/defiTemplates.ts.

1. Add import at top of file:
```typescript
import { DEFI_TEMPLATES, DEFI_REWARDS } from '../constants/defiTemplates';
```

2. Create `const defisLancement: TemplatePack` with:
- id: 'defis-lancement'
- name: 'Defis de lancement'
- emoji: the target/bullseye emoji
- description: '3 defis faciles pour demarrer la gamification'
- generate(ctx):
  - Pick 3 specific facile defis from DEFI_TEMPLATES by id: `lecture_histoire`, `nature_sortie`, `famille_compliment`
  - Get them via: `const picks = ['lecture_histoire', 'nature_sortie', 'famille_compliment'].map(id => DEFI_TEMPLATES.find(d => d.id === id)!);`
  - Compute profile ID slugs: `const slug = (name: string) => name.toLowerCase().normalize('NFD').replace(/[\u0300-\u036f]/g, '').replace(/\s+/g, '-');`
  - Build participants string: `const allSlugs = [...ctx.parents.map(par => slug(par.name)), ...ctx.children.map(c => slug(c.name))].join(', ');`
  - Compute dates: `const today = new Date(ctx.today + 'T12:00:00');`
  - For each picked defi, get rewards: `const rewards = DEFI_REWARDS[defi.difficulty];`
  - Generate content in parseDefis() format. Each defi block:
    ```
    ## ${defi.title}
    id: ${defi.id}
    type: ${defi.type}
    emoji: ${defi.emoji}
    difficulty: ${defi.difficulty}
    startDate: ${ctx.today}
    endDate: ${futureDate(today, defi.targetDays)}
    targetDays: ${defi.targetDays}
    participants: ${allSlugs}
    status: active
    rewardPoints: ${rewards.points}
    rewardLootBoxes: ${rewards.lootBoxes}
    description: ${defi.description}

    ### Progression
    ```
  - Separate each defi block with a blank line
  - File has NO frontmatter (defis.md already exists from scaffoldVault with frontmatter)
  - Returns single file at path `defis.md` with `append: true`

NOTE on deduplication: append:true is safe because installTemplates() tracks installed packs via SecureStore markTemplateInstalled() and skips already-installed packs. This is first-run-only.

3. Add `defisLancement` to the `TEMPLATE_PACKS` array (after stockInitial). Do NOT add to DEFAULT_SELECTED_PACKS.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const src=fs.readFileSync('lib/vault-templates.ts','utf8'); const checks=[\"id: 'defis-lancement'\", 'defisLancement', 'DEFI_TEMPLATES', 'DEFI_REWARDS', 'lecture_histoire', 'nature_sortie', 'famille_compliment', 'append: true', 'normalize']; let ok=true; for(const c of checks){if(!src.includes(c)){console.log('MISSING:',c);ok=false}} if(src.match(/DEFAULT_SELECTED_PACKS[^;]*defis/)){console.log('FAIL: defis should NOT be in DEFAULT_SELECTED_PACKS');ok=false} if(src.includes('defi1Title')||src.includes('defi2Title')){console.log('FAIL: should not define custom defi content keys');ok=false} if(ok)console.log('DEFIS_PACK_OK'); else process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
- `lib/vault-templates.ts` contains `id: 'defis-lancement'` for the new defis pack
- `lib/vault-templates.ts` imports `DEFI_TEMPLATES` and `DEFI_REWARDS` from `constants/defiTemplates`
- `lib/vault-templates.ts` defisLancement picks defis by id: `lecture_histoire`, `nature_sortie`, `famille_compliment`
- `lib/vault-templates.ts` defisLancement does NOT define custom defi content (no `defi1Title`, `defi2Title` etc.)
- `lib/vault-templates.ts` defisLancement uses `append: true` on the returned file
- `lib/vault-templates.ts` defisLancement computes profile ID slugs using `toLowerCase().normalize('NFD').replace` formula
- `lib/vault-templates.ts` TEMPLATE_PACKS array contains `defisLancement`
- `lib/vault-templates.ts` defisLancement is NOT in DEFAULT_SELECTED_PACKS
- `npx tsc --noEmit` reports 0 new errors
  </acceptance_criteria>
  <done>defis-lancement template pack picks 3 facile defis from DEFI_TEMPLATES (no custom content duplication), serializes them with real profile slugs. Registered in TEMPLATE_PACKS.</done>
</task>

<task type="auto">
  <name>Task 4: Redesign onboarding step 4 — farm scene replaces tree emojis</name>
  <files>app/setup.tsx</files>
  <read_first>app/setup.tsx (lines 40-60 for constants, lines 410-476 for renderMascotSlide, lines 1263-1295 for styles), .planning/mockups/onboarding-ferme-v2.html (reference mockup for layout)</read_first>
  <action>
Redesign the onboarding step 4 in `app/setup.tsx` to show a mini pixel art farm scene instead of the 3 emoji row (🌱 🌳 ✨).

**1. Update imports** — add Image from react-native if not present.

**2. Add sprite requires** at the top of the file (near line 51, replacing MASCOT_EMOJIS):

Replace:
```typescript
const MASCOT_EMOJIS = ['🌱', '🌳', '✨'];
```

With farm scene sprite constants:
```typescript
const FARM_SPRITES = {
  tree: require('../assets/trees/cerisier/stage_3.png'),
  wheat: require('../assets/garden/crops/wheat/stage_4_a.png'),
  carrot: require('../assets/garden/crops/carrot/stage_4_a.png'),
  tomato: require('../assets/garden/crops/tomato/stage_4_a.png'),
  strawberry: require('../assets/garden/crops/strawberry/stage_4_a.png'),
  poulailler: require('../assets/buildings/poulailler_lv1.png'),
  grange: require('../assets/buildings/grange_lv1.png'),
  moulin: require('../assets/buildings/moulin_lv1.png'),
  chien: require('../assets/garden/animals/chien/adulte/idle_1.png'),
  bush: require('../assets/garden/ground/bush_1.png'),
  flower: require('../assets/garden/ground/small_flower_1.png'),
  rock: require('../assets/garden/ground/rock_1.png'),
};
```

**3. Rewrite `renderMascotSlide`** (lines 410-476):

Replace the entire function body. The new version:
- Shows a farm scene container (rounded rect, ~270px height) with:
  - Sky gradient background (top 35%) using LinearGradient (already imported)
  - Green ground (bottom 65%) with the grass green color (#4a9d3a)
  - 2 rows of farmland cells (brown rectangles) in top portion of ground
  - Pixel art crop sprites on the farmland cells (wheat, carrot, tomato, strawberry) — use `<Image source={FARM_SPRITES.wheat} style={{ width: 20, height: 36 }} resizeMode="contain" />` with `imageRendering` not available in RN, but pixel art sprites are already designed to look good at small sizes
  - Tree sprite (cerisier stage 3) center-left, ~100px tall
  - 3 building sprites (poulailler, grange, moulin) stacked on the right side, ~48px each
  - Companion (chien) sprite near the tree, ~32px
  - Small decoration sprites (bush, flower, rock) scattered at bottom
- Title using `t('setup.farm.title')` instead of `t('setup.mascot.title')`
- Subtitle using `t('setup.farm.subtitle')` instead of `t('setup.mascot.subtitle')`
- 3 feature cards with keys `crops`, `buildings`, `adventures` using `t('setup.farm.features.${key}')`:
  - crops icon: FARM_SPRITES.wheat (Image, 28x28)
  - buildings icon: FARM_SPRITES.poulailler (Image, 28x28)
  - adventures icon: FARM_SPRITES.chien (Image, 28x28)

The farm scene layout (simplified — absolute positioning inside a container View):
```
<View style={s.farmScene}>
  <LinearGradient colors={['#7ec8e3', '#a8dff0']} style={s.farmSky} />
  <View style={s.farmGround} />
  {/* Farmland plots */}
  <View style={s.farmPlots}>
    {/* 2 rows x 5 brown cells */}
  </View>
  {/* Crops - absolute positioned on farmland */}
  <Image source={FARM_SPRITES.wheat} style={[s.farmCrop, { top: 72, left: 38 }]} />
  <Image source={FARM_SPRITES.carrot} style={[s.farmCrop, { top: 80, left: 72 }]} />
  ...etc
  {/* Tree */}
  <Image source={FARM_SPRITES.tree} style={s.farmTree} />
  {/* Companion */}
  <Image source={FARM_SPRITES.chien} style={s.farmCompanion} />
  {/* Buildings */}
  <Image source={FARM_SPRITES.poulailler} style={[s.farmBuilding, { top: 92 }]} />
  <Image source={FARM_SPRITES.grange} style={[s.farmBuilding, { top: 145 }]} />
  <Image source={FARM_SPRITES.moulin} style={[s.farmBuilding, { top: 198 }]} />
  {/* Decos */}
  <Image source={FARM_SPRITES.bush} style={[s.farmDeco, { bottom: 12, left: 10 }]} />
  <Image source={FARM_SPRITES.rock} style={[s.farmDeco, { bottom: 20, left: 100 }]} />
  <Image source={FARM_SPRITES.flower} style={[s.farmDeco, { bottom: 15, right: 60 }]} />
</View>
```

Use Animated.View with FadeInDown/FadeInUp entering animations (already imported via react-native-reanimated in setup.tsx) for the farm scene, title, subtitle, and feature cards — same pattern as the current implementation.

**4. Add styles** in the StyleSheet at the bottom (near line 1263):

Replace `featureHeroEmojis` and `featureHeroEmoji` styles with farm scene styles:
```typescript
farmScene: {
  width: '100%',
  height: 270,
  borderRadius: Radius.xl,
  overflow: 'hidden',
  marginBottom: Spacing['3xl'],
},
farmSky: {
  position: 'absolute',
  top: 0, left: 0, right: 0,
  height: '35%',
},
farmGround: {
  position: 'absolute',
  top: '35%', left: 0, right: 0, bottom: 0,
  backgroundColor: '#4a9d3a',
},
farmPlots: {
  position: 'absolute',
  top: '38%',
  left: 28,
  flexDirection: 'column',
  gap: 2,
},
farmPlotRow: {
  flexDirection: 'row',
  gap: 2,
},
farmPlotCell: {
  width: 34,
  height: 34,
  backgroundColor: '#6b4f1d',
  borderRadius: 3,
},
farmCrop: {
  position: 'absolute',
  width: 20,
  height: 32,
  resizeMode: 'contain',
},
farmTree: {
  position: 'absolute',
  bottom: 30,
  left: 90,
  width: 100,
  height: 130,
  resizeMode: 'contain',
},
farmCompanion: {
  position: 'absolute',
  bottom: 55,
  left: 195,
  width: 32,
  height: 32,
  resizeMode: 'contain',
},
farmBuilding: {
  position: 'absolute',
  right: 15,
  width: 48,
  height: 48,
  resizeMode: 'contain',
},
farmDeco: {
  position: 'absolute',
  width: 18,
  height: 18,
  resizeMode: 'contain',
},
farmFeatureIcon: {
  width: 32,
  height: 32,
  resizeMode: 'contain',
},
```

Keep all existing styles that are still used by other steps (featureSlideTitle, featureSlideSubtitle, slideDetails, slideDetailRow, slideDetailText, slideDetailIcon). Only remove/replace featureHeroEmojis and featureHeroEmoji since they're only used in renderMascotSlide.

**5. Update feature card icons** — in the feature cards section, replace the emoji Text elements with Image elements using FARM_SPRITES. Wrap each Image in the existing slideDetailIcon spot but use farmFeatureIcon style for the image:

Instead of:
```
<Text style={s.slideDetailIcon}>
  {key === 'grow' ? '🌿' : key === 'sagas' ? '📖' : '🍃'}
</Text>
```

Use:
```
<Image
  source={key === 'crops' ? FARM_SPRITES.wheat : key === 'buildings' ? FARM_SPRITES.poulailler : FARM_SPRITES.chien}
  style={s.farmFeatureIcon}
/>
```

The feature keys change from `['grow', 'sagas', 'rewards']` to `['crops', 'buildings', 'adventures']`, and the i18n prefix changes from `setup.mascot.features.${key}` to `setup.farm.features.${key}`.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -c "setup.tsx" || echo "0 errors in setup.tsx"; node -e "const fs=require('fs'); const src=fs.readFileSync('app/setup.tsx','utf8'); const checks=['FARM_SPRITES','farmScene','setup.farm.title','setup.farm.subtitle','setup.farm.features','cerisier/stage_3','poulailler_lv1','chien/adulte/idle_1','crops','buildings','adventures']; let ok=true; for(const c of checks){if(!src.includes(c)){console.log('MISSING:',c);ok=false}} if(src.includes('MASCOT_EMOJIS')){console.log('FAIL: MASCOT_EMOJIS should be removed');ok=false} if(ok)console.log('FARM_SLIDE_OK'); else process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
- `app/setup.tsx` does NOT contain `MASCOT_EMOJIS` constant
- `app/setup.tsx` contains `FARM_SPRITES` with requires for cerisier, crops, buildings, chien
- `app/setup.tsx` renderMascotSlide uses `t('setup.farm.title')` not `t('setup.mascot.title')`
- `app/setup.tsx` renderMascotSlide uses `t('setup.farm.subtitle')` not `t('setup.mascot.subtitle')`
- `app/setup.tsx` feature keys are `['crops', 'buildings', 'adventures']` not `['grow', 'sagas', 'rewards']`
- `app/setup.tsx` contains farmScene style with height 270 and borderRadius
- `app/setup.tsx` contains farmTree style with cerisier positioning
- `app/setup.tsx` feature icons use Image components with FARM_SPRITES not emoji Text
- `npx tsc --noEmit` reports 0 new errors in setup.tsx
  </acceptance_criteria>
  <done>Onboarding step 4 shows a mini pixel art farm with real sprites (tree, crops, buildings, companion, decorations) instead of 3 abstract emojis. Title is "Vos taches accomplies prennent vie". Features highlight potager, batiments, and compagnon/sagas.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes without new errors
2. Both locale files are valid JSON with stockInitial, defisLancement, and farm key blocks
3. TEMPLATE_PACKS array has 10 elements (8 original + 2 new)
4. Stock pack conditionally adds baby section
5. Defis pack imports and uses DEFI_TEMPLATES — does NOT define custom content
6. Neither new pack is in DEFAULT_SELECTED_PACKS
7. Onboarding step 4 shows farm scene with real pixel art sprites
8. Onboarding step 4 uses setup.farm.* i18n keys
9. Old setup.mascot.* keys preserved for other usages
</verification>

<success_criteria>
- 2 new template packs are functional and registered in TEMPLATE_PACKS
- Stock pack generates table-format stock file matching parseStock() expectations
- Defis pack reuses DEFI_TEMPLATES (no content duplication)
- Onboarding step 4 visually shows the farm with pixel art sprites
- All content uses t() with bilingual i18n keys
- No TypeScript compilation errors introduced
</success_criteria>

<output>
After completion, create `.planning/phases/12-templates-onboarding-vivants-contenu-personnalis-et-complet/12-02-SUMMARY.md`
</output>
