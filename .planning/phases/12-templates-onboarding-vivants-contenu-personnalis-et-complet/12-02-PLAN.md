---
phase: 12-templates-onboarding-vivants-contenu-personnalis-et-complet
plan: 02
type: execute
wave: 2
depends_on:
  - 12-01
files_modified:
  - lib/vault-templates.ts
  - locales/fr/common.json
  - locales/en/common.json
autonomous: true
requirements:
  - TMPL-06
  - TMPL-07
  - TMPL-08

must_haves:
  truths:
    - "stock-initial pack generates a pantry/fridge stock file with 10+ household items"
    - "stock-initial pack adds baby items section when family has a bebe"
    - "defis-lancement pack generates 2-3 starter family challenges with correct profile ID slugs"
    - "defis-lancement pack uses correct parseDefis() format with all required fields"
    - "Both new packs are added to TEMPLATE_PACKS export array"
    - "All new strings use t() with keys in both FR and EN locale files"
  artifacts:
    - path: "lib/vault-templates.ts"
      provides: "2 new template packs: stockInitial and defisLancement"
      contains: "stock-initial"
    - path: "locales/fr/common.json"
      provides: "French i18n keys for stock-initial and defis-lancement packs"
      contains: "stockInitial"
    - path: "locales/en/common.json"
      provides: "English i18n keys for stock-initial and defis-lancement packs"
      contains: "stockInitial"
  key_links:
    - from: "lib/vault-templates.ts"
      to: "locales/fr/common.json"
      via: "t() calls with setup.templateContent.stockInitial and defis keys"
      pattern: "stockInitial|defisLancement"
    - from: "lib/vault-templates.ts"
      to: "lib/vault.ts"
      via: "installTemplates reads TEMPLATE_PACKS array"
      pattern: "TEMPLATE_PACKS"
---

<objective>
Add 2 new template packs (stock-initial and defis-lancement) to give day-1 value for pantry tracking and gamification challenges.

Purpose: New users who select these packs get a pre-populated pantry/fridge stock list and 2-3 active family challenges so the gamification and stock features feel immediately useful.
Output: 2 new TemplatePack objects in `lib/vault-templates.ts`, ~30 new i18n keys in both locale files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-templates-onboarding-vivants-contenu-personnalis-et-complet/12-RESEARCH.md
@.planning/phases/12-templates-onboarding-vivants-contenu-personnalis-et-complet/12-01-SUMMARY.md
</context>

<interfaces>
<!-- Key formats the executor needs from RESEARCH.md -->

Stock file format (from _stockContent() in vault.ts):

| Produit | Detail | Qte restante | Seuil | Qte/achat |
| ------- | ------ | ------------ | ----- | --------- |
| Pates   |        | 3            | 1     | 3         |

File must have frontmatter with tags: [stock, maison] and cssclasses: [stock].
Must have backlink: > <- [[00 - Dashboard/Dashboard|Dashboard]]

Defis file format (from parseDefis() in parser.ts):
Each defi starts with ## Title, then key-value lines (id, type, emoji, difficulty, startDate, endDate, targetDays, participants, status, rewardPoints, rewardLootBoxes, description), ends with ### Progression.

Profile ID slug formula:
name.toLowerCase().normalize('NFD').replace(/[accents]/g, '').replace(/spaces/g, '-')
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add i18n keys for stock-initial and defis-lancement packs</name>
  <files>locales/fr/common.json, locales/en/common.json</files>
  <read_first>locales/fr/common.json (lines 193-390, the templateContent block including keys added by Plan 01), locales/en/common.json (same range)</read_first>
  <action>
Add 2 new key blocks inside `setup.templateContent` in both FR and EN locale files.

**1. stock-initial keys** -- add `"stockInitial"` block:

FR values / EN values:
- "title": "Stock maison" / "Household stock"
- "epicerie": "Placards -- Epicerie" / "Pantry -- Dry goods"
- "frigo": "Frigo" / "Fridge"
- "congelo": "Congelateur" / "Freezer"
- "pates": "Pates" / "Pasta"
- "riz": "Riz" / "Rice"
- "huile": "Huile d'olive" / "Olive oil"
- "farine": "Farine" / "Flour"
- "sucre": "Sucre" / "Sugar"
- "sel": "Sel" / "Salt"
- "conservesTomates": "Conserves de tomates" / "Canned tomatoes"
- "cafe": "Cafe" / "Coffee"
- "the": "The" / "Tea"
- "cereales": "Cereales" / "Cereal"
- "lait": "Lait" / "Milk"
- "beurre": "Beurre" / "Butter"
- "oeufs": "Oeufs" / "Eggs"
- "fromage": "Fromage" / "Cheese"
- "yaourts": "Yaourts" / "Yogurts"
- "legumes": "Legumes frais" / "Fresh vegetables"
- "fruits": "Fruits" / "Fruits"
- "painCongele": "Pain congele" / "Frozen bread"
- "legumesC": "Legumes congeles" / "Frozen vegetables"
- "glaces": "Glaces" / "Ice cream"
- "couches": "Couches" / "Diapers"
- "lingettes": "Lingettes" / "Wipes"
- "petitsPots": "Petits pots / compotes" / "Baby food / purees"
- "laitInfantile": "Lait infantile" / "Baby formula"
- "bebeHeader": "Produits bebe" / "Baby products"

**2. defis-lancement keys** -- add `"defisLancement"` block:

FR values / EN values:
- "title": "Defis familiaux" / "Family challenges"
- "defi1Title": "Soiree en famille" / "Family evening"
- "defi1Desc": "Passer une soiree sans ecran ensemble -- jeux de societe, cuisine, ou balade" / "Spend a screen-free evening together -- board games, cooking, or a walk"
- "defi2Title": "Ranger ensemble" / "Tidy up together"
- "defi2Desc": "Chaque jour, 10 minutes de rangement tous ensemble avant le diner" / "Every day, 10 minutes of tidying up together before dinner"
- "defi3Title": "Compliment du jour" / "Compliment of the day"
- "defi3Desc": "Chaque membre dit un compliment sincere a un autre membre de la famille" / "Each family member gives a sincere compliment to another family member"

IMPORTANT: Use proper French accents in actual JSON values (e with accent aigu for Cereales, Congele, etc.). Match the accent style of existing keys.
Ensure both FR and EN files have EXACTLY the same key structure.
  </action>
  <verify>
    <automated>node -e "const fr=require('./locales/fr/common.json'); const en=require('./locales/en/common.json'); const tc=fr.setup.templateContent; const tce=en.setup.templateContent; const checks=['stockInitial.pates','stockInitial.lait','stockInitial.bebeHeader','defisLancement.defi1Title','defisLancement.defi2Title','defisLancement.defi3Title','defisLancement.defi1Desc']; let ok=true; for(const c of checks){const parts=c.split('.'); let vf=tc,ve=tce; for(const p of parts){vf=vf?.[p];ve=ve?.[p]} if(!vf||!ve){console.log('MISSING:',c,'fr=',!!vf,'en=',!!ve);ok=false}} if(ok)console.log('ALL_STOCK_DEFIS_KEYS_PRESENT'); else process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
- `locales/fr/common.json` contains key `setup.templateContent.stockInitial.pates` with non-empty FR value
- `locales/en/common.json` contains key `setup.templateContent.stockInitial.pates` with value "Pasta"
- `locales/fr/common.json` contains key `setup.templateContent.defisLancement.defi1Title` with non-empty FR value
- `locales/en/common.json` contains key `setup.templateContent.defisLancement.defi1Title` with value "Family evening"
- `locales/fr/common.json` contains key `setup.templateContent.stockInitial.bebeHeader` (baby section header)
- Both FR and EN files are valid JSON
  </acceptance_criteria>
  <done>~30 new i18n keys exist in both FR and EN for stock-initial and defis-lancement template packs.</done>
</task>

<task type="auto">
  <name>Task 2: Create stock-initial and defis-lancement template packs</name>
  <files>lib/vault-templates.ts</files>
  <read_first>lib/vault-templates.ts (full file, will be ~850+ lines after Plan 01), lib/vault.ts (search for _stockContent to see stock file format pattern), .planning/phases/12-templates-onboarding-vivants-contenu-personnalis-et-complet/12-RESEARCH.md (file format references for stock and defis)</read_first>
  <action>
Add 2 new TemplatePack objects in `lib/vault-templates.ts` BEFORE the export section. Then add them to the `TEMPLATE_PACKS` array.

**A. stockInitial pack:**

Create a `const stockInitial: TemplatePack` with:
- id: 'stock-initial'
- name: 'Stock maison'
- emoji: the convenience store emoji
- description: 'Inventaire de depart pour placards, frigo et congelateur'
- generate(ctx): computes `hasBebe = ctx.children.some(c => c.ageCategory === 'bebe')`
- Uses `const p = 'setup.templateContent.stockInitial'`
- Generates a stock markdown file at path `02 - Maison/Stock maison.md`
- File has frontmatter: tags [stock, maison], cssclasses [stock]
- Has backlink: `> <- [[00 - Dashboard/Dashboard|Dashboard]]`
- 3 sections: Epicerie (10 items), Frigo (7 items), Congelateur (3 items)
- Each section has table format: `| Produit | Detail | Qte restante | Seuil | Qte/achat |`
- Conditional 4th section: baby products (4 items) when hasBebe is true
- All product names use t() with stockInitial.* keys
- Returns single file, NOT append mode (new file)

**B. defisLancement pack:**

Create a `const defisLancement: TemplatePack` with:
- id: 'defis-lancement'
- name: 'Defis de lancement'
- emoji: the target/bullseye emoji
- description: '2-3 defis familiaux pour demarrer la gamification'
- generate(ctx): 
  - Computes profile ID slugs: `const slug = (name: string) => name.toLowerCase().normalize('NFD').replace(/[\u0300-\u036f]/g, '').replace(/\s+/g, '-');`
  - Builds participants string from all parent + child slugs joined by comma
  - Uses `const p = 'setup.templateContent.defisLancement'`
  - Computes endDate = futureDate(today, 7) using existing helper
  - Generates 3 defis in parseDefis() format:
    - Defi 1: id=defi_launch_1, type=daily, emoji=family emoji, difficulty=facile, targetDays=7, rewardPoints=15, rewardLootBoxes=0
    - Defi 2: id=defi_launch_2, type=daily, emoji=broom emoji, difficulty=facile, targetDays=5, rewardPoints=10, rewardLootBoxes=0
    - Defi 3: id=defi_launch_3, type=daily, emoji=heart emoji, difficulty=facile, targetDays=7, rewardPoints=10, rewardLootBoxes=1
  - Each defi has: ## Title, then flat key-value lines (id, type, emoji, difficulty, startDate, endDate, targetDays, participants, status, rewardPoints, rewardLootBoxes, description), then ### Progression
  - File has frontmatter: tags [defis]
  - Returns single file at path `defis.md` with `append: true` (scaffold may have created it already)

**C. Update TEMPLATE_PACKS:**
Add stockInitial and defisLancement at the end of the array (after vieDeFamille).

**D. Do NOT add new packs to DEFAULT_SELECTED_PACKS** -- they are opt-in.

IMPORTANT: In the defisLancement generate function, the variable `p` for the i18n prefix will shadow the outer `ctx.parents.map(p => ...)` -- rename one of them. Use `const pr = 'setup.templateContent.defisLancement'` or rename the map parameter to `par`.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -c "vault-templates" || echo "0 errors"; node -e "const m=require('fs').readFileSync('lib/vault-templates.ts','utf8'); console.log('stock-initial:', m.includes(\"id: 'stock-initial'\")); console.log('defis-lancement:', m.includes(\"id: 'defis-lancement'\")); console.log('in TEMPLATE_PACKS:', m.includes('defisLancement') && m.includes('stockInitial'))"</automated>
  </verify>
  <acceptance_criteria>
- `lib/vault-templates.ts` contains `id: 'stock-initial'` for the new stock pack
- `lib/vault-templates.ts` contains `id: 'defis-lancement'` for the new defis pack
- `lib/vault-templates.ts` TEMPLATE_PACKS array contains `stockInitial` and `defisLancement`
- `lib/vault-templates.ts` stockInitial.generate checks `hasBebe` for conditional baby section
- `lib/vault-templates.ts` defisLancement.generate computes profile ID slugs using `toLowerCase().normalize('NFD').replace` formula
- `lib/vault-templates.ts` defisLancement uses `append: true` on the returned file
- `lib/vault-templates.ts` defisLancement content includes `id: defi_launch_1`, `type: daily`, `status: active`, `### Progression` (parseDefis format)
- `npx tsc --noEmit` reports 0 new errors in vault-templates.ts
  </acceptance_criteria>
  <done>2 new template packs (stock-initial, defis-lancement) are added to TEMPLATE_PACKS. Stock pack generates pantry/fridge/freezer with conditional baby items. Defis pack generates 3 starter challenges with correct profile slugs and parseDefis-compatible format.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes without new errors
2. Both locale files are valid JSON with stockInitial and defisLancement key blocks
3. TEMPLATE_PACKS array has 9 elements (7 original + 2 new)
4. Stock pack conditionally adds baby section
5. Defis pack uses slug formula for participants field
</verification>

<success_criteria>
- 2 new template packs are functional and registered in TEMPLATE_PACKS
- Stock pack generates table-format stock file matching parseStock() expectations
- Defis pack generates key-value format matching parseDefis() expectations
- Profile ID slugs match the formula used in scaffoldVault()
- All content uses t() with bilingual i18n keys
- No TypeScript compilation errors introduced
</success_criteria>

<output>
After completion, create `.planning/phases/12-templates-onboarding-vivants-contenu-personnalis-et-complet/12-02-SUMMARY.md`
</output>
