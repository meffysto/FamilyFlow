---
phase: 12-templates-onboarding-vivants-contenu-personnalis-et-complet
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/vault-templates.ts
  - locales/fr/common.json
  - locales/en/common.json
autonomous: true
requirements:
  - TMPL-01
  - TMPL-02
  - TMPL-03
  - TMPL-04
  - TMPL-05
  - TMPL-08

must_haves:
  truths:
    - "repas-semaine includes petit-dej on weekdays and gouter row when family has children"
    - "budget-familial monthly file contains 5+ example entries with category names matching DEFAULT_BUDGET_CONFIG"
    - "anniversaires always generates a file even without children, with placeholder extended family rows"
    - "vie-de-famille generates 3-5 examples per file instead of 1"
    - "suivi-medical RDV files have non-empty heure/medecin placeholders"
    - "All new strings use t() with keys present in both FR and EN locale files"
  artifacts:
    - path: "lib/vault-templates.ts"
      provides: "Enriched template packs for repas-semaine, budget-familial, anniversaires, vie-de-famille, suivi-medical"
      contains: "petitDej"
    - path: "locales/fr/common.json"
      provides: "French i18n keys for all enriched template content"
      contains: "petitDejLundi"
    - path: "locales/en/common.json"
      provides: "English i18n keys for all enriched template content"
      contains: "petitDejLundi"
  key_links:
    - from: "lib/vault-templates.ts"
      to: "locales/fr/common.json"
      via: "t() calls with setup.templateContent.* keys"
      pattern: "t\\(`\\$\\{p\\}\\."
    - from: "lib/vault-templates.ts"
      to: "lib/budget.ts"
      via: "budget category names alignment"
      pattern: "Courses|Transport|Sante|Loisirs"
---

<objective>
Enrich 5 existing template packs so the onboarding vault feels alive from day 1 instead of showing empty forms and skeletal content.

Purpose: New users currently see empty budget sections, only 1 example in vie-de-famille files, no weekday petit-dej in meal plans, and no extended family in anniversaires. This plan fixes all of that.
Output: Modified `lib/vault-templates.ts` with enriched packs and ~50 new i18n keys in both locale files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-templates-onboarding-vivants-contenu-personnalis-et-complet/12-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add i18n keys for enriched template content (FR + EN)</name>
  <files>locales/fr/common.json, locales/en/common.json</files>
  <read_first>locales/fr/common.json (lines 193-386, the templateContent block), locales/en/common.json (lines 193-386), lib/budget.ts (lines 29-39, DEFAULT_BUDGET_CONFIG categories)</read_first>
  <action>
Add new i18n keys inside the existing `setup.templateContent` object in both FR and EN locale files. Add them at the END of the `templateContent` block (before the closing `}` of templateContent, before the `"recap"` key).

**1. repas-semaine keys** — add under `setup.templateContent.repas`:
```json
"petitDejLabel": "Petit-dej" (FR) / "Breakfast" (EN),
"gouterLabel": "Gouter" (FR) / "Snack" (EN),
"lundiPetitDej": "Tartines beurre-confiture + jus d'orange" (FR) / "Toast with butter & jam + orange juice" (EN),
"mardiPetitDej": "Cereales + lait + fruit" (FR) / "Cereal + milk + fruit" (EN),
"mercrediPetitDej": "Pain au chocolat + yaourt" (FR) / "Chocolate croissant + yogurt" (EN),
"jeudiPetitDej": "Tartines miel + compote" (FR) / "Toast with honey + applesauce" (EN),
"vendrediPetitDej": "Cereales + jus de fruits" (FR) / "Cereal + fruit juice" (EN),
"lundiGouter": "Compote + biscuits" (FR) / "Applesauce + cookies" (EN),
"mardiGouter": "Fruit + pain au chocolat" (FR) / "Fruit + chocolate croissant" (EN),
"mercrediGouter": "Yaourt + cereales" (FR) / "Yogurt + cereal" (EN),
"jeudiGouter": "Tartine confiture + lait" (FR) / "Jam toast + milk" (EN),
"vendrediGouter": "Gateau maison + fruit" (FR) / "Homemade cake + fruit" (EN)
```
Note: The existing key `petitDej` is already "Petit-dej" / "Breakfast" and is used for weekends. The new `petitDejLabel` is for the weekday label. Actually, reuse the existing `petitDej` key for weekday labels too — no need for `petitDejLabel`. Just add the 5 weekday petit-dej content keys and 5 gouter content keys + `gouterLabel`.

**2. budget keys** — Fix category mismatch. Replace `alimentation` key value:
- FR: change `"alimentation": "Alimentation"` to `"alimentation": "Courses"` 
- EN: keep `"alimentation": "Groceries"` (already correct)

Add new budget keys under `setup.templateContent.budget`:
```json
"bebe": "Bebe" (FR) / "Baby" (EN),
"abonnements": "Abonnements" (FR) / "Subscriptions" (EN),
"exempleSuffix": "(exemple)" (FR) / "(example)" (EN),
"exempleCourses": "Supermarche hebdomadaire" (FR) / "Weekly groceries" (EN),
"exempleTransport": "Carburant" (FR) / "Gas" (EN),
"exempleSante": "Pharmacie" (FR) / "Pharmacy" (EN),
"exempleLoisirs": "Cinema en famille" (FR) / "Family movie night" (EN),
"exempleEnfants": "Fournitures scolaires" (FR) / "School supplies" (EN),
"exempleMaison": "Reparation plomberie" (FR) / "Plumbing repair" (EN),
"exempleDivers": "Cadeau anniversaire" (FR) / "Birthday gift" (EN),
"depenses": "Depenses" (FR) / "Expenses" (EN)
```

**3. anniversaires keys** — add new block under `setup.templateContent`:
```json
"anniversaires": {
  "title": "Anniversaires" (FR) / "Birthdays" (EN),
  "aCompleter": "A completer" (FR) / "To complete" (EN),
  "grandMereMaternelle": "Grand-mere Maternelle" (FR) / "Maternal Grandmother" (EN),
  "grandPerePaternelle": "Grand-pere Paternel" (FR) / "Paternal Grandfather" (EN),
  "oncleTante": "Oncle/Tante" (FR) / "Uncle/Aunt" (EN),
  "cousin": "Cousin(e)" (FR) / "Cousin" (EN),
  "ami": "Ami(e) proche" (FR) / "Close Friend" (EN),
  "aMetteAJour": "A mettre a jour" (FR) / "To be updated" (EN)
}
```

**4. vie-de-famille keys** — add additional examples under existing `setup.templateContent.vieDeFamille`:
```json
"gratitude": {
  ... existing keys ...,
  "example2": "Les enfants ont joue ensemble toute l'apres-midi sans se disputer." (FR) / "The kids played together all afternoon without arguing." (EN),
  "example3": "Un bon repas en famille ce dimanche." (FR) / "A nice family meal this Sunday." (EN),
  "example4": "Reconnaissant pour cette balade en foret." (FR) / "Grateful for that walk in the forest." (EN)
},
"moods": {
  ... existing keys ...,
  "example2": "Journee chargee mais productive" (FR) / "Busy but productive day" (EN),
  "example3": "Fatigue mais content du weekend" (FR) / "Tired but happy with the weekend" (EN)
},
"quotes": {
  ... existing keys ...,
  "example2": "Maman, c'est vrai que les poissons boivent de l'eau de mer ?" (FR) / "Mom, do fish really drink sea water?" (EN),
  "context2": "Au diner" (FR) / "At dinner" (EN),
  "example3": "Quand je serai grand, je veux etre astronaute-pompier" (FR) / "When I grow up, I want to be an astronaut-firefighter" (EN),
  "context3": "Avant de dormir" (FR) / "Before bed" (EN)
},
"wishlist": {
  ... existing keys ...,
  "exampleParent2": "Massage relaxant | 60eur | Fete des meres" (FR) / "Relaxing massage | $60 | Mother's Day" (EN),
  "exampleParent3": "Montre connectee | 200eur | Anniversaire" (FR) / "Smart watch | $200 | Birthday" (EN),
  "exampleChild2": "Jeu de societe | 30eur | Noel" (FR) / "Board game | $30 | Christmas" (EN)
}
```

**5. suivi-medical keys** — add under `setup.templateContent.medical`:
```json
"heureDefaut": "A definir" (FR) / "To be scheduled" (EN),
"medecinDefaut": "A definir" (FR) / "To be determined" (EN),
"lieuDefaut": "Cabinet medical habituel" (FR) / "Usual doctor's office" (EN)
```

IMPORTANT: Use proper French accents in actual JSON values (e, a with accents). The values shown above are simplified — use proper Unicode: "A definir" should be "A definir", "Cereales" should be "Cereales", etc. Match the accent style of existing keys in the file.

IMPORTANT: Ensure both FR and EN files have EXACTLY the same key structure. Every key added to FR must exist in EN with the English translation.
  </action>
  <verify>
    <automated>node -e "const fr=require('./locales/fr/common.json'); const en=require('./locales/en/common.json'); const tc=fr.setup.templateContent; const tce=en.setup.templateContent; const checks=['repas.lundiPetitDej','repas.gouterLabel','budget.exempleCourses','budget.abonnements','anniversaires.grandMereMaternelle','vieDeFamille.gratitude.example2','vieDeFamille.moods.example2','vieDeFamille.quotes.example2','vieDeFamille.wishlist.exampleParent2','medical.heureDefaut']; let ok=true; for(const c of checks){const parts=c.split('.'); let vf=tc,ve=tce; for(const p of parts){vf=vf?.[p];ve=ve?.[p]} if(!vf||!ve){console.log('MISSING:',c,'fr=',!!vf,'en=',!!ve);ok=false}} if(ok)console.log('ALL_KEYS_PRESENT'); else process.exit(1); if(tc.budget.alimentation==='Courses')console.log('BUDGET_CATEGORY_FIXED'); else{console.log('BUDGET_CATEGORY_NOT_FIXED:',tc.budget.alimentation);process.exit(1)}"</automated>
  </verify>
  <acceptance_criteria>
- `locales/fr/common.json` contains key `setup.templateContent.repas.lundiPetitDej` with value containing "Tartines"
- `locales/en/common.json` contains key `setup.templateContent.repas.lundiPetitDej` with value containing "Toast"
- `locales/fr/common.json` contains key `setup.templateContent.budget.alimentation` with value `"Courses"` (NOT "Alimentation")
- `locales/fr/common.json` contains key `setup.templateContent.anniversaires.grandMereMaternelle` with a non-empty string value
- `locales/fr/common.json` contains key `setup.templateContent.vieDeFamille.gratitude.example2` with a non-empty string value
- `locales/fr/common.json` contains key `setup.templateContent.medical.heureDefaut` with a non-empty string value
- Both FR and EN files are valid JSON (no syntax errors)
  </acceptance_criteria>
  <done>All ~50 new i18n keys exist in both FR and EN locale files with correct translations. Budget category "alimentation" value is "Courses" in FR. Both files parse as valid JSON.</done>
</task>

<task type="auto">
  <name>Task 2: Enrich 5 existing template packs in vault-templates.ts</name>
  <files>lib/vault-templates.ts</files>
  <read_first>lib/vault-templates.ts (full file — 714 lines), lib/budget.ts (lines 29-39, DEFAULT_BUDGET_CONFIG), .planning/phases/12-templates-onboarding-vivants-contenu-personnalis-et-complet/12-RESEARCH.md (file format references)</read_first>
  <action>
Modify 5 existing packs in `lib/vault-templates.ts`. Keep all existing helper functions and types unchanged.

**A. repas-semaine (repasSemaine, line ~115):**

1. Change `generate: ()` to `generate: (ctx)` to receive the context.
2. Compute `const hasChildren = ctx.children.some(c => ['petit', 'enfant', 'ado'].includes(c.ageCategory));`
3. Add petit-dej row on weekdays (Mon-Fri) using existing `petitDej` key as label and new content keys:
```
## ${t(`${p}.lundi`)}
- ${t(`${p}.petitDej`)}: ${t(`${p}.lundiPetitDej`)}
- ${t(`${p}.dejeuner`)}: ${t(`${p}.lundiDej`)}
- ${t(`${p}.diner`)}: ${t(`${p}.lundiDin`)}
```
Apply same pattern for mardi through vendredi.

4. Add optional gouter row after dejeuner when `hasChildren`, for weekdays only:
```
- ${t(`${p}.gouterLabel`)}: ${t(`${p}.lundiGouter`)}
```
Use a conditional: `${hasChildren ? `\n- ${t(`${p}.gouterLabel`)}: ${t(`${p}.lundiGouter`)}` : ''}`

Weekend days (samedi/dimanche) keep their existing 3-meal structure unchanged.

**B. budget-familial (budgetFamilial, line ~524):**

1. Replace config categories to match `DEFAULT_BUDGET_CONFIG` from `lib/budget.ts`. The config content section `## ${t(`${p}.categories`)}` must use these exact category lines:
```
- ${'\u{1F6D2}'} ${t(`${p}.alimentation`)}: 600
- ${'\u{1F476}'} ${t(`${p}.bebe`)}: 200
- ${'\u{1F3E0}'} ${t(`${p}.maison`)}: 300
- ${'\u{1F697}'} ${t(`${p}.transport`)}: 150
- ${'\u{1F389}'} ${t(`${p}.loisirs`)}: 200
- ${'\u{1F3E5}'} ${t(`${p}.sante`)}: 100
- ${'\u{1F4F1}'} ${t(`${p}.abonnements`)}: 80
- ${'\u{1F381}'} ${t(`${p}.divers`)}: 150
```
Wait — use the actual emoji characters like the existing code does: `🛒`, `👶`, `🏠`, `🚗`, `🎉`, `🏥`, `📱`, `🎁`.

2. Replace the monthly file content. Instead of empty sections, generate a `## ${t(`${p}.depenses`)}` section with 7 example entries in `parseBudgetMonth()` format. Each entry format: `- YYYY-MM-DD | EMOJI CATEGORY | AMOUNT | LABEL (exemple)`.

Use `ctx.today` for dates (first 5 days of current month). Compute `const monthStart = format(today, 'yyyy-MM') + '-01';` and use dates from 01 to 07.

Example entries (use t() for labels):
```
- ${moisNum}-01 | 🛒 ${t(`${p}.alimentation`)} | 85.50 | ${t(`${p}.exempleCourses`)} ${t(`${p}.exempleSuffix`)}
- ${moisNum}-02 | 🚗 ${t(`${p}.transport`)} | 60.00 | ${t(`${p}.exempleTransport`)} ${t(`${p}.exempleSuffix`)}
- ${moisNum}-03 | 🏥 ${t(`${p}.sante`)} | 25.00 | ${t(`${p}.exempleSante`)} ${t(`${p}.exempleSuffix`)}
- ${moisNum}-04 | 🎉 ${t(`${p}.loisirs`)} | 35.00 | ${t(`${p}.exempleLoisirs`)} ${t(`${p}.exempleSuffix`)}
- ${moisNum}-05 | 👶 ${t(`${p}.enfants`)} | 45.00 | ${t(`${p}.exempleEnfants`)} ${t(`${p}.exempleSuffix`)}
- ${moisNum}-06 | 🏠 ${t(`${p}.maison`)} | 120.00 | ${t(`${p}.exempleMaison`)} ${t(`${p}.exempleSuffix`)}
- ${moisNum}-07 | 🎁 ${t(`${p}.divers`)} | 30.00 | ${t(`${p}.exempleDivers`)} ${t(`${p}.exempleSuffix`)}
```

Remove all the empty `## EMOJI Category` section headers from the monthly file — the entries go under a single `## ${t(`${p}.depenses`)}` header.

**C. anniversaires (line ~588):**

1. Remove the early return `if (rows.length === 0) return [];` — always generate the file.
2. After the children rows loop, add placeholder extended family rows using i18n keys. Add them in a comment section:
```
|  |  |  |  |  |  |
| ${t(`${pa}.grandMereMaternelle`)} | 03-15 |  | Famille |  | ${t(`${pa}.aMetteAJour`)} |
| ${t(`${pa}.grandPerePaternelle`)} | 06-20 |  | Famille |  | ${t(`${pa}.aMetteAJour`)} |
| ${t(`${pa}.oncleTante`)} 1 | 09-10 |  | Famille |  | ${t(`${pa}.aMetteAJour`)} |
| ${t(`${pa}.cousin`)} 1 | 11-25 |  | Famille |  | ${t(`${pa}.aMetteAJour`)} |
| ${t(`${pa}.ami`)} 1 | 01-30 |  | Amis |  | ${t(`${pa}.aMetteAJour`)} |
```
Where `const pa = 'setup.templateContent.anniversaires';`

3. Add parent birthdates too (if available — parents don't have birthdate in TemplateContext, so skip this).

The blank separator row `|  |  |  |  |  |  |` visually separates real data from placeholders.

**D. vie-de-famille (vieDeFamille, line ~624):**

1. **Gratitude:** Add 3 more example entries with different dates. Compute past dates: `const yesterday = format(addDays(todayDate, -1), 'dd/MM/yyyy');` and `const twoDaysAgo = format(addDays(todayDate, -2), 'dd/MM/yyyy');` (where `todayDate = new Date(ctx.today + 'T12:00:00')`). Use second parent name if available, and first child name.

Add after the existing example:
```

## ${yesterday}

### 🙏 ${secondParent}
${t(`${p}.gratitude.example2`)}

## ${twoDaysAgo}

### 🙏 ${parentName}
${t(`${p}.gratitude.example3`)}
```
If a child exists, add one more:
```

### 🙏 ${childName}
${t(`${p}.gratitude.example4`)}
```

2. **Moods:** Add 2 more rows to the table with past ISO dates:
```
| ${yesterdayIso} | ${secondParent} | 😴 | ${t(`${p}.moods.example2`)} |
| ${twoDaysAgoIso} | ${parentName} | 😌 | ${t(`${p}.moods.example3`)} |
```

3. **Quotes (mots d'enfants):** Add 2 more rows if child exists. Use second child name if available, otherwise same child:
```
| ${yesterdayIso} | ${secondChild || childName} | ${t(`${p}.quotes.example2`)} | ${t(`${p}.quotes.context2`)} |
| ${twoDaysAgoIso} | ${childName} | ${t(`${p}.quotes.example3`)} | ${t(`${p}.quotes.context3`)} |
```

4. **Wishlist:** Add 2 more items per parent section and 1 more per child:
```
## ${parentName}
- [ ] ${t(`${p}.wishlist.exampleParent`)}
- [ ] ${t(`${p}.wishlist.exampleParent2`)}
- [ ] ${t(`${p}.wishlist.exampleParent3`)}
```
For child section:
```
## ${childName}
- [ ] ${t(`${p}.wishlist.exampleChild`)}
- [ ] ${t(`${p}.wishlist.exampleChild2`)}
```

Compute helper variables at top of generate():
```typescript
const todayDate = new Date(ctx.today + 'T12:00:00');
const yesterdayIso = format(addDays(todayDate, -1), 'yyyy-MM-dd');
const twoDaysAgoIso = format(addDays(todayDate, -2), 'yyyy-MM-dd');
const yesterday = format(addDays(todayDate, -1), 'dd/MM/yyyy');
const twoDaysAgo = format(addDays(todayDate, -2), 'dd/MM/yyyy');
const secondParent = ctx.parents[1]?.name || parentName;
const secondChild = ctx.children[1]?.name || '';
```

**E. suivi-medical (suiviMedical, line ~258):**

Replace all `heure: ""` with `heure: "${t(`${pm}.heureDefaut`)}"` and all `medecin: ""` with `medecin: "${t(`${pm}.medecinDefaut`)}"` and all `lieu: ""` with `lieu: "${t(`${pm}.lieuDefaut`)}"` in the frontmatter of generated RDV files. Where `const pm = 'setup.templateContent.medical';` (already defined as `p` in the existing code — reuse it).

Specifically, in every file.push content block that has `heure: ""`, replace with `heure: "${t(`${p}.heureDefaut`)}"`. Same for medecin and lieu.

Also add `addDays` to the existing import if not already there (it IS already imported on line 1).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -c "vault-templates" || echo "0 errors in vault-templates"</automated>
  </verify>
  <acceptance_criteria>
- `lib/vault-templates.ts` repasSemaine.generate takes `(ctx)` parameter (not `()`)
- `lib/vault-templates.ts` contains string `lundiPetitDej` (weekday breakfast keys)
- `lib/vault-templates.ts` contains string `gouterLabel` (gouter row)
- `lib/vault-templates.ts` budget config section contains `abonnements` key reference
- `lib/vault-templates.ts` budget monthly section contains `exempleCourses` key reference  
- `lib/vault-templates.ts` anniversaires generate does NOT contain `if (rows.length === 0) return []`
- `lib/vault-templates.ts` anniversaires contains `grandMereMaternelle` key reference
- `lib/vault-templates.ts` vieDeFamille contains `example2` key reference (gratitude)
- `lib/vault-templates.ts` vieDeFamille contains `exampleParent2` key reference (wishlist)
- `lib/vault-templates.ts` suiviMedical contains `heureDefaut` key reference (no more empty `heure: ""`)
- `npx tsc --noEmit` reports 0 new errors related to vault-templates.ts
  </acceptance_criteria>
  <done>All 5 existing template packs are enriched with richer content. repas-semaine has weekday petit-dej + conditional gouter. budget-familial has example entries matching DEFAULT_BUDGET_CONFIG categories. anniversaires always generates with placeholder family rows. vie-de-famille has 3-5 examples per file. suivi-medical has non-empty placeholder fields.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes without new errors in vault-templates.ts
2. Both locale files are valid JSON: `node -e "require('./locales/fr/common.json'); require('./locales/en/common.json'); console.log('VALID_JSON')"`
3. Budget category alignment: FR alimentation key value is "Courses", matching DEFAULT_BUDGET_CONFIG
4. All new t() calls in vault-templates.ts reference keys that exist in both locale files
</verification>

<success_criteria>
- Template packs generate richer content for 5 existing packs
- Budget category names match DEFAULT_BUDGET_CONFIG exactly
- All new content strings use t() with keys present in both FR and EN
- No TypeScript compilation errors introduced
</success_criteria>

<output>
After completion, create `.planning/phases/12-templates-onboarding-vivants-contenu-personnalis-et-complet/12-01-SUMMARY.md`
</output>
