---
phase: 23-mus-e-des-effets-seed-002-lite
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/museum/engine.ts
  - lib/parser.ts
  - hooks/useGamification.ts
  - locales/fr/common.json
  - locales/en/common.json
autonomous: true
requirements: [MUSEUM-01, MUSEUM-03]

must_haves:
  truths:
    - "Chaque effet sémantique déclenché est enregistré dans gami-{id}.md section ## Musée"
    - "Les entrées musée survivent aux redémarrages (persistance gami-{id}.md)"
    - "serializeGamification ne détruit plus la section ## Musée lors des réécritures"
  artifacts:
    - path: "lib/museum/engine.ts"
      provides: "parseMuseumEntries, appendMuseumEntry, appendMuseumEntryToVault, groupEntriesByWeek"
      exports: ["parseMuseumEntries", "appendMuseumEntry", "appendMuseumEntryToVault", "groupEntriesByWeek", "MuseumEntry"]
    - path: "lib/parser.ts"
      provides: "serializeGamification préserve la section ## Musée"
      contains: "Musée"
  key_links:
    - from: "hooks/useGamification.ts"
      to: "lib/museum/engine.ts"
      via: "appendMuseumEntryToVault fire-and-forget après effectApplied"
      pattern: "appendMuseumEntryToVault"
    - from: "lib/parser.ts"
      to: "lib/museum/engine.ts"
      via: "serializeGamification préserve la section ## Musée existante"
      pattern: "museumSection"
---

<objective>
Moteur de persistance du musée des effets sémantiques : module pur `lib/museum/engine.ts` + injection fire-and-forget dans `completeTask()` + protection de la section `## Musée` contre l'écrasement par `serializeGamification()`.

Purpose: MUSEUM-01 et MUSEUM-03 — chaque effet déclenché est enregistré de manière persistante dans le fichier gami-{id}.md du profil, et survit aux redémarrages et aux réécritures gamification.

Output: Module engine.ts fonctionnel, point d'injection câblé, sérialisation gamification sécurisée, clés i18n musée.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-mus-e-des-effets-seed-002-lite/23-CONTEXT.md
@.planning/phases/23-mus-e-des-effets-seed-002-lite/23-RESEARCH.md

@lib/parser.ts (serializeGamification lignes ~923-978 — CRITICAL: reconstruit le fichier entier, doit préserver ## Musée)
@hooks/useGamification.ts (completeTask lignes ~97-320 — point d'injection après ligne 293)
@lib/semantic/effect-toasts.ts (EFFECT_TOASTS, CATEGORY_VARIANT — source données)
@lib/semantic/categories.ts (CategoryId type)
@lib/semantic/caps.ts (getWeekStart — réutiliser pour groupement semaine)
@lib/vault.ts (VaultManager — readFile/writeFile)

<interfaces>
<!-- Interfaces existantes que l'exécuteur doit connaître -->

From lib/semantic/categories.ts:
```typescript
export type CategoryId =
  | 'menage_quotidien' | 'menage_saisonnier' | 'courses'
  | 'enfants_routines' | 'enfants_devoirs' | 'sante_rdv'
  | 'gratitude_anniversaires' | 'budget_admin' | 'bebe_soins'
  | 'cuisine_repas';
```

From lib/semantic/effect-toasts.ts:
```typescript
export const EFFECT_TOASTS: Record<CategoryId, EffectToastDef>;
// EffectToastDef = { icon: string; fr: string; en: string; subtitle_fr: string; subtitle_en: string; type: 'success'|'info' }
export const CATEGORY_VARIANT: Record<CategoryId, HarvestBurstVariant>;
```

From lib/semantic/caps.ts:
```typescript
export function getWeekStart(now?: Date): string; // 'YYYY-MM-DD' du lundi
```

From lib/parser.ts (ligne 923):
```typescript
export function serializeGamification(data: GamificationData): string;
// Reconstruit le fichier ENTIER — sections: profils, Récompenses actives, Récompenses utilisées, Journal des gains
// NE préserve PAS les sections inconnues → ## Musée serait détruit
```

From hooks/useGamification.ts (ligne 173):
```typescript
await vault.writeFile(file, serializeGamification(singleData));
// Écrit le fichier gami-{id}.md AVANT le bloc feedback (ligne 271-293)
// Le musée doit être appendé APRÈS (ligne ~293) et survivre au prochain appel de serializeGamification
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Créer lib/museum/engine.ts + protéger serializeGamification</name>
  <files>lib/museum/engine.ts, lib/parser.ts</files>
  <read_first>
    - lib/parser.ts (lignes 820-978 — parseGamification + serializeGamification complet)
    - lib/semantic/categories.ts (CategoryId type)
    - lib/semantic/caps.ts (getWeekStart export)
    - lib/semantic/effect-toasts.ts (EFFECT_TOASTS structure)
    - lib/vault.ts (VaultManager.readFile/writeFile)
  </read_first>
  <action>
    **1. Créer `lib/museum/engine.ts`** (module pur, zéro import hook/context — per D-03c) :

    - Type `MuseumEntry = { date: Date; categoryId: CategoryId; icon: string; label: string }` (per D-01d)
    - `parseMuseumEntries(content: string): MuseumEntry[]` — trouve la section `## Musée`, parse chaque ligne `- YYYY-MM-DDTHH:mm:ss | categoryId | icon label` (per D-01c). Si la section n'existe pas, retourne `[]`. Lignes malformées silencieusement ignorées.
    - `appendMuseumEntry(content: string, entry: MuseumEntry): string` — ajoute une ligne à la fin de la section `## Musée` (per D-01c). Si la section n'existe pas, la crée. Timestamp : `entry.date.toISOString().slice(0, 19)` (supprime le Z — heure locale, per Pitfall 4 de RESEARCH.md). ATTENTION Pitfall 5 : si `## Musée` n'est PAS la dernière section, insérer AVANT la section suivante `## `. Sinon, appendre en fin de fichier.
    - `extractMuseumSection(content: string): string` — extrait la section `## Musée` complète (header + lignes) depuis un contenu gami-{id}.md. Retourne `''` si absente. Utilisée par serializeGamification pour la préserver.
    - `groupEntriesByWeek(entries: MuseumEntry[]): Array<{ weekKey: string; weekLabel: string; data: MuseumEntry[] }>` — utilise `getWeekStart()` de `lib/semantic/caps.ts`. Tri descendant (plus récent en haut). Header : "Cette semaine" si semaine courante, sinon "Semaine du DD/MM/AAAA" via `format(date, 'dd/MM/yyyy')` de date-fns avec `getDateLocale()`.
    - `formatRelativeTime(date: Date): string` — "il y a 2h" si < 24h (via `formatDistanceToNow`), "Hier HH:mm" si hier, "EEE HH:mm" sinon. Utiliser `getDateLocale()`.
    - `appendMuseumEntryToVault(vault: VaultManager, profileId: string, entry: MuseumEntry): Promise<void>` — lit `gami-{profileId}.md`, appelle `appendMuseumEntry`, réécrit. Import VaultManager depuis `../vault` (seul import non-pur, isolé dans cette fonction async).

    **2. Modifier `lib/parser.ts` — serializeGamification** (CRITICAL per pitfall orchestrateur) :

    - Ajouter `'Musée'` à `RESERVED_SECTIONS` dans `parseGamification()` (per Pitfall 2 de RESEARCH.md — empêche la création d'un profil fictif "musée").
    - Modifier la signature de `serializeGamification` : `serializeGamification(data: GamificationData, museumSection?: string): string`. Si `museumSection` est fourni et non vide, l'appendre après le `## Journal des gains`. Ceci garantit que la section `## Musée` survit aux réécritures.
    - Dans `hooks/useGamification.ts` (task 2), les sites d'appel de `serializeGamification` passent le `museumSection` extrait du contenu précédent.

    **3. Pas de barrel file** — engine.ts est le seul fichier dans lib/museum/, un barrel serait redondant.
  </action>
  <verify>
    <automated>cd /Users/gabrielwaltio/Documents/family-vault && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "parseMuseumEntries" lib/museum/engine.ts
    - grep -q "appendMuseumEntry" lib/museum/engine.ts
    - grep -q "extractMuseumSection" lib/museum/engine.ts
    - grep -q "groupEntriesByWeek" lib/museum/engine.ts
    - grep -q "appendMuseumEntryToVault" lib/museum/engine.ts
    - grep -q "Musée" lib/parser.ts (dans RESERVED_SECTIONS)
    - grep -q "museumSection" lib/parser.ts (paramètre de serializeGamification)
  </acceptance_criteria>
  <done>
    - lib/museum/engine.ts exporte parseMuseumEntries, appendMuseumEntry, extractMuseumSection, groupEntriesByWeek, formatRelativeTime, appendMuseumEntryToVault, MuseumEntry
    - serializeGamification accepte museumSection? et le préserve
    - 'Musée' est dans RESERVED_SECTIONS de parseGamification
    - `npx tsc --noEmit` passe sans erreur nouvelle
  </done>
</task>

<task type="auto">
  <name>Task 2: Injection fire-and-forget dans completeTask + clés i18n</name>
  <files>hooks/useGamification.ts, locales/fr/common.json, locales/en/common.json</files>
  <read_first>
    - hooks/useGamification.ts (lignes 95-320 — completeTask complet, sites d'appel serializeGamification)
    - lib/museum/engine.ts (créé en Task 1)
    - locales/fr/common.json (structure existante pour ajouter museum.*)
    - locales/en/common.json (parité EN)
  </read_first>
  <action>
    **1. Modifier `hooks/useGamification.ts`** :

    - Importer `{ appendMuseumEntryToVault, extractMuseumSection }` depuis `'../lib/museum/engine'` et `{ MuseumEntry }` type-only.
    - **Site d'appel serializeGamification ligne 173** : AVANT le write, extraire la section musée du contenu lu précédemment : `const museumSection = extractMuseumSection(gamiContent);` (gamiContent est déjà lu ligne 105). Passer à serializeGamification : `serializeGamification(singleData, museumSection)`.
    - **Site d'appel serializeGamification ligne 366** (openLootBox) : même pattern — extraire museumSection du gamiContent lu au début du bloc, le passer à `serializeGamification(singleData, museumSection)`.
    - **Site d'appel serializeGamification ligne 308** (onDataChange merge) : celui-ci est en lecture seule (pas de writeFile) — ne génère pas de contenu fichier, seulement un merge mémoire. Pas besoin de museumSection ici.
    - **Point d'injection MUSEUM-01** (après ligne 293, dans le bloc `if (effectResult?.effectApplied && derivedCategory)`) :
      ```
      // Phase 23 : Musée des effets (MUSEUM-01, MUSEUM-03)
      try {
        const catId = derivedCategory.id;
        const toastDef = EFFECT_TOASTS[catId];
        if (toastDef) {
          const lang = i18n.language?.startsWith('en') ? 'en' : 'fr';
          const label = lang === 'en' ? toastDef.en : toastDef.fr;
          const museumEntry: MuseumEntry = {
            date: new Date(),
            categoryId: catId,
            icon: toastDef.icon,
            label,
          };
          appendMuseumEntryToVault(vault, profile.id, museumEntry).catch(() => {});
        }
      } catch { /* Musée — non-critical */ }
      ```
    - Placer ce bloc APRÈS la ligne `SecureStore.setItemAsync('last_semantic_category', catId).catch(() => {});` (ligne 289) et APRÈS le write farm-{id}.md (ligne 296). Idéalement juste avant le `dispatchNotificationAsync` (ligne 298).

    **2. Ajouter clés i18n** dans `locales/fr/common.json` sous `"museum"` :
    ```json
    "museum": {
      "title": "Musée des effets",
      "thisWeek": "Cette semaine",
      "weekHeader": "Semaine du {{date}}",
      "empty": "Aucun effet enregistré — complète des tâches pour remplir le musée !",
      "variant": {
        "ambient": "Ambiant",
        "rare": "Rare",
        "golden": "Épique"
      }
    }
    ```

    **3. Parité EN** dans `locales/en/common.json` :
    ```json
    "museum": {
      "title": "Effects Museum",
      "thisWeek": "This week",
      "weekHeader": "Week of {{date}}",
      "empty": "No effects recorded — complete tasks to fill the museum!",
      "variant": {
        "ambient": "Ambient",
        "rare": "Rare",
        "golden": "Epic"
      }
    }
    ```
  </action>
  <verify>
    <automated>cd /Users/gabrielwaltio/Documents/family-vault && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "appendMuseumEntryToVault" hooks/useGamification.ts
    - grep -q "extractMuseumSection" hooks/useGamification.ts
    - grep -q "museumSection" hooks/useGamification.ts
    - grep -q "Musée — non-critical" hooks/useGamification.ts
    - grep -q '"museum"' locales/fr/common.json
    - grep -q '"museum"' locales/en/common.json
    - grep -q "thisWeek" locales/fr/common.json
    - grep -q "thisWeek" locales/en/common.json
  </acceptance_criteria>
  <done>
    - completeTask() enregistre chaque effet appliqué dans gami-{id}.md via appendMuseumEntryToVault (fire-and-forget)
    - Tous les sites serializeGamification passent museumSection pour préserver la section
    - Clés i18n museum.* présentes en FR et EN avec parité stricte
    - `npx tsc --noEmit` passe sans erreur nouvelle
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — zéro erreur nouvelle
2. `grep -r "appendMuseumEntryToVault" hooks/useGamification.ts` — point d'injection présent
3. `grep -r "museumSection" lib/parser.ts hooks/useGamification.ts` — préservation câblée
4. `grep -r "Musée" lib/parser.ts` — dans RESERVED_SECTIONS
5. `grep -r "museum" locales/fr/common.json locales/en/common.json` — clés i18n présentes
</verification>

<success_criteria>
- Les entrées musée sont persistées dans gami-{id}.md section ## Musée après chaque effet sémantique (MUSEUM-01)
- La section ## Musée survit aux réécritures de serializeGamification (MUSEUM-03, pitfall critique)
- Le module lib/museum/engine.ts est pur (zéro import hook/context) et exporte toutes les fonctions nécessaires au Plan 02
- Les clés i18n museum.* sont disponibles en FR et EN
</success_criteria>

<output>
After completion, create `.planning/phases/23-mus-e-des-effets-seed-002-lite/23-01-SUMMARY.md`
</output>
