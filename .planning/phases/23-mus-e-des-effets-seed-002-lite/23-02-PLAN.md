---
phase: 23-mus-e-des-effets-seed-002-lite
plan: 02
type: execute
wave: 2
depends_on: ["23-01"]
files_modified:
  - components/mascot/MuseumModal.tsx
  - app/(tabs)/tree.tsx
autonomous: false
requirements: [MUSEUM-02, MUSEUM-04, MUSEUM-05]

must_haves:
  truths:
    - "User peut ouvrir un écran Musée depuis l'actionBar de l'arbre"
    - "User voit les entrées musée groupées par semaine avec headers datés"
    - "User reconnaît les patterns Codex UI (FadeInDown, Spacing, modal pageSheet)"
    - "User voit un empty state soigné quand le musée est vide"
  artifacts:
    - path: "components/mascot/MuseumModal.tsx"
      provides: "Modal Musée avec SectionList groupée par semaine"
      min_lines: 100
    - path: "app/(tabs)/tree.tsx"
      provides: "Bouton Musée dans l'actionBar + state showMuseum + rendu MuseumModal"
      contains: "MuseumModal"
  key_links:
    - from: "app/(tabs)/tree.tsx"
      to: "components/mascot/MuseumModal.tsx"
      via: "showMuseum state + MuseumModal visible prop"
      pattern: "showMuseum"
    - from: "components/mascot/MuseumModal.tsx"
      to: "lib/museum/engine.ts"
      via: "parseMuseumEntries + groupEntriesByWeek pour générer les sections"
      pattern: "groupEntriesByWeek"
---

<objective>
Écran Musée minimal accessible depuis l'arbre : modal pageSheet avec SectionList groupée par semaine, réutilisant les patterns Codex UI de Phase 17.

Purpose: MUSEUM-02, MUSEUM-04, MUSEUM-05 — l'utilisateur peut ouvrir et consulter le musée chronologique de ses effets, groupés par semaine, avec un design cohérent avec le Codex.

Output: MuseumModal.tsx fonctionnel + bouton câblé dans tree.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/23-mus-e-des-effets-seed-002-lite/23-CONTEXT.md
@.planning/phases/23-mus-e-des-effets-seed-002-lite/23-RESEARCH.md
@.planning/phases/23-mus-e-des-effets-seed-002-lite/23-01-SUMMARY.md

@components/mascot/FarmCodexModal.tsx (pattern UI complet à réutiliser — MUSEUM-05)
@components/settings/SettingsCoupling.tsx (badge variant inline pattern)
@lib/museum/engine.ts (parseMuseumEntries, groupEntriesByWeek, formatRelativeTime, MuseumEntry — créé Plan 01)
@lib/semantic/effect-toasts.ts (CATEGORY_VARIANT, VARIANT_CONFIG)
@app/(tabs)/tree.tsx (actionBar, showCodex/showBadges pattern)

<interfaces>
<!-- Interfaces créées par Plan 01 -->

From lib/museum/engine.ts:
```typescript
export type MuseumEntry = { date: Date; categoryId: CategoryId; icon: string; label: string };
export function parseMuseumEntries(content: string): MuseumEntry[];
export function groupEntriesByWeek(entries: MuseumEntry[]): Array<{ weekKey: string; weekLabel: string; data: MuseumEntry[] }>;
export function formatRelativeTime(date: Date): string;
export function appendMuseumEntryToVault(vault: VaultManager, profileId: string, entry: MuseumEntry): Promise<void>;
```

From lib/semantic/effect-toasts.ts:
```typescript
export const CATEGORY_VARIANT: Record<CategoryId, HarvestBurstVariant>; // 'ambient' | 'rare' | 'golden'
```

From components/mascot/HarvestBurst.tsx:
```typescript
export const VARIANT_CONFIG: Record<HarvestBurstVariant, { particleColor: string; ... }>;
// ambient → '#34D399', rare → '#A78BFA', golden → '#FFD700'
```

From app/(tabs)/tree.tsx (pattern actionBar existant):
```typescript
const [showCodex, setShowCodex] = useState(false);
const [showBadges, setShowBadges] = useState(false);
// Pattern: <TouchableOpacity style={styles.actionItem} onPress={() => setShowXxx(true)}>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Créer MuseumModal.tsx + câbler dans tree.tsx</name>
  <files>components/mascot/MuseumModal.tsx, app/(tabs)/tree.tsx</files>
  <read_first>
    - components/mascot/FarmCodexModal.tsx (ENTIER — copier la structure modal pageSheet, SafeAreaView, header maison, animations FadeInDown)
    - components/settings/SettingsCoupling.tsx (badge variant inline avec variantColor + '33')
    - components/mascot/HarvestBurst.tsx (VARIANT_CONFIG export — couleurs particleColor)
    - app/(tabs)/tree.tsx (lignes 350-370 pour les states show*, lignes 1990-2045 pour l'actionBar, lignes 2220-2310 pour les modals rendus)
    - lib/museum/engine.ts (créé Plan 01 — imports nécessaires)
    - locales/fr/common.json (clés museum.* créées Plan 01)
  </read_first>
  <action>
    **1. Créer `components/mascot/MuseumModal.tsx`** (per D-02a à D-02g) :

    - Props : `{ visible: boolean; onClose: () => void; profileId: string | null; vault: VaultManager | null }`.
    - Pattern calqué fidèlement sur `FarmCodexModal.tsx` (MUSEUM-05) :
      - `Modal` avec `presentationStyle="pageSheet"` + `animationType="slide"` + `onRequestClose={onClose}`.
      - `SafeAreaView` englobant (même pattern que FarmCodexModal).
      - **Header maison** (View + Text + TouchableOpacity fermer) — PAS ModalHeader component (per anti-pattern RESEARCH.md). Titre : `t('museum.title', 'Musée des effets')`.
      - `SectionList` directement enfant de SafeAreaView (PAS dans un ScrollView — Pitfall 3 RESEARCH.md).
    - **Chargement des données** : `useEffect` quand `visible && profileId && vault` — lire `gami-{profileId}.md` via `vault.readFile()`, parser avec `parseMuseumEntries()`, grouper avec `groupEntriesByWeek()`. Stocker dans `useState<MuseumSection[]>([])`.
    - **SectionList** (per D-02c) :
      - `sections` : résultat de `groupEntriesByWeek()` mappé en `{ title: weekLabel, data: entries }`.
      - `renderSectionHeader` : Text avec `section.weekLabel`. Style : fond léger `colors.surface`, padding `Spacing.md`, `FontSize.caption`, `FontWeight.semibold`, `color: colors.textSub`.
      - `renderItem` : `Animated.View` avec `entering={FadeInDown.delay(index * 50)}` (per D-02g, Pitfall 6 acceptable — index section-local comme FarmCodexModal).
        - Layout row horizontal : icône (Text emoji `item.icon`, 24px) | colonne label+date (flex:1) | badge variant.
        - Label : `item.label` en `FontSize.body`, `color: colors.text`.
        - Date relative : `formatRelativeTime(item.date)` en `FontSize.caption`, `color: colors.textSub`.
        - Badge variant inline (per D-02e, pattern SettingsCoupling) : `CATEGORY_VARIANT[item.categoryId]` → `VARIANT_CONFIG[variant].particleColor` → `View` avec `backgroundColor: variantColor + '33'`, `borderRadius: Radius.sm`, `paddingHorizontal: Spacing.sm`. Text avec `color: variantColor`, `FontSize.caption`. Label : `t('museum.variant.ambient')` / `t('museum.variant.rare')` / `t('museum.variant.golden')`.
      - `keyExtractor` : `(item, index) => \`${item.date.getTime()}-${index}\``.
      - `ListEmptyComponent` : View centré avec Text 🏛️ (grande taille) + Text `t('museum.empty')` (per D-02f).
      - `stickySectionHeadersEnabled={true}` pour que les headers semaine restent visibles au scroll.
    - **React.memo** sur le composant row (per CLAUDE.md).
    - Styles via `StyleSheet.create({})` en bas de fichier pour les éléments statiques + inline pour les couleurs dynamiques `useThemeColors()`.
    - Tokens design : `Spacing`, `Radius`, `FontSize`, `FontWeight` — jamais de valeurs numériques hardcodées.

    **2. Modifier `app/(tabs)/tree.tsx`** (per D-02a) :

    - Ajouter `const [showMuseum, setShowMuseum] = useState(false);` avec les autres states show* (~ligne 362).
    - Ajouter le bouton dans l'actionBar (après le bouton Badges, ~ligne 2037) :
      ```
      <TouchableOpacity style={styles.actionItem} onPress={() => setShowMuseum(true)} activeOpacity={0.7}>
        <Text style={styles.actionItemIcon}>{'🏛️'}</Text>
        <Text style={[styles.actionItemLabel, { color: colors.textSub }]}>{t('museum.title', 'Musée')}</Text>
      </TouchableOpacity>
      ```
    - Ajouter le rendu du modal (après les autres modals, ~ligne 2300) :
      ```
      <MuseumModal
        visible={showMuseum}
        onClose={() => setShowMuseum(false)}
        profileId={activeProfile?.id ?? null}
        vault={vault}
      />
      ```
    - Importer `MuseumModal` depuis `'../../components/mascot/MuseumModal'`.
  </action>
  <verify>
    <automated>cd /Users/gabrielwaltio/Documents/family-vault && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "MuseumModal" components/mascot/MuseumModal.tsx
    - grep -q "SectionList" components/mascot/MuseumModal.tsx
    - grep -q "FadeInDown" components/mascot/MuseumModal.tsx
    - grep -q "groupEntriesByWeek" components/mascot/MuseumModal.tsx
    - grep -q "pageSheet" components/mascot/MuseumModal.tsx
    - grep -q "showMuseum" "app/(tabs)/tree.tsx"
    - grep -q "MuseumModal" "app/(tabs)/tree.tsx"
    - grep -q "🏛️" "app/(tabs)/tree.tsx"
  </acceptance_criteria>
  <done>
    - MuseumModal.tsx est un modal pageSheet avec SectionList groupée par semaine, FadeInDown, badges variant, empty state
    - tree.tsx a un bouton 🏛️ Musée dans l'actionBar et rend MuseumModal
    - Le design est cohérent avec FarmCodexModal (MUSEUM-05)
    - `npx tsc --noEmit` passe sans erreur nouvelle
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Vérification visuelle du Musée des effets</name>
  <files>components/mascot/MuseumModal.tsx, app/(tabs)/tree.tsx</files>
  <action>Vérification humaine du Musée des effets complet : persistance dans gami-{id}.md + écran modal accessible depuis l'arbre avec entrées groupées par semaine.</action>
  <how-to-verify>
    1. Ouvrir l'app sur device/simulator
    2. Compléter une tâche ménage (ou utiliser le bouton DEV Effets si disponible) — observer le toast feedback
    3. Ouvrir l'écran arbre → trouver le bouton 🏛️ "Musée" dans l'actionBar → tap
    4. Vérifier : le modal s'ouvre en pageSheet avec l'entrée de l'effet qu'on vient de déclencher
    5. Vérifier : l'entrée montre l'icône, le label FR, la date relative ("il y a X min"), et un badge variant coloré
    6. Vérifier : le header de section affiche "Cette semaine"
    7. Fermer le modal (drag-to-dismiss ou bouton fermer)
    8. Kill + restart l'app → rouvrir le Musée → vérifier que l'entrée persiste (MUSEUM-03)
    9. Compléter une autre tâche → rouvrir le Musée → vérifier que la nouvelle entrée apparaît en haut
    10. Si aucun effet déclenché : vérifier l'empty state "Aucun effet enregistré..."
  </how-to-verify>
  <verify>Vérification visuelle humaine</verify>
  <done>L'utilisateur confirme que le Musée fonctionne correctement</done>
  <resume-signal>Type "approved" ou décris les problèmes visuels/fonctionnels</resume-signal>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — zéro erreur nouvelle
2. Bouton 🏛️ visible dans l'actionBar de tree.tsx
3. Modal s'ouvre en pageSheet avec SectionList
4. Entrées groupées par semaine avec headers datés DD/MM/AAAA
5. Badges variant colorés (ambient vert, rare violet, golden doré)
6. Empty state affiché quand le musée est vide
7. Entrées persistent après restart (section ## Musée dans gami-{id}.md)
8. FadeInDown animé sur chaque row
</verification>

<success_criteria>
- L'utilisateur peut ouvrir le Musée depuis l'arbre et voir ses effets chronologiquement (MUSEUM-02)
- Les entrées sont groupées par semaine avec headers datés (MUSEUM-04)
- Le design est cohérent avec le Codex — modal pageSheet, FadeInDown, Spacing, badges variant (MUSEUM-05)
- L'empty state guide l'utilisateur quand le musée est vide
</success_criteria>

<output>
After completion, create `.planning/phases/23-mus-e-des-effets-seed-002-lite/23-02-SUMMARY.md`
</output>
