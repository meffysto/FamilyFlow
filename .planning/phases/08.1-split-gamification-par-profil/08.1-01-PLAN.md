---
phase: 08.1-split-gamification-par-profil
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/vault.ts
  - hooks/useFarm.ts
  - hooks/useGamification.ts
  - components/settings/SettingsGamification.tsx
autonomous: true
requirements:
  - ARCH-02

must_haves:
  truths:
    - "initVault crée des fichiers gami-{id}.md par profil au lieu d'un seul gamification.md"
    - "addChild crée un fichier gami-{id}.md pour le nouveau profil"
    - "useFarm.ts lit/écrit le fichier gami-{profileId}.md au lieu de gamification.md"
    - "useGamification.ts lit/écrit le fichier gami-{profileId}.md au lieu de gamification.md"
    - "Le reset gamification dans les settings écrit des fichiers per-profil"
  artifacts:
    - path: "lib/vault.ts"
      provides: "initVault et addChild écrivent gami-{id}.md"
      contains: "gami-"
    - path: "hooks/useFarm.ts"
      provides: "deductLeaves/addLeaves scoped par profil"
      contains: "gamiFile"
    - path: "hooks/useGamification.ts"
      provides: "completeTask/openLootBox scoped par profil"
      contains: "gamiFile"
    - path: "components/settings/SettingsGamification.tsx"
      provides: "Reset gamification per-profile"
      contains: "gamiFile"
  key_links:
    - from: "hooks/useFarm.ts"
      to: "vault.readFile/writeFile"
      via: "gamiFile(profileId) path helper"
      pattern: "gamiFile\\("
    - from: "hooks/useGamification.ts"
      to: "vault.readFile/writeFile"
      via: "gamiFile(profileId) path helper"
      pattern: "gamiFile\\("
    - from: "lib/vault.ts"
      to: "vault.writeFile"
      via: "gamiFile(id) in addChild and initVault"
      pattern: "gami-"
---

<objective>
Remplacer toutes les references a gamification.md dans lib/vault.ts, hooks/useFarm.ts, hooks/useGamification.ts et components/settings/SettingsGamification.tsx par des fichiers per-profil gami-{id}.md.

Purpose: Eliminer les conflits d'ecriture iCloud multi-device en scopant les lectures/ecritures gamification par profil.
Output: 4 fichiers modifies avec zero reference restante a GAMI_FILE ou 'gamification.md' en tant que cible d'ecriture.
</objective>

<execution_context>
@/Users/gabrielwaltio/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gabrielwaltio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08.1-split-gamification-par-profil/08.1-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrer lib/vault.ts — initVault et addChild ecrivent gami-{id}.md</name>
  <files>lib/vault.ts</files>
  <read_first>
    - lib/vault.ts (voir initVault lignes ~405-414, addChild lignes ~601-611, ensureVault lignes ~523-524)
    - .planning/phases/08.1-split-gamification-par-profil/08.1-RESEARCH.md (section Pitfall 2)
  </read_first>
  <action>
1. Ajouter un helper local en haut du fichier (apres les imports existants):
```typescript
function gamiFile(profileId: string): string {
  return `gami-${profileId}.md`;
}
```

2. Dans `initVault` (lignes ~405-414): remplacer l'ecriture unique a `gamification.md` par une boucle sur les profils. Le code actuel fait `await this.writeFile('gamification.md', serializeGamification(initialGami))`. Le remplacer par:
- Pour chaque profil dans `initialGami.profiles`, creer un `GamificationData` filtre contenant uniquement ce profil, son history, ses activeRewards, ses usedLoots (filtres par `profileId`)
- Ecrire `await this.writeFile(gamiFile(profil.id), serializeGamification(singleProfileData))` pour chaque profil
- NE PAS ecrire `gamification.md` du tout (les nouvelles installations n'en ont pas besoin)

3. Dans `addChild` (lignes ~601-611): le code actuel lit `gamification.md`, ajoute un profil, et reecrit tout. Le remplacer par:
- Creer un `GamificationData` contenant uniquement le nouveau profil (profiles: [newGamiProfile], history: [], activeRewards: [], usedLoots: [])
- Ecrire `await this.writeFile(gamiFile(newChildId), serializeGamification(singleChildData))`
- NE PAS toucher aux autres fichiers gami-*.md existants

4. Verifier qu'il n'y a plus aucune reference a la string 'gamification.md' dans lib/vault.ts (sauf eventuels commentaires). Chercher aussi les references indirectes via des constantes importees.
  </action>
  <verify>
    <automated>grep -n "gamification\.md" lib/vault.ts | grep -v "^.*//.*gamification" | grep -v "\.bak" ; test $? -eq 1 && echo "PASS: no gamification.md references" || echo "FAIL: still has references"</automated>
  </verify>
  <acceptance_criteria>
    - lib/vault.ts contient `function gamiFile(profileId: string): string`
    - lib/vault.ts contient `gamiFile(` dans initVault (au moins 1 occurrence dans les lignes de initVault)
    - lib/vault.ts contient `gamiFile(` dans addChild (au moins 1 occurrence dans les lignes de addChild)
    - `grep -c "gamification\.md" lib/vault.ts` retourne 0 (aucune reference restante a la string literal, hors commentaires)
    - `npx tsc --noEmit 2>&1 | grep -c "vault\.ts"` retourne 0 (pas de nouvelles erreurs TS)
  </acceptance_criteria>
  <done>initVault et addChild ecrivent des fichiers gami-{id}.md individuels, zero reference a gamification.md</done>
</task>

<task type="auto">
  <name>Task 2: Migrer useFarm.ts et useGamification.ts — lectures/ecritures per-profil</name>
  <files>hooks/useFarm.ts, hooks/useGamification.ts, components/settings/SettingsGamification.tsx</files>
  <read_first>
    - hooks/useFarm.ts (entier, 510 lignes — voir GAMI_FILE ligne 42, deductLeaves lignes ~80-96, addLeaves lignes ~102-118)
    - hooks/useGamification.ts (entier, 233 lignes — voir GAMI_FILE ligne 55, completeTask lignes ~71-85, openLootBox ligne ~178)
    - components/settings/SettingsGamification.tsx (chercher toute reference a gamification.md ou GAMI_FILE)
    - .planning/phases/08.1-split-gamification-par-profil/08.1-RESEARCH.md (sections Pitfall 1 et Pitfall 6)
  </read_first>
  <action>
**hooks/useFarm.ts (8 sites):**

1. Remplacer `const GAMI_FILE = 'gamification.md';` (ligne ~42) par un helper local:
```typescript
function gamiFile(profileId: string): string {
  return `gami-${profileId}.md`;
}
```

2. Dans `deductLeaves` (lignes ~80-96): la fonction doit accepter un parametre `profileId: string`. Actuellement elle lit `vault.readFile(GAMI_FILE)` et ecrit `vault.writeFile(GAMI_FILE, ...)`. Remplacer par:
- `const file = gamiFile(profileId);`
- `vault.readFile(file)` au lieu de `vault.readFile(GAMI_FILE)`
- `vault.writeFile(file, ...)` au lieu de `vault.writeFile(GAMI_FILE, ...)`
- Filtrer le contenu parse pour ne garder que le profil cible avant serialisation: `{ profiles: [targetProfile], history: gami.history.filter(e => e.profileId === profileId), activeRewards: (gami.activeRewards ?? []).filter(r => r.profileId === profileId), usedLoots: (gami.usedLoots ?? []).filter(u => u.profileId === profileId) }`
- Mettre a jour tous les call sites de `deductLeaves` dans useFarm.ts pour passer le `profileId` (generalement `activeProfile?.id` ou equivalent disponible dans le hook)

3. Dans `addLeaves` (lignes ~102-118): meme pattern que deductLeaves — ajouter `profileId: string`, lire/ecrire `gamiFile(profileId)`, filtrer par profil.

4. Verifier tous les appels a `deductLeaves` et `addLeaves` dans useFarm.ts passent le profileId.

**hooks/useGamification.ts (5 sites):**

1. Remplacer `const GAMI_FILE = 'gamification.md';` (ligne ~55) par le meme helper `gamiFile`.

2. Dans `completeTask` (lignes ~71-85): la fonction recoit deja un contexte avec `profileId` ou `activeProfile`. Remplacer:
- `vault.readFile(GAMI_FILE)` → `vault.readFile(gamiFile(profileId))`
- `vault.writeFile(GAMI_FILE, ...)` → `vault.writeFile(gamiFile(profileId), ...)`
- Filtrer le contenu serialise pour ne contenir que ce profil

3. Dans `openLootBox` (ligne ~178): meme pattern — scope read/write a `gamiFile(profileId)`.

**components/settings/SettingsGamification.tsx (1 site):**

1. Identifier la reference a `gamification.md` (directe ou via import de constante). Si le composant fait un `writeFile('gamification.md', ...)` pour le reset, le remplacer par une boucle sur tous les profils:
- Pour chaque profil, ecrire un fichier vide ou initial a `gamiFile(profile.id)`
- Ou si le reset passe par un callback du hook useVault, s'assurer que ce callback est deja mis a jour dans Plan 02

2. Si la reference dans SettingsGamification est indirecte (via useVault), documenter dans le code avec un commentaire `// Migration 08.1: le reset est handle par useVault.resetGamification()` et ne pas modifier ce fichier.
  </action>
  <verify>
    <automated>grep -rn "GAMI_FILE\|gamification\.md" hooks/useFarm.ts hooks/useGamification.ts components/settings/SettingsGamification.tsx | grep -v "//" ; test $? -eq 1 && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - hooks/useFarm.ts contient `function gamiFile(` (helper local)
    - hooks/useFarm.ts ne contient plus la string `GAMI_FILE` ni `'gamification.md'`
    - hooks/useFarm.ts `deductLeaves` et `addLeaves` acceptent un parametre `profileId`
    - hooks/useGamification.ts contient `function gamiFile(` (helper local)
    - hooks/useGamification.ts ne contient plus la string `GAMI_FILE` ni `'gamification.md'`
    - components/settings/SettingsGamification.tsx ne contient plus de reference directe a `gamification.md`
    - `npx tsc --noEmit 2>&1 | grep -c "useFarm\|useGamification\|SettingsGamification"` retourne 0
  </acceptance_criteria>
  <done>useFarm, useGamification et SettingsGamification lisent/ecrivent uniquement des fichiers gami-{id}.md per-profil</done>
</task>

</tasks>

<verification>
- `grep -rn "GAMI_FILE" lib/vault.ts hooks/useFarm.ts hooks/useGamification.ts` retourne zero resultats
- `grep -rn "'gamification.md'" lib/vault.ts hooks/useFarm.ts hooks/useGamification.ts` retourne zero resultats
- `npx tsc --noEmit` ne produit pas de nouvelles erreurs dans ces fichiers
</verification>

<success_criteria>
Les 4 fichiers (lib/vault.ts, hooks/useFarm.ts, hooks/useGamification.ts, components/settings/SettingsGamification.tsx) n'ont plus aucune reference active a gamification.md et lisent/ecrivent exclusivement des fichiers gami-{profileId}.md.
</success_criteria>

<output>
After completion, create `.planning/phases/08.1-split-gamification-par-profil/08.1-01-SUMMARY.md`
</output>
