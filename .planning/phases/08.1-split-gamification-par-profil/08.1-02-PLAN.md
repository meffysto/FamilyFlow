---
phase: 08.1-split-gamification-par-profil
plan: 02
type: execute
wave: 2
depends_on:
  - "08.1-01"
files_modified:
  - hooks/useVault.ts
autonomous: true
requirements:
  - ARCH-02

must_haves:
  truths:
    - "loadVaultData lit N fichiers gami-{id}.md et les fusionne en un seul gamiData"
    - "La migration split automatiquement gamification.md en fichiers per-profil au premier chargement"
    - "Chaque mutation (buyMascotItem, placeMascotItem, updateProfile, etc.) ecrit uniquement dans le fichier du profil cible"
    - "refreshGamification lit tous les fichiers per-profil et merge le resultat"
    - "Le fichier gamification.md legacy est conserve mais jamais ecrit"
    - "npx tsc --noEmit ne produit aucune nouvelle erreur"
  artifacts:
    - path: "hooks/useVault.ts"
      provides: "Migration gamification.md + merged read + scoped writes pour 27 sites"
      contains: "gamiFile"
  key_links:
    - from: "hooks/useVault.ts:loadVaultData"
      to: "vault.readFile"
      via: "Promise.allSettled sur gamiFile(p.id) pour chaque profil"
      pattern: "gamiFile\\("
    - from: "hooks/useVault.ts:migrateGamification"
      to: "vault.writeFile"
      via: "Ecriture per-profil filtree par profileId"
      pattern: "migrateGamification"
    - from: "hooks/useVault.ts:refreshGamification"
      to: "vault.readFile"
      via: "Lecture multi-fichier + merge"
      pattern: "gamiFile\\("
---

<objective>
Migrer les 27 sites de lecture/ecriture gamification dans hooks/useVault.ts vers des fichiers per-profil gami-{id}.md, incluant la logique de migration automatique depuis gamification.md et le merge en memoire.

Purpose: useVault.ts est le coeur de l'etat — c'est le fichier avec le plus de references a gamification.md (27 sites). Cette migration complete le split per-profil pour eliminer les conflits iCloud.
Output: hooks/useVault.ts modifie avec zero reference active a gamification.md, migration backward-compatible, et merged gamiData inchange pour les consumers.
</objective>

<execution_context>
@/Users/gabrielwaltio/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gabrielwaltio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08.1-split-gamification-par-profil/08.1-RESEARCH.md
@.planning/phases/08.1-split-gamification-par-profil/08.1-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration automatique + loadVaultData merged read</name>
  <files>hooks/useVault.ts</files>
  <read_first>
    - hooks/useVault.ts (lignes 1-300 pour les constantes et imports, lignes ~490-540 pour loadVaultData lecture gami, lignes ~2280-2300 pour refreshGamification)
    - .planning/phases/08.1-split-gamification-par-profil/08.1-RESEARCH.md (sections "Pattern: One-Time Migration on Load", "Pattern: Merged In-Memory View", "Pitfall 4", "Pitfall 5")
    - lib/parser.ts (chercher parseGamification et serializeGamification signatures)
  </read_first>
  <action>
1. Remplacer la constante `const GAMI_FILE = 'gamification.md';` (ligne ~272) par un helper local:
```typescript
function gamiFile(profileId: string): string {
  return `gami-${profileId}.md`;
}
```

2. Ajouter une fonction de migration AVANT loadVaultData:
```typescript
async function migrateGamification(vault: VaultManager, profiles: { id: string }[]): Promise<void> {
  const legacyExists = await vault.exists('gamification.md');
  if (!legacyExists) return;
  // Verifier si au moins un fichier per-profil manque
  const checks = await Promise.all(
    profiles.map(async p => ({ id: p.id, exists: await vault.exists(gamiFile(p.id)) }))
  );
  const needsMigration = checks.some(c => !c.exists);
  if (!needsMigration) return;
  const legacyContent = await vault.readFile('gamification.md');
  const legacyData = parseGamification(legacyContent);
  for (const check of checks) {
    if (check.exists) continue;
    const prof = legacyData.profiles.find(p => p.id === check.id);
    if (!prof) continue;
    const singleData = {
      profiles: [prof],
      history: legacyData.history.filter(e => e.profileId === check.id),
      activeRewards: (legacyData.activeRewards ?? []).filter(r => r.profileId === check.id),
      usedLoots: (legacyData.usedLoots ?? []).filter(u => u.profileId === check.id),
    };
    await vault.writeFile(gamiFile(check.id), serializeGamification(singleData));
  }
  // NE PAS supprimer gamification.md — le garder comme backup
}
```

3. Dans `loadVaultData`, APRES avoir parse les profils depuis famille.md (la variable `baseProfiles` ou equivalent contenant les profils avec leurs `id`):
- Appeler `await migrateGamification(vault, baseProfiles);`
- Remplacer la lecture unique `gamiContent = await vault.readFile(GAMI_FILE);` par une lecture multi-fichier:
```typescript
const gamiFiles = await Promise.allSettled(
  baseProfiles.map(p => vault.readFile(gamiFile(p.id)))
);
const mergedProfiles: any[] = [];
const mergedHistory: any[] = [];
const mergedActiveRewards: any[] = [];
const mergedUsedLoots: any[] = [];
for (let i = 0; i < baseProfiles.length; i++) {
  const result = gamiFiles[i];
  const content = result.status === 'fulfilled' ? result.value : '';
  if (!content) continue;
  const gami = parseGamification(content);
  mergedProfiles.push(...gami.profiles);
  mergedHistory.push(...gami.history);
  mergedActiveRewards.push(...(gami.activeRewards ?? []));
  mergedUsedLoots.push(...(gami.usedLoots ?? []));
}
const gamiData: GamificationData = {
  profiles: mergedProfiles,
  history: mergedHistory,
  activeRewards: mergedActiveRewards,
  usedLoots: mergedUsedLoots,
};
```
- Le reste du code qui utilise `gamiData` (mergeProfiles, setProfiles, etc.) reste INCHANGE — gamiData a la meme forme qu'avant.

4. Dans `refreshGamification` (ligne ~2287-2292): meme pattern que loadVaultData — lire tous les fichiers per-profil via `Promise.allSettled`, merger, puis mettre a jour l'etat. NE PAS lire un seul fichier.

5. IMPORTANT pour mergeProfiles: si `mergeProfiles(familleContent, gamiContent)` est appele dans loadVaultData, il faut soit:
   - Option A (preferee): Construire `gamiContent` en reserialisent le `gamiData` merge avec `serializeGamification(gamiData)` avant de le passer a `mergeProfiles`
   - Option B: Passer le `gamiData` directement et bypasser `mergeProfiles` dans le chemin de reload (si la logique le permet)
   Choisir l'option qui minimise les changements aux call sites existants.
  </action>
  <verify>
    <automated>grep -n "GAMI_FILE" hooks/useVault.ts ; test $? -eq 1 && echo "PASS: GAMI_FILE removed" || echo "FAIL: GAMI_FILE still present"</automated>
  </verify>
  <acceptance_criteria>
    - hooks/useVault.ts contient `function gamiFile(`
    - hooks/useVault.ts contient `async function migrateGamification(`
    - hooks/useVault.ts contient `Promise.allSettled` dans loadVaultData (lecture multi-fichier)
    - hooks/useVault.ts contient `Promise.allSettled` dans refreshGamification (lecture multi-fichier)
    - hooks/useVault.ts ne contient plus `const GAMI_FILE`
    - `grep -c "GAMI_FILE" hooks/useVault.ts` retourne 0
  </acceptance_criteria>
  <done>loadVaultData et refreshGamification lisent les fichiers per-profil et fusionnent les donnees. La migration automatique depuis gamification.md fonctionne.</done>
</task>

<task type="auto">
  <name>Task 2: Scoped writes — migrer les 20+ sites de mutation dans useVault.ts</name>
  <files>hooks/useVault.ts</files>
  <read_first>
    - hooks/useVault.ts (lire le fichier entier pour trouver tous les `vault.readFile(GAMI_FILE)` et `vault.writeFile(GAMI_FILE` restants — environ 20 sites identifies dans la research)
    - .planning/phases/08.1-split-gamification-par-profil/08.1-RESEARCH.md (section "Complete call-site audit" pour la liste exhaustive des 27 sites, section "Anti-Patterns to Avoid", section "Pitfall 3" pour updateProfile)
  </read_first>
  <action>
IMPORTANT: Ce task couvre TOUS les sites de mutation restants dans useVault.ts apres Task 1. Chaque site qui fait `vault.readFile(GAMI_FILE)` + mutation + `vault.writeFile(GAMI_FILE, ...)` doit etre converti au pattern per-profil.

**Pattern de conversion pour chaque site:**
```typescript
// AVANT:
const gamiContent = await vault.readFile(GAMI_FILE);
const gami = parseGamification(gamiContent);
// ... mutation sur gami ...
await vault.writeFile(GAMI_FILE, serializeGamification(gami));

// APRES:
const file = gamiFile(profileId);
const gamiContent = await vault.readFile(file).catch(() => '');
const gami = parseGamification(gamiContent);
// ... mutation sur le profil cible uniquement ...
const singleProfileData: GamificationData = {
  profiles: gami.profiles.filter(p => p.id === profileId),
  history: gami.history.filter(e => e.profileId === profileId),
  activeRewards: (gami.activeRewards ?? []).filter(r => r.profileId === profileId),
  usedLoots: (gami.usedLoots ?? []).filter(u => u.profileId === profileId),
};
await vault.writeFile(file, serializeGamification(singleProfileData));
```

**Sites a migrer (liste exhaustive de la research):**

1. **Ligne ~525**: ecriture active rewards — identifier le profileId du reward, ecrire dans `gamiFile(reward.profileId)`
2. **Ligne ~775**: defi reward write — le profileId est disponible dans le contexte de la function
3. **Lignes ~1222/1236**: `buyMascotItem` — utiliser le profileId passe en parametre ou depuis `activeProfile`
4. **Ligne ~1339**: `placeMascotItem` — lire/ecrire `gamiFile(profileId)`
5. **Ligne ~1393**: optimistic refresh apres placeMascotItem — lire le fichier per-profil
6. **Lignes ~1449/1465**: `updateProfile` rename — ATTENTION Pitfall 3: le fichier reste `gami-{profileId}.md` (l'ID ne change pas), seul le header `## Name` a l'interieur change. Lire `gamiFile(profileId)`, modifier le header, reecrire dans le MEME fichier.
7. **Ligne ~1474**: optimistic reload — lire le fichier per-profil
8. **Lignes ~2387/2388/2419/2433**: optimistic reloads divers — lire le fichier per-profil concerne
9. **Lignes ~2653/2666**: defi check-in reward — utiliser le profileId du defi
10. **Lignes ~2689/2716**: completeDefi reward — utiliser le profileId du defi
11. **Ligne ~3084**: `completeAdventure` — utilise `childProfileId` en parametre, remplacer la string hardcodee `'gamification.md'` par `gamiFile(childProfileId)`
12. **Lignes ~3149/3203**: `completeSagaChapter` — identifier le profileId, scoper lecture/ecriture
13. **Lignes ~3214-3241**: `markLootUsed` — identifier le profileId du loot, lire/ecrire son fichier

**Pour les optimistic reloads** (sites qui font juste readFile + parse + setState):
- Si le reload met a jour `gamiData` (l'etat global merge), il faut soit:
  - Relire TOUS les fichiers per-profil (comme refreshGamification) — couteux mais correct
  - Ou faire un merge partiel: lire le fichier modifie, remplacer la tranche de ce profil dans l'etat existant gamiData — plus efficace
- Preferer le merge partiel quand c'est simple: `setGamiData(prev => ({ ...prev, profiles: prev.profiles.map(p => p.id === profileId ? updatedProfile : p), ... }))`

**Pour updateProfile (Pitfall 3):**
- Le profileId est STABLE (derive du nom original a la creation)
- Quand l'utilisateur renomme son profil, le fichier reste `gami-{originalId}.md`
- Seul le contenu interne change (header `## NewName`)
- NE PAS renommer le fichier

**Verification finale:** Apres toutes les conversions, faire `grep -n "gamification\.md\|GAMI_FILE" hooks/useVault.ts` — le resultat doit etre vide (hors commentaires).
  </action>
  <verify>
    <automated>grep -n "'gamification\.md'" hooks/useVault.ts | grep -v "//" ; test $? -eq 1 && echo "PASS: no gamification.md string literals" || echo "FAIL"; npx tsc --noEmit 2>&1 | grep "useVault" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "'gamification.md'" hooks/useVault.ts` retourne 0 (zero string literal restante)
    - `grep -c "GAMI_FILE" hooks/useVault.ts` retourne 0 (la constante n'existe plus)
    - hooks/useVault.ts contient au moins 15 occurrences de `gamiFile(` (remplacant les 20+ anciens sites)
    - La ligne ~3084 (completeAdventure) contient `gamiFile(childProfileId)` au lieu de `'gamification.md'`
    - La section updateProfile contient `gamiFile(profileId)` et NE contient PAS de logique de renommage de fichier
    - `npx tsc --noEmit 2>&1 | grep -c "useVault\.ts"` retourne 0 (pas de nouvelles erreurs TS dans ce fichier)
  </acceptance_criteria>
  <done>Les 27 sites de lecture/ecriture dans useVault.ts sont tous migres vers gami-{profileId}.md. Zero reference restante a gamification.md ou GAMI_FILE. npx tsc --noEmit passe.</done>
</task>

</tasks>

<verification>
- `grep -rn "GAMI_FILE" hooks/useVault.ts hooks/useFarm.ts hooks/useGamification.ts lib/vault.ts` retourne zero resultats
- `grep -rn "'gamification.md'" hooks/useVault.ts hooks/useFarm.ts hooks/useGamification.ts lib/vault.ts` retourne zero resultats (hors commentaires)
- `npx tsc --noEmit` ne rapporte aucune nouvelle erreur dans ces fichiers
- hooks/useVault.ts contient `migrateGamification` (migration backward-compatible)
- hooks/useVault.ts loadVaultData et refreshGamification utilisent Promise.allSettled pour lire N fichiers
</verification>

<success_criteria>
hooks/useVault.ts n'a plus aucune reference active a gamification.md. Toutes les 27 lectures/ecritures sont scopees par profil via gamiFile(profileId). La migration automatique split l'ancien fichier unique au premier chargement. Le gamiData en memoire reste un merge de tous les profils — les consumers (loot.tsx, tree.tsx, index.tsx) ne changent pas.
</success_criteria>

<output>
After completion, create `.planning/phases/08.1-split-gamification-par-profil/08.1-02-SUMMARY.md`
</output>
