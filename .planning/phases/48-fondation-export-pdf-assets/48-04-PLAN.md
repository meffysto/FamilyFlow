---
phase: 48-fondation-export-pdf-assets
plan: 04
type: execute
wave: 4
depends_on: [48-03]
files_modified:
  - lib/vault-cache.ts
autonomous: false
requirements: [PDF-05, QA-01, QA-02]

must_haves:
  truths:
    - "CACHE_VERSION reste à 13 (inchangé) — le manifeste impressions n'est pas dans VaultCacheState"
    - "Un commentaire dans lib/vault-cache.ts documente explicitement la décision pour Phase 48"
    - "npx tsc --noEmit clean sur l'ensemble du projet (hors erreurs pré-existantes connues)"
    - "npx jest --no-coverage clean — aucune régression sur la suite complète"
    - "Aucun import de lib/pdf depuis vault-cache.ts (preuve que manifeste hors cache)"
  artifacts:
    - path: "lib/vault-cache.ts"
      provides: "Commentaire de décision Phase 48 + CACHE_VERSION inchangé"
      contains: "Phase 48"
  key_links:
    - from: "lib/vault-cache.ts"
      to: "lib/pdf"
      via: "AUCUN — preuve que manifeste hors VaultCacheState"
      pattern: "from.*lib/pdf"
---

<objective>
Décision finale `CACHE_VERSION` + sweep de non-régression :
1. Documenter dans `lib/vault-cache.ts` la décision **NE PAS bumper** `CACHE_VERSION` (reste à 13) avec justification (RESEARCH.md lignes 212-229)
2. Vérifier qu'aucun import `lib/pdf/*` n'a été ajouté à `vault-cache.ts` (preuve formelle que manifeste = nouveau domaine non caché)
3. Lancer la suite Jest complète + `tsc --noEmit` pour valider zéro régression sur les phases 1-47

Purpose : éviter Pitfall 3 RESEARCH.md ligne 361 (bumper "par sécurité" → invalide cache de tous les utilisateurs → layout shift 300-800ms sans bénéfice).

Output : 1 commentaire ajouté + suite verte + checkpoint final humain pour validation phase 48.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/48-fondation-export-pdf-assets/48-CONTEXT.md
@.planning/phases/48-fondation-export-pdf-assets/48-RESEARCH.md
@CLAUDE.md
@lib/vault-cache.ts
</context>

<interfaces>
État actuel `lib/vault-cache.ts` (lignes 54-68) :

```typescript
// v5 : Phase 40 — shape WagerModifier étendu...
// ...
// v13: Phase B Histoires — Profile.storyDefaults (préférences durables wizard)
const CACHE_VERSION = 13;
const CACHE_FILENAME = 'vault-cache-v13.json';
```

Règle de bump (lignes 12-22 + commentaires existants) : bumper UNIQUEMENT si shape de type CACHÉ change ou si on ajoute/retire un domaine de `VaultCacheState`. Le manifeste impressions :
- N'est pas dans `VaultCacheState`
- Volume attendu très faible (≤50 entrées sur durée de vie app)
- Lecture rare (uniquement lors d'export PDF) → pas de gain perf à cacher

→ **NE PAS bumper. Reste à 13.**
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1 : Documenter la décision "no bump" dans lib/vault-cache.ts</name>
  <files>lib/vault-cache.ts</files>
  <behavior>
    - Ajouter UNE ligne de commentaire dans le bloc historique versions (avant la ligne `const CACHE_VERSION = 13;`) qui explicite que Phase 48 a vérifié et décidé de ne pas bumper
    - NE PAS toucher la valeur `CACHE_VERSION = 13` ni `CACHE_FILENAME = 'vault-cache-v13.json'`
    - NE PAS importer quoi que ce soit depuis `lib/pdf` ici
  </behavior>
  <action>
  Ouvrir `lib/vault-cache.ts` ligne ~66 (juste avant `const CACHE_VERSION = 13;`) et ajouter le commentaire suivant à la suite du commentaire `// v13: Phase B Histoires — ...` :

  ```typescript
  // v13: Phase B Histoires — Profile.storyDefaults (préférences durables wizard)
  // Phase 48 (Export PDF Lulu) : aucun bump — manifeste impressions est un
  // nouveau domaine NON inclus dans VaultCacheState (lecture rare, volume
  // faible). Voir lib/pdf/manifest-parser.ts + .planning/phases/48-*/48-RESEARCH.md
  // section "CACHE_VERSION Decision".
  const CACHE_VERSION = 13;
  ```

  Aucune autre modification au fichier. Vérifier qu'AUCUN `import` ne pointe vers `'./pdf'` ou `'./pdf/types'` ou similar dans ce fichier.
  </action>
  <verify>
    <automated>grep -n "Phase 48" lib/vault-cache.ts && ! grep -E "from ['\"](\\./pdf|\\.\\./pdf)" lib/vault-cache.ts</automated>
  </verify>
  <done>
  - Commentaire "Phase 48" présent dans `lib/vault-cache.ts` autour de la ligne 67
  - `CACHE_VERSION` = 13 (inchangé)
  - `CACHE_FILENAME` = `'vault-cache-v13.json'` (inchangé)
  - Aucun import de `lib/pdf` dans ce fichier
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2 : Sweep non-régression — TS clean + suite Jest verte</name>
  <files>(aucun fichier modifié — vérification seulement)</files>
  <behavior>
    - `npx tsc --noEmit` ne génère AUCUNE nouvelle erreur (les erreurs pré-existantes dans MemoryEditor.tsx, cooklang.ts, useVault.ts sont acceptables — CLAUDE.md section Testing)
    - `npx jest --no-coverage` passe en intégralité, incluant le nouveau fichier `pdf-manifest-parser.test.ts`
  </behavior>
  <action>
  Lancer les deux commandes et capturer les résultats :

  ```bash
  # 1. Vérification TypeScript
  npx tsc --noEmit 2>&1 | tee /tmp/tsc-48.log

  # 2. Suite Jest complète
  npx jest --no-coverage 2>&1 | tee /tmp/jest-48.log
  ```

  **Critères d'acceptation :**

  - `tsc` : comparer la sortie aux erreurs connues. Si une nouvelle erreur apparaît dans `lib/pdf/*`, `app/_layout.tsx` (depuis 48-01), ou `lib/vault-cache.ts` → corriger. Erreurs dans `MemoryEditor.tsx`, `cooklang.ts`, `useVault.ts` → ignorer (pré-existantes).

  - `jest` : doit afficher `Tests: ... passed` avec `0 failed`. Le fichier `pdf-manifest-parser.test.ts` doit apparaître dans les fichiers exécutés (`PASS lib/__tests__/pdf-manifest-parser.test.ts`).

  Si une suite échoue (autre que les erreurs pré-existantes connues) :
  1. Identifier le fichier concerné
  2. Vérifier si la régression vient de Phase 48 (changement à `app/_layout.tsx`, ajout de `lib/pdf/`, modif `vault-cache.ts`) ou est antérieure
  3. Si liée à Phase 48 → corriger immédiatement
  4. Si antérieure et reproductible sur main → documenter dans le SUMMARY mais ne pas bloquer la phase

  ⚠ Cas particulier (RESEARCH.md ligne 367 — Pitfall 4) : si Metro ou Jest plante sur `text-encoding`, c'est lié à `react-native-qrcode-svg` (pas testé directement Phase 48) — ajouter mock Jest si nécessaire (pattern `lib/__tests__/__mocks__/`).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -cE "lib/pdf|vault-cache" || echo "0 nouvelles erreurs lib/pdf/vault-cache" ; npx jest --no-coverage --listFailing 2>&1 | tail -5</automated>
  </verify>
  <done>
  - `npx tsc --noEmit` : aucune nouvelle erreur introduite par Phase 48
  - `npx jest --no-coverage` : 0 failed, le test `pdf-manifest-parser.test.ts` apparaît avec PASS
  - Logs `/tmp/tsc-48.log` et `/tmp/jest-48.log` archivés (ou résumés dans SUMMARY)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3 : Validation finale phase 48 — boot device + suite verte</name>
  <what-built>
  Phase 48 complète :
  - Plan 48-01 : `expo-print` + `react-native-qrcode-svg` installés, Andika bundled, chargement boot OK
  - Plan 48-02 : Module `lib/pdf/` (3 fichiers : index, constants, types)
  - Plan 48-03 : `manifest-parser.ts` + tests Jest round-trip + barrel étendu
  - Plan 48-04 : `CACHE_VERSION` inchangé documenté + non-régression validée
  </what-built>
  <how-to-verify>
    1. **Vérification finale boot device** (si pas déjà fait à la fin de 48-01) :
       - `npx expo run:ios --device`
       - App boot normalement, pas de log d'erreur sur Andika ou expo-print
       - Naviguer dans l'app : dashboard, recettes, mascotte → aucune régression visible

    2. **Vérification critères ROADMAP** (lignes 427-432) :
       ```bash
       # Critère #1
       npm list expo-print react-native-qrcode-svg --depth=0
       # Critère #2
       ls assets/fonts/Andika/
       # Critère #3
       grep -E "TRIM_SIZE_CM|BLEED_CM|PAGE_COUNT|BookExportSpec|BookManifestEntry" lib/pdf/index.ts
       # Critère #4
       npx jest --no-coverage lib/__tests__/pdf-manifest-parser.test.ts
       # Critère #5
       npx tsc --noEmit ; npx jest --no-coverage
       ```
       Tous doivent retourner les résultats attendus.

    3. **Décision documentation cache** : ouvrir `lib/vault-cache.ts` autour de la ligne 67 — le commentaire "Phase 48" doit être présent et `CACHE_VERSION = 13` inchangé.

    4. **Sanity manifeste vault** (optionnel) : ouvrir Obsidian sur le vault, vérifier que rien n'a été créé dans `12 - Impressions/` automatiquement (création paresseuse Phase 49 — voir SUMMARY 48-03).
  </how-to-verify>
  <resume-signal>Tape "phase 48 OK" ou décris la régression rencontrée pour rollback ciblé</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cache schema | Fichier JSON local lu au boot — invalidation incorrecte = layout shift |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-09 | DoS | Bump CACHE_VERSION inutile | mitigate | Décision documentée explicitement (commentaire Task 1) — Pitfall 3 évité |
| T-48-10 | Repudiation | Décision non tracée | mitigate | Commentaire dans le code + SUMMARY 48-04 + section RESEARCH.md référencée |
</threat_model>

<verification>
- `grep "Phase 48" lib/vault-cache.ts` → ≥ 1 occurrence (PDF-05)
- `grep "CACHE_VERSION = 13" lib/vault-cache.ts` → 1 occurrence (inchangé)
- `grep "CACHE_FILENAME = 'vault-cache-v13.json'" lib/vault-cache.ts` → 1 occurrence (inchangé)
- `npx tsc --noEmit` → 0 nouvelle erreur (QA-01)
- `npx jest --no-coverage` → 0 failed (QA-02)
- Boot device OK (checkpoint Task 3)
</verification>

<success_criteria>
- [x] Critère succès #5 ROADMAP : `npx tsc --noEmit` clean (hors erreurs pré-existantes), `npx jest --no-coverage` clean
- [x] Décision `CACHE_VERSION` documentée dans le code (PDF-05)
- [x] Aucune régression sur les 47 phases précédentes
</success_criteria>

<output>
Créer `.planning/phases/48-fondation-export-pdf-assets/48-04-SUMMARY.md` avec :
- Décision finale : CACHE_VERSION reste à 13 (justification résumée)
- Résultats `tsc` et `jest` (compteurs : nb tests passés, nb fichiers tests, durée)
- Confirmation des 5 critères de succès ROADMAP (un check par critère)
- Note clôture phase 48 : prêt pour Phase 49 (génération PDF effective)
</output>
