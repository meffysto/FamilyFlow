---
phase: 48-fondation-export-pdf-assets
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - app/_layout.tsx
  - assets/fonts/Andika/Andika-Regular.ttf
  - assets/fonts/Andika/Andika-Bold.ttf
  - assets/fonts/Andika/OFL.txt
autonomous: false
requirements: [PDF-01, PDF-03, QA-01]
user_setup:
  - service: andika-font
    why: "Téléchargement zip officiel SIL — pas distribué via npm"
    dashboard_config:
      - task: "Télécharger Andika-7.000.zip depuis https://github.com/silnrsi/font-andika/releases/tag/v7.000"
        location: "GitHub release (10.13 MB)"
      - task: "Extraire et copier Andika-Regular.ttf, Andika-Bold.ttf et OFL.txt dans assets/fonts/Andika/"
        location: "assets/fonts/Andika/"

must_haves:
  truths:
    - "Les commandes `expo-print` et `react-native-qrcode-svg` apparaissent dans package.json"
    - "Les fichiers Andika-Regular.ttf et Andika-Bold.ttf sont présents dans assets/fonts/Andika/"
    - "Au boot de l'app, les polices Andika sont chargées sans erreur (testé sur device)"
    - "OFL.txt présent dans assets/fonts/Andika/ pour conformité license SIL"
  artifacts:
    - path: "package.json"
      provides: "expo-print + react-native-qrcode-svg dans dependencies"
      contains: "expo-print"
    - path: "assets/fonts/Andika/Andika-Regular.ttf"
      provides: "Police corps de texte livre"
    - path: "assets/fonts/Andika/Andika-Bold.ttf"
      provides: "Police gras livre"
    - path: "assets/fonts/Andika/OFL.txt"
      provides: "License SIL Open Font 1.1 (compliance)"
    - path: "app/_layout.tsx"
      provides: "Chargement Andika via useFonts() d'expo-font"
      contains: "Andika-Regular"
  key_links:
    - from: "app/_layout.tsx"
      to: "assets/fonts/Andika/*.ttf"
      via: "require() dans useFonts()"
      pattern: "require.*Andika.*ttf"
---

<objective>
Poser les fondations binaires pour la génération PDF du milestone v1.8 :
1. Installer `expo-print@~15.0.8` et `react-native-qrcode-svg@^6.3.21` (versions vérifiées RESEARCH.md lignes 28-32)
2. Bundler la police Andika 7.000 (Regular + Bold) dans `assets/fonts/Andika/`
3. Étendre le hook `useFonts` existant dans `app/_layout.tsx` pour charger Andika au boot

Purpose : sans deps + police, aucun rendu PDF Lulu n'est possible. Cette étape se fait en une seule passe d'install/build pour éviter deux cycles `npx expo run:ios`.

Output : binaires installés, polices bundled, app qui boot sans erreur de police.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/48-fondation-export-pdf-assets/48-CONTEXT.md
@.planning/phases/48-fondation-export-pdf-assets/48-RESEARCH.md
@CLAUDE.md
@app/_layout.tsx
</context>

<interfaces>
Pattern existant `useFonts` (app/_layout.tsx:32-34, 170-176) — extraire et étendre :

```typescript
import { useFonts as useDMSerif, DMSerifDisplay_400Regular } from '@expo-google-fonts/dm-serif-display';
import { Caveat_400Regular, Caveat_600SemiBold } from '@expo-google-fonts/caveat';
import { PatrickHand_400Regular } from '@expo-google-fonts/patrick-hand';

const [fontsLoaded, fontsError] = useDMSerif({
  DMSerifDisplay_400Regular,
  Caveat_400Regular,
  Caveat_600SemiBold,
  PatrickHand_400Regular,
});
const fontsReady = fontsLoaded || !!fontsError;
```

Andika est local (pas Google Fonts) → utiliser `useFonts` direct depuis `expo-font`, en parallèle.
</interfaces>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1 : Téléchargement manuel police Andika (asset bundling)</name>
  <what-built>Aucun code — préparation des fichiers binaires nécessaires à Task 2.</what-built>
  <how-to-verify>
    1. Visiter https://github.com/silnrsi/font-andika/releases/tag/v7.000
    2. Télécharger `Andika-7.000.zip` (10.13 MB)
    3. Extraire le zip
    4. Créer le dossier `assets/fonts/Andika/` à la racine du projet
    5. Copier dans ce dossier UNIQUEMENT :
       - `Andika-Regular.ttf` (depuis `fonts/ttf/`)
       - `Andika-Bold.ttf` (depuis `fonts/ttf/`)
       - `OFL.txt` (license SIL — racine du zip)
    6. NE PAS copier les variantes Italic/BoldItalic ni les WOFF
    7. Vérifier : `ls assets/fonts/Andika/` doit afficher exactement 3 fichiers
  </how-to-verify>
  <resume-signal>Tape "fichiers en place" ou décris le souci rencontré (zip introuvable, structure différente, etc.)</resume-signal>
</task>

<task type="auto">
  <name>Task 2 : Installer expo-print + react-native-qrcode-svg via expo install</name>
  <files>package.json, package-lock.json</files>
  <action>
  Exécuter `npx expo install expo-print react-native-qrcode-svg` (commande recommandée par RESEARCH.md lignes 49-54 pour résolution auto SDK 54 via bundledNativeModules.json).

  Versions attendues après install (vérifier package.json) :
  - `expo-print` : `~15.0.8` (pinned SDK 54)
  - `react-native-qrcode-svg` : `^6.3.21` (latest stable, peer react-native-svg ≥14 déjà satisfait par `^15.12.1`)

  Si la commande propose une version différente, accepter (la résolution Expo est canonique). Documenter la version finale dans le SUMMARY.

  Ne pas committer les fichiers Andika dans cette tâche — ils ont été placés en Task 1, le commit groupé arrive en Task 4.

  IMPORTANT : ne pas lancer de build prebuild/run:ios ici — le build sera fait par le développeur après les 4 plans (consigne `npm install / pod install` non requise pour Task 2 isolée, mais nécessaire en Task 5).
  </action>
  <verify>
    <automated>npm list expo-print react-native-qrcode-svg --depth=0 2>&1 | grep -E "expo-print|qrcode-svg"</automated>
  </verify>
  <done>
  - `package.json` contient `"expo-print"` et `"react-native-qrcode-svg"` dans `dependencies`
  - `package-lock.json` mis à jour avec les nouvelles entrées
  - `npx tsc --noEmit` ne génère pas de nouvelle erreur (aucun import des nouvelles libs encore)
  </done>
</task>

<task type="auto">
  <name>Task 3 : Étendre app/_layout.tsx pour charger Andika au boot</name>
  <files>app/_layout.tsx</files>
  <action>
  Étendre le mécanisme `useFonts` existant (app/_layout.tsx:32-34, 170-176) pour charger Andika en parallèle des Google Fonts. Suivre l'Option A recommandée dans RESEARCH.md lignes 99-114.

  1. Ajouter cet import après ligne 34 (à côté des autres imports `@expo-google-fonts/*`) :
     ```typescript
     import { useFonts as useExpoFonts } from 'expo-font';
     ```

  2. Dans `RootLayout()` (ligne 164+), ajouter le hook Andika juste APRÈS le `useDMSerif` existant (après ligne 175) :
     ```typescript
     // Charge la police Andika bundled (corps de texte des livres PDF Lulu).
     // Andika n'est pas sur Google Fonts → utilisation d'expo-font direct avec
     // les fichiers TTF locaux. Si la charge échoue, on continue (fallback
     // système) — ne jamais bloquer l'app (pattern cohérent avec useDMSerif).
     const [andikaLoaded, andikaError] = useExpoFonts({
       'Andika-Regular': require('../assets/fonts/Andika/Andika-Regular.ttf'),
       'Andika-Bold': require('../assets/fonts/Andika/Andika-Bold.ttf'),
     });
     ```

  3. Modifier la ligne `fontsReady` (actuellement ligne 176) pour combiner les deux hooks :
     ```typescript
     const fontsReady =
       (fontsLoaded || !!fontsError) &&
       (andikaLoaded || !!andikaError);
     ```

  4. Ne PAS modifier d'autre logique du fichier (Sentry, AuthProvider, providers, deep links, etc.).

  5. Commentaires : français (convention CLAUDE.md). Pas de hardcoded colors touchés.

  ⚠ Pitfall RESEARCH.md ligne 116 : les clés `'Andika-Regular'` / `'Andika-Bold'` deviennent l'alias canonique utilisé en CSS Phase 49 — ne PAS les renommer. Cohérent avec `FONT_SLOTS.body` qui sera défini dans 48-02.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "_layout.tsx|expo-font" || echo "tsc clean sur _layout.tsx"</automated>
  </verify>
  <done>
  - `app/_layout.tsx` importe `useFonts as useExpoFonts` depuis `expo-font`
  - Le hook `useExpoFonts({ 'Andika-Regular': ..., 'Andika-Bold': ... })` est présent dans `RootLayout()`
  - `fontsReady` combine les deux hooks via `&&`
  - `npx tsc --noEmit` clean (hors erreurs pré-existantes connues)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4 : Vérification boot device — police Andika chargée sans erreur</name>
  <what-built>
  - `expo-print` et `react-native-qrcode-svg` installés (Task 2)
  - `assets/fonts/Andika/{Regular,Bold}.ttf` + `OFL.txt` (Task 1)
  - `app/_layout.tsx` charge Andika via `useFonts` (Task 3)
  </what-built>
  <how-to-verify>
    1. Lancer `npx expo prebuild --clean` puis `cd ios && pod install && cd ..` (nécessaire pour module natif `expo-print`)
    2. Lancer `npx expo run:ios --device`
    3. Pendant le boot, surveiller les logs Metro / device :
       - PAS d'erreur `Unable to resolve module 'expo-print'`
       - PAS d'erreur `Unable to resolve module 'react-native-qrcode-svg'`
       - PAS d'erreur `Failed to load font Andika-Regular` ou similar
       - PAS d'erreur `Unable to resolve 'text-encoding'` (Pitfall 4 RESEARCH.md ligne 367)
    4. App démarre sur l'écran d'accueil habituel sans flash blanc anormalement long
    5. Si erreur Pitfall 4 (text-encoding) : voir RESEARCH.md ligne 370 pour mitigation
  </how-to-verify>
  <resume-signal>Tape "boot OK" ou colle l'erreur rencontrée pour diagnostic</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Asset bundling | Fichiers TTF tiers embarqués dans le binaire — vecteur d'inclusion |
| npm registry | Packages tiers (expo-print, qrcode-svg) intégrés au runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-01 | Tampering | Andika TTF files | accept | Source officielle SIL via release GitHub signée — checksum non vérifié manuellement (acceptable pour police OFL publique, pas de PII) |
| T-48-02 | Spoofing | npm packages | mitigate | `npx expo install` utilise `bundledNativeModules.json` officiel Expo SDK 54 (pinning vérifié RESEARCH.md ligne 30) ; `package-lock.json` fige les versions |
| T-48-03 | Information disclosure | OFL.txt absent | mitigate | License SIL Open Font 1.1 livrée avec les fichiers (compliance) — voir Task 1 |
</threat_model>

<verification>
- `npm list expo-print react-native-qrcode-svg` → versions installées (PDF-03)
- `ls assets/fonts/Andika/` → 3 fichiers (Regular.ttf, Bold.ttf, OFL.txt) (PDF-01)
- `grep -n "Andika" app/_layout.tsx` → au moins 2 occurrences (require + alias)
- Boot device sans erreur fontsError pour Andika (PDF-01, manuel checkpoint Task 4)
- `npx tsc --noEmit` clean (QA-01)
</verification>

<success_criteria>
- [x] Critère succès #1 ROADMAP : `npm list expo-print react-native-qrcode-svg` retourne versions installées
- [x] Critère succès #2 ROADMAP : `assets/fonts/Andika/` contient Regular + Bold + OFL, chargés via expo-font sans erreur boot device
- [x] `npx tsc --noEmit` clean
- [x] App boot sur device sans nouvelle erreur runtime
</success_criteria>

<output>
Créer `.planning/phases/48-fondation-export-pdf-assets/48-01-SUMMARY.md` avec :
- Versions exactes installées (expo-print, react-native-qrcode-svg)
- Tailles fichiers Andika après extraction (assumption A1 RESEARCH.md ligne 526)
- Notes éventuelles sur Pitfall 4 (text-encoding) si rencontré
- Confirmation boot device OK
</output>
