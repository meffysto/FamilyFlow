---
phase: 48-fondation-export-pdf-assets
plan: 03
type: execute
wave: 3
depends_on: [48-02]
files_modified:
  - lib/pdf/manifest-parser.ts
  - lib/pdf/index.ts
  - lib/__tests__/pdf-manifest-parser.test.ts
autonomous: true
requirements: [PDF-04, QA-02]

must_haves:
  truths:
    - "parseManifeste('') retourne []"
    - "serializeManifeste([]) produit un fichier markdown valide avec frontmatter version: 1 + header + table vide"
    - "Round-trip strict : parseManifeste(serializeManifeste(SAMPLE_THREE)) === SAMPLE_THREE (toEqual)"
    - "Lignes malformées (id vide, header, separator) ignorées silencieusement"
    - "MANIFESTE_FILE est exporté avec valeur '12 - Impressions/manifeste.md'"
    - "Le barrel lib/pdf/index.ts re-export MANIFESTE_FILE, parseManifeste, serializeManifeste"
  artifacts:
    - path: "lib/pdf/manifest-parser.ts"
      provides: "parseManifeste, serializeManifeste, MANIFESTE_FILE constante"
      min_lines: 50
      contains: "parseManifeste"
    - path: "lib/__tests__/pdf-manifest-parser.test.ts"
      provides: "Suite Jest round-trip + edge cases"
      min_lines: 60
    - path: "lib/pdf/index.ts"
      provides: "Barrel étendu avec exports manifest-parser"
      contains: "manifest-parser"
  key_links:
    - from: "lib/pdf/manifest-parser.ts"
      to: "lib/pdf/types.ts"
      via: "import type { BookManifestEntry }"
      pattern: "import type.*BookManifestEntry.*from './types'"
    - from: "lib/__tests__/pdf-manifest-parser.test.ts"
      to: "lib/pdf"
      via: "import { parseManifeste, serializeManifeste, type BookManifestEntry } from '../pdf'"
      pattern: "from '../pdf'"
---

<objective>
Créer le parser bidirectionnel du manifeste impressions :
1. `lib/pdf/manifest-parser.ts` avec `parseManifeste`, `serializeManifeste`, `MANIFESTE_FILE`
2. `lib/__tests__/pdf-manifest-parser.test.ts` couvrant round-trip 3 entrées + edge cases
3. Étendre le barrel `lib/pdf/index.ts` pour exposer les 3 nouveaux exports

Pattern : hybride `parseRDV` (frontmatter gray-matter) + `parseAnniversaires` (table markdown) — RESEARCH.md lignes 122-159.

Format manifeste retenu (RESEARCH.md ligne 132-148) :
```markdown
---
tags:
  - impressions
  - manifeste
version: 1
---

# Manifeste impressions

Registre des PDFs exportés pour impression Lulu Direct.

| ID histoire | Hash | Date | Format | Chemin |
|-------------|------|------|--------|--------|
| story_foret | a3f7... | 2026-05-04 | Lulu 21×21 | 12 - Impressions/PDFs/foret.pdf |
```

Output : parser pur testable, suite Jest verte, barrel complet.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/48-fondation-export-pdf-assets/48-CONTEXT.md
@.planning/phases/48-fondation-export-pdf-assets/48-RESEARCH.md
@CLAUDE.md
@lib/pdf/types.ts
@lib/parser.ts
</context>

<interfaces>
Type cible (déjà créé en 48-02) :

```typescript
// lib/pdf/types.ts
export interface BookManifestEntry {
  id: string;
  hash: string;
  date: string;
  format: string;
  chemin: string;
}
```

Pattern parseAnniversaries (lib/parser.ts:2210-2254) à imiter pour le scan ligne par ligne :
```typescript
for (const line of lines) {
  if (!line.startsWith('|')) continue;
  const cells = line.split('|').slice(1, -1).map(c => c.trim());
  if (cells.length < N) continue;
  if (cells[0] === 'HeaderName' || cells[0].startsWith('---')) continue;
  // ... extraction
}
```

`gray-matter` est déjà installé (`^4.0.3` package.json:49) — RESEARCH.md ligne 39.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 : Écrire les tests Jest avant l'implémentation (TDD)</name>
  <files>lib/__tests__/pdf-manifest-parser.test.ts</files>
  <behavior>
    - Test 1 : `parseManifeste('')` → `[]`
    - Test 2 : `parseManifeste(frontmatter sans table)` → `[]`
    - Test 3 : `parseManifeste(serializeManifeste(SAMPLE_THREE))` → `SAMPLE_THREE` (strict toEqual) — couvre PDF-04 (critère succès #4 ROADMAP)
    - Test 4 : `parseManifeste(table avec ligne id vide + ligne valide)` → 1 entrée (la ligne valide)
    - Test 5 : `serializeManifeste([])` produit frontmatter + header + table headers (sans data row)
    - Test 6 : Le serializer n'oublie pas le `version: 1` dans le frontmatter
  </behavior>
  <action>
  Créer `lib/__tests__/pdf-manifest-parser.test.ts` selon le template RESEARCH.md lignes 287-332, en respectant la convention 100% des tests projet sont dans `lib/__tests__/` (RESEARCH.md ligne 284) :

  ```typescript
  // lib/__tests__/pdf-manifest-parser.test.ts
  // Tests parser bidirectionnel du manifeste impressions Lulu Direct.

  import {
    parseManifeste,
    serializeManifeste,
    MANIFESTE_FILE,
    type BookManifestEntry,
  } from '../pdf';

  const SAMPLE_THREE: BookManifestEntry[] = [
    {
      id: 'story_foret_2026',
      hash: 'a3f7c4d2e1b8f9a0c3d5e7b1f4a6c8d2e9b3f5a7c1d4e6b8f2a9c0d3e5b7f1a4',
      date: '2026-05-04',
      format: 'Lulu 21×21',
      chemin: '12 - Impressions/PDFs/foret-2026.pdf',
    },
    {
      id: 'story_pirates_2026',
      hash: '8e2b1f4a6c8d2e9b3f5a7c1d4e6b8f2a9c0d3e5b7f1a4a3f7c4d2e1b8f9a0c3d',
      date: '2026-05-10',
      format: 'Lulu 21×21',
      chemin: '12 - Impressions/PDFs/pirates-2026.pdf',
    },
    {
      id: 'story_dragons_2026',
      hash: 'cd91e8f2a4b6c0d3e5b7f1a4a3f7c4d2e1b8f9a0c3d5e7b1f4a6c8d2e9b3f5a7',
      date: '2026-05-12',
      format: 'Lulu 21×21',
      chemin: '12 - Impressions/PDFs/dragons-2026.pdf',
    },
  ];

  describe('MANIFESTE_FILE', () => {
    it('pointe vers 12 - Impressions/manifeste.md', () => {
      expect(MANIFESTE_FILE).toBe('12 - Impressions/manifeste.md');
    });
  });

  describe('parseManifeste', () => {
    it('contenu vide → []', () => {
      expect(parseManifeste('')).toEqual([]);
      expect(parseManifeste('   \n\n  ')).toEqual([]);
    });

    it('frontmatter sans table → []', () => {
      const content = `---\nversion: 1\n---\n\n# Manifeste\n\nPas encore d'export.\n`;
      expect(parseManifeste(content)).toEqual([]);
    });

    it('parse 3 entrées depuis la table sérialisée', () => {
      const content = serializeManifeste(SAMPLE_THREE);
      const parsed = parseManifeste(content);
      expect(parsed).toHaveLength(3);
      expect(parsed[0].id).toBe('story_foret_2026');
      expect(parsed[2].chemin).toBe('12 - Impressions/PDFs/dragons-2026.pdf');
    });

    it('ignore silencieusement les lignes id vide ou header/separator', () => {
      const malformed = [
        '---',
        'version: 1',
        '---',
        '',
        '| ID histoire | Hash | Date | Format | Chemin |',
        '|-------------|------|------|--------|--------|',
        '|  |  |  |  |  |',
        '| story_ok | h1 | 2026-05-04 | Lulu 21×21 | a.pdf |',
      ].join('\n');
      const parsed = parseManifeste(malformed);
      expect(parsed).toHaveLength(1);
      expect(parsed[0].id).toBe('story_ok');
    });
  });

  describe('serializeManifeste', () => {
    it('liste vide → frontmatter + header + table headers (pas de data)', () => {
      const out = serializeManifeste([]);
      expect(out).toContain('version: 1');
      expect(out).toContain('# Manifeste impressions');
      expect(out).toContain('| ID histoire | Hash | Date | Format | Chemin |');
      // Pas de ligne data : la dernière | du fichier doit être celle du séparateur
      const dataRows = out.split('\n').filter((l) => l.startsWith('|') && !l.includes('---') && !l.includes('ID histoire'));
      expect(dataRows).toHaveLength(0);
    });

    it('inclut le frontmatter version: 1 (migrations futures)', () => {
      const out = serializeManifeste(SAMPLE_THREE);
      expect(out.startsWith('---\n')).toBe(true);
      expect(out).toMatch(/version:\s*1/);
    });
  });

  describe('round-trip parse → serialize → parse (PDF-04)', () => {
    it('Test critique : 3 entrées identiques après round-trip (strict equality)', () => {
      const original: BookManifestEntry[] = SAMPLE_THREE;
      const serialized = serializeManifeste(original);
      const reparsed = parseManifeste(serialized);
      expect(reparsed).toEqual(original);
    });

    it('round-trip 1 seule entrée', () => {
      const single: BookManifestEntry[] = [SAMPLE_THREE[0]];
      expect(parseManifeste(serializeManifeste(single))).toEqual(single);
    });

    it('round-trip liste vide', () => {
      expect(parseManifeste(serializeManifeste([]))).toEqual([]);
    });
  });
  ```

  ⚠ À ce stade `parseManifeste` / `serializeManifeste` / `MANIFESTE_FILE` n'existent pas encore → le test DOIT échouer en compilation/runtime. C'est attendu (RED). Task 2 fait passer au GREEN.

  Vérification RED :
  ```bash
  npx jest --no-coverage lib/__tests__/pdf-manifest-parser.test.ts 2>&1 | tail -20
  # Doit afficher des erreurs d'import ou de TS (parseManifeste introuvable)
  ```
  </action>
  <verify>
    <automated>npx jest --no-coverage lib/__tests__/pdf-manifest-parser.test.ts 2>&1 | grep -E "FAIL|Cannot find|TypeError" | head -3</automated>
  </verify>
  <done>
  - Fichier de test existe avec 3 `describe` (MANIFESTE_FILE, parseManifeste, serializeManifeste, round-trip = 4)
  - Test critique PDF-04 (round-trip strict equality 3 entrées) présent
  - Tests échouent (RED) car parser pas encore implémenté
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 : Implémenter parseManifeste / serializeManifeste / MANIFESTE_FILE</name>
  <files>lib/pdf/manifest-parser.ts</files>
  <behavior>
    - `MANIFESTE_FILE` constante : `'12 - Impressions/manifeste.md'`
    - `parseManifeste(content: string): BookManifestEntry[]` — pure, ne throw jamais
    - `serializeManifeste(entries: BookManifestEntry[]): string` — pure, deterministe
    - Frontmatter géré via `gray-matter` (cohérent avec parseRDV)
    - Lignes de table extraites avec le pattern `parseAnniversaries` (lib/parser.ts:2220-2229)
  </behavior>
  <action>
  Créer `lib/pdf/manifest-parser.ts` selon le template RESEARCH.md lignes 162-210, en aplomb avec parseAnniversaries (lib/parser.ts:2210) :

  ```typescript
  // lib/pdf/manifest-parser.ts
  // Parser bidirectionnel du manifeste impressions PDF Lulu.
  //
  // Format : un seul fichier `12 - Impressions/manifeste.md` avec :
  // - frontmatter gray-matter (tags + version pour migrations futures)
  // - table markdown 5 colonnes (ID histoire | Hash | Date | Format | Chemin)
  //
  // Pattern hybride parseRDV (frontmatter) + parseAnniversaries (table).
  //
  // Contrainte : aucune valeur ne peut contenir `|` (Pitfall 5 RESEARCH.md).
  // Le hash SHA-256 est hex, les chemins vault n'ont pas de pipe → satisfait.

  import matter from 'gray-matter';
  import type { BookManifestEntry } from './types';

  /** Chemin relatif vault vers le fichier manifeste. */
  export const MANIFESTE_FILE = '12 - Impressions/manifeste.md';

  /**
   * Parse le contenu markdown du manifeste vers une liste d'entrées.
   * Lignes malformées (id vide, header, separator) sont ignorées silencieusement.
   * Ne throw jamais — retourne `[]` sur contenu vide ou invalide.
   */
  export function parseManifeste(content: string): BookManifestEntry[] {
    if (!content || !content.trim()) return [];

    let body: string;
    try {
      ({ content: body } = matter(content));
    } catch {
      // Frontmatter invalide → traiter le contenu brut
      body = content;
    }

    const lines = body.split('\n');
    const items: BookManifestEntry[] = [];

    for (const line of lines) {
      if (!line.startsWith('|')) continue;
      const cells = line.split('|').slice(1, -1).map((c) => c.trim());
      if (cells.length < 5) continue;
      // Skip header et separator
      if (cells[0] === 'ID histoire' || cells[0].startsWith('---')) continue;

      const [id, hash, date, format, chemin] = cells;
      if (!id || !hash) continue;

      items.push({ id, hash, date, format, chemin });
    }

    return items;
  }

  /**
   * Sérialise une liste d'entrées vers le format markdown manifeste.
   * Inclut frontmatter (version: 1 pour migrations futures) + header + table.
   * Round-trip prouvable avec parseManifeste (cf. tests).
   */
  export function serializeManifeste(entries: BookManifestEntry[]): string {
    const parts: string[] = [
      '---',
      'tags:',
      '  - impressions',
      '  - manifeste',
      'version: 1',
      '---',
      '',
      '# Manifeste impressions',
      '',
      'Registre des PDFs exportés pour impression Lulu Direct.',
      '',
      '| ID histoire | Hash | Date | Format | Chemin |',
      '|-------------|------|------|--------|--------|',
    ];

    for (const e of entries) {
      parts.push(`| ${e.id} | ${e.hash} | ${e.date} | ${e.format} | ${e.chemin} |`);
    }

    parts.push('');
    return parts.join('\n');
  }
  ```

  Vérification GREEN :
  ```bash
  npx jest --no-coverage lib/__tests__/pdf-manifest-parser.test.ts
  # Tous les tests passent
  ```
  </action>
  <verify>
    <automated>npx jest --no-coverage lib/__tests__/pdf-manifest-parser.test.ts 2>&1 | tail -10</automated>
  </verify>
  <done>
  - `lib/pdf/manifest-parser.ts` créé avec 3 exports (`MANIFESTE_FILE`, `parseManifeste`, `serializeManifeste`)
  - Tous les tests Jest du fichier `pdf-manifest-parser.test.ts` passent (GREEN)
  - Test critique PDF-04 (round-trip 3 entrées strict equality) ✅
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3 : Étendre lib/pdf/index.ts pour exposer le parser via barrel</name>
  <files>lib/pdf/index.ts</files>
  <behavior>
    - Ajouter le re-export des 3 symboles `manifest-parser`
    - Conserver les exports existants (constants + types)
    - Retirer le commentaire "manifest-parser sera ajouté par 48-03"
  </behavior>
  <action>
  Étendre `lib/pdf/index.ts` (créé en 48-02) pour ajouter le re-export du parser. Résultat final attendu (RESEARCH.md lignes 252-275) :

  ```typescript
  // lib/pdf/ — Barrel export pour le domaine export PDF (Lulu Direct).
  // Pattern cohérent avec lib/gamification/index.ts.

  export {
    TRIM_SIZE_CM,
    BLEED_CM,
    PAGE_COUNT,
    LULU_FORMAT_LABEL,
    BOOK_PALETTE,
    FONT_SLOTS,
  } from './constants';

  export type {
    BookExportSpec,
    BookManifestEntry,
    BookPalette,
    FontSlot,
  } from './types';

  export {
    MANIFESTE_FILE,
    parseManifeste,
    serializeManifeste,
  } from './manifest-parser';
  ```

  Vérifier que l'import depuis `'../pdf'` dans le test fonctionne après cette modification.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep "lib/pdf" | head -5 || echo "tsc OK barrel complet"</automated>
  </verify>
  <done>
  - `lib/pdf/index.ts` exporte 6 const + 4 types + 3 du parser (MANIFESTE_FILE, parseManifeste, serializeManifeste)
  - `npx jest --no-coverage lib/__tests__/pdf-manifest-parser.test.ts` passe (les tests utilisent l'import barrel `from '../pdf'`)
  - `npx tsc --noEmit` clean
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vault file → parser | Contenu markdown user-controllable lu depuis iCloud |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-06 | Tampering | parseManifeste input | mitigate | Parser ne throw jamais, ignore lignes malformées (id/hash vide), gray-matter wrappé en try/catch |
| T-48-07 | DoS | parseManifeste très long fichier | accept | Volume attendu très faible (≤50 entrées sur durée de vie app — RESEARCH.md ligne 224), pas de risque pratique |
| T-48-08 | Tampering | Pipe `\|` dans valeurs | mitigate | Hash hex et chemins vault sans pipe par construction (Pitfall 5) — documenté dans le code |
</threat_model>

<verification>
- `ls lib/pdf/` → 4 fichiers (`index.ts`, `constants.ts`, `types.ts`, `manifest-parser.ts`)
- `grep -c "^export" lib/pdf/index.ts` ≥ 3 (3 blocs export)
- `grep "MANIFESTE_FILE" lib/pdf/manifest-parser.ts` ≥ 1
- `npx jest --no-coverage lib/__tests__/pdf-manifest-parser.test.ts` → tous tests passent (PDF-04, QA-02)
- `npx tsc --noEmit` clean
</verification>

<success_criteria>
- [x] Critère succès #4 ROADMAP : round-trip parser manifeste — 3 entrées → relire → strict equality (test Jest passant)
- [x] Critère succès #5 ROADMAP : `npx jest --no-coverage` clean (au moins pour ce fichier — non-régression vérifiée en 48-04)
- [x] Module `lib/pdf/` complet avec 4 fichiers
</success_criteria>

<output>
Créer `.planning/phases/48-fondation-export-pdf-assets/48-03-SUMMARY.md` avec :
- Format manifeste finalisé (frontmatter + table 5 colonnes)
- Liste des tests Jest ajoutés (nb d'assertions, tests critiques)
- Confirmation barrel `lib/pdf/index.ts` complet (3 + 3 + 4 = 10 exports)
- Note : fichier vault `12 - Impressions/manifeste.md` PAS créé physiquement par cette phase — il sera créé par Phase 49 au premier export PDF (cohérent avec convention vault "lazy creation"). Si besoin d'un fichier squelette pour test manuel : laisser à la discrétion du dev (utiliser `serializeManifeste([])` pour générer le contenu).
</output>
