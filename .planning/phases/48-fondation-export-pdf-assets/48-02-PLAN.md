---
phase: 48-fondation-export-pdf-assets
plan: 02
type: execute
wave: 2
depends_on: [48-01]
files_modified:
  - lib/pdf/index.ts
  - lib/pdf/constants.ts
  - lib/pdf/types.ts
autonomous: true
requirements: [PDF-02, QA-01]

must_haves:
  truths:
    - "lib/pdf/index.ts exporte les constantes Lulu (TRIM_SIZE_CM, BLEED_CM, PAGE_COUNT, BOOK_PALETTE, FONT_SLOTS)"
    - "lib/pdf/index.ts exporte les types BookExportSpec, BookManifestEntry, BookPalette, FontSlot"
    - "FONT_SLOTS.body et FONT_SLOTS.bodyBold pointent vers les alias Andika déclarés en 48-01"
    - "Les imports `from '../pdf'` ou `from '@/lib/pdf'` fonctionnent (barrel)"
  artifacts:
    - path: "lib/pdf/constants.ts"
      provides: "Constantes Lulu Direct (immuables) + palette + slots polices"
      min_lines: 25
    - path: "lib/pdf/types.ts"
      provides: "Types BookExportSpec, BookManifestEntry, BookPalette, FontSlot"
      min_lines: 20
    - path: "lib/pdf/index.ts"
      provides: "Barrel exports du module pdf"
      min_lines: 15
  key_links:
    - from: "lib/pdf/types.ts"
      to: "lib/pdf/constants.ts"
      via: "import type { BOOK_PALETTE, FONT_SLOTS }"
      pattern: "import type.*from './constants'"
    - from: "lib/pdf/index.ts"
      to: "lib/pdf/constants.ts + lib/pdf/types.ts"
      via: "re-export barrel"
      pattern: "export .* from './(constants|types)'"
---

<objective>
Créer le module `lib/pdf/` avec une structure aplatie 3 fichiers (le 4ème — `manifest-parser.ts` — sera ajouté par 48-03). Le module fournit :
- Constantes Lulu Direct figées (TRIM_SIZE_CM, BLEED_CM, PAGE_COUNT, LULU_FORMAT_LABEL)
- Palette couleurs livre (placeholder Phase 48, finalisable Phase 49 sans casser l'API)
- Slots polices (mapping symbolique vers les alias `useFonts` chargés en 48-01)
- Types TypeScript pour spec d'export et entrée de manifeste

Purpose : permettre à 48-03 d'importer `BookManifestEntry` depuis `./types` et à Phase 49+ d'importer toutes les constantes/types via le barrel `import { ... } from '@/lib/pdf'`.

Output : 3 fichiers TS clean, types exportés via barrel, `npx tsc --noEmit` vert.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/48-fondation-export-pdf-assets/48-CONTEXT.md
@.planning/phases/48-fondation-export-pdf-assets/48-RESEARCH.md
@CLAUDE.md
</context>

<interfaces>
Pattern barrel à imiter (`lib/gamification/index.ts`) — RESEARCH.md ligne 245 confirme ce template comme référence.

Alias polices à respecter (cohérent avec `useFonts({ 'Andika-Regular': ..., 'Andika-Bold': ... })` posé en 48-01 Task 3) :
- `Andika-Regular` → corps de texte
- `Andika-Bold` → gras
- `DMSerifDisplay_400Regular` → titres serif (déjà chargé via Google Fonts)
- `Caveat_600SemiBold` → "voix tendre" / italique manuscrit (déjà chargé)
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1 : Créer lib/pdf/constants.ts (Lulu + palette + slots polices)</name>
  <files>lib/pdf/constants.ts</files>
  <behavior>
    - Toutes les constantes sont `export const` avec types primitifs littéraux
    - BOOK_PALETTE et FONT_SLOTS utilisent `as const` pour préserver les types littéraux
    - Aucune dépendance externe (module pur)
  </behavior>
  <action>
  Créer le fichier exactement selon le template RESEARCH.md lignes 382-413 :

  ```typescript
  // lib/pdf/constants.ts — Constantes Lulu Direct (specs immuables)

  /** Trim size carré 21×21 cm (Lulu Direct standard saddle-stitch) */
  export const TRIM_SIZE_CM = 21;

  /** Bleed Lulu Direct : 3.2 mm tous bords */
  export const BLEED_CM = 0.32;

  /** Saddle-stitch impose multiple de 4 pages ; livre cible 16 pages */
  export const PAGE_COUNT = 16;

  /** Label affichable du format (audit trail manifeste) */
  export const LULU_FORMAT_LABEL = 'Lulu 21×21';

  /**
   * Palette couleurs livre (placeholder Phase 48 — finalisée Phase 49).
   * L'API est figée (clés ivory/terracotta/sage/ink/paperShadow), seules les
   * valeurs hex peuvent évoluer en Phase 49 sans casser les imports.
   */
  export const BOOK_PALETTE = {
    ivory: '#FAF6EE',
    terracotta: '#C97D5F',
    sage: '#8FA68E',
    ink: '#2B2A28',
    paperShadow: '#E8E0D0',
  } as const;

  /**
   * Slots de polices disponibles pour le rendu PDF (Phase 49+).
   * Les valeurs DOIVENT correspondre aux alias déclarés dans
   * `app/_layout.tsx` via useFonts() — sinon expo-print ne trouvera pas la
   * police au moment du rendu HTML→PDF (Pitfall 2 RESEARCH.md ligne 355).
   */
  export const FONT_SLOTS = {
    body: 'Andika-Regular',
    bodyBold: 'Andika-Bold',
    display: 'DMSerifDisplay_400Regular',
    whisper: 'Caveat_600SemiBold',
  } as const;
  ```

  Conventions CLAUDE.md respectées : commentaires français, pas de `colors.*` ici (constantes domaine, pas thème UI — la palette livre est figée pour audit Lulu, pas dynamique selon dark/light mode).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep "lib/pdf/constants.ts" | head -5 || echo "tsc OK constants"</automated>
  </verify>
  <done>
  - Fichier existe avec les 6 exports (`TRIM_SIZE_CM`, `BLEED_CM`, `PAGE_COUNT`, `LULU_FORMAT_LABEL`, `BOOK_PALETTE`, `FONT_SLOTS`)
  - `as const` présent sur BOOK_PALETTE et FONT_SLOTS
  - `npx tsc --noEmit` clean sur ce fichier
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2 : Créer lib/pdf/types.ts (BookExportSpec, BookManifestEntry, types dérivés)</name>
  <files>lib/pdf/types.ts</files>
  <behavior>
    - `BookExportSpec` aligne les valeurs runtime sur les constantes (audit trail)
    - `BookManifestEntry` est shape stricte 5 champs string (consommée par 48-03 parser)
    - Types dérivés `BookPalette` et `FontSlot` exportés pour usage Phase 49+
  </behavior>
  <action>
  Créer le fichier exactement selon le template RESEARCH.md lignes 416-442 :

  ```typescript
  // lib/pdf/types.ts — Types domaine export PDF (Lulu Direct)

  import type { BOOK_PALETTE, FONT_SLOTS } from './constants';

  /**
   * Spécifications de génération d'un livre PDF Lulu.
   * Les champs trimCm/bleedCm/pageCount sont figés au moment de l'export pour
   * audit (un livre déjà imprimé doit pouvoir être reproduit même si
   * TRIM_SIZE_CM évolue plus tard).
   */
  export interface BookExportSpec {
    storyId: string;
    format: 'Lulu 21×21';
    trimCm: number;       // = TRIM_SIZE_CM (figé pour audit)
    bleedCm: number;      // = BLEED_CM
    pageCount: number;    // = PAGE_COUNT
    palette: BookPalette;
  }

  /**
   * Une entrée du registre `12 - Impressions/manifeste.md`.
   * Contrainte format markdown table : aucun champ ne doit contenir le
   * caractère `|` (Pitfall 5 RESEARCH.md ligne 373) — le hash SHA-256 est
   * hex, les chemins vault n'ont pas de pipe, c'est satisfait par construction.
   */
  export interface BookManifestEntry {
    id: string;       // storyId (clé unique par export)
    hash: string;     // SHA-256 hex du PDF (intégrité, généré Phase 49)
    date: string;     // YYYY-MM-DD de l'export
    format: string;   // ex: 'Lulu 21×21' — string pour futur-proof autres formats
    chemin: string;   // chemin relatif vault vers le PDF
  }

  /** Type dérivé de la palette (préserve les clés littérales). */
  export type BookPalette = typeof BOOK_PALETTE;

  /** Clé de slot police (body | bodyBold | display | whisper). */
  export type FontSlot = keyof typeof FONT_SLOTS;
  ```

  Note de conception : `BookManifestEntry.format` est `string` (pas le littéral `'Lulu 21×21'`) pour permettre des formats Lulu alternatifs Phase 49+ sans migration. `BookExportSpec.format` reste figé sur le littéral car c'est la cible de Phase 49.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep "lib/pdf/types.ts" | head -5 || echo "tsc OK types"</automated>
  </verify>
  <done>
  - Fichier existe avec les exports : `BookExportSpec`, `BookManifestEntry`, `BookPalette`, `FontSlot`
  - Import `import type { BOOK_PALETTE, FONT_SLOTS } from './constants'` fonctionne
  - `npx tsc --noEmit` clean sur ce fichier
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3 : Créer lib/pdf/index.ts (barrel exports)</name>
  <files>lib/pdf/index.ts</files>
  <behavior>
    - Re-export des constantes via `export { ... } from './constants'`
    - Re-export des types via `export type { ... } from './types'`
    - Pas de re-export de manifest-parser ici — sera ajouté par 48-03
  </behavior>
  <action>
  Créer le fichier — version partielle (sans manifest-parser, qui n'existe pas encore) :

  ```typescript
  // lib/pdf/ — Barrel export pour le domaine export PDF (Lulu Direct).
  // Pattern cohérent avec lib/gamification/index.ts.
  // Note : MANIFESTE_FILE/parseManifeste/serializeManifeste seront ajoutés
  // par le plan 48-03 (manifeste impressions).

  export {
    TRIM_SIZE_CM,
    BLEED_CM,
    PAGE_COUNT,
    LULU_FORMAT_LABEL,
    BOOK_PALETTE,
    FONT_SLOTS,
  } from './constants';

  export type {
    BookExportSpec,
    BookManifestEntry,
    BookPalette,
    FontSlot,
  } from './types';
  ```

  ⚠ Ne pas anticiper l'export de `manifest-parser` ici — laisser un commentaire explicite. 48-03 modifiera ce fichier pour ajouter les 3 exports manquants (`MANIFESTE_FILE`, `parseManifeste`, `serializeManifeste`).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep "lib/pdf/" | head -10 || echo "tsc OK module pdf"</automated>
  </verify>
  <done>
  - `lib/pdf/index.ts` existe et re-export les 6 constantes + 4 types
  - Aucun export pointant vers `./manifest-parser` (ajouté en 48-03)
  - `npx tsc --noEmit` clean sur l'ensemble du module pdf
  - Test importabilité (sanity) : `node -e "console.log(Object.keys(require('./lib/pdf')))"` (si transpilable) ou `grep -c "^export" lib/pdf/index.ts` ≥ 2
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Module pur lib/pdf/ | Pas de boundary — code interne, données statiques |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-04 | Tampering | FONT_SLOTS aliases | mitigate | Les valeurs `'Andika-Regular'` / `'Andika-Bold'` doivent matcher exactement les clés `useFonts` de 48-01 — vérifié par compilation Phase 49 (CSS `font-family`) |
| T-48-05 | Information disclosure | BOOK_PALETTE | accept | Couleurs hex publiques, aucun secret |
</threat_model>

<verification>
- `ls lib/pdf/` → 3 fichiers exactement (`index.ts`, `constants.ts`, `types.ts`)
- `grep -E "^export" lib/pdf/index.ts | wc -l` ≥ 2 (un export const, un export type)
- `grep "Andika-Regular" lib/pdf/constants.ts` ≥ 1 (FONT_SLOTS.body)
- `npx tsc --noEmit` clean (QA-01)
</verification>

<success_criteria>
- [x] Critère succès #3 ROADMAP : `lib/pdf/index.ts` exporte constantes Lulu (TRIM_SIZE_CM=21, BLEED_CM=0.32, PAGE_COUNT=16, palette, slots polices) et types (BookExportSpec, BookManifestEntry)
- [x] Structure aplatie 3 fichiers (RESEARCH.md ligne 233 — `manifest-parser.ts` ajouté en 48-03)
- [x] `npx tsc --noEmit` clean
</success_criteria>

<output>
Créer `.planning/phases/48-fondation-export-pdf-assets/48-02-SUMMARY.md` avec :
- Liste des exports finaux du barrel (6 const + 4 types)
- Note explicite : `manifest-parser.ts` à ajouter par 48-03 → barrel index.ts sera étendu
- Confirmation TS clean
</output>
