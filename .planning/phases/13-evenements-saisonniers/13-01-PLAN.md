---
phase: 13-evenements-saisonniers
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/mascot/seasonal-events-types.ts
  - lib/mascot/seasonal-events-engine.ts
  - lib/mascot/seasonal-events-storage.ts
  - lib/mascot/seasonal-events-content.ts
  - locales/fr/gamification.json
  - locales/en/gamification.json
autonomous: true
requirements: [EVT-01, EVT-02, EVT-03]

must_haves:
  truths:
    - "getActiveEvent() + shouldShowEventVisitor() retourne true pendant une période saisonnière si le profil n'a pas encore interagi"
    - "drawGuaranteedSeasonalReward() retourne toujours une récompense (jamais null/undefined) avec fallback commun"
    - "saveEventProgress() persiste la complétion en SecureStore avec eventId+year comme clé composite"
    - "SEASONAL_EVENT_DIALOGUES contient un dialogue pour chacun des 8 événements de SEASONAL_EVENTS"
    - "buildSeasonalEventAsSaga() produit un objet Saga valide avec un seul chapitre"
  artifacts:
    - path: "lib/mascot/seasonal-events-types.ts"
      provides: "SeasonalEventProgress, SeasonalEventContent types"
      exports: ["SeasonalEventProgress", "SeasonalEventContent"]
    - path: "lib/mascot/seasonal-events-engine.ts"
      provides: "shouldShowEventVisitor, getVisibleEventId, drawGuaranteedSeasonalReward, buildSeasonalEventAsSaga"
      exports: ["shouldShowEventVisitor", "getVisibleEventId", "drawGuaranteedSeasonalReward", "buildSeasonalEventAsSaga"]
    - path: "lib/mascot/seasonal-events-storage.ts"
      provides: "loadEventProgressList, saveEventProgress"
      exports: ["loadEventProgressList", "saveEventProgress"]
    - path: "lib/mascot/seasonal-events-content.ts"
      provides: "SEASONAL_EVENT_DIALOGUES map pour les 8 événements"
      exports: ["SEASONAL_EVENT_DIALOGUES"]
  key_links:
    - from: "lib/mascot/seasonal-events-engine.ts"
      to: "lib/gamification/seasonal.ts"
      via: "import getActiveEvent"
      pattern: "getActiveEvent"
    - from: "lib/mascot/seasonal-events-engine.ts"
      to: "lib/mascot/sagas-types.ts"
      via: "import Saga, SagaProgress, createEmptySagaProgress"
      pattern: "buildSeasonalEventAsSaga"
    - from: "lib/mascot/seasonal-events-storage.ts"
      to: "expo-secure-store"
      via: "SecureStoreCompat"
      pattern: "SecureStoreCompat"
---

<objective>
Créer le moteur d'événements saisonniers : types, engine (détection + récompenses + adaptateur saga), storage SecureStore, contenu narratif i18n pour les 8 événements.

Purpose: Fournir toute la logique métier et le contenu pour que le Plan 02 puisse câbler l'UI sans toucher au moteur.
Output: 4 fichiers lib/mascot/seasonal-events-*.ts + clés i18n fr/en pour les 8 événements.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-evenements-saisonniers/13-CONTEXT.md
@.planning/phases/13-evenements-saisonniers/13-RESEARCH.md

@lib/mascot/sagas-types.ts
@lib/mascot/sagas-storage.ts
@lib/mascot/sagas-content.ts
@lib/gamification/seasonal.ts
@lib/gamification/seasonal-rewards.ts

<interfaces>
<!-- Types saga existants à réutiliser — pas de duplication -->

From lib/mascot/sagas-types.ts:
```typescript
export type SagaTrait = 'courage' | 'sagesse' | 'générosité' | 'malice' | 'curiosité';
export interface SagaChoice {
  id: string; labelKey: string; emoji: string;
  traits: Partial<Record<SagaTrait, number>>; points: number;
}
export interface SagaChapter {
  id: number; narrativeKey: string; choices: SagaChoice[];
  cliffhangerKey: string; narrativeVariants?: Partial<Record<SagaTrait, string>>;
}
export interface Saga {
  id: string; emoji: string; titleKey: string; descriptionKey: string;
  chapters: SagaChapter[]; finale: SagaFinale; sceneEmoji: string;
}
export interface SagaProgress {
  sagaId: string; profileId: string; currentChapter: number;
  choices: Record<number, string>; traits: Record<SagaTrait, number>;
  startDate: string; lastChapterDate: string; status: 'active' | 'completed'; rewardClaimed: boolean;
}
export function createEmptySagaProgress(sagaId: string, profileId: string, date: string): SagaProgress;
```

From lib/gamification/seasonal.ts:
```typescript
export function getActiveEvent(now?: Date): SeasonalEvent | null;
```

From lib/gamification/seasonal-rewards.ts:
```typescript
export const SEASONAL_EVENTS: SeasonalEvent[];
// 8 events: nouvel-an, st-valentin, poisson-avril, paques, ete, rentree, halloween, noel
// Each has rewards: { commun: [...], rare?: [...], épique?: [...], légendaire?: [...], mythique?: [...] }
```

From lib/mascot/sagas-storage.ts (pattern à suivre):
```typescript
import { SecureStoreCompat as SecureStore } from './utils';
function sagaKey(profileId: string): string { return `saga_progress_${profileId}`; }
export async function loadSagaProgress(profileId: string): Promise<SagaProgress | null>;
export async function saveSagaProgress(progress: SagaProgress): Promise<void>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Types + engine + storage des événements saisonniers</name>
  <files>
    lib/mascot/seasonal-events-types.ts,
    lib/mascot/seasonal-events-engine.ts,
    lib/mascot/seasonal-events-storage.ts
  </files>
  <read_first>
    lib/mascot/sagas-types.ts,
    lib/mascot/sagas-storage.ts,
    lib/gamification/seasonal.ts,
    lib/gamification/seasonal-rewards.ts,
    lib/mascot/utils.ts
  </read_first>
  <action>
    **seasonal-events-types.ts** — Créer les types (per D-08, D-03):

    ```typescript
    import type { SagaChapter } from './sagas-types';

    export interface SeasonalEventProgress {
      eventId: string;       // ex: 'paques', 'halloween'
      year: number;          // année calendaire (clé composite eventId+year pour éviter Pitfall 1)
      profileId: string;
      completed: boolean;
      completedAt?: string;  // YYYY-MM-DD
      choiceId?: string;     // choix fait (influence rareté per D-13)
    }

    export interface SeasonalEventContent {
      eventId: string;           // doit matcher SEASONAL_EVENTS[].id
      emoji: string;
      titleKey: string;          // clé i18n
      visitorNameKey: string;    // ex: 'mascot.event.paques.visitor_name'
      chapter: SagaChapter;      // un seul chapitre (per D-03)
      bonusXP: number;           // bonus XP à la complétion (per D-12) — 15 pour tous
    }
    ```

    **seasonal-events-engine.ts** — Créer les fonctions du moteur:

    1. `shouldShowEventVisitor(progressList: SeasonalEventProgress[], profileId: string, now?: Date): boolean` — retourne true si getActiveEvent(now) est actif ET pas de complétion pour eventId+year+profileId dans progressList (per D-08, D-09)

    2. `getVisibleEventId(progressList: SeasonalEventProgress[], profileId: string, now?: Date): string | null` — retourne l'eventId actif non complété, ou null

    3. `drawGuaranteedSeasonalReward(event: SeasonalEvent, choiceIndex: number): { reward: SeasonalRewardItem; rarity: string }` — per D-11, D-13:
       - choiceIndex 0 → pool 'commun'
       - choiceIndex 1 → pool 'rare'
       - choiceIndex 2 → pool 'épique'
       - Fallback : si le pool cible est vide/undefined, descendre au pool inférieur, puis fallback 'commun'
       - Random pick dans le pool sélectionné
       - Type retour : utiliser le type SeasonalRewardItem de lib/types.ts (vérifier le type exact dans types.ts avant d'implémenter)

    4. `buildSeasonalEventAsSaga(content: SeasonalEventContent, profileId: string): { saga: Saga; progress: SagaProgress }` — per D-01:
       - Construire un objet Saga avec chapters: [content.chapter] (un seul chapitre)
       - finale: { variants: {}, defaultTrait: 'courage' } (pas de finale pour events)
       - Utiliser createEmptySagaProgress(content.eventId, profileId, new Date().toISOString().slice(0, 10))
       - Import Saga, SagaProgress, createEmptySagaProgress depuis sagas-types.ts

    5. Constante `SEASONAL_EVENT_BONUS_XP = 15`

    6. Constante `CHOICE_RARITY_MAP: string[] = ['commun', 'rare', 'épique']` — mapping choiceIndex → rareté cible

    **seasonal-events-storage.ts** — Persistance SecureStore (même pattern que sagas-storage.ts):

    ```typescript
    import { SecureStoreCompat as SecureStore } from './utils';
    import type { SeasonalEventProgress } from './seasonal-events-types';

    function eventKey(profileId: string): string {
      return `seasonal_events_${profileId}`;
    }

    export async function loadEventProgressList(profileId: string): Promise<SeasonalEventProgress[]> {
      try {
        const raw = await SecureStore.getItemAsync(eventKey(profileId));
        if (!raw) return [];
        return JSON.parse(raw) as SeasonalEventProgress[];
      } catch { return []; }
    }

    export async function saveEventProgress(progress: SeasonalEventProgress): Promise<void> {
      try {
        const current = await loadEventProgressList(progress.profileId);
        const updated = [
          ...current.filter(p => !(p.eventId === progress.eventId && p.year === progress.year)),
          progress,
        ];
        await SecureStore.setItemAsync(eventKey(progress.profileId), JSON.stringify(updated));
      } catch { /* non-critical */ }
    }
    ```
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -c "seasonal-events" | xargs -I{} test {} -eq 0 && echo "PASS" || echo "FAIL — seasonal-events files have type errors"</automated>
  </verify>
  <acceptance_criteria>
    - lib/mascot/seasonal-events-types.ts exporte SeasonalEventProgress et SeasonalEventContent
    - lib/mascot/seasonal-events-engine.ts exporte shouldShowEventVisitor, getVisibleEventId, drawGuaranteedSeasonalReward, buildSeasonalEventAsSaga, SEASONAL_EVENT_BONUS_XP, CHOICE_RARITY_MAP
    - lib/mascot/seasonal-events-storage.ts exporte loadEventProgressList et saveEventProgress
    - drawGuaranteedSeasonalReward contient un fallback: le code a un chemin qui descend au pool 'commun' si le pool cible est vide
    - saveEventProgress filtre par eventId ET year (clé composite, pas juste eventId)
    - buildSeasonalEventAsSaga retourne un objet avec chapters de length 1
  </acceptance_criteria>
  <done>Les 3 fichiers types/engine/storage existent avec tous les exports, `npx tsc --noEmit` ne rapporte aucune erreur dans ces fichiers</done>
</task>

<task type="auto">
  <name>Task 2: Contenu narratif i18n des 8 événements saisonniers</name>
  <files>
    lib/mascot/seasonal-events-content.ts,
    locales/fr/gamification.json,
    locales/en/gamification.json
  </files>
  <read_first>
    lib/mascot/sagas-content.ts,
    lib/gamification/seasonal-rewards.ts,
    locales/fr/gamification.json,
    locales/en/gamification.json,
    lib/mascot/seasonal-events-types.ts
  </read_first>
  <action>
    **seasonal-events-content.ts** — Créer un SEASONAL_EVENT_DIALOGUES qui mappe chaque eventId à un SeasonalEventContent (per D-14, D-15, D-16).

    Chaque événement a :
    - 1 chapitre (SagaChapter) avec id: 1
    - 2-3 choix (SagaChoice) avec id 'A', 'B', 'C'
    - Chaque choix a des traits (réutiliser les SagaTrait existants) et points: 5
    - narrativeKey, cliffhangerKey, labelKey pointent vers les clés i18n
    - bonusXP: 15 pour tous (SEASONAL_EVENT_BONUS_XP)

    Structure pour chaque événement — exemple Pâques (eventId: 'paques'):
    ```typescript
    {
      eventId: 'paques',
      emoji: '🐣',
      titleKey: 'mascot.event.paques.title',
      visitorNameKey: 'mascot.event.paques.visitor_name',
      bonusXP: SEASONAL_EVENT_BONUS_XP,
      chapter: {
        id: 1,
        narrativeKey: 'mascot.event.paques.narrative',
        cliffhangerKey: 'mascot.event.paques.cliffhanger',
        choices: [
          { id: 'A', labelKey: 'mascot.event.paques.choiceA', emoji: '🥚', traits: { curiosité: 2 }, points: 5 },
          { id: 'B', labelKey: 'mascot.event.paques.choiceB', emoji: '🌸', traits: { générosité: 2 }, points: 5 },
          { id: 'C', labelKey: 'mascot.event.paques.choiceC', emoji: '🍫', traits: { sagesse: 2 }, points: 5 },
        ],
      },
    }
    ```

    Créer les 8 événements avec les eventIds matchant SEASONAL_EVENTS : 'nouvel-an', 'st-valentin', 'poisson-avril', 'paques', 'ete', 'rentree', 'halloween', 'noel'.

    Exporter : `export const SEASONAL_EVENT_DIALOGUES: Record<string, SeasonalEventContent>` et `export function getEventContent(eventId: string): SeasonalEventContent | undefined`

    **locales/fr/gamification.json** — Ajouter sous la clé `mascot.event` les textes FR pour les 8 événements. Chaque événement a : title, visitor_name, narrative, choiceA, choiceB, choiceC (si 3 choix), cliffhanger. Textes courts et festifs (per D-15).

    Exemples FR :
    - paques.title: "Visite du Lapin de Pâques"
    - paques.visitor_name: "Lapin de Pâques"
    - paques.narrative: "Un lapin coloré bondit vers toi, les oreilles dressées et un panier d'œufs sous le bras !"
    - paques.choiceA: "🥚 Chercher les œufs cachés"
    - paques.choiceB: "🌸 Décorer l'arbre de fleurs"
    - paques.choiceC: "🍫 Partager le chocolat"
    - paques.cliffhanger: "Le lapin s'éloigne en sautillant, laissant quelques pépites dorées..."

    - nouvel-an.title: "Visite du Lutin de Minuit"
    - halloween.title: "Visite du Fantôme Farceur"
    - noel.title: "Visite du Père Noël"
    - st-valentin.title: "Visite de Cupidon"
    - poisson-avril.title: "Visite du Poisson Magique"
    - ete.title: "Visite du Crabe Voyageur"
    - rentree.title: "Visite du Hibou Savant"

    **locales/en/gamification.json** — Ajouter les mêmes clés en anglais. Adapter les noms/textes culturellement.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -c "seasonal-events-content" | xargs -I{} test {} -eq 0 && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - lib/mascot/seasonal-events-content.ts exporte SEASONAL_EVENT_DIALOGUES et getEventContent
    - SEASONAL_EVENT_DIALOGUES contient exactement 8 entrées (une par eventId de SEASONAL_EVENTS)
    - Chaque entrée a un chapter avec 2-3 choices, chaque choice a id/labelKey/emoji/traits/points
    - locales/fr/gamification.json contient la clé mascot.event avec 8 sous-clés (nouvel-an, st-valentin, poisson-avril, paques, ete, rentree, halloween, noel)
    - locales/en/gamification.json contient les mêmes clés mascot.event avec 8 sous-clés
    - Chaque sous-clé contient au minimum : title, visitor_name, narrative, choiceA, choiceB, cliffhanger
  </acceptance_criteria>
  <done>Les 8 événements ont leur contenu narratif complet en FR et EN, SEASONAL_EVENT_DIALOGUES est typé et exporté, `npx tsc --noEmit` passe</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` ne rapporte aucune erreur dans les 4 fichiers seasonal-events-*.ts
- `SEASONAL_EVENT_DIALOGUES` a 8 entrées matchant exactement les IDs de `SEASONAL_EVENTS`
- Les clés i18n fr et en sont symétriques (mêmes clés dans les deux fichiers)
- `drawGuaranteedSeasonalReward` ne peut jamais retourner undefined (fallback testé)
</verification>

<success_criteria>
Le moteur d'événements saisonniers est complet et typé : détection calendrier, tirage récompense garanti, persistance SecureStore, adaptateur saga, contenu narratif i18n pour les 8 événements. Prêt à être câblé dans l'UI (Plan 02).
</success_criteria>

<output>
After completion, create `.planning/phases/13-evenements-saisonniers/13-01-SUMMARY.md`
</output>
