---
phase: 51-ux-export-manuel-lulu
plan: 02
type: execute
wave: 2
depends_on: ["51-01"]
files_modified:
  - app/impressions.tsx
  - components/pdf/ExportCard.tsx
  - components/pdf/index.ts
  - lib/pdf/book-storage.ts
  - lib/pdf/index.ts
  - app/(tabs)/more.tsx
autonomous: true
requirements: [UX-01, UX-04]
must_haves:
  truths:
    - "Un nouvel écran `app/impressions.tsx` est navigable via expo-router (route `/impressions`)"
    - "L'écran lit `12 - Impressions/manifeste.md` via `parseManifeste(vault.readFile(MANIFESTE_FILE))` et affiche les entrées en cards verticales scrollables"
    - "Chaque card affiche : titre histoire (ou ID en italique si supprimée — RESEARCH Open Question 4), date JJ/MM/AAAA (CLAUDE.md), format `Lulu 21×21`, hash court (8 chars)"
    - "Tap card → `Print.printAsync({ uri })` (iOS) / `Linking.openURL(uri)` (Android) avec URI absolu reconstruit via `buildVaultPdfUri` exporté depuis `book-storage.ts`"
    - "Bouton header CTA primaire 'Nouveau livre' ouvre `BookExportModal` (51-01)"
    - "Empty state si `entries.length === 0` : message FR + CTA 'Nouveau livre'"
    - "Pull-to-refresh re-parse le manifeste"
    - "Une row 'Mes impressions' est ajoutée à `app/(tabs)/more.tsx` catégorie `souvenirs` (RESEARCH §8 Open Question 1) avec icône `Printer` ou `BookOpen` lucide → navigation `/impressions`"
    - "L'édit de `more.tsx` est minimal : 1 seule entrée ajoutée au tableau menuItems, AUCUN refacto, AUCUNE autre ligne touchée (working tree user en cours)"
    - "`buildVaultPdfUri(vault, entry)` est exporté depuis `lib/pdf/book-storage.ts` puis re-exporté via `lib/pdf/index.ts` — réutilise la logique `buildVaultUri` existante avec check path traversal"
    - "`npx tsc --noEmit` clean (hors pré-existantes)"
  artifacts:
    - path: "app/impressions.tsx"
      provides: "Écran 'Mes impressions' liste manifeste + CTA générer + pull-to-refresh"
      min_lines: 120
    - path: "components/pdf/ExportCard.tsx"
      provides: "Card item liste manifeste — React.memo, useCallback handlers"
      min_lines: 60
    - path: "lib/pdf/book-storage.ts"
      provides: "+export `buildVaultPdfUri(vault, entry)` (refactor mineur — RESEARCH Pitfall 6)"
      contains: "export function buildVaultPdfUri"
    - path: "app/(tabs)/more.tsx"
      provides: "+1 row 'Mes impressions' catégorie souvenirs (édit minimal)"
  key_links:
    - from: "app/impressions.tsx"
      to: "lib/pdf/manifest-parser.ts"
      via: "parseManifeste(vault.readFile(MANIFESTE_FILE))"
      pattern: "parseManifeste"
    - from: "app/impressions.tsx"
      to: "components/pdf/BookExportModal.tsx"
      via: "import { BookExportModal } from '../components/pdf'"
      pattern: "BookExportModal"
    - from: "app/impressions.tsx"
      to: "lib/pdf/book-storage.ts"
      via: "buildVaultPdfUri(vault, entry) pour reconstruire URI absolu"
      pattern: "buildVaultPdfUri"
    - from: "app/(tabs)/more.tsx"
      to: "/impressions"
      via: "router.push('/impressions') ou Link href='/impressions'"
      pattern: "/impressions"
---

<objective>
Créer l'écran `app/impressions.tsx` ("Mes impressions") qui :
1. Lit le manifeste vault (`12 - Impressions/manifeste.md`) via `parseManifeste`
2. Affiche les entrées en cards verticales scrollables (`ExportCard`)
3. Permet de tap une card pour ouvrir le PDF correspondant via `Print.printAsync` (URI absolu reconstruit via `buildVaultPdfUri` à exporter depuis `book-storage.ts`)
4. Expose un CTA header "Nouveau livre" qui ouvre `BookExportModal` (Plan 51-01)
5. Gère pull-to-refresh + empty state + histoires supprimées
6. Est référencé depuis `app/(tabs)/more.tsx` via une row minimaliste catégorie souvenirs (édit chirurgical, working tree user en cours).

Purpose : c'est le point d'entrée utilisateur de Phase 51 (CONTEXT.md déviation ROADMAP — écran dédié plutôt que long-press menu). Sans cet écran, `BookExportModal` n'a aucun contexte d'invocation et le manifeste reste invisible.

Output : route `/impressions` fonctionnelle, ExportCard memoïsé, refactor mineur `book-storage.ts` (export `buildVaultPdfUri`), édit minimal `more.tsx`. Phase post-export et i18n JSON FR sont à 51-03 — ici on référence les clés `t('impressions.screen.*')` et on accepte qu'elles affichent les clés brutes en attendant 51-03 (fallback i18next standard).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/51-ux-export-manuel-lulu/51-CONTEXT.md
@.planning/phases/51-ux-export-manuel-lulu/51-RESEARCH.md
@.planning/phases/51-ux-export-manuel-lulu/51-01-SUMMARY.md
@lib/pdf/book-storage.ts
@lib/pdf/manifest-parser.ts
@lib/pdf/types.ts
@lib/pdf/index.ts
@components/pdf/BookExportModal.tsx
@app/(tabs)/more.tsx
@contexts/VaultContext.tsx
@CLAUDE.md

<interfaces>
<!-- API à exporter depuis lib/pdf/book-storage.ts (refactor 51-02) -->
```ts
/**
 * Reconstruit l'URI absolu file:// d'un PDF depuis une entrée manifeste.
 * Réutilise la même logique de validation path-traversal que `buildVaultUri`
 * (book-storage.ts:72-92, déjà existante en interne — Phase 49).
 *
 * @throws Error si entry.chemin contient '..' ou tente une sortie du vault
 */
export function buildVaultPdfUri(vault: VaultManager, entry: BookManifestEntry): string;
```

<!-- Manifeste structure (lib/pdf/types.ts — existant) -->
```ts
export interface BookManifestEntry {
  id: string;        // ex "voyage-de-lucas"
  hash: string;      // SHA-256 hex 64 chars
  date: string;      // ISO YYYY-MM-DD
  format: string;    // ex "Lulu 21x21"
  chemin: string;    // relatif vault, ex "12 - Impressions/PDFs/voyage-de-lucas-2026-05-04.pdf"
}
```

<!-- more.tsx structure menuItems (vérifié grep) -->
```tsx
// app/(tabs)/more.tsx — ligne ~191-194 catégorie 'souvenirs'
{ Icon: Camera, label: t('menu.items.photos'), route: '/(tabs)/photos', color: colors.catSouvenirs, category: 'souvenirs' as const },
{ Icon: MessageCircle, label: t('menu.items.quotes'), route: '/(tabs)/quotes', color: colors.catSouvenirs, category: 'souvenirs' as const },
{ Icon: HandHeart, label: t('menu.items.gratitude'), route: '/(tabs)/gratitude', color: colors.catSouvenirs, category: 'souvenirs' as const },
{ Icon: BookOpen, label: t('menu.items.bedtimeStories'), route: '/(tabs)/stories', color: colors.catSouvenirs, category: 'souvenirs' as const },
// AJOUT 51-02 : juste après la ligne bedtimeStories
{ Icon: Printer, label: t('menu.items.impressions'), route: '/impressions', color: colors.catSouvenirs, category: 'souvenirs' as const },
```

<!-- Format date JJ/MM/AAAA (CLAUDE.md) -->
```ts
function formatDateFR(iso: string): string {
  // "2026-05-04" → "04/05/2026"
  const [y, m, d] = iso.split('-');
  return `${d}/${m}/${y}`;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 : Exporter buildVaultPdfUri depuis book-storage.ts</name>
  <files>lib/pdf/book-storage.ts, lib/pdf/index.ts, lib/pdf/__tests__/book-storage.uri.test.ts</files>
  <behavior>
    - `buildVaultPdfUri(vault, entry)` retourne `file://{vaultPath}/{entry.chemin}` correctement quote-encodé
    - Si `entry.chemin` contient `..` ou tente sortir du vault → throw Error("Path traversal detected")
    - Si `vaultPath` se termine déjà par `/` → pas de double slash
    - Si l'URI ne commence pas par `file://` → préfixer
    - Test 1 : entrée valide → URI correct
    - Test 2 : `chemin` avec `..` → throw
    - Test 3 : `vaultPath` avec trailing slash → pas de double slash
  </behavior>
  <action>
    1. Ouvrir `lib/pdf/book-storage.ts`. Repérer la logique interne `buildVaultUri` (lignes 72-92 selon RESEARCH).
    2. Si `buildVaultUri` n'est pas exporté, créer une nouvelle fonction publique `buildVaultPdfUri(vault: VaultManager, entry: BookManifestEntry): string` qui :
       - Valide `if (entry.chemin.includes('..')) throw new Error('Path traversal detected');`
       - Reconstruit `const base = vault.vaultPath.replace(/\/$/, '');` puis `const full = \`${base}/${entry.chemin}\`;`
       - Retourne `full.startsWith('file://') ? full : \`file://${full}\``
       - Importer `BookManifestEntry` depuis `./types`
    3. Mettre à jour `lib/pdf/index.ts` : `export { persistBookPdf, buildVaultPdfUri } from './book-storage';`
    4. Créer `lib/pdf/__tests__/book-storage.uri.test.ts` avec 3 tests Jest. Mocker `VaultManager` minimal :
       ```ts
       const fakeVault = { vaultPath: '/tmp/vault' } as VaultManager;
       const entry: BookManifestEntry = { id: 'x', hash: 'h', date: '2026-05-04', format: 'Lulu 21x21', chemin: '12 - Impressions/PDFs/x-2026-05-04.pdf' };
       expect(buildVaultPdfUri(fakeVault, entry)).toBe('file:///tmp/vault/12 - Impressions/PDFs/x-2026-05-04.pdf');
       expect(() => buildVaultPdfUri(fakeVault, { ...entry, chemin: '../etc/passwd' })).toThrow();
       expect(buildVaultPdfUri({ vaultPath: '/tmp/vault/' } as VaultManager, entry)).not.toContain('//12');
       ```
    5. NE PAS dupliquer la logique `buildVaultUri` existante — soit l'exposer (rename + export), soit appeler la fonction interne. Préférer un wrapper public propre.
  </action>
  <verify>
    <automated>npx jest --no-coverage lib/pdf/__tests__/book-storage.uri.test.ts</automated>
  </verify>
  <done>`buildVaultPdfUri` exporté depuis `lib/pdf/index.ts`, 3 tests Jest verts, path traversal protégé.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 : Créer ExportCard + écran app/impressions.tsx + row more.tsx</name>
  <files>components/pdf/ExportCard.tsx, components/pdf/index.ts, app/impressions.tsx, app/(tabs)/more.tsx</files>
  <behavior>
    - ExportCard : composant React.memo qui affiche titre/date/format/hash court + onPress
    - Si `storyTitle` est l'ID brut (histoire supprimée) → italique + tooltip i18n `t('impressions.card.deletedStory')`
    - app/impressions.tsx : chargement initial du manifeste (try/catch → []), pull-to-refresh, ScrollView de cards, header CTA "Nouveau livre", BookExportModal contrôlée par état local
    - Empty state si `entries.length === 0`
    - more.tsx : 1 seule ligne ajoutée dans le tableau menuItems après bedtimeStories — aucun autre touché
  </behavior>
  <action>
    1. Créer `components/pdf/ExportCard.tsx` :
       ```tsx
       import React, { useCallback } from 'react';
       import { Pressable, View, Text, StyleSheet } from 'react-native';
       import { useThemeColors } from '../../contexts/ThemeContext';
       import { Spacing, Typography } from '../../constants';
       import type { BookManifestEntry } from '../../lib/pdf';
       import { useTranslation } from 'react-i18next';

       interface Props {
         entry: BookManifestEntry;
         storyTitle: string;
         storyDeleted: boolean;
         onPress: (entry: BookManifestEntry) => void;
       }

       function formatDateFR(iso: string): string {
         const [y, m, d] = iso.split('-');
         return `${d}/${m}/${y}`;
       }

       function ExportCardImpl({ entry, storyTitle, storyDeleted, onPress }: Props) {
         const { t } = useTranslation();
         const { colors } = useThemeColors();
         const handlePress = useCallback(() => onPress(entry), [entry, onPress]);
         return (
           <Pressable onPress={handlePress} style={[styles.card, { backgroundColor: colors.card }]}>
             <Text style={[styles.title, { color: colors.text, fontStyle: storyDeleted ? 'italic' : 'normal' }]}>
               {storyTitle}
             </Text>
             <View style={styles.row}>
               <Text style={[styles.meta, { color: colors.muted }]}>{t('impressions.card.exportedOn', { date: formatDateFR(entry.date) })}</Text>
               <Text style={[styles.meta, { color: colors.muted }]}>{entry.format}</Text>
             </View>
             <Text style={[styles.hash, { color: colors.muted }]}>#{entry.hash.slice(0, 8)}</Text>
           </Pressable>
         );
       }

       export const ExportCard = React.memo(ExportCardImpl);

       const styles = StyleSheet.create({
         card: { padding: Spacing.lg, marginBottom: Spacing.md, borderRadius: 12 },
         title: { ...Typography.h3 },
         row: { flexDirection: 'row', justifyContent: 'space-between', marginTop: Spacing.xs },
         meta: { ...Typography.caption },
         hash: { ...Typography.caption, marginTop: Spacing.xs, fontFamily: 'monospace' },
       });
       ```
       (adapter `Typography.h3`/`caption` aux clés réelles du projet — `Typography.body`, etc. si nécessaire)
    2. Mettre à jour `components/pdf/index.ts` : `export { ExportCard } from './ExportCard';`
    3. Créer `app/impressions.tsx` :
       ```tsx
       import { useCallback, useEffect, useState } from 'react';
       import { View, Text, ScrollView, RefreshControl, Pressable, Alert, StyleSheet, Platform, Linking } from 'react-native';
       import { SafeAreaView } from 'react-native-safe-area-context';
       import { Stack, useLocalSearchParams } from 'expo-router';
       import * as Print from 'expo-print';
       import { useTranslation } from 'react-i18next';
       import { Plus } from 'lucide-react-native';
       import { useVault } from '../contexts/VaultContext';
       import { useThemeColors } from '../contexts/ThemeContext';
       import { parseManifeste, MANIFESTE_FILE } from '../lib/pdf/manifest-parser';
       import { buildVaultPdfUri } from '../lib/pdf';
       import type { BookManifestEntry } from '../lib/pdf';
       import { BookExportModal, ExportCard } from '../components/pdf';
       import { Spacing, Typography } from '../constants';

       export default function ImpressionsScreen() {
         const { t } = useTranslation();
         const { vault, stories } = useVault();
         const { colors, primary } = useThemeColors();
         const [entries, setEntries] = useState<BookManifestEntry[]>([]);
         const [refreshing, setRefreshing] = useState(false);
         const [modalOpen, setModalOpen] = useState(false);

         const loadManifeste = useCallback(async () => {
           if (!vault) return;
           try {
             const raw = await vault.readFile(MANIFESTE_FILE);
             setEntries(parseManifeste(raw));
           } catch {
             setEntries([]);
           }
         }, [vault]);

         useEffect(() => { loadManifeste(); }, [loadManifeste]);

         const onRefresh = useCallback(async () => {
           setRefreshing(true);
           await loadManifeste();
           setRefreshing(false);
         }, [loadManifeste]);

         const handleOpenPdf = useCallback(async (entry: BookManifestEntry) => {
           if (!vault) return;
           try {
             const uri = buildVaultPdfUri(vault, entry);
             if (Platform.OS === 'ios') await Print.printAsync({ uri });
             else await Linking.openURL(uri);
           } catch (err) {
             if (__DEV__) console.warn('[impressions] open error', err);
             Alert.alert(t('impressions.errors.openTitle'), t('impressions.errors.openBody'));
           }
         }, [vault, t]);

         const handleSuccess = useCallback((_uri: string, _title: string) => {
           setModalOpen(false);
           loadManifeste();
         }, [loadManifeste]);

         return (
           <SafeAreaView style={[styles.safe, { backgroundColor: colors.bg }]}>
             <Stack.Screen options={{ title: t('impressions.screen.title') }} />
             <View style={styles.header}>
               <Text style={[styles.title, { color: colors.text }]}>{t('impressions.screen.title')}</Text>
               <Pressable onPress={() => setModalOpen(true)} style={[styles.cta, { backgroundColor: primary }]}>
                 <Plus size={16} color={colors.bg} />
                 <Text style={{ color: colors.bg, marginLeft: Spacing.xs }}>{t('impressions.screen.newButton')}</Text>
               </Pressable>
             </View>
             <ScrollView
               contentContainerStyle={styles.list}
               refreshControl={<RefreshControl refreshing={refreshing} onRefresh={onRefresh} />}
             >
               {entries.length === 0 ? (
                 <View style={styles.empty}>
                   <Text style={[styles.emptyTitle, { color: colors.text }]}>{t('impressions.screen.empty.title')}</Text>
                   <Text style={[styles.emptyDesc, { color: colors.muted }]}>{t('impressions.screen.empty.description')}</Text>
                   <Pressable onPress={() => setModalOpen(true)} style={[styles.cta, { backgroundColor: primary, marginTop: Spacing.lg }]}>
                     <Text style={{ color: colors.bg }}>{t('impressions.screen.empty.cta')}</Text>
                   </Pressable>
                 </View>
               ) : (
                 entries.map((e) => {
                   const story = stories.find((s) => s.id === e.id);
                   return (
                     <ExportCard
                       key={`${e.id}-${e.date}-${e.hash.slice(0, 8)}`}
                       entry={e}
                       storyTitle={story?.titre ?? e.id}
                       storyDeleted={!story}
                       onPress={handleOpenPdf}
                     />
                   );
                 })
               )}
             </ScrollView>
             <BookExportModal
               visible={modalOpen}
               onClose={() => setModalOpen(false)}
               onSuccess={handleSuccess}
             />
           </SafeAreaView>
         );
       }

       const styles = StyleSheet.create({
         safe: { flex: 1 },
         header: { flexDirection: 'row', alignItems: 'center', justifyContent: 'space-between', padding: Spacing.lg },
         title: { ...Typography.h1 },
         cta: { flexDirection: 'row', alignItems: 'center', paddingHorizontal: Spacing.md, paddingVertical: Spacing.sm, borderRadius: 8 },
         list: { padding: Spacing.lg },
         empty: { alignItems: 'center', paddingVertical: Spacing['3xl'] },
         emptyTitle: { ...Typography.h2, marginBottom: Spacing.sm },
         emptyDesc: { ...Typography.body, textAlign: 'center' },
       });
       ```
       (Ajuster `Typography.*`/`Spacing.*` aux tokens réels du projet)
    4. Édit `app/(tabs)/more.tsx` — **MINIMAL, working tree user en cours** :
       - Lire le fichier complet (juste pour comprendre le tableau menuItems)
       - Ajouter `Printer` à l'import lucide-react-native (ou utiliser un icon déjà importé comme `BookOpen` si conflit)
       - Ajouter UNE seule ligne au tableau menuItems, juste après la ligne `bedtimeStories` (ligne ~194), catégorie `souvenirs` :
         ```tsx
         { Icon: Printer, label: t('menu.items.impressions'), route: '/impressions', color: colors.catSouvenirs, category: 'souvenirs' as const },
         ```
       - Ajouter UNE clé `impressions` dans `locales/fr/common.json` sous `menu.items` (lecture du fichier existant pour repérer la section `menu.items`) — valeur `"Mes impressions"`
       - NE PAS toucher à autre chose. NE PAS reformater. NE PAS optimiser les imports.
       - Si conflit logique avec une modif user → STOP et demander confirmation utilisateur.
    5. Vérifier route expo-router : `app/impressions.tsx` est auto-discovery, pas de modif `app/_layout.tsx` requise (route racine, hors tabs). Confirmer en lisant `app/_layout.tsx` qu'il n'y a pas de Stack.Screen explicite à ajouter (sinon ajouter `<Stack.Screen name="impressions" options={{ headerShown: false }} />` ou `{ title: 'Mes impressions' }`).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "(app/impressions|components/pdf/ExportCard|app/\\(tabs\\)/more)" | grep -v "MemoryEditor\\|cooklang\\|useVault" || echo "tsc OK"</automated>
  </verify>
  <done>Route `/impressions` accessible, ExportCard memoïsé, more.tsx édit minimal validé, `buildVaultPdfUri` consommé pour ouvrir le PDF, empty state + pull-to-refresh fonctionnels, tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vault file → app | `manifeste.md` parsé → `entry.chemin` peut potentiellement être tampered si l'utilisateur édite le fichier markdown directement |
| Manifeste entry → file system | `buildVaultPdfUri` valide path traversal |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-02-01 | Tampering | manifeste corrompu (édit manuel utilisateur) | mitigate | `parseManifeste` ignore lignes malformées (déjà testé Phase 49) + `buildVaultPdfUri` throw sur `..` |
| T-51-02-02 | Information Disclosure | URI vault exposé via Print/Linking | accept | URI local `file://`, jamais transmis externe |
| T-51-02-03 | DoS | manifeste >1000 entrées rend ScrollView lent | accept | RESEARCH Assumption A5 : <100 entrées attendues. Pagination future si besoin (Phase 52+) |
</threat_model>

<verification>
1. `npx tsc --noEmit` clean (hors pré-existantes)
2. `npx jest --no-coverage lib/pdf/__tests__/book-storage.uri.test.ts` vert
3. `git diff app/(tabs)/more.tsx` — montre 1 seule ligne ajoutée + 1 import (pas de refacto)
4. Test manuel : navigation menu Plus → "Mes impressions" → écran s'ouvre, manifeste affiché ou empty state, CTA "Nouveau livre" ouvre BookExportModal
</verification>

<success_criteria>
- Route `/impressions` fonctionnelle
- ExportCard rend correctement les entrées manifeste avec titre/date/format/hash
- `buildVaultPdfUri` exporté + testé
- more.tsx édit minimal (1 ligne menuItems + 1 clé i18n common.json)
- Aucune régression `npx tsc --noEmit` (hors pré-existantes)
- Tap card ouvre le PDF via Print.printAsync (iOS) ou Linking (Android)
</success_criteria>

<output>
Après complétion, créer `.planning/phases/51-ux-export-manuel-lulu/51-02-SUMMARY.md` documentant :
- Choix layout (cards verticales scrollables retenu)
- Édit minimal more.tsx (préciser ligne ajoutée + raison du non-refacto)
- Refactor `buildVaultPdfUri` (export public + tests)
- Branchement BookExportModal vers `onSuccess` (refresh manifeste après génération)
- Commit : `feat(51-02): écran Mes impressions + row more.tsx + buildVaultPdfUri`
</output>
