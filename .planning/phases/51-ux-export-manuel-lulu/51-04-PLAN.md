---
phase: 51-ux-export-manuel-lulu
plan: 04
type: execute
wave: 4
depends_on: ["51-01", "51-02", "51-03"]
files_modified:
  - app/dev-deep-link.tsx
  - CLAUDE.md
  - .planning/phases/51-ux-export-manuel-lulu/51-04-SUMMARY.md
  - .planning/phases/51-ux-export-manuel-lulu/51-PHASE-SUMMARY.md
autonomous: false
requirements: [QA-01, QA-02, QA-03]
must_haves:
  truths:
    - "Le manifeste `12 - Impressions/manifeste.md` est mis à jour automatiquement à chaque export depuis l'écran 'Mes impressions' (validation e2e device)"
    - "`CLAUDE.md` racine est à jour : Stack mentionne `expo-print`, `expo-sharing`, `qrcode` v1.5.4, `expo-clipboard` ; Architecture mentionne `lib/pdf/` et `app/impressions.tsx` ; Vault mentionne `12 - Impressions/PDFs/{id}-{date}.pdf` + `12 - Impressions/manifeste.md`"
    - "`app/dev-deep-link.tsx` ne contient plus de boutons 'PDF' (redondants avec UI Phase 51) ; la fonction `generatePdf` est supprimée ; le fichier reste pour les tests deep links Phase 50"
    - "`app/dev-deep-link.tsx` conserve son gating `if (!__DEV__) return <Indisponible/>`"
    - "Aucun import inutilisé restant dans `dev-deep-link.tsx` (suppression `generateBookPdf, persistBookPdf` notamment)"
    - "`npx tsc --noEmit` clean (hors pré-existantes MemoryEditor.tsx, cooklang.ts, useVault.ts)"
    - "`npx jest --no-coverage` clean (incluant les nouveaux tests Phase 51-01, 51-02, 51-03)"
    - "Test device manuel : depuis 'Mes impressions' → générer un livre → 3 actions post-export fonctionnent → manifeste mis à jour → scan du QR du PDF généré ouvre l'app + autoplay (carry-over Phase 50)"
    - "`51-04-SUMMARY.md` et `51-PHASE-SUMMARY.md` créés"
  artifacts:
    - path: "app/dev-deep-link.tsx"
      provides: "Écran dev allégé : tests deep links Phase 50 conservés, boutons PDF supprimés"
    - path: "CLAUDE.md"
      provides: "Stack/Architecture/Vault mis à jour pour Phase 49+50+51"
    - path: ".planning/phases/51-ux-export-manuel-lulu/51-04-SUMMARY.md"
      provides: "Summary du plan 04"
    - path: ".planning/phases/51-ux-export-manuel-lulu/51-PHASE-SUMMARY.md"
      provides: "Summary global de Phase 51"
  key_links:
    - from: "app/impressions.tsx"
      to: "12 - Impressions/manifeste.md"
      via: "BookExportModal → persistBookPdf (Phase 49) → manifeste auto-update — validation e2e Phase 51"
      pattern: "manifeste"
    - from: "CLAUDE.md"
      to: "lib/pdf/"
      via: "section Architecture mentionne pipeline PDF"
      pattern: "lib/pdf"
---

<objective>
Verrouiller la phase 51 avec :
1. Validation e2e du flux complet sur device : génération depuis l'écran "Mes impressions" → 3 actions post-export → manifeste mis à jour → scan QR du PDF (carry-over Phase 50)
2. Cleanup `app/dev-deep-link.tsx` : retirer les boutons "PDF" et la fonction `generatePdf` (redondants avec UI Phase 51) tout en conservant le fichier pour les tests deep links Phase 50
3. Mise à jour `CLAUDE.md` racine : Stack (+ expo-print, expo-sharing, qrcode, expo-clipboard) + Architecture (+ lib/pdf/, app/impressions.tsx) + Vault (+ 12 - Impressions/)
4. Tests automatisés : tsc + jest verts
5. Rédaction des SUMMARY (51-04 + 51-PHASE)

Purpose : sceller la non-régression et la documentation. C'est le quality gate avant TestFlight. Sans ce plan, la Phase 51 reste "fonctionnelle" mais l'écran dev expose des entry-points obsolètes et CLAUDE.md ne reflète pas la nouvelle architecture.

Output : repo cohérent, docs alignées, validation device confirmée par checkpoint humain.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/51-ux-export-manuel-lulu/51-CONTEXT.md
@.planning/phases/51-ux-export-manuel-lulu/51-RESEARCH.md
@.planning/phases/51-ux-export-manuel-lulu/51-01-SUMMARY.md
@.planning/phases/51-ux-export-manuel-lulu/51-02-SUMMARY.md
@.planning/phases/51-ux-export-manuel-lulu/51-03-SUMMARY.md
@.planning/phases/49-layout-livre-generation-pdf/49-PHASE-SUMMARY.md
@.planning/phases/50-qr-audio-deep-links/50-PHASE-SUMMARY.md
@app/dev-deep-link.tsx
@CLAUDE.md
@lib/pdf/book-storage.ts

<interfaces>
<!-- Cleanup ciblé app/dev-deep-link.tsx (RESEARCH §8) -->
- Lignes ~76-105 : supprimer la fonction `generatePdf`
- Lignes ~149-162 : supprimer la `<View style={styles.row}>` qui wrap PressableScale "PDF" — revenir au `<Pressable>` simple
- Imports : retirer `import { generateBookPdf, persistBookPdf } from '../lib/pdf';` (devenu inutile)
- Conserver : gating `__DEV__`, tests `import-note`, `open/meals`, `story/<id>` (Phase 50)

<!-- CLAUDE.md sections cibles -->

# Stack — AVANT
- React Native 0.81.5 + Expo SDK 54 + expo-router v6
- react-native-reanimated ~4.1
- Données: Markdown files dans un vault Obsidian
- Persistance prefs: expo-secure-store
- Build: `npx expo run:ios --device`
- Type check: `npx tsc --noEmit`

# Stack — APRÈS (ajouts uniquement)
- ... (existant)
- Génération PDF : `expo-print` ~15.0.x + `expo-sharing` ~14.0.x (Share Sheet iOS)
- QR codes : `qrcode` 1.5.4 + `expo-clipboard` ~8.0.x

# Architecture — AVANT
- `contexts/VaultContext.tsx`, `hooks/useVault.ts`, `lib/parser.ts`, `lib/vault.ts`, ...

# Architecture — APRÈS (ajouts)
- `lib/pdf/` — pipeline export PDF Lulu (génération `expo-print`, hash SHA-256, manifeste, QR audio) — voir Phase 49+50
- `app/impressions.tsx` — écran "Mes impressions" (liste manifeste + génération nouveaux livres) — voir Phase 51
- `components/pdf/` — UI export (BookExportModal, PostExportView, LuluInstructionsModal, ExportCard)

# Vault structure — AJOUT (nouvelle section ou mise à jour existante)
- `12 - Impressions/PDFs/{id}-{date}.pdf` — PDFs générés persistés
- `12 - Impressions/manifeste.md` — index des exports (frontmatter + table 5 colonnes)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 : Cleanup app/dev-deep-link.tsx (suppression boutons PDF)</name>
  <files>app/dev-deep-link.tsx</files>
  <action>
    1. Lire `app/dev-deep-link.tsx` complet pour repérer la structure exacte (les lignes peuvent avoir bougé depuis le RESEARCH).
    2. Supprimer :
       - La fonction `generatePdf` (handler async qui appelle generateBookPdf + persistBookPdf)
       - Le `<View style={styles.row}>` ou `<PressableScale>` qui correspond au bouton "PDF" (label "Tester génération PDF" ou similaire)
       - L'import `import { generateBookPdf, persistBookPdf } from '../lib/pdf';` (vérifier d'abord qu'il n'est plus utilisé ailleurs dans le fichier)
       - Tout state local lié au PDF (ex: `const [pdfBusy, setPdfBusy] = useState(false);` si présent)
    3. Conserver :
       - Le gating `if (!__DEV__) return <Indisponible/>;`
       - Tous les boutons de test deep links Phase 50 : `import-note`, `open/meals`, `story/<id>`, scan QR papier, etc.
       - Le SafeAreaView, le ModalHeader, les styles partagés
    4. Lancer `npx tsc --noEmit` et corriger immédiatement les erreurs liées au cleanup (imports orphelins, variables inutilisées).
    5. Si des styles deviennent inutilisés (ex: `styles.row` n'est plus référencé que par le bloc supprimé), les retirer aussi pour ne pas laisser de dead code.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep "app/dev-deep-link" | grep -v "MemoryEditor\\|cooklang\\|useVault" || echo "tsc OK pour dev-deep-link.tsx"</automated>
  </verify>
  <done>`app/dev-deep-link.tsx` ne contient plus aucune référence à `generateBookPdf`/`persistBookPdf`, le bouton "PDF" est supprimé, les tests deep links Phase 50 restent intacts, tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2 : Mettre à jour CLAUDE.md racine (Stack + Architecture + Vault)</name>
  <files>CLAUDE.md</files>
  <action>
    1. Lire `CLAUDE.md` complet pour repérer les sections `## Stack`, `## Architecture`, et toute mention existante du vault (recherche grep `Vault recettes|03 - Cuisine`).
    2. Section `## Stack` — ajouter à la fin de la liste existante :
       ```
       - Génération PDF : `expo-print` ~15.0.8 + `expo-sharing` ~14.0.8 (Share Sheet iOS)
       - QR codes : `qrcode` 1.5.4 + `expo-clipboard` ~8.0.8
       ```
    3. Section `## Architecture` — ajouter à la liste existante (après les hooks de domaines) :
       ```
       - `lib/pdf/` — pipeline export PDF Lulu (Phase 49+50+51) : `pdf-generator.ts` (génération expo-print + hash SHA-256), `book-storage.ts` (persistence vault + manifeste), `manifest-parser.ts` (parser bidirectionnel manifeste), `qr-generator.ts` (QR audio deep links Phase 50), `html-template.ts` (mise en page livre Lulu 21×21), `saga-detection.ts` (détection sagas multi-tomes), `text-splitter.ts` (pagination texte), `ornaments.ts` (ornements décoratifs), `print-illustrations.ts` (intégration illustrations), `asset-loader.ts` (chargement assets ornements), `constants.ts` (specs Lulu), `types.ts` (BookManifestEntry, etc.)
       - `app/impressions.tsx` — écran "Mes impressions" (liste manifeste + génération nouveaux livres) — Phase 51
       - `components/pdf/` — UI export (BookExportModal state machine, PostExportView 3 actions, LuluInstructionsModal manuel Lulu FR, ExportCard) — Phase 51
       ```
    4. Section `## Architecture` — ajouter (ou créer une sous-section "Vault" si elle n'existe pas) la structure des dossiers vault. Si une mention "Vault recettes" existe déjà, ajouter dans le même bloc :
       ```
       - Vault impressions : `12 - Impressions/PDFs/{id}-{date}.pdf` (PDFs générés) + `12 - Impressions/manifeste.md` (index frontmatter + table 5 colonnes)
       ```
    5. Vérifier qu'aucun nom personnel réel n'est introduit — privacy CLAUDE.md (utiliser uniquement Lucas/Emma/Dupont si exemples nécessaires, ce qui n'est pas le cas ici).
    6. NE PAS toucher aux autres sections (Conventions, Animations, Patterns de code, Testing, Project, Constraints, GSD Workflow, Developer Profile).
  </action>
  <verify>
    <automated>grep -E "expo-print|expo-sharing|qrcode|expo-clipboard|lib/pdf|app/impressions|12 - Impressions" CLAUDE.md | wc -l</automated>
  </verify>
  <done>CLAUDE.md mentionne les 4 nouvelles libs du Stack, le dossier `lib/pdf/`, l'écran `app/impressions.tsx`, et la structure vault `12 - Impressions/`. Aucune autre section modifiée.</done>
</task>

<task type="auto">
  <name>Task 3 : Validation tsc + jest globale</name>
  <files></files>
  <action>
    1. Lancer `npx tsc --noEmit` et capturer la sortie. Filtrer les erreurs pré-existantes documentées dans CLAUDE.md (MemoryEditor.tsx, cooklang.ts, useVault.ts). Toute autre erreur DOIT être corrigée avant de passer à la suite.
    2. Lancer `npx jest --no-coverage` complet. Vérifier :
       - Test state machine BookExportModal (51-01)
       - Test buildVaultPdfUri (51-02)
       - Test i18n impressions namespace (51-03)
       - Tests existants Phase 49 (manifest-parser round-trip, etc.) toujours verts
       - Tests existants Phase 50 (deep link router, QR generator) toujours verts
    3. Si un test échoue, identifier la cause (régression Phase 51 ou test pré-existant fragile). Corriger les régressions ; documenter les flakies dans le SUMMARY si les tests pré-existants sont déjà connus instables.
    4. Lancer un `grep -rE "console\\.(log|warn|error)" components/pdf/ app/impressions.tsx` — vérifier que TOUS les console.* sont sous `if (__DEV__)` (CLAUDE.md convention).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -vE "MemoryEditor\\.tsx|cooklang\\.ts|useVault\\.ts" | grep -E "error TS" | wc -l | grep -q "^0$" && npx jest --no-coverage 2>&1 | tail -5</automated>
  </verify>
  <done>tsc clean (hors pré-existantes), jest --no-coverage complet vert, aucun console.* hors __DEV__.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint humain : test device end-to-end Phase 51</name>
  <what-built>
    Pipeline complet UX export PDF :
    - Écran "Mes impressions" navigable depuis le menu Plus (catégorie Souvenirs)
    - BookExportModal avec sélection histoire → génération → aperçu PDF (Print.printAsync natif iOS)
    - PostExportView 3 actions : Sauvegarder (Share Sheet), Voir, Commander chez Lulu
    - LuluInstructionsModal manuel FR pas-à-pas
    - Manifeste vault auto-mis à jour à chaque export
    - Carry-over Phase 50 : scan QR du PDF généré ouvre l'app + autoplay
  </what-built>
  <how-to-verify>
    1. **Pré-requis** : `npx expo prebuild --clean` + `npx expo run:ios --device` doivent avoir été lancés au moins une fois après l'install de `expo-sharing` (51-03). Si l'app crash au lancement avec "Native module 'ExpoSharing' not found", lancer le rebuild.
    2. **Navigation** : Ouvre l'app → tab "Plus" → catégorie "Souvenirs" → tap "Mes impressions". L'écran doit s'afficher avec soit la liste d'exports passés, soit l'empty state.
    3. **Génération** : Tap "Nouveau livre" → choisis une histoire dans la liste → tap "Générer". Le modal doit afficher la progression (4 étapes) puis la phase "ready" avec récap (Format Lulu 21×21, 16 pages, durée perçue).
    4. **Aperçu** : Tap "Aperçu du PDF". Le QLPreviewController natif iOS doit s'ouvrir avec le livre. Ferme.
    5. **Continuer** : Tap "Continuer" → la phase post-export apparaît avec un haptic Medium (vibration moyenne) + 3 boutons. Vérifie le titre "Et maintenant ?" et le sous-titre.
    6. **Sauvegarder** : Tap "Sauvegarder le PDF". Le Share Sheet iOS doit s'ouvrir (Files, AirDrop, Mail, etc.). Ferme.
    7. **Voir** : Tap "Voir le PDF". QLPreviewController s'ouvre. Ferme.
    8. **Lulu** : Tap "Commander chez Lulu". Le sub-modal doit s'ouvrir avec 5 étapes FR. Tap "Ouvrir Lulu Studio" → safari ouvre `https://www.lulu.com/create/print-books/`. Reviens à l'app.
    9. **Terminé** : Tap "Terminé" → le modal se ferme → retour à l'écran "Mes impressions" avec la nouvelle entrée dans la liste (pull-to-refresh si pas auto).
    10. **Validation manifeste** : Vérifier que `12 - Impressions/manifeste.md` (dans le vault Obsidian iCloud) contient bien la nouvelle entrée avec id/hash/date/format/chemin.
    11. **Carry-over Phase 50 — scan QR** : Ouvre le PDF généré (sur Mac via AirDrop par exemple), zoom sur la page QR, scanne avec l'app Camera iOS sur un autre iPhone (ou imprime + scan papier réel). Le deep link `family-vault://story/<id>` doit ouvrir l'app et déclencher l'autoplay TTS de l'histoire (cf. Phase 50-04).
    12. **Régression** : Vérifie que `app/dev-deep-link.tsx` (en mode `__DEV__`) ne propose PLUS de bouton "PDF" mais conserve les boutons import-note / open/meals / story.

    Si tout est OK → tape "approved". Si un point échoue → décris précisément quel point + comportement observé.
  </how-to-verify>
  <resume-signal>Tape "approved" si tout fonctionne, ou décris le bug exact (ex : "5. haptic absent" / "8. Lulu modal vide" / "10. manifeste pas mis à jour").</resume-signal>
</task>

<task type="auto">
  <name>Task 4 : Rédiger 51-04-SUMMARY.md + 51-PHASE-SUMMARY.md</name>
  <files>.planning/phases/51-ux-export-manuel-lulu/51-04-SUMMARY.md, .planning/phases/51-ux-export-manuel-lulu/51-PHASE-SUMMARY.md</files>
  <action>
    1. Créer `51-04-SUMMARY.md` avec :
       - Cleanup `dev-deep-link.tsx` (lignes supprimées, imports retirés)
       - CLAUDE.md sections modifiées (Stack/Architecture/Vault — résumé des ajouts)
       - Résultats validation tsc/jest (n erreurs pré-existantes ignorées, n tests verts)
       - Résultats checkpoint device (récap user "approved" + éventuels bugs corrigés en cours)
       - Commit : `chore(51-04): cleanup dev-deep-link + docs CLAUDE.md + validation phase`
    2. Créer `51-PHASE-SUMMARY.md` (synthèse globale Phase 51) avec :
       - **Objectif phase** : brancher pipeline PDF Phase 49 + QR Phase 50 dans l'UX
       - **Plans livrés** :
         - 51-01 : BookExportModal state machine + aperçu PDF natif
         - 51-02 : Écran Mes impressions + ExportCard + buildVaultPdfUri + row more.tsx
         - 51-03 : Post-export 3 actions + manuel Lulu FR + i18n impressions
         - 51-04 : Cleanup dev-deep-link + docs CLAUDE.md + validation
       - **Décisions clés** :
         - Aperçu PDF via `Print.printAsync({ uri })` natif (pas de react-native-pdf, pas de WebView base64)
         - Modal pageSheet unique avec state machine 4 phases (pas de nesting Modal)
         - Lulu = lien web + manuel FR (pas d'API Lulu Direct)
         - i18n FR strict, namespace `impressions`
         - Lazy import expo-sharing pour résilience rebuild dev-client
       - **Patterns établis** : state machine reducer pour modals multi-phases ; édit minimal sur fichiers user en cours (more.tsx) ; composants pdf/ memoïsés
       - **Pitfalls évités** : drag-to-dismiss bloqué pendant generating, Platform.OS branch preview iOS/Android, path traversal dans buildVaultPdfUri
       - **Carry-over** : entry-points contextuels (long-press saga, fin génération histoire) et Lulu Direct API restent deferred (CONTEXT.md)
       - **Tests** : state machine BookExportModal + buildVaultPdfUri + i18n namespace = 3 nouveaux fichiers tests Jest verts
       - **Stack ajouté** : expo-sharing ~14.0.x
       - **Files touchés** : ~10 fichiers (cf. files_modified des 4 plans)
       - **Effort réel** : (à remplir post-exécution)
    3. NE PAS commit avant validation finale — laisser au workflow `/ship` ou commit manuel utilisateur.
  </action>
  <verify>
    <automated>ls .planning/phases/51-ux-export-manuel-lulu/51-04-SUMMARY.md .planning/phases/51-ux-export-manuel-lulu/51-PHASE-SUMMARY.md</automated>
  </verify>
  <done>2 SUMMARY créés, contenu structuré et fidèle aux livraisons réelles.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repo → CLAUDE.md (docs) | Doc agent-readable, pas de surface attaque |
| Dev-only screen → prod build | `__DEV__` gate empêche inclusion App Store |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-04-01 | Tampering | docs CLAUDE.md erronées induisent futurs devs en erreur | mitigate | Vérification grep des libs/paths mentionnés (Task 2 verify) |
| T-51-04-02 | Information Disclosure | dev-deep-link en prod | accept | gating `__DEV__` conservé (RESEARCH §8) |
</threat_model>

<verification>
1. `npx tsc --noEmit` clean (hors pré-existantes)
2. `npx jest --no-coverage` complet vert
3. `app/dev-deep-link.tsx` : grep `generateBookPdf|persistBookPdf` → 0 hits
4. `CLAUDE.md` : grep `expo-print|expo-sharing|qrcode|expo-clipboard|lib/pdf|app/impressions|12 - Impressions` → ≥7 hits
5. Checkpoint device approuvé par l'utilisateur (12 points de vérification)
6. SUMMARY 51-04 et 51-PHASE rédigés
</verification>

<success_criteria>
- Cleanup dev-deep-link.tsx fait sans casser tests deep links Phase 50
- CLAUDE.md à jour (Stack + Architecture + Vault)
- tsc + jest verts globalement
- Test device e2e validé par l'utilisateur (12 points de vérif)
- Manifeste mis à jour automatiquement vérifié
- QR scan Phase 50 toujours fonctionnel (non-régression)
- 2 SUMMARY rédigés
</success_criteria>

<output>
Après complétion :
- `.planning/phases/51-ux-export-manuel-lulu/51-04-SUMMARY.md`
- `.planning/phases/51-ux-export-manuel-lulu/51-PHASE-SUMMARY.md`
- Commit suggéré : `chore(51-04): cleanup dev-deep-link + docs CLAUDE.md + validation phase`
- Préparer le ship via `/ship` (tsc + privacy check + commit FR + push)
</output>
