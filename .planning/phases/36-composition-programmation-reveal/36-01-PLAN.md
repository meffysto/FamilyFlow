---
phase: 36-composition-programmation-reveal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/lovenotes/reveal-engine.ts
  - lib/lovenotes/index.ts
  - lib/scheduled-notifications.ts
  - hooks/useRevealOnForeground.ts
  - hooks/useVaultLoveNotes.ts
  - hooks/useVault.ts
autonomous: true
requirements: [LOVE-11, LOVE-12]
must_haves:
  truths:
    - "User dispose de presets reveal pure JS testables (Demain matin / Dimanche soir / Dans 1 mois)"
    - "User voit une notif locale silencieuse planifiée au revealAt via scheduleLoveNoteReveal — idempotente (cancel-then-schedule)"
    - "User voit les loveNotes pending due basculer en revealed au foreground via useRevealOnForeground hook"
    - "addLoveNote retourne le sourceFile résolu (Promise<string>) — source unique de vérité, plus de duplication de loveNotePath côté écran"
  artifacts:
    - path: "lib/lovenotes/reveal-engine.ts"
      provides: "presetTomorrowMorning / presetNextSundayEvening / presetInOneMonth + localIso helper"
      exports: ["presetTomorrowMorning", "presetNextSundayEvening", "presetInOneMonth", "localIso"]
    - path: "lib/scheduled-notifications.ts"
      provides: "scheduleLoveNoteReveal + cancelLoveNoteReveal (extension)"
      contains: "scheduleLoveNoteReveal"
    - path: "hooks/useRevealOnForeground.ts"
      provides: "Hook AppState bascule pending → revealed (mount + foreground)"
      exports: ["useRevealOnForeground"]
    - path: "lib/lovenotes/index.ts"
      provides: "Barrel — réexporte selectors + reveal-engine"
    - path: "hooks/useVaultLoveNotes.ts"
      provides: "addLoveNote signature étendue Promise<string> (retourne sourceFile)"
      contains: "Promise<string>"
  key_links:
    - from: "lib/scheduled-notifications.ts:scheduleLoveNoteReveal"
      to: "expo-notifications"
      via: "Notifications.scheduleNotificationAsync({ trigger:{ type:DATE, date }})"
      pattern: "scheduleNotificationAsync"
    - from: "hooks/useRevealOnForeground.ts"
      to: "lib/lovenotes/selectors.ts:isRevealed"
      via: "import isRevealed"
      pattern: "isRevealed\\("
    - from: "hooks/useVaultLoveNotes.ts:addLoveNote"
      to: "Plan 03 handleSave consomme sourceFile retourné"
      via: "Promise<string> au lieu de Promise<void>"
      pattern: "addLoveNote.*Promise<string>"
---

<objective>
Livrer les fondations pures consommées par les Plans 02-04 : presets reveal JS pur, extension scheduleLoveNoteReveal/cancelLoveNoteReveal idempotente dans `lib/scheduled-notifications.ts`, hook AppState `useRevealOnForeground`, ET modification de la signature `addLoveNote` pour retourner `Promise<string>` (sourceFile résolu — source unique de vérité, élimine la reconstruction fragile côté Plan 03).

Purpose : zéro UI ici — fonctions pures + hook réutilisables + contrat addLoveNote stabilisé. Les Plans 02-04 consomment cette API stable.
Output : 1 fichier neuf reveal-engine.ts, 1 hook neuf, extension scheduled-notifications.ts, mise à jour barrel, signature addLoveNote durcie.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-composition-programmation-reveal/36-RESEARCH.md
@lib/lovenotes/selectors.ts
@hooks/useVaultLoveNotes.ts
@hooks/useVault.ts
@lib/scheduled-notifications.ts
@lib/types.ts
@lib/parser.ts

<interfaces>
From hooks/useVaultLoveNotes.ts (signature ACTUELLE — à modifier en Task 4) :
```typescript
addLoveNote: (note: Omit<LoveNote, 'sourceFile'>) => Promise<void>;             // AVANT
updateLoveNoteStatus: (sourceFile: string, status: LoveNoteStatus, readAt?: string) => Promise<void>;
```

From hooks/useVaultLoveNotes.ts (signature CIBLE après Task 4) :
```typescript
addLoveNote: (note: Omit<LoveNote, 'sourceFile'>) => Promise<string>;           // APRÈS — retourne sourceFile
```

From hooks/useVault.ts:290 (re-export interface VaultContextValue) — à mettre à jour en Task 4.

From lib/parser.ts:2779 :
```typescript
export function loveNotePath(toProfileId: string, createdAt: string): string;
```

From lib/lovenotes/selectors.ts :
```typescript
export function isRevealed(note: LoveNote, now?: Date): boolean;
```

From lib/types.ts (LoveNote shape) :
```typescript
interface LoveNote {
  sourceFile: string;
  from: string;
  to: string;
  createdAt: string;   // 'YYYY-MM-DDTHH:mm:ss' local sans Z
  revealAt: string;    // idem
  status: 'pending' | 'revealed' | 'read';
  readAt?: string;
  body: string;
}
```

From lib/scheduled-notifications.ts :
```typescript
export function configureNotifications(): void;
export async function requestNotificationPermissions(): Promise<boolean>;
// Pattern existant cancelByCategory(prefix) lignes 147-154
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Créer lib/lovenotes/reveal-engine.ts (presets purs + helper localIso)</name>
  <files>lib/lovenotes/reveal-engine.ts, lib/lovenotes/index.ts</files>
  <read_first>
    - .planning/phases/36-composition-programmation-reveal/36-RESEARCH.md (Pattern 2, lignes 252-282)
    - lib/lovenotes/selectors.ts (style commentaires + pattern pad helper ligne 25)
    - lib/types.ts (vérifier shape LoveNote.revealAt format ISO local sans Z)
  </read_first>
  <action>
    Créer `lib/lovenotes/reveal-engine.ts` avec exactement ces exports (signatures strictes) :

    ```typescript
    /**
     * lib/lovenotes/reveal-engine.ts — Presets reveal pure JS + helper localIso
     * Phase 36 Plan 01. Fonctions pures testables (now injectable).
     * Format LoveNote.revealAt : 'YYYY-MM-DDTHH:mm:ss' heure locale sans Z (cf. types.ts:585).
     */

    function pad(n: number): string { return String(n).padStart(2, '0'); }

    /** Convertit une Date en ISO local sans Z (heure de l'appareil). */
    export function localIso(d: Date): string {
      return `${d.getFullYear()}-${pad(d.getMonth()+1)}-${pad(d.getDate())}` +
        `T${pad(d.getHours())}:${pad(d.getMinutes())}:${pad(d.getSeconds())}`;
    }

    /** Demain matin 08:00. */
    export function presetTomorrowMorning(now: Date = new Date()): { date: string; time: string } {
      const d = new Date(now);
      d.setDate(d.getDate() + 1);
      d.setHours(8, 0, 0, 0);
      return { date: localIso(d).slice(0, 10), time: '08:00' };
    }

    /** Prochain dimanche 19:00 (si on est dimanche → dimanche suivant). */
    export function presetNextSundayEvening(now: Date = new Date()): { date: string; time: string } {
      const d = new Date(now);
      const day = d.getDay();                   // 0=Sun..6=Sat
      const daysUntilSun = (7 - day) % 7 || 7;  // toujours futur
      d.setDate(d.getDate() + daysUntilSun);
      d.setHours(19, 0, 0, 0);
      return { date: localIso(d).slice(0, 10), time: '19:00' };
    }

    /** Aujourd'hui +1 mois 09:00 (Date.setMonth gère overflow). */
    export function presetInOneMonth(now: Date = new Date()): { date: string; time: string } {
      const d = new Date(now);
      d.setMonth(d.getMonth() + 1);
      d.setHours(9, 0, 0, 0);
      return { date: localIso(d).slice(0, 10), time: '09:00' };
    }
    ```

    Mettre à jour `lib/lovenotes/index.ts` pour réexporter :
    ```typescript
    export * from './selectors';
    export * from './reveal-engine';
    ```

    NE PAS importer date-fns. JS pur strict.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep "reveal-engine\|lovenotes/index" | grep -v "MemoryEditor\|cooklang\|useVault" ; test $? -eq 1</automated>
  </verify>
  <done>
    - `lib/lovenotes/reveal-engine.ts` exporte `localIso`, `presetTomorrowMorning`, `presetNextSundayEvening`, `presetInOneMonth`
    - `lib/lovenotes/index.ts` réexporte les deux modules (selectors + reveal-engine)
    - Aucune dépendance externe (pas de date-fns import)
    - `grep -c "export function preset" lib/lovenotes/reveal-engine.ts` retourne 3
    - `npx tsc --noEmit` clean (hors erreurs pré-existantes documentées CLAUDE.md)
  </done>
</task>

<task type="auto">
  <name>Task 2: Étendre lib/scheduled-notifications.ts avec scheduleLoveNoteReveal + cancelLoveNoteReveal</name>
  <files>lib/scheduled-notifications.ts</files>
  <read_first>
    - .planning/phases/36-composition-programmation-reveal/36-RESEARCH.md (Pattern 3, lignes 286-331 + Pitfall 3 + Pitfall 4)
    - lib/scheduled-notifications.ts:100-154 (catégories existantes + helper cancelByCategory + requestNotificationPermissions)
    - lib/types.ts (LoveNote interface — pour import type)
  </read_first>
  <action>
    Ajouter dans `lib/scheduled-notifications.ts` :

    1. Import en tête du fichier (à côté des imports existants RDV/Task/StockItem) :
    ```typescript
    import type { LoveNote } from './types';
    ```

    2. Ajouter constante catégorie après les autres CAT_* (vers ligne 106) :
    ```typescript
    const CAT_LOVENOTE = 'lovenote-reveal';
    ```

    3. Ajouter à la fin du fichier (avant le dernier `}` ou en bas après `setupAllNotifications`) :

    ```typescript
    // ─── 8. Love Notes — reveal au revealAt (Phase 36) ──────────────────────────

    function loveNoteIdentifier(sourceFile: string): string {
      // Sanitize / et . pour identifier sûr
      return `${CAT_LOVENOTE}-${sourceFile.replace(/[/.]/g, '_')}`;
    }

    /**
     * Programme une notification locale silencieuse au revealAt de la note.
     * Idempotent : cancel précédente avant schedule.
     * Returns false si permission refusée OU revealAt déjà passé (Pitfall 3).
     */
    export async function scheduleLoveNoteReveal(note: LoveNote): Promise<boolean> {
      const permitted = await requestNotificationPermissions();
      if (!permitted) return false;

      // Cancel précédente (idempotence)
      await cancelLoveNoteReveal(note.sourceFile);

      // Parse revealAt en heure locale (ISO sans Z, cf types.ts:585)
      const [datePart, timePart] = note.revealAt.split('T');
      const [y, m, d] = datePart.split('-').map(Number);
      const [hh, mm, ss] = (timePart ?? '00:00:00').split(':').map(Number);
      const revealDate = new Date(y, m - 1, d, hh, mm, ss);

      if (revealDate.getTime() <= Date.now()) return false; // Pitfall 3

      try {
        await Notifications.scheduleNotificationAsync({
          identifier: loveNoteIdentifier(note.sourceFile),
          content: {
            title: '💌 Nouvelle love note',
            body: 'Une note vient d\'arriver dans ta boîte aux lettres',
            sound: false,
            data: { route: '/(tabs)/lovenotes', sourceFile: note.sourceFile },
          },
          trigger: { type: Notifications.SchedulableTriggerInputTypes.DATE, date: revealDate },
        });
        return true;
      } catch (e) {
        if (__DEV__) console.warn('[scheduleLoveNoteReveal]', e);
        return false;
      }
    }

    /** Annule la notif programmée pour cette love note. Idempotent (no-op si absente). */
    export async function cancelLoveNoteReveal(sourceFile: string): Promise<void> {
      try {
        await Notifications.cancelScheduledNotificationAsync(loveNoteIdentifier(sourceFile));
      } catch {
        // Idempotent
      }
    }
    ```

    NE PAS modifier `setupAllNotifications` ni les autres scheduleXxx existants.
    NE PAS bumper CACHE_VERSION (per RESEARCH user_constraints).
  </action>
  <verify>
    <automated>grep -c "export async function scheduleLoveNoteReveal" lib/scheduled-notifications.ts && grep -c "export async function cancelLoveNoteReveal" lib/scheduled-notifications.ts && npx tsc --noEmit 2>&1 | grep "scheduled-notifications" | grep -v "MemoryEditor\|cooklang\|useVault" ; test $? -eq 1</automated>
  </verify>
  <done>
    - `scheduleLoveNoteReveal(note: LoveNote): Promise<boolean>` exporté
    - `cancelLoveNoteReveal(sourceFile: string): Promise<void>` exporté
    - `scheduleLoveNoteReveal` appelle `cancelLoveNoteReveal` avant `scheduleNotificationAsync` (idempotence)
    - Return `false` si revealDate <= now ou permission refusée
    - `content.sound: false`, `content.data.route: '/(tabs)/lovenotes'`, `data.sourceFile: note.sourceFile`
    - Trigger : `{ type: Notifications.SchedulableTriggerInputTypes.DATE, date: revealDate }`
    - `npx tsc --noEmit` clean (hors erreurs pré-existantes)
  </done>
</task>

<task type="auto">
  <name>Task 3: Créer hooks/useRevealOnForeground.ts</name>
  <files>hooks/useRevealOnForeground.ts</files>
  <read_first>
    - .planning/phases/36-composition-programmation-reveal/36-RESEARCH.md (Pattern 5, lignes 358-396)
    - lib/lovenotes/selectors.ts (signature isRevealed)
    - hooks/useVault.ts:710-720 (pattern AppState.addEventListener avec cleanup pour référence)
  </read_first>
  <action>
    Créer `hooks/useRevealOnForeground.ts` :

    ```typescript
    /**
     * useRevealOnForeground.ts — Bascule auto pending → revealed (Phase 36 Plan 01)
     *
     * Au mount + à chaque retour app foreground, scanne loveNotes :
     * pour chaque note `pending` dont revealAt est passé (filet de sécurité
     * complémentaire à la notif), appelle updateStatus(sourceFile, 'revealed').
     *
     * Idempotent : updateLoveNoteStatus relit le fichier avant patch (Phase 34).
     * Brancher UNIQUEMENT dans app/(tabs)/lovenotes.tsx (per RESEARCH Open Q 3).
     */

    import { useEffect, useCallback } from 'react';
    import { AppState, AppStateStatus } from 'react-native';
    import type { LoveNote, LoveNoteStatus } from '../lib/types';
    import { isRevealed } from '../lib/lovenotes/selectors';

    export function useRevealOnForeground(
      loveNotes: LoveNote[],
      updateStatus: (sourceFile: string, status: LoveNoteStatus, readAt?: string) => Promise<void>,
    ): void {
      const reveal = useCallback(async () => {
        const now = new Date();
        for (const n of loveNotes) {
          if (n.status === 'pending' && isRevealed(n, now)) {
            try {
              await updateStatus(n.sourceFile, 'revealed');
            } catch (e) {
              if (__DEV__) console.warn('[useRevealOnForeground]', e);
            }
          }
        }
      }, [loveNotes, updateStatus]);

      useEffect(() => {
        // Mount : reveal immédiat
        reveal();
        // Foreground : reveal au retour
        const sub = AppState.addEventListener('change', (state: AppStateStatus) => {
          if (state === 'active') reveal();
        });
        return () => sub.remove();
      }, [reveal]);
    }
    ```

    Le hook est branché par le Plan 03 dans `app/(tabs)/lovenotes.tsx` — cette tâche livre uniquement le hook.
  </action>
  <verify>
    <automated>grep -c "export function useRevealOnForeground" hooks/useRevealOnForeground.ts && grep -c "AppState.addEventListener" hooks/useRevealOnForeground.ts && grep -c "sub.remove" hooks/useRevealOnForeground.ts && npx tsc --noEmit 2>&1 | grep "useRevealOnForeground" | grep -v "MemoryEditor\|cooklang\|useVault" ; test $? -eq 1</automated>
  </verify>
  <done>
    - `hooks/useRevealOnForeground.ts` exporte `useRevealOnForeground(loveNotes, updateStatus): void`
    - useEffect appelle `reveal()` au mount ET au passage foreground
    - Cleanup `sub.remove()` au unmount
    - Filtre `n.status === 'pending' && isRevealed(n, now)` strict
    - `console.warn` sous `if (__DEV__)`
    - `npx tsc --noEmit` clean (hors pré-existantes)
  </done>
</task>

<task type="auto">
  <name>Task 4: Modifier signature addLoveNote → Promise<string> (retourne sourceFile résolu)</name>
  <files>hooks/useVaultLoveNotes.ts, hooks/useVault.ts</files>
  <read_first>
    - hooks/useVaultLoveNotes.ts:34-42 (interface UseVaultLoveNotesResult)
    - hooks/useVaultLoveNotes.ts:76-85 (impl addLoveNote actuelle — calcule déjà relPath)
    - hooks/useVault.ts:285-295 (interface VaultContextValue qui re-expose addLoveNote)
    - hooks/useVault.ts:1865-1870 (re-exposition concrète depuis loveNotesHook)
  </read_first>
  <action>
    **Pourquoi cette modif** : Le Plan 03 (handleSave dans lovenotes.tsx) doit appeler `scheduleLoveNoteReveal({...note, sourceFile})`. Sans cette modif, le Plan 03 reconstruit `sourceFile` via `loveNotePath(to, createdAt)` côté écran — duplication fragile : si la formule de chemin change dans `addLoveNote`, divergence silencieuse + collision possible. **Solution** : `addLoveNote` retourne le `sourceFile` qu'il vient d'écrire — source unique de vérité.

    1. **Modifier `hooks/useVaultLoveNotes.ts`** :

    Interface (ligne 38) :
    ```typescript
    // AVANT
    addLoveNote: (note: Omit<LoveNote, 'sourceFile'>) => Promise<void>;
    // APRÈS
    addLoveNote: (note: Omit<LoveNote, 'sourceFile'>) => Promise<string>;
    ```

    Implémentation (lignes 76-85) — ajouter `return relPath;` :
    ```typescript
    const addLoveNote = useCallback(async (note: Omit<LoveNote, 'sourceFile'>): Promise<string> => {
      if (!vaultRef.current) throw new Error('Vault non initialisé');
      const dir = `${LOVENOTES_DIR}/${note.to}`;
      await vaultRef.current.ensureDir(dir);
      const relPath = loveNotePath(note.to, note.createdAt);
      await vaultRef.current.writeFile(relPath, serializeLoveNote(note));
      const exists = await vaultRef.current.exists(relPath);
      if (!exists) throw new Error("Echec de l'ecriture de la love note");
      setLoveNotes((prev) => [{ ...note, sourceFile: relPath }, ...prev]);
      return relPath;                                           // ← AJOUT
    }, [vaultRef]);
    ```

    NOTE : remplacer `if (!vaultRef.current) return;` par `throw new Error(...)` pour rester typé `Promise<string>` (pas `Promise<string | undefined>`). Cas pathologique acceptable — vault toujours init quand l'éditeur est ouvert (UI bloquée par activeProfile).

    2. **Modifier `hooks/useVault.ts`** :

    Si l'interface `VaultContextValue` redéclare explicitement le type d'`addLoveNote` (ligne 290) :
    ```typescript
    // AVANT
    addLoveNote: (note: Omit<LoveNote, 'sourceFile'>) => Promise<void>;
    // APRÈS
    addLoveNote: (note: Omit<LoveNote, 'sourceFile'>) => Promise<string>;
    ```

    La re-exposition ligne 1867 (`addLoveNote: loveNotesHook.addLoveNote`) ne nécessite aucun changement — TypeScript propagera le nouveau type.

    3. **Vérifier consommateurs existants** : `grep -rn "addLoveNote(" --include="*.tsx" --include="*.ts"` — il NE doit PAS y avoir de consommateur actuel qui ferait `await addLoveNote(...)` puis traiterait le retour comme `void`. Phase 34 a livré le hook mais pas de UI consommatrice — Plan 03 sera le premier consommateur. Si grep révèle un consommateur (ex : un test), s'assurer qu'il accepte le nouveau retour string.
  </action>
  <verify>
    <automated>grep -c "addLoveNote.*Promise<string>" hooks/useVaultLoveNotes.ts && grep -c "return relPath" hooks/useVaultLoveNotes.ts && grep -c "addLoveNote.*Promise<string>" hooks/useVault.ts && npx tsc --noEmit 2>&1 | grep -v "MemoryEditor\|cooklang\|useVault.ts" | grep -E "addLoveNote|useVaultLoveNotes" ; test $? -eq 1</automated>
  </verify>
  <done>
    - `hooks/useVaultLoveNotes.ts` interface `addLoveNote: ... => Promise<string>`
    - Implémentation retourne `relPath` après écriture confirmée
    - Garde `if (!vaultRef.current)` lève une exception (au lieu de return silencieux) pour rester typé strict
    - `hooks/useVault.ts` re-exporte le nouveau type (interface VaultContextValue mise à jour si typage explicite)
    - `npx tsc --noEmit` clean (hors pré-existantes documentées CLAUDE.md)
    - Aucun consommateur existant cassé (grep `addLoveNote(` retourne uniquement la définition + le futur Plan 03)
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` clean (hors erreurs pré-existantes MemoryEditor.tsx / cooklang.ts / useVault.ts documentées CLAUDE.md)
- 6 fichiers livrés : `lib/lovenotes/reveal-engine.ts` (neuf), `lib/lovenotes/index.ts` (modif), `lib/scheduled-notifications.ts` (étendu), `hooks/useRevealOnForeground.ts` (neuf), `hooks/useVaultLoveNotes.ts` (modif signature), `hooks/useVault.ts` (modif typage re-export)
- Aucune nouvelle dépendance npm
- Pas d'UI touchée — fondations consommées par Plans 02/03/04
- `addLoveNote` retourne désormais le `sourceFile` — Plan 03 peut consommer sans dupliquer `loveNotePath`
</verification>

<success_criteria>
- 3 fonctions presets pures + 1 helper localIso exportés
- `scheduleLoveNoteReveal` + `cancelLoveNoteReveal` exportés et idempotents
- `useRevealOnForeground` hook avec cleanup AppState
- Barrel `lib/lovenotes/index.ts` à jour
- `addLoveNote` retourne `Promise<string>` — contrat propagé via `useVault()`
- TypeScript clean
</success_criteria>

<output>
Créer `.planning/phases/36-composition-programmation-reveal/36-01-SUMMARY.md` après complétion.
</output>
