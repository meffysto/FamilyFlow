---
phase: 36-composition-programmation-reveal
plan: 03
type: execute
wave: 2
depends_on: [36-01]
files_modified:
  - components/lovenotes/LoveNoteEditor.tsx
  - components/lovenotes/index.ts
  - app/(tabs)/lovenotes.tsx
autonomous: true
requirements: [LOVE-09, LOVE-10, LOVE-12]
must_haves:
  truths:
    - "User peut tap un FAB '✏️ Écrire' dans /lovenotes pour ouvrir le LoveNoteEditor (Modal pageSheet drag-to-dismiss)"
    - "User sélectionne un destinataire via Chips (excluant l'auteur), un body markdown avec preview toggle, une date+heure de révélation"
    - "User dispose de 4 chips presets (Demain matin / Dimanche soir / Dans 1 mois / Custom) qui pré-remplissent date+time sans bloquer le custom"
    - "User voit l'éditeur pré-rempli avec preset 'Demain matin' au mount (per RESEARCH Open Q 5)"
    - "User voit la note créée + notif planifiée + bascule pending due → revealed au foreground via useRevealOnForeground branché dans lovenotes.tsx"
  artifacts:
    - path: "components/lovenotes/LoveNoteEditor.tsx"
      provides: "Modal éditeur composition love note"
      exports: ["LoveNoteEditor"]
      min_lines: 180
    - path: "app/(tabs)/lovenotes.tsx"
      provides: "FAB Écrire + LoveNoteEditor wiring + useRevealOnForeground branché"
      contains: "LoveNoteEditor"
  key_links:
    - from: "LoveNoteEditor handleSave"
      to: "addLoveNote + scheduleLoveNoteReveal"
      via: "props.onSave appelé puis scheduleLoveNoteReveal côté écran"
      pattern: "addLoveNote.*scheduleLoveNoteReveal|scheduleLoveNoteReveal.*addLoveNote"
    - from: "app/(tabs)/lovenotes.tsx"
      to: "useRevealOnForeground"
      via: "useRevealOnForeground(loveNotes, updateLoveNoteStatus)"
      pattern: "useRevealOnForeground\\("
    - from: "LoveNoteEditor presets chips"
      to: "lib/lovenotes/reveal-engine"
      via: "presetTomorrowMorning/presetNextSundayEvening/presetInOneMonth"
      pattern: "preset(TomorrowMorning|NextSundayEvening|InOneMonth)"
---

<objective>
Livrer le `LoveNoteEditor` modal `pageSheet` (composition complète) + le FAB "✏️ Écrire" qui l'ouvre depuis l'écran boîte aux lettres + brancher le hook `useRevealOnForeground` (LOVE-12). Câbler `addLoveNote` + `scheduleLoveNoteReveal` au save flow.

Purpose : permettre à l'utilisateur d'écrire et programmer une love note. Boucle complète composition → schedule → bascule auto.
Output : 1 composant neuf (`LoveNoteEditor.tsx`), barrel mis à jour, écran lovenotes étendu (FAB + state editor + handlers + hook foreground).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-composition-programmation-reveal/36-RESEARCH.md
@.planning/phases/36-composition-programmation-reveal/36-01-PLAN.md
@app/(tabs)/lovenotes.tsx
@hooks/useVaultLoveNotes.ts
@components/ui/Chip.tsx
@components/ui/MarkdownText.tsx
@components/ui/DateInput.tsx
@components/ui/ModalHeader.tsx
@components/ui/Button.tsx
@components/NoteEditor.tsx

<interfaces>
From hooks/useVaultLoveNotes.ts (déjà câblé via useVault) :
```typescript
addLoveNote: (note: Omit<LoveNote, 'sourceFile'>) => Promise<void>;
updateLoveNoteStatus: (sourceFile: string, status: LoveNoteStatus, readAt?: string) => Promise<void>;
```

From useVault() :
```typescript
const { loveNotes, addLoveNote, updateLoveNoteStatus, activeProfile, profiles } = useVault();
```

From lib/lovenotes/reveal-engine (Plan 01) :
```typescript
export function localIso(d: Date): string;
export function presetTomorrowMorning(now?: Date): { date: string; time: string };
export function presetNextSundayEvening(now?: Date): { date: string; time: string };
export function presetInOneMonth(now?: Date): { date: string; time: string };
```

From lib/scheduled-notifications (Plan 01) :
```typescript
export async function scheduleLoveNoteReveal(note: LoveNote): Promise<boolean>;
```

From hooks/useRevealOnForeground (Plan 01) :
```typescript
export function useRevealOnForeground(loveNotes, updateStatus): void;
```

From lib/parser.ts :
```typescript
export function loveNotePath(toProfileId: string, createdAt: string): string;
```

From components/ui :
```typescript
export { Chip, MarkdownText, DateInput, ModalHeader, Button } from './...';
// Chip props: { label, emoji?, color?, selected?, onPress }
// MarkdownText props: { children: string, style? }
// DateInput props: { value: string, onChange: (iso) => void, mode?: 'date'|'time', placeholder? }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Créer components/lovenotes/LoveNoteEditor.tsx (modal pageSheet)</name>
  <files>components/lovenotes/LoveNoteEditor.tsx, components/lovenotes/index.ts</files>
  <read_first>
    - .planning/phases/36-composition-programmation-reveal/36-RESEARCH.md (Pattern 1 lignes 196-249, Code Examples lignes 580-617, Pitfalls 8/9)
    - components/NoteEditor.tsx (référence pattern Modal pageSheet + state pattern)
    - components/ui/Chip.tsx (vérifier signature exacte des props)
    - components/ui/DateInput.tsx (vérifier mode 'date'/'time')
    - components/ui/MarkdownText.tsx (vérifier props children)
    - lib/types.ts (Profile shape : id, name, avatar, etc.)
  </read_first>
  <action>
    Créer `components/lovenotes/LoveNoteEditor.tsx` avec cette structure stricte :

    ```typescript
    /**
     * LoveNoteEditor.tsx — Modal composition love note (Phase 36 Plan 03)
     *
     * Modal presentationStyle="pageSheet" (drag-to-dismiss iOS natif) :
     * - Chips destinataire (filtre exclut auteur — Pitfall 9)
     * - TextInput body markdown + toggle preview MarkdownText
     * - Chips presets (Demain matin / Dimanche soir / Dans 1 mois / Custom) + DateInput date + DateInput time
     * - Pré-rempli avec preset "Demain matin" au mount (RESEARCH Open Q 5)
     * - Validation : destinataire non vide, body trim non vide, revealAt > now+60s
     */

    import React, { useState, useCallback, useMemo, useEffect } from 'react';
    import {
      Modal, View, Text, TextInput, ScrollView, KeyboardAvoidingView, Platform,
      Alert, StyleSheet, Pressable
    } from 'react-native';
    import * as Haptics from 'expo-haptics';

    import { useThemeColors } from '../../contexts/ThemeContext';
    import { ModalHeader, Chip, MarkdownText, DateInput, Button } from '../ui';
    import { Spacing } from '../../constants/spacing';
    import { FontSize, FontWeight } from '../../constants/typography';
    import {
      presetTomorrowMorning,
      presetNextSundayEvening,
      presetInOneMonth,
    } from '../../lib/lovenotes/reveal-engine';
    import type { Profile } from '../../lib/types';

    type PresetKey = 'tomorrow' | 'sunday' | 'month' | 'custom';

    interface LoveNoteEditorProps {
      visible: boolean;
      fromProfile: Profile;
      recipientProfiles: Profile[]; // déjà filtrés (excluding author)
      onSave: (to: string, body: string, revealAt: string) => Promise<void>;
      onClose: () => void;
    }

    export function LoveNoteEditor({
      visible, fromProfile, recipientProfiles, onSave, onClose,
    }: LoveNoteEditorProps) {
      const { colors } = useThemeColors();
      const [to, setTo] = useState<string>('');
      const [body, setBody] = useState('');
      const [revealDate, setRevealDate] = useState('');
      const [revealTime, setRevealTime] = useState('08:00');
      const [activePreset, setActivePreset] = useState<PresetKey>('tomorrow');
      const [showPreview, setShowPreview] = useState(false);
      const [saving, setSaving] = useState(false);

      // Reset state au mount du Modal (Anti-pattern : ouvrir sans clearer)
      useEffect(() => {
        if (!visible) return;
        const tm = presetTomorrowMorning();
        setTo('');
        setBody('');
        setRevealDate(tm.date);
        setRevealTime(tm.time);
        setActivePreset('tomorrow');
        setShowPreview(false);
        setSaving(false);
      }, [visible]);

      const applyPreset = useCallback((key: PresetKey) => {
        Haptics.selectionAsync().catch(() => {});
        setActivePreset(key);
        if (key === 'custom') return;
        const p =
          key === 'tomorrow' ? presetTomorrowMorning() :
          key === 'sunday'   ? presetNextSundayEvening() :
                               presetInOneMonth();
        setRevealDate(p.date);
        setRevealTime(p.time);
      }, []);

      const onDateChange = useCallback((iso: string) => {
        setRevealDate(iso);
        setActivePreset('custom');
      }, []);
      const onTimeChange = useCallback((iso: string) => {
        // DateInput mode='time' renvoie 'HH:MM' (cf DateInput.tsx toHHMM helper)
        setRevealTime(iso);
        setActivePreset('custom');
      }, []);

      const handleSave = useCallback(async () => {
        if (!to) { Alert.alert('Choisis un destinataire'); return; }
        if (!body.trim()) { Alert.alert('Le message ne peut pas être vide'); return; }
        if (!revealDate) { Alert.alert('Choisis une date de révélation'); return; }

        const revealAt = `${revealDate}T${revealTime}:00`;
        const [y, m, d] = revealDate.split('-').map(Number);
        const [hh, mm] = revealTime.split(':').map(Number);
        const revealDt = new Date(y, m - 1, d, hh, mm, 0);
        if (revealDt.getTime() <= Date.now() + 60_000) {
          Alert.alert('La date de révélation doit être dans le futur');
          return;
        }

        setSaving(true);
        try {
          await onSave(to, body.trim(), revealAt);
          Haptics.notificationAsync(Haptics.NotificationFeedbackType.Success).catch(() => {});
          onClose();
        } catch (e) {
          Alert.alert('Erreur', 'Impossible de programmer la note');
          if (__DEV__) console.error('[LoveNoteEditor] save', e);
        } finally {
          setSaving(false);
        }
      }, [to, body, revealDate, revealTime, onSave, onClose]);

      // Garde — famille à 1 profil
      const noRecipients = recipientProfiles.length === 0;

      return (
        <Modal
          visible={visible}
          presentationStyle="pageSheet"
          animationType="slide"
          onRequestClose={onClose}
        >
          <KeyboardAvoidingView
            behavior={Platform.OS === 'ios' ? 'padding' : undefined}
            style={{ flex: 1, backgroundColor: colors.bg }}
          >
            <ModalHeader title="Nouvelle love note" onClose={onClose} />
            <ScrollView contentContainerStyle={{ padding: Spacing['2xl'], gap: Spacing['2xl'] }}>

              {/* Destinataire */}
              <View>
                <Text style={[styles.label, { color: colors.textMuted }]}>Pour qui ?</Text>
                {noRecipients ? (
                  <Text style={{ color: colors.textMuted, marginTop: Spacing.md }}>
                    Tu es seul·e dans ta famille — invite un proche pour utiliser les Love Notes.
                  </Text>
                ) : (
                  <View style={styles.chipsRow}>
                    {recipientProfiles.map((p) => (
                      <Chip
                        key={p.id}
                        label={p.name}
                        emoji={p.avatar}
                        selected={to === p.id}
                        onPress={() => { Haptics.selectionAsync().catch(() => {}); setTo(p.id); }}
                      />
                    ))}
                  </View>
                )}
              </View>

              {/* Body + preview toggle */}
              <View>
                <View style={styles.bodyHeader}>
                  <Text style={[styles.label, { color: colors.textMuted }]}>Message</Text>
                  <Pressable onPress={() => setShowPreview((v) => !v)}>
                    <Text style={{ color: colors.tint, fontSize: FontSize.sm }}>
                      {showPreview ? 'Éditer' : 'Aperçu'}
                    </Text>
                  </Pressable>
                </View>
                {showPreview ? (
                  <View style={[styles.preview, { backgroundColor: colors.cardAlt, borderColor: colors.border }]}>
                    <MarkdownText>{body || '_Aperçu vide_'}</MarkdownText>
                  </View>
                ) : (
                  <TextInput
                    multiline
                    value={body}
                    onChangeText={setBody}
                    placeholder="Ton message... (markdown supporté : **gras**, *italique*)"
                    placeholderTextColor={colors.textMuted}
                    style={[styles.textInput, { color: colors.text, backgroundColor: colors.cardAlt, borderColor: colors.border }]}
                  />
                )}
              </View>

              {/* Presets reveal */}
              <View>
                <Text style={[styles.label, { color: colors.textMuted }]}>Révéler quand ?</Text>
                <View style={styles.chipsRow}>
                  <Chip label="Demain matin" emoji="🌅" selected={activePreset === 'tomorrow'} onPress={() => applyPreset('tomorrow')} />
                  <Chip label="Dimanche soir" emoji="🌙" selected={activePreset === 'sunday'} onPress={() => applyPreset('sunday')} />
                  <Chip label="Dans 1 mois" emoji="📅" selected={activePreset === 'month'} onPress={() => applyPreset('month')} />
                  <Chip label="Custom" emoji="✏️" selected={activePreset === 'custom'} onPress={() => applyPreset('custom')} />
                </View>
                <View style={styles.dateRow}>
                  <View style={{ flex: 1 }}>
                    <DateInput value={revealDate} onChange={onDateChange} mode="date" placeholder="JJ/MM/AAAA" />
                  </View>
                  <View style={{ flex: 1 }}>
                    <DateInput value={revealTime} onChange={onTimeChange} mode="time" placeholder="HH:MM" />
                  </View>
                </View>
              </View>

              <Button
                label={saving ? 'Programmation...' : 'Programmer la note'}
                onPress={handleSave}
                disabled={saving || noRecipients}
              />
            </ScrollView>
          </KeyboardAvoidingView>
        </Modal>
      );
    }

    const styles = StyleSheet.create({
      label: { fontSize: FontSize.sm, fontWeight: FontWeight.semibold as any, marginBottom: Spacing.md },
      chipsRow: { flexDirection: 'row', flexWrap: 'wrap', gap: Spacing.md },
      bodyHeader: { flexDirection: 'row', justifyContent: 'space-between', alignItems: 'center', marginBottom: Spacing.md },
      textInput: {
        borderWidth: StyleSheet.hairlineWidth, borderRadius: 12,
        padding: Spacing.xl, minHeight: 140, textAlignVertical: 'top', fontSize: FontSize.body,
      },
      preview: {
        borderWidth: StyleSheet.hairlineWidth, borderRadius: 12,
        padding: Spacing.xl, minHeight: 140,
      },
      dateRow: { flexDirection: 'row', gap: Spacing.md, marginTop: Spacing.md },
    });
    ```

    **IMPORTANT — Vérifier la signature exacte de Chip et Button** :
    - Si Chip prop est `label` ou `text` ou autre → ajuster (read components/ui/Chip.tsx au préalable)
    - Si Button prop est `label`, `title`, ou `children` → ajuster (read components/ui/Button.tsx)
    - Si DateInput mode='time' renvoie `HH:MM` ou `HH:MM:SS` → adapter le slice

    Mettre à jour `components/lovenotes/index.ts` :
    ```typescript
    export { WaxSeal } from './WaxSeal';
    export { EnvelopeFlap } from './EnvelopeFlap';
    export { EnvelopeCard } from './EnvelopeCard';
    export { LoveNoteCard } from './LoveNoteCard';
    export { LoveNoteEditor } from './LoveNoteEditor';
    ```
  </action>
  <verify>
    <automated>grep -c "export function LoveNoteEditor" components/lovenotes/LoveNoteEditor.tsx && grep -c "presentationStyle=\"pageSheet\"" components/lovenotes/LoveNoteEditor.tsx && grep -c "presetTomorrowMorning\|presetNextSundayEvening\|presetInOneMonth" components/lovenotes/LoveNoteEditor.tsx && grep -c "LoveNoteEditor" components/lovenotes/index.ts && npx tsc --noEmit 2>&1 | grep "LoveNoteEditor\|lovenotes/index" | grep -v "MemoryEditor\|cooklang\|useVault" ; test $? -eq 1</automated>
  </verify>
  <done>
    - Composant `LoveNoteEditor` exporté avec props `{ visible, fromProfile, recipientProfiles, onSave, onClose }`
    - Modal `presentationStyle="pageSheet"` + `animationType="slide"` + `onRequestClose`
    - useEffect reset state au mount visible (preset 'tomorrow' pré-rempli)
    - Chips destinataire affichés depuis `recipientProfiles` (déjà filtré)
    - 4 chips presets (tomorrow / sunday / month / custom) qui appellent `applyPreset`
    - 2 `DateInput` côte-à-côte (mode='date' + mode='time')
    - Toggle preview MarkdownText / TextInput
    - Validation 3 niveaux (destinataire / body / revealAt > now+60s)
    - Garde noRecipients désactive le bouton + message FR
    - `Haptics.selectionAsync` au tap chip + `notificationAsync(Success)` au save OK
    - Barrel `components/lovenotes/index.ts` réexporte `LoveNoteEditor`
    - `npx tsc --noEmit` clean (hors pré-existantes)
  </done>
</task>

<task type="auto">
  <name>Task 2: Câbler FAB Écrire + LoveNoteEditor + useRevealOnForeground dans app/(tabs)/lovenotes.tsx</name>
  <files>app/(tabs)/lovenotes.tsx</files>
  <read_first>
    - app/(tabs)/lovenotes.tsx (état actuel — Plan 35-01)
    - .planning/phases/36-composition-programmation-reveal/36-RESEARCH.md (Code Examples lignes 619-663)
    - hooks/useVault.ts (vérifier que addLoveNote/updateLoveNoteStatus/activeProfile/profiles sont exposés via useVault())
    - lib/parser.ts (vérifier signature loveNotePath(to, createdAt))
  </read_first>
  <action>
    Modifier `app/(tabs)/lovenotes.tsx` pour ajouter :

    1. **Imports** (en haut) :
    ```typescript
    import { Pressable, Text } from 'react-native'; // ajouter Pressable si manquant
    import * as Haptics from 'expo-haptics';
    import { LoveNoteCard, LoveNoteEditor } from '../../components/lovenotes';
    import { useRevealOnForeground } from '../../hooks/useRevealOnForeground';
    import { scheduleLoveNoteReveal } from '../../lib/scheduled-notifications';
    import { localIso } from '../../lib/lovenotes/reveal-engine';
    import { loveNotePath } from '../../lib/parser';
    import { Shadows } from '../../constants/shadows';
    ```

    2. **Étendre destructuring useVault** :
    ```typescript
    const { loveNotes, activeProfile, profiles, addLoveNote, updateLoveNoteStatus } = useVault();
    ```

    3. **Brancher hook foreground** (après `const profileId = activeProfile?.id ?? '';`) :
    ```typescript
    useRevealOnForeground(loveNotes, updateLoveNoteStatus);
    ```

    4. **State editor + recipientProfiles memo** (après les useMemo existants) :
    ```typescript
    const [editorVisible, setEditorVisible] = useState(false);

    const recipientProfiles = useMemo(
      () => profiles.filter((p) => p.id !== profileId),
      [profiles, profileId],
    );

    const handleSave = useCallback(async (to: string, body: string, revealAt: string) => {
      if (!activeProfile) return;
      const createdAt = localIso(new Date());
      const note = {
        from: activeProfile.id,
        to,
        body,
        revealAt,
        createdAt,
        status: 'pending' as const,
      };
      await addLoveNote(note);
      // sourceFile prévisible via loveNotePath (même formule que addLoveNote)
      const sourceFile = loveNotePath(to, createdAt);
      await scheduleLoveNoteReveal({ ...note, sourceFile });
    }, [activeProfile, addLoveNote]);
    ```

    5. **JSX FAB + Modal** — ajouter à la fin du JSX, AVANT le `</SafeAreaView>` :
    ```tsx
    {activeProfile && (
      <Pressable
        onPress={() => {
          Haptics.impactAsync(Haptics.ImpactFeedbackStyle.Light).catch(() => {});
          setEditorVisible(true);
        }}
        style={[styles.fab, { backgroundColor: colors.tint }]}
      >
        <Text style={styles.fabText}>✏️ Écrire</Text>
      </Pressable>
    )}
    {activeProfile && (
      <LoveNoteEditor
        visible={editorVisible}
        fromProfile={activeProfile}
        recipientProfiles={recipientProfiles}
        onSave={handleSave}
        onClose={() => setEditorVisible(false)}
      />
    )}
    ```

    6. **Étendre styles** :
    ```typescript
    fab: {
      position: 'absolute',
      bottom: Spacing['2xl'],
      right: Spacing['2xl'],
      paddingHorizontal: Spacing.xl,
      paddingVertical: Spacing.md,
      borderRadius: 999,
      ...Shadows.md,
    },
    fabText: {
      color: '#fff',
      fontWeight: '600',
      fontSize: FontSize.body,
    },
    ```

    Si `Shadows.md` n'existe pas (vérifier `constants/shadows.ts`), utiliser un fallback inline :
    ```typescript
    shadowColor: '#000', shadowOffset: { width: 0, height: 4 }, shadowOpacity: 0.15, shadowRadius: 8, elevation: 4,
    ```

    NE PAS toucher au reste de l'écran (segmented control, FlatList, empty state).
  </action>
  <verify>
    <automated>grep -c "useRevealOnForeground" app/\(tabs\)/lovenotes.tsx && grep -c "LoveNoteEditor" app/\(tabs\)/lovenotes.tsx && grep -c "scheduleLoveNoteReveal" app/\(tabs\)/lovenotes.tsx && grep -c "addLoveNote" app/\(tabs\)/lovenotes.tsx && grep -c "loveNotePath" app/\(tabs\)/lovenotes.tsx && npx tsc --noEmit 2>&1 | grep "lovenotes.tsx" | grep -v "MemoryEditor\|cooklang\|useVault" ; test $? -eq 1</automated>
  </verify>
  <done>
    - `app/(tabs)/lovenotes.tsx` importe `LoveNoteEditor`, `useRevealOnForeground`, `scheduleLoveNoteReveal`, `localIso`, `loveNotePath`
    - `useRevealOnForeground(loveNotes, updateLoveNoteStatus)` appelé dans le composant
    - State `editorVisible` + memo `recipientProfiles` (filtre `p.id !== profileId`)
    - `handleSave` appelle `addLoveNote(note)` PUIS `scheduleLoveNoteReveal({...note, sourceFile})`
    - FAB rendu en `position: 'absolute'` bottom-right si `activeProfile` non null
    - Modal `LoveNoteEditor` rendu avec props correctes (fromProfile, recipientProfiles, onSave, onClose)
    - `Haptics.impactAsync(Light)` au tap FAB
    - `npx tsc --noEmit` clean (hors pré-existantes)
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` clean
- 3 fichiers livrés : LoveNoteEditor.tsx (neuf), index.ts (modif), lovenotes.tsx (étendu)
- LOVE-09 (composition modal) + LOVE-10 (presets) + LOVE-12 (foreground reveal hook branché) couverts
- LOVE-11 schedule notif déclenché côté handleSave (le routing notif lui-même est dans Plan 02)
</verification>

<success_criteria>
- User peut tap FAB → modal éditeur s'ouvre
- User remplit destinataire (chips) + body + revealAt (presets ou custom)
- Save → addLoveNote + scheduleLoveNoteReveal + onClose
- Au foreground (ou mount) → useRevealOnForeground bascule pending due → revealed
- Validation FR + Alert français pour erreurs
</success_criteria>

<output>
Créer `.planning/phases/36-composition-programmation-reveal/36-03-SUMMARY.md` après complétion.
</output>
