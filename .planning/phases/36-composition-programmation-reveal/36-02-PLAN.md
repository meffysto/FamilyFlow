---
phase: 36-composition-programmation-reveal
plan: 02
type: execute
wave: 2
depends_on: [36-01]
files_modified:
  - app/_layout.tsx
autonomous: true
requirements: [LOVE-11]
must_haves:
  truths:
    - "User qui tap une notif love note arrive sur /(tabs)/lovenotes — warm start (app vivante)"
    - "User qui tap une notif love note depuis l'app tuée arrive sur /(tabs)/lovenotes — cold start"
  artifacts:
    - path: "app/_layout.tsx"
      provides: "Listener notification response (warm) + getLastNotificationResponseAsync (cold)"
      contains: "addNotificationResponseReceivedListener"
  key_links:
    - from: "Notification tap (data.route)"
      to: "expo-router router.push"
      via: "addNotificationResponseReceivedListener callback"
      pattern: "router.push\\(route"
    - from: "Cold start"
      to: "router.push"
      via: "getLastNotificationResponseAsync().then"
      pattern: "getLastNotificationResponseAsync"
---

<objective>
Brancher le routing depuis notification tap dans `app/_layout.tsx` — warm start via `addNotificationResponseReceivedListener` ET cold start via `getLastNotificationResponseAsync`. Lit `data.route` posé par `scheduleLoveNoteReveal` (Plan 01) → `router.push(route)`.

Purpose : sans ce câblage, LOVE-11 partiellement complète — la notif arrive mais le tap n'ouvre pas la boîte. Pitfall 7 RESEARCH (cold start race) résolu via la double approche.
Output : 1 fichier modifié (`app/_layout.tsx`) — minimaliste, pas de nouveau provider.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-composition-programmation-reveal/36-RESEARCH.md
@.planning/phases/36-composition-programmation-reveal/36-01-PLAN.md
@app/_layout.tsx

<interfaces>
From lib/scheduled-notifications.ts (Plan 01) :
```typescript
// data posé sur la notif scheduled :
data: { route: '/(tabs)/lovenotes', sourceFile: note.sourceFile }
```

From expo-notifications :
```typescript
addNotificationResponseReceivedListener(
  (response: NotificationResponse) => void
): EventSubscription;
getLastNotificationResponseAsync(): Promise<NotificationResponse | null>;
// response.notification.request.content.data
```

From expo-router :
```typescript
import { router } from 'expo-router';
router.push(href);
```

Existant ligne 129-136 de app/_layout.tsx — un useEffect existe avec configureNotifications() + loadSavedLanguage().
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Étendre le useEffect de RootLayout avec listener + cold start handler</name>
  <files>app/_layout.tsx</files>
  <read_first>
    - app/_layout.tsx (lignes 1-150 pour imports + useEffect existant ligne 129)
    - .planning/phases/36-composition-programmation-reveal/36-RESEARCH.md (Pattern 4, lignes 333-356 + Pitfall 7, lignes 540-555)
    - lib/scheduled-notifications.ts (vérifier la shape data sur scheduleLoveNoteReveal posée par Plan 01)
  </read_first>
  <action>
    Modifier `app/_layout.tsx` :

    1. **Ajouter imports** (en haut du fichier, à côté des imports existants) :
    ```typescript
    import * as Notifications from 'expo-notifications';
    import { router } from 'expo-router';
    ```
    (`router` est probablement déjà importé sinon — vérifier d'abord avec `grep "from 'expo-router'" app/_layout.tsx` et `grep "expo-notifications" app/_layout.tsx`.)

    2. **Étendre le useEffect existant** ligne ~129 (NE PAS créer un nouveau useEffect — étendre l'existant pour rester cohérent) :

    AVANT :
    ```typescript
    useEffect(() => {
      configureNotifications();

      (async () => {
        await loadSavedLanguage();
        setLangReady(true);
      })();
    }, []);
    ```

    APRÈS :
    ```typescript
    useEffect(() => {
      configureNotifications();

      // Cold start : récupérer la dernière notif tappée si l'app a été lancée par notif (Pitfall 7)
      Notifications.getLastNotificationResponseAsync().then((resp) => {
        const route = resp?.notification.request.content.data?.route;
        if (typeof route === 'string') {
          // Délai léger pour laisser expo-router monter le Stack
          setTimeout(() => router.push(route as any), 0);
        }
      }).catch(() => { /* idempotent — silent */ });

      // Warm start : listener pour notifs reçues pendant que l'app est vivante
      const sub = Notifications.addNotificationResponseReceivedListener((response) => {
        const route = response.notification.request.content.data?.route;
        if (typeof route === 'string') {
          router.push(route as any);
        }
      });

      (async () => {
        await loadSavedLanguage();
        setLangReady(true);
      })();

      return () => sub.remove();
    }, []);
    ```

    3. **NE PAS toucher** :
    - hiérarchie providers
    - autres useEffect
    - imports inutilisés (laisser tel quel)
    - `configureNotifications()` est déjà appelé — préserver

    Justification du `setTimeout(..., 0)` : `getLastNotificationResponseAsync` peut résoudre AVANT que le Stack expo-router ne soit monté (cold start race) — déférer l'appel `router.push` au prochain tick garantit que la nav est ready.
  </action>
  <verify>
    <automated>grep -c "addNotificationResponseReceivedListener" app/_layout.tsx && grep -c "getLastNotificationResponseAsync" app/_layout.tsx && grep -c "sub.remove" app/_layout.tsx && grep -c "from 'expo-notifications'" app/_layout.tsx && npx tsc --noEmit 2>&1 | grep "_layout.tsx" | grep -v "MemoryEditor\|cooklang\|useVault" ; test $? -eq 1</automated>
  </verify>
  <done>
    - `app/_layout.tsx` importe `* as Notifications from 'expo-notifications'` et `router from 'expo-router'`
    - Le useEffect existant ligne ~129 est étendu (PAS dupliqué) avec :
      - `getLastNotificationResponseAsync()` pour cold start
      - `addNotificationResponseReceivedListener` pour warm start
      - cleanup `return () => sub.remove()`
    - `data.route` lu et passé à `router.push(route as any)` dans les deux cas
    - `configureNotifications()` toujours appelé en premier
    - Hiérarchie providers inchangée
    - `npx tsc --noEmit` clean (hors pré-existantes)
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` clean
- `grep "addNotificationResponseReceivedListener" app/_layout.tsx` retourne 1 match
- `grep "getLastNotificationResponseAsync" app/_layout.tsx` retourne 1 match
- Pas de nouveau provider ajouté à la hiérarchie
- Test device manuel (Phase 37 polish) : tap notif → écran lovenotes
</verification>

<success_criteria>
- LOVE-11 routing 100% câblé (warm + cold start)
- Aucune régression dans la hiérarchie providers
- 1 seul useEffect dans RootLayout (étendu, pas dupliqué)
</success_criteria>

<output>
Créer `.planning/phases/36-composition-programmation-reveal/36-02-SUMMARY.md` après complétion.
</output>
