---
phase: 36-composition-programmation-reveal
plan: 04
type: execute
wave: 3
depends_on: [36-01, 36-03]
files_modified:
  - components/lovenotes/EnvelopeUnfoldModal.tsx
  - components/lovenotes/LoveNoteCard.tsx
  - components/lovenotes/index.ts
  - app/(tabs)/lovenotes.tsx
autonomous: true
requirements: [LOVE-13]
must_haves:
  truths:
    - "User tap une LoveNoteCard dont status === 'revealed' → EnvelopeUnfoldModal s'ouvre full-screen"
    - "User voit animation Reanimated : rabat rotateX 0→175° (800ms) + cachet scale sequence 1→1.4→0 + body opacity 0→1"
    - "User sent Haptics.notificationAsync(Success) au peak de l'animation (callback contentOpacity withTiming)"
    - "User voit la note passer en 'read' APRÈS la fin de l'animation (onUnfoldComplete → updateLoveNoteStatus)"
  artifacts:
    - path: "components/lovenotes/EnvelopeUnfoldModal.tsx"
      provides: "Modal full-screen animation unfold enveloppe"
      exports: ["EnvelopeUnfoldModal"]
      min_lines: 100
  key_links:
    - from: "LoveNoteCard onPress"
      to: "EnvelopeUnfoldModal (via state écran lovenotes.tsx)"
      via: "setUnfoldNote(note) pour notes status==='revealed'"
      pattern: "setUnfoldNote|unfoldNote"
    - from: "EnvelopeUnfoldModal onUnfoldComplete"
      to: "updateLoveNoteStatus(read)"
      via: "callback withTiming opacity final"
      pattern: "updateLoveNoteStatus.*'read'|status:.*'read'"
---

<objective>
Livrer l'animation finale du cycle Love Notes : `EnvelopeUnfoldModal` full-screen qui déplie le rabat en Reanimated (rotateX 0→175° sans `perspective` — conforme strict CLAUDE.md per RESEARCH Discretion A), fait sauter le cachet, révèle le body et bascule la note en `read` au callback final.

Purpose : boucler LOVE-13 (animation + haptic + passage read). L'expérience "lettre qui s'ouvre" ferme la boucle du cycle Love Notes.
Output : 1 composant neuf (`EnvelopeUnfoldModal.tsx`), câblage `LoveNoteCard.onPress` (props), state `unfoldNote` dans écran lovenotes.tsx.

**Pourquoi Wave 3 et pas Wave 2** : ce plan modifie `app/(tabs)/lovenotes.tsx` qui est aussi modifié par Plan 03 (FAB + LoveNoteEditor). Pour éviter race / merge conflict, on séquentialise : 36-01 (Wave 1) → 36-03 (Wave 2) → 36-04 (Wave 3). Coût +1 wave, zéro race.

**Décision tranchée (per RESEARCH Open Q 1)** : PAS de `perspective` dans le transform array — respect strict CLAUDE.md. Feel 2D acceptable — le rabat s'écrase verticalement mais l'effet "enveloppe qui s'ouvre" reste lisible via la sequence (seal jump + body reveal). Si test device UX inacceptable → revoir en Phase 37 polish avec dérogation locale justifiée.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-composition-programmation-reveal/36-RESEARCH.md
@.planning/phases/36-composition-programmation-reveal/36-01-PLAN.md
@.planning/phases/36-composition-programmation-reveal/36-03-PLAN.md
@components/lovenotes/EnvelopeFlap.tsx
@components/lovenotes/WaxSeal.tsx
@components/lovenotes/LoveNoteCard.tsx
@lib/lovenotes/selectors.ts

<interfaces>
From components/lovenotes (Phase 35) :
```typescript
export function EnvelopeFlap({ width, height }): JSX.Element;
export const WaxSeal: React.FC<{ count: number; size?: number; pulse?: boolean; initial?: string }>;
```

From hooks/useVaultLoveNotes :
```typescript
updateLoveNoteStatus: (sourceFile, status: 'pending'|'revealed'|'read', readAt?) => Promise<void>;
// Si status='read' et readAt non fourni, le hook auto-injecte new Date().toISOString().slice(0,19)
```

From lib/lovenotes/selectors :
```typescript
export function isRevealed(note: LoveNote, now?: Date): boolean;
```

From react-native-reanimated :
```typescript
useSharedValue, useAnimatedStyle, withTiming, withSequence, withSpring, runOnJS, Easing
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Créer components/lovenotes/EnvelopeUnfoldModal.tsx</name>
  <files>components/lovenotes/EnvelopeUnfoldModal.tsx, components/lovenotes/index.ts</files>
  <read_first>
    - .planning/phases/36-composition-programmation-reveal/36-RESEARCH.md (Pattern 6 lignes 398-472, Pitfalls 1/2/5/6)
    - components/lovenotes/EnvelopeFlap.tsx (vérifier props width/height + couleurs PAPER/INK existantes)
    - components/lovenotes/WaxSeal.tsx (vérifier props size/count/pulse/initial)
    - CLAUDE.md (règle perspective interdite — PAS dérogée dans ce plan)
  </read_first>
  <action>
    Créer `components/lovenotes/EnvelopeUnfoldModal.tsx` :

    ```typescript
    /**
     * EnvelopeUnfoldModal.tsx — Animation unfold full-screen (Phase 36 Plan 04)
     *
     * Modal full-screen transparent. Au mount (visible=true) :
     *  1. Seal jump (200ms) — withSequence scale 1 → 1.4 → 0 (disparaît)
     *  2. Rabat unfold (800ms) — rotateX 0° → 175° (PAS de perspective — strict CLAUDE.md)
     *  3. Body reveal (400ms) — opacity 0 → 1
     *  4. Callback final : Haptics.notificationAsync(Success) + onUnfoldComplete()
     *
     * Parent (écran lovenotes.tsx) :
     *  - Sur unfoldComplete → updateLoveNoteStatus(sourceFile, 'read') — patch différé
     *    pour éviter flicker (Pitfall 6).
     *
     * Pitfall 5 : requestAnimationFrame wrap pour garantir le premier frame posé.
     */

    import React, { useEffect } from 'react';
    import { Modal, View, Text, StyleSheet, Pressable, ScrollView, Dimensions } from 'react-native';
    import Animated, {
      useSharedValue, useAnimatedStyle, withTiming, withSequence, withSpring,
      runOnJS, Easing,
    } from 'react-native-reanimated';
    import * as Haptics from 'expo-haptics';

    import { useThemeColors } from '../../contexts/ThemeContext';
    import { Spacing } from '../../constants/spacing';
    import { FontSize, FontWeight } from '../../constants/typography';
    import { EnvelopeFlap } from './EnvelopeFlap';
    import { WaxSeal } from './WaxSeal';

    const SPRING_SEAL_JUMP = { damping: 8, stiffness: 200 } as const;
    const UNFOLD_DURATION_MS = 800;
    const SEAL_JUMP_MS = 200;
    const CONTENT_REVEAL_MS = 400;

    // Couleurs identitaires enveloppe — hors thème (cf Phase 35 EnvelopeFlap/WaxSeal)
    // Le papier crème + encre brune sont des constantes de design propres au domaine
    // Love Notes (lettre manuscrite). NE PAS remplacer par useThemeColors() — perdrait
    // l'identité visuelle voulue. Si refonte théming nécessaire en Phase 37+, déplacer
    // ces tokens dans constants/colors.ts namespace `envelope.*`.
    const PAPER = '#faf4e4';
    const INK = '#3d2a1a';

    interface EnvelopeUnfoldModalProps {
      visible: boolean;
      fromName: string;
      body: string;
      onClose: () => void;
      /** Appelé à la fin de la séquence (avant que le parent patche 'read'). */
      onUnfoldComplete: () => void;
    }

    const SCREEN_WIDTH = Dimensions.get('window').width;
    const ENVELOPE_W = Math.min(SCREEN_WIDTH - Spacing['2xl'] * 2, 340);
    const ENVELOPE_H = Math.round(ENVELOPE_W / (2 / 1.15));
    const FLAP_H = Math.round(ENVELOPE_H * 0.55);

    function triggerHaptic() {
      Haptics.notificationAsync(Haptics.NotificationFeedbackType.Success).catch(() => {});
    }

    export function EnvelopeUnfoldModal({
      visible, fromName, body, onClose, onUnfoldComplete,
    }: EnvelopeUnfoldModalProps) {
      const { colors } = useThemeColors();
      const flapRotate = useSharedValue(0);
      const sealScale = useSharedValue(1);
      const contentOpacity = useSharedValue(0);

      useEffect(() => {
        if (!visible) {
          // Reset quand invisible pour prochaine ouverture
          flapRotate.value = 0;
          sealScale.value = 1;
          contentOpacity.value = 0;
          return;
        }
        // Pitfall 5 : déférer d'un frame pour garantir render initial posé
        const raf = requestAnimationFrame(() => {
          sealScale.value = withSequence(
            withTiming(1.4, { duration: SEAL_JUMP_MS }),
            withSpring(0, SPRING_SEAL_JUMP),
          );
          flapRotate.value = withTiming(175, {
            duration: UNFOLD_DURATION_MS,
            easing: Easing.out(Easing.cubic),
          });
          contentOpacity.value = withTiming(
            1,
            { duration: CONTENT_REVEAL_MS, easing: Easing.linear },
            (finished) => {
              if (finished) {
                runOnJS(triggerHaptic)();
                runOnJS(onUnfoldComplete)();
              }
            },
          );
        });
        return () => cancelAnimationFrame(raf);
      }, [visible]);

      const flapStyle = useAnimatedStyle(() => ({
        transform: [{ rotateX: `${flapRotate.value}deg` }],
        // Pas de perspective — conforme strict CLAUDE.md (feel 2D)
        transformOrigin: 'top',
      }));
      const sealStyle = useAnimatedStyle(() => ({
        transform: [{ scale: sealScale.value }],
      }));
      const contentStyle = useAnimatedStyle(() => ({
        opacity: contentOpacity.value,
      }));

      return (
        <Modal visible={visible} transparent animationType="fade" onRequestClose={onClose}>
          <Pressable style={styles.backdrop} onPress={onClose}>
            <View style={[styles.envelope, { width: ENVELOPE_W, height: ENVELOPE_H, backgroundColor: PAPER }]} pointerEvents="box-none">
              {/* Rabat animé — top */}
              <Animated.View style={[styles.flap, { width: ENVELOPE_W, height: FLAP_H }, flapStyle]}>
                <EnvelopeFlap width={ENVELOPE_W} height={FLAP_H} />
              </Animated.View>
              {/* Cachet animé — centré */}
              <Animated.View style={[styles.sealSlot, sealStyle]} pointerEvents="none">
                <WaxSeal size={72} count={0} pulse={false} initial="💌" />
              </Animated.View>
              {/* Contenu révélé */}
              <Animated.View style={[styles.content, contentStyle]}>
                <ScrollView contentContainerStyle={{ padding: Spacing.xl }}>
                  <Text style={[styles.from, { color: INK }]}>De {fromName}</Text>
                  <Text style={[styles.body, { color: INK }]}>{body}</Text>
                </ScrollView>
              </Animated.View>
            </View>
          </Pressable>
        </Modal>
      );
    }

    const styles = StyleSheet.create({
      backdrop: {
        flex: 1,
        // Backdrop modal standard — pattern universel iOS/Android, pas un token de thème
        backgroundColor: 'rgba(0,0,0,0.6)',
        alignItems: 'center',
        justifyContent: 'center',
      },
      envelope: {
        borderRadius: 14,
        overflow: 'hidden',
        position: 'relative',
      },
      flap: {
        position: 'absolute',
        top: 0,
        left: 0,
        zIndex: 3,
      },
      sealSlot: {
        position: 'absolute',
        top: '40%',
        left: '50%',
        marginLeft: -36,
        marginTop: -36,
        zIndex: 4,
      },
      content: {
        flex: 1,
        paddingTop: Spacing['2xl'],
      },
      from: {
        fontSize: FontSize.sm,
        fontWeight: FontWeight.semibold as any,
        marginBottom: Spacing.md,
      },
      body: {
        fontSize: FontSize.body,
        lineHeight: 22,
      },
    });
    ```

    Mettre à jour `components/lovenotes/index.ts` pour réexporter :
    ```typescript
    export { EnvelopeUnfoldModal } from './EnvelopeUnfoldModal';
    ```

    NOTE : Si `transformOrigin: 'top'` n'est pas supporté dans Reanimated worklet à runtime (Pitfall 2), le rabat tournera autour de son centre. C'est acceptable pour une Phase 36 — le test device en Phase 37 validera ou demandera un fallback (pivot manuel via translateY). Ne PAS implémenter le fallback par anticipation.
  </action>
  <verify>
    <automated>grep -c "export function EnvelopeUnfoldModal" components/lovenotes/EnvelopeUnfoldModal.tsx && grep -c "withSequence\|withTiming\|withSpring" components/lovenotes/EnvelopeUnfoldModal.tsx && grep -c "Haptics.notificationAsync" components/lovenotes/EnvelopeUnfoldModal.tsx && grep -c "EnvelopeUnfoldModal" components/lovenotes/index.ts && grep -c "Couleurs identitaires enveloppe" components/lovenotes/EnvelopeUnfoldModal.tsx && grep -c "Backdrop modal standard" components/lovenotes/EnvelopeUnfoldModal.tsx && grep -c "perspective" components/lovenotes/EnvelopeUnfoldModal.tsx ; [ $? -eq 1 ] && npx tsc --noEmit 2>&1 | grep "EnvelopeUnfoldModal" | grep -v "MemoryEditor\|cooklang\|useVault" ; test $? -eq 1</automated>
  </verify>
  <done>
    - `EnvelopeUnfoldModal` exporté avec props `{ visible, fromName, body, onClose, onUnfoldComplete }`
    - 3 sharedValues : flapRotate (0→175), sealScale (sequence 1→1.4→0), contentOpacity (0→1)
    - useEffect avec requestAnimationFrame wrap (Pitfall 5) + cleanup
    - Callback final de `contentOpacity.value` déclenche `runOnJS(triggerHaptic)()` + `runOnJS(onUnfoldComplete)()`
    - `transformOrigin: 'top'` sur flapStyle
    - **PAS de `perspective` dans le code** (vérifié par grep returning 0 match)
    - Constantes PAPER/INK précédées d'un commentaire justificatif (couleurs identitaires hors thème)
    - Backdrop `rgba(0,0,0,0.6)` précédé d'un commentaire `// Backdrop modal standard`
    - Barrel `index.ts` réexporte `EnvelopeUnfoldModal`
    - `npx tsc --noEmit` clean (hors pré-existantes)
  </done>
</task>

<task type="auto">
  <name>Task 2: Câbler tap LoveNoteCard → EnvelopeUnfoldModal dans app/(tabs)/lovenotes.tsx</name>
  <files>app/(tabs)/lovenotes.tsx, components/lovenotes/LoveNoteCard.tsx</files>
  <read_first>
    - app/(tabs)/lovenotes.tsx (état APRÈS Plan 03 — donc FAB + LoveNoteEditor câblés, NE PAS supprimer)
    - components/lovenotes/LoveNoteCard.tsx (déjà onPress prop existante ligne 25 — Phase 35)
    - .planning/phases/36-composition-programmation-reveal/36-RESEARCH.md (Pitfall 6 lignes 533-537 : patch read APRÈS animation)
    - lib/lovenotes/selectors.ts (isRevealed pour le cas pending due)
  </read_first>
  <action>
    **NOTE Wave 3** : ce plan tourne APRÈS Plan 03 (depends_on: [36-01, 36-03]). L'écran `app/(tabs)/lovenotes.tsx` contient déjà FAB + LoveNoteEditor + useRevealOnForeground (Plan 03). Ce plan ÉTEND, ne remplace rien.

    Ajouts UNIQUEMENT :
    - import `EnvelopeUnfoldModal` + `isRevealed`
    - `useState<LoveNote | null>(null)` pour `unfoldNote`
    - `handleCardPress` callback
    - `handleUnfoldComplete` callback
    - prop `onPress={handleCardPress}` sur `<LoveNoteCard>` dans `renderItem`
    - `<EnvelopeUnfoldModal>` dans le JSX

    NE PAS toucher aux ajouts du Plan 03 (FAB / LoveNoteEditor / useRevealOnForeground / scheduleLoveNoteReveal / handleSave).

    ---

    Étapes concrètes :

    1. **Ajouter imports** (à regrouper avec ceux du Plan 03 déjà présents) :
    ```typescript
    import { EnvelopeUnfoldModal } from '../../components/lovenotes';
    import { isRevealed } from '../../lib/lovenotes/selectors';
    ```

    2. **State unfold** (dans le composant, à côté du `editorVisible` du Plan 03) :
    ```typescript
    const [unfoldNote, setUnfoldNote] = useState<LoveNote | null>(null);
    ```

    3. **Handler tap card** :
    ```typescript
    const handleCardPress = useCallback((note: LoveNote) => {
      // status 'revealed' → ouvrir unfold direct
      if (note.status === 'revealed') {
        setUnfoldNote(note);
        return;
      }
      // pending due → upgrade en revealed puis ouvrir
      if (note.status === 'pending' && isRevealed(note)) {
        updateLoveNoteStatus(note.sourceFile, 'revealed')
          .then(() => setUnfoldNote({ ...note, status: 'revealed' }))
          .catch((e) => { if (__DEV__) console.warn('[handleCardPress]', e); });
        return;
      }
      // pending future / read → noop (ou future : navigation détail)
    }, [updateLoveNoteStatus]);
    ```

    4. **Passer onPress à LoveNoteCard** — modifier le `renderItem` :
    ```typescript
    const renderItem = useCallback(
      ({ item }: { item: LoveNote }) => (
        <LoveNoteCard note={item} profiles={profiles} onPress={handleCardPress} />
      ),
      [profiles, handleCardPress],
    );
    ```

    5. **Handler unfold complete (patch read APRÈS animation)** :
    ```typescript
    const handleUnfoldComplete = useCallback(async () => {
      if (!unfoldNote) return;
      try {
        await updateLoveNoteStatus(unfoldNote.sourceFile, 'read');
      } catch (e) {
        if (__DEV__) console.warn('[handleUnfoldComplete]', e);
      }
    }, [unfoldNote, updateLoveNoteStatus]);
    ```

    6. **JSX — ajouter avant `</SafeAreaView>`** (après le FAB / LoveNoteEditor du Plan 03) :
    ```tsx
    {unfoldNote && (
      <EnvelopeUnfoldModal
        visible={!!unfoldNote}
        fromName={profiles.find((p) => p.id === unfoldNote.from)?.name ?? 'Famille'}
        body={unfoldNote.body}
        onClose={() => setUnfoldNote(null)}
        onUnfoldComplete={handleUnfoldComplete}
      />
    )}
    ```

    **Concernant LoveNoteCard.tsx** : la prop `onPress` existe déjà Phase 35 (ligne 25) et le handler interne ligne 51-54 appelle `onPress?.(note)`. **AUCUNE modification nécessaire** sur `LoveNoteCard.tsx` — la file n'apparaît dans `files_modified` que par précaution si on devait corriger un bug. Si la relecture confirme onPress opérationnel, ne rien toucher.
  </action>
  <verify>
    <automated>grep -c "EnvelopeUnfoldModal" app/\(tabs\)/lovenotes.tsx && grep -c "unfoldNote" app/\(tabs\)/lovenotes.tsx && grep -c "handleUnfoldComplete\|updateLoveNoteStatus.*'read'" app/\(tabs\)/lovenotes.tsx && grep -c "handleCardPress" app/\(tabs\)/lovenotes.tsx && grep -c "onPress={handleCardPress}" app/\(tabs\)/lovenotes.tsx && npx tsc --noEmit 2>&1 | grep "lovenotes.tsx\|LoveNoteCard" | grep -v "MemoryEditor\|cooklang\|useVault" ; test $? -eq 1</automated>
  </verify>
  <done>
    - `app/(tabs)/lovenotes.tsx` importe `EnvelopeUnfoldModal` et `isRevealed`
    - State `unfoldNote: LoveNote | null` + setter
    - `handleCardPress` gère 3 cas (revealed → open ; pending due → update+open ; autre → noop)
    - `renderItem` passe `onPress={handleCardPress}` à `<LoveNoteCard>`
    - `handleUnfoldComplete` appelle `updateLoveNoteStatus(sourceFile, 'read')` — APRÈS la fin de l'animation
    - `<EnvelopeUnfoldModal>` rendu conditionnellement quand `unfoldNote` non null
    - Le plan 03 (FAB + LoveNoteEditor + useRevealOnForeground + handleSave) reste intact (non-régression)
    - `LoveNoteCard.tsx` inchangé (prop onPress déjà existante Phase 35)
    - `npx tsc --noEmit` clean (hors pré-existantes)
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` clean
- 3 fichiers neufs/modifs : `EnvelopeUnfoldModal.tsx` (neuf), `components/lovenotes/index.ts` (modif), `app/(tabs)/lovenotes.tsx` (étendu — APRÈS Plan 03 en Wave 3)
- `LoveNoteCard.tsx` listé en files_modified par précaution (Phase 35 shipped avec prop onPress — aucune modif attendue sauf bug)
- PAS de `perspective` dans EnvelopeUnfoldModal.tsx (conformité stricte CLAUDE.md tranchée)
- Couleurs hardcoded `PAPER`/`INK`/`rgba backdrop` justifiées par commentaires inline (W1 closed)
- LOVE-13 : rotateX 175° + seal sequence + opacity reveal + Haptics Success + passage read
</verification>

<success_criteria>
- User tap LoveNoteCard `revealed` → modal full-screen s'ouvre
- Animation Reanimated : seal jump + rabat unfold + body reveal en ~1200ms total
- `Haptics.notificationAsync(Success)` tiré au callback final
- Note passée en `read` après animation (pas de flicker pendant — Pitfall 6 résolu)
- Pending due upgradé via `updateLoveNoteStatus(revealed)` puis ouvert
- Dismiss via tap backdrop ou system back
</success_criteria>

<output>
Créer `.planning/phases/36-composition-programmation-reveal/36-04-SUMMARY.md` après complétion.
</output>
