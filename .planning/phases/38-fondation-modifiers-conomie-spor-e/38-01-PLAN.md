---
phase: 38-fondation-modifiers-conomie-spor-e
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/mascot/types.ts
  - lib/mascot/farm-engine.ts
  - lib/vault-cache.ts
  - lib/__tests__/farm-engine.test.ts
autonomous: true
requirements: [MOD-01, MOD-02, SPOR-13]
requirements_addressed: [MOD-01, MOD-02, SPOR-13]

must_haves:
  truths:
    - "Plants pré-v1.7 (CSV 6 champs) restent parsables — modifiers === undefined"
    - "Plants avec wager round-trip deep-equal via serializeCrops → parseCrops"
    - "CACHE_VERSION passe de 3 à 4 avec commentaire inline Phase 38"
    - "Tests Jest farm-engine passent, incluant nouveaux cas modifiers"
  artifacts:
    - path: "lib/mascot/types.ts"
      provides: "PlantedCrop.modifiers?, FarmCropModifiers, WagerModifier exportés"
      contains: "modifiers?: FarmCropModifiers"
    - path: "lib/mascot/farm-engine.ts"
      provides: "serializeCrops/parseCrops avec 7e champ pipe-escape JSON + encodeModifiers/decodeModifiers"
      contains: "encodeModifiers"
    - path: "lib/vault-cache.ts"
      provides: "CACHE_VERSION = 4 + fichier vault-cache-v4.json"
      contains: "CACHE_VERSION = 4"
    - path: "lib/__tests__/farm-engine.test.ts"
      provides: "Suite tests round-trip modifiers + backward-compat legacy"
      contains: "describe('serializeCrops / parseCrops with modifiers'"
  key_links:
    - from: "serializeCrops"
      to: "encodeModifiers"
      via: "conditional append 7th segment"
      pattern: "modifiers\\s*\\?\\s*:\\s*`:\\$\\{encodeModifiers"
    - from: "parseCrops"
      to: "decodeModifiers"
      via: "split(':') parts[6] → decodeModifiers(parts[6])"
      pattern: "decodeModifiers\\(parts\\[6\\]\\)"
---

<objective>
Poser la fondation data pure : étendre `PlantedCrop` avec champ optionnel `modifiers` extensible, étendre la sérialisation CSV markdown `serializeCrops`/`parseCrops` avec un 7e champ pipe-escape JSON backward-compatible (plants legacy sans 7e fragment → `modifiers === undefined`), bumper `CACHE_VERSION` de 3 à 4, et livrer la suite Jest de round-trip + backward-compat.

Purpose: Permettre aux phases 39-40 de consommer `PlantedCrop.modifiers.wager` sans rupture du vault existant.
Output: Shape canonique + sérialisation pipe-escape + cache bumpé + tests verts.
</objective>

<context>
@CLAUDE.md
@.planning/phases/38-fondation-modifiers-conomie-spor-e/38-CONTEXT.md
@.planning/phases/38-fondation-modifiers-conomie-spor-e/38-RESEARCH.md
@lib/mascot/types.ts
@lib/mascot/farm-engine.ts
@lib/vault-cache.ts
@lib/__tests__/farm-engine.test.ts

<interfaces>
Shape actuelle (lib/mascot/types.ts:305) :
```ts
export interface PlantedCrop {
  cropId: string;
  plotIndex: number;
  currentStage: number;
  tasksCompleted: number;
  plantedAt: string;       // YYYY-MM-DD
  isGolden?: boolean;
}
```

Sérialisation actuelle (lib/mascot/farm-engine.ts:225-245) :
```ts
// serializeCrops → `${plotIndex}:${cropId}:${currentStage}:${tasksCompleted}:${plantedAt}:${goldenFlag}` joined by ','
// parseCrops → split(',') puis split(':') → 6 champs positionnels
```

Cache (lib/vault-cache.ts:45-46) :
```ts
const CACHE_VERSION = 3;
const CACHE_FILE_URI = FileSystem.documentDirectory + 'vault-cache-v3.json';
```

Forme cible à livrer (Phase 38) :
```ts
export type WagerDuration = 'chill' | 'engage' | 'sprint';
export type WagerMultiplier = 1.3 | 1.7 | 2.5;

export interface WagerModifier {
  sporeeId: string;
  duration: WagerDuration;
  multiplier: WagerMultiplier;
  appliedAt: string;           // ISO YYYY-MM-DD
  sealerProfileId: string;
  cumulTarget?: number;        // Phase 39 — nullable phase 38
  cumulCurrent?: number;       // Phase 39 — nullable phase 38
}

export interface FarmCropModifiers {
  wager?: WagerModifier;
  graftedWith?: string;        // v1.8 Pollen — anticipé, zéro impl phase 38
}

export interface PlantedCrop {
  cropId: string;
  plotIndex: number;
  currentStage: number;
  tasksCompleted: number;
  plantedAt: string;
  isGolden?: boolean;
  modifiers?: FarmCropModifiers;  // ← NEW
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tâche 1 : Étendre PlantedCrop + helpers encodeModifiers/decodeModifiers + round-trip CSV 7e champ</name>
  <files>lib/mascot/types.ts, lib/mascot/farm-engine.ts</files>
  <read_first>
    - lib/mascot/types.ts (lignes 295-312 pour la shape PlantedCrop actuelle + CROP_CATALOG ligne 331 pour classification tiers)
    - lib/mascot/farm-engine.ts (lignes 225-245 pour serializeCrops/parseCrops à étendre)
    - .planning/phases/38-fondation-modifiers-conomie-spor-e/38-RESEARCH.md section "Investigations détaillées §1" (stratégie pipe-escape exacte)
  </read_first>
  <behavior>
    - Round-trip plant SANS modifier : `parseCrops(serializeCrops([p]))` deep-equal `[p]` pour un plant legacy (pas de 7e fragment produit)
    - Round-trip plant AVEC wager complet : deep-equal après parse(serialize(x))
    - Parse CSV legacy 6 champs (pré-v1.7) : `modifiers === undefined` (jamais `null` ni `{}`)
    - Parse CSV avec 7e fragment vide string : `modifiers === undefined`
    - sporeeId contenant `-` (uuid style `sp-abc-123`) round-trip sans corruption
    - Plant avec modifier + plant sans dans le même CSV : array retourné identique après round-trip
  </behavior>
  <action>
    1. Dans `lib/mascot/types.ts` (après PlantedCrop ligne 312), ajouter les types :
       ```ts
       export type WagerDuration = 'chill' | 'engage' | 'sprint';
       export type WagerMultiplier = 1.3 | 1.7 | 2.5;

       export interface WagerModifier {
         sporeeId: string;
         duration: WagerDuration;
         multiplier: WagerMultiplier;
         appliedAt: string;           // ISO YYYY-MM-DD
         sealerProfileId: string;
         cumulTarget?: number;        // Phase 39
         cumulCurrent?: number;       // Phase 39
       }

       export interface FarmCropModifiers {
         wager?: WagerModifier;
         graftedWith?: string;        // v1.8 Pollen — anticipé, zéro impl phase 38
       }
       ```
       Ajouter `modifiers?: FarmCropModifiers;` à `PlantedCrop` (ligne 305).

    2. Dans `lib/mascot/farm-engine.ts`, importer `FarmCropModifiers` depuis `./types` (ajouter au `import type` existant en tête de fichier).

    3. Ajouter juste avant `serializeCrops` (ligne 225) deux helpers pure (per D-01 pipe-escape JSON — lisible Obsidian, backward-compat stricte) :
       ```ts
       // Phase 38 (MOD-01) : encodage modifiers JSON avec escape des séparateurs CSV
       // `,` → `|`   (séparateur entre plants)
       // `:` → `§`   (séparateur entre champs d'un plant)
       // Résultat lisible à l'œil dans Obsidian. Voir 38-RESEARCH.md §1.
       export function encodeModifiers(modifiers: FarmCropModifiers | undefined): string {
         if (!modifiers || Object.keys(modifiers).length === 0) return '';
         return JSON.stringify(modifiers).replace(/,/g, '|').replace(/:/g, '§');
       }

       export function decodeModifiers(raw: string | undefined): FarmCropModifiers | undefined {
         if (!raw || raw.trim() === '') return undefined;
         try {
           const restored = raw.replace(/§/g, ':').replace(/\|/g, ',');
           const parsed = JSON.parse(restored) as FarmCropModifiers;
           if (!parsed || typeof parsed !== 'object') return undefined;
           return parsed;
         } catch {
           return undefined;
         }
       }
       ```

    4. Remplacer `serializeCrops` (ligne 225) — conditionnel strict sur présence modifiers (JAMAIS de trailing `:` pour legacy) :
       ```ts
       export function serializeCrops(crops: PlantedCrop[]): string {
         return crops
           .map(c => {
             const base = `${c.plotIndex}:${c.cropId}:${c.currentStage}:${c.tasksCompleted}:${c.plantedAt}:${c.isGolden ? '1' : ''}`;
             const modStr = encodeModifiers(c.modifiers);
             return modStr ? `${base}:${modStr}` : base;
           })
           .join(',');
       }
       ```

    5. Remplacer `parseCrops` (ligne 232) — split positional préservé, 7e fragment optionnel :
       ```ts
       export function parseCrops(csv: string): PlantedCrop[] {
         if (!csv || csv.trim() === '') return [];
         return csv.split(',').map(entry => {
           const parts = entry.split(':');
           const [plotIndex, cropId, currentStage, tasksCompleted, plantedAt, goldenFlag, modifiersRaw] = parts;
           const modifiers = parts.length >= 7 ? decodeModifiers(modifiersRaw) : undefined;
           const crop: PlantedCrop = {
             plotIndex: parseInt(plotIndex, 10),
             cropId,
             currentStage: parseInt(currentStage, 10),
             tasksCompleted: parseFloat(tasksCompleted),
             plantedAt: plantedAt || new Date().toISOString().slice(0, 10),
             isGolden: goldenFlag === '1',
           };
           if (modifiers) crop.modifiers = modifiers;
           return crop;
         }).filter(c => !isNaN(c.plotIndex) && c.cropId);
       }
       ```
       Note : `modifiers` n'est ajouté à l'objet que si défini, garantissant `modifiers === undefined` côté consommateur (pas `modifiers: undefined` qui bruit les deep-equal).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "(farm-engine|types)\.ts" | grep -v "MemoryEditor|cooklang|useVault" ; test $? -eq 1</automated>
  </verify>
  <done>
    - `PlantedCrop.modifiers` exporté + types `WagerModifier`/`FarmCropModifiers` présents dans types.ts
    - `encodeModifiers`/`decodeModifiers` exportés depuis farm-engine.ts
    - `serializeCrops` ne produit JAMAIS un trailing `:` pour plants legacy
    - `parseCrops` accepte 6 ET 7 fragments, retourne `modifiers === undefined` pour 6
    - `npx tsc --noEmit` clean (hors erreurs pré-existantes MemoryEditor/cooklang/useVault)
  </done>
  <acceptance_criteria>
    - `grep -n "modifiers?: FarmCropModifiers" lib/mascot/types.ts` trouve la ligne dans PlantedCrop
    - `grep -n "export function encodeModifiers" lib/mascot/farm-engine.ts` trouve la fonction
    - `grep -n "export function decodeModifiers" lib/mascot/farm-engine.ts` trouve la fonction
    - `grep -n "WagerDuration\|WagerMultiplier\|WagerModifier\|FarmCropModifiers" lib/mascot/types.ts` trouve ≥4 occurrences
    - `grep "modStr ? .*\${base}:\${modStr}. : base" lib/mascot/farm-engine.ts` retourne la ligne conditionnelle (pas de trailing `:` legacy)
    - `grep "parts.length >= 7" lib/mascot/farm-engine.ts` confirme le check backward-compat
    - `npx tsc --noEmit` exit 0 (hors erreurs pré-existantes connues)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Tâche 2 : Bump CACHE_VERSION 3→4 + fichier vault-cache-v4.json</name>
  <files>lib/vault-cache.ts</files>
  <read_first>
    - lib/vault-cache.ts (lignes 30-70 pour CACHE_VERSION et CACHE_FILE_URI)
    - .planning/phases/38-fondation-modifiers-conomie-spor-e/38-RESEARCH.md section "§8 VaultCache — bump obligatoire mais impact nul"
  </read_first>
  <action>
    1. Dans `lib/vault-cache.ts` ligne 45 : remplacer `const CACHE_VERSION = 3;` par :
       ```ts
       // v4 : Phase 38 — shape PlantedCrop.modifiers + sporeeCount frontmatter farm (invalidation propre au premier boot post-migration)
       const CACHE_VERSION = 4;
       ```
    2. Ligne 46 : remplacer `'vault-cache-v3.json'` par `'vault-cache-v4.json'`.
    3. Ne RIEN modifier d'autre dans ce fichier (VaultCacheState reste inchangé — farm/mascot sont exclus volontairement, cf. CLAUDE.md Cache section).
  </action>
  <verify>
    <automated>grep -c "const CACHE_VERSION = 4" lib/vault-cache.ts && grep -c "vault-cache-v4.json" lib/vault-cache.ts</automated>
  </verify>
  <done>
    - `CACHE_VERSION = 4` avec commentaire Phase 38 inline (ligne 45-46)
    - `CACHE_FILE_URI` référence `vault-cache-v4.json`
    - Aucune autre modification au fichier
  </done>
  <acceptance_criteria>
    - `grep -n "const CACHE_VERSION = 4" lib/vault-cache.ts` trouve exactement 1 ligne
    - `grep -n "v4 : Phase 38" lib/vault-cache.ts` trouve le commentaire inline
    - `grep -n "vault-cache-v4.json" lib/vault-cache.ts` trouve exactement 1 ligne
    - `grep -c "CACHE_VERSION = 3\|vault-cache-v3.json" lib/vault-cache.ts` retourne 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Tâche 3 : Suite Jest round-trip modifiers + backward-compat legacy dans farm-engine.test.ts</name>
  <files>lib/__tests__/farm-engine.test.ts</files>
  <read_first>
    - lib/__tests__/farm-engine.test.ts (structure existante, pattern describe/it, jest.spyOn(Math, 'random'))
    - lib/mascot/farm-engine.ts (encodeModifiers/decodeModifiers/serializeCrops/parseCrops après Tâche 1)
    - lib/mascot/types.ts (WagerModifier, FarmCropModifiers exportés)
  </read_first>
  <behavior>
    - Round-trip plant legacy (6 champs) : plant planté sans `modifiers` → serialize → parse → deep-equal identique, propriété `modifiers` ABSENTE de l'objet
    - Round-trip plant avec `wager` : deep-equal après parse(serialize(x)), tous champs WagerModifier préservés
    - Parse CSV pré-v1.7 (pas de 7e fragment) : `modifiers === undefined`
    - Parse CSV 7e fragment vide string : `modifiers === undefined`
    - sporeeId avec tirets (`sp-abc-123`) preserved exactement
    - Mix plants legacy + plants avec wager dans un même CSV : chacun préserve son état
    - `encodeModifiers({})` retourne `''` (pas de bruit JSON vide)
    - `decodeModifiers('')` / `decodeModifiers(undefined)` retournent `undefined`
    - `decodeModifiers('{invalid')` retourne `undefined` (fallback défensif)
  </behavior>
  <action>
    Ajouter à la fin de `lib/__tests__/farm-engine.test.ts` un nouveau bloc `describe` :

    ```ts
    import { encodeModifiers, decodeModifiers } from '../mascot/farm-engine';
    import type { WagerModifier, FarmCropModifiers } from '../mascot/types';

    describe('serializeCrops / parseCrops with modifiers (Phase 38 MOD-01)', () => {
      it('round-trip plant legacy sans modifier (6 champs, pas de trailing :)', () => {
        const plant: PlantedCrop = {
          plotIndex: 0,
          cropId: 'carrot',
          currentStage: 2,
          tasksCompleted: 1,
          plantedAt: '2026-04-18',
        };
        const csv = serializeCrops([plant]);
        expect(csv).toBe('0:carrot:2:1:2026-04-18:');
        const parsed = parseCrops(csv);
        expect(parsed).toHaveLength(1);
        expect(parsed[0]).toEqual({ ...plant, isGolden: false });
        expect('modifiers' in parsed[0]).toBe(false);
      });

      it('round-trip plant avec wager complet deep-equal', () => {
        const wager: WagerModifier = {
          sporeeId: 'sp-lucas-abc-123',
          duration: 'engage',
          multiplier: 1.7,
          appliedAt: '2026-04-18',
          sealerProfileId: 'parent-lucas',
        };
        const plant: PlantedCrop = {
          plotIndex: 2,
          cropId: 'tomato',
          currentStage: 1,
          tasksCompleted: 0.5,
          plantedAt: '2026-04-18',
          isGolden: false,
          modifiers: { wager },
        };
        const csv = serializeCrops([plant]);
        const parsed = parseCrops(csv);
        expect(parsed).toHaveLength(1);
        expect(parsed[0].modifiers?.wager).toEqual(wager);
      });

      it('parse CSV legacy 6 champs (pré-v1.7) → modifiers undefined', () => {
        const legacyCsv = '0:carrot:2:1:2026-04-18:,1:wheat:0:0:2026-04-17:1';
        const parsed = parseCrops(legacyCsv);
        expect(parsed).toHaveLength(2);
        expect(parsed[0].modifiers).toBeUndefined();
        expect('modifiers' in parsed[0]).toBe(false);
        expect(parsed[1].isGolden).toBe(true);
        expect(parsed[1].modifiers).toBeUndefined();
      });

      it('parse 7e fragment vide string → modifiers undefined', () => {
        const csv = '0:carrot:2:1:2026-04-18::';
        const parsed = parseCrops(csv);
        expect(parsed[0].modifiers).toBeUndefined();
      });

      it('round-trip sporeeId avec tirets uuid-style', () => {
        const plant: PlantedCrop = {
          plotIndex: 0,
          cropId: 'corn',
          currentStage: 0,
          tasksCompleted: 0,
          plantedAt: '2026-04-18',
          modifiers: {
            wager: {
              sporeeId: 'sp-emma-d4e5-f678',
              duration: 'sprint',
              multiplier: 2.5,
              appliedAt: '2026-04-18',
              sealerProfileId: 'parent-emma',
            },
          },
        };
        const parsed = parseCrops(serializeCrops([plant]));
        expect(parsed[0].modifiers?.wager?.sporeeId).toBe('sp-emma-d4e5-f678');
      });

      it('mix plants legacy + plants avec wager dans un même CSV', () => {
        const legacy: PlantedCrop = { plotIndex: 0, cropId: 'carrot', currentStage: 1, tasksCompleted: 0, plantedAt: '2026-04-18' };
        const withWager: PlantedCrop = {
          plotIndex: 1, cropId: 'tomato', currentStage: 2, tasksCompleted: 1, plantedAt: '2026-04-18',
          modifiers: { wager: { sporeeId: 'sp-1', duration: 'chill', multiplier: 1.3, appliedAt: '2026-04-18', sealerProfileId: 'parent1' } },
        };
        const parsed = parseCrops(serializeCrops([legacy, withWager]));
        expect(parsed).toHaveLength(2);
        expect(parsed[0].modifiers).toBeUndefined();
        expect(parsed[1].modifiers?.wager?.duration).toBe('chill');
      });

      it('encodeModifiers({}) retourne empty string', () => {
        expect(encodeModifiers({})).toBe('');
        expect(encodeModifiers(undefined)).toBe('');
      });

      it('decodeModifiers defensif sur input invalide', () => {
        expect(decodeModifiers('')).toBeUndefined();
        expect(decodeModifiers(undefined)).toBeUndefined();
        expect(decodeModifiers('{invalide')).toBeUndefined();
      });

      it('JSON modifiers n inclut JAMAIS de "," ni ":" bruts (escape pipe/§)', () => {
        const mods: FarmCropModifiers = {
          wager: { sporeeId: 'sp-x', duration: 'engage', multiplier: 1.7, appliedAt: '2026-04-18', sealerProfileId: 'p1' },
        };
        const encoded = encodeModifiers(mods);
        expect(encoded.includes(',')).toBe(false);
        expect(encoded.includes(':')).toBe(false);
        expect(encoded.includes('|')).toBe(true);
        expect(encoded.includes('§')).toBe(true);
      });
    });
    ```

    Respecter la privacy : utiliser uniquement `lucas`, `emma`, `parent1`, `parent-lucas` (génériques, conformes CLAUDE.md).
  </action>
  <verify>
    <automated>npx jest --no-coverage lib/__tests__/farm-engine.test.ts</automated>
  </verify>
  <done>
    - Nouveau describe "serializeCrops / parseCrops with modifiers (Phase 38 MOD-01)" ajouté au fichier
    - 9 it() couvrent round-trip legacy, round-trip wager, backward-compat 6-champs, fragment vide, sporeeId uuid, mix, encode vide, decode défensif, escape sans collision
    - `npx jest --no-coverage lib/__tests__/farm-engine.test.ts` passe entièrement
    - Privacy respectée (Lucas/Emma/parent1/parent-lucas uniquement)
  </done>
  <acceptance_criteria>
    - `grep -c "describe('serializeCrops / parseCrops with modifiers" lib/__tests__/farm-engine.test.ts` retourne 1
    - `grep -cE "^\s*it\('(round-trip plant legacy|round-trip plant avec wager|parse CSV legacy|parse 7e fragment vide|round-trip sporeeId|mix plants legacy|encodeModifiers|decodeModifiers defensif|JSON modifiers)" lib/__tests__/farm-engine.test.ts` retourne 9
    - `npx jest --no-coverage lib/__tests__/farm-engine.test.ts --silent` exit 0
    - `grep -i "dupont\|durand\|martin" lib/__tests__/farm-engine.test.ts` retourne 0 (privacy)
  </acceptance_criteria>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` clean (hors erreurs pré-existantes MemoryEditor.tsx, cooklang.ts, useVault.ts)
2. `npx jest --no-coverage lib/__tests__/farm-engine.test.ts` passe entièrement
3. CSV legacy pré-v1.7 parse sans erreur : créer manuellement `parseCrops('0:carrot:2:1:2026-04-18:')` → `[{plotIndex:0, cropId:'carrot', ..., isGolden:false}]` sans propriété `modifiers`
</verification>

<success_criteria>
- `PlantedCrop.modifiers?: FarmCropModifiers` déclaré + exporté
- `WagerModifier`, `FarmCropModifiers`, `WagerDuration`, `WagerMultiplier` exportés depuis types.ts
- `encodeModifiers`/`decodeModifiers` exportés depuis farm-engine.ts
- `serializeCrops` ne casse PAS le format legacy (pas de trailing `:` injustifié)
- `parseCrops` tolère 6 ET 7 fragments
- `CACHE_VERSION = 4` + `vault-cache-v4.json`
- Suite Jest 9 nouveaux tests passent, `tsc --noEmit` clean
</success_criteria>

<output>
Après complétion, créer `.planning/phases/38-fondation-modifiers-conomie-spor-e/38-01-SUMMARY.md` documentant :
- Stratégie pipe-escape JSON (`,` → `|`, `:` → `§`) retenue vs base64/ligne séparée
- Ligne exacte où `encodeModifiers` apparaît (farm-engine.ts)
- Types exportés (WagerModifier, FarmCropModifiers, WagerDuration, WagerMultiplier)
- CACHE_VERSION 3→4 confirmé
- Nombre de tests ajoutés (9) et résultat `npx jest`
</output>
