---
phase: 38-fondation-modifiers-conomie-spor-e
plan: 03
type: execute
wave: 3
depends_on: [38-01, 38-02]
files_modified:
  - lib/types.ts
  - lib/parser.ts
  - hooks/useFarm.ts
  - hooks/useExpeditions.ts
  - hooks/useGamification.ts
  - lib/__tests__/farm-parser.test.ts
autonomous: true
requirements: [SPOR-08, SPOR-09]
requirements_addressed: [SPOR-08, SPOR-09]

must_haves:
  truths:
    - "4 nouveaux champs profil ferme (sporeeCount, sporeeShopBoughtToday, sporeeShopLastResetDate, sporeeOnboardingGiftClaimed) round-trip via parseFarmProfile/serializeFarmProfile"
    - "Récolte : Math.random < 0.03 (base) déclenche appel tryIncrementSporeeCount ; overflow → toast 'Inventaire Sporée plein'"
    - "Expedition Pousse+ : Math.random < 0.05 déclenche increment inventaire Sporée ; overflow → toast"
    - "Transition arbuste→arbre (detectEvolution côté useGamification) : +1 Sporée si alreadyClaimed=false, flag passe true après succès uniquement"
    - "Zéro régression TS (npx tsc --noEmit clean hors pré-existantes) + tests existants passent"
  artifacts:
    - path: "lib/types.ts"
      provides: "4 nouveaux champs FarmProfileData"
      contains: "sporeeCount?: number"
    - path: "lib/parser.ts"
      provides: "parseFarmProfile + serializeFarmProfile étendus (4 clés sporee_*)"
      contains: "sporee_count"
    - path: "hooks/useFarm.ts"
      provides: "Drop Sporée post-harvest + toast overflow"
      contains: "rollSporeeDropOnHarvest"
    - path: "hooks/useExpeditions.ts"
      provides: "Drop Sporée post-expedition + toast overflow"
      contains: "rollSporeeDropOnExpedition"
    - path: "hooks/useGamification.ts"
      provides: "Cadeau onboarding à transition arbuste→arbre"
      contains: "shouldGiftOnboardingSporee"
    - path: "lib/__tests__/farm-parser.test.ts"
      provides: "Tests parser farm avec sporee_*"
      contains: "sporee_count"
  key_links:
    - from: "hooks/useFarm.ts harvest()"
      to: "lib/mascot/sporee-economy.ts rollSporeeDropOnHarvest + tryIncrementSporeeCount"
      via: "import + post-rollSeedDrop call"
      pattern: "rollSporeeDropOnHarvest\\(classifyHarvestTier"
    - from: "hooks/useExpeditions.ts collectExpedition()"
      to: "lib/mascot/sporee-economy.ts rollSporeeDropOnExpedition"
      via: "import + post-rollExpeditionLoot call"
      pattern: "rollSporeeDropOnExpedition\\(exp.difficulty"
    - from: "hooks/useGamification.ts completeTask()"
      to: "lib/mascot/sporee-economy.ts shouldGiftOnboardingSporee + detectEvolution"
      via: "post-addPoints level comparison"
      pattern: "shouldGiftOnboardingSporee"
---

<objective>
Câbler le moteur pur (Plan 02) dans le vault : étendre `FarmProfileData` avec 4 champs `sporee_*`, étendre `parseFarmProfile`/`serializeFarmProfile` pour round-trip, puis brancher les 3 sources de drop dans leurs hooks respectifs (`useFarm.harvest`, `useExpeditions.collectExpedition`, `useGamification.completeTask`). Toast "Inventaire Sporée plein" via `useToast()` sur overflow. La 4e source (shop) n'est PAS câblée en UI en Phase 38 — l'engine `canBuySporee` est prêt pour consommation Phase 40.

Purpose: Livrer l'économie Sporée fonctionnelle côté data/hooks — prêt pour consommation UI en Phase 40.
Output: 4 champs profil persistés + drops opérationnels récolte/expedition/cadeau + toast overflow français.
</objective>

<context>
@CLAUDE.md
@.planning/phases/38-fondation-modifiers-conomie-spor-e/38-CONTEXT.md
@.planning/phases/38-fondation-modifiers-conomie-spor-e/38-RESEARCH.md
@lib/types.ts
@lib/parser.ts
@hooks/useFarm.ts
@hooks/useExpeditions.ts
@hooks/useGamification.ts
@lib/mascot/sporee-economy.ts
@contexts/ToastContext.tsx

<interfaces>
FarmProfileData (lib/types.ts:611-646) — extension requise :
```ts
// Ajouter à la fin de l'interface, avant la fermeture
// Phase 38 — économie Sporée (per-profil)
sporeeCount?: number;                    // 0-10, default undefined (absent)
sporeeShopBoughtToday?: number;          // 0-2, default undefined
sporeeShopLastResetDate?: string;        // YYYY-MM-DD local, default undefined
sporeeOnboardingGiftClaimed?: boolean;   // true une fois le cadeau stade 3 donné
```

ToastContext API (contexts/ToastContext.tsx) :
```ts
const { showToast } = useToast();
showToast('Inventaire Sporée plein', 'error');
```

Helpers sporee-economy.ts à utiliser (import depuis '../lib/mascot/sporee-economy') :
- `classifyHarvestTier(cropId): 'base'|'rare'|'expedition'`
- `rollSporeeDropOnHarvest(tier): boolean`
- `rollSporeeDropOnExpedition(difficulty): boolean`
- `tryIncrementSporeeCount(currentCount, qty=1): { accepted, newCount, reason? }`
- `shouldGiftOnboardingSporee({ fromStage, toStage, alreadyClaimed }): boolean`
- `detectEvolution(oldLevel, newLevel): EvolutionEvent` (import depuis '../lib/mascot/engine')

Frontmatter keys utilisées (lib/parser.ts parseFarmProfile/serializeFarmProfile) :
- `sporee_count: 3`
- `sporee_shop_bought_today: 1`
- `sporee_shop_last_reset: 2026-04-18`
- `sporee_onboarding_gift_claimed: true`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tâche 1 : Étendre FarmProfileData + parseFarmProfile/serializeFarmProfile + tests round-trip</name>
  <files>lib/types.ts, lib/parser.ts, lib/__tests__/farm-parser.test.ts</files>
  <read_first>
    - lib/types.ts (lignes 611-646 pour FarmProfileData)
    - lib/parser.ts (lignes 613-755 parseFarmProfile/serializeFarmProfile)
    - lib/__tests__/ (vérifier si farm-parser.test.ts existe, sinon créer)
  </read_first>
  <behavior>
    - `parseFarmProfile(content avec sporee_count: 3)` retourne `sporeeCount: 3`
    - `serializeFarmProfile({...data, sporeeCount: 3})` inclut ligne `sporee_count: 3`
    - `sporeeCount: undefined` → ligne absente du serialize (parity avec autres champs optionnels)
    - Round-trip deep-equal pour les 4 nouveaux champs
    - Parse profil legacy (pré-v1.7, sans sporee_*) : 4 champs sont `undefined`
    - `sporeeOnboardingGiftClaimed: true` sérialisé comme `sporee_onboarding_gift_claimed: true`, parsé back en boolean true
  </behavior>
  <action>
    **1. Étendre FarmProfileData** dans `lib/types.ts` — ajouter AVANT la fermeture `}` ligne 646 :
    ```ts
      // Phase 38 — économie Sporée per-profil (MOD/SPOR v1.7)
      sporeeCount?: number;                    // inventaire 0-10, default undefined
      sporeeShopBoughtToday?: number;          // achats shop du jour 0-2
      sporeeShopLastResetDate?: string;        // YYYY-MM-DD local — dernier reset cap quotidien
      sporeeOnboardingGiftClaimed?: boolean;   // true = cadeau stade 3 déjà donné (anti-rejeu)
    ```

    **2. Étendre parseFarmProfile** dans `lib/parser.ts` (bloc return ligne 648-691, avant `};` fermeture) — ajouter :
    ```ts
        // Phase 38 — économie Sporée
        sporeeCount: props.sporee_count !== undefined ? parseInt(props.sporee_count, 10) : undefined,
        sporeeShopBoughtToday: props.sporee_shop_bought_today !== undefined ? parseInt(props.sporee_shop_bought_today, 10) : undefined,
        sporeeShopLastResetDate: props.sporee_shop_last_reset || undefined,
        sporeeOnboardingGiftClaimed: props.sporee_onboarding_gift_claimed === 'true' ? true : undefined,
    ```

    **3. Étendre serializeFarmProfile** dans `lib/parser.ts` — ajouter APRÈS le bloc `daily_deal_purchases` ligne 749-752, AVANT `return lines.join('\n') + '\n';` :
    ```ts
      // Phase 38 — économie Sporée (ne sérialiser que si non-default)
      if (typeof data.sporeeCount === 'number' && data.sporeeCount > 0) {
        lines.push(`sporee_count: ${data.sporeeCount}`);
      }
      if (typeof data.sporeeShopBoughtToday === 'number' && data.sporeeShopBoughtToday > 0) {
        lines.push(`sporee_shop_bought_today: ${data.sporeeShopBoughtToday}`);
      }
      if (data.sporeeShopLastResetDate) {
        lines.push(`sporee_shop_last_reset: ${data.sporeeShopLastResetDate}`);
      }
      if (data.sporeeOnboardingGiftClaimed === true) {
        lines.push(`sporee_onboarding_gift_claimed: true`);
      }
    ```

    **4. Tests** — ajouter fichier `lib/__tests__/farm-parser.test.ts` (nouveau) :

    ```ts
    /**
     * Phase 38 (SPOR-13) — Round-trip farm parser avec champs sporee_*.
     * Privacy : noms génériques (Lucas/Emma/parent1).
     */
    import { parseFarmProfile, serializeFarmProfile } from '../parser';
    import type { FarmProfileData } from '../types';

    describe('parseFarmProfile/serializeFarmProfile avec champs Sporée (Phase 38)', () => {
      const baseData: FarmProfileData = {
        mascotDecorations: [],
        mascotInhabitants: [],
        mascotPlacements: {},
      };

      it('parse profil legacy (pré-v1.7) : 4 champs sporee undefined', () => {
        const content = '# Farm — Lucas\n\nfarm_crops: 0:carrot:2:1:2026-04-18:\n';
        const parsed = parseFarmProfile(content);
        expect(parsed.sporeeCount).toBeUndefined();
        expect(parsed.sporeeShopBoughtToday).toBeUndefined();
        expect(parsed.sporeeShopLastResetDate).toBeUndefined();
        expect(parsed.sporeeOnboardingGiftClaimed).toBeUndefined();
      });

      it('parse sporee_count: 3', () => {
        const content = '# Farm — Lucas\n\nsporee_count: 3\n';
        expect(parseFarmProfile(content).sporeeCount).toBe(3);
      });

      it('parse sporee_shop_bought_today: 2', () => {
        const content = '# Farm — Lucas\n\nsporee_shop_bought_today: 2\n';
        expect(parseFarmProfile(content).sporeeShopBoughtToday).toBe(2);
      });

      it('parse sporee_shop_last_reset: 2026-04-18', () => {
        const content = '# Farm — Lucas\n\nsporee_shop_last_reset: 2026-04-18\n';
        expect(parseFarmProfile(content).sporeeShopLastResetDate).toBe('2026-04-18');
      });

      it('parse sporee_onboarding_gift_claimed: true', () => {
        const content = '# Farm — Lucas\n\nsporee_onboarding_gift_claimed: true\n';
        expect(parseFarmProfile(content).sporeeOnboardingGiftClaimed).toBe(true);
      });

      it('serialize sporeeCount=3 inclut ligne sporee_count: 3', () => {
        const content = serializeFarmProfile('Lucas', { ...baseData, sporeeCount: 3 });
        expect(content).toContain('sporee_count: 3');
      });

      it('serialize sporeeCount=0 ou undefined OMET la ligne', () => {
        const content0 = serializeFarmProfile('Lucas', { ...baseData, sporeeCount: 0 });
        expect(content0).not.toContain('sporee_count:');
        const contentU = serializeFarmProfile('Lucas', baseData);
        expect(contentU).not.toContain('sporee_count:');
      });

      it('serialize sporeeOnboardingGiftClaimed=false OMET la ligne (pas de bruit)', () => {
        const content = serializeFarmProfile('Lucas', { ...baseData, sporeeOnboardingGiftClaimed: false as any });
        expect(content).not.toContain('sporee_onboarding_gift_claimed');
      });

      it('serialize sporeeOnboardingGiftClaimed=true écrit la ligne', () => {
        const content = serializeFarmProfile('Lucas', { ...baseData, sporeeOnboardingGiftClaimed: true });
        expect(content).toContain('sporee_onboarding_gift_claimed: true');
      });

      it('round-trip deep-equal des 4 champs sporee', () => {
        const original: FarmProfileData = {
          ...baseData,
          sporeeCount: 7,
          sporeeShopBoughtToday: 1,
          sporeeShopLastResetDate: '2026-04-18',
          sporeeOnboardingGiftClaimed: true,
        };
        const content = serializeFarmProfile('Emma', original);
        const parsed = parseFarmProfile(content);
        expect(parsed.sporeeCount).toBe(7);
        expect(parsed.sporeeShopBoughtToday).toBe(1);
        expect(parsed.sporeeShopLastResetDate).toBe('2026-04-18');
        expect(parsed.sporeeOnboardingGiftClaimed).toBe(true);
      });
    });
    ```
  </action>
  <verify>
    <automated>npx jest --no-coverage lib/__tests__/farm-parser.test.ts</automated>
  </verify>
  <done>
    - 4 champs ajoutés à FarmProfileData (lib/types.ts)
    - parseFarmProfile lit sporee_count, sporee_shop_bought_today, sporee_shop_last_reset, sporee_onboarding_gift_claimed
    - serializeFarmProfile écrit conditionnellement (> 0 / truthy / === true)
    - Tests farm-parser.test.ts : 10 it() tous verts
    - `npx tsc --noEmit` clean
  </done>
  <acceptance_criteria>
    - `grep -c "sporeeCount?: number" lib/types.ts` retourne 1
    - `grep -c "sporeeShopBoughtToday?: number" lib/types.ts` retourne 1
    - `grep -c "sporeeShopLastResetDate?: string" lib/types.ts` retourne 1
    - `grep -c "sporeeOnboardingGiftClaimed?: boolean" lib/types.ts` retourne 1
    - `grep -c "sporee_count" lib/parser.ts` retourne ≥2 (parse + serialize)
    - `grep -c "sporee_shop_last_reset" lib/parser.ts` retourne ≥2
    - `grep -c "sporee_onboarding_gift_claimed" lib/parser.ts` retourne ≥2
    - `npx jest --no-coverage lib/__tests__/farm-parser.test.ts --silent` exit 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Tâche 2 : Câbler drop Sporée dans useFarm.harvest (post-rollSeedDrop) + toast overflow</name>
  <files>hooks/useFarm.ts</files>
  <read_first>
    - hooks/useFarm.ts (lignes 293-400 pour la fonction `harvest`)
    - lib/mascot/sporee-economy.ts (exports de classifyHarvestTier, rollSporeeDropOnHarvest, tryIncrementSporeeCount)
    - contexts/ToastContext.tsx (useToast API)
  </read_first>
  <action>
    **1. Ajouter imports en tête de `hooks/useFarm.ts`** (après les imports farm-engine existants ligne 11) :
    ```ts
    import { classifyHarvestTier, rollSporeeDropOnHarvest, tryIncrementSporeeCount } from '../lib/mascot/sporee-economy';
    import { useToast } from '../contexts/ToastContext';
    ```

    **2. Récupérer `showToast`** dans le corps du hook `useFarm` (ajouter au début de la fonction, avec les autres useXxx calls) :
    ```ts
    const { showToast } = useToast();
    ```

    **3. Modifier `harvest`** dans `hooks/useFarm.ts` — après la ligne `const seedDrop = rollSeedDrop(result.harvestedCropId);` (ligne ~327), insérer le bloc Sporée AVANT le check `if (wasGoldenEffect)` :

    ```ts
        // Phase 38 (SPOR-08) — Drop Sporée post-récolte
        // Tier classifié via CROP_CATALOG (base 3% / rare 8% / expedition 15%)
        const sporeeTier = classifyHarvestTier(result.harvestedCropId);
        let sporeeDropped = false;
        let sporeeRefused = false;
        if (rollSporeeDropOnHarvest(sporeeTier)) {
          const currentSporee = profile.sporeeCount ?? 0;
          const inc = tryIncrementSporeeCount(currentSporee, 1);
          if (inc.accepted) {
            profile.sporeeCount = inc.newCount;
            sporeeDropped = true;
          } else {
            sporeeRefused = true;
          }
        }
    ```

    **4. Inclure `sporee_count` dans les écritures vault** — deux branches à modifier :

    Branche `wasGoldenEffect` (~ligne 331-347) — étendre `updatedProfile` spread :
    ```ts
          const updatedProfile = {
            ...profile,
            farmCrops: serializeCrops(result.crops),
            harvestInventory: updatedHarvestInv,
            ...(seedDrop ? { farmRareSeeds: { ...(profile.farmRareSeeds ?? {}), [seedDrop.seedId]: ((profile.farmRareSeeds ?? {})[seedDrop.seedId] ?? 0) + 1 } } : {}),
            ...(sporeeDropped ? { sporeeCount: profile.sporeeCount } : {}),
          };
    ```

    Branche standard (~ligne 351-362 `fieldsToWrite`) — ajouter conditionnellement :
    ```ts
        if (sporeeDropped && typeof profile.sporeeCount === 'number') {
          fieldsToWrite.sporee_count = String(profile.sporeeCount);
        }
    ```

    **5. Afficher le toast** — juste après `await refreshFarm(profileId);` (dans les deux branches) :
    ```ts
        if (sporeeRefused) {
          showToast('Inventaire Sporée plein', 'error');
        }
    ```

    **6. Note sur writeProfileFields** : vérifier que `writeProfileFields` (déjà utilisé dans ce hook) écrit bien la clé brute — si besoin, utiliser le helper existant qui appelle `serializeFarmProfile` (cf. pattern existant ligne 365 `await writeProfileFields(profileId, fieldsToWrite);`). Si `writeProfileFields` ne supporte pas `sporee_count` brut, alternative : reconstruire via `serializeFarmProfile` comme dans la branche `wasGoldenEffect`.

    Vérifier au passage que `writeProfileFields` est bien générique sur les clés (grep local avant d'écrire). Si ce n'est pas générique, basculer la branche standard sur le pattern `serializeFarmProfile` complet (comme la branche `wasGoldenEffect`).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep "useFarm.ts" | grep -v "pre-existing" | wc -l</automated>
  </verify>
  <done>
    - Imports ajoutés en tête de useFarm.ts
    - Hook `useToast` appelé dans le corps de useFarm
    - Bloc Sporée inséré après `rollSeedDrop` dans `harvest`
    - Les deux branches d'écriture (wasGoldenEffect + standard) persistent `sporeeCount` si droppé
    - Toast "Inventaire Sporée plein" déclenché sur overflow refus
    - `npx tsc --noEmit` clean sur useFarm.ts (hors erreurs pré-existantes connues)
  </done>
  <acceptance_criteria>
    - `grep -c "classifyHarvestTier\|rollSporeeDropOnHarvest\|tryIncrementSporeeCount" hooks/useFarm.ts` retourne ≥3
    - `grep -c "useToast" hooks/useFarm.ts` retourne ≥2 (import + call)
    - `grep "'Inventaire Sporée plein'" hooks/useFarm.ts` trouve la chaîne exacte
    - `grep "sporeeCount" hooks/useFarm.ts` retourne ≥3 occurrences
    - `npx tsc --noEmit 2>&1 | grep -E "hooks/useFarm\.ts.*error"` retourne rien de NOUVEAU vs baseline (erreur pré-existante useVault.ts exemptée)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Tâche 3 : Câbler drop Sporée dans useExpeditions.collectExpedition + câbler cadeau onboarding dans useGamification</name>
  <files>hooks/useExpeditions.ts, hooks/useGamification.ts</files>
  <read_first>
    - hooks/useExpeditions.ts (lignes 230-295 pour `collectExpedition`)
    - hooks/useGamification.ts (lignes 100-180 pour `completeTask` et manipulation profile.points → profile.level)
    - lib/mascot/engine.ts (ligne 51 detectEvolution signature + getTreeStage)
    - lib/mascot/sporee-economy.ts (rollSporeeDropOnExpedition, tryIncrementSporeeCount, shouldGiftOnboardingSporee)
    - contexts/ToastContext.tsx (useToast)
  </read_first>
  <action>
    **PARTIE A — useExpeditions (drop expedition 5%) :**

    1. Ajouter imports en tête de `hooks/useExpeditions.ts` :
       ```ts
       import { rollSporeeDropOnExpedition, tryIncrementSporeeCount } from '../lib/mascot/sporee-economy';
       import { useToast } from '../contexts/ToastContext';
       ```

    2. Récupérer `showToast` dans le corps du hook useExpeditions :
       ```ts
       const { showToast } = useToast();
       ```

    3. Dans `collectExpedition`, après la ligne 244 `const loot = rollExpeditionLoot(exp.difficulty, outcome);`, ajouter — rouler la Sporée **indépendamment de l'outcome** (per 38-RESEARCH Open Q2, aligné "expedition = loot à part") :
       ```ts
       // Phase 38 (SPOR-08) — Drop Sporée post-expedition (5% sur Pousse+)
       // Indépendant de l'outcome : une mission ratée peut donner une Sporée (loot séparé).
       let sporeeRefused = false;
       if (rollSporeeDropOnExpedition(exp.difficulty)) {
         const currentSporee = farm.sporeeCount ?? 0;
         const inc = tryIncrementSporeeCount(currentSporee, 1);
         if (inc.accepted) {
           farm.sporeeCount = inc.newCount;
         } else {
           sporeeRefused = true;
         }
       }
       ```

    4. Après `await refreshFarm(currentProfile.id);` (ligne ~291), avant `return { outcome, loot };` — afficher toast :
       ```ts
       if (sporeeRefused) {
         showToast('Inventaire Sporée plein', 'error');
       }
       ```

       `farm.sporeeCount` est déjà inclus dans `serializeFarmProfile(profileName, farm)` ligne 290 via l'extension de Tâche 1.

    **PARTIE B — useGamification (cadeau onboarding arbuste→arbre) :**

    5. Ajouter imports en tête de `hooks/useGamification.ts` (après les imports mascot existants) :
       ```ts
       import { detectEvolution } from '../lib/mascot/engine';
       import { shouldGiftOnboardingSporee, tryIncrementSporeeCount } from '../lib/mascot/sporee-economy';
       import { useToast } from '../contexts/ToastContext';
       import { parseFarmProfile, serializeFarmProfile } from '../lib/parser';
       // Helper path farm file — aligner avec le pattern existant (voir autres hooks farm)
       const farmFile = (profileId: string) => `04 - Gamification/farm-${profileId}.md`;
       ```
       **NOTE** : vérifier d'abord le chemin exact `farmFile` utilisé dans useFarm.ts (ligne ~11-15) et aligner. Si un helper existe déjà, l'importer au lieu de redéfinir.

    6. Récupérer `showToast` (déjà peut-être présent dans useGamification — vérifier) :
       ```ts
       const { showToast } = useToast();
       ```

    7. Dans `completeTask`, juste APRÈS le bloc `awardTaskCompletion` qui calcule `updatedProfile.points` (après ligne 139, AVANT `// Update data`), détecter l'évolution + gate cadeau :
       ```ts
       // Phase 38 (SPOR-08) — Cadeau onboarding Sporée à la transition arbuste → arbre
       try {
         const evolution = detectEvolution(profile.level, updatedProfile.level);
         if (evolution.evolved && shouldGiftOnboardingSporee({
           fromStage: evolution.fromStage,
           toStage: evolution.toStage,
           alreadyClaimed: false, // sera vérifié post-read du farm file
         })) {
           // Re-read farm pour source of truth fraîche (évite race condition stale VaultContext)
           const farmPath = farmFile(profile.id);
           const farmContent = await vault!.readFile(farmPath).catch(() => '');
           const farmData = parseFarmProfile(farmContent);
           if (!farmData.sporeeOnboardingGiftClaimed) {
             const currentSporee = farmData.sporeeCount ?? 0;
             const inc = tryIncrementSporeeCount(currentSporee, 1);
             if (inc.accepted) {
               farmData.sporeeCount = inc.newCount;
               farmData.sporeeOnboardingGiftClaimed = true;
               await vault!.writeFile(farmPath, serializeFarmProfile(profile.name, farmData));
             } else {
               // Inventaire plein : NE PAS marquer claimed, le cadeau re-tentera au prochain level-up
               // Cohérent avec "zéro perte silencieuse" (38-RESEARCH Open Q1)
               showToast('Inventaire Sporée plein', 'error');
             }
           }
         }
       } catch {
         // Cadeau Sporée — non-critical, ne doit pas casser le flow de tâche
         if (__DEV__) console.warn('Cadeau onboarding Sporée — non-critical');
       }
       ```

    8. Le type `Profile` doit exposer `level` (vérifier rapidement, il est calculé à partir de `points`). Si `updatedProfile.level` n'est pas mis à jour automatiquement par `awardTaskCompletion`, dériver via la fonction level-from-points existante. Grep `level` dans `packages/core/src/gamification/` pour trouver la dérivation (à appliquer manuellement si besoin).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "(hooks/useExpeditions|hooks/useGamification)\.ts.*error" | grep -v "pre-existing" | wc -l</automated>
  </verify>
  <done>
    - useExpeditions : import + appel `rollSporeeDropOnExpedition(exp.difficulty)` + `tryIncrementSporeeCount` + toast overflow
    - useGamification : import + `detectEvolution` + `shouldGiftOnboardingSporee` + re-read farm avant mutation + flag persisté
    - Flag `sporeeOnboardingGiftClaimed` reste `false` si inventaire plein au moment du cadeau (re-tentable)
    - Toast français "Inventaire Sporée plein" câblé dans les 2 hooks
    - `npx tsc --noEmit` clean
  </done>
  <acceptance_criteria>
    - `grep -c "rollSporeeDropOnExpedition" hooks/useExpeditions.ts` retourne ≥2 (import + call)
    - `grep -c "tryIncrementSporeeCount" hooks/useExpeditions.ts` retourne ≥1
    - `grep "'Inventaire Sporée plein'" hooks/useExpeditions.ts` trouve la chaîne
    - `grep -c "detectEvolution" hooks/useGamification.ts` retourne ≥2
    - `grep -c "shouldGiftOnboardingSporee" hooks/useGamification.ts` retourne ≥1
    - `grep -c "sporeeOnboardingGiftClaimed" hooks/useGamification.ts` retourne ≥1
    - `grep "'Inventaire Sporée plein'" hooks/useGamification.ts` trouve la chaîne
    - `npx tsc --noEmit` n'ajoute AUCUNE erreur nouvelle vs baseline
    - `npx jest --no-coverage --silent` clean (aucune régression sur la suite existante)
  </acceptance_criteria>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` clean (hors MemoryEditor/cooklang/useVault pré-existantes)
2. `npx jest --no-coverage` — la suite complète passe (farm-engine.test.ts + sporee-economy.test.ts + farm-parser.test.ts + tests existants)
3. Smoke path manuel (optionnel mais recommandé) : simuler harvest carrot avec Math.random mocké à 0.02 → profile.sporeeCount incrémenté dans farm-{id}.md
</verification>

<success_criteria>
- 4 champs sporee_* persistés et round-trip dans farm-{profileId}.md
- Récolte : 3% base / 8% rare / 15% expedition déclenchent increment inventaire Sporée
- Expedition Pousse+ : 5% drop indépendamment de l'outcome
- Transition arbuste→arbre : +1 Sporée si pas déjà claimed, flag passe true après succès uniquement
- Toast français "Inventaire Sporée plein" sur overflow dans les 3 sites
- Aucune régression TS/Jest
</success_criteria>

<output>
Après complétion, créer `.planning/phases/38-fondation-modifiers-conomie-spor-e/38-03-SUMMARY.md` documentant :
- Numéros de lignes EXACTS d'insertion dans useFarm.ts, useExpeditions.ts, useGamification.ts
- Résolution Open Q1 (flag reste false si overflow au cadeau)
- Résolution Open Q2 (roll Sporée indépendant de l'outcome expedition)
- Résolution Open Q4 (getLocalDateKey utilisé, pas toISOString UTC)
- État global Phase 38 : économie Sporée opérationnelle côté data, UI Phase 40 peut consommer
</output>
