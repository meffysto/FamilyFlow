---
phase: 02-write-safety-couleurs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/vault.ts
  - lib/gamification/rewards.ts
  - lib/__tests__/write-queue.test.ts
autonomous: true
requirements:
  - ARCH-01
  - GAME-01

must_haves:
  truths:
    - "Les ecritures concurrentes sur gamification.md ne perdent plus de donnees"
    - "toggleTask sur le meme fichier en succession rapide ne perd aucune modification"
    - "Un modele XP budget est documente avec des valeurs calibrees"
  artifacts:
    - path: "lib/vault.ts"
      provides: "Write queue per-file via _writeQueues Map + enqueueWrite()"
      contains: "_writeQueues"
    - path: "lib/vault.ts"
      provides: "toggleTask et appendTask proteges par enqueueWrite"
      contains: "enqueueWrite"
    - path: "lib/gamification/rewards.ts"
      provides: "XP_BUDGET export avec model documente"
      exports: ["XP_BUDGET"]
    - path: "lib/__tests__/write-queue.test.ts"
      provides: "Tests unitaires pour la write queue"
      contains: "enqueueWrite"
  key_links:
    - from: "lib/vault.ts"
      to: "hooks/useGamification.ts"
      via: "writeFile serialisee par enqueueWrite"
      pattern: "enqueueWrite"
    - from: "lib/vault.ts"
      to: "lib/vault.ts toggleTask"
      via: "read-modify-write body entier dans enqueueWrite"
      pattern: "enqueueWrite.*relativePath"
---

<objective>
Implementer la write queue per-file dans VaultManager et documenter le modele XP budget.

Purpose: Prevenir la perte de donnees lors d'ecritures concurrentes (completion rapide de taches) et fournir un cadre de reference pour toute future source de recompense XP.
Output: VaultManager avec write queue fonctionnelle, tests unitaires, et XP_BUDGET documente dans rewards.ts.
</objective>

<execution_context>
@/Users/gabrielwaltio/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gabrielwaltio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-write-safety-couleurs/02-RESEARCH.md

<interfaces>
<!-- VaultManager class in lib/vault.ts -->
From lib/vault.ts:
```typescript
export class VaultManager {
  vaultPath: string;
  constructor(vaultPath: string);
  async readFile(relativePath: string): Promise<string>;
  async writeFile(relativePath: string, content: string): Promise<void>;
  async toggleTask(relativePath: string, lineIndex: number, completed: boolean): Promise<void>;
  async appendTask(relativePath: string, section: string | null, taskText: string): Promise<void>;
}
```

From lib/gamification/rewards.ts:
```typescript
export const GAMI_CONFIG_KEY = 'gami_config_v1';
export const POINTS_PER_TASK = 10;
export const LOOT_THRESHOLD = { enfant: 50, ado: 75, adulte: 100 };
export const PITY_THRESHOLD = 5;
export const STREAK_MILESTONES = [...];
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write queue per-file dans VaultManager</name>
  <files>lib/vault.ts, lib/__tests__/write-queue.test.ts</files>
  <read_first>lib/vault.ts, hooks/useGamification.ts</read_first>
  <behavior>
    - Test 1: Deux writeFile concurrents sur le meme path s'executent en serie (pas de perte)
    - Test 2: Deux writeFile sur des paths differents s'executent en parallele
    - Test 3: toggleTask enveloppe son read-modify-write dans enqueueWrite (pas de race)
    - Test 4: appendTask enveloppe son read-modify-write dans enqueueWrite
    - Test 5: La queue se nettoie apres resolution (pas de fuite memoire)
    - Test 6: Une erreur dans un write n'empeche pas les writes suivants sur le meme path
  </behavior>
  <action>
1. Creer `lib/__tests__/write-queue.test.ts` avec les 6 tests ci-dessus. Mocker `coordinatedWriteFile` et `coordinatedReadFile` et `FileSystem` de expo-file-system. Tester en creant une instance VaultManager avec un vaultPath bidon et en verifiant l'ordre d'execution via un tableau de traces.

2. Dans `lib/vault.ts`, ajouter a la classe VaultManager :
   - Propriete privee `private _writeQueues = new Map<string, Promise<void>>();`
   - Methode privee `private enqueueWrite(relativePath: string, fn: () => Promise<void>): Promise<void>` qui chaine les Promise par path. Utiliser `prev.then(() => fn())` pour propager les erreurs. Ajouter `.finally()` pour nettoyer la Map quand la chaine se termine.
   - Renommer le body actuel de `writeFile` en `private async _writeFileDirect(relativePath: string, content: string): Promise<void>`
   - Le nouveau `writeFile` appelle `this.enqueueWrite(relativePath, () => this._writeFileDirect(relativePath, content))`

3. Pour `toggleTask` : envelopper TOUT le body (de `const content = await this.readFile(...)` jusqu'au `await this.writeFile(...)`) dans `this.enqueueWrite(relativePath, async () => { ... })`. A l'interieur, utiliser `this._writeFileDirect` au lieu de `this.writeFile` pour eviter le double enqueue.

4. Pour `appendTask` : meme pattern — envelopper tout le body dans `this.enqueueWrite(relativePath, async () => { ... })` et utiliser `this._writeFileDirect`.

5. Ne PAS toucher aux call sites dans useGamification.ts ou useVault.ts — la queue est transparente.
  </action>
  <verify>
    <automated>cd /Users/gabrielwaltio/Documents/family-vault && npx jest lib/__tests__/write-queue.test.ts --no-cache 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '_writeQueues' lib/vault.ts` retourne au moins 3 (declaration + enqueueWrite + cleanup)
    - `grep -c 'enqueueWrite' lib/vault.ts` retourne au moins 4 (definition + writeFile + toggleTask + appendTask)
    - `grep '_writeFileDirect' lib/vault.ts` montre la methode privee renommee
    - `npx jest lib/__tests__/write-queue.test.ts` passe avec 6 tests
    - `npx tsc --noEmit 2>&1 | grep -c 'vault.ts'` retourne 0 (pas de nouvelles erreurs TS)
  </acceptance_criteria>
  <done>La write queue serialise toutes les ecritures par fichier. toggleTask et appendTask sont proteges. 6 tests passent. Aucune nouvelle erreur TypeScript.</done>
</task>

<task type="auto">
  <name>Task 2: Modele XP budget dans rewards.ts</name>
  <files>lib/gamification/rewards.ts</files>
  <read_first>lib/gamification/rewards.ts, lib/gamification/engine.ts</read_first>
  <action>
1. Lire `lib/gamification/rewards.ts` en entier pour trouver la fin du fichier.

2. Ajouter a la FIN du fichier (apres les exports existants) un bloc JSDoc + export :

```typescript
// --- XP Budget Model -----------------------------------------------------------
/**
 * XP Budget Model
 *
 * Formule de niveau : xpForLevel(n) = 50n^2 + 50n
 * Base XP par tache : POINTS_PER_TASK = 10
 * Bonus streak : +5 (2j), +10 (7j), +15 (14j), +25 (30j)
 *
 * Scenario annee scolaire (baseline) :
 *   3 taches/jour x 5 jours/semaine x 36 semaines = 540 taches/an
 *
 * Progression annuelle :
 *   Sans streak  : ~5 400 XP -> Niveau ~10
 *   Streak max   : ~18 900 XP -> Niveau ~19
 *
 * Regle pour nouvelles sources de recompense :
 *   Exprimer en "equivalents taches" par rapport a POINTS_PER_TASK.
 *   - Completion aventure quotidienne = 1 equivalent (10 pts)
 *   - Chapitre saga = 3 equivalents (30 pts)
 *   - Defi complete = configurable, defaut 50 pts (5 taches)
 *
 * Pacing loot box (enfant) :
 *   Seuil 50 pts = 5 taches par box = ~108 boxes/an (sans streak)
 *   Intentionnellement genereux pour les enfants.
 *
 * IMPORTANT: Ne PAS modifier xpForLevel() ni POINTS_PER_TASK sans migration.
 * Les niveaux existants sont calcules en temps reel depuis les points bruts.
 */
export const XP_BUDGET = {
  /** Taches estimees par jour (scenario baseline) */
  tasksPerDayBudget: 3,
  /** Jours par semaine (hors week-end) */
  daysPerWeekBudget: 5,
  /** Semaines dans une annee scolaire */
  weeksPerYearBudget: 36,
  /** XP annuel approximatif sans streak (3 * 5 * 36 * 10) */
  baselineYearlyXP: 5400,
  /** Fourchette de niveau cible apres 1 annee scolaire [min, max] */
  targetLevelRange: [10, 19] as [number, number],
} as const;
```

3. Ne PAS modifier les constantes existantes (POINTS_PER_TASK, LOOT_THRESHOLD, etc.).
  </action>
  <verify>
    <automated>cd /Users/gabrielwaltio/Documents/family-vault && grep -c 'XP_BUDGET' lib/gamification/rewards.ts && npx tsc --noEmit 2>&1 | grep -c 'rewards.ts'</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'export const XP_BUDGET' lib/gamification/rewards.ts` retourne une ligne
    - `grep 'tasksPerDayBudget' lib/gamification/rewards.ts` retourne une ligne
    - `grep 'targetLevelRange' lib/gamification/rewards.ts` retourne une ligne
    - `grep 'baselineYearlyXP: 5400' lib/gamification/rewards.ts` retourne une ligne
    - `npx tsc --noEmit 2>&1 | grep 'rewards.ts'` ne retourne rien (0 erreurs)
  </acceptance_criteria>
  <done>XP_BUDGET est exporte de rewards.ts avec JSDoc complet. La documentation couvre le scenario annee scolaire, les regles pour nouvelles sources, et l'avertissement de ne pas modifier la formule sans migration. Aucune constante existante modifiee.</done>
</task>

</tasks>

<verification>
1. `npx jest lib/__tests__/write-queue.test.ts --no-cache` — 6 tests passent
2. `grep -c 'enqueueWrite' lib/vault.ts` >= 4
3. `grep 'export const XP_BUDGET' lib/gamification/rewards.ts` — present
4. `npx tsc --noEmit` — pas de nouvelles erreurs dans vault.ts ni rewards.ts
</verification>

<success_criteria>
- La write queue serialise toutes les ecritures au meme fichier sans modifier aucun call site
- toggleTask et appendTask sont proteges contre les races read-modify-write
- XP_BUDGET documente l'economie avec scenario annee scolaire et regles pour nouvelles sources
- Tous les tests passent, pas de regression TypeScript
</success_criteria>

<output>
After completion, create `.planning/phases/02-write-safety-couleurs/02-01-SUMMARY.md`
</output>
