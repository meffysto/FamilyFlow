---
phase: 02-write-safety-couleurs
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/_layout.tsx
  - app/setup.tsx
  - app/(tabs)/tasks.tsx
  - app/(tabs)/more.tsx
  - components/VaultPicker.tsx
  - components/RecipeCard.tsx
  - components/RecipeCookingMode.tsx
  - components/PhotoViewer.tsx
  - components/ContactImporter.tsx
  - components/TabletSidebar.tsx
  - components/ui/LiquidXPBar.tsx
  - components/ui/SegmentedControl.tsx
autonomous: true
requirements:
  - QUAL-03

must_haves:
  truths:
    - "Le mode nuit n'affiche plus de couleurs hardcodees dans les ecrans principaux"
    - "Les ecrans _layout, tasks, more, setup utilisent colors.* pour tout le chrome UI"
    - "Les composants VaultPicker, RecipeCard, RecipeCookingMode suivent le theme"
  artifacts:
    - path: "app/_layout.tsx"
      provides: "Loading overlay et error screen avec tokens semantiques"
      contains: "colors.bg"
    - path: "app/(tabs)/tasks.tsx"
      provides: "Textes taches avec tokens semantiques (text, textSub, textMuted)"
      contains: "colors.text"
    - path: "components/VaultPicker.tsx"
      provides: "Boutons et erreurs avec tokens semantiques"
      contains: "colors.error"
  key_links:
    - from: "app/_layout.tsx"
      to: "constants/colors.ts"
      via: "useThemeColors() inline styles"
      pattern: "colors\\."
    - from: "components/VaultPicker.tsx"
      to: "constants/colors.ts"
      via: "useThemeColors() inline styles"
      pattern: "colors\\."
---

<objective>
Remplacer les couleurs hardcodees Tier 1 dans les ecrans et composants high-priority par des tokens semantiques useThemeColors().

Purpose: Le mode nuit affiche actuellement des couleurs fixes (#FFFFFF, #1F2937, etc.) qui ne s'adaptent pas au theme sombre. Chaque hex literal dans un StyleSheet de chrome UI est un bug dark mode.
Output: 12 fichiers migres vers colors.*, zero hex literal dans le chrome UI de ces fichiers.
</objective>

<execution_context>
@/Users/gabrielwaltio/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gabrielwaltio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-write-safety-couleurs/02-RESEARCH.md

<interfaces>
From constants/colors.ts (LightColors — DarkColors has same keys):
```typescript
export const LightColors = {
  bg: '#F3F4F6', card: '#FFFFFF', cardAlt: '#F9FAFB',
  text: '#111827', textSub: '#374151', textMuted: '#6B7280', textFaint: '#9CA3AF',
  border: '#E5E7EB', borderLight: '#F3F4F6', separator: '#D1D5DB',
  inputBg: '#F9FAFB', inputBorder: '#D1D5DB',
  error: '#EF4444', errorBg: '#FEE2E2', errorText: '#991B1B',
  warning: '#F59E0B', warningBg: '#FEF3C7', warningText: '#92400E',
  success: '#10B981', successBg: '#D1FAE5', successText: '#15803D',
  info: '#8B5CF6', infoBg: '#EDE9FE',
  onPrimary: '#FFFFFF', onPrimaryMuted: '#C4B5FD',
  overlay: 'rgba(0,0,0,0.5)', overlayLight: 'rgba(0,0,0,0.4)',
  glassBg: 'rgba(255,255,255,0.65)', glassBorder: 'rgba(255,255,255,0.45)',
};
```

From contexts/ThemeContext.tsx:
```typescript
function useThemeColors(): {
  primary: string; tint: string; colors: typeof LightColors;
  isDark: boolean; setThemeId: (id: string) => void;
  darkModePreference: string; setDarkModePreference: (pref: string) => void;
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration couleurs ecrans (app/)</name>
  <files>app/_layout.tsx, app/setup.tsx, app/(tabs)/tasks.tsx, app/(tabs)/more.tsx</files>
  <read_first>app/_layout.tsx, app/setup.tsx, app/(tabs)/tasks.tsx, app/(tabs)/more.tsx, constants/colors.ts</read_first>
  <action>
Pour chaque fichier, remplacer UNIQUEMENT les hex literals Tier 1 (chrome UI) par le token semantique correspondant. Ne PAS toucher aux couleurs cosmetics (WEATHER_COLORS, menu item color fields).

**Regle StyleSheet.create** : Les valeurs theme-dependantes ne peuvent PAS aller dans StyleSheet.create (evalue une seule fois au chargement du module). Deplacer les props couleur en inline style avec `colors.*`.

**app/_layout.tsx** (7 remplacements) :
- Ligne ~145: `backgroundColor: systemScheme === 'dark' ? '#0F172A' : '#F9FAFB'` → Ce composant est AVANT ThemeProvider, donc on ne peut pas utiliser useThemeColors(). Utiliser `DarkColors.bg` et `LightColors.bg` importes directement de `constants/colors.ts` : `backgroundColor: systemScheme === 'dark' ? DarkColors.bg : LightColors.bg`
- Ligne ~146: `color="#7C3AED"` sur ActivityIndicator → Meme probleme (avant ThemeProvider). Garder tel quel ou utiliser une constante. Le violet est le primary par defaut, garder `#7C3AED` comme fallback ici car c'est avant le provider.
- Ligne ~177: `backgroundColor: '#F9FAFB'` dans styles.loading → `backgroundColor: LightColors.cardAlt` (import direct, pas hook)
- Ligne ~186: `color: '#1F2937'` dans styles.errorTitle → Deplacer en inline avec `colors.text` dans le composant ErrorBoundary qui EST dans le provider
- Ligne ~192: `color: '#6B7280'` dans styles.errorMessage → Inline `colors.textMuted`
- Ligne ~198: `backgroundColor: '#7C3AED'` dans styles.retryButton → Inline avec `primary` de useThemeColors()
- Ligne ~204: `color: '#FFFFFF'` dans styles.retryText → Inline `colors.onPrimary`

**app/setup.tsx** (1 remplacement) :
- Ligne ~1450: `color: '#16a34a'` dans recapCheckDone → Deplacer en inline `{ color: colors.success }`

**app/(tabs)/tasks.tsx** (3 remplacements — NE PAS toucher WEATHER_COLORS ligne 72) :
- Ligne ~142: `color: '#1F2937'` → Inline `{ color: colors.text }`
- Ligne ~146: `color: '#374151'` → Inline `{ color: colors.textSub }`
- Ligne ~152: `color: '#4B5563'` → Inline `{ color: colors.textMuted }`

**app/(tabs)/more.tsx** (1 remplacement — NE PAS toucher les color fields des menu items lignes 109-124) :
- Ligne ~378: `color: '#fff'` → Inline `{ color: colors.onPrimary }`

Pour chaque fichier : verifier que `useThemeColors` est deja importe. Si non, ajouter `import { useThemeColors } from '@/contexts/ThemeContext';` et `const { colors } = useThemeColors();` (ou `const { colors, primary } = useThemeColors();` si besoin du primary).

Pour app/_layout.tsx : ajouter `import { LightColors, DarkColors } from '@/constants/colors';` pour les styles pre-provider.
  </action>
  <verify>
    <automated>cd /Users/gabrielwaltio/Documents/family-vault && echo "=== _layout ===" && grep -n "'#" "app/_layout.tsx" | grep -v 'DarkColors\|LightColors\|7C3AED\|import' | head -5 && echo "=== setup ===" && grep -n "'#16a34a'" app/setup.tsx | head -3 && echo "=== tasks ===" && grep -n "'#1F2937'\|'#374151'\|'#4B5563'" "app/(tabs)/tasks.tsx" | head -5 && echo "=== more ===" && grep -n "color: '#fff'" "app/(tabs)/more.tsx" | head -3 && echo "=== tsc ===" && npx tsc --noEmit 2>&1 | grep -E '_layout|setup|tasks|more' | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep "'#F9FAFB'" app/_layout.tsx` ne retourne rien (remplace par LightColors.cardAlt)
    - `grep "'#1F2937'" app/_layout.tsx` ne retourne rien (remplace par colors.text)
    - `grep "'#FFFFFF'" app/_layout.tsx` ne retourne rien (remplace par colors.onPrimary)
    - `grep "'#16a34a'" app/setup.tsx` ne retourne rien
    - `grep "'#1F2937'" "app/(tabs)/tasks.tsx"` ne retourne rien
    - `grep "color: '#fff'" "app/(tabs)/more.tsx"` ne retourne rien
    - WEATHER_COLORS dans tasks.tsx est INTACT (grep confirme la ligne existe toujours)
    - Les menu item color fields dans more.tsx (lignes 109-124) sont INTACTS
    - `npx tsc --noEmit` ne rapporte pas de nouvelles erreurs dans ces 4 fichiers
  </acceptance_criteria>
  <done>Les 4 ecrans utilisent colors.* pour tout le chrome UI. Les couleurs cosmetiques (WEATHER_COLORS, menu item colors) sont preservees. Aucune nouvelle erreur TypeScript.</done>
</task>

<task type="auto">
  <name>Task 2: Migration couleurs composants high-priority</name>
  <files>components/VaultPicker.tsx, components/RecipeCard.tsx, components/RecipeCookingMode.tsx, components/PhotoViewer.tsx, components/ContactImporter.tsx, components/TabletSidebar.tsx, components/ui/LiquidXPBar.tsx, components/ui/SegmentedControl.tsx</files>
  <read_first>components/VaultPicker.tsx, components/RecipeCard.tsx, components/RecipeCookingMode.tsx, components/PhotoViewer.tsx, components/ContactImporter.tsx, components/TabletSidebar.tsx, components/ui/LiquidXPBar.tsx, components/ui/SegmentedControl.tsx, constants/colors.ts</read_first>
  <action>
Pour chaque fichier, remplacer les hex literals Tier 1 par tokens semantiques. Meme regle : StyleSheet.create = statique seulement, couleurs theme-dependantes en inline.

**components/VaultPicker.tsx** (6 remplacements) :
- Lignes ~225, ~236: `color: colors.info || '#1D4ED8'` → Garder le fallback si colors.info peut etre undefined, sinon simplifier en `colors.info`
- Ligne ~283: `color="#FFFFFF"` sur ActivityIndicator → `color={colors.onPrimary}`
- Ligne ~307: `color: '#FFFFFF'` → Inline `colors.onPrimary`
- Ligne ~352: `borderColor: '#EF4444'` → Inline `colors.error`
- Ligne ~356: `color: '#EF4444'` → Inline `colors.error`
- Ligne ~407: `backgroundColor: '#A78BFA'` → Inline avec `primary` (le violet est le primary du theme)
- Ligne ~412: `color: '#FFFFFF'` → Inline `colors.onPrimary`

**components/RecipeCard.tsx** (2 remplacements) :
- Ligne ~63: `color: '#fff'` → `colors.onPrimary`
- Ligne ~188: `color: '#fff'` → `colors.onPrimary`

**components/RecipeCookingMode.tsx** (2 remplacements) :
- Ligne ~611: `color: '#FFFFFF'` → Deplacer en inline `{ color: colors.onPrimary }`
- Ligne ~686: `color: '#FFFFFF'` → Deplacer en inline `{ color: colors.onPrimary }`

**components/PhotoViewer.tsx** (1 remplacement) :
- Ligne ~218: `backgroundColor: '#000'` → Inline `colors.overlay` ou garder `'#000'` si c'est un vrai fond noir photo viewer. Decision: utiliser `'#000'` est intentionnel pour un viewer photo (fond noir absolu, pas theme-dependant). GARDER tel quel — exempt comme background de viewer.

**components/ContactImporter.tsx** (1 remplacement) :
- Ligne ~444: `color: '#FFFFFF'` → Inline `colors.onPrimary`

**components/TabletSidebar.tsx** (1 remplacement) :
- Ligne ~277: `color: '#fff'` → Inline `colors.onPrimary`

**components/ui/LiquidXPBar.tsx** (1 remplacement) :
- Ligne ~161: `color: '#fff'` → Inline `colors.onPrimary`

**components/ui/SegmentedControl.tsx** (1 remplacement) :
- Ligne ~74: `shadowColor: '#000'` → Garder `'#000'` — les shadowColor sont toujours noirs quel que soit le theme (convention React Native). EXEMPT.

Pour chaque fichier modifie : verifier que `useThemeColors` est importe et appele. Si le composant est un `React.memo()`, s'assurer que le hook est appele DANS le composant (pas en dehors).
  </action>
  <verify>
    <automated>cd /Users/gabrielwaltio/Documents/family-vault && for f in components/VaultPicker.tsx components/RecipeCard.tsx components/RecipeCookingMode.tsx components/ContactImporter.tsx components/TabletSidebar.tsx components/ui/LiquidXPBar.tsx; do echo "=== $f ===" && grep -cn "'#[0-9a-fA-F]" "$f"; done && npx tsc --noEmit 2>&1 | grep -E 'VaultPicker|RecipeCard|RecipeCookingMode|ContactImporter|TabletSidebar|LiquidXPBar' | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep "'#FFFFFF'" components/VaultPicker.tsx` ne retourne rien
    - `grep "'#EF4444'" components/VaultPicker.tsx` ne retourne rien
    - `grep "'#fff'" components/RecipeCard.tsx` ne retourne rien
    - `grep "'#FFFFFF'" components/RecipeCookingMode.tsx` ne retourne rien
    - `grep "'#FFFFFF'" components/ContactImporter.tsx` ne retourne rien
    - `grep "'#fff'" components/TabletSidebar.tsx` ne retourne rien
    - `grep "'#fff'" components/ui/LiquidXPBar.tsx` ne retourne rien
    - PhotoViewer.tsx `#000` est INTACT (fond noir intentionnel pour viewer)
    - SegmentedControl.tsx `shadowColor: '#000'` est INTACT (convention shadow)
    - `npx tsc --noEmit` ne rapporte pas de nouvelles erreurs dans ces fichiers
  </acceptance_criteria>
  <done>8 composants migres vers colors.*. PhotoViewer (#000 fond noir) et SegmentedControl (shadowColor) gardes intentionnellement. Aucune nouvelle erreur TypeScript.</done>
</task>

</tasks>

<verification>
1. `grep -rn "'#[0-9a-fA-F]" app/_layout.tsx app/setup.tsx "app/(tabs)/tasks.tsx" "app/(tabs)/more.tsx" | grep -v 'WEATHER_COLORS\|DarkColors\|LightColors\|7C3AED\|color:.*menu\|emoji' | wc -l` devrait etre 0 ou quasi-0
2. `npx tsc --noEmit` — pas de nouvelles erreurs dans les fichiers modifies
3. Les couleurs cosmetiques (WEATHER_COLORS, menu item colors, shadowColor, PhotoViewer bg) sont preservees
</verification>

<success_criteria>
- Les 4 ecrans app/ et 8 composants utilisent colors.* pour le chrome UI
- Le mode nuit fonctionne sans couleurs hardcodees visibles dans ces fichiers
- Les couleurs cosmetiques et SVG ne sont pas modifiees
- Aucune regression TypeScript
</success_criteria>

<output>
After completion, create `.planning/phases/02-write-safety-couleurs/02-02-SUMMARY.md`
</output>
