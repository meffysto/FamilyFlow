---
phase: 02-write-safety-couleurs
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - components/SkillNode.tsx
  - components/mascot/TreeShop.tsx
  - components/mascot/WeeklyGoal.tsx
  - app/(tabs)/tree.tsx
autonomous: false
requirements:
  - QUAL-03

must_haves:
  truths:
    - "SkillNode utilise colors.* pour bordures, texte et badge"
    - "TreeShop utilise colors.* pour texte achat et indicateur equipe"
    - "WeeklyGoal utilise colors.* pour indicateurs de progression"
    - "tree.tsx utilise colors.* pour le chrome UI (pas les SVG cosmetics)"
    - "Le mode nuit affiche correctement tous les ecrans modifies dans la phase"
  artifacts:
    - path: "components/SkillNode.tsx"
      provides: "Tokens semantiques pour bordure, texte XP, badge"
      contains: "colors."
    - path: "components/mascot/TreeShop.tsx"
      provides: "Tokens semantiques pour texte achat et indicateur"
      contains: "colors.onPrimary"
    - path: "components/mascot/WeeklyGoal.tsx"
      provides: "Tokens semantiques pour progression"
      contains: "colors.success"
  key_links:
    - from: "components/SkillNode.tsx"
      to: "constants/colors.ts"
      via: "useThemeColors() inline styles"
      pattern: "colors\\."
---

<objective>
Migrer les couleurs hardcodees restantes dans les composants Tier 1 et valider visuellement le mode nuit.

Purpose: Completer la migration QUAL-03 sur les fichiers restants (SkillNode, TreeShop, WeeklyGoal, tree.tsx) et faire une verification visuelle du mode nuit.
Output: 4 fichiers migres, validation dark mode par l'utilisateur.
</objective>

<execution_context>
@/Users/gabrielwaltio/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gabrielwaltio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-write-safety-couleurs/02-RESEARCH.md

<interfaces>
From constants/colors.ts (tokens utiles pour cette tache):
```typescript
// Text: text, textSub, textMuted, textFaint
// Status: success (#10B981), error (#EF4444), warning (#F59E0B), info (#8B5CF6)
// Surface: onPrimary (#FFFFFF light), card, bg
// Border: border, borderLight, separator
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration couleurs SkillNode, TreeShop, WeeklyGoal, tree.tsx</name>
  <files>components/SkillNode.tsx, components/mascot/TreeShop.tsx, components/mascot/WeeklyGoal.tsx, app/(tabs)/tree.tsx</files>
  <read_first>components/SkillNode.tsx, components/mascot/TreeShop.tsx, components/mascot/WeeklyGoal.tsx, app/(tabs)/tree.tsx, constants/colors.ts</read_first>
  <action>
**components/SkillNode.tsx** (5 remplacements) :
- Ligne ~113: `borderColor: '#475569'` -> Inline `colors.separator` (gris moyen pour bordure)
- Ligne ~130: `color: '#10B981'` -> Inline `colors.success`
- Ligne ~142: `color: '#475569'` -> Inline `colors.textMuted`
- Ligne ~156: `return '#475569'` -> `return colors.textMuted` (attention: si c'est dans une fonction pure hors composant, il faut passer colors en parametre ou deplacer la logique)
- Ligne ~266: `backgroundColor: '#10B981'` -> Inline `colors.success`
- Ligne ~271: `color: '#FFFFFF'` -> Inline `colors.onPrimary`

Note pour ligne 156: Si la fonction qui retourne la couleur est definie hors du composant React, elle ne peut pas utiliser le hook. Deux options: (a) deplacer la logique dans le composant, ou (b) passer `colors` en parametre. Choisir (a) si la fonction est courte.

**components/mascot/TreeShop.tsx** (3 remplacements -- NE PAS toucher le map rarity lignes 63-67) :
- Ligne ~328: `color: canAfford ? '#FFFFFF' : colors.textMuted` -> `color: canAfford ? colors.onPrimary : colors.textMuted` (deja utilise colors.textMuted, juste fixer le #FFFFFF)
- Ligne ~418: `borderColor: owned ? '#F59E0B' : colors.borderLight` -> `borderColor: owned ? colors.warning : colors.borderLight` (le jaune "owned" correspond semantiquement a warning/gold)
- Ligne ~447: `color: '#4ADE80'` -> `colors.success` (texte vert "equipe")

**components/mascot/WeeklyGoal.tsx** (3 remplacements) :
- Ligne ~34: `color: isComplete ? '#4ADE80' : colors.textSub` -> `color: isComplete ? colors.success : colors.textSub`
- Ligne ~46: `backgroundColor: isComplete ? '#4ADE80' : '#3B82F6'` -> `backgroundColor: isComplete ? colors.success : colors.info`
- Ligne ~52: `color: isComplete ? '#4ADE80' : colors.textMuted` -> `color: isComplete ? colors.success : colors.textMuted`

**app/(tabs)/tree.tsx** (remplacements selectifs -- beaucoup sont cosmetic/SVG) :
- Ligne ~133: `color: '#FFF'` -> `colors.onPrimary` (texte sur fond colore)
- Les autres hex dans tree.tsx (FFD700, 4CAF50, F59E0B, FFFFFF pour border diorama) sont des couleurs cosmetiques de jeu ou SVG. Les GARDER.

Pour chaque fichier : s'assurer que useThemeColors est importe et appele dans le composant.
  </action>
  <verify>
    <automated>cd /Users/gabrielwaltio/Documents/family-vault && echo "=== SkillNode ===" && grep -n "'#" components/SkillNode.tsx | grep -v 'import\|shadowColor' && echo "=== TreeShop ===" && grep -n "'#FFFFFF'\|'#4ADE80'\|'#F59E0B'" components/mascot/TreeShop.tsx | grep -v 'RARITY\|rarity\|commun\|rare\|pique\|gendaire\|prestige' && echo "=== WeeklyGoal ===" && grep -n "'#" components/mascot/WeeklyGoal.tsx && echo "=== tsc ===" && npx tsc --noEmit 2>&1 | grep -E 'SkillNode|TreeShop|WeeklyGoal|tree\.tsx' | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep "'#475569'" components/SkillNode.tsx` ne retourne rien
    - `grep "'#10B981'" components/SkillNode.tsx` ne retourne rien
    - `grep "'#FFFFFF'" components/SkillNode.tsx` ne retourne rien
    - `grep "'#FFFFFF'" components/mascot/TreeShop.tsx` ne retourne rien (hors rarity map)
    - `grep "'#4ADE80'" components/mascot/TreeShop.tsx` ne retourne rien
    - `grep "'#4ADE80'\|'#3B82F6'" components/mascot/WeeklyGoal.tsx` ne retourne rien
    - Le rarity map dans TreeShop (lignes 63-67) est INTACT
    - `npx tsc --noEmit` ne rapporte pas de nouvelles erreurs
  </acceptance_criteria>
  <done>4 fichiers migres vers colors.*. Les couleurs cosmetiques (rarity map, SVG diorama, FFD700 golden) sont preservees. Aucune nouvelle erreur TypeScript.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verification visuelle mode nuit</name>
  <files>none</files>
  <action>
Verification visuelle par l'utilisateur. Aucune action automatisee -- ce checkpoint valide le resultat visuel de la migration couleurs complete (plans 02 et 03).
  </action>
  <what-built>Migration complete des couleurs Tier 1 : 16 fichiers (4 ecrans + 12 composants) utilisent desormais colors.* via useThemeColors(). Les couleurs cosmetiques (SVG art, loot box skins, WEATHER_COLORS, rarity maps, category colors) sont preservees.</what-built>
  <how-to-verify>
    1. Lancer l'app : `npx expo run:ios --device`
    2. Aller dans Reglages > Apparence > Mode sombre
    3. Verifier ces ecrans en mode nuit :
       - Dashboard (page d'accueil) -- fond sombre, texte lisible
       - Taches -- texte et labels lisibles, pas de blanc sur blanc
       - Menu (More) -- items lisibles, le texte en bas est sur fond sombre
       - Arbre/Ferme -- le chrome UI est sombre, les SVG colorees sont preservees
       - Vault Picker (si accessible) -- boutons et erreurs lisibles
    4. Repasser en mode clair -- verifier que rien n'a casse
    5. Verifier l'ecran de chargement initial (splash/loading) -- fond correct selon le mode
  </how-to-verify>
  <verify>Validation visuelle par l'utilisateur sur device physique</verify>
  <done>L'utilisateur confirme que le mode nuit est visuellement correct sur tous les ecrans.</done>
  <resume-signal>Taper "approved" si le mode nuit est correct, ou decrire les problemes observes</resume-signal>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` -- pas de nouvelles erreurs dans les fichiers modifies
2. Verification visuelle du mode nuit par l'utilisateur
3. Les couleurs cosmetiques (TreeView SVG, LootBoxOpener, FarmPlots, HarvestBurst, WEATHER_COLORS, rarity maps, category maps) sont intactes
</verification>

<success_criteria>
- Tous les fichiers Tier 1 utilisent colors.* pour le chrome UI
- Le mode nuit est visuellement correct sur tous les ecrans
- Les couleurs cosmetiques et SVG sont inchangees
- L'utilisateur a valide visuellement
</success_criteria>

<output>
After completion, create `.planning/phases/02-write-safety-couleurs/02-03-SUMMARY.md`
</output>
