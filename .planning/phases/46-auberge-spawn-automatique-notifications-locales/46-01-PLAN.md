---
phase: 46-auberge-spawn-automatique-notifications-locales
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/auberge/auto-tick.ts
  - lib/scheduled-notifications.ts
  - lib/types.ts
  - lib/notifications.ts
  - lib/__tests__/auberge-auto-tick.test.ts
autonomous: true
requirements: [AUBERGE-46-01, AUBERGE-46-02, AUBERGE-46-04]
must_haves:
  truths:
    - "Une fonction pure orchestre expire + spawn et persiste l'état Auberge en 1 seul writeFile farm"
    - "Une fonction de schedule pose la notif d'arrivée immédiate et la notif de rappel H-4"
    - "Une fonction de cancel retire toutes les notifs d'un instanceId donné (idempotent)"
    - "Le toggle 'Visiteurs auberge' apparaît dans NotificationSettings (BUILTIN_NOTIFICATIONS)"
    - "Les tests Jest du helper auto-tick passent (mock VaultManager + Notifications)"
  artifacts:
    - path: "lib/auberge/auto-tick.ts"
      provides: "tickAubergeAuto(profileId, deps) — orchestrateur fire-and-forget"
      contains: "export async function tickAubergeAuto"
    - path: "lib/scheduled-notifications.ts"
      provides: "scheduleAubergeVisitorArrival / scheduleAubergeVisitorReminder / cancelAubergeVisitorNotifs"
      contains: "scheduleAubergeVisitorArrival"
    - path: "lib/types.ts"
      provides: "NotifEvent étendu avec 'auberge_visitor_reminder'"
      contains: "auberge_visitor_reminder"
    - path: "lib/notifications.ts"
      provides: "Entrée BUILTIN_NOTIFICATIONS pour 'auberge_visitor_reminder'"
      contains: "auberge_visitor_reminder"
    - path: "lib/__tests__/auberge-auto-tick.test.ts"
      provides: "Couverture tickAubergeAuto (spawn / expire / cancel notifs)"
      contains: "describe('tickAubergeAuto'"
  key_links:
    - from: "lib/auberge/auto-tick.ts"
      to: "lib/mascot/auberge-engine.ts"
      via: "import { parseAuberge, serializeAuberge, expireVisitors, spawnVisitor, getTotalReputation }"
      pattern: "from '../mascot/auberge-engine'"
    - from: "lib/auberge/auto-tick.ts"
      to: "lib/scheduled-notifications.ts"
      via: "import { scheduleAubergeVisitorArrival, scheduleAubergeVisitorReminder, cancelAubergeVisitorNotifs }"
      pattern: "from '../scheduled-notifications'"
---

<objective>
Créer le socle réutilisable de la Phase 46 : helper `tickAubergeAuto` (orchestrateur engine + persistance + notifs) + 3 fonctions de scheduling/cancel notifs Auberge + entrée toggle dans BUILTIN_NOTIFICATIONS. Ce plan ne wire rien dans `useVault.ts` ni dans `useAuberge.ts` — il pose les briques que les Plans 02 et 03 consommeront.

Purpose: isoler l'orchestration auto-tick dans un module pur (testable Jest sans React) pour éviter la duplication entre wiring launch et wiring task-complete (Plan 02), et garantir la cancellation centralisée des notifs (Plan 03).
Output: 1 nouveau module `lib/auberge/auto-tick.ts`, 3 fonctions ajoutées à `lib/scheduled-notifications.ts`, 1 entrée dans `BUILTIN_NOTIFICATIONS`, 1 valeur dans `NotifEvent`, 1 test suite Jest.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-auberge-spawn-automatique-notifications-locales/46-CONTEXT.md
@.planning/phases/43-auberge-mod-le-moteur-visiteurs/43-04-SUMMARY.md
@lib/mascot/auberge-engine.ts
@lib/scheduled-notifications.ts
@lib/notifications.ts
@lib/types.ts
@CLAUDE.md

<interfaces>
<!-- Contracts existants à consommer -->

From lib/mascot/auberge-engine.ts:
```typescript
export function parseAuberge(serialized: { visitors?: string; reputations?: string; lastSpawn?: string; totalDeliveries?: number; }): AubergeState;
export function serializeAuberge(state: AubergeState, now?: Date): { visitors: string; reputations: string; lastSpawn?: string; totalDeliveries: number };
export function expireVisitors(state: AubergeState, now: Date): { state: AubergeState; expired: ActiveVisitor[] };
export function spawnVisitor(state: AubergeState, treeStage: TreeStage, now: Date, totalReputation: number, rng?: () => number): { state: AubergeState; visitor: ActiveVisitor } | null;
export function getTotalReputation(state: AubergeState): number;
```

From lib/mascot/types.ts:
```typescript
export interface ActiveVisitor {
  visitorId: string;
  instanceId: string;
  arrivedAt: string;     // ISO
  deadlineAt: string;    // ISO
  request: VisitorRequestItem[];
  status: 'active' | 'delivered' | 'expired';
  rewardCoins: number;
  rewardLootKey?: string;
}
```

From lib/visitor-catalog.ts (consommé pour le label Nom):
```typescript
export const VISITOR_CATALOG: VisitorDefinition[];  // chaque def a `name: string`, `emoji: string`
```

From lib/parser.ts (déjà utilisé par hooks/useAuberge.ts) :
```typescript
export function parseFarmProfile(content: string): FarmProfileData;
export function serializeFarmProfile(name: string, data: FarmProfileData): string;
```

From lib/mascot/engine.ts:
```typescript
export function getTreeStageInfo(level: number): { stage: TreeStage; ... };
```

<!-- Contrats nouveaux à exposer -->

To create in lib/scheduled-notifications.ts:
```typescript
/** Schedule la notif "arrivée immédiate" (trigger: null). Idempotent. Silencieux si permission refusée. */
export async function scheduleAubergeVisitorArrival(
  instanceId: string,
  visitorName: string,
  emoji: string,
  deadlineHours: number,
): Promise<void>;

/** Schedule la notif "rappel H-4" (trigger DATE = deadlineAt - 4h). Skip si déjà passé. Idempotent. */
export async function scheduleAubergeVisitorReminder(
  instanceId: string,
  visitorName: string,
  emoji: string,
  deadlineAt: Date,
): Promise<void>;

/** Cancel toutes les notifs d'un visiteur (arrival + reminder) — idempotent (no-throw si absent). */
export async function cancelAubergeVisitorNotifs(instanceId: string): Promise<void>;
```

To create in lib/auberge/auto-tick.ts:
```typescript
import type { VaultManager } from '../vault';
import type { Profile } from '../types';

export interface AutoTickDeps {
  vault: VaultManager;
  profiles: Profile[];
}

/**
 * Fire-and-forget : ne throw jamais. Lit farm-{id}.md, expire/spawn, persiste,
 * schedule notif arrivée + reminder H-4 sur spawn, cancel notifs des expirés.
 * Idempotent (le cooldown 6h global du moteur garantit qu'un appel répété ne re-spawn pas).
 */
export async function tickAubergeAuto(profileId: string, deps: AutoTickDeps): Promise<void>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 : Étendre NotifEvent + BUILTIN_NOTIFICATIONS + 3 fonctions notifs Auberge</name>
  <files>lib/types.ts, lib/notifications.ts, lib/scheduled-notifications.ts</files>
  <behavior>
    - `NotifEvent` (lib/types.ts:523) inclut désormais `'auberge_visitor_reminder'`.
    - `BUILTIN_NOTIFICATIONS` (lib/notifications.ts:104) contient une entrée id `'auberge_visitor_reminder'`, label `'Visiteurs auberge'`, emoji `'🛖'`, enabled `true`, event `'auberge_visitor_reminder'`, availableVariables `[]` (pas de templating Telegram pour cette notif locale-only).
    - `scheduleAubergeVisitorArrival(instanceId, visitorName, emoji, deadlineHours)` : pose une notif via `Notifications.scheduleNotificationAsync` avec identifier `auberge-visitor-arrival-{instanceId}`, trigger `null` (immédiat). Title `"{emoji} {visitorName} à l'auberge"`, body `"Une nouvelle commande t'attend : {deadlineHours}h pour livrer."`. Avant schedule : `await cancelAubergeVisitorNotifs(instanceId)` pour idempotence. Lit `loadNotifConfig` ou les prefs pour skip si toggle désactivé — mais comme cette fonction est aussi appelée hors écosystème Telegram, on lit `loadBuiltinPref('auberge_visitor_reminder')`. Décision simplifiée pour ce plan : la fonction lit `requestNotificationPermissions()` et skip silencieux si `false`. Le toggle BUILTIN n'est consulté que par dispatchNotification Telegram — la notif locale schedule toujours si la permission OS est OK.
    - `scheduleAubergeVisitorReminder(instanceId, visitorName, emoji, deadlineAt)` : calcule `reminderDate = deadlineAt - 4h`. Skip silencieux si `reminderDate <= now`. Identifier `auberge-visitor-reminder-{instanceId}`, trigger `DATE`. Title `"⏰ {emoji} {visitorName}"`, body `"Plus que 4h pour livrer la commande."`.
    - `cancelAubergeVisitorNotifs(instanceId)` : `getAllScheduledNotificationsAsync` puis cancel tous ceux dont l'identifier commence par `auberge-visitor-arrival-{instanceId}` ou `auberge-visitor-reminder-{instanceId}`. Idempotent (try/catch silencieux par item).
    - Pattern à suivre : `cancelByCategory` lib/scheduled-notifications.ts:160 (boucle filtre prefix).
    - Permission silencieuse : si `requestNotificationPermissions()` retourne false, les schedule sont des no-op (aucun throw).
  </behavior>
  <action>
    1. **lib/types.ts** : ajouter `| 'auberge_visitor_reminder'` à l'union `NotifEvent` (ligne 523, après `'manual'` ou avant — peu importe, juste cohérent).
    2. **lib/notifications.ts** :
       - Ajouter une const `DEFAULT_AUBERGE_VISITOR_REMINDER_TEMPLATE = '🛖 {{visitorName}} arrive à l\'auberge — {{hours}}h pour livrer'` (utilisé uniquement côté Telegram si jamais le toggle est activé — pour l'instant la notif locale n'utilise pas ce template).
       - Ajouter dans `BUILTIN_NOTIFICATIONS` l'entrée :
         ```ts
         {
           id: 'auberge_visitor_reminder',
           label: 'Visiteurs auberge',
           emoji: '🛖',
           enabled: true,
           template: DEFAULT_AUBERGE_VISITOR_REMINDER_TEMPLATE,
           defaultTemplate: DEFAULT_AUBERGE_VISITOR_REMINDER_TEMPLATE,
           event: 'auberge_visitor_reminder',
           availableVariables: [],
           isCustom: false,
         }
         ```
    3. **lib/scheduled-notifications.ts** :
       - Ajouter en haut une const `CAT_AUBERGE_ARRIVAL = 'auberge-visitor-arrival'` et `CAT_AUBERGE_REMINDER = 'auberge-visitor-reminder'`.
       - Implémenter `scheduleAubergeVisitorArrival` :
         ```ts
         export async function scheduleAubergeVisitorArrival(
           instanceId: string, visitorName: string, emoji: string, deadlineHours: number,
         ): Promise<void> {
           const permitted = await requestNotificationPermissions();
           if (!permitted) return;
           await cancelAubergeVisitorNotifs(instanceId);
           try {
             await Notifications.scheduleNotificationAsync({
               identifier: `${CAT_AUBERGE_ARRIVAL}-${instanceId}`,
               content: {
                 title: `${emoji} ${visitorName} à l'auberge`,
                 body: `Une nouvelle commande t'attend : ${deadlineHours}h pour livrer.`,
                 sound: true,
                 data: { type: 'auberge_visitor_arrival', instanceId },
               },
               trigger: null,
             });
           } catch (e) {
             if (__DEV__) console.warn('[scheduleAubergeVisitorArrival]', e);
           }
         }
         ```
       - Implémenter `scheduleAubergeVisitorReminder` (similaire, trigger DATE = deadlineAt - 4h, skip si passé, identifier `${CAT_AUBERGE_REMINDER}-${instanceId}`).
       - Implémenter `cancelAubergeVisitorNotifs` :
         ```ts
         export async function cancelAubergeVisitorNotifs(instanceId: string): Promise<void> {
           try {
             const all = await Notifications.getAllScheduledNotificationsAsync();
             for (const notif of all) {
               if (
                 notif.identifier === `${CAT_AUBERGE_ARRIVAL}-${instanceId}` ||
                 notif.identifier === `${CAT_AUBERGE_REMINDER}-${instanceId}`
               ) {
                 await Notifications.cancelScheduledNotificationAsync(notif.identifier).catch(() => {});
               }
             }
           } catch (e) {
             if (__DEV__) console.warn('[cancelAubergeVisitorNotifs]', e);
           }
         }
         ```
    4. Ne PAS modifier `setupAllNotifications` — les notifs Auberge sont schedulées à la demande par `tickAubergeAuto` (Task 2), pas en bulk au boot.
    5. `npx tsc --noEmit` → 0 nouvelle erreur.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "(types\.ts|notifications\.ts|scheduled-notifications\.ts)" | grep -v "MemoryEditor\|cooklang\|useVault" | wc -l | grep -q "^0$" && echo OK || echo FAIL</automated>
  </verify>
  <done>NotifEvent étend `auberge_visitor_reminder` ; BUILTIN_NOTIFICATIONS expose l'entrée ; 3 fonctions exportées depuis scheduled-notifications.ts ; tsc clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 : Helper lib/auberge/auto-tick.ts + tests Jest</name>
  <files>lib/auberge/auto-tick.ts, lib/__tests__/auberge-auto-tick.test.ts</files>
  <behavior>
    - `tickAubergeAuto(profileId, { vault, profiles })` lit `farm-{profileId}.md`, parse les 4 champs `auberge_*`, appelle `expireVisitors(state, now)` puis `spawnVisitor(...)`.
    - Pour chaque visiteur expiré : `cancelAubergeVisitorNotifs(instanceId)` (cleanup).
    - Si spawn : `scheduleAubergeVisitorArrival` + `scheduleAubergeVisitorReminder` avec le nom et emoji issus de `VISITOR_CATALOG.find(d => d.id === visitor.visitorId)` et `deadlineHours` issus de la même def.
    - Persiste via `serializeAuberge` + `serializeFarmProfile` (1 seul writeFile farm).
    - Fire-and-forget : ne throw JAMAIS. Tout `try/catch` silencieux (sous `__DEV__ && console.warn`).
    - Idempotent : si appelé 5x en 1s, le moteur garantit (cooldown 6h) qu'un seul spawn se déclenche.
    - Tests :
      - Test 1 : si vault.readFile échoue → no-op silencieux.
      - Test 2 : si state vide + treeStage='pousse' + cooldown OK → un visiteur est spawn et `scheduleAubergeVisitorArrival` est appelé 1 fois.
      - Test 3 : si state contient un visiteur avec deadlineAt < now → `expireVisitors` le marque expired et `cancelAubergeVisitorNotifs` est appelé pour cet instanceId.
      - Test 4 : si profile introuvable dans `profiles` → no-op silencieux (level inconnu = pas de tickAuberge).
      - Test 5 : 1 seul `vault.writeFile` par appel (atomicité).
  </behavior>
  <action>
    1. **Créer le dossier** `lib/auberge/` (mkdir si nécessaire).
    2. **Créer `lib/auberge/auto-tick.ts`** :
       ```ts
       /**
        * tickAubergeAuto — orchestrateur fire-and-forget Phase 46.
        *
        * Appelé depuis useVault.ts (launch + task complete). Lit le profil ferme,
        * expire les visiteurs périmés, tente un spawn, persiste l'état Auberge en
        * 1 seul writeFile farm, et schedule/cancel les notifs locales correspondantes.
        *
        * Ne throw JAMAIS. Si vault indisponible ou profil introuvable → no-op silencieux.
        * Idempotent : le cooldown 6h global du moteur empêche le re-spawn.
        */
       import type { VaultManager } from '../vault';
       import type { Profile } from '../types';
       import { parseFarmProfile, serializeFarmProfile } from '../parser';
       import {
         parseAuberge, serializeAuberge,
         expireVisitors as engineExpire,
         spawnVisitor as engineSpawn,
         getTotalReputation as engineGetTotalReputation,
       } from '../mascot/auberge-engine';
       import { getTreeStageInfo } from '../mascot/engine';
       import { VISITOR_CATALOG } from '../mascot/visitor-catalog';
       import {
         scheduleAubergeVisitorArrival,
         scheduleAubergeVisitorReminder,
         cancelAubergeVisitorNotifs,
       } from '../scheduled-notifications';

       export interface AutoTickDeps {
         vault: VaultManager;
         profiles: Profile[];
       }

       export async function tickAubergeAuto(
         profileId: string,
         deps: AutoTickDeps,
       ): Promise<void> {
         try {
           const { vault, profiles } = deps;
           if (!vault) return;
           const profile = profiles.find(p => p.id === profileId);
           if (!profile) return;

           const file = `farm-${profileId}.md`;
           const content = await vault.readFile(file).catch(() => '');
           const farmData = parseFarmProfile(content);

           const currentState = parseAuberge({
             visitors: farmData.auberge_visitors,
             reputations: farmData.auberge_reputations,
             lastSpawn: farmData.auberge_last_spawn,
             totalDeliveries: farmData.auberge_total_deliveries,
           });

           const now = new Date();

           // 1. Expire (cancel notifs des expirés)
           const expireResult = engineExpire(currentState, now);
           let nextState = expireResult.state;
           for (const exp of expireResult.expired) {
             await cancelAubergeVisitorNotifs(exp.instanceId).catch(() => {});
           }

           // 2. Tente spawn
           const treeStage = getTreeStageInfo(profile.level ?? 1).stage;
           const totalRep = engineGetTotalReputation(nextState);
           const spawnResult = engineSpawn(nextState, treeStage, now, totalRep);
           if (spawnResult) {
             nextState = spawnResult.state;
             const v = spawnResult.visitor;
             const def = VISITOR_CATALOG.find(d => d.id === v.visitorId);
             if (def) {
               const deadlineDate = new Date(v.deadlineAt);
               const hours = Math.max(1, Math.round((deadlineDate.getTime() - now.getTime()) / 3600000));
               await scheduleAubergeVisitorArrival(v.instanceId, def.name, def.emoji, hours).catch(() => {});
               await scheduleAubergeVisitorReminder(v.instanceId, def.name, def.emoji, deadlineDate).catch(() => {});
             }
           }

           // 3. Persist (1 seul writeFile)
           const serialized = serializeAuberge(nextState, now);
           farmData.auberge_visitors = serialized.visitors || undefined;
           farmData.auberge_reputations = serialized.reputations || undefined;
           farmData.auberge_last_spawn = serialized.lastSpawn;
           farmData.auberge_total_deliveries = serialized.totalDeliveries > 0 ? serialized.totalDeliveries : undefined;

           await vault.writeFile(file, serializeFarmProfile(profile.name, farmData));
         } catch (e) {
           if (__DEV__) console.warn('[tickAubergeAuto]', e);
         }
       }
       ```
    3. **Créer `lib/__tests__/auberge-auto-tick.test.ts`** : suivre le pattern des autres tests Jest du dossier (ex. `auberge-engine.test.ts`). Mocker :
       - `expo-notifications` (cf. `lib/__tests__/__mocks__/`) — fournir stubs `scheduleNotificationAsync`, `getAllScheduledNotificationsAsync`, `cancelScheduledNotificationAsync`, `getPermissionsAsync`, `requestPermissionsAsync`.
       - `VaultManager` minimal (objet `{ readFile, writeFile }` avec `jest.fn()`).
       - `profiles: [{ id: 'p1', name: 'Papa', level: 5, ... }]` (cast partiel `as Profile`).
       - Tester les 5 cas listés en `<behavior>`.
       - Vérifier les appels à `scheduleAubergeVisitorArrival` / `cancelAubergeVisitorNotifs` via les mocks `Notifications.scheduleNotificationAsync` etc.
    4. `npx tsc --noEmit` clean.
    5. `npx jest --no-coverage lib/__tests__/auberge-auto-tick.test.ts` → tous verts.
    6. Vérifier que `npx jest --no-coverage lib/__tests__/auberge-engine.test.ts` reste 45/45 verts (non-régression).
  </action>
  <verify>
    <automated>npx jest --no-coverage lib/__tests__/auberge-auto-tick.test.ts lib/__tests__/auberge-engine.test.ts</automated>
  </verify>
  <done>`lib/auberge/auto-tick.ts` exporte `tickAubergeAuto` ; tests Jest passent ; auberge-engine.test.ts toujours 45/45 ; tsc clean.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` : 0 nouvelle erreur (baseline pré-existantes ignorées).
- `npx jest --no-coverage lib/__tests__/auberge-*.test.ts` : tous verts.
- `grep -n "auberge_visitor_reminder" lib/types.ts lib/notifications.ts` : 2+ occurrences.
- `grep -n "scheduleAubergeVisitorArrival\|scheduleAubergeVisitorReminder\|cancelAubergeVisitorNotifs" lib/scheduled-notifications.ts` : 3 exports.
- `grep -n "tickAubergeAuto" lib/auberge/auto-tick.ts` : 1 export.
</verification>

<success_criteria>
- Helper `tickAubergeAuto` testable en isolation Jest (sans React).
- 3 fonctions notif Auberge exportées depuis `scheduled-notifications.ts`.
- Toggle BUILTIN visible dans NotificationSettings au prochain re-render.
- Aucune modification de `useVault.ts`, `useAuberge.ts`, `VaultContext.tsx`, ou `lib/vault-cache.ts`.
- Aucun bump CACHE_VERSION nécessaire.
</success_criteria>

<output>
Après complétion, créer `.planning/phases/46-auberge-spawn-automatique-notifications-locales/46-01-SUMMARY.md`
</output>
