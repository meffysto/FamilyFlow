---
phase: 46-auberge-spawn-automatique-notifications-locales
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - components/mascot/AubergeSheet.tsx
autonomous: true
requirements: [AUBERGE-46-05]
must_haves:
  truths:
    - "Le bouton 'Forcer un visiteur (dev)' n'est PAS visible en build release"
    - "Le bouton reste visible et fonctionnel en build dev (__DEV__)"
  artifacts:
    - path: "components/mascot/AubergeSheet.tsx"
      provides: "Bouton dev ré-encadré par {__DEV__ && ...}"
      contains: "{__DEV__"
  key_links: []
---

<objective>
Restaurer le gating `__DEV__` autour du bouton "🪄 Forcer un visiteur (dev)" dans `components/mascot/AubergeSheet.tsx`. Ce bouton avait été temporairement exposé en release pour tester la Phase 45 (commit `28d7810`) — maintenant que le spawn auto fonctionne (Plans 46-01 + 46-02), le bouton doit redevenir dev-only.

Purpose: hygiène release — pas de bouton "magique" exposé aux utilisateurs finaux.
Output: 1 modification ciblée dans `AubergeSheet.tsx` (entourer le `<TouchableOpacity>` du bouton dev d'un `{__DEV__ && (...)}`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/46-auberge-spawn-automatique-notifications-locales/46-CONTEXT.md
@components/mascot/AubergeSheet.tsx
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1 : Re-gate __DEV__ autour du bouton "Forcer un visiteur"</name>
  <files>components/mascot/AubergeSheet.tsx</files>
  <action>
    1. Localiser le bloc autour de la ligne 544-557 :
       ```tsx
       {/* Bouton dev — temporairement exposé en release pour test Phase 45 (à remettre derrière __DEV__ avant ship) */}
       <TouchableOpacity
         onPress={handleForceSpawn}
         style={[
           styles.devBtn,
           { backgroundColor: colors.cardAlt, borderColor: colors.border },
         ]}
         accessibilityLabel="Forcer un visiteur (dev)"
         accessibilityRole="button"
       >
         <Text style={[styles.devBtnText, { color: colors.textMuted }]}>
           🪄 Forcer un visiteur (dev)
         </Text>
       </TouchableOpacity>
       ```
    2. Remplacer par :
       ```tsx
       {/* Phase 46 : bouton dev re-gated derrière __DEV__ (le spawn auto prend le relais en release) */}
       {__DEV__ && (
         <TouchableOpacity
           onPress={handleForceSpawn}
           style={[
             styles.devBtn,
             { backgroundColor: colors.cardAlt, borderColor: colors.border },
           ]}
           accessibilityLabel="Forcer un visiteur (dev)"
           accessibilityRole="button"
         >
           <Text style={[styles.devBtnText, { color: colors.textMuted }]}>
             🪄 Forcer un visiteur (dev)
           </Text>
         </TouchableOpacity>
       )}
       ```
    3. Ne PAS retirer `forceSpawn` de la déstructuration `useAuberge()` (ligne 327) ni `handleForceSpawn` (lignes 422-438) — ces refs restent utiles en dev et le code mort éventuel sera tree-shaken en release par Metro/Hermes (`__DEV__` est une constante remplacée par false en release).
    4. `npx tsc --noEmit` clean.
  </action>
  <verify>
    <automated>grep -n "{__DEV__ && (" components/mascot/AubergeSheet.tsx | head -5 && npx tsc --noEmit 2>&1 | grep -c "AubergeSheet.tsx" | grep -q "^0$"</automated>
  </verify>
  <done>Le bouton est entouré de `{__DEV__ && (...)}` ; le commentaire de wrapping est mis à jour ; tsc clean.</done>
</task>

</tasks>

<verification>
- `grep -B 1 -A 1 "Forcer un visiteur" components/mascot/AubergeSheet.tsx` : confirme le wrapping `__DEV__`.
- `npx tsc --noEmit` : 0 nouvelle erreur.
- Smoke test : en build dev, le bouton reste visible. En build release (TestFlight), il disparaît.
</verification>

<success_criteria>
- Aucune modif d'autre fichier que `components/mascot/AubergeSheet.tsx`.
- Aucun retrait de `forceSpawn` ou `handleForceSpawn` (ces refs sont conservées pour le dev workflow).
- Plan parallélisable avec 46-01 (aucune dépendance).
</success_criteria>

<output>
Après complétion, créer `.planning/phases/46-auberge-spawn-automatique-notifications-locales/46-04-SUMMARY.md`
</output>
