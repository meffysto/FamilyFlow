---
phase: 46-auberge-spawn-automatique-notifications-locales
plan: 03
type: execute
wave: 2
depends_on: [46-01]
files_modified:
  - hooks/useAuberge.ts
autonomous: true
requirements: [AUBERGE-46-03]
must_haves:
  truths:
    - "Sur deliverVisitor, les notifs (arrivée + reminder) du visiteur sont annulées après la persistance ferme"
    - "Sur dismissVisitor, les notifs du visiteur sont annulées après la persistance ferme"
    - "L'annulation est idempotente — un second appel ne throw pas"
  artifacts:
    - path: "hooks/useAuberge.ts"
      provides: "deliverVisitor/dismissVisitor cancel les notifs locales correspondantes"
      contains: "cancelAubergeVisitorNotifs"
  key_links:
    - from: "hooks/useAuberge.ts"
      to: "lib/scheduled-notifications.ts"
      via: "import { cancelAubergeVisitorNotifs }"
      pattern: "cancelAubergeVisitorNotifs"
---

<objective>
Étendre `hooks/useAuberge.ts` pour annuler proprement les notifs locales d'un visiteur lorsqu'il est livré (`deliverVisitor`) ou refusé (`dismissVisitor`). Évite les "notifs fantômes" qui annoncent un rappel H-4 alors que la commande est déjà résolue.

Purpose: hygiène utilisateur — pas de notif de rappel pour une commande livrée il y a 2h.
Output: 2 appels `cancelAubergeVisitorNotifs` ajoutés dans `hooks/useAuberge.ts` (1 dans `deliverVisitor`, 1 dans `dismissVisitor`), placés APRÈS la persistance ferme réussie (mêmes garanties d'ordre que le crédit coins).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/46-auberge-spawn-automatique-notifications-locales/46-CONTEXT.md
@.planning/phases/46-auberge-spawn-automatique-notifications-locales/46-01-PLAN.md
@hooks/useAuberge.ts
@CLAUDE.md

<interfaces>
From lib/scheduled-notifications.ts (créé en 46-01) :
```typescript
export async function cancelAubergeVisitorNotifs(instanceId: string): Promise<void>;
// Idempotent — try/catch silencieux. Ne throw jamais.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 : Cancel notifs sur deliverVisitor + dismissVisitor</name>
  <files>hooks/useAuberge.ts</files>
  <action>
    1. Ajouter l'import en haut du fichier (sous les imports existants de `../lib/parser` et autres) :
       ```ts
       import { cancelAubergeVisitorNotifs } from '../lib/scheduled-notifications';
       ```
    2. Dans `deliverVisitor` (lignes 140-240) : juste APRÈS le `await refreshGamification();` (ligne ~237) et AVANT le `return { ok: true, reward: result.reward };` final, ajouter :
       ```ts
       // Phase 46 : cancel notifs locales du visiteur livré (idempotent, fire-and-forget)
       cancelAubergeVisitorNotifs(instanceId).catch(() => {});
       ```
    3. Dans `dismissVisitor` (lignes 243-264) : juste APRÈS le `await refreshFarm(profileId);` final (ligne ~263), ajouter :
       ```ts
       // Phase 46 : cancel notifs locales du visiteur refusé (idempotent, fire-and-forget)
       cancelAubergeVisitorNotifs(instanceId).catch(() => {});
       ```
    4. Note : l'expiration via `tickAuberge` est déjà gérée côté `tickAubergeAuto` (Plan 46-01), pas dans ce hook. Ne PAS modifier `tickAuberge` ni `forceSpawn` ici.
    5. `npx tsc --noEmit` clean.
  </action>
  <verify>
    <automated>grep -c "cancelAubergeVisitorNotifs" hooks/useAuberge.ts | grep -qE "^[3-9]$|^[0-9]{2,}$" && echo OK || echo "expected ≥3 occurrences (1 import + 2 calls)"</automated>
  </verify>
  <done>2 appels `cancelAubergeVisitorNotifs(instanceId)` ajoutés (1 dans deliverVisitor, 1 dans dismissVisitor), tous deux APRÈS la persistance vault. Fire-and-forget. tsc clean.</done>
</task>

</tasks>

<verification>
- `grep -c "cancelAubergeVisitorNotifs" hooks/useAuberge.ts` : ≥ 3 (import + 2 calls).
- `npx tsc --noEmit` : 0 nouvelle erreur.
- Smoke test manuel suggéré : forcer un visiteur via bouton dev → livrer → vérifier qu'aucune notif H-4 n'arrive plus tard.
</verification>

<success_criteria>
- Aucune modif d'autre fichier que `hooks/useAuberge.ts`.
- Pas de bump CACHE_VERSION.
- Pattern fire-and-forget respecté : la cancel ne bloque ni la livraison ni la refusal.
</success_criteria>

<output>
Après complétion, créer `.planning/phases/46-auberge-spawn-automatique-notifications-locales/46-03-SUMMARY.md`
</output>
