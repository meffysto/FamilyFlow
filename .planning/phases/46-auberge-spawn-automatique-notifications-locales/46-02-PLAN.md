---
phase: 46-auberge-spawn-automatique-notifications-locales
plan: 02
type: execute
wave: 2
depends_on: [46-01]
files_modified:
  - hooks/useVault.ts
autonomous: true
requirements: [AUBERGE-46-01, AUBERGE-46-02]
must_haves:
  truths:
    - "Au launch (et à chaque refresh vault), un appel fire-and-forget tente un tick Auberge sur le profil actif"
    - "Après chaque transition tâche false→true, un appel fire-and-forget tente un tick Auberge"
    - "Le tick ne bloque jamais le refresh principal (catch silencieux)"
  artifacts:
    - path: "hooks/useVault.ts"
      provides: "Wiring tickAubergeAuto au launch + souscription subscribeTaskComplete"
      contains: "tickAubergeAuto"
  key_links:
    - from: "hooks/useVault.ts"
      to: "lib/auberge/auto-tick.ts"
      via: "import { tickAubergeAuto }"
      pattern: "from '../lib/auberge/auto-tick'"
    - from: "hooks/useVault.ts"
      to: "hooks/useVaultTasks.ts"
      via: "subscribeTaskComplete listener"
      pattern: "subscribeTaskComplete\\("
---

<objective>
Wirer `tickAubergeAuto` du Plan 46-01 dans `hooks/useVault.ts` à deux moments :
1. Après chaque refresh complet du vault (cohabite avec `setupAllNotifications` ligne ~1548).
2. Sur chaque tâche complétée (transition false→true) via `subscribeTaskComplete`.

Purpose: faire passer l'Auberge de "démo bouton dev" à "feature jouable" — un visiteur peut apparaître naturellement sans intervention utilisateur, dans le respect du cooldown 6h global du moteur.
Output: 2 modifications ciblées dans `hooks/useVault.ts` (1 appel post-refresh + 1 useEffect d'abonnement).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/46-auberge-spawn-automatique-notifications-locales/46-CONTEXT.md
@.planning/phases/46-auberge-spawn-automatique-notifications-locales/46-01-PLAN.md
@hooks/useVault.ts
@hooks/useVaultTasks.ts
@CLAUDE.md

<interfaces>
From lib/auberge/auto-tick.ts (créé en 46-01) :
```typescript
export interface AutoTickDeps { vault: VaultManager; profiles: Profile[]; }
export async function tickAubergeAuto(profileId: string, deps: AutoTickDeps): Promise<void>;
// Ne throw JAMAIS. No-op silencieux si vault/profile absent.
```

From hooks/useVaultTasks.ts:66 :
```typescript
const subscribeTaskComplete: (listener: (task: Task) => void | Promise<void>) => () => void;
```
Exposé via le retour de `useVaultTasks` puis remonté dans `VaultState` (vérifier dans useVault.ts comment `subscribeTaskComplete` est exposé — ligne ~1700+).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 : Appel tickAubergeAuto post-refresh dans useVault.ts</name>
  <files>hooks/useVault.ts</files>
  <action>
    1. Ajouter en haut du fichier l'import :
       ```ts
       import { tickAubergeAuto } from '../lib/auberge/auto-tick';
       ```
    2. Localiser la fonction de refresh (autour de la ligne 1548 où `setupAllNotifications(...)` est appelé). Identifier la variable `activeProfile` (ou `profilesSnapshot` + l'ID actif) accessible à ce moment.
    3. Juste après le bloc `setupAllNotifications({...}).catch(() => {})` (après la ligne ~1554), ajouter un appel fire-and-forget :
       ```ts
       // Phase 46 : tente un spawn Auberge auto sur le profil actif (cooldown 6h géré par le moteur)
       const activeProfileId = profilesSnapshot.find(p => p.id === activeProfile?.id)?.id ?? activeProfile?.id;
       if (activeProfileId && vault) {
         tickAubergeAuto(activeProfileId, { vault, profiles: profilesSnapshot }).catch(() => {});
       }
       ```
       (Adapter selon les noms réels disponibles dans la closure — relire les lignes 1500-1620 pour identifier `vault`, `profiles`/`profilesSnapshot`, `activeProfile`. La règle : ne PAS bloquer le refresh, ne PAS attendre la promesse.)
    4. Vérifier que ça ne casse rien : `npx tsc --noEmit`.
    5. **Ne PAS** modifier `setupAllNotifications` ni le bloc défi spawn — l'ajout est strictement additif.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep "useVault.ts" | grep -v "useVault.ts:[0-9]*:[0-9]*.*pré-existant" | head -5</automated>
  </verify>
  <done>Appel `tickAubergeAuto` ajouté dans la fonction de refresh, après `setupAllNotifications`. tsc clean (aucune nouvelle erreur).</done>
</task>

<task type="auto">
  <name>Task 2 : Souscription subscribeTaskComplete dans useVault.ts</name>
  <files>hooks/useVault.ts</files>
  <action>
    1. Identifier l'endroit où `subscribeTaskComplete` est exposé dans `useVaultInternal` (provenant de `useVaultTasks`). Grep `subscribeTaskComplete` dans `hooks/useVault.ts` pour voir s'il est déjà déstructuré.
    2. Ajouter un `useEffect` dans `useVaultInternal` (ou wrapper équivalent) qui s'abonne au listener une seule fois à la mount du contexte :
       ```ts
       // Phase 46 : tick Auberge auto sur chaque tâche complétée (transition false→true)
       useEffect(() => {
         const unsub = tasksHook.subscribeTaskComplete(async (_task) => {
           const activeId = activeProfile?.id;
           if (!activeId || !vault) return;
           tickAubergeAuto(activeId, { vault, profiles }).catch(() => {});
         });
         return unsub;
       }, [tasksHook.subscribeTaskComplete, activeProfile?.id, vault, profiles]);
       ```
       (Adapter aux noms réels — `tasksHook` peut être nommé différemment dans useVault.ts ; le subscribe doit être stable via useCallback côté useVaultTasks.ts:66, ce qui est déjà le cas.)
    3. Si `useEffect` n'est pas déjà importé en haut, l'ajouter à l'import React.
    4. Le listener est fire-and-forget — il ne bloque PAS la complétion de la tâche (cf. pattern useVaultTasks.ts:99-114).
    5. `npx tsc --noEmit` clean.
    6. Test manuel suggéré (post-ship) : compléter une tâche → vérifier qu'un visiteur peut apparaître (respectant le cooldown 6h depuis le précédent spawn).
  </action>
  <verify>
    <automated>grep -c "tickAubergeAuto" hooks/useVault.ts | grep -E "^[2-9]$|^[0-9]{2,}$" && npx tsc --noEmit 2>&1 | grep -c "error TS" | head -1</automated>
  </verify>
  <done>useEffect d'abonnement ajouté ; `tickAubergeAuto` appelé sur chaque task complete ; tsc clean ; aucun nouveau warn ESLint sur les deps useEffect.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` : 0 nouvelle erreur.
- `grep -c "tickAubergeAuto" hooks/useVault.ts` : ≥ 2 (import + 2 call sites).
- Smoke test manuel suggéré : reload de l'app → l'arrivée d'un visiteur peut déclencher la notif locale (si stade ferme ≥ pousse + cooldown OK + permission OS).
</verification>

<success_criteria>
- Un seul module modifié : `hooks/useVault.ts`.
- Aucune modif de `VaultContext.tsx`, `useAuberge.ts`, `lib/vault-cache.ts`.
- Pas de bump CACHE_VERSION.
- Le wiring est strictement additif et fire-and-forget (aucune chance de bloquer le refresh ou la complétion de tâche).
</success_criteria>

<output>
Après complétion, créer `.planning/phases/46-auberge-spawn-automatique-notifications-locales/46-02-SUMMARY.md`
</output>
