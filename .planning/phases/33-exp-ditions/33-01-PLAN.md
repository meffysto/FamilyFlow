---
phase: 33-exp-ditions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/types.ts
  - lib/parser.ts
  - lib/mascot/expedition-engine.ts
autonomous: true
requirements: [VILL-16, VILL-18, VILL-19, VILL-20]

must_haves:
  truths:
    - "Le moteur d'expedition calcule correctement les timers (complete/remaining)"
    - "Le roll de resultat respecte les probabilites par difficulte (60/25/10/5 easy, 40/30/20/10 medium, 25/25/30/20 hard)"
    - "Le pity system garantit une reussite apres 5 echecs consecutifs"
    - "Le pool quotidien est deterministe pour une date donnee"
    - "Les expeditions actives survivent un restart (serialisation CSV dans farm-{id}.md)"
  artifacts:
    - path: "lib/mascot/expedition-engine.ts"
      provides: "Logique pure expeditions : catalogue, roll, timer, pity, pool quotidien"
      exports: ["EXPEDITION_CATALOG", "EXPEDITION_DROP_RATES", "MAX_ACTIVE_EXPEDITIONS", "getDailyExpeditionPool", "rollExpeditionResult", "rollExpeditionLoot", "isExpeditionComplete", "getExpeditionRemainingMinutes", "getExpeditionCostDescription", "canAffordExpedition"]
    - path: "lib/types.ts"
      provides: "ActiveExpedition interface + types ExpeditionDifficulty/ExpeditionOutcome + champs FarmProfileData"
      contains: "interface ActiveExpedition"
    - path: "lib/parser.ts"
      provides: "parseActiveExpeditions + serializeActiveExpeditions + integration dans parseFarmProfile/serializeFarmProfile"
      exports: ["parseActiveExpeditions", "serializeActiveExpeditions"]
  key_links:
    - from: "lib/parser.ts"
      to: "lib/types.ts"
      via: "ActiveExpedition type import"
      pattern: "import.*ActiveExpedition.*from.*types"
    - from: "lib/mascot/expedition-engine.ts"
      to: "lib/types.ts"
      via: "ActiveExpedition + ExpeditionDifficulty imports"
      pattern: "import.*ActiveExpedition.*from.*types"
    - from: "lib/parser.ts parseFarmProfile"
      to: "lib/parser.ts parseActiveExpeditions"
      via: "inline call dans parseFarmProfile return"
      pattern: "parseActiveExpeditions\\(props\\.active_expeditions\\)"
---

<objective>
Creer la couche data et le moteur logique du systeme d'expeditions : types, serialisation CSV dans farm-{id}.md, et logique pure (catalogue, roll probabiliste, timer, pity system, pool quotidien deterministe).

Purpose: Fournir toute l'infrastructure algorithmique pour que les plans suivants (hook + UI) puissent lancer, timer et resoudre des expeditions sans reimplementer la logique.
Output: 3 fichiers — types etendus, parser etendu, expedition-engine.ts complet et testable.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-exp-ditions/33-CONTEXT.md
@.planning/phases/33-exp-ditions/33-RESEARCH.md

<interfaces>
<!-- Source of truth pour FarmProfileData (lib/types.ts:576-603) -->
export interface FarmProfileData {
  treeSpecies?: TreeSpecies;
  mascotDecorations: string[];
  mascotInhabitants: string[];
  mascotPlacements: Record<string, string>;
  farmCrops?: string;
  farmBuildings?: PlacedBuilding[];
  farmInventory?: FarmInventory;
  harvestInventory?: HarvestInventory;
  craftedItems?: CraftedItem[];
  farmTech?: string[];
  farmRareSeeds?: RareSeedInventory;
  wearEvents?: WearEvent[];
  companion?: CompanionData | null;
  giftHistory?: string;
  giftsSentToday?: string;
  buildingTurboUntil?: string;
  growthSprintUntil?: string;
  capacityBoostUntil?: string;
  nextHarvestGolden?: boolean;
  unlockedEffectRecipes?: string[];
  effectGoldenMultiplier?: number;
  village_claimed_week?: string;
  trade_claimed_codes?: string[];
  trade_sent_today?: string;
}

<!-- parseFarmProfile pattern (lib/parser.ts:574-638) -->
// Reads key: value lines, splits on first ': '
// Return object with typed fields
// serializeFarmProfile (645-687) rebuilds entire file via lines.push()

<!-- building-engine.ts ISO date pattern -->
// restParts.join(':') to reconstruct ISO date from colon-split CSV

<!-- rewards.ts pity pattern -->
// PITY_THRESHOLD = 5 — after 5 boxes without epic+, next guarantees epic
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Types ActiveExpedition + extensions FarmProfileData + parser CSV</name>
  <files>lib/types.ts, lib/parser.ts</files>
  <read_first>lib/types.ts, lib/parser.ts, lib/mascot/building-engine.ts</read_first>
  <behavior>
    - parseActiveExpeditions('foret_facile:easy:2026-04-14T10:30:00.000Z:4:') retourne [{missionId:'foret_facile', difficulty:'easy', startedAt:'2026-04-14T10:30:00.000Z', durationHours:4, result:undefined}]
    - parseActiveExpeditions('foret_facile:easy:2026-04-14T10:30:00.000Z:4:success') retourne [{...result:'success'}]
    - parseActiveExpeditions('a:easy:2026-04-14T10:30:00.000Z:4:|b:hard:2026-04-14T12:00:00.000Z:24:') retourne 2 elements
    - parseActiveExpeditions(undefined) retourne []
    - parseActiveExpeditions('') retourne []
    - serializeActiveExpeditions([{missionId:'foret_facile', difficulty:'easy', startedAt:'2026-04-14T10:30:00.000Z', durationHours:4}]) retourne 'foret_facile:easy:2026-04-14T10:30:00.000Z:4:'
    - Round-trip: parseActiveExpeditions(serializeActiveExpeditions(exps)) === exps
    - parseFarmProfile avec ligne 'active_expeditions: ...' retourne les expeditions parsees
    - serializeFarmProfile inclut active_expeditions si present et non-vide
  </behavior>
  <action>
1. Dans lib/types.ts, APRES la ligne 603 (fin de FarmProfileData), ajouter :
   ```typescript
   export type ExpeditionDifficulty = 'easy' | 'medium' | 'hard';
   export type ExpeditionOutcome = 'success' | 'partial' | 'failure' | 'rare_discovery';
   
   export interface ActiveExpedition {
     missionId: string;
     difficulty: ExpeditionDifficulty;
     startedAt: string;           // ISO string
     durationHours: number;       // 4 | 12 | 24
     result?: ExpeditionOutcome;  // undefined = pas encore collecte
     lootItemId?: string;         // ID item obtenu
     lootType?: 'inhabitant' | 'seed' | 'booster';
   }
   ```
   Dans FarmProfileData (avant la fermeture }), ajouter :
   ```typescript
   // Phase 33 — Expeditions
   activeExpeditions?: ActiveExpedition[];
   expeditionPity?: number;
   ```

2. Dans lib/parser.ts, ajouter deux fonctions AVANT parseFarmProfile :
   ```typescript
   export function parseActiveExpeditions(csv: string | undefined): ActiveExpedition[] {
     if (!csv) return [];
     return csv.split('|').map(entry => {
       const parts = entry.trim().split(':');
       if (parts.length < 5) return null;
       const missionId = parts[0];
       const difficulty = parts[1] as ExpeditionDifficulty;
       const durationHours = parseInt(parts[parts.length - 2], 10);
       const resultRaw = parts[parts.length - 1] || undefined;
       const startedAt = parts.slice(2, parts.length - 2).join(':');
       if (!missionId || !startedAt || isNaN(durationHours)) return null;
       return { missionId, difficulty, startedAt, durationHours, result: resultRaw as ExpeditionOutcome | undefined } as ActiveExpedition;
     }).filter((e): e is ActiveExpedition => e !== null);
   }

   export function serializeActiveExpeditions(exps: ActiveExpedition[]): string {
     return exps.map(e =>
       `${e.missionId}:${e.difficulty}:${e.startedAt}:${e.durationHours}:${e.result ?? ''}`
       + (e.lootItemId ? `:${e.lootItemId}:${e.lootType ?? ''}` : '')
     ).join('|');
   }
   ```
   ATTENTION au pattern ISO ':' — le startedAt contient des ':'. Utiliser `parts.slice(2, parts.length - 2).join(':')` exactement comme building-engine.ts ligne 68.

3. Dans parseFarmProfile (return block ~ligne 609), ajouter :
   ```typescript
   activeExpeditions: parseActiveExpeditions(props.active_expeditions),
   expeditionPity: props.expedition_pity ? parseInt(props.expedition_pity, 10) : 0,
   ```

4. Dans serializeFarmProfile (~ligne 684, avant le return), ajouter :
   ```typescript
   if (data.activeExpeditions && data.activeExpeditions.length > 0) {
     lines.push(`active_expeditions: ${serializeActiveExpeditions(data.activeExpeditions)}`);
   }
   if (data.expeditionPity && data.expeditionPity > 0) {
     lines.push(`expedition_pity: ${data.expeditionPity}`);
   }
   ```

5. Ajouter import ActiveExpedition et ExpeditionDifficulty/ExpeditionOutcome dans parser.ts depuis types.ts.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - lib/types.ts contient `export interface ActiveExpedition {`
    - lib/types.ts contient `export type ExpeditionDifficulty =`
    - lib/types.ts contient `export type ExpeditionOutcome =`
    - FarmProfileData contient `activeExpeditions?: ActiveExpedition[]`
    - FarmProfileData contient `expeditionPity?: number`
    - lib/parser.ts contient `export function parseActiveExpeditions(`
    - lib/parser.ts contient `export function serializeActiveExpeditions(`
    - lib/parser.ts parseFarmProfile return contient `activeExpeditions: parseActiveExpeditions(`
    - lib/parser.ts serializeFarmProfile contient `active_expeditions: ${serializeActiveExpeditions(`
    - lib/parser.ts contient `parts.slice(2, parts.length - 2).join(':')`
    - npx tsc --noEmit ne produit pas de nouvelles erreurs
  </acceptance_criteria>
  <done>Types ActiveExpedition exportes, FarmProfileData etendu, parser CSV round-trip fonctionnel, tsc passe</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expedition Engine — catalogue, roll, timer, pity, pool quotidien</name>
  <files>lib/mascot/expedition-engine.ts, lib/__tests__/expedition-engine.test.ts</files>
  <read_first>lib/types.ts, lib/mascot/building-engine.ts, lib/gamification/rewards.ts, lib/mascot/types.ts</read_first>
  <behavior>
    - isExpeditionComplete({startedAt:'2026-04-14T10:00:00.000Z', durationHours:4}, new Date('2026-04-14T15:00:00.000Z')) === true
    - isExpeditionComplete({startedAt:'2026-04-14T10:00:00.000Z', durationHours:4}, new Date('2026-04-14T13:00:00.000Z')) === false
    - getExpeditionRemainingMinutes({startedAt:'2026-04-14T10:00:00.000Z', durationHours:4}, new Date('2026-04-14T13:00:00.000Z')) === 60
    - getExpeditionRemainingMinutes retourne 0 si deja complete
    - rollExpeditionResult('easy', 5) === 'success' (pity guarantee)
    - rollExpeditionResult('easy', 0) retourne un des 4 outcomes valides
    - getDailyExpeditionPool('2026-04-14') retourne exactement 3 missions (1 easy, 1 medium, 1 hard)
    - getDailyExpeditionPool('2026-04-14') === getDailyExpeditionPool('2026-04-14') (deterministe)
    - getDailyExpeditionPool('2026-04-15') !== getDailyExpeditionPool('2026-04-14') (varie par jour, au moins un element different)
    - EXPEDITION_CATALOG contient au moins 9 missions (3 par difficulte)
    - canAffordExpedition verifie coins + harvestInventory
    - MAX_ACTIVE_EXPEDITIONS === 2
  </behavior>
  <action>
Creer lib/mascot/expedition-engine.ts avec le contenu suivant :

1. **Constantes** :
   ```typescript
   export const MAX_ACTIVE_EXPEDITIONS = 2;
   ```

2. **Types internes** :
   ```typescript
   export interface ExpeditionMission {
     id: string;
     name: string;
     emoji: string;
     difficulty: ExpeditionDifficulty;
     durationHours: number;
     costCoins: number;
     costCrops: { cropId: string; quantity: number }[];
     description: string;
   }
   
   export interface ExpeditionLoot {
     itemId: string;
     type: 'inhabitant' | 'seed' | 'booster';
     label: string;
     emoji: string;
   }
   ```

3. **EXPEDITION_CATALOG** — 9 missions statiques (3 par difficulte) avec les valeurs du UI-SPEC :
   - Facile (4h, 40-50 feuilles, 2 recoltes) : foret_facile/riviere_facile/prairie_facile
   - Moyen (12h, 120-200 feuilles, 3-5 recoltes mix) : montagne_moyen/ocean_moyen/caverne_moyen
   - Dur (24h, 350-500 feuilles, 5-10 recoltes multi-types) : volcan_dur/toundra_dur/nuages_dur
   Utiliser les noms/emojis du UI-SPEC (Foret Ancienne 🌲, Sommets Brumeux ⛰️, etc.).

4. **EXPEDITION_DROP_RATES** — Record<ExpeditionDifficulty, Record<ExpeditionOutcome, number>> :
   - easy: { success: 0.60, partial: 0.25, failure: 0.12, rare_discovery: 0.03 }
   - medium: { success: 0.40, partial: 0.30, failure: 0.20, rare_discovery: 0.10 }
   - hard: { success: 0.25, partial: 0.25, failure: 0.30, rare_discovery: 0.20 }

5. **EXPEDITION_LOOT_TABLE** — Record<ExpeditionDifficulty, ExpeditionLoot[]> avec les exclusifs du UI-SPEC :
   - easy: boost_recolte_2x
   - medium: renard_expedition, fleur_de_lave, boost_recolte_2x, boost_production_2x
   - hard: aigle_expedition, graine_celeste, fleur_de_lave, boost_chance_doree, boost_production_2x

6. **Fonctions timer** (pattern building-engine.ts) :
   ```typescript
   export function isExpeditionComplete(exp: ActiveExpedition, now = new Date()): boolean
   export function getExpeditionRemainingMinutes(exp: ActiveExpedition, now = new Date()): number
   ```

7. **Roll resultat** (pattern rewards.ts DROP_RATES) :
   ```typescript
   export function rollExpeditionResult(difficulty: ExpeditionDifficulty, pityCount: number): ExpeditionOutcome
   export function rollExpeditionLoot(difficulty: ExpeditionDifficulty, outcome: ExpeditionOutcome): ExpeditionLoot | undefined
   ```
   - Pity: si pityCount >= 5, retourner 'success'
   - Pour rollExpeditionLoot: 'failure' → undefined, 'partial' → petites feuilles (pas d'item), 'success'/'rare_discovery' → random dans EXPEDITION_LOOT_TABLE[difficulty]

8. **Pool quotidien** (seed date-based deterministe) :
   ```typescript
   export function getDailyExpeditionPool(date?: string): ExpeditionMission[]
   ```
   - Seed = parseInt(YYYYMMDD) sans tirets
   - LCG pseudo-random : `((seed * 1103515245 + offset * 12345) & 0x7fffffff) / 0x7fffffff`
   - Selectionner 1 mission par difficulte depuis le catalogue filtre

9. **Helpers** :
   ```typescript
   export function canAffordExpedition(mission: ExpeditionMission, coins: number, harvestInventory: Record<string, number>): boolean
   export function getExpeditionCostDescription(mission: ExpeditionMission): string  // "50 🍃 + 2 carotte"
   ```

10. Creer lib/__tests__/expedition-engine.test.ts avec tests pour : timer complete/incomplete, remaining minutes, pity guarantee, pool determinisme, pool 3 missions, canAfford true/false.
  </action>
  <verify>
    <automated>npx jest lib/__tests__/expedition-engine.test.ts --no-coverage 2>&1 | tail -20 && npx tsc --noEmit 2>&1 | head -10</automated>
  </verify>
  <acceptance_criteria>
    - lib/mascot/expedition-engine.ts contient `export const MAX_ACTIVE_EXPEDITIONS = 2`
    - lib/mascot/expedition-engine.ts contient `export const EXPEDITION_CATALOG`
    - lib/mascot/expedition-engine.ts contient `export const EXPEDITION_DROP_RATES`
    - lib/mascot/expedition-engine.ts contient `export function isExpeditionComplete(`
    - lib/mascot/expedition-engine.ts contient `export function getExpeditionRemainingMinutes(`
    - lib/mascot/expedition-engine.ts contient `export function rollExpeditionResult(`
    - lib/mascot/expedition-engine.ts contient `export function rollExpeditionLoot(`
    - lib/mascot/expedition-engine.ts contient `export function getDailyExpeditionPool(`
    - lib/mascot/expedition-engine.ts contient `export function canAffordExpedition(`
    - lib/mascot/expedition-engine.ts contient `pityCount >= 5`
    - lib/__tests__/expedition-engine.test.ts existe et passe avec npx jest
    - npx tsc --noEmit ne produit pas de nouvelles erreurs
  </acceptance_criteria>
  <done>Expedition engine complet avec catalogue 9 missions, timer, roll probabiliste, pity system, pool deterministe, helpers cout. Tests unitaires passent.</done>
</task>

</tasks>

<verification>
- npx tsc --noEmit passe sans nouvelles erreurs
- npx jest lib/__tests__/expedition-engine.test.ts --no-coverage passe
- Round-trip CSV : parseActiveExpeditions(serializeActiveExpeditions(data)) === data
</verification>

<success_criteria>
- ActiveExpedition type exporte dans lib/types.ts
- FarmProfileData etendu avec activeExpeditions + expeditionPity
- parseFarmProfile/serializeFarmProfile gere les nouveaux champs
- expedition-engine.ts exporte toutes les fonctions listees dans must_haves
- Tests unitaires passent pour timer, roll, pity, pool
</success_criteria>

<output>
After completion, create `.planning/phases/33-exp-ditions/33-01-SUMMARY.md`
</output>
