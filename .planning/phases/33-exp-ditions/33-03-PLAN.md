---
phase: 33-exp-ditions
plan: 03
type: execute
wave: 3
depends_on: [33-01, 33-02]
files_modified:
  - components/mascot/ExpeditionsSheet.tsx
  - components/mascot/CampExplorationCell.tsx
  - components/mascot/ExpeditionChest.tsx
  - app/(tabs)/tree.tsx
autonomous: false
requirements: [VILL-16, VILL-17, VILL-18, VILL-19, VILL-20]

must_haves:
  truths:
    - "User peut ouvrir le catalogue d'expeditions en tappant le Camp d'exploration"
    - "User voit 3 missions du jour avec cout, duree et difficulte"
    - "User peut lancer une expedition avec confirmation du risque"
    - "User voit un timer en cours sur les expeditions actives"
    - "User recoit un resultat via un coffre anime avec haptic"
    - "User retrouve ses expeditions apres un restart"
  artifacts:
    - path: "components/mascot/ExpeditionsSheet.tsx"
      provides: "Modal pageSheet avec 3 onglets : Catalogue / En cours / Resultats"
      min_lines: 150
    - path: "components/mascot/CampExplorationCell.tsx"
      provides: "Cellule ferme avec badge actif + countdown"
      min_lines: 40
    - path: "components/mascot/ExpeditionChest.tsx"
      provides: "Coffre anime tap-to-open avec reveal resultat"
      min_lines: 80
    - path: "app/(tabs)/tree.tsx"
      provides: "Integration Camp + ExpeditionsSheet + ExpeditionChest"
      contains: "ExpeditionsSheet"
  key_links:
    - from: "app/(tabs)/tree.tsx"
      to: "hooks/useExpeditions.ts"
      via: "useExpeditions() hook call"
      pattern: "useExpeditions\\(\\)"
    - from: "components/mascot/ExpeditionsSheet.tsx"
      to: "hooks/useExpeditions.ts"
      via: "Props passees depuis tree.tsx (pas d'appel hook direct dans le composant)"
      pattern: "dailyPool|activeExpeditions|launchExpedition"
    - from: "components/mascot/CampExplorationCell.tsx"
      to: "lib/mascot/world-grid.ts"
      via: "CAMP_EXPLORATION_CELL position"
      pattern: "CAMP_EXPLORATION_CELL"
    - from: "app/(tabs)/tree.tsx"
      to: "components/mascot/ExpeditionsSheet.tsx"
      via: "Modal visibility state + props"
      pattern: "showExpeditions.*ExpeditionsSheet"
---

<objective>
Construire les 3 composants UI (modal expeditions, cellule ferme, coffre anime) et les integrer dans tree.tsx pour completer le systeme d'expeditions.

Purpose: Livrer l'experience utilisateur complete — du tap sur le Camp d'exploration au coffre anime revelant le loot.
Output: 3 composants UI + integration tree.tsx. Feature complete et jouable.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-exp-ditions/33-CONTEXT.md
@.planning/phases/33-exp-ditions/33-RESEARCH.md
@.planning/phases/33-exp-ditions/33-UI-SPEC.md
@.planning/phases/33-exp-ditions/33-01-SUMMARY.md
@.planning/phases/33-exp-ditions/33-02-SUMMARY.md

<interfaces>
<!-- From Plan 02 — useExpeditions hook -->
export function useExpeditions(): {
  dailyPool: ExpeditionMission[];
  activeExpeditions: ActiveExpedition[];
  completedExpeditions: ActiveExpedition[];
  pendingResults: ActiveExpedition[];
  activeCount: number;
  canLaunch: boolean;
  pityCount: number;
  launchExpedition: (mission: ExpeditionMission) => Promise<boolean>;
  collectExpedition: (missionId: string) => Promise<{outcome: ExpeditionOutcome, loot?: ExpeditionLoot}>;
  dismissExpedition: (missionId: string) => Promise<void>;
}

<!-- From Plan 01 — expedition-engine.ts -->
export interface ExpeditionMission { id, name, emoji, difficulty, durationHours, costCoins, costCrops, description }
export interface ExpeditionLoot { itemId, type, label, emoji }
export function isExpeditionComplete(exp, now?): boolean;
export function getExpeditionRemainingMinutes(exp, now?): number;
export function getExpeditionCostDescription(mission): string;

<!-- From Plan 02 — world-grid.ts -->
export const CAMP_EXPLORATION_CELL: WorldCell = { id:'camp_exp', x:0.10, y:0.90, size:'large' }

<!-- Existing UI patterns -->
// BuildingsCatalog.tsx : Modal pageSheet + SPRING_CATALOG + AwningStripes + tab pattern
// BuildingSprite.tsx : Pixel art cell rendering in world grid
// ModalHeader : components/ui/ModalHeader
// Chip : components/ui/Chip
// useThemeColors() : colors.bg, colors.card, colors.text, colors.textMuted, colors.success, colors.warning, colors.error, colors.info, primary
// Farm theme : constants/farm-theme.ts — Farm.gold, Farm.parchment, Farm.wood, Farm.accent
// Spacing : constants/spacing.ts — Spacing.xs(4), Spacing.md(8), Spacing['2xl'](16), Spacing['4xl'](24)
// FontSize/FontWeight : constants/typography.ts
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ExpeditionsSheet modal + ExpeditionChest + CampExplorationCell</name>
  <files>components/mascot/ExpeditionsSheet.tsx, components/mascot/CampExplorationCell.tsx, components/mascot/ExpeditionChest.tsx</files>
  <read_first>components/village/BuildingsCatalog.tsx, components/mascot/BuildingShopSheet.tsx, app/(tabs)/tree.tsx, constants/farm-theme.ts, constants/spacing.ts, constants/typography.ts, hooks/useExpeditions.ts, lib/mascot/expedition-engine.ts, .planning/phases/33-exp-ditions/33-UI-SPEC.md</read_first>
  <action>
**ExpeditionsSheet.tsx** — Modal pageSheet avec 3 onglets (calque sur BuildingsCatalog.tsx) :

1. Props :
   ```typescript
   interface Props {
     visible: boolean;
     onClose: () => void;
     dailyPool: ExpeditionMission[];
     activeExpeditions: ActiveExpedition[];
     completedExpeditions: ActiveExpedition[];
     pendingResults: ActiveExpedition[];
     canLaunch: boolean;
     pityCount: number;
     onLaunch: (mission: ExpeditionMission) => void;
     onCollect: (missionId: string) => void;
     onDismiss: (missionId: string) => void;
   }
   ```

2. Structure du Modal :
   - `<Modal visible={visible} presentationStyle="pageSheet" animationType="slide" onRequestClose={onClose}>`
   - AwningStripes en haut (copier le pattern exact de BuildingsCatalog)
   - ModalHeader titre "Expeditions" + icone compass (MaterialCommunityIcons 'compass-outline')
   - Tab row : 3 TouchableOpacity tabs ("Catalogue" / "En cours" / "Resultats")
   - Tab indicator : Animated.View underline primary color, withTiming translateX 150ms (UI-SPEC)
   - ScrollView per active tab

3. Onglet Catalogue :
   - Mapper dailyPool → ExpeditionCard inline (pas composant separe — economie de fichiers)
   - Chaque carte : TouchableOpacity avec border radius Radius.lg
   - Row : emoji (28px) + colonne (nom FontSize.subtitle semibold + duree FontSize.label textMuted)
   - Difficulty badge : Chip colore (success/warning/error selon easy/medium/hard)
   - Cost row : Chip feuilles (Farm.gold bg) + Chip recoltes (colors.catJeux)
   - Bouton "Lancer l'expedition" (primary bg, 44px height) si canLaunch, sinon disabled "Complet" (textMuted, opacity 0.5)
   - Si pityCount >= 4 : note italic "Ta prochaine expedition est protegee ✨" (colors.info, FontSize.caption style italic)

4. Onglet En cours :
   - Mapper activeExpeditions.filter(e => !e.result)
   - Chaque row : emoji + nom + difficulty badge + countdown "Hh Mm restant" (FontSize.label)
   - Countdown via getExpeditionRemainingMinutes() — recalculer toutes les 60s via useEffect + setInterval
   - Si < 30 min : countdown text colors.warning
   - Si complete (remaining === 0) : bouton "Collecter" primary style au lieu du countdown
   - Empty state : "Aucune expedition en cours. Lance une mission depuis le catalogue !"

5. Onglet Resultats :
   - Mapper completedExpeditions (result non-undefined), newest first (sort par startedAt descending)
   - Chaque row : emoji + nom + result badge colore (success=vert, partial=amber, failure=rouge, rare=Farm.gold)
   - Si lootItemId : afficher emoji loot + label
   - Bouton "Supprimer" discret (textMuted) → onDismiss
   - Empty state : "Tes resultats d'expeditions apparaitront ici."

6. Animations spring identiques a BuildingsCatalog : SPRING_CATALOG = { damping: 12, stiffness: 180 }
7. Haptics.selectionAsync() sur switch de tab

**CampExplorationCell.tsx** — Cellule speciale ferme :

1. Props : `{ activeCount: number; hasResult: boolean; shortestRemaining: number; onPress: () => void }`
2. TouchableOpacity avec CAMP_EXPLORATION_CELL.size (64px) + position absolute
3. Sprite : utiliser MaterialCommunityIcons 'tent' ou 'compass-rose' (size 40, Farm.wood color) sur fond Farm.parchment circle
4. Badge top-right si activeCount > 0 : Text "${activeCount}/2" sur cercle Farm.gold bg (20px height, 8px hPad, FontSize.micro)
5. Badge bottom si hasResult : Text "Retour !" sur rect colors.success bg + pulse animation (withRepeat(withSequence(scale 1→1.08→1)), 2 repeats)
6. Si activeCount > 0 et !hasResult : mini countdown text sous le sprite ("3h 24m", FontSize.micro, textMuted)

**ExpeditionChest.tsx** — Coffre anime overlay :

1. Props :
   ```typescript
   interface Props {
     visible: boolean;
     outcome: ExpeditionOutcome;
     loot?: ExpeditionLoot;
     missionName: string;
     onClose: () => void;
   }
   ```
2. Modal transparent fullscreen avec fond opacity 0.6 (colors.bg)
3. Etat interne : `opened` (boolean)
4. Avant tap :
   - Coffre 96x96 centre — utiliser MaterialCommunityIcons 'treasure-chest' (size 96, Farm.wood color)
   - Text "Appuie pour ouvrir !" FontSize.label textMuted
5. Tap coffre :
   - withSpring scale 1.0 → 1.3 → 1.0 (SPRING_CHEST = { damping: 8, stiffness: 160 })
   - Haptic selon outcome : Success → notificationAsync(Success), Failure → notificationAsync(Error), Rare → notificationAsync(Success) + 200ms delay + impactAsync(Heavy)
   - Set opened = true
6. Apres ouverture :
   - withTiming opacity 0 → 1 (300ms) sur le contenu revele
   - Outcome badge : texte colore per UI-SPEC (success=vert "Expedition reussie !", partial=amber "Retour partiel", failure=rouge "Expedition echouee — mise perdue.", rare=Farm.gold "Decouverte rare !")
   - Si loot : emoji + label + type badge
   - Si rare : 4-6 star Views avec withTiming opacity 1→0 + translateY 0→-40 (staggered 0/80/160/240ms) autour du coffre
7. Bouton "Fermer le coffre" en bas (secondary style, 44px)

Toutes les couleurs via useThemeColors(). Spacing tokens partout. Spring configs comme constantes module.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - components/mascot/ExpeditionsSheet.tsx existe et contient `presentationStyle="pageSheet"`
    - ExpeditionsSheet.tsx contient 3 onglets : grep 'Catalogue' + 'En cours' + 'Resultats'
    - ExpeditionsSheet.tsx contient `getExpeditionRemainingMinutes`
    - ExpeditionsSheet.tsx contient `useThemeColors`
    - components/mascot/CampExplorationCell.tsx existe et contient `CAMP_EXPLORATION_CELL` ou `camp_exp`
    - CampExplorationCell.tsx contient badge `Retour !`
    - CampExplorationCell.tsx contient `withRepeat` (pulse animation)
    - components/mascot/ExpeditionChest.tsx existe et contient `SPRING_CHEST`
    - ExpeditionChest.tsx contient `Haptics.notificationAsync`
    - ExpeditionChest.tsx contient `Appuie pour ouvrir`
    - ExpeditionChest.tsx contient `Fermer le coffre`
    - npx tsc --noEmit ne produit pas de nouvelles erreurs
  </acceptance_criteria>
  <done>3 composants UI complets : modal 3 onglets, cellule ferme avec badges, coffre anime avec reveal et haptics.</done>
</task>

<task type="auto">
  <name>Task 2: Integration tree.tsx — Camp + ExpeditionsSheet + ExpeditionChest</name>
  <files>app/(tabs)/tree.tsx</files>
  <read_first>app/(tabs)/tree.tsx, hooks/useExpeditions.ts, components/mascot/ExpeditionsSheet.tsx, components/mascot/CampExplorationCell.tsx, components/mascot/ExpeditionChest.tsx, lib/mascot/world-grid.ts</read_first>
  <action>
Dans app/(tabs)/tree.tsx, ajouter le cablage minimal pour le systeme d'expeditions :

1. **Imports** (en haut du fichier avec les autres imports) :
   ```typescript
   import { useExpeditions } from '../../hooks/useExpeditions';
   import { ExpeditionsSheet } from '../../components/mascot/ExpeditionsSheet';
   import { CampExplorationCell } from '../../components/mascot/CampExplorationCell';
   import { ExpeditionChest } from '../../components/mascot/ExpeditionChest';
   import { CAMP_EXPLORATION_CELL } from '../../lib/mascot/world-grid';
   import { isExpeditionComplete, getExpeditionRemainingMinutes } from '../../lib/mascot/expedition-engine';
   ```

2. **State** (dans le composant, avec les autres useState) :
   ```typescript
   const [showExpeditions, setShowExpeditions] = useState(false);
   const [chestData, setChestData] = useState<{
     visible: boolean;
     outcome: ExpeditionOutcome;
     loot?: ExpeditionLoot;
     missionName: string;
   }>({ visible: false, outcome: 'failure', missionName: '' });
   ```

3. **Hook** (avec les autres hooks) :
   ```typescript
   const {
     dailyPool, activeExpeditions, completedExpeditions, pendingResults,
     activeCount, canLaunch, pityCount,
     launchExpedition, collectExpedition, dismissExpedition,
   } = useExpeditions();
   ```

4. **Handlers** (useCallback) :
   ```typescript
   const handleCollectExpedition = useCallback(async (missionId: string) => {
     const { outcome, loot } = await collectExpedition(missionId);
     const mission = dailyPool.find(m => m.id === missionId) ?? { name: 'Expedition' };
     setShowExpeditions(false);
     // Petit delai pour fermer le modal avant d'ouvrir le coffre
     setTimeout(() => {
       setChestData({ visible: true, outcome, loot, missionName: mission.name });
     }, 300);
   }, [collectExpedition, dailyPool]);
   
   const handleCloseChest = useCallback(() => {
     setChestData(prev => ({ ...prev, visible: false }));
   }, []);
   ```

5. **CampExplorationCell dans le JSX** — rendre comme sibling du WorldGridView (pattern identique aux avatars Phase 29, pointerEvents none sur le container parent). Position absolue basee sur CAMP_EXPLORATION_CELL.x / .y :
   ```tsx
   <CampExplorationCell
     activeCount={activeCount}
     hasResult={pendingResults.length > 0}
     shortestRemaining={
       activeExpeditions
         .filter(e => !e.result && !isExpeditionComplete(e))
         .reduce((min, e) => Math.min(min, getExpeditionRemainingMinutes(e)), Infinity)
     }
     onPress={() => setShowExpeditions(true)}
   />
   ```
   Wrapper View absolute avec left: `CAMP_EXPLORATION_CELL.x * containerWidth` et top: `CAMP_EXPLORATION_CELL.y * containerHeight` (meme pattern que les buildings dans WorldGridView).

6. **ExpeditionsSheet modal** (en fin de JSX, avec les autres modaux) :
   ```tsx
   <ExpeditionsSheet
     visible={showExpeditions}
     onClose={() => setShowExpeditions(false)}
     dailyPool={dailyPool}
     activeExpeditions={activeExpeditions}
     completedExpeditions={completedExpeditions}
     pendingResults={pendingResults}
     canLaunch={canLaunch}
     pityCount={pityCount}
     onLaunch={launchExpedition}
     onCollect={handleCollectExpedition}
     onDismiss={dismissExpedition}
   />
   ```

7. **ExpeditionChest overlay** (tout en fin de JSX, apres tous les modaux) :
   ```tsx
   <ExpeditionChest
     visible={chestData.visible}
     outcome={chestData.outcome}
     loot={chestData.loot}
     missionName={chestData.missionName}
     onClose={handleCloseChest}
   />
   ```

NE PAS modifier la logique existante de tree.tsx — ajout pur (nouvelles lignes uniquement). Quoter le path dans les commandes git : `"app/(tabs)/tree.tsx"`.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - app/(tabs)/tree.tsx contient `import.*useExpeditions`
    - app/(tabs)/tree.tsx contient `import.*ExpeditionsSheet`
    - app/(tabs)/tree.tsx contient `import.*CampExplorationCell`
    - app/(tabs)/tree.tsx contient `import.*ExpeditionChest`
    - app/(tabs)/tree.tsx contient `showExpeditions`
    - app/(tabs)/tree.tsx contient `setShowExpeditions`
    - app/(tabs)/tree.tsx contient `handleCollectExpedition`
    - app/(tabs)/tree.tsx contient `<ExpeditionsSheet` avec props visible/onClose/dailyPool
    - app/(tabs)/tree.tsx contient `<CampExplorationCell` avec props activeCount/onPress
    - app/(tabs)/tree.tsx contient `<ExpeditionChest` avec props visible/outcome/onClose
    - npx tsc --noEmit ne produit pas de nouvelles erreurs
  </acceptance_criteria>
  <done>tree.tsx cable avec Camp d'exploration visible sur la ferme, modal expeditions ouvrable, coffre anime pour les resultats.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verification visuelle et fonctionnelle du systeme d'expeditions</name>
  <files>app/(tabs)/tree.tsx</files>
  <action>Verification humaine du systeme d'expeditions complet sur device iOS. Aucune modification de code — uniquement validation visuelle et fonctionnelle.</action>
  <what-built>Systeme complet d'expeditions : Camp d'exploration sur la ferme, modal catalogue 3 onglets, lancement avec confirmation risque, timer, coffre anime avec resultat aleatoire, persistance.</what-built>
  <how-to-verify>
    1. Ouvrir l'app sur device iOS (npx expo run:ios --device)
    2. Aller sur l'ecran ferme (arbre)
    3. Verifier que le Camp d'exploration est visible en bas-gauche
    4. Tapper le Camp → le modal Expeditions s'ouvre (pageSheet, drag-to-dismiss)
    5. Onglet Catalogue : 3 missions affichees (Facile/Moyen/Dur) avec cout et duree
    6. Tapper "Lancer" sur une mission Facile → confirmation Alert avec texte risque
    7. Confirmer → vibration + ressources deduites + badge "1/2" sur le Camp
    8. Onglet En cours : expedition visible avec countdown
    9. Attendre que le timer expire (ou modifier la date systeme pour tester) → "Collecter" visible
    10. Tapper Collecter → coffre anime apparait → tapper pour ouvrir → resultat revele avec haptic
    11. Tapper "Fermer le coffre" → retour a l'ecran ferme
    12. Forcer quit + restart → verifier que les expeditions actives/resultats sont preserves
  </how-to-verify>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -5</automated>
  </verify>
  <done>User approuve le rendu visuel et le flow fonctionnel des expeditions sur device.</done>
  <resume-signal>Type "approved" ou decris les problemes</resume-signal>
</task>

</tasks>

<verification>
- npx tsc --noEmit passe
- Modal expeditions s'ouvre et se ferme correctement
- Lancement avec confirmation risque fonctionne
- Coffre anime revele le resultat
- Persistance apres restart
</verification>

<success_criteria>
- Camp d'exploration visible sur la ferme
- Modal catalogue montre 3 missions du jour
- Lancement deduit ressources et demarre timer
- Coffre anime avec haptic revele le resultat
- Expeditions persistent apres restart
</success_criteria>

<output>
After completion, create `.planning/phases/33-exp-ditions/33-03-SUMMARY.md`
</output>
