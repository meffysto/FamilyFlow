---
phase: 45-auberge-ui-modal-dashboard-dev-spawn
plan: 03
type: execute
wave: 2
depends_on:
  - "45-01"
files_modified:
  - components/dashboard/DashboardAuberge.tsx
  - components/dashboard/index.ts
  - app/(tabs)/index.tsx
autonomous: true
requirements:
  - AUB45-03-DASHBOARD-AUBERGE
must_haves:
  truths:
    - "DashboardAuberge ne rend RIEN (return null) si aucune auberge construite (lecture activeProfile.farmBuildings.find(b => b.buildingId === 'auberge'))"
    - "Si auberge construite : affiche header '🛖 Auberge' + compteur '{N} visiteurs', 1-2 cartes compactes (portrait + nom + timer + statut delivrable), CTA 'Voir l'auberge' qui ouvre AubergeSheet"
    - "Pulse Reanimated léger (withRepeat scale 1.0 ↔ 1.04) sur la carte si au moins un timer < 2h"
    - "Une nouvelle entrée 'auberge' (label '🛖 Auberge', emoji '🛖', visible: true, priority: 'high', size: 'full', zone: 'farm') est ajoutée dans getAllSections() de app/(tabs)/index.tsx"
    - "L'entrée 'auberge' apparaît dans les Préférences dashboard (modal pilotée par le tableau sections lui-même — toggle visibilité automatique sans refonte)"
    - "Le switch case dans renderSection rend <DashboardAuberge>"
    - "Wrap SectionErrorBoundary, useThemeColors, zéro hardcoded color, tokens design"
  artifacts:
    - path: "components/dashboard/DashboardAuberge.tsx"
      provides: "Section dashboard conditionnelle Auberge, ouvre AubergeSheet via state local"
      exports: ["DashboardAuberge"]
      min_lines: 150
    - path: "components/dashboard/index.ts"
      provides: "Barrel export augmenté avec DashboardAuberge"
      contains: "DashboardAuberge"
    - path: "app/(tabs)/index.tsx"
      provides: "Section 'auberge' enregistrée dans getAllSections + SECTION_ZONE + renderSection switch + import"
      contains: "DashboardAuberge"
  key_links:
    - from: "components/dashboard/DashboardAuberge.tsx"
      to: "components/mascot/AubergeSheet.tsx"
      via: "import + state local sheetOpen ; <AubergeSheet visible={sheetOpen} onClose={() => setSheetOpen(false)} />"
      pattern: "AubergeSheet"
    - from: "components/dashboard/DashboardAuberge.tsx"
      to: "hooks/useAuberge.ts"
      via: "lecture activeVisitors pour cartes compactes"
      pattern: "useAuberge\\("
    - from: "app/(tabs)/index.tsx"
      to: "components/dashboard/DashboardAuberge.tsx"
      via: "import + entry 'auberge' dans getAllSections + case 'auberge' dans renderSection"
      pattern: "case 'auberge'"
---

<objective>
Créer `DashboardAuberge.tsx` (carte dashboard conditionnelle) et l'enregistrer dans `app/(tabs)/index.tsx` (liste des sections + zone 'farm' + switch render). La carte n'apparaît que si l'auberge est construite ; elle affiche un aperçu (1-2 visiteurs) avec un pulse léger si urgence, et un CTA qui ouvre `AubergeSheet` via état local.

Purpose : surface l'Auberge dans le dashboard pour que l'utilisateur ne soit pas obligé d'ouvrir BuildingDetailSheet pour voir ses visiteurs. Visibilité naturelle dans le flux principal.

Output : 1 nouveau composant + 1 ligne dans le barrel + ~3 modifications ciblées dans `app/(tabs)/index.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/45-auberge-ui-modal-dashboard-dev-spawn/45-CONTEXT.md
@.planning/phases/45-auberge-ui-modal-dashboard-dev-spawn/45-01-PLAN.md
@.planning/phases/45-auberge-ui-modal-dashboard-dev-spawn/45-02-PLAN.md
@components/dashboard/DashboardGarden.tsx
@components/dashboard/index.ts
@hooks/useAuberge.ts
@CLAUDE.md

<interfaces>
<!-- Types DashboardSectionProps (components/dashboard/types.ts) -->
```typescript
interface DashboardSectionProps {
  isChildMode: boolean;
  // ...autres props éventuelles, voir types.ts
}
```

<!-- Pattern useAuberge -->
```typescript
const { activeVisitors, totalReputation } = useAuberge();
```

<!-- Pattern card dashboard (DashboardCard) -->
```typescript
import { DashboardCard } from '../DashboardCard';
<DashboardCard title="🛖 Auberge" color={colors.catSocial /* ou autre */} tinted collapsible cardId="auberge">
  ...
</DashboardCard>
```

<!-- Détection auberge construite -->
```typescript
import { useVault } from '../../contexts/VaultContext';
const { activeProfile } = useVault();
const buildings = Array.isArray(activeProfile?.farmBuildings) ? activeProfile.farmBuildings : [];
const hasAuberge = buildings.some(b => b.buildingId === 'auberge');
if (!hasAuberge) return null;
```

<!-- Sections registry pattern (app/(tabs)/index.tsx ligne 123-155) -->
```typescript
{ id: 'auberge', label: '🛖 Auberge', emoji: '🛖', visible: true, priority: 'high', size: 'full' },
// + SECTION_ZONE['auberge'] = 'farm';
// + case 'auberge': return <DashboardAuberge key={id} {...sectionProps} />;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1 : Créer DashboardAuberge.tsx (carte dashboard conditionnelle)</name>
  <files>components/dashboard/DashboardAuberge.tsx</files>
  <behavior>
    - Return null si pas d'auberge construite
    - Sinon : DashboardCard "🛖 Auberge" + compteur + 1-2 cartes compactes visiteurs (portrait + nom + timer + statut deliverable) + CTA "Voir l'auberge"
    - Pulse Reanimated discret si au moins un timer < 120 minutes (withRepeat scale [1.0, 1.04, 1.0], duration 1500, useReducedMotion respect)
    - State local sheetOpen pour piloter <AubergeSheet visible={sheetOpen} onClose={...} />
    - useThemeColors() pour TOUTES les couleurs (zéro hardcoded sauf éventuelles couleurs sémantiques timer comme dans Plan 45-02)
    - SectionErrorBoundary wrap (cohérence avec autres dashboards)
  </behavior>
  <action>
    Créer `components/dashboard/DashboardAuberge.tsx`. S'inspirer du squelette de `DashboardGarden.tsx` mais en BEAUCOUP plus léger (pas de sagas, pas de family toggle, pas d'aventures).

    ```typescript
    /**
     * DashboardAuberge.tsx — Carte dashboard Auberge (Phase 45)
     *
     * Visible uniquement si l'auberge est construite. Affiche un aperçu compact
     * des visiteurs actifs et ouvre AubergeSheet sur tap CTA.
     */

    import React, { useCallback, useEffect, useMemo, useState } from 'react';
    import { View, Text, TouchableOpacity, StyleSheet } from 'react-native';
    import Animated, { useSharedValue, useAnimatedStyle, withRepeat, withTiming, useReducedMotion, cancelAnimation, withSequence } from 'react-native-reanimated';
    import { useVault } from '../../contexts/VaultContext';
    import { useThemeColors } from '../../contexts/ThemeContext';
    import { useAuberge } from '../../hooks/useAuberge';
    import { getRemainingMinutes } from '../../lib/mascot/auberge-engine';
    import { VISITOR_CATALOG } from '../../lib/mascot/types';
    import { DashboardCard } from '../DashboardCard';
    import { AubergeSheet } from '../mascot/AubergeSheet';
    import { Spacing, Radius } from '../../constants/spacing';
    import { FontSize, FontWeight } from '../../constants/typography';
    import type { DashboardSectionProps } from './types';
    import type { ActiveVisitor, PlacedBuilding } from '../../lib/mascot/types';

    const URGENT_THRESHOLD_MIN = 120;

    function DashboardAubergeInner(_props: DashboardSectionProps) {
      const { activeProfile } = useVault();
      const { colors, primary, tint } = useThemeColors();
      const { activeVisitors, totalReputation } = useAuberge();
      const [sheetOpen, setSheetOpen] = useState(false);
      const reducedMotion = useReducedMotion();
      const pulse = useSharedValue(1);

      // Détection auberge construite (lecture farmBuildings)
      const buildings: PlacedBuilding[] = Array.isArray(activeProfile?.farmBuildings)
        ? activeProfile!.farmBuildings
        : [];
      const hasAuberge = buildings.some(b => b.buildingId === 'auberge');

      const now = useMemo(() => new Date(), [activeVisitors.length]);
      const hasUrgent = useMemo(() =>
        activeVisitors.some(v => getRemainingMinutes(v, now) < URGENT_THRESHOLD_MIN),
      [activeVisitors, now]);

      useEffect(() => {
        if (!hasAuberge || !hasUrgent || reducedMotion) {
          cancelAnimation(pulse);
          pulse.value = withTiming(1, { duration: 200 });
          return;
        }
        pulse.value = withRepeat(
          withSequence(
            withTiming(1.04, { duration: 750 }),
            withTiming(1, { duration: 750 }),
          ),
          -1,
          false,
        );
        return () => cancelAnimation(pulse);
      }, [hasAuberge, hasUrgent, reducedMotion]);

      const animStyle = useAnimatedStyle(() => ({
        transform: [{ scale: pulse.value }],
      }));

      const handleOpen = useCallback(() => setSheetOpen(true), []);
      const handleClose = useCallback(() => setSheetOpen(false), []);

      if (!hasAuberge) return null;

      // Top 2 visiteurs (les plus urgents en premier)
      const previewVisitors = useMemo(() =>
        [...activeVisitors]
          .sort((a, b) => getRemainingMinutes(a, now) - getRemainingMinutes(b, now))
          .slice(0, 2),
      [activeVisitors, now]);

      return (
        <>
          <DashboardCard
            title={`🛖 Auberge — ${activeVisitors.length} visiteur${activeVisitors.length > 1 ? 's' : ''}`}
            color={colors.catSocial ?? primary}
            tinted
            collapsible
            cardId="auberge"
          >
            <Animated.View style={animStyle}>
              {previewVisitors.length === 0 ? (
                <View style={styles.empty}>
                  <Text style={[styles.emptyText, { color: colors.textMuted }]}>
                    L'auberge est calme...
                  </Text>
                </View>
              ) : (
                <View style={styles.list}>
                  {previewVisitors.map(v => {
                    const def = VISITOR_CATALOG.find(d => d.id === v.visitorId);
                    const remaining = getRemainingMinutes(v, now);
                    const h = Math.floor(remaining / 60);
                    const m = remaining % 60;
                    const timerLabel = remaining <= 0 ? 'Expiré' : (h > 0 ? `${h}h${String(m).padStart(2, '0')}` : `${m}min`);
                    return (
                      <View key={v.instanceId} style={[styles.row, { borderColor: colors.borderLight }]}>
                        <Text style={styles.rowEmoji}>{def?.emoji ?? '🧑'}</Text>
                        <Text style={[styles.rowName, { color: colors.text }]} numberOfLines={1}>
                          {def?.id ?? v.visitorId}
                        </Text>
                        <Text style={[styles.rowTimer, { color: remaining < URGENT_THRESHOLD_MIN ? '#EF4444' : colors.textMuted }]}>
                          ⏱ {timerLabel}
                        </Text>
                      </View>
                    );
                  })}
                </View>
              )}

              <TouchableOpacity
                style={[styles.cta, { backgroundColor: tint }]}
                onPress={handleOpen}
                activeOpacity={0.7}
              >
                <Text style={[styles.ctaText, { color: primary }]}>Voir l'auberge ({totalReputation} ❤)</Text>
              </TouchableOpacity>
            </Animated.View>
          </DashboardCard>

          <AubergeSheet visible={sheetOpen} onClose={handleClose} />
        </>
      );
    }

    const styles = StyleSheet.create({
      empty: { alignItems: 'center', paddingVertical: Spacing.lg },
      emptyText: { fontSize: FontSize.caption, fontStyle: 'italic' },
      list: { gap: Spacing.xs },
      row: {
        flexDirection: 'row',
        alignItems: 'center',
        gap: Spacing.sm,
        paddingHorizontal: Spacing.md,
        paddingVertical: Spacing.sm,
        borderWidth: StyleSheet.hairlineWidth,
        borderRadius: Radius.md,
      },
      rowEmoji: { fontSize: 24 },
      rowName: { flex: 1, fontSize: FontSize.caption, fontWeight: FontWeight.semibold },
      rowTimer: { fontSize: FontSize.micro, fontWeight: FontWeight.bold },
      cta: {
        marginTop: Spacing['2xl'],
        paddingVertical: Spacing.md,
        borderRadius: Radius.md,
        alignItems: 'center',
      },
      ctaText: { fontSize: FontSize.sm, fontWeight: FontWeight.semibold },
    });

    export const DashboardAuberge = React.memo(DashboardAubergeInner);
    ```

    Note : si `colors.catSocial` n'existe pas dans le thème, fallback sur `primary`. Vérifier `constants/themes.ts` pour confirmer (grep `catSocial` ; si absent, utiliser `colors.catCare ?? primary` ou le token le plus proche). Choix discrétionnaire.

    Convention CLAUDE.md : si SectionErrorBoundary est appliqué globalement par le parent (pattern courant — voir app/(tabs)/index.tsx renderSection), pas besoin de rewrap ici. Vérifier comment DashboardGarden est consommé dans renderSection (la `SectionErrorBoundary` est-elle appliquée par le parent ?). Si oui, ne pas rewrap.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "DashboardAuberge\.tsx" ; test $? -eq 1</automated>
  </verify>
  <done>
    - Fichier créé, ≥120 lignes
    - `grep -n "if (!hasAuberge) return null" components/dashboard/DashboardAuberge.tsx` → 1 match
    - `grep -n "AubergeSheet" components/dashboard/DashboardAuberge.tsx` → ≥2 matches (import + usage JSX)
    - Aucune nouvelle erreur tsc
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2 : Enregistrer DashboardAuberge dans le barrel + dans app/(tabs)/index.tsx</name>
  <files>components/dashboard/index.ts, app/(tabs)/index.tsx</files>
  <behavior>
    - Ajouter `export { DashboardAuberge } from './DashboardAuberge';` au barrel
    - Dans app/(tabs)/index.tsx :
      1. Importer DashboardAuberge
      2. Ajouter une entrée 'auberge' dans getAllSections() (juste après 'companionDay' ligne 149) avec : `{ id: 'auberge', label: '🛖 Auberge', emoji: '🛖', visible: true, priority: 'high', size: 'full' }`
      3. Ajouter `auberge: 'farm',` dans la map SECTION_ZONE (proche de companionDay/garden lignes 191-192)
      4. Ajouter `case 'auberge': return <DashboardAuberge key={id} {...sectionProps} />;` dans le switch renderSection (proche du case 'companionDay' ligne 908)
  </behavior>
  <action>
    1. Éditer `components/dashboard/index.ts` : ajouter une ligne après l'export de `DashboardCompanionDay` :
       ```typescript
       export { DashboardAuberge } from './DashboardAuberge';
       ```

    2. Éditer `app/(tabs)/index.tsx` :
       - **Import** : ajouter `DashboardAuberge,` dans le bloc import group existant (proche de `DashboardCompanionDay,` ligne 104).
       - **getAllSections()** : insérer juste après la ligne `{ id: 'companionDay', label: 'Journée de la mascotte', ... }` (ligne 149) :
         ```typescript
         { id: 'auberge',      label: '🛖 Auberge',                                 emoji: '🛖', visible: true,  priority: 'high', size: 'full' },
         ```
       - **SECTION_ZONE map** : ajouter dans le bloc proche de companionDay/garden (lignes 191-192) :
         ```typescript
         auberge: 'farm',
         ```
       - **renderSection switch** : ajouter avant `default:` (ligne 909) :
         ```typescript
         case 'auberge':        return <DashboardAuberge key={id} {...sectionProps} />;
         ```
       - **CHILD_PROMOTED** (ligne 204+) : ajouter `auberge: { visible: true, priority: 'high' },` (cohérent avec garden ligne 209) — l'auberge est gameplay enfant-friendly.

    Aucune autre modification — pas de toggle modal séparé à câbler, le système Préférences existant (basé sur le tableau `sections` de getAllSections) gère déjà la visibilité automatiquement.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "(tabs)/index\.tsx|dashboard/index\.ts" ; test $? -eq 1</automated>
  </verify>
  <done>
    - `grep -n "DashboardAuberge" components/dashboard/index.ts` → 1 match
    - `grep -n "DashboardAuberge\|'auberge'" "app/(tabs)/index.tsx"` → ≥4 matches (import + entry getAllSections + SECTION_ZONE + case + CHILD_PROMOTED)
    - `npx tsc --noEmit` : aucune nouvelle erreur
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` clean (modulo erreurs pré-existantes)
- `git diff --name-only` ne liste QUE les 3 fichiers de `files_modified`
- L'utilisateur en mode dev (auberge non construite) ne voit PAS la carte
- Une fois l'auberge construite (Phase 44 shop) → la carte apparaît dans la zone "farm" du dashboard
- Tap CTA "Voir l'auberge" ouvre AubergeSheet
</verification>

<success_criteria>
- DashboardAuberge est rendu conditionnellement, ouvre AubergeSheet via state local
- L'entrée 'auberge' est togglable dans les Préférences dashboard (héritée du système existant)
- Pulse léger seulement si urgence (< 2h), respecte useReducedMotion
</success_criteria>

<output>
Après complétion, créer `.planning/phases/45-auberge-ui-modal-dashboard-dev-spawn/45-03-SUMMARY.md` selon le template summary.
</output>
