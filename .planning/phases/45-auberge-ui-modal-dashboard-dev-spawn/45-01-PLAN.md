---
phase: 45-auberge-ui-modal-dashboard-dev-spawn
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - hooks/useAuberge.ts
autonomous: true
requirements:
  - AUB45-01-FORCE-SPAWN
must_haves:
  truths:
    - "Le hook useAuberge expose forceSpawn(profileId) → Promise<ActiveVisitor | null>"
    - "Un appel forceSpawn persiste l'AubergeState (writeFile farm atomique unique) et invalide refreshFarm"
    - "forceSpawn retourne null si aucun candidat éligible (treeStage trop bas, cap atteint), pas d'exception"
    - "forceSpawn ne dépend pas de shouldSpawnVisitor (bypass cooldown / shouldSpawn) — c'est l'intérêt debug"
    - "Aucune régression sur deliverVisitor / dismissVisitor / tickAuberge (mêmes signatures, mêmes retours)"
  artifacts:
    - path: "hooks/useAuberge.ts"
      provides: "forceSpawn(profileId) en plus des 3 setters existants"
      contains: "forceSpawn"
  key_links:
    - from: "hooks/useAuberge.ts (forceSpawn)"
      to: "lib/mascot/auberge-engine.ts (engineSpawn)"
      via: "import + appel direct sans gating"
      pattern: "engineSpawn\\("
---

<objective>
Étendre `hooks/useAuberge.ts` (livré Phase 43-04) avec un setter `forceSpawn(profileId)` qui contourne `shouldSpawnVisitor` et appelle directement `engineSpawn` (catalogue auberge-engine), persiste l'état farm de façon atomique et rafraîchit le snapshot.

Purpose : prérequis pour le bouton dev `__DEV__` "🪄 Forcer un visiteur" de l'AubergeSheet (Plan 45-02). Permet à l'utilisateur de tester end-to-end sans attendre les conditions de spawn naturelles.

Output : 1 méthode publique de plus sur le hook, retournée dans l'objet exposé.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/45-auberge-ui-modal-dashboard-dev-spawn/45-CONTEXT.md
@.planning/phases/43-auberge-mod-le-moteur-visiteurs/43-04-SUMMARY.md
@hooks/useAuberge.ts

<interfaces>
<!-- Signatures déjà disponibles dans hooks/useAuberge.ts (Phase 43-04) -->

```typescript
// Méthodes exposées (à augmenter avec forceSpawn)
deliverVisitor: (profileId: string, instanceId: string) => Promise<{ ok: boolean; reward?: { coins: number; loot?: string }; missing?: VisitorRequestItem[] }>;
dismissVisitor: (profileId: string, instanceId: string) => Promise<void>;
tickAuberge: (profileId: string) => Promise<{ spawned?: ActiveVisitor; expired: ActiveVisitor[] }>;
```

```typescript
// Signature engine (lib/mascot/auberge-engine.ts) — déjà importée comme engineSpawn
spawnVisitor(state: AubergeState, treeStage: TreeStage, now: Date, totalReputation: number):
  | { state: AubergeState; visitor: ActiveVisitor }
  | null;
```

```typescript
// Pattern persistance déjà utilisé par tickAuberge (référence ligne 267-311)
// 1. readFile(farm-{id}.md) → parseFarmProfile
// 2. parseAuberge → engineSpawn (sans gating)
// 3. applyAubergeToFarmData
// 4. writeFile(farm-{id}.md, serializeFarmProfile(...))  ← unique, atomique
// 5. refreshFarm(profileId)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1 : Ajouter forceSpawn(profileId) au hook useAuberge</name>
  <files>hooks/useAuberge.ts</files>
  <behavior>
    - forceSpawn relit toujours depuis vault.readFile (source de vérité, pattern uniforme avec deliverVisitor/tickAuberge/dismissVisitor)
    - Calcule treeStage depuis profile.level via getTreeStageInfo (pattern existant ligne 293)
    - Appelle engineSpawn(currentState, treeStage, now, totalRep) directement (PAS shouldSpawnVisitor — c'est tout l'intérêt)
    - Si engineSpawn retourne null → return null sans persister (rien à écrire)
    - Si engineSpawn retourne { state, visitor } → applyAubergeToFarmData + 1 seul writeFile farm + refreshFarm
    - Pas de gamification touchée (aucun coin crédité)
    - Cas vault absent : guard `if (!vault) return null;`
  </behavior>
  <action>
    Ouvrir `hooks/useAuberge.ts`. Avant le `return` final (ligne 313), ajouter une nouvelle méthode `forceSpawn` mémoïsée via `useCallback` :

    ```typescript
    // ─── forceSpawn — debug helper (bypass shouldSpawn / cooldown) ──────────
    // Utilisé par le bouton __DEV__ "Forcer un visiteur" de l'AubergeSheet.
    // Ne consulte PAS shouldSpawnVisitor : appelle engineSpawn directement
    // pour permettre le test E2E sans attendre les conditions naturelles.
    const forceSpawn = useCallback(async (profileId: string): Promise<ActiveVisitor | null> => {
      if (!vault) return null;

      const file = farmFile(profileId);
      const content = await vault.readFile(file).catch(() => '');
      const farmData = parseFarmProfile(content);

      const currentState = parseAuberge({
        visitors: farmData.auberge_visitors,
        reputations: farmData.auberge_reputations,
        lastSpawn: farmData.auberge_last_spawn,
        totalDeliveries: farmData.auberge_total_deliveries,
      });

      const profile = profiles.find(p => p.id === profileId);
      const level = profile?.level ?? 1;
      const treeStage = getTreeStageInfo(level).stage;

      const now = new Date();
      const totalRep = engineGetTotalReputation(currentState);
      const result = engineSpawn(currentState, treeStage, now, totalRep);
      if (!result) return null;

      applyAubergeToFarmData(farmData, result.state, now);
      const profileName = profiles.find(p => p.id === profileId)?.name ?? profileId;
      await vault.writeFile(file, serializeFarmProfile(profileName, farmData));
      await refreshFarm(profileId);

      return result.visitor;
    }, [vault, profiles, applyAubergeToFarmData, refreshFarm]);
    ```

    Puis ajouter `forceSpawn` au `return { ... }` final (ligne ~313). Conserver l'ordre alphabétique ou simplement l'ajouter en dernier.

    Ne PAS modifier deliverVisitor/dismissVisitor/tickAuberge. Ne PAS toucher VaultContext.tsx, lib/vault-cache.ts, ni les tests existants.

    Convention CLAUDE.md : commentaires/code FR pour les nouveaux ajouts.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "useAuberge\.ts" ; test $? -eq 1</automated>
  </verify>
  <done>
    - `grep -n "forceSpawn" hooks/useAuberge.ts` → 3+ occurrences (déclaration + usage profiles.find + retour final)
    - `npx tsc --noEmit` → aucune nouvelle erreur introduite (les erreurs pré-existantes MemoryEditor.tsx / cooklang.ts / useVault.ts / TechTreeSheet.tsx sont OK et hors scope)
    - L'objet retourné par `useAuberge()` expose 9 entrées (5 lectures + 4 setters dont forceSpawn)
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` clean (modulo erreurs pré-existantes hors scope)
- Aucune modification de fichiers critiques (VaultContext.tsx, hooks/useVault.ts, lib/vault-cache.ts) — `git diff --name-only` ne doit lister que `hooks/useAuberge.ts`.
- Pattern atomic write respecté : un seul `vault.writeFile` dans la nouvelle méthode.
</verification>

<success_criteria>
- `forceSpawn` est exporté par `useAuberge()` avec la signature `(profileId: string) => Promise<ActiveVisitor | null>`.
- L'appel bypass `shouldSpawnVisitor` (validé par lecture du diff : aucun import de `shouldSpawnVisitor`).
- En cas de candidat éligible : visiteur visible immédiatement après refreshFarm via la lecture mémoïsée du hook.
- En cas d'inéligibilité (treeStage trop bas, cap atteint) : retourne null sans écriture vault.
</success_criteria>

<output>
Après complétion, créer `.planning/phases/45-auberge-ui-modal-dashboard-dev-spawn/45-01-SUMMARY.md` selon le template summary.
</output>
