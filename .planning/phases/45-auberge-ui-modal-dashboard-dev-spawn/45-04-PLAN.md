---
phase: 45-auberge-ui-modal-dashboard-dev-spawn
plan: 04
type: execute
wave: 3
depends_on:
  - "45-02"
files_modified:
  - components/mascot/BuildingDetailSheet.tsx
autonomous: true
requirements:
  - AUB45-04-WIRING-CTA
must_haves:
  truths:
    - "Le placeholder Phase 44 (carte 'Bâtiment social — Voir l'intérieur prochainement') est REMPLACÉ par un CTA primaire '🛖 Voir l'auberge' uniquement quand def.id === 'auberge'"
    - "Tap CTA → ouvre <AubergeSheet visible={true}> par-dessus BuildingDetailSheet (state local aubergeOpen)"
    - "Pour les autres bâtiments non-productifs (futurs) : conserver le placeholder 'Voir l'intérieur prochainement' (rétrocompat)"
    - "Pas de régression sur les 4 bâtiments productifs (poulailler, grange, moulin, ruche) : producesResource undefined ⇒ chemin productif inchangé"
    - "useThemeColors n'est PAS introduit (BuildingDetailSheet utilise une palette T.* locale, cohérence avec le pattern existant — voir Phase 44-04 SUMMARY note)"
  artifacts:
    - path: "components/mascot/BuildingDetailSheet.tsx"
      provides: "CTA secondaire '🛖 Voir l'auberge' qui ouvre AubergeSheet pour le bâtiment auberge"
      contains: "AubergeSheet"
  key_links:
    - from: "components/mascot/BuildingDetailSheet.tsx"
      to: "components/mascot/AubergeSheet.tsx"
      via: "import + state local aubergeOpen + <AubergeSheet visible={aubergeOpen} onClose={...} />"
      pattern: "AubergeSheet"
---

<objective>
Remplacer le placeholder Phase 44 ("Bâtiment social — Voir l'intérieur prochainement") par un vrai CTA "🛖 Voir l'auberge" dans `BuildingDetailSheet.tsx`, mais UNIQUEMENT pour le bâtiment auberge (`def.id === 'auberge'`). Les autres bâtiments non-productifs (futurs) conservent le placeholder.

Purpose : connecter physiquement la modale Auberge (Plan 45-02) au point d'entrée principal côté ferme. Sans ce wiring, l'utilisateur n'a aucun moyen d'ouvrir l'AubergeSheet en cliquant sur l'auberge dans la grille ferme.

Output : ~30 lignes de modification ciblée dans BuildingDetailSheet.tsx (import + state + CTA conditionnel + Modal AubergeSheet co-rendue).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/45-auberge-ui-modal-dashboard-dev-spawn/45-CONTEXT.md
@.planning/phases/45-auberge-ui-modal-dashboard-dev-spawn/45-02-PLAN.md
@.planning/phases/44-auberge-b-timent-branche-tech-social/44-04-SUMMARY.md
@components/mascot/BuildingDetailSheet.tsx

<interfaces>
<!-- État actuel BuildingDetailSheet (Phase 44-04) -->
<!-- Ligne 291 : const isProductive = def.producesResource !== false; -->
<!-- Lignes 495-506 : placeholder Animated.View nonProductiveCard avec emoji 🛖 + texte -->

<!-- AubergeSheet (Plan 45-02) -->
```typescript
import { AubergeSheet } from './AubergeSheet';
// Props : { visible: boolean; onClose: () => void }
```

<!-- Sous-composant CTA local existant (lignes 148-198) -->
```typescript
function CTA({ label, emoji, variant, onPress }: CTAProps) { ... }
// variants : 'primary' | 'secondary' | 'warning' | 'disabled'
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1 : Wirer CTA "Voir l'auberge" + AubergeSheet dans BuildingDetailSheet</name>
  <files>components/mascot/BuildingDetailSheet.tsx</files>
  <behavior>
    - Ajout import `AubergeSheet` depuis `./AubergeSheet`
    - Ajout state local `const [aubergeOpen, setAubergeOpen] = useState(false);`
    - Ajout flag dérivé `const isAuberge = def.id === 'auberge';`
    - Dans le bloc placeholder non-productif (lignes 495-506), si `isAuberge` : remplacer le contenu par un CTA primaire 'Voir l'auberge' qui set aubergeOpen=true (au lieu du body texte)
    - Pour les autres bâtiments non-productifs (futurs) : conserver le placeholder existant
    - Hors du Modal principal (avant le `);` final du return) : rendre `<AubergeSheet visible={aubergeOpen} onClose={() => setAubergeOpen(false)} />`
    - Pas de modification du chemin productif (lignes 434-494)
    - Pas de modification du panneau upgrade (gardé pour les futures upgrades sociales tech)
  </behavior>
  <action>
    1. **Imports** : ajouter en haut, dans le bloc imports existants :
       ```typescript
       import { AubergeSheet } from './AubergeSheet';
       ```
       Et `useState` à l'import de `react` (actuellement seul `useEffect` est importé ligne 15) :
       ```typescript
       import React, { useEffect, useState } from 'react';
       ```

    2. **State local** : à l'intérieur du composant `BuildingDetailSheet`, juste après la ligne 285 (`const { t } = useTranslation();`) :
       ```typescript
       const [aubergeOpen, setAubergeOpen] = useState(false);
       ```

    3. **Flag dérivé** : juste après ligne 291 (`const isProductive = def.producesResource !== false;`) :
       ```typescript
       const isAuberge = def.id === 'auberge';
       ```

    4. **Remplacer le placeholder** : lignes 495-506 actuelles :
       ```typescript
       <Animated.View
         entering={FadeIn.delay(100).springify().damping(12).stiffness(180)}
         style={styles.nonProductiveCard}
       >
         <Text style={styles.nonProductiveIcon}>🛖</Text>
         <Text style={styles.nonProductiveTitle}>Bâtiment social</Text>
         <Text style={styles.nonProductiveBody}>
           Ce bâtiment ne produit pas de ressources passives. Voir l'intérieur prochainement.
         </Text>
       </Animated.View>
       ```
       Remplacer par un branchement `isAuberge` :
       ```typescript
       <Animated.View
         entering={FadeIn.delay(100).springify().damping(12).stiffness(180)}
         style={styles.nonProductiveCard}
       >
         <Text style={styles.nonProductiveIcon}>🛖</Text>
         <Text style={styles.nonProductiveTitle}>Auberge</Text>
         {isAuberge ? (
           <>
             <Text style={styles.nonProductiveBody}>
               Accueille des visiteurs et livre leurs demandes pour gagner de la réputation et des récompenses.
             </Text>
             <View style={{ height: Spacing.md }} />
             <CTA
               label="Voir l'auberge"
               emoji="🛖"
               variant="primary"
               onPress={() => setAubergeOpen(true)}
             />
           </>
         ) : (
           <Text style={styles.nonProductiveBody}>
             Ce bâtiment ne produit pas de ressources passives. Voir l'intérieur prochainement.
           </Text>
         )}
       </Animated.View>
       ```

    5. **Co-rendre AubergeSheet** : juste avant le `</Modal>` final (autour de la ligne 571), insérer un fragment qui rend AubergeSheet HORS du Modal principal mais à l'intérieur du return (donc le placer SOIT à la racine du return en wrappant le tout dans un fragment, SOIT après la fermeture du Modal). La voie la plus simple :
       - Remplacer le `return ( <Modal ...>...</Modal> )` actuel par un fragment :
         ```tsx
         return (
           <>
             <Modal ...>
               ...contenu existant...
             </Modal>
             <AubergeSheet visible={aubergeOpen} onClose={() => setAubergeOpen(false)} />
           </>
         );
         ```
       Cela autorise l'AubergeSheet à s'afficher par-dessus BuildingDetailSheet (les 2 modaux RN coexistent — pageSheet sur pageSheet OK iOS).

    Convention CLAUDE.md : FR (déjà le cas dans tout le fichier).

    **Aucune modification** des styles existants — la `nonProductiveCard` accommode déjà le nouveau contenu (CTA enfant-compatible).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "BuildingDetailSheet\.tsx" ; test $? -eq 1</automated>
  </verify>
  <done>
    - `grep -n "isAuberge" components/mascot/BuildingDetailSheet.tsx` → ≥2 matches (déclaration + usage ternaire)
    - `grep -n "AubergeSheet" components/mascot/BuildingDetailSheet.tsx` → ≥2 matches (import + JSX)
    - `grep -n "aubergeOpen" components/mascot/BuildingDetailSheet.tsx` → ≥3 matches (state + setter dans CTA + prop visible)
    - `npx tsc --noEmit` : aucune nouvelle erreur introduite
    - Test mental : ouvrir BuildingDetailSheet sur un poulailler → exactement comme avant Phase 45 (rétrocompat). Sur l'auberge → placeholder remplacé par CTA "Voir l'auberge" qui ouvre la modale Auberge.
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` clean (modulo erreurs pré-existantes hors scope)
- `git diff --name-only` ne liste QUE `components/mascot/BuildingDetailSheet.tsx`
- Aucune régression visible : les 4 bâtiments productifs (poulailler, grange, moulin, ruche) restent identiques (chemin `isProductive === true`)
- Le bâtiment auberge bascule du placeholder Phase 44 au CTA Phase 45 fonctionnel
</verification>

<success_criteria>
- L'utilisateur tap l'auberge dans la grille ferme → BuildingDetailSheet s'ouvre → tap "Voir l'auberge" → AubergeSheet s'ouvre par-dessus
- Tap close de AubergeSheet → revient sur BuildingDetailSheet (qui reste ouvert)
- Tap close de BuildingDetailSheet → ferme tout
- Sur un poulailler : flux production identique à avant (zéro régression)
</success_criteria>

<output>
Après complétion, créer `.planning/phases/45-auberge-ui-modal-dashboard-dev-spawn/45-04-SUMMARY.md` selon le template summary.

**Critère d'acceptation final phase 45 (E2E manuel) :**
1. Construire l'auberge via le shop (déjà OK Phase 44)
2. Tap auberge dans grille ferme → BuildingDetailSheet
3. Tap "Voir l'auberge" → AubergeSheet (empty state au début)
4. Tap "🪄 Forcer un visiteur (dev)" → un visiteur apparaît
5. Si inventaire OK → tap "Livrer" → reward + carte disparaît + toast
6. Si inventaire KO → "Livrer" disabled, missing items visibles
7. Tap "Décliner" → confirm → visiteur disparaît
8. Carte DashboardAuberge apparaît dans dashboard (zone "farm")
</output>
