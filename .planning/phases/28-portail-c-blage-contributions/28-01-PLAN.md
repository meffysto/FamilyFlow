---
phase: 28-portail-c-blage-contributions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/(tabs)/tree.tsx
  - hooks/useFarm.ts
  - hooks/useGamification.ts
  - app/(tabs)/tasks.tsx
  - app/(tabs)/index.tsx
  - app/(tabs)/routines.tsx
autonomous: true
requirements: [MAP-03, COOP-01, COOP-02]

must_haves:
  truths:
    - "Un portail anime (glow loop) est visible sur l'ecran ferme en remplacement du FAB temporaire"
    - "Le tap sur le portail declenche un fade cross-dissolve 400ms puis navigue vers le village"
    - "Chaque recolte dans la ferme perso ajoute automatiquement une contribution 'harvest' au village"
    - "Chaque tache completee via useGamification ajoute automatiquement une contribution 'task' au village"
    - "Un toast discret '+1 Village' s'affiche apres chaque contribution automatique"
  artifacts:
    - path: "app/(tabs)/tree.tsx"
      provides: "PortalSprite inline + cablage addContribution dans useFarm"
      contains: "PortalSprite"
    - path: "hooks/useFarm.ts"
      provides: "Callback onContribution injecte apres harvestCrop"
      contains: "onContribution"
    - path: "hooks/useGamification.ts"
      provides: "Callback onContribution injecte dans completeTask apres blocs effets/musee"
      contains: "onContribution"
  key_links:
    - from: "hooks/useFarm.ts"
      to: "hooks/useGarden.ts"
      via: "callback onContribution passe a l'instanciation"
      pattern: "onContribution.*harvest"
    - from: "hooks/useGamification.ts"
      to: "hooks/useGarden.ts"
      via: "callback onContribution passe a l'instanciation"
      pattern: "onContribution.*task"
    - from: "app/(tabs)/tree.tsx"
      to: "app/(tabs)/village.tsx"
      via: "router.push apres fade withTiming 400ms"
      pattern: "router\\.push.*village"
---

<objective>
Remplacer le FAB temporaire de tree.tsx par un portail anime Reanimated (glow loop + tap spring + fade navigation) et cabler l'auto-contribution village dans useFarm.ts (recoltes) et useGamification.ts (taches) avec toast discret.

Purpose: Connecter la ferme perso au village cooperatif -- chaque action productive alimente automatiquement l'objectif collectif.
Output: Portail interactif dans tree.tsx, contributions automatiques dans 2 hooks, toast "+1 Village" dans tous les ecrans qui completent des taches.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-portail-c-blage-contributions/28-CONTEXT.md
@.planning/phases/28-portail-c-blage-contributions/28-RESEARCH.md
@.planning/phases/28-portail-c-blage-contributions/28-UI-SPEC.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From hooks/useGarden.ts:
```typescript
export interface UseGardenReturn {
  gardenData: VillageData;
  currentTarget: number;
  progress: number;
  isGoalReached: boolean;
  currentTemplate: ObjectiveTemplate;
  addContribution: (type: ContributionType, profileId: string) => Promise<void>;
  claimReward: (profileId: string) => Promise<boolean>;
  weekHistory: VillageWeekRecord[];
  isLoading: boolean;
}
export function useGarden(): UseGardenReturn;
```

From lib/village/types.ts:
```typescript
export type ContributionType = 'harvest' | 'task';
```

From hooks/useFarm.ts (current signature):
```typescript
export function useFarm(
  onQuestProgress?: (profileId: string, type: string, amount: number) => Promise<void>,
): { plant, harvest, buyBuilding, ... };
```

From hooks/useGamification.ts (current signature):
```typescript
interface UseGamificationArgs {
  vault: VaultManager | null;
  notifPrefs: NotificationPreferences;
  onDataChange?: (profiles: Profile[]) => void;
  onQuestProgress?: (profileId: string, type: string, amount: number) => Promise<void>;
}
export function useGamification(args: UseGamificationArgs): UseGamificationResult;
```

Current useFarm call in tree.tsx (ligne 339):
```typescript
const { plant, harvest, ... } = useFarm(contributeFamilyQuest);
```

Current useGamification calls:
- tree.tsx: NOT directly -- uses useFarm
- tasks.tsx:198: useGamification({ vault, notifPrefs, onQuestProgress: contributeFamilyQuest })
- index.tsx:220: useGamification({ vault, notifPrefs, onQuestProgress: contributeFamilyQuest })
- loot.tsx:57: useGamification({ vault, notifPrefs }) -- only openLootBox, no completeTask
- routines.tsx:346: useGamification({ vault, notifPrefs }) -- uses completeTask, no quest progress
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Portail anime + cablage useFarm contribution recolte</name>
  <files>app/(tabs)/tree.tsx, hooks/useFarm.ts</files>
  <read_first>
    - app/(tabs)/tree.tsx (FAB villageFAB ligne 2031-2051 + styles villageFAB/villageFABText lignes 2939-2957 + useFarm instanciation ligne 339)
    - hooks/useFarm.ts (signature ligne 138 + harvest function)
    - hooks/useGarden.ts (UseGardenReturn interface lignes 53-63)
    - lib/village/types.ts (ContributionType)
    - .planning/phases/28-portail-c-blage-contributions/28-UI-SPEC.md (animations, spacing, copy)
    - .planning/phases/28-portail-c-blage-contributions/28-RESEARCH.md (architecture portail, code examples)
  </read_first>
  <action>
**hooks/useFarm.ts -- Ajouter parametre onContribution (per D-05):**

1. Modifier la signature de `useFarm` pour accepter un second parametre optionnel :
```typescript
export function useFarm(
  onQuestProgress?: (profileId: string, type: string, amount: number) => Promise<void>,
  onContribution?: (type: ContributionType, profileId: string) => Promise<void>,
)
```
2. Ajouter `import type { ContributionType } from '../lib/village';` en haut du fichier.
3. Dans la fonction `harvest()`, APRES le bloc `onQuestProgress` existant (et apres `refreshFarm`), inserer :
```typescript
// Contribution village (COOP-01) -- fire-and-forget non-critical
if (onContribution) {
  try { await onContribution('harvest', profileId); } catch { /* Village -- non-critical */ }
}
```

**app/(tabs)/tree.tsx -- Remplacer FAB par PortalSprite (per D-01, D-02, D-08):**

1. Ajouter `import { useGarden } from '../../hooks/useGarden';` + `import type { ContributionType } from '../../lib/village';`
2. Dans le composant principal, appeler `const { addContribution } = useGarden();`
3. Modifier l'instanciation de `useFarm` (ligne 339) :
```typescript
const { plant, harvest, ... } = useFarm(contributeFamilyQuest, addContribution);
```
4. Ajouter un `useSharedValue(1)` pour l'opacity du fade de transition :
```typescript
const screenOpacity = useSharedValue(1);
const fadeStyle = useAnimatedStyle(() => ({ opacity: screenOpacity.value }));
```
5. Creer le handler de navigation portail :
```typescript
const handlePortalPress = useCallback(() => {
  screenOpacity.value = withTiming(
    0,
    { duration: 400, easing: Easing.out(Easing.ease) },
    (finished) => {
      if (finished) runOnJS(router.push)('/(tabs)/village' as any);
    },
  );
}, [screenOpacity, router]);
```
6. Ajouter un `useEffect` qui reset l'opacity quand l'ecran regagne le focus (via `useFocusEffect` ou `useEffect` sur navigation state) :
```typescript
useFocusEffect(useCallback(() => {
  screenOpacity.value = 1;
}, [screenOpacity]));
```
7. Creer le composant inline `PortalSprite` (< 80 lignes) avec :
   - `const SPRING_PORTAL = { damping: 12, stiffness: 200 } as const;`
   - Glow loop : `useSharedValue(0.4)` + `withRepeat(withTiming(0.8, { duration: 1200 }), -1, true)`
   - Scale spring tap : `withSpring(0.92, SPRING_PORTAL, () => { scaleAnim.value = withSpring(1, SPRING_PORTAL); })`
   - Haptic : `Haptics.selectionAsync()` au tap
   - AccessibilityLabel : `"Portail vers le village"`
   - Emoji sprite : `🏛️` (Text style fontSize 28)
   - Glow overlay : `Animated.View` avec `backgroundColor: colors.catJeux`, `borderRadius: Radius.xl`, opacity animee, `position: 'absolute'`
8. Supprimer le bloc FAB (lignes 2040-2051) et le remplacer par :
```tsx
{/* Couche 7 : Portail village (MAP-03 -- remplace FAB temporaire per D-08) */}
<PortalSprite onPress={handlePortalPress} />
```
Positionner avec le meme style absolu que `villageFAB` : `position: 'absolute', bottom: Spacing['4xl'], right: Spacing['2xl']`
9. Supprimer les styles `villageFAB` et `villageFABText` du `StyleSheet.create` et ajouter les styles portail : `portalContainer`, `portalEmoji`, `portalGlow`.
10. Wrapper le contenu principal de la ferme dans `<Animated.View style={fadeStyle}>` pour le fade de transition. NE PAS wrapper le toast ou les modals -- uniquement le contenu visuel de la diorama.
  </action>
  <verify>
    <automated>cd /Users/gabrielwaltio/Documents/family-vault && npx tsc --noEmit 2>&1 | head -40</automated>
  </verify>
  <acceptance_criteria>
    - hooks/useFarm.ts contient `onContribution?: (type: ContributionType, profileId: string) => Promise<void>` dans la signature
    - hooks/useFarm.ts contient `await onContribution('harvest', profileId)` dans un try/catch
    - app/(tabs)/tree.tsx contient `function PortalSprite`
    - app/(tabs)/tree.tsx contient `withRepeat(withTiming(0.8, { duration: 1200 }), -1, true)` (glow loop)
    - app/(tabs)/tree.tsx contient `SPRING_PORTAL = { damping: 12, stiffness: 200 }`
    - app/(tabs)/tree.tsx contient `duration: 400` dans le withTiming du fade
    - app/(tabs)/tree.tsx contient `accessibilityLabel="Portail vers le village"`
    - app/(tabs)/tree.tsx contient `addContribution` passe en second argument de `useFarm(`
    - app/(tabs)/tree.tsx ne contient PLUS `villageFAB` (ni le style ni le composant)
    - app/(tabs)/tree.tsx contient `useGarden` dans les imports
    - `npx tsc --noEmit` ne produit pas de nouvelles erreurs liees a tree.tsx ou useFarm.ts
  </acceptance_criteria>
  <done>Le portail anime remplace le FAB, le glow loop tourne en idle, le tap declenche un scale spring + fade 400ms + navigation vers village, et chaque recolte ferme appelle addContribution('harvest', profileId).</done>
</task>

<task type="auto">
  <name>Task 2: Cablage useGamification contribution taches + toast dans tous les ecrans</name>
  <files>hooks/useGamification.ts, app/(tabs)/tasks.tsx, app/(tabs)/index.tsx, app/(tabs)/routines.tsx</files>
  <read_first>
    - hooks/useGamification.ts (UseGamificationArgs interface lignes 58-63 + completeTask function + bloc applyTaskEffect ligne ~234 + bloc Museum ligne ~315)
    - app/(tabs)/tasks.tsx (useGamification instanciation ligne 198 + useVault destructuring ligne 197)
    - app/(tabs)/index.tsx (useGamification instanciation ligne 220 + useVault destructuring ligne ~197-215)
    - app/(tabs)/routines.tsx (useGamification instanciation ligne 346 + useVault destructuring ligne 345)
    - hooks/useGarden.ts (addContribution signature)
    - lib/village/types.ts (ContributionType)
    - contexts/ToastContext.tsx (showToast API)
    - .planning/phases/28-portail-c-blage-contributions/28-RESEARCH.md (Pitfall 1 toast doublon, Pitfall 2 instanciation multiple)
  </read_first>
  <action>
**hooks/useGamification.ts -- Ajouter onContribution dans les args (per D-05):**

**NOTE insertion point (D-05 clarification) :** D-05 mentionne `applyTaskEffect()` (ligne ~234) comme point d'insertion, mais `applyTaskEffect` est une fonction PURE importee de `lib/semantic/effects.ts` -- elle retourne un resultat sans side-effects et ne peut pas appeler un callback async. Le point d'insertion correct est dans `completeTask()` apres les blocs effets/musee (ligne ~316), ou l'on a acces au `profile.id` resolu et au contexte async. C'est le meme pattern que `onQuestProgress` qui est deja appele dans `completeTask()`, pas dans `applyTaskEffect()`.

1. Ajouter `import type { ContributionType } from '../lib/village';` en haut du fichier.
2. Etendre l'interface `UseGamificationArgs` :
```typescript
interface UseGamificationArgs {
  vault: VaultManager | null;
  notifPrefs: NotificationPreferences;
  onDataChange?: (profiles: Profile[]) => void;
  onQuestProgress?: (profileId: string, type: string, amount: number) => Promise<void>;
  onContribution?: (type: ContributionType, profileId: string) => Promise<void>;
}
```
3. Ajouter `onContribution` dans le destructuring de la fonction `useGamification`.
4. Dans `completeTask()`, APRES le bloc Museum existant (ligne ~316, chercher le commentaire "Musee" ou le dernier bloc try/catch non-critique avant l'ecriture vault), inserer :
```typescript
// Contribution village (COOP-02) -- fire-and-forget non-critical
if (onContribution) {
  try { await onContribution('task', profile.id); } catch { /* Village -- non-critical */ }
}
```
5. Toast contribution village (per D-04) -- APRES le bloc contribution ci-dessus, avec un delai de 300ms pour eviter le doublon avec le toast semantique (Pitfall 1) :
```typescript
// Toast village (D-04) -- delai 300ms pour eviter doublon avec toast semantique
if (onContribution) {
  setTimeout(() => {
    try { showToast('+1 Village 🏡', 'success'); } catch {}
  }, 300);
}
```

**app/(tabs)/tasks.tsx -- Passer addContribution (per Pitfall 2):**

1. Ajouter `import { useGarden } from '../../hooks/useGarden';` en haut.
2. Dans `TasksScreen()`, apres le destructuring de `useVault()`, ajouter : `const { addContribution } = useGarden();`
3. Modifier l'instanciation de useGamification (ligne 198) :
```typescript
const { completeTask } = useGamification({ vault, notifPrefs, onQuestProgress: contributeFamilyQuest, onContribution: addContribution });
```

**app/(tabs)/index.tsx -- Passer addContribution (per Pitfall 2):**

1. Ajouter `import { useGarden } from '../../hooks/useGarden';` en haut.
2. Dans le composant principal, apres le destructuring de `useVault()`, ajouter : `const { addContribution } = useGarden();`
3. Modifier l'instanciation de useGamification (ligne 220) :
```typescript
const { completeTask } = useGamification({ vault, notifPrefs, onQuestProgress: contributeFamilyQuest, onContribution: addContribution });
```

**app/(tabs)/routines.tsx -- Passer addContribution (per Pitfall 2):**

1. Ajouter `import { useGarden } from '../../hooks/useGarden';` en haut.
2. Dans `RoutinesScreen()`, apres le destructuring de `useVault()`, ajouter : `const { addContribution } = useGarden();`
3. Modifier l'instanciation de useGamification (ligne 346) :
```typescript
const { completeTask } = useGamification({ vault, notifPrefs, onContribution: addContribution });
```

**NOTE loot.tsx :** NE PAS modifier `loot.tsx` -- il n'utilise que `openLootBox`, pas `completeTask`. Aucune contribution village pour les lootbox.
  </action>
  <verify>
    <automated>cd /Users/gabrielwaltio/Documents/family-vault && npx tsc --noEmit 2>&1 | head -40</automated>
  </verify>
  <acceptance_criteria>
    - hooks/useGamification.ts contient `onContribution?: (type: ContributionType, profileId: string) => Promise<void>` dans UseGamificationArgs
    - hooks/useGamification.ts contient `await onContribution('task', profile.id)` dans un try/catch
    - hooks/useGamification.ts contient `showToast('+1 Village 🏡'` avec setTimeout 300ms
    - app/(tabs)/tasks.tsx contient `onContribution: addContribution` dans l'appel useGamification
    - app/(tabs)/tasks.tsx contient `useGarden` dans les imports
    - app/(tabs)/index.tsx contient `onContribution: addContribution` dans l'appel useGamification
    - app/(tabs)/index.tsx contient `useGarden` dans les imports
    - app/(tabs)/routines.tsx contient `onContribution: addContribution` dans l'appel useGamification
    - app/(tabs)/routines.tsx contient `useGarden` dans les imports
    - app/(tabs)/loot.tsx ne contient PAS `onContribution` (inchange)
    - `npx tsc --noEmit` ne produit pas de nouvelles erreurs liees a useGamification, tasks, index ou routines
  </acceptance_criteria>
  <done>Chaque tache completee depuis n'importe quel ecran (tasks, dashboard, routines) ajoute automatiquement une contribution 'task' au village avec un toast discret '+1 Village' decale de 300ms.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` -- zero nouvelle erreur
2. Grep confirmation : `grep -r "onContribution" hooks/useFarm.ts hooks/useGamification.ts` retourne les deux hooks
3. Grep confirmation : `grep -r "addContribution" app/\(tabs\)/tasks.tsx app/\(tabs\)/index.tsx app/\(tabs\)/routines.tsx app/\(tabs\)/tree.tsx` retourne les 4 ecrans
4. Grep confirmation : `grep "villageFAB" app/\(tabs\)/tree.tsx` ne retourne RIEN (FAB supprime)
5. Grep confirmation : `grep "PortalSprite" app/\(tabs\)/tree.tsx` retourne le composant
</verification>

<success_criteria>
- Le portail est visible dans tree.tsx avec glow anime, remplacant le FAB
- Le tap portail declenche fade 400ms + navigation village
- Les recoltes et taches ajoutent des contributions automatiques au village
- Le toast "+1 Village" apparait apres chaque contribution
- Aucune regression TypeScript
</success_criteria>

<output>
After completion, create `.planning/phases/28-portail-c-blage-contributions/28-01-SUMMARY.md`
</output>
