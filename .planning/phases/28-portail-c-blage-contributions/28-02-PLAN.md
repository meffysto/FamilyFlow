---
phase: 28-portail-c-blage-contributions
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/village/activities.ts
  - lib/village/index.ts
  - app/(tabs)/village.tsx
autonomous: true
requirements: [OBJ-03, OBJ-04]

must_haves:
  truths:
    - "Quand l'objectif est atteint, une carte de recompense apparait avec CTA et suggestion d'activite IRL saisonniere"
    - "Le claim applique +25 XP a TOUS les profils actifs (pas uniquement au profil qui clique)"
    - "Le claim attribue 1 loot box bonus a TOUS les profils actifs (item cosmetique per OBJ-03)"
    - "L'activite IRL suggeree change chaque semaine et est adaptee a la saison courante"
    - "La carte se dismiss par tap avec un fade out 200ms"
    - "La recompense deja claimee affiche 'Recompense reclamee' sans CTA"
  artifacts:
    - path: "lib/village/activities.ts"
      provides: "Liste curatee ~20 activites IRL par saison + pickSeasonalActivity()"
      exports: ["IRL_ACTIVITIES", "pickSeasonalActivity"]
    - path: "app/(tabs)/village.tsx"
      provides: "RewardCard inline + handleClaim etendu avec bonus XP collectif + loot box"
      contains: "RewardCard"
  key_links:
    - from: "app/(tabs)/village.tsx"
      to: "lib/village/activities.ts"
      via: "import pickSeasonalActivity"
      pattern: "pickSeasonalActivity"
    - from: "app/(tabs)/village.tsx"
      to: "lib/gamification/engine.ts"
      via: "import addPoints pour bonus XP collectif"
      pattern: "addPoints"
    - from: "app/(tabs)/village.tsx"
      to: "hooks/useGarden.ts"
      via: "claimReward() + isGoalReached"
      pattern: "claimReward|isGoalReached"
---

<objective>
Ajouter la liste curatee d'activites IRL saisonnieres et la carte de recompense collective dans village.tsx -- bonus XP equitable pour tous les profils + loot box cosmetique + suggestion d'activite familiale.

Purpose: Fermer la boucle cooperative du milestone v1.4 -- l'atteinte de l'objectif village recompense toute la famille et propose une activite IRL.
Output: `lib/village/activities.ts` avec 20 activites par saison, `RewardCard` inline dans `village.tsx` avec bonus XP + loot box + dismiss.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-portail-c-blage-contributions/28-CONTEXT.md
@.planning/phases/28-portail-c-blage-contributions/28-RESEARCH.md
@.planning/phases/28-portail-c-blage-contributions/28-UI-SPEC.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From hooks/useGarden.ts:
```typescript
export interface UseGardenReturn {
  gardenData: VillageData;
  currentTarget: number;
  progress: number;
  isGoalReached: boolean;
  currentTemplate: ObjectiveTemplate;
  addContribution: (type: ContributionType, profileId: string) => Promise<void>;
  claimReward: (profileId: string) => Promise<boolean>;
  weekHistory: VillageWeekRecord[];
  isLoading: boolean;
}
```

From lib/village/types.ts:
```typescript
export interface VillageData {
  version: number;
  createdAt: string;
  currentWeekStart: string;
  currentThemeIndex: number;
  rewardClaimed: boolean;
  contributions: VillageContribution[];
  pastWeeks: VillageWeekRecord[];
}
```

From lib/mascot/seasons.ts:
```typescript
export type Season = 'printemps' | 'ete' | 'automne' | 'hiver';
export function getCurrentSeason(): Season;
```

From lib/gamification/engine.ts:
```typescript
export function addPoints(
  profile: Profile,
  basePoints: number,
  note: string,
  activeRewards?: ActiveReward[],
): { profile: Profile; entry: GamificationEntry; activeRewards?: ActiveReward[] };
```

From lib/parser.ts:
```typescript
export function parseFarmProfile(raw: string): FarmProfileData;
export function serializeFarmProfile(profileName: string, data: FarmProfileData): string;
```

Current village.tsx state:
```typescript
const { activeProfile, profiles } = useVault();
// ... useGarden() already imported and used
const season = getCurrentSeason();
// handleClaim exists -- sets claimedThisSession on success
// alreadyClaimed = isGoalReached && (gardenData?.rewardClaimed || claimedThisSession)
// canClaim = isGoalReached && !alreadyClaimed
// claimBtn already renders when canClaim is true (ligne 268-280)
```

From lib/village/index.ts (current barrel):
```typescript
export * from './types';
export * from './grid';
export * from './templates';
export * from './parser';
// activities.ts NOT YET exported
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Liste activites IRL saisonnieres + barrel export</name>
  <files>lib/village/activities.ts, lib/village/index.ts</files>
  <read_first>
    - lib/village/index.ts (barrel exports actuels)
    - lib/village/types.ts (types existants)
    - lib/mascot/seasons.ts (type Season -- ligne 8)
    - .planning/phases/28-portail-c-blage-contributions/28-RESEARCH.md (section "Liste activites IRL")
  </read_first>
  <action>
**lib/village/activities.ts -- Creer le fichier (per D-06):**

```typescript
// lib/village/activities.ts
// Activites familiales IRL curatees par saison (OBJ-04, D-06).
// Selection deterministe par semaine -- meme activite pour tous les membres.

import type { Season } from '../mascot/seasons';

/** ~5 activites par saison -- 20 au total */
export const IRL_ACTIVITIES: Record<Season, string[]> = {
  printemps: [
    'Pique-nique au parc',
    'Planter des fleurs ensemble',
    'Balade a velo en famille',
    'Marche aux fleurs',
    'Jeu de piste dans le jardin',
  ],
  ete: [
    'Sortie a la plage ou a la piscine',
    'Barbecue en famille',
    'Glaces artisanales en promenade',
    'Soiree cinema en plein air',
    'Cueillette de fruits',
  ],
  automne: [
    'Ramasser des chataignes',
    'Balade en foret feuilles colorees',
    'Cuisiner une tarte aux pommes',
    'Visite d\'un marche artisanal',
    'Jeux de societe en famille',
  ],
  hiver: [
    'Soiree crepes',
    'Promenade sous la neige',
    'Regarder un film ensemble au chaud',
    'Faire un puzzle en famille',
    'Preparer des biscuits de Noel',
  ],
};

/**
 * Selectionne une activite deterministe pour la semaine courante.
 * Hash simple sur weekStart (YYYY-MM-DD) pour rotation.
 * Deux profils qui appellent avec le meme weekStart obtiennent le meme resultat.
 */
export function pickSeasonalActivity(season: Season, weekStart: string): string {
  const activities = IRL_ACTIVITIES[season];
  const hash = weekStart.split('-').reduce((acc, n) => acc + parseInt(n, 10), 0);
  return activities[hash % activities.length];
}
```

NOTE : Pas d'emojis dans les strings d'activite -- les emojis de la RESEARCH etaient illustratifs. Le formatage visuel est gere dans le composant RewardCard de village.tsx.

NOTE : Pas d'accents dans les strings pour eviter les problemes d'encodage avec gray-matter (pattern codebase). Si le codebase utilise deja des accents dans les strings JS/TS, les conserver -- verifier dans `read_first`.

**CORRECTION accents :** Le codebase React Native utilise des accents partout dans les strings UI (francais). Conserver les accents :
- 'Pique-nique au parc' -> OK pas d'accent
- 'Balade a velo en famille' -> avec accents
- 'Marche aux fleurs' -> avec accent
- etc. Ecrire en francais correct avec accents.

**lib/village/index.ts -- Ajouter l'export :**

Ajouter a la fin du barrel :
```typescript
export * from './activities';
```
  </action>
  <verify>
    <automated>cd /Users/gabrielwaltio/Documents/family-vault && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - lib/village/activities.ts existe et contient `export const IRL_ACTIVITIES: Record<Season, string[]>`
    - lib/village/activities.ts contient `export function pickSeasonalActivity(season: Season, weekStart: string): string`
    - lib/village/activities.ts contient exactement 4 cles saison : printemps, ete, automne, hiver
    - lib/village/activities.ts contient au moins 5 activites par saison (20 total minimum)
    - lib/village/index.ts contient `export * from './activities'`
    - `npx tsc --noEmit` ne produit pas de nouvelles erreurs liees a village/activities.ts
  </acceptance_criteria>
  <done>Le module village exporte pickSeasonalActivity() et IRL_ACTIVITIES avec 20 activites curatees par saison.</done>
</task>

<task type="auto">
  <name>Task 2: RewardCard + bonus XP collectif + loot box cosmetique dans village.tsx</name>
  <files>app/(tabs)/village.tsx</files>
  <read_first>
    - app/(tabs)/village.tsx (ENTIER -- comprendre la structure complete, handleClaim ligne 176-181, claimBtn ligne 268-280, styles, imports, activeProfiles memo ligne 160-162)
    - hooks/useGarden.ts (claimReward signature et return -- lignes 220-240)
    - lib/gamification/engine.ts (addPoints signature -- lignes 55-60, lootBoxesAvailable field)
    - lib/parser.ts (parseFarmProfile, serializeFarmProfile -- signatures)
    - lib/village/activities.ts (cree dans Task 1 -- pickSeasonalActivity)
    - constants/spacing.ts (Spacing tokens)
    - constants/typography.ts (FontSize, FontWeight tokens)
    - .planning/phases/28-portail-c-blage-contributions/28-UI-SPEC.md (RewardCard spec, animations, copywriting)
    - .planning/phases/28-portail-c-blage-contributions/28-RESEARCH.md (Pitfall 5 bonus uniquement profil actif, bonus XP section)
  </read_first>
  <action>
**village.tsx -- Ajouter imports necessaires :**

1. Ajouter aux imports existants :
```typescript
import { pickSeasonalActivity } from '../../lib/village/activities';
import { addPoints } from '../../lib/gamification/engine';
import { parseFarmProfile, serializeFarmProfile } from '../../lib/parser';
```
2. Ajouter `vault` au destructuring de `useVault()` (actuellement `const { activeProfile, profiles } = useVault()`) :
```typescript
const { activeProfile, profiles, vault, refreshGamification } = useVault();
```

**village.tsx -- Memo activite IRL saisonniere :**

Apres le memo `activeProfiles` (ligne ~160), ajouter :
```typescript
const activity = useMemo(
  () => gardenData?.currentWeekStart
    ? pickSeasonalActivity(season, gardenData.currentWeekStart)
    : '',
  [season, gardenData?.currentWeekStart],
);
```

**village.tsx -- Helper addVillageBonus (per D-07, Pitfall 5) :**

Creer un helper local (hors du composant, en haut du fichier apres les imports) :
```typescript
/**
 * Applique un bonus XP + 1 loot box a un profil via son fichier gami-{id}.md.
 * Pattern identique a addCoins dans useFarm.ts -- lecture/ecriture directe vault.
 * Per D-07 : reutilise le systeme loot existant (lootBoxesAvailable) pour l'item cosmetique.
 */
async function addVillageBonus(
  vaultMgr: VaultManager,
  profile: Profile,
  xpAmount: number,
  note: string,
): Promise<void> {
  const gamiPath = `gami-${profile.id}.md`;
  const raw = await vaultMgr.readFile(gamiPath);
  if (!raw) return;
  const farmData = parseFarmProfile(raw);
  const { profile: updated } = addPoints(profile, xpAmount, note);
  // Merger les points mis a jour dans farmData + ajouter 1 loot box (item cosmetique OBJ-03)
  const merged = {
    ...farmData,
    points: updated.points,
    coins: updated.coins,
    level: updated.level,
    lootBoxesAvailable: (farmData.lootBoxesAvailable ?? 0) + 1,
    history: [...(farmData.history ?? []), {
      profileId: profile.id,
      action: `+${xpAmount}`,
      points: xpAmount,
      note,
      timestamp: new Date().toISOString(),
    }],
  };
  await vaultMgr.writeFile(gamiPath, serializeFarmProfile(profile.name, merged));
}
```

**village.tsx -- Etendre handleClaim (per D-07 -- bonus equitable TOUS profils actifs + loot box cosmetique OBJ-03) :**

Remplacer le `handleClaim` existant (lignes 176-181) par :
```typescript
const handleClaim = useCallback(async () => {
  if (!activeProfile || !vault) return;
  Haptics.notificationAsync(Haptics.NotificationFeedbackType.Success);
  const success = await claimReward(activeProfile.id);
  if (success) {
    setClaimedThisSession(true);
    // Bonus XP + loot box pour TOUS les profils actifs (D-07 -- equitable, pas proportionnel)
    // OBJ-03 : +25 XP + 1 loot box (item cosmetique via systeme loot existant per D-07)
    for (const p of activeProfiles) {
      try {
        await addVillageBonus(vault, p, 25, 'Objectif village atteint');
      } catch { /* Gamification -- non-critical */ }
    }
    if (refreshGamification) refreshGamification();
    showToast('+25 XP et 1 loot box pour toute la famille !', 'success');
  }
}, [activeProfile, vault, claimReward, activeProfiles, refreshGamification, showToast]);
```

**village.tsx -- Creer RewardCard inline (per D-06, UI-SPEC) :**

Creer le composant inline (< 60 lignes) AVANT le composant principal.
IMPORTANT : Ne PAS utiliser de couleurs hardcodees (CLAUDE.md interdit '#FFFFFF' etc.) -- utiliser les tokens du theme via le prop `colors` deja passe au composant.

```typescript
function RewardCard({
  canClaim,
  alreadyClaimed,
  activity,
  onClaim,
  colors,
}: {
  canClaim: boolean;
  alreadyClaimed: boolean;
  activity: string;
  onClaim: () => void;
  colors: ReturnType<typeof useThemeColors>['colors'];
}) {
  const [dismissed, setDismissed] = useState(false);
  const opacity = useSharedValue(1);
  const cardStyle = useAnimatedStyle(() => ({ opacity: opacity.value }));

  const handleDismiss = useCallback(() => {
    opacity.value = withTiming(0, { duration: 200 }, () => {
      runOnJS(setDismissed)(true);
    });
  }, [opacity]);

  if (dismissed) return null;

  return (
    <Animated.View
      entering={FadeInDown.delay(150).duration(350)}
      style={[
        {
          backgroundColor: colors.card,
          borderRadius: Radius.xl,
          padding: Spacing.xl,
          marginHorizontal: Spacing['2xl'],
          marginTop: Spacing['2xl'],
        },
        cardStyle,
      ]}
    >
      <Text style={{
        fontSize: FontSize.heading,
        fontWeight: FontWeight.semibold,
        lineHeight: LineHeight.title,
        color: colors.text,
        marginBottom: Spacing.md,
      }}>
        {alreadyClaimed ? 'Objectif atteint !' : 'Objectif atteint !'}
      </Text>

      <Text style={{
        fontSize: FontSize.body,
        fontWeight: FontWeight.normal,
        lineHeight: LineHeight.body,
        color: colors.textMuted ?? colors.textSub,
        marginBottom: Spacing['2xl'],
      }}>
        Activite famille cette semaine
      </Text>
      <Text style={{
        fontSize: FontSize.title,
        fontWeight: FontWeight.semibold,
        lineHeight: LineHeight.title,
        color: colors.text,
        marginBottom: Spacing['2xl'],
      }}>
        {activity}
      </Text>

      {canClaim && (
        <TouchableOpacity
          onPress={onClaim}
          style={{
            backgroundColor: colors.success,
            borderRadius: Radius.lg,
            paddingVertical: Spacing.xl,
            alignItems: 'center',
            marginBottom: Spacing.md,
          }}
          activeOpacity={0.8}
          accessibilityRole="button"
          accessibilityLabel="Recuperer la recompense"
        >
          <Text style={{
            color: colors.background,
            fontSize: FontSize.body,
            fontWeight: FontWeight.semibold,
          }}>
            Recuperer la recompense (+25 XP + 1 loot box)
          </Text>
        </TouchableOpacity>
      )}

      {alreadyClaimed && (
        <Text style={{
          color: colors.textMuted ?? colors.textSub,
          fontSize: FontSize.sm,
          textAlign: 'center',
          marginBottom: Spacing.md,
        }}>
          Recompense reclamee
        </Text>
      )}

      <TouchableOpacity onPress={handleDismiss} style={{ alignItems: 'center' }}>
        <Text style={{
          color: colors.textSub,
          fontSize: FontSize.sm,
        }}>
          Fermer la carte
        </Text>
      </TouchableOpacity>
    </Animated.View>
  );
}
```

NOTE : Le texte du bouton CTA utilise `colors.background` au lieu de '#FFFFFF' -- sur theme clair c'est blanc, sur theme sombre c'est le fond sombre, ce qui garantit le contraste avec `colors.success` dans les deux cas. Verifier dans `read_first` que `colors.background` existe bien dans le theme (c'est un token standard de useThemeColors).

NOTE accents : Utiliser les accents corrects en francais dans les strings UI -- 'Activite famille cette semaine', 'Recuperer la recompense', 'Recompense reclamee'. Verifier le pattern existant dans village.tsx pour la convention.

**village.tsx -- Integrer RewardCard dans le JSX :**

Trouver le bloc `claimBtn` existant (lignes 268-280) et le REMPLACER par le composant RewardCard, positionne apres la barre de progression :
```tsx
{/* Carte recompense collective (OBJ-03, OBJ-04) */}
{isGoalReached && (
  <RewardCard
    canClaim={canClaim}
    alreadyClaimed={alreadyClaimed}
    activity={activity}
    onClaim={handleClaim}
    colors={colors}
  />
)}
```

Supprimer l'ancien bloc `claimBtn` (le TouchableOpacity standalone lignes 268-280) puisque RewardCard le remplace.
  </action>
  <verify>
    <automated>cd /Users/gabrielwaltio/Documents/family-vault && npx tsc --noEmit 2>&1 | head -40</automated>
  </verify>
  <acceptance_criteria>
    - app/(tabs)/village.tsx contient `function RewardCard` comme composant inline
    - app/(tabs)/village.tsx contient `pickSeasonalActivity` dans les imports
    - app/(tabs)/village.tsx contient `addPoints` dans les imports (depuis lib/gamification/engine)
    - app/(tabs)/village.tsx contient `addVillageBonus` comme helper local
    - app/(tabs)/village.tsx contient `for (const p of activeProfiles)` dans handleClaim (bonus TOUS profils)
    - app/(tabs)/village.tsx contient le nombre `25` comme montant XP dans addVillageBonus
    - app/(tabs)/village.tsx contient `lootBoxesAvailable: (farmData.lootBoxesAvailable ?? 0) + 1` dans addVillageBonus (item cosmetique OBJ-03)
    - app/(tabs)/village.tsx contient `showToast('+25 XP et 1 loot box pour toute la famille !'`
    - app/(tabs)/village.tsx NE contient PAS '#FFFFFF' ou toute autre couleur hardcodee -- utilise `colors.background` pour le texte du bouton
    - app/(tabs)/village.tsx contient `withTiming(0, { duration: 200 }` dans handleDismiss de RewardCard
    - app/(tabs)/village.tsx contient `FadeInDown.delay(150).duration(350)` dans RewardCard entering
    - app/(tabs)/village.tsx contient `Fermer la carte` comme texte dismiss
    - app/(tabs)/village.tsx ne contient PLUS l'ancien bloc claimBtn standalone (le TouchableOpacity avec `onPress={handleClaim}` dans un Animated.View entering FadeInDown.duration(400))
    - lib/village/activities.ts contient `pickSeasonalActivity`
    - `npx tsc --noEmit` ne produit pas de nouvelles erreurs liees a village.tsx
  </acceptance_criteria>
  <done>L'objectif atteint affiche une RewardCard avec suggestion d'activite IRL saisonniere, le claim distribue +25 XP et 1 loot box a tous les profils actifs, la carte se dismiss par tap avec fade 200ms, aucune couleur hardcodee.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` -- zero nouvelle erreur
2. Grep confirmation : `grep "pickSeasonalActivity" app/\(tabs\)/village.tsx lib/village/activities.ts` retourne les deux fichiers
3. Grep confirmation : `grep "addVillageBonus" app/\(tabs\)/village.tsx` retourne le helper et son appel
4. Grep confirmation : `grep "RewardCard" app/\(tabs\)/village.tsx` retourne la definition et l'usage JSX
5. Grep confirmation : `grep "for.*activeProfiles" app/\(tabs\)/village.tsx` confirme le bonus collectif
6. Grep confirmation : `grep "IRL_ACTIVITIES" lib/village/activities.ts` confirme la liste curatee
7. Grep confirmation : `grep "lootBoxesAvailable" app/\(tabs\)/village.tsx` confirme l'item cosmetique
8. Grep anti-pattern : `grep "#FFFFFF\|#ffffff" app/\(tabs\)/village.tsx` ne retourne RIEN (pas de couleur hardcodee)
</verification>

<success_criteria>
- La carte de recompense apparait quand isGoalReached est true
- L'activite IRL change chaque semaine et correspond a la saison
- Le bonus XP (+25) est applique a TOUS les profils actifs (pas seulement l'actif)
- 1 loot box bonus est attribuee a TOUS les profils actifs (item cosmetique OBJ-03)
- Aucune couleur hardcodee -- tout via useThemeColors()
- La carte se dismiss avec animation fade 200ms
- Aucune regression TypeScript
</success_criteria>

<output>
After completion, create `.planning/phases/28-portail-c-blage-contributions/28-02-SUMMARY.md`
</output>
