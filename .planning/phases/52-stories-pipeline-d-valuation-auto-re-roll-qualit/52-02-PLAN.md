---
phase: 52-stories-pipeline-d-valuation-auto-re-roll-qualit
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - lib/types.ts
  - lib/parser.ts
  - lib/eval/pipeline.ts
  - lib/eval/prompts.ts
  - lib/__tests__/eval-frontmatter-roundtrip.test.ts
  - app/(tabs)/stories.tsx
autonomous: true
requirements:
  - EVAL-02
  - EVAL-03
  - EVAL-07
must_haves:
  truths:
    - "Quand une génération produit une story hardFail, le pipeline déclenche 1 re-roll automatique avec prompt augmenté (issues incluses)"
    - "Cap re-roll strictement = 1 : la 2ème version ship même si elle hardFail encore (avec quality_issues remplis)"
    - "Le frontmatter d'une story persiste : quality_score (0-10), quality_dimensions, quality_issues, quality_retried, quality_evaluated_at"
    - "Round-trip parser → serializer → parser conserve tous les champs quality_* sans perte"
    - "Si isEvalEnabled() === false, le comportement de stories.tsx est strictement identique au commit baseline (zéro appel à evaluateStoryDeterministic, zéro re-roll, zéro champ quality_*)"
  artifacts:
    - path: "lib/eval/pipeline.ts"
      provides: "runRubricAndMaybeReroll() — orchestre rubric + 1 re-roll cap"
      contains: "export async function runRubricAndMaybeReroll"
    - path: "lib/types.ts"
      provides: "BedtimeStory étendu avec champs quality_* optionnels"
      contains: "quality_score?: number"
    - path: "lib/parser.ts"
      provides: "serializeBedtimeStory + parseStoryFrontmatter étendus pour quality_*"
    - path: "lib/__tests__/eval-frontmatter-roundtrip.test.ts"
      provides: "Test round-trip frontmatter quality_*"
  key_links:
    - from: "app/(tabs)/stories.tsx"
      to: "lib/eval/pipeline.ts"
      via: "import { runRubricAndMaybeReroll } puis appel après generateBedtimeStory"
      pattern: "runRubricAndMaybeReroll\\("
    - from: "lib/eval/pipeline.ts"
      to: "lib/eval/rubric.ts"
      via: "import { evaluateStoryDeterministic }"
      pattern: "evaluateStoryDeterministic\\("
    - from: "lib/parser.ts (serializeBedtimeStory)"
      to: "BedtimeStory.quality_score"
      via: "if (story.quality_score !== undefined) lines.push(...)"
      pattern: "quality_score"
---

<objective>
Brancher le rubric (Plan 52-01) sur le call site de génération `app/(tabs)/stories.tsx` : si `isEvalEnabled()` et hardFail détecté, déclencher 1 re-roll Sonnet avec prompt augmenté, persister scores + issues + flag retried dans le frontmatter Markdown du vault. Cap strict 1 retry, fallback ship 1ère version si 2ème échoue. Test round-trip parser obligatoire pour garantir la non-corruption du vault.

Purpose: Réaliser le cœur de Phase 52 — la boucle de qualité. Le parent voit silencieusement les hard fails attrapés ; le frontmatter trace la provenance de chaque story (re-roll ou non, score, issues).
Output: Pipeline de re-roll branché, frontmatter étendu, test de non-régression du parser, comportement baseline strictement préservé quand flag off.
</objective>

<execution_context>
@.claude/CLAUDE.md
@.planning/phases/52-stories-pipeline-d-valuation-auto-re-roll-qualit/52-AI-SPEC.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/52-stories-pipeline-d-valuation-auto-re-roll-qualit/52-AI-SPEC.md
@.planning/phases/52-stories-pipeline-d-valuation-auto-re-roll-qualit/52-01-SUMMARY.md
@lib/types.ts
@lib/parser.ts
@lib/ai-service.ts
@lib/eval/rubric.ts
@lib/eval/types.ts
@lib/eval/feature-flag.ts
@lib/eval/prompts.ts
@app/(tabs)/stories.tsx

<interfaces>
<!-- Du Plan 52-01 (déjà existant après Wave 1) -->

From lib/eval/types.ts:
```typescript
export interface RubricResult {
  dimensions: RubricDimension[];
  hardFail: boolean;
  softWarnings: string[];
  rerollPromptHint: string;
  qualityScore: number;
}
```

From lib/eval/rubric.ts:
```typescript
export function evaluateStoryDeterministic(
  story: BedtimeStory,
  child: Profile,
  recentStories: BedtimeStory[],
): RubricResult;
```

From lib/eval/feature-flag.ts:
```typescript
export function isEvalEnabled(): boolean; // false par défaut
```

From lib/ai-service.ts:992 :
```typescript
export async function generateBedtimeStory(
  config: AIConfig,
  params: { story, child, recentHistory?, ... },
): Promise<{ titre: string; texte: string; ... }>;
```

From app/(tabs)/stories.tsx:1838 — call site existant à NE PAS casser :
```typescript
const resp = await generateBedtimeStory(storyConfig ?? aiConfig, { ..., recentHistory });
// puis construction d'un BedtimeStory et saveStory(story)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Étendre BedtimeStory + parser/serializer pour champs quality_* (round-trip)</name>
  <files>lib/types.ts, lib/parser.ts, lib/__tests__/eval-frontmatter-roundtrip.test.ts</files>
  <read_first>
    - lib/types.ts (lignes 761-797 — BedtimeStory)
    - lib/parser.ts (lignes 3539-3700 — serializeBedtimeStory + parseStoryFrontmatter)
    - lib/__tests__/parser-extended.test.ts (style tests round-trip existants)
    - .planning/phases/52-stories-pipeline-d-valuation-auto-re-roll-qualit/52-AI-SPEC.md (Section 7 — schéma frontmatter ligne 757)
    - lib/eval/types.ts (RubricResult — pour shape quality_dimensions)
  </read_first>
  <behavior>
    - Test : story avec quality_score=8.5, quality_dimensions={...}, quality_issues=["foo"], quality_retried=true → serialize → parse → mêmes valeurs
    - Test : story sans aucun champ quality_* (legacy) → serialize → parse → champs undefined (rétrocompat)
    - Test : parser tolère YAML manquant (story pré-Phase 52) sans throw
    - Test : quality_score est sérialisé en number (pas string)
    - Test : quality_issues est sérialisé en array YAML inline `quality_issues: ["foo", "bar"]`
    - Test : quality_dimensions sérialisé en mapping clé/valeur lisible
  </behavior>
  <action>
1. **Étendre `lib/types.ts` BedtimeStory** (après ligne 797, AVANT la fermeture `}` de l'interface) :

```typescript
  // ─── Phase 52 — Pipeline d'évaluation auto (tous optionnels, rétrocompat 100%) ──
  /** Score qualité 0-10 (moyenne pondérée du rubric déterministe). null si flag eval off. */
  quality_score?: number;
  /** Scores par dimension du rubric (1=hard fail, 2=soft warning, 3=OK). */
  quality_dimensions?: {
    longueur?: 1 | 2 | 3;
    fin_paisible?: 1 | 2 | 3;
    vocabulaire?: 1 | 2 | 3;
    anti_clones?: 1 | 2 | 3;
    tags_tts?: 1 | 2 | 3;
    coherence_saga?: 1 | 2 | 3;
  };
  /** Raisons des soft warnings (FR, lisibles parent). */
  quality_issues?: string[];
  /** True si la story actuelle est issue d'un re-roll Phase 52. */
  quality_retried?: boolean;
  /** ISO timestamp évaluation déterministe. */
  quality_evaluated_at?: string;
  /** Sous-scores LLM-judge (rempli async par Plan 52-03). null tant que pas résolu. */
  llm_judge?: {
    rythme: number;
    originalite: number;
    charge_emotionnelle: number;
    fluidite: number;
    justification: string;
    evaluated_at: string;
  } | null;
```

2. **Étendre `lib/parser.ts:serializeBedtimeStory`** (vers ligne 3539) — ajouter les lignes de frontmatter à la suite des autres champs optionnels :

```typescript
// Pattern existant : if (story.foo !== undefined) lines.push(`foo: ${story.foo}`)
if (story.quality_score !== undefined) lines.push(`quality_score: ${story.quality_score}`);
if (story.quality_dimensions) {
  lines.push('quality_dimensions:');
  for (const [k, v] of Object.entries(story.quality_dimensions)) {
    if (v !== undefined) lines.push(`  ${k}: ${v}`);
  }
}
if (story.quality_issues && story.quality_issues.length > 0) {
  // YAML inline array, échapper les " internes
  const items = story.quality_issues.map((s) => `"${s.replace(/"/g, '\\"')}"`).join(', ');
  lines.push(`quality_issues: [${items}]`);
}
if (story.quality_retried !== undefined) lines.push(`quality_retried: ${story.quality_retried}`);
if (story.quality_evaluated_at) lines.push(`quality_evaluated_at: ${story.quality_evaluated_at}`);
if (story.llm_judge) {
  lines.push('llm_judge:');
  lines.push(`  rythme: ${story.llm_judge.rythme}`);
  lines.push(`  originalite: ${story.llm_judge.originalite}`);
  lines.push(`  charge_emotionnelle: ${story.llm_judge.charge_emotionnelle}`);
  lines.push(`  fluidite: ${story.llm_judge.fluidite}`);
  lines.push(`  justification: "${story.llm_judge.justification.replace(/"/g, '\\"')}"`);
  lines.push(`  evaluated_at: ${story.llm_judge.evaluated_at}`);
}
```

3. **Étendre `lib/parser.ts:parseStoryFrontmatter` et le code consommateur** (vers ligne 3591, dans la fonction qui retourne `BedtimeStory`) — ajouter parsing :

```typescript
// Après les autres champs optionnels (cf. trancheAge ligne 3662)
const quality_score = d.quality_score !== undefined ? Number(d.quality_score) : undefined;
const quality_retried = d.quality_retried === 'true' ? true : d.quality_retried === 'false' ? false : undefined;
const quality_evaluated_at = typeof d.quality_evaluated_at === 'string' ? d.quality_evaluated_at : undefined;
// quality_dimensions et quality_issues parsés via le bloc YAML structuré (utiliser le parser YAML existant si gray-matter / parser maison)
// Si parser maison ligne-à-ligne : ajouter handler pour les blocs indentés `quality_dimensions:` et arrays inline `quality_issues: [...]`
// llm_judge idem (bloc indenté)
```

⚠ Étudier le pattern de parsing existant dans `parseStoryFrontmatter` (ligne 3591) — si le parser est ligne-à-ligne maison (probable), implémenter les handlers structurés pour `quality_dimensions:` (sub-keys indentées 2 espaces) et `llm_judge:` idem. Référence : pattern `voice:` déjà géré (regarder lignes 3625-3680).

4. **Créer `lib/__tests__/eval-frontmatter-roundtrip.test.ts`** :

```typescript
import { serializeBedtimeStory, parseBedtimeStory } from '../parser';
import type { BedtimeStory } from '../types';

const baseStory: BedtimeStory = {
  id: 'test-1',
  titre: 'Test',
  enfant: 'Enfant 1',
  enfantId: 'e1',
  univers: 'foret',
  texte: 'Une histoire.',
  date: '2026-05-06',
  duree_lecture: 60,
  voice: { engine: 'expo', /* ... champs requis */ } as any,
  version: 1,
  sourceFile: 'test-1.md',
};

describe('Phase 52 — Round-trip frontmatter quality_* (EVAL-03)', () => {
  it('story sans champs quality_* (legacy) round-trip propre', () => {
    const serialized = serializeBedtimeStory(baseStory);
    const parsed = parseBedtimeStory(serialized, 'test-1.md');
    expect(parsed?.quality_score).toBeUndefined();
    expect(parsed?.quality_issues).toBeUndefined();
  });

  it('story avec champs quality_* round-trip avec valeurs identiques', () => {
    const story: BedtimeStory = {
      ...baseStory,
      quality_score: 8.5,
      quality_dimensions: {
        longueur: 3, fin_paisible: 3, vocabulaire: 2,
        anti_clones: 3, tags_tts: 3, coherence_saga: 3,
      },
      quality_issues: ['TTR 0.43, "doucement" ×5, 1 cliché'],
      quality_retried: false,
      quality_evaluated_at: '2026-05-06T20:14:32Z',
    };
    const serialized = serializeBedtimeStory(story);
    const parsed = parseBedtimeStory(serialized, 'test-1.md');
    expect(parsed?.quality_score).toBe(8.5);
    expect(parsed?.quality_dimensions).toEqual(story.quality_dimensions);
    expect(parsed?.quality_issues).toEqual(story.quality_issues);
    expect(parsed?.quality_retried).toBe(false);
    expect(parsed?.quality_evaluated_at).toBe('2026-05-06T20:14:32Z');
  });

  it('llm_judge round-trip avec justification contenant quotes échappées', () => {
    const story: BedtimeStory = {
      ...baseStory,
      llm_judge: {
        rythme: 8, originalite: 7, charge_emotionnelle: 9, fluidite: 9,
        justification: 'Arc fluide, image "marquante" de l\'étoile.',
        evaluated_at: '2026-05-06T20:14:55Z',
      },
    };
    const serialized = serializeBedtimeStory(story);
    const parsed = parseBedtimeStory(serialized, 'test-1.md');
    expect(parsed?.llm_judge?.justification).toBe(story.llm_judge!.justification);
  });
});
```

5. **Vérifier** que les tests existants `parser-extended.test.ts`, `parser.test.ts` passent toujours (pas de régression).
  </action>
  <verify>
    <automated>npx jest lib/__tests__/eval-frontmatter-roundtrip.test.ts lib/__tests__/parser.test.ts lib/__tests__/parser-extended.test.ts --no-coverage</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'quality_score?: number' lib/types.ts`
    - `grep -q 'quality_dimensions?:' lib/types.ts`
    - `grep -q 'quality_issues?:' lib/types.ts`
    - `grep -q 'llm_judge?:' lib/types.ts`
    - `grep -q 'quality_score:' lib/parser.ts` (serializer)
    - `grep -q "d\.quality_score" lib/parser.ts` (parser consume)
    - `npx jest lib/__tests__/eval-frontmatter-roundtrip.test.ts --no-coverage` exit 0
    - `npx jest lib/__tests__/parser.test.ts lib/__tests__/parser-extended.test.ts --no-coverage` exit 0 (non-régression parser)
    - `npx tsc --noEmit` clean
    - Pas de bump CACHE_VERSION nécessaire (Stories exclu du cache, cf. CLAUDE.md)
  </acceptance_criteria>
  <done>BedtimeStory étendu, parser+serializer round-trip vérifié, zéro régression tests existants.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pipeline re-roll cap 1 + branchement call site stories.tsx (flag-gated)</name>
  <files>lib/eval/pipeline.ts, lib/eval/prompts.ts, app/(tabs)/stories.tsx</files>
  <read_first>
    - lib/eval/rubric.ts (Plan 52-01)
    - lib/eval/feature-flag.ts (Plan 52-01)
    - lib/eval/types.ts
    - lib/ai-service.ts (lignes 992-1370 — generateBedtimeStory signature complète)
    - app/(tabs)/stories.tsx (lignes 1820-2080 — flux génération + saveStory)
    - .planning/phases/52-stories-pipeline-d-valuation-auto-re-roll-qualit/52-AI-SPEC.md (Section 4 ligne 156-200 = pipeline ; Section 6 G1-G7 = guardrails)
  </read_first>
  <behavior>
    - Test : isEvalEnabled() === false ⇒ runRubricAndMaybeReroll() retourne `{ story: input, rubric: null, retried: false }` instantanément (zéro appel rubric, zéro re-roll)
    - Test : isEvalEnabled() === true && rubric.hardFail === false ⇒ retourne `{ story: input, rubric, retried: false }` (1 seul appel rubric)
    - Test : isEvalEnabled() === true && hardFail === true ⇒ déclenche 1 re-roll via callback `regenerate`, puis retourne la 2ème story (avec rubric ré-évalué)
    - Test : isEvalEnabled() === true && 1ère hardFail && 2ème hardFail aussi ⇒ retourne la 2ème story avec quality_retried=true et quality_issues remplis (cap strict, pas de 3ème tentative)
    - Test : prompt re-roll inclut `REROLL_INSTRUCTIONS_HEADER` + chaque ligne de `rubric.rerollPromptHint`
    - Test (intégration stories.tsx) : avec flag off, le diff de comportement vs commit baseline est vide (zéro nouveau call, zéro nouveau champ écrit)
  </behavior>
  <action>
1. **Compléter `lib/eval/prompts.ts`** (créé Plan 52-01 vide) :

```typescript
// lib/eval/prompts.ts
export const REROLL_INSTRUCTIONS_HEADER =
  '\n\n## Instructions de correction (re-roll Phase 52)\n\nLa version précédente présentait ces problèmes — corrige-les impérativement :\n';

// Plan 52-03 remplira LLM_EVAL_SYSTEM_PROMPT et LLM_EVAL_RETRY_PROMPT
export const LLM_EVAL_SYSTEM_PROMPT = '';
export const LLM_EVAL_RETRY_PROMPT = '';
```

2. **Créer `lib/eval/pipeline.ts`** :

```typescript
import type { BedtimeStory, Profile } from '../types';
import { evaluateStoryDeterministic } from './rubric';
import type { RubricResult } from './types';
import { isEvalEnabled } from './feature-flag';
import { REROLL_INSTRUCTIONS_HEADER } from './prompts';

/**
 * Callback de re-génération injecté par stories.tsx.
 * Reçoit le hint à append au prompt utilisateur, retourne une nouvelle story.
 */
export type RegenerateFn = (rerollHint: string) => Promise<BedtimeStory>;

export interface RubricAndRerollResult {
  story: BedtimeStory;             // story finale (1ère ou 2ème selon re-roll)
  rubric: RubricResult | null;     // null si flag off
  retried: boolean;                // true si re-roll a eu lieu
}

/**
 * Phase 52 — Évalue le rubric, déclenche 1 re-roll si hardFail, persiste les champs quality_* dans la story retournée.
 * Cap strict : 1 retry. Si 2ème échec, ship la 2ème version (pas la 1ère — la 2ème est par hypothèse mieux ou égale).
 *
 * - Si isEvalEnabled() === false : retourne { story, rubric: null, retried: false } immédiatement (EVAL-07).
 * - Idempotent : ne pas appeler 2× sur la même story (responsabilité appelant).
 */
export async function runRubricAndMaybeReroll(
  story: BedtimeStory,
  child: Profile,
  recentStories: BedtimeStory[],
  regenerate: RegenerateFn,
): Promise<RubricAndRerollResult> {
  if (!isEvalEnabled()) {
    return { story, rubric: null, retried: false };
  }

  const rubric1 = evaluateStoryDeterministic(story, child, recentStories);
  if (!rubric1.hardFail) {
    return {
      story: applyRubricToStory(story, rubric1, /* retried */ false),
      rubric: rubric1,
      retried: false,
    };
  }

  // Hard fail → 1 re-roll cap (G6 AI-SPEC)
  if (__DEV__) console.log('[eval] hard fail, déclenchement re-roll', rubric1.rerollPromptHint);
  const hint = REROLL_INSTRUCTIONS_HEADER + rubric1.rerollPromptHint;
  let story2: BedtimeStory;
  try {
    story2 = await regenerate(hint);
  } catch (e) {
    if (__DEV__) console.warn('[eval] re-roll failed, ship original', e);
    return {
      story: applyRubricToStory(story, rubric1, /* retried */ true),
      rubric: rubric1,
      retried: true,
    };
  }

  const rubric2 = evaluateStoryDeterministic(story2, child, recentStories);
  // Cap : on ship story2 quoi qu'il arrive (pas de 3ème tentative)
  return {
    story: applyRubricToStory(story2, rubric2, /* retried */ true),
    rubric: rubric2,
    retried: true,
  };
}

function applyRubricToStory(
  story: BedtimeStory,
  rubric: RubricResult,
  retried: boolean,
): BedtimeStory {
  const dims = Object.fromEntries(rubric.dimensions.map((d) => [d.id, d.score]));
  return {
    ...story,
    quality_score: Math.round(rubric.qualityScore * 10) / 10,
    quality_dimensions: dims as BedtimeStory['quality_dimensions'],
    quality_issues: rubric.softWarnings.length > 0
      ? rubric.softWarnings
      : rubric.dimensions.filter((d) => d.score === 1 && d.reason).map((d) => d.reason!),
    quality_retried: retried,
    quality_evaluated_at: new Date().toISOString(),
  };
}
```

3. **Brancher dans `app/(tabs)/stories.tsx`** (ligne ~1838 où `generateBedtimeStory` est appelé, puis ligne ~2058 où `saveStory(story)` est appelé) :

   - Importer en haut du fichier :
     ```typescript
     import { runRubricAndMaybeReroll } from '../../lib/eval/pipeline';
     ```

   - Localiser le bloc qui construit `story: BedtimeStory` à partir de `resp` (la réponse de generateBedtimeStory) — typiquement entre lignes 1838 et 2058.
   - **Avant** `saveStory(story).catch(...)`, insérer :
     ```typescript
     // Phase 52 — Pipeline d'évaluation auto (no-op si flag off, EVAL-07)
     const evalResult = await runRubricAndMaybeReroll(
       story,
       enfant, // Profile correspondant à story.enfantId
       childRecentStories.slice(0, 5), // top 5 pour anti_clones
       async (rerollHint: string) => {
         // Re-génère avec le prompt augmenté
         const resp2 = await generateBedtimeStory(storyConfig ?? aiConfig, {
           ...originalParams, // mêmes params que l'appel initial
           extraSystemPrompt: rerollHint, // ajout au prompt — AI-SPEC ligne 712
         });
         // Reconstruire BedtimeStory depuis resp2 — réutiliser le même bloc de construction
         return buildStoryFromResponse(resp2, /* same context */);
       },
     );
     // Utiliser evalResult.story (qui inclut les champs quality_*) pour saveStory
     saveStory(evalResult.story).catch((e) => { /* ... */ });
     ```

   ⚠ Le call site existant n'a probablement pas un helper `buildStoryFromResponse` extrait. **2 options** :
   - **Option A (recommandée)** : extraire un helper local `buildStoryFromResponse(resp, ctx)` à partir du bloc actuel ligne 1838-2058, puis l'appeler 2× (1ère gen + re-roll).
   - **Option B** : dupliquer inline dans le callback `regenerate`. Moins propre mais évite refactor.

   Choisir Option A si le bloc est < 60 lignes, Option B sinon.

   ⚠ Si `generateBedtimeStory` n'expose PAS de paramètre `extraSystemPrompt` ou équivalent, ajouter ce paramètre dans `lib/ai-service.ts` (concat au system prompt existant). Documenter l'ajout dans le SUMMARY.

   ⚠ **Garde-fou flag off** : si `isEvalEnabled() === false`, `runRubricAndMaybeReroll` retourne `{ story, rubric: null, retried: false }` instantanément, donc `evalResult.story === story` (même référence). Aucun re-roll, aucun champ quality_*, aucune divergence comportementale.

4. **Vérifier flux complet** :
   - `npx tsc --noEmit` clean
   - `npx jest --no-coverage` clean
   - Lancer manuellement avec flag off (`FEATURE_EVAL_ENABLED = false`) → comportement identique baseline (vérifier qu'un commit + rebuild app produit les mêmes histoires sans champs quality_*)
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -v '^#' | grep -E "lib/eval|app/\(tabs\)/stories" | grep -c "error" | (read n; test "$n" = "0")</automated>
  </verify>
  <acceptance_criteria>
    - `test -f lib/eval/pipeline.ts`
    - `grep -q 'export async function runRubricAndMaybeReroll' lib/eval/pipeline.ts`
    - `grep -q 'isEvalEnabled' lib/eval/pipeline.ts` (early return flag off)
    - `grep -q 'REROLL_INSTRUCTIONS_HEADER' lib/eval/pipeline.ts`
    - `grep -q 'runRubricAndMaybeReroll' app/\(tabs\)/stories.tsx`
    - `grep -v '^#' lib/eval/pipeline.ts | grep -c "applyRubricToStory" -gt 1` (helper utilisé pour les 2 branches)
    - `npx tsc --noEmit` clean (aucune nouvelle erreur)
    - `npx jest --no-coverage` exit 0 (zéro régression sur tests existants ; les nouveaux tests Plan 52-01 passent toujours)
    - Vérification flag off : `grep "FEATURE_EVAL_ENABLED" lib/eval/feature-flag.ts | grep "false"` (toujours false par défaut — Plan 52-04 fournira le toggle)
  </acceptance_criteria>
  <done>Pipeline re-roll branché, cap 1 strict, frontmatter persisté, comportement baseline préservé sous flag off.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client RN ↔ Anthropic API (Sonnet) | Re-roll = 2ème appel Sonnet — surface réseau (existait déjà, on appelle 2× max) |
| Code génération ↔ Vault Markdown | Frontmatter écrit dans le vault iCloud — risque corruption si serializer incorrect |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-02-01 | Denial of Service / Cost | Boucle re-roll infinie | mitigate | Cap STRICT 1 retry via signature `RubricAndRerollResult.retried`. Test unitaire qui force 1ère + 2ème hardFail vérifie qu'il n'y a pas de 3ème appel à `regenerate`. AI-SPEC §602 : coût annuel borné ~$1.5/famille. |
| T-52-02-02 | Tampering | parser/serializer round-trip | mitigate | Test `eval-frontmatter-roundtrip.test.ts` obligatoire avant merge. Si shape diverge → cassure vault entrée existante. AI-SPEC §503-505 (frontmatter shape drift). |
| T-52-02-03 | Information Disclosure | quality_issues dans frontmatter | accept | Les issues sont du texte technique FR (ex: "TTR 0.43, doucement ×5") — pas de PII. Vault iCloud privé du parent. |
| T-52-02-04 | Repudiation | Re-roll silencieux dégrade UX | mitigate | `quality_retried: true` est tracé dans le frontmatter — parent peut auditer pourquoi une story a été re-rolled. `__DEV__` console log également. |
| T-52-02-05 | Elevation of Privilege | Prompt injection via rerollPromptHint | mitigate | `rerollPromptHint` est construit à partir de constantes regex côté code (pas d'input utilisateur direct injecté). AI-SPEC §240 : tests offline. |
</threat_model>

<verification>
- `npx tsc --noEmit` clean
- `npx jest --no-coverage` exit 0 (Plan 52-01 tests + nouveaux roundtrip tests + tous les tests existants)
- Sous `FEATURE_EVAL_ENABLED = false`, `git diff app/(tabs)/stories.tsx` vs baseline génère des stories sans champs quality_* (test manuel sur device dev)
- Cap re-roll vérifié : test unitaire force 2 hardFails consécutifs, attend exactement 1 appel à `regenerate`
</verification>

<success_criteria>
- Re-roll automatique cap 1 fonctionne (EVAL-02)
- quality_score, quality_dimensions, quality_issues, quality_retried persistés dans frontmatter (EVAL-03)
- Round-trip parser sans perte (EVAL-08 partie tests)
- Flag off ⇒ comportement strictement identique baseline, zéro champ quality_* écrit (EVAL-07)
- `npx tsc --noEmit` clean, `npx jest --no-coverage` clean
</success_criteria>

<output>
À la fin du Plan : créer `.planning/phases/52-stories-pipeline-d-valuation-auto-re-roll-qualit/52-02-SUMMARY.md` avec :
- Décision Option A vs Option B sur le refactor `buildStoryFromResponse`
- Nom exact du paramètre ajouté à `generateBedtimeStory` pour le re-roll prompt (si nécessaire)
- Tests roundtrip count (combien de scenarios couverts)
- Note : flag toujours off — Plan 52-03 active la pipeline LLM-judge, Plan 52-04 fournira le toggle utilisateur
</output>
