---
phase: 52-stories-pipeline-d-valuation-auto-re-roll-qualit
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - lib/eval/llm-eval.ts
  - lib/eval/prompts.ts
  - lib/eval/__tests__/llm-eval.test.ts
  - components/stories/QualityBadge.tsx
  - app/(tabs)/stories.tsx
  - package.json
autonomous: true
requirements:
  - EVAL-05
  - EVAL-06
must_haves:
  truths:
    - "Après saveStory(), un appel async fire-and-forget à evaluateStoryWithLlm() est déclenché (Claude Haiku 4.5, temp 0, max_tokens 300)"
    - "Le LLM-judge retourne 4 sous-scores 0-10 + justification ≤ 280 chars, validés par Zod"
    - "JSON malformé ⇒ 1 retry avec prompt durci ; 2ème échec ⇒ fallback neutre 5/10 (pas d'exception remontée)"
    - "Le coût par story ≤ $0.005 (cap explicite max_tokens=300, model=claude-haiku-4-5-20251001)"
    - "QualityBadge affiche couleur verte (score ≥ 7), ambre (4-7), rouge (< 4) selon quality_score ; tap = modal listant quality_issues + llm_judge.justification"
    - "Texte envoyé au LLM-judge est anonymisé (prénom enfant → 'Enfant') avant envoi"
    - "Si flag off ou story.llm_judge déjà rempli ⇒ skip appel LLM (idempotence G7)"
  artifacts:
    - path: "lib/eval/llm-eval.ts"
      provides: "evaluateStoryWithLlm(story, child, config) → Promise<LlmEvalResponse>"
      contains: "claude-haiku-4-5-20251001"
    - path: "lib/eval/prompts.ts"
      provides: "LLM_EVAL_SYSTEM_PROMPT, LLM_EVAL_RETRY_PROMPT (FR, few-shot 2 exemples)"
    - path: "components/stories/QualityBadge.tsx"
      provides: "Composant React badge couleur + modal détails"
      contains: "export function QualityBadge"
  key_links:
    - from: "app/(tabs)/stories.tsx (post-saveStory)"
      to: "lib/eval/llm-eval.ts"
      via: "evaluateStoryWithLlm(story, child, aiConfig).then(judge => saveStory({...story, llm_judge: judge}))"
      pattern: "evaluateStoryWithLlm\\("
    - from: "components/stories/StoryBookCard.tsx ou StoryCard"
      to: "components/stories/QualityBadge.tsx"
      via: "<QualityBadge story={story} />"
      pattern: "QualityBadge"
---

<objective>
Compléter la pipeline avec la couche qualitative subjective : LLM-judge async (Claude Haiku 4.5, temp 0, JSON+Zod, fallback neutre), affichage UI badge couleur + modal détails dans la liste stories. Strictement async fire-and-forget post-save : zéro impact sur l'UX bedtime critique. i18n FR sur toutes les chaînes user-facing.

Purpose: Donner un signal qualitatif (rythme, originalité, charge émotionnelle, fluidité) qui complète le rubric déterministe sans le bloquer. Le parent voit en un coup d'œil dans la liste si une histoire est forte/moyenne/faible.
Output: LLM-judge opérationnel, badge dans la bibliothèque stories, justification visible au tap.
</objective>

<execution_context>
@.claude/CLAUDE.md
@.planning/phases/52-stories-pipeline-d-valuation-auto-re-roll-qualit/52-AI-SPEC.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/52-stories-pipeline-d-valuation-auto-re-roll-qualit/52-AI-SPEC.md
@.planning/phases/52-stories-pipeline-d-valuation-auto-re-roll-qualit/52-01-SUMMARY.md
@.planning/phases/52-stories-pipeline-d-valuation-auto-re-roll-qualit/52-02-SUMMARY.md
@lib/types.ts
@lib/ai-service.ts
@lib/eval/feature-flag.ts
@lib/eval/rubric-helpers.ts
@app/(tabs)/stories.tsx
@components/stories/StoryBookCard.tsx
@contexts/ThemeContext.tsx

<interfaces>
<!-- Du Plan 52-02 -->
From lib/types.ts (étendu Plan 52-02) :
```typescript
interface BedtimeStory {
  // ...
  llm_judge?: {
    rythme: number;
    originalite: number;
    charge_emotionnelle: number;
    fluidite: number;
    justification: string;
    evaluated_at: string;
  } | null;
  quality_score?: number;
  quality_issues?: string[];
}
```

From lib/ai-service.ts:409-423 — pattern direct fetch Anthropic :
```typescript
const r = await fetch('https://api.anthropic.com/v1/messages', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
    'x-api-key': config.apiKey,
    'anthropic-version': '2023-06-01',
    'anthropic-dangerous-direct-browser-access': 'true',
  },
  body: JSON.stringify({ model, max_tokens, temperature, system, messages }),
});
```

From lib/eval/rubric-helpers.ts (Plan 52-01) :
```typescript
export function anonymizeStoryText(text: string, child: Profile): string;
```

From AIConfig (lib/ai-service.ts) :
```typescript
interface AIConfig { apiKey: string; model: string; /* ... */ }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: LLM-judge async (Haiku 4.5 + Zod + fallback) + prompts FR</name>
  <files>lib/eval/llm-eval.ts, lib/eval/prompts.ts, lib/eval/__tests__/llm-eval.test.ts, package.json</files>
  <read_first>
    - .planning/phases/52-stories-pipeline-d-valuation-auto-re-roll-qualit/52-AI-SPEC.md (Section 4 lignes 405-493 — code complet llm-eval.ts ; Section 4b.3 prompt engineering ; Section 5 D9-D10 critères ; Section 6 G7 idempotence)
    - lib/ai-service.ts (lignes 371-444 — pattern direct fetch Anthropic à calquer)
    - lib/eval/rubric-helpers.ts (anonymizeStoryText)
    - lib/eval/feature-flag.ts (isEvalEnabled)
    - lib/anonymizer.ts (pattern existant pour confirmer FR strings)
  </read_first>
  <behavior>
    - Test : evaluateStoryWithLlm() avec mock fetch retournant `{"rythme":8,"originalite":7,"charge_emotionnelle":9,"fluidite":9,"justification":"Arc fluide."}` ⇒ retourne objet validé Zod
    - Test : mock fetch retourne JSON avec markdown fences ` ```json ... ``` ` ⇒ strip + parse OK
    - Test : 1ère réponse malformée + 2ème valide ⇒ retourne 2ème (1 retry max)
    - Test : 1ère + 2ème malformées ⇒ retourne FALLBACK_NEUTRAL `{rythme:5,...,justification:"Score neutre — validation JSON échouée, fallback automatique."}` (jamais d'exception)
    - Test : fetch throw HTTP 500 ⇒ catch + fallback neutre, pas d'exception
    - Test : texte envoyé contient "Enfant" pas le prénom réel (anonymisation appliquée avant envoi)
    - Test : body.model === 'claude-haiku-4-5-20251001', body.max_tokens === 300, body.temperature === 0
    - Test : LLM_EVAL_SYSTEM_PROMPT contient les 4 dimensions en FR + barème + format JSON strict + 2 few-shot exemples
  </behavior>
  <action>
1. **Installer Zod** :
```bash
npm install zod
```
Vérifier dans package.json que `"zod"` est ajouté en dependencies (pas devDependencies — utilisé en runtime).

2. **Compléter `lib/eval/prompts.ts`** :

```typescript
// lib/eval/prompts.ts — Phase 52
// FR strict : tout output user-facing du LLM-judge est en français.

export const REROLL_INSTRUCTIONS_HEADER =
  '\n\n## Instructions de correction (re-roll Phase 52)\n\nLa version précédente présentait ces problèmes — corrige-les impérativement :\n';

export const LLM_EVAL_SYSTEM_PROMPT = `Tu es un évaluateur littéraire d'histoires du soir pour enfants 3-6 ans (canon École des Loisirs / Flammarion Jeunesse).

Tu évalues UNIQUEMENT le texte qui suit, sur 4 dimensions, en FRANÇAIS.

DIMENSIONS (note chaque dimension de 0 à 10) :
- "rythme" : fluidité de la lecture à voix haute, longueur des phrases adaptée 3-6 ans (8-12 mots), respiration ponctuation. 10 = parfait pour rituel coucher.
- "originalite" : ressort narratif, image, vocabulaire renouvelés vs clichés (ex: "tout doucement, tout doucement", "un pas, deux pas, trois pas"). 10 = histoire qu'on n'a pas envie de zapper.
- "charge_emotionnelle" : capacité à toucher l'enfant sans l'agiter (figure d'attachement présente, fin paisible, image marquante). 10 = câlin texte.
- "fluidite" : absence de typos lexicales / corruption morphosyntaxique ("papates" au lieu de "patates", "museau dessert"). 10 = aucun frottement à la lecture.

JUSTIFICATION : 1 phrase ≤ 280 caractères, FR, factuelle, sans juger l'enfant ni les parents.

FORMAT — RÉPONDS UNIQUEMENT PAR UN OBJET JSON VALIDE, AUCUN TEXTE AVANT OU APRÈS, AUCUN BACKTICK :
{"rythme":N,"originalite":N,"charge_emotionnelle":N,"fluidite":N,"justification":"..."}

EXEMPLES :

Histoire forte (notation cible 8-9) :
"Lina ferma les yeux. La forêt chuchotait sa berceuse, et l'étoile bleue posa sa lumière douce sur son cœur. Tout était paisible. Bonne nuit, petite princesse."
→ {"rythme":9,"originalite":8,"charge_emotionnelle":9,"fluidite":10,"justification":"Arc descendant fluide, image marquante de l'étoile sur le cœur, fin claire."}

Histoire faible (notation cible 2-4) :
"Tom marchait. Un pas. Deux pas. Trois pas. Tout doucement, tout doucement. Il marchait encore. Un pas. Deux pas. Et soudain, un monstre apparut !"
→ {"rythme":3,"originalite":2,"charge_emotionnelle":2,"fluidite":7,"justification":"Formules en boucle (clichés P2), fin agitée avec exclamation et apparition de monstre."}`;

export const LLM_EVAL_RETRY_PROMPT = `Ta réponse précédente n'était pas du JSON valide. Réessaie strictement, sans préambule, sans fences markdown. Format exact attendu :
{"rythme":N,"originalite":N,"charge_emotionnelle":N,"fluidite":N,"justification":"..."}`;
```

3. **Créer `lib/eval/llm-eval.ts`** — calquer AI-SPEC Section 4 lignes 405-493 :

```typescript
import { z } from 'zod';
import type { BedtimeStory, Profile } from '../types';
import type { AIConfig } from '../ai-service';
import { LLM_EVAL_SYSTEM_PROMPT, LLM_EVAL_RETRY_PROMPT } from './prompts';
import { anonymizeStoryText } from './rubric-helpers';
import { isEvalEnabled } from './feature-flag';

export const LlmEvalSchema = z.object({
  rythme: z.number().int().min(0).max(10),
  originalite: z.number().int().min(0).max(10),
  charge_emotionnelle: z.number().int().min(0).max(10),
  fluidite: z.number().int().min(0).max(10),
  justification: z.string().min(1).max(280),
});
export type LlmEvalResponse = z.infer<typeof LlmEvalSchema>;

const HAIKU_MODEL = 'claude-haiku-4-5-20251001';
const MAX_TOKENS = 300; // cap coût ≤ $0.005/story (AI-SPEC §595)

const FALLBACK_NEUTRAL: LlmEvalResponse = {
  rythme: 5,
  originalite: 5,
  charge_emotionnelle: 5,
  fluidite: 5,
  justification: 'Score neutre — validation JSON échouée, fallback automatique.',
};

/**
 * LLM-judge async fire-and-forget.
 * - Si flag off ⇒ retourne null (skip).
 * - Anonymise le texte avant envoi (privacy).
 * - 1 retry max sur JSON invalide.
 * - Jamais d'exception remontée — fallback neutre 5/10.
 */
export async function evaluateStoryWithLlm(
  story: BedtimeStory,
  child: Profile,
  config: AIConfig,
): Promise<LlmEvalResponse | null> {
  if (!isEvalEnabled()) return null;
  // Idempotence G7 : skip si déjà jugée
  if (story.llm_judge) return null;

  const safeText = anonymizeStoryText(story.texte, child);
  const userMsg = `Tranche d'âge: ${story.trancheAge ?? 'non précisée'}\n\nHistoire:\n${safeText}`;

  try {
    const first = await callJudge(config, LLM_EVAL_SYSTEM_PROMPT, userMsg);
    const parsed = tryParse(first);
    if (parsed.success) return parsed.data;

    if (__DEV__) console.warn('[eval] JSON invalide, retry durci:', parsed.error);
    const second = await callJudge(
      config,
      `${LLM_EVAL_SYSTEM_PROMPT}\n\n${LLM_EVAL_RETRY_PROMPT}`,
      userMsg,
    );
    const reparsed = tryParse(second);
    if (reparsed.success) return reparsed.data;

    if (__DEV__) console.warn('[eval] LLM-judge fallback neutre 5/10');
    return FALLBACK_NEUTRAL;
  } catch (e) {
    if (__DEV__) console.warn('[eval] LLM-judge fetch failed, fallback neutre:', e);
    return FALLBACK_NEUTRAL;
  }
}

async function callJudge(config: AIConfig, system: string, user: string): Promise<string> {
  const r = await fetch('https://api.anthropic.com/v1/messages', {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json',
      'x-api-key': config.apiKey,
      'anthropic-version': '2023-06-01',
      'anthropic-dangerous-direct-browser-access': 'true',
    },
    body: JSON.stringify({
      model: HAIKU_MODEL,
      max_tokens: MAX_TOKENS,
      temperature: 0,
      system,
      messages: [{ role: 'user', content: user }],
    }),
  });
  if (!r.ok) throw new Error(`Haiku ${r.status}`);
  const data = await r.json();
  return data.content?.[0]?.text ?? '';
}

function tryParse(
  raw: string,
): { success: true; data: LlmEvalResponse } | { success: false; error: string } {
  const cleaned = raw.replace(/^```json\s*|\s*```$/g, '').trim();
  try {
    const json = JSON.parse(cleaned);
    const result = LlmEvalSchema.safeParse(json);
    return result.success
      ? { success: true, data: result.data }
      : { success: false, error: result.error.message };
  } catch (e: any) {
    return { success: false, error: e?.message ?? 'JSON.parse failed' };
  }
}
```

4. **Créer `lib/eval/__tests__/llm-eval.test.ts`** avec mocks `global.fetch` (pas de vrai appel réseau) :

```typescript
import { evaluateStoryWithLlm } from '../llm-eval';
import { isEvalEnabled } from '../feature-flag';

jest.mock('../feature-flag', () => ({ isEvalEnabled: jest.fn(() => true) }));

const mockFetch = (response: string, ok = true) => {
  (global as any).fetch = jest.fn(async () => ({
    ok,
    json: async () => ({ content: [{ type: 'text', text: response }] }),
  }));
};

const baseStory = { id: '1', texte: 'Histoire test.', llm_judge: undefined, trancheAge: '3-5' } as any;
const baseChild = { id: 'c1', prenom: 'Enfant 1' } as any;
const cfg = { apiKey: 'test', model: 'sonnet' } as any;

describe('llm-eval (EVAL-05)', () => {
  it('parse JSON valide', async () => {
    mockFetch('{"rythme":8,"originalite":7,"charge_emotionnelle":9,"fluidite":9,"justification":"OK."}');
    const r = await evaluateStoryWithLlm(baseStory, baseChild, cfg);
    expect(r).toEqual({ rythme: 8, originalite: 7, charge_emotionnelle: 9, fluidite: 9, justification: 'OK.' });
  });

  it('strip markdown fences', async () => {
    mockFetch('```json\n{"rythme":5,"originalite":5,"charge_emotionnelle":5,"fluidite":5,"justification":"x"}\n```');
    const r = await evaluateStoryWithLlm(baseStory, baseChild, cfg);
    expect(r?.rythme).toBe(5);
  });

  it('fallback neutre sur 2 échecs JSON', async () => {
    let n = 0;
    (global as any).fetch = jest.fn(async () => ({
      ok: true,
      json: async () => ({ content: [{ type: 'text', text: 'pas du json' + (++n) }] }),
    }));
    const r = await evaluateStoryWithLlm(baseStory, baseChild, cfg);
    expect(r?.rythme).toBe(5);
    expect(r?.justification).toMatch(/Score neutre/);
  });

  it('fallback neutre sur fetch error', async () => {
    (global as any).fetch = jest.fn(async () => { throw new Error('network'); });
    const r = await evaluateStoryWithLlm(baseStory, baseChild, cfg);
    expect(r?.rythme).toBe(5);
  });

  it('skip si flag off (G7)', async () => {
    (isEvalEnabled as jest.Mock).mockReturnValueOnce(false);
    const r = await evaluateStoryWithLlm(baseStory, baseChild, cfg);
    expect(r).toBeNull();
  });

  it('skip si llm_judge déjà rempli (idempotence)', async () => {
    const story = { ...baseStory, llm_judge: { rythme: 8 } };
    const r = await evaluateStoryWithLlm(story, baseChild, cfg);
    expect(r).toBeNull();
  });

  it('utilise model claude-haiku-4-5-20251001 et max_tokens 300', async () => {
    mockFetch('{"rythme":5,"originalite":5,"charge_emotionnelle":5,"fluidite":5,"justification":"x"}');
    await evaluateStoryWithLlm(baseStory, baseChild, cfg);
    const callBody = JSON.parse(((global as any).fetch as jest.Mock).mock.calls[0][1].body);
    expect(callBody.model).toBe('claude-haiku-4-5-20251001');
    expect(callBody.max_tokens).toBe(300);
    expect(callBody.temperature).toBe(0);
  });

  it('anonymise le prénom enfant avant envoi', async () => {
    mockFetch('{"rythme":5,"originalite":5,"charge_emotionnelle":5,"fluidite":5,"justification":"x"}');
    const story = { ...baseStory, texte: 'Lucas marchait.' };
    const child = { ...baseChild, prenom: 'Lucas' };
    await evaluateStoryWithLlm(story, child, cfg);
    const callBody = JSON.parse(((global as any).fetch as jest.Mock).mock.calls[0][1].body);
    expect(callBody.messages[0].content).not.toMatch(/Lucas/);
    expect(callBody.messages[0].content).toMatch(/Enfant/);
  });
});
```
  </action>
  <verify>
    <automated>npx jest lib/eval/__tests__/llm-eval.test.ts --no-coverage</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '"zod"' package.json`
    - `test -f node_modules/zod/package.json` (installé)
    - `test -f lib/eval/llm-eval.ts`
    - `grep -q "claude-haiku-4-5-20251001" lib/eval/llm-eval.ts`
    - `grep -q "max_tokens: 300" lib/eval/llm-eval.ts`
    - `grep -q "temperature: 0" lib/eval/llm-eval.ts`
    - `grep -q "FALLBACK_NEUTRAL" lib/eval/llm-eval.ts`
    - `grep -q "LlmEvalSchema" lib/eval/llm-eval.ts`
    - `grep -q "anonymizeStoryText" lib/eval/llm-eval.ts`
    - `grep -q "rythme" lib/eval/prompts.ts && grep -q "charge_emotionnelle" lib/eval/prompts.ts`
    - `grep -v '^#' lib/eval/prompts.ts | grep -c "EXEMPLES" -ge 1` (few-shot présent)
    - `npx jest lib/eval/__tests__/llm-eval.test.ts --no-coverage` exit 0 (tous les 8 scenarios)
    - `npx tsc --noEmit` clean
  </acceptance_criteria>
  <done>LLM-judge fonctionnel offline (mocks), prompts FR avec 2 few-shot, Zod validation, fallback neutre, anonymisation testée.</done>
</task>

<task type="auto">
  <name>Task 2: UI badge qualité + branchement async post-saveStory</name>
  <files>components/stories/QualityBadge.tsx, app/(tabs)/stories.tsx</files>
  <read_first>
    - components/stories/StoryBookCard.tsx (style cards existant — pour intégration badge)
    - contexts/ThemeContext.tsx (useThemeColors — couleurs vert/ambre/rouge via theme)
    - components/ui/Badge.tsx (composant Badge existant si présent — réutiliser pattern)
    - app/(tabs)/stories.tsx (lignes 2050-2080 — flow post-saveStory, où injecter le LLM call)
    - lib/eval/llm-eval.ts (signature evaluateStoryWithLlm)
    - lib/eval/feature-flag.ts (isEvalEnabled)
    - .planning/phases/52-stories-pipeline-d-valuation-auto-re-roll-qualit/52-AI-SPEC.md (Section 4b ligne 551-557 — JAMAIS await sur le chemin critique)
    - CLAUDE.md (couleurs via useThemeColors, modal pageSheet drag-to-dismiss)
  </read_first>
  <action>
1. **Créer `components/stories/QualityBadge.tsx`** — composant badge + modal détails :

```typescript
import React, { useState } from 'react';
import { Pressable, View, Text, Modal, StyleSheet, ScrollView } from 'react-native';
import { useThemeColors } from '../../contexts/ThemeContext';
import { Spacing } from '../../constants/spacing';
import * as Haptics from 'expo-haptics';
import type { BedtimeStory } from '../../lib/types';
import { isEvalEnabled } from '../../lib/eval/feature-flag';

interface QualityBadgeProps {
  story: BedtimeStory;
  size?: 'sm' | 'md';
}

/**
 * Phase 52 — Badge couleur selon quality_score (EVAL-06).
 * - vert : score ≥ 7
 * - ambre : 4-7
 * - rouge : < 4
 * Tap = modal listant quality_issues + llm_judge.justification.
 * Si flag off OU score absent ⇒ ne rend rien (null).
 */
export function QualityBadge({ story, size = 'sm' }: QualityBadgeProps) {
  const colors = useThemeColors();
  const [open, setOpen] = useState(false);

  if (!isEvalEnabled()) return null;
  if (story.quality_score === undefined) return null;

  const score = story.quality_score;
  const tier: 'vert' | 'ambre' | 'rouge' = score >= 7 ? 'vert' : score >= 4 ? 'ambre' : 'rouge';
  const bg = tier === 'vert' ? colors.colors.success : tier === 'ambre' ? colors.colors.warning : colors.colors.danger;
  const label = `${score.toFixed(1)}/10`;
  const dot = size === 'sm' ? 8 : 12;

  return (
    <>
      <Pressable
        onPress={() => { Haptics.selectionAsync(); setOpen(true); }}
        accessibilityRole="button"
        accessibilityLabel={`Score qualité ${label}, voir détails`}
        style={[styles.badge, { backgroundColor: bg }]}
      >
        <View style={{ width: dot, height: dot, borderRadius: dot / 2, backgroundColor: '#fff', marginRight: 4 }} />
        <Text style={[styles.label, { fontSize: size === 'sm' ? 11 : 13 }]}>{label}</Text>
      </Pressable>

      <Modal visible={open} animationType="slide" presentationStyle="pageSheet" onRequestClose={() => setOpen(false)}>
        <ScrollView style={{ flex: 1, backgroundColor: colors.colors.background, padding: Spacing['2xl'] }}>
          <Text style={[styles.modalTitle, { color: colors.colors.text }]}>Qualité de l'histoire</Text>
          <Text style={[styles.modalScore, { color: bg }]}>{label}</Text>

          {story.quality_issues && story.quality_issues.length > 0 && (
            <>
              <Text style={[styles.section, { color: colors.colors.text }]}>Avertissements</Text>
              {story.quality_issues.map((issue, i) => (
                <Text key={i} style={[styles.issue, { color: colors.colors.textMuted }]}>• {issue}</Text>
              ))}
            </>
          )}

          {story.llm_judge && (
            <>
              <Text style={[styles.section, { color: colors.colors.text }]}>Évaluation narrative</Text>
              <Text style={[styles.dim, { color: colors.colors.textMuted }]}>Rythme : {story.llm_judge.rythme}/10</Text>
              <Text style={[styles.dim, { color: colors.colors.textMuted }]}>Originalité : {story.llm_judge.originalite}/10</Text>
              <Text style={[styles.dim, { color: colors.colors.textMuted }]}>Émotion : {story.llm_judge.charge_emotionnelle}/10</Text>
              <Text style={[styles.dim, { color: colors.colors.textMuted }]}>Fluidité : {story.llm_judge.fluidite}/10</Text>
              <Text style={[styles.justification, { color: colors.colors.text }]}>« {story.llm_judge.justification} »</Text>
            </>
          )}

          {story.quality_retried && (
            <Text style={[styles.retry, { color: colors.colors.textMuted }]}>
              ℹ Cette histoire a été régénérée automatiquement pour améliorer sa qualité.
            </Text>
          )}

          <Pressable onPress={() => setOpen(false)} style={[styles.closeBtn, { backgroundColor: colors.colors.primary }]}>
            <Text style={styles.closeLabel}>Fermer</Text>
          </Pressable>
        </ScrollView>
      </Modal>
    </>
  );
}

const styles = StyleSheet.create({
  badge: { flexDirection: 'row', alignItems: 'center', paddingHorizontal: 8, paddingVertical: 3, borderRadius: 12 },
  label: { color: '#fff', fontWeight: '600' },
  modalTitle: { fontSize: 22, fontWeight: '700', marginBottom: Spacing.lg },
  modalScore: { fontSize: 48, fontWeight: '800', marginBottom: Spacing.xl },
  section: { fontSize: 16, fontWeight: '600', marginTop: Spacing.lg, marginBottom: Spacing.sm },
  issue: { fontSize: 14, lineHeight: 20, marginBottom: 4 },
  dim: { fontSize: 14, marginBottom: 4 },
  justification: { fontSize: 14, fontStyle: 'italic', marginTop: Spacing.md },
  retry: { fontSize: 13, marginTop: Spacing.lg },
  closeBtn: { marginTop: Spacing.xl, padding: Spacing.lg, borderRadius: 12, alignItems: 'center' },
  closeLabel: { color: '#fff', fontWeight: '600', fontSize: 16 },
});
```

⚠ Vérifier le nom EXACT du champ `useThemeColors()` qui contient les couleurs success/warning/danger — adapter (ex: `colors.colors.success` vs `colors.success`). Lire ContextThemeContext.tsx pour confirmer.

2. **Brancher LLM-judge async post-saveStory dans `app/(tabs)/stories.tsx`** :

   - Importer en haut :
     ```typescript
     import { evaluateStoryWithLlm } from '../../lib/eval/llm-eval';
     ```

   - Localiser le bloc `saveStory(evalResult.story).catch(...)` ajouté Plan 52-02 (vers ligne ~2058).
   - **Juste après**, ajouter (fire-and-forget — JAMAIS await sur le chemin critique, AI-SPEC §551) :

     ```typescript
     // Phase 52 — LLM-judge async post-show (EVAL-05). NE JAMAIS await — bedtime UX critique.
     // Idempotent : evaluateStoryWithLlm skip si flag off OU si story.llm_judge déjà rempli (G7).
     evaluateStoryWithLlm(evalResult.story, enfant, aiConfig)
       .then((judge) => {
         if (!judge) return;
         const enriched = { ...evalResult.story, llm_judge: { ...judge, evaluated_at: new Date().toISOString() } };
         saveStory(enriched).catch(() => { /* non-critique — frontmatter écriture peut échouer offline */ });
       })
       .catch((e) => { if (__DEV__) console.warn('[eval] LLM-judge background failed:', e); });
     ```

3. **Intégrer `<QualityBadge>` dans la liste stories** :
   - Localiser `StoryCard` (composant memoïsé ligne 216 de stories.tsx) ou `StoryBookCard` (components/stories/StoryBookCard.tsx).
   - Ajouter `<QualityBadge story={story} size="sm" />` dans le coin top-right de la card (ou à côté du titre, choisir un emplacement non-intrusif).
   - Importer : `import { QualityBadge } from '../../components/stories/QualityBadge';`

4. **Vérifier non-régression** :
   - `npx tsc --noEmit` clean
   - `npx jest --no-coverage` clean
   - Si flag off : `<QualityBadge>` retourne null donc l'UI est strictement identique baseline (EVAL-07).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "QualityBadge|llm-eval" | grep -c "error" | (read n; test "$n" = "0") && npx jest --no-coverage 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `test -f components/stories/QualityBadge.tsx`
    - `grep -q 'export function QualityBadge' components/stories/QualityBadge.tsx`
    - `grep -q 'useThemeColors' components/stories/QualityBadge.tsx` (couleurs theme, pas hardcoded)
    - `grep -q 'isEvalEnabled' components/stories/QualityBadge.tsx` (early return flag off)
    - `grep -q 'pageSheet' components/stories/QualityBadge.tsx` (modal pattern projet)
    - `grep -q 'Haptics' components/stories/QualityBadge.tsx`
    - `grep -q 'evaluateStoryWithLlm' app/\(tabs\)/stories.tsx`
    - `grep -q 'QualityBadge' app/\(tabs\)/stories.tsx components/stories/StoryBookCard.tsx`
    - Vérifier qu'aucun `await evaluateStoryWithLlm` n'existe : `! grep -E "await\s+evaluateStoryWithLlm" app/\(tabs\)/stories.tsx components/stories/`
    - `npx tsc --noEmit` clean
    - Toutes chaînes user-facing en FR (`grep -E "\"[A-Z][a-z]+ " components/stories/QualityBadge.tsx` retourne uniquement du FR : "Qualité", "Avertissements", "Fermer", "Évaluation narrative")
  </acceptance_criteria>
  <done>UI badge fonctionnel, modal détails FR, branchement async fire-and-forget vérifié, zéro await sur le chemin critique.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client RN ↔ Anthropic API (Haiku) | Nouveau call LLM-judge — surface réseau additionnelle |
| Texte story (potentiellement avec PII) ↔ LLM-judge prompt | Anonymisation OBLIGATOIRE avant envoi |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-03-01 | Information Disclosure | Texte story envoyé à Anthropic | mitigate | `anonymizeStoryText(story.texte, child)` appelé OBLIGATOIREMENT avant envoi (test unitaire dédié vérifie absence prénom). Même politique que `lib/anonymizer.ts:333` existant. |
| T-52-03-02 | Tampering / Prompt Injection | Texte story injecté dans user message au LLM-judge | mitigate | Le LLM-judge a un system prompt strict ("évalue UNIQUEMENT le texte"), réponse cappée 300 tokens, JSON Zod validé. Si l'histoire contient du texte malicieux qui tente de détourner le judge, output reste contraint au schema (rythme/originalite/charge_emotionnelle/fluidite/justification). Pas d'effet de bord (pas de tool calling). |
| T-52-03-03 | Denial of Service / Cost | Boucle d'appels Haiku | mitigate | Cap dur via idempotence G7 (`if story.llm_judge return null`) + cap retry 1 dans tryParse + fallback neutre sans 3ème appel. AI-SPEC §595 : coût annuel borné ~$2.34/famille. Test unitaire vérifie absence d'appel quand `llm_judge` déjà rempli. |
| T-52-03-04 | Spoofing | Réponse Haiku falsifiée | accept | Connection HTTPS + clé API custodial. Pas de signature additionnelle. Risque limité à un MITM résidentiel — out of scope. |
| T-52-03-05 | Repudiation | Modal QualityBadge donne fausse confiance | accept | Justification LLM est explicitement "évaluation narrative" pas une garantie ; parent reste seul juge. AI-SPEC §745 : "Le parent regarde le dashboard manuellement". |
</threat_model>

<verification>
- `npx jest lib/eval/__tests__/llm-eval.test.ts --no-coverage` exit 0
- `npx tsc --noEmit` clean
- `grep -E "await\s+evaluateStoryWithLlm" app/(tabs)/stories.tsx` retourne vide (jamais await sur chemin critique)
- `<QualityBadge>` retourne null sous flag off (test manuel : flag off → rebuild → app identique baseline)
- Cost cap : `max_tokens: 300` + `temperature: 0` + `model: 'claude-haiku-4-5-20251001'` présents dans body
- FR strict : aucune chaîne EN dans QualityBadge ni prompts.ts (`grep -iE "\\bthe\\b|\\bplease\\b|\\berror\\b" components/stories/QualityBadge.tsx lib/eval/prompts.ts` retourne vide)
</verification>

<success_criteria>
- LLM-judge async opérationnel avec Zod + fallback (EVAL-05)
- Coût ≤ $0.005/story (max_tokens=300 vérifié) (EVAL-05)
- Anonymisation pré-LLM testée
- Badge qualité couleur vert/ambre/rouge dans la liste (EVAL-06)
- Modal détails FR avec issues + 4 sous-scores + justification
- Aucun await sur chemin critique bedtime (UX préservée)
- Flag off ⇒ badge invisible, zéro appel LLM (EVAL-07)
</success_criteria>

<output>
À la fin du Plan : créer `.planning/phases/52-stories-pipeline-d-valuation-auto-re-roll-qualit/52-03-SUMMARY.md` avec :
- Confirmation Zod ajouté à package.json (version installée)
- Emplacement choisi pour le badge dans la card (StoryCard inline / StoryBookCard / autre)
- Nombre de scenarios couverts par llm-eval.test.ts
- Test calibration LLM-judge (5 stories du golden set × 3 runs = 15 mesures, corrélation Spearman avec verdict humain) — peut être fait offline avec une clé API valide en script dev (optionnel pour Plan 52-04)
</output>
