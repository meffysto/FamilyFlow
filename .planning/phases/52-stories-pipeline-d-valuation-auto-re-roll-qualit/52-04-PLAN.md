---
phase: 52-stories-pipeline-d-valuation-auto-re-roll-qualit
plan: 04
type: execute
wave: 4
depends_on: [01, 02, 03]
files_modified:
  - lib/eval/feature-flag.ts
  - lib/eval/__tests__/non-regression-baseline.test.ts
  - scripts/eval-dashboard.ts
  - CLAUDE.md
  - .planning/phases/52-stories-pipeline-d-valuation-auto-re-roll-qualit/52-OVERVIEW.md
autonomous: true
requirements:
  - EVAL-04
  - EVAL-07
  - EVAL-08
must_haves:
  truths:
    - "Test de non-régression du golden set passe en CI : 14/20 flagged ±1, distribution par dimension stable vs baseline"
    - "Toggle feature flag exposé en settings dev (ou variable env documentée) — parent peut activer/désactiver Phase 52 sans rebuild"
    - "Script scripts/eval-dashboard.ts parse tous les frontmatters stories du vault et imprime distribution flagged + tendances 30j"
    - "CLAUDE.md mis à jour : section Architecture mentionne lib/eval/, section Testing mentionne le golden set"
    - "52-OVERVIEW.md récapitule la phase complète : ce qui a été livré, comment activer, comment auditer"
  artifacts:
    - path: "lib/eval/__tests__/non-regression-baseline.test.ts"
      provides: "Test CI gate sur distribution golden set (F1 AI-SPEC §739)"
    - path: "scripts/eval-dashboard.ts"
      provides: "Dashboard manuel — parse vault, imprime stats"
      contains: "Total stories"
    - path: ".planning/phases/52-stories-pipeline-d-valuation-auto-re-roll-qualit/52-OVERVIEW.md"
      provides: "Récap phase pour future reprise"
  key_links:
    - from: "scripts/eval-dashboard.ts"
      to: "lib/parser.ts (parseBedtimeStory)"
      via: "import + glob sur 09 - Histoires/"
      pattern: "parseBedtimeStory"
---

<objective>
Verrouiller la non-régression de Phase 52 et finaliser la livraison : test CI gate sur le golden set (bloque toute PR qui dégrade la distribution flagged), script dashboard pour auditer la prod, exposition du feature flag à l'utilisateur (settings dev ou variable runtime), mise à jour de la documentation projet.

Purpose: Sortir Phase 52 de l'état "infrastructure prête mais flag off" vers "feature livrable, auditable, et résistante à la régression". Le parent peut activer le pipeline en confiance.
Output: CI gate + dashboard + flag toggle + docs.
</objective>

<execution_context>
@.claude/CLAUDE.md
@.planning/phases/52-stories-pipeline-d-valuation-auto-re-roll-qualit/52-AI-SPEC.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/52-stories-pipeline-d-valuation-auto-re-roll-qualit/52-AI-SPEC.md
@.planning/phases/52-stories-pipeline-d-valuation-auto-re-roll-qualit/52-01-SUMMARY.md
@.planning/phases/52-stories-pipeline-d-valuation-auto-re-roll-qualit/52-02-SUMMARY.md
@.planning/phases/52-stories-pipeline-d-valuation-auto-re-roll-qualit/52-03-SUMMARY.md
@CLAUDE.md
@lib/eval/feature-flag.ts
@lib/eval/rubric.ts
@lib/parser.ts
@lib/__tests__/flag.test.ts

<interfaces>
<!-- Tous artefacts Plan 52-01/02/03 disponibles -->

From lib/eval/feature-flag.ts (Plan 52-01) :
```typescript
export const FEATURE_EVAL_ENABLED: boolean; // false par défaut
export function isEvalEnabled(): boolean;
```

From lib/eval/__tests__/fixtures/golden-set.json (Plan 52-01) : 20 fixtures
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Test non-régression CI gate + toggle feature flag exposable</name>
  <files>lib/eval/feature-flag.ts, lib/eval/__tests__/non-regression-baseline.test.ts</files>
  <read_first>
    - lib/eval/feature-flag.ts (Plan 52-01)
    - lib/eval/rubric.ts (Plan 52-01)
    - lib/eval/__tests__/fixtures/golden-set.json
    - lib/__tests__/flag.test.ts (pattern test flag existant projet — référence style)
    - .planning/phases/52-stories-pipeline-d-valuation-auto-re-roll-qualit/52-AI-SPEC.md (Section 6 F1 = CI gate ; Section 7 Key Metrics)
  </read_first>
  <behavior>
    - Test : sur les 20 fixtures, distribution `flagged ∈ [13, 15]` (cible 14, tolérance ±1) — F1 AI-SPEC
    - Test : sur les 20 fixtures, `clean count ∈ [5, 7]` (cible 6, tolérance ±1)
    - Test : count hard fails sur dim D5 tags_tts ≥ 16 (cible 18, tolérance -2 — P1 baseline)
    - Test : aucun false positive — chaque fixture marquée 🟢 par humain ne doit PAS être hardFail (au plus soft warning)
    - Test : flag toggle via setEvalEnabledOverride() change le retour de isEvalEnabled() en mémoire (utile pour tests + settings dev)
  </behavior>
  <action>
1. **Étendre `lib/eval/feature-flag.ts`** avec un override runtime (pour settings dev / tests) :

```typescript
// lib/eval/feature-flag.ts — Phase 52
// Off par défaut : comportement strictement identique au commit baseline (EVAL-07).

const DEFAULT_FEATURE_EVAL_ENABLED = false;
let runtimeOverride: boolean | null = null;

/**
 * Active/désactive le pipeline eval en runtime (settings dev, tests, hot toggle).
 * Persistence : à brancher sur expo-secure-store dans une phase ultérieure si besoin parent UI.
 * Phase 52 livre uniquement l'override mémoire — toggle UI exposé via dev menu.
 */
export function setEvalEnabledOverride(enabled: boolean | null): void {
  runtimeOverride = enabled;
}

export function isEvalEnabled(): boolean {
  if (runtimeOverride !== null) return runtimeOverride;
  return DEFAULT_FEATURE_EVAL_ENABLED;
}

// Compat : conserver l'export de la constante pour le code qui aurait pu l'importer
export const FEATURE_EVAL_ENABLED = DEFAULT_FEATURE_EVAL_ENABLED;
```

⚠ Vérifier qu'aucun fichier Plan 52-01/02/03 n'utilise `FEATURE_EVAL_ENABLED` directement — tous doivent passer par `isEvalEnabled()`. Si ce n'est pas le cas, remplacer.

2. **Créer `lib/eval/__tests__/non-regression-baseline.test.ts`** — CI gate F1 :

```typescript
import goldenSet from './fixtures/golden-set.json';
import { evaluateStoryDeterministic } from '../rubric';
import { setEvalEnabledOverride } from '../feature-flag';

beforeAll(() => setEvalEnabledOverride(true));
afterAll(() => setEvalEnabledOverride(null));

describe('Phase 52 — Non-régression baseline (F1 CI gate, EVAL-04)', () => {
  it('distribution flagged ∈ [13, 15] (cible 14/20)', () => {
    let flagged = 0;
    for (const f of goldenSet) {
      const r = evaluateStoryDeterministic(f.story, f.child, f.recentStories);
      if (r.hardFail || r.softWarnings.length > 0) flagged++;
    }
    expect(flagged).toBeGreaterThanOrEqual(13);
    expect(flagged).toBeLessThanOrEqual(15);
  });

  it('distribution clean ∈ [5, 7] (cible 6/20)', () => {
    let clean = 0;
    for (const f of goldenSet) {
      const r = evaluateStoryDeterministic(f.story, f.child, f.recentStories);
      if (!r.hardFail && r.softWarnings.length === 0) clean++;
    }
    expect(clean).toBeGreaterThanOrEqual(5);
    expect(clean).toBeLessThanOrEqual(7);
  });

  it('aucun false positive — fixtures 🟢 ne doivent pas hardFail', () => {
    const greenFixtures = goldenSet.filter((f) => f.expected.verdict === '🟢');
    expect(greenFixtures.length).toBeGreaterThanOrEqual(5); // au moins 5 fixtures vertes dans le set
    for (const f of greenFixtures) {
      const r = evaluateStoryDeterministic(f.story, f.child, f.recentStories);
      expect(r.hardFail).toBe(false);
    }
  });

  it('P1 tags TTS détectés sur ≥ 16 fixtures (baseline 18/20)', () => {
    let tagsHardFail = 0;
    for (const f of goldenSet) {
      const r = evaluateStoryDeterministic(f.story, f.child, f.recentStories);
      const tagsDim = r.dimensions.find((d) => d.id === 'tags_tts');
      if (tagsDim?.score === 1) tagsHardFail++;
    }
    expect(tagsHardFail).toBeGreaterThanOrEqual(16);
  });
});
```

3. **Vérifier** que `npx jest lib/eval/__tests__/non-regression-baseline.test.ts` passe.

4. **Documenter le flag** : ajouter dans `lib/eval/feature-flag.ts` un commentaire en-tête expliquant comment le parent active la feature :

```typescript
// Activation Phase 52 (3 méthodes) :
// 1. Modifier DEFAULT_FEATURE_EVAL_ENABLED = true ici + rebuild app (recommandé production)
// 2. Appeler setEvalEnabledOverride(true) au boot dans app/_layout.tsx (test temporaire)
// 3. Brancher un toggle dans le dev menu (more.tsx) qui appelle setEvalEnabledOverride
```
  </action>
  <verify>
    <automated>npx jest lib/eval/__tests__/non-regression-baseline.test.ts --no-coverage</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'setEvalEnabledOverride' lib/eval/feature-flag.ts`
    - `grep -q 'runtimeOverride' lib/eval/feature-flag.ts`
    - `test -f lib/eval/__tests__/non-regression-baseline.test.ts`
    - `grep -v '^#' lib/eval/__tests__/non-regression-baseline.test.ts | grep -c "toBeGreaterThanOrEqual" -ge 4`
    - `npx jest lib/eval/__tests__/non-regression-baseline.test.ts --no-coverage` exit 0
    - `npx jest --no-coverage` exit 0 (l'ensemble du test suite passe — non-régression Plans 52-01/02/03)
    - `npx tsc --noEmit` clean
    - DEFAULT_FEATURE_EVAL_ENABLED reste à `false` par défaut (vérifier `grep -q 'DEFAULT_FEATURE_EVAL_ENABLED = false' lib/eval/feature-flag.ts`)
  </acceptance_criteria>
  <done>CI gate non-régression vert, override flag testable, baseline verrouillée à 14/20.</done>
</task>

<task type="auto">
  <name>Task 2: Script eval-dashboard + docs CLAUDE.md + 52-OVERVIEW.md</name>
  <files>scripts/eval-dashboard.ts, CLAUDE.md, .planning/phases/52-stories-pipeline-d-valuation-auto-re-roll-qualit/52-OVERVIEW.md</files>
  <read_first>
    - lib/parser.ts (parseBedtimeStory pour réutiliser le parser dans le script)
    - lib/vault.ts (VaultManager — mais le script doit pouvoir lire des .md sans VaultManager iCloud — utiliser fs direct)
    - .planning/phases/52-stories-pipeline-d-valuation-auto-re-roll-qualit/52-AI-SPEC.md (Section 7 ligne 800-825 — sortie attendue dashboard)
    - CLAUDE.md (sections Stack, Architecture, Testing — pour mise à jour)
    - .planning/phases/48-fondation-export-pdf-impressions/48-OVERVIEW.md ou autres OVERVIEW existants comme template
    - scripts/ existants pour le pattern (s'il y en a)
  </read_first>
  <action>
1. **Vérifier l'existence du dossier `scripts/`** :
```bash
ls scripts/ 2>/dev/null || mkdir scripts
```

2. **Créer `scripts/eval-dashboard.ts`** — script `tsx` standalone (pas Jest) :

```typescript
#!/usr/bin/env tsx
/**
 * Phase 52 — Dashboard manuel d'évaluation des histoires.
 * Usage : npx tsx scripts/eval-dashboard.ts [--vault <path>] [--since 30d]
 *
 * Parse tous les frontmatters stories du vault et imprime :
 * - Distribution clean / soft / hard fail
 * - Re-roll convergence rate
 * - Score moyen LLM-judge
 * - Top issues
 *
 * Pas de réseau, pas d'écriture — read-only.
 */
import * as fs from 'fs';
import * as path from 'path';
import { parseBedtimeStory } from '../lib/parser';
import type { BedtimeStory } from '../lib/types';

const argv = process.argv.slice(2);
const vaultArg = argv.indexOf('--vault');
const sinceArg = argv.indexOf('--since');
const vaultPath = vaultArg >= 0 ? argv[vaultArg + 1] : process.env.VAULT_PATH ?? './vault';
const sinceDays = sinceArg >= 0 ? parseInt(argv[sinceArg + 1].replace(/d$/, ''), 10) : 30;

const storiesDir = path.join(vaultPath, '09 - Histoires');
if (!fs.existsSync(storiesDir)) {
  console.error(`Vault stories dir introuvable : ${storiesDir}`);
  process.exit(1);
}

function* walk(dir: string): Generator<string> {
  for (const entry of fs.readdirSync(dir, { withFileTypes: true })) {
    const p = path.join(dir, entry.name);
    if (entry.isDirectory()) yield* walk(p);
    else if (entry.isFile() && entry.name.endsWith('.md')) yield p;
  }
}

const cutoff = new Date(Date.now() - sinceDays * 86400_000);
const stories: BedtimeStory[] = [];
for (const file of walk(storiesDir)) {
  try {
    const content = fs.readFileSync(file, 'utf-8');
    const s = parseBedtimeStory(content, file);
    if (s && new Date(s.date) >= cutoff) stories.push(s);
  } catch (e) {
    if (process.env.DEBUG) console.warn(`[skip] ${file}: ${e}`);
  }
}

const total = stories.length;
const clean = stories.filter((s) => (s.quality_issues?.length ?? 0) === 0 && !hasHardFail(s)).length;
const soft = stories.filter((s) => (s.quality_issues?.length ?? 0) > 0 && !hasHardFail(s)).length;
const hard = stories.filter((s) => hasHardFail(s)).length;
const retried = stories.filter((s) => s.quality_retried).length;
const llmScores = stories.map((s) => s.quality_score).filter((n): n is number => typeof n === 'number');
const avgLlm = llmScores.length > 0 ? llmScores.reduce((a, b) => a + b, 0) / llmScores.length : null;
const fallbacks = stories.filter((s) => s.llm_judge?.justification?.startsWith('Score neutre')).length;

function hasHardFail(s: BedtimeStory): boolean {
  if (!s.quality_dimensions) return false;
  return Object.values(s.quality_dimensions).some((v) => v === 1);
}

const issueCounts = new Map<string, number>();
for (const s of stories) {
  for (const issue of s.quality_issues ?? []) {
    issueCounts.set(issue, (issueCounts.get(issue) ?? 0) + 1);
  }
}
const topIssues = [...issueCounts.entries()].sort((a, b) => b[1] - a[1]).slice(0, 5);

const pct = (n: number) => total > 0 ? `${((n / total) * 100).toFixed(1)}%` : 'n/a';

console.log(`=== Eval Dashboard (${sinceDays} derniers jours) ===`);
console.log(`Total stories : ${total}`);
console.log(`  🟢 Clean (0 issues)    : ${clean} (${pct(clean)})`);
console.log(`  🟡 Soft warnings        : ${soft} (${pct(soft)})`);
console.log(`  🔴 Hard fail            : ${hard} (${pct(hard)})`);
console.log(``);
console.log(`Re-roll : ${retried}/${total} (${pct(retried)})`);
console.log(`LLM-judge score moy     : ${avgLlm !== null ? avgLlm.toFixed(2) + ' / 10' : 'n/a'}`);
console.log(`LLM-judge fallback      : ${fallbacks}/${total} (${pct(fallbacks)})`);
console.log(``);
console.log(`Top issues :`);
for (const [issue, count] of topIssues) {
  console.log(`  - ${issue} : ${count}`);
}
console.log(``);
const hardRate = total > 0 ? hard / total : 0;
const cleanRate = total > 0 ? clean / total : 0;
console.log(`Cible Phase 52 : hard fail < 10% ${hardRate < 0.10 ? '✓' : '✗'} | clean > 60% ${cleanRate > 0.60 ? '✓' : '✗'}`);
```

3. **Mettre à jour `CLAUDE.md`** — ajouter aux sections appropriées :

   - Section **Stack** : ajouter `zod ^3.x (validation JSON LLM-eval, Phase 52)`
   - Section **Architecture** : ajouter ligne `lib/eval/ — pipeline d'évaluation auto Phase 52 : rubric déterministe (rubric.ts), helpers (rubric-helpers.ts), LLM-judge async (llm-eval.ts), feature flag (feature-flag.ts), pipeline orchestrateur (pipeline.ts)`
   - Section **Testing** : ajouter `Golden set Phase 52 : 20 fixtures dans lib/eval/__tests__/fixtures/golden-set.json — test de non-régression CI (lib/eval/__tests__/non-regression-baseline.test.ts) reproduit 14/20 flagged ±1`
   - Section **Cache** : ajouter une note `Stories : nouveaux champs frontmatter Phase 52 (quality_score, quality_dimensions, quality_issues, quality_retried, llm_judge) → pas de bump CACHE_VERSION nécessaire (Stories exclu du cache)`

4. **Créer `.planning/phases/52-stories-pipeline-d-valuation-auto-re-roll-qualit/52-OVERVIEW.md`** :

```markdown
# Phase 52 — Récap : Stories pipeline d'évaluation auto + re-roll qualité

## Livré

- **Rubric déterministe 6 dimensions** (lib/eval/rubric.ts) calibré sur golden set 20 stories — distribution 14/20 flagged ±1 reproduite en CI (Plan 52-01)
- **Helpers réutilisables** : typeTokenRatio, ngramOverlap, countOccurrences, anonymizeStoryText (Plan 52-01)
- **Re-roll cap 1** : si hardFail, 1 retry Sonnet avec prompt augmenté ; ship 2ème version quoi qu'il arrive (Plan 52-02)
- **Persistence frontmatter** : quality_score, quality_dimensions, quality_issues, quality_retried, llm_judge (Plan 52-02)
- **LLM-judge async** Claude Haiku 4.5, temp 0, max_tokens 300, JSON+Zod, fallback neutre 5/10, anonymisation pré-envoi (Plan 52-03)
- **UI badge** vert/ambre/rouge dans la liste stories + modal détails FR (Plan 52-03)
- **CI gate non-régression** : test golden set bloque toute PR qui dégrade la distribution (Plan 52-04)
- **Dashboard manuel** : `npx tsx scripts/eval-dashboard.ts --since 30d` (Plan 52-04)

## Activer la pipeline

3 méthodes (cf. lib/eval/feature-flag.ts) :

1. **Production** : modifier `DEFAULT_FEATURE_EVAL_ENABLED = true` puis rebuild app
2. **Test temporaire** : appeler `setEvalEnabledOverride(true)` au boot dans `app/_layout.tsx`
3. **Dev menu** : brancher un toggle settings qui appelle `setEvalEnabledOverride` (chantier futur)

## Auditer

```bash
# Dashboard 30 jours sur le vault local
npx tsx scripts/eval-dashboard.ts --vault ~/Library/Mobile\ Documents/iCloud~com~familyvault/Documents

# Test CI gate
npx jest lib/eval/__tests__ --no-coverage

# Type check
npx tsc --noEmit
```

## Coûts vérifiés

- Rubric déterministe : 0 (pure JS, < 50ms par story)
- LLM-judge Haiku 4.5 : ~$0.0032/story → ~$2.34/an/famille (730 stories/an)
- Re-roll cap 1 (Sonnet) : ~$1.50/an/famille
- **Total Phase 52 : ~$4/an/famille**

## Métriques cibles (régime stationnaire post-activation)

- Hard fail rate < 10%
- Clean rate > 60%
- LLM-judge fallback rate < 5%
- Distribution golden set CI : 14/20 ±1

## Prochaines étapes possibles

- Toggle UI parent dans settings (expo-secure-store)
- Calibration corrélation Spearman LLM-judge vs verdict humain (script dev)
- Extension dimension D6 cohérence saga quand >= 3 chapitres consécutifs
- Dimension D7 entités nommées (registry profil enfant)
```

5. **Vérification finale** :
   - `npx tsc --noEmit` clean
   - `npx jest --no-coverage` clean
   - `npx tsx scripts/eval-dashboard.ts --vault ./test-fixtures-vault 2>&1 | head -3` exécute sans crash (au moins jusqu'à "Vault stories dir introuvable" si pas de vault test — c'est OK)
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx jest --no-coverage 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `test -f scripts/eval-dashboard.ts`
    - `grep -q 'parseBedtimeStory' scripts/eval-dashboard.ts`
    - `grep -q '=== Eval Dashboard' scripts/eval-dashboard.ts`
    - `grep -q 'lib/eval/' CLAUDE.md`
    - `grep -q 'zod' CLAUDE.md`
    - `grep -q 'golden set' CLAUDE.md` (i ou I, FR ou EN — vérifier au moins une occurrence)
    - `test -f .planning/phases/52-stories-pipeline-d-valuation-auto-re-roll-qualit/52-OVERVIEW.md`
    - `grep -q 'DEFAULT_FEATURE_EVAL_ENABLED' .planning/phases/52-stories-pipeline-d-valuation-auto-re-roll-qualit/52-OVERVIEW.md`
    - `npx tsc --noEmit` clean
    - `npx jest --no-coverage` exit 0
    - Documentation FR (CLAUDE.md, 52-OVERVIEW.md) — `grep -iE "\\bplease\\b|\\bthe quality\\b" .planning/phases/52-*/52-OVERVIEW.md` retourne vide
  </acceptance_criteria>
  <done>CI gate vert, dashboard fonctionnel, docs à jour, OVERVIEW prêt pour reprise future.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Script CLI ↔ Vault filesystem | Lecture seule du vault — pas d'écriture, pas de réseau |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-04-01 | Information Disclosure | scripts/eval-dashboard.ts lit prénoms enfants du vault | accept | Script local dev, lance par le parent uniquement, output console (pas de log fichier). Aucun upload. |
| T-52-04-02 | Tampering | CI gate contournable via fixture editing | mitigate | Fixtures sous git, modifications visibles en review. Test exige `goldenSet.length === 20` (vu dans Plan 52-01) — ajout/retrait fixture cassera le compteur. |
| T-52-04-03 | Repudiation | flag toggle non-tracé | accept | Toggle limité au runtime override mémoire (in-process). Pas de persistance Phase 52. |
</threat_model>

<verification>
- `npx jest --no-coverage` exit 0 (suite complète : Plan 52-01 + 52-02 + 52-03 + 52-04)
- `npx tsc --noEmit` clean
- CI gate `non-regression-baseline.test.ts` produit la distribution attendue
- Script dashboard exécute sans crash (sortie cohérente sur vault test ou message "Vault dir introuvable")
- Documentation à jour : CLAUDE.md mentionne lib/eval/, zod, golden set
- OVERVIEW lisible et complet (3 méthodes activation, coûts vérifiés, métriques cibles)
</verification>

<success_criteria>
- Test non-régression bloque toute PR qui dégrade le golden set (EVAL-04 + EVAL-08)
- Feature flag exposable runtime via setEvalEnabledOverride (EVAL-07)
- Dashboard auditable manuellement (AI-SPEC §F2)
- Documentation projet à jour (CLAUDE.md sections Stack/Architecture/Testing/Cache)
- 52-OVERVIEW.md récapitule la phase pour reprise future
- Aucune régression sur les phases précédentes
</success_criteria>

<output>
À la fin du Plan : créer `.planning/phases/52-stories-pipeline-d-valuation-auto-re-roll-qualit/52-04-SUMMARY.md` avec :
- Distribution flagged effective sur le golden set au moment du build (X/20, Y soft, Z hard)
- Sortie attendue de `npx tsx scripts/eval-dashboard.ts --vault <fake>` (capture)
- Liste des sections CLAUDE.md modifiées
- Note finale : Phase 52 prête à activer (DEFAULT_FEATURE_EVAL_ENABLED reste false jusqu'au feu vert parent)
</output>
