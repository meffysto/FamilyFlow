---
phase: 06-ambiance-retention-time-of-day-ambiance-golden-crop-mutation-streak-flames
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/mascot/types.ts
  - lib/mascot/farm-engine.ts
  - components/mascot/FarmPlots.tsx
autonomous: true
requirements: [AMB-02]

must_haves:
  truths:
    - "plantCrop() a 3% de chance de creer une culture doree (isGolden: true)"
    - "parseCrops() lit correctement l'ancien format CSV 5 champs ET le nouveau format 6 champs"
    - "serializeCrops() ecrit le champ golden comme 6eme champ separe par :"
    - "harvestCrop() multiplie la recompense x5 pour les cultures dorees"
    - "Les parcelles dorees affichent un liseré or #FFD700 quand la culture est mature"
    - "Les parcelles dorees affichent un liseré subtil rgba(255,215,0,0.3) pendant la croissance"
  artifacts:
    - path: "lib/mascot/types.ts"
      provides: "PlantedCrop.isGolden?: boolean"
      contains: "isGolden"
    - path: "lib/mascot/farm-engine.ts"
      provides: "Golden mutation logic in plantCrop, harvestCrop, parseCrops, serializeCrops"
      contains: "GOLDEN_CROP_CHANCE"
    - path: "components/mascot/FarmPlots.tsx"
      provides: "Golden visual glow on farm plots"
      contains: "goldenGlow"
  key_links:
    - from: "lib/mascot/farm-engine.ts"
      to: "lib/mascot/types.ts"
      via: "PlantedCrop interface with isGolden field"
      pattern: "isGolden"
    - from: "components/mascot/FarmPlots.tsx"
      to: "lib/mascot/farm-engine.ts"
      via: "parseCrops returns PlantedCrop with isGolden"
      pattern: "crop\\.isGolden"
---

<objective>
Ajouter la mutation "culture doree" (Golden Crop) au systeme de ferme : 3% de chance a la plantation, visuel dore distinctif, recompense x5 a la recolte.

Purpose: Ajouter un element de surprise et de recompense rare a la boucle de plantation/recolte, augmentant l'engagement sans modifier le flux existant.
Output: PlantedCrop etendu avec isGolden, logique farm-engine mise a jour, visuel dore sur FarmPlots.
</objective>

<execution_context>
@/Users/gabrielwaltio/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gabrielwaltio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-ambiance-retention-time-of-day-ambiance-golden-crop-mutation-streak-flames/06-RESEARCH.md

<interfaces>
<!-- Existing PlantedCrop interface from lib/mascot/types.ts:270 -->
```typescript
export interface PlantedCrop {
  cropId: string;
  plotIndex: number;       // index de la parcelle (0-9)
  currentStage: number;    // 0 = graine, 4 = pret a recolter
  tasksCompleted: number;  // taches completees dans le stade actuel
  plantedAt: string;       // YYYY-MM-DD
}
```

<!-- Existing farm-engine functions -->
```typescript
// lib/mascot/farm-engine.ts
export function plantCrop(crops: PlantedCrop[], plotIndex: number, cropId: string): PlantedCrop[];
export function harvestCrop(crops: PlantedCrop[], plotIndex: number): { crops: PlantedCrop[]; reward: number };
export function serializeCrops(crops: PlantedCrop[]): string;
export function parseCrops(csv: string): PlantedCrop[];
```

<!-- Current CSV format: plotIndex:cropId:currentStage:tasksCompleted:plantedAt -->
<!-- Example: "0:carotte:2:3:2026-03-15,1:tomate:4:5:2026-03-10" -->

<!-- Existing FarmPlots matureGlow style -->
```typescript
// components/mascot/FarmPlots.tsx:197
matureGlow: {
  position: 'absolute',
  width: '100%',
  height: '100%',
  borderRadius: 8,
  borderWidth: 2,
  borderColor: '#4ADE80',
  backgroundColor: 'rgba(74, 222, 128, 0.1)',
},
```

<!-- FarmPlot component receives: crop (PlantedCrop), cropDef (CropDefinition), isMature (boolean) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Etendre PlantedCrop et la logique farm-engine pour la mutation doree</name>
  <files>lib/mascot/types.ts, lib/mascot/farm-engine.ts</files>
  <read_first>lib/mascot/types.ts, lib/mascot/farm-engine.ts</read_first>
  <action>
1. Dans `lib/mascot/types.ts`, ajouter le champ optionnel `isGolden?: boolean` a l'interface `PlantedCrop` (apres `plantedAt`):
```typescript
export interface PlantedCrop {
  cropId: string;
  plotIndex: number;
  currentStage: number;
  tasksCompleted: number;
  plantedAt: string;
  isGolden?: boolean;  // mutation doree — 3% chance a la plantation
}
```

2. Dans `lib/mascot/farm-engine.ts`, ajouter la constante en haut du fichier (apres les imports existants):
```typescript
/** Probabilite de mutation doree a la plantation */
export const GOLDEN_CROP_CHANCE = 0.03;

/** Multiplicateur de recompense pour les cultures dorees */
export const GOLDEN_HARVEST_MULTIPLIER = 5;
```

3. Modifier `plantCrop()` pour ajouter le roll de mutation. Ajouter `isGolden: Math.random() < GOLDEN_CROP_CHANCE` dans l'objet PlantedCrop retourne. Le reste de la fonction reste identique.

4. Modifier `harvestCrop()` pour multiplier la recompense x5 quand `crop.isGolden` est true. Changer la ligne `const reward = cropDef?.harvestReward ?? 0;` en:
```typescript
const baseReward = cropDef?.harvestReward ?? 0;
const reward = crop.isGolden ? baseReward * GOLDEN_HARVEST_MULTIPLIER : baseReward;
```

5. Modifier `serializeCrops()` pour ajouter le 6eme champ:
```typescript
export function serializeCrops(crops: PlantedCrop[]): string {
  return crops
    .map(c => `${c.plotIndex}:${c.cropId}:${c.currentStage}:${c.tasksCompleted}:${c.plantedAt}:${c.isGolden ? '1' : ''}`)
    .join(',');
}
```

6. Modifier `parseCrops()` pour lire le 6eme champ de maniere backward-compatible:
```typescript
export function parseCrops(csv: string): PlantedCrop[] {
  if (!csv || csv.trim() === '') return [];
  return csv.split(',').map(entry => {
    const [plotIndex, cropId, currentStage, tasksCompleted, plantedAt, goldenFlag] = entry.split(':');
    return {
      plotIndex: parseInt(plotIndex, 10),
      cropId,
      currentStage: parseInt(currentStage, 10),
      tasksCompleted: parseInt(tasksCompleted, 10),
      plantedAt: plantedAt || new Date().toISOString().slice(0, 10),
      isGolden: goldenFlag === '1',
    };
  }).filter(c => !isNaN(c.plotIndex) && c.cropId);
}
```

IMPORTANT: `goldenFlag` sera `undefined` pour les anciennes donnees CSV a 5 champs. `undefined === '1'` est `false`, donc `isGolden` sera `false` par defaut — backward-compatible sans migration.
  </action>
  <verify>
    <automated>cd /Users/gabrielwaltio/Documents/family-vault && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
- `lib/mascot/types.ts` contient `isGolden?: boolean` dans l'interface PlantedCrop
- `lib/mascot/farm-engine.ts` contient `export const GOLDEN_CROP_CHANCE = 0.03`
- `lib/mascot/farm-engine.ts` contient `export const GOLDEN_HARVEST_MULTIPLIER = 5`
- `lib/mascot/farm-engine.ts` `plantCrop` contient `Math.random() < GOLDEN_CROP_CHANCE`
- `lib/mascot/farm-engine.ts` `harvestCrop` contient `GOLDEN_HARVEST_MULTIPLIER`
- `lib/mascot/farm-engine.ts` `serializeCrops` contient `c.isGolden ? '1' : ''`
- `lib/mascot/farm-engine.ts` `parseCrops` contient `goldenFlag === '1'`
- `npx tsc --noEmit` ne rapporte aucune nouvelle erreur
  </acceptance_criteria>
  <done>PlantedCrop a le champ isGolden, plantCrop() fait le roll 3%, harvestCrop() multiplie x5, serialization/deserialization backward-compatible</done>
</task>

<task type="auto">
  <name>Task 2: Ajouter le visuel dore sur FarmPlots</name>
  <files>components/mascot/FarmPlots.tsx</files>
  <read_first>components/mascot/FarmPlots.tsx, lib/mascot/types.ts</read_first>
  <action>
1. Dans le composant `FarmPlot` (qui recoit `crop`, `cropDef`, `isMature`, etc.), ajouter la logique de glow doree. Le composant a deja un `{isMature && <View style={styles.matureGlow} />}` — on le remplace par une logique conditionnelle:

```typescript
{/* Lueur mature ou doree */}
{isMature && !crop?.isGolden && <View style={styles.matureGlow} />}
{crop?.isGolden && (
  <View style={[
    styles.goldenGlow,
    // Subtil pendant croissance, plein eclat quand mature
    !isMature && styles.goldenGlowGrowing,
  ]} />
)}
```

2. Ajouter les styles dans le `StyleSheet.create` existant:

```typescript
goldenGlow: {
  position: 'absolute',
  width: '100%',
  height: '100%',
  borderRadius: 8,
  borderWidth: 2,
  borderColor: '#FFD700',
  backgroundColor: 'rgba(255, 215, 0, 0.15)',
  // iOS shadow pour effet glow:
  shadowColor: '#FFD700',
  shadowOffset: { width: 0, height: 0 },
  shadowOpacity: 0.6,
  shadowRadius: 6,
},
goldenGlowGrowing: {
  borderColor: 'rgba(255, 215, 0, 0.3)',
  backgroundColor: 'rgba(255, 215, 0, 0.05)',
  shadowOpacity: 0.2,
  shadowRadius: 3,
},
```

3. Ajouter un feedback haptique quand une culture doree est plantee. Dans le parent qui appelle `plantCrop()` (probablement dans `useFarm.ts` ou `tree.tsx`), ajouter apres le plant: `if (newCrop.isGolden) Haptics.notificationAsync(Haptics.NotificationFeedbackType.Success)`. NOTE: Si le retour de `plantCrop` ne permet pas de savoir facilement si le crop plante est golden, SKIP le haptics — ne pas casser le flow existant pour ca. Le visuel suffit.

NOTE sur les couleurs: `#FFD700` et les rgba dores sont des couleurs de contenu (or), pas des couleurs semantiques du theme. Les definir comme constantes dans le fichier ou dans le StyleSheet est correct — pas besoin de `useThemeColors()` pour celles-ci.
  </action>
  <verify>
    <automated>cd /Users/gabrielwaltio/Documents/family-vault && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
- `components/mascot/FarmPlots.tsx` contient `goldenGlow` dans le StyleSheet
- `components/mascot/FarmPlots.tsx` contient `goldenGlowGrowing` dans le StyleSheet
- `components/mascot/FarmPlots.tsx` contient `crop?.isGolden`
- `components/mascot/FarmPlots.tsx` contient `borderColor: '#FFD700'`
- `components/mascot/FarmPlots.tsx` contient `shadowColor: '#FFD700'`
- `npx tsc --noEmit` ne rapporte aucune nouvelle erreur
  </acceptance_criteria>
  <done>Les parcelles dorees ont un liseré or subtil pendant la croissance et un glow complet quand mature, le matureGlow vert standard ne s'affiche pas sur les crops golden</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passe sans nouvelles erreurs
- Le fichier `lib/mascot/types.ts` contient `isGolden?: boolean` dans PlantedCrop
- Les fonctions `plantCrop`, `harvestCrop`, `serializeCrops`, `parseCrops` gèrent toutes le champ isGolden
- `GOLDEN_CROP_CHANCE` et `GOLDEN_HARVEST_MULTIPLIER` sont exportées de farm-engine.ts
- Le CSV backward-compatible: un CSV a 5 champs se parse avec isGolden = false
</verification>

<success_criteria>
- La mutation doree fonctionne de bout en bout: plantation (3% chance) -> serialisation -> parsing -> visuel -> recolte (x5)
- Aucune culture existante (CSV 5 champs) ne casse au parsing
- Le visuel distingue clairement les cultures dorees (or) des cultures normales (vert)
- TypeScript compile sans erreur
</success_criteria>

<output>
After completion, create `.planning/phases/06-ambiance-retention-time-of-day-ambiance-golden-crop-mutation-streak-flames/06-01-SUMMARY.md`
</output>
