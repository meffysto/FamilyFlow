---
phase: 06-ambiance-retention-time-of-day-ambiance-golden-crop-mutation-streak-flames
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/mascot/ambiance.ts
  - components/mascot/AmbientParticles.tsx
  - components/mascot/StreakFlames.tsx
  - app/(tabs)/tree.tsx
autonomous: true
requirements: [AMB-01, AMB-03]

must_haves:
  truths:
    - "Le diorama affiche des particules de rosee (bleu clair, tombantes) entre 05:00 et 08:59"
    - "Le diorama affiche des lucioles (vert-jaune, flottantes, pulsantes) entre 21:00 et 04:59"
    - "Le diorama a un tint colore subtil selon le moment de la journee (matin bleu, soir ambre, nuit sombre)"
    - "Aucune particule ambiance ne s'affiche entre 09:00 et 18:59 (jour)"
    - "Les flammes de streak apparaissent sous le diorama quand profile.streak >= 2"
    - "Le nombre et la taille des flammes augmentent avec le tier de streak (2+, 7+, 14+, 30+)"
    - "Les animations respectent useReducedMotion — desactivees si l'utilisateur a active Reduce Motion"
    - "Les particules ambiance n'interceptent pas les touches (pointerEvents='none')"
  artifacts:
    - path: "lib/mascot/ambiance.ts"
      provides: "getTimeSlot(), AMBIENT_CONFIGS, TimeSlot type"
      exports: ["getTimeSlot", "AMBIENT_CONFIGS", "TimeSlot", "AmbiantConfig"]
    - path: "components/mascot/AmbientParticles.tsx"
      provides: "Composant overlay de particules ambiantes"
      contains: "AmbientParticles"
    - path: "components/mascot/StreakFlames.tsx"
      provides: "Composant flammes de streak"
      contains: "StreakFlames"
    - path: "app/(tabs)/tree.tsx"
      provides: "Integration AmbientParticles dans le diorama et StreakFlames sous le diorama"
      contains: "AmbientParticles"
  key_links:
    - from: "components/mascot/AmbientParticles.tsx"
      to: "lib/mascot/ambiance.ts"
      via: "getTimeSlot() et AMBIENT_CONFIGS pour determiner les particules"
      pattern: "getTimeSlot|AMBIENT_CONFIGS"
    - from: "app/(tabs)/tree.tsx"
      to: "components/mascot/AmbientParticles.tsx"
      via: "Import et rendu dans le diorama layer stack"
      pattern: "AmbientParticles"
    - from: "app/(tabs)/tree.tsx"
      to: "components/mascot/StreakFlames.tsx"
      via: "Import et rendu sous le diorama avec profile.streak"
      pattern: "StreakFlames"
    - from: "components/mascot/StreakFlames.tsx"
      to: "lib/gamification/engine.ts"
      via: "Utilise STREAK_MILESTONES pour determiner le tier"
      pattern: "STREAK_MILESTONES"
---

<objective>
Ajouter l'ambiance horaire (particules de rosee le matin, lucioles la nuit, tint colore) et les flammes de streak sous le diorama de l'arbre.

Purpose: Rendre l'ecran arbre vivant et reactif au moment de la journee et a l'engagement de l'utilisateur, augmentant la retention par le plaisir visuel.
Output: lib/mascot/ambiance.ts (logique pure), AmbientParticles.tsx (overlay), StreakFlames.tsx (flammes), tree.tsx mis a jour.
</objective>

<execution_context>
@/Users/gabrielwaltio/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gabrielwaltio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-ambiance-retention-time-of-day-ambiance-golden-crop-mutation-streak-flames/06-RESEARCH.md

<interfaces>
<!-- Existing SeasonParticle pattern from components/mascot/TreeView.tsx:1488 -->
```typescript
// Pattern a suivre: useSharedValue + withRepeat + withDelay
function SeasonParticle({ particle, index, containerSize }) {
  const translateY = useSharedValue(0);
  const translateX = useSharedValue(0);
  const opacity = useSharedValue(0);
  useEffect(() => {
    if (reducedMotion) return;
    const delay = index * 600;
    // direction 'down': withTiming linear fall
    // direction 'float': withSequence oscillation
    opacity.value = withDelay(delay, withRepeat(withSequence(...), -1, true));
  }, [reducedMotion]);
}
```

<!-- Existing STREAK_MILESTONES from lib/gamification/engine.ts:24 -->
```typescript
export const STREAK_MILESTONES = [
  { days: 30, bonus: 25, emoji: '🔥💎', label: 'Flamme Legendaire' },
  { days: 14, bonus: 15, emoji: '🔥🔥', label: 'Flamme Intense' },
  { days: 7, bonus: 10, emoji: '🔥', label: 'Flamme Ardente' },
  { days: 2, bonus: 5, emoji: '✨', label: 'Serie en cours' },
] as const;

export function getStreakMilestone(streak: number): { emoji: string; label: string; bonus: number } | null;
```

<!-- Diorama layer stack in tree.tsx:537-614 -->
```
treeBg TouchableOpacity (height = DIORAMA_HEIGHT_BY_STAGE[stageIdx])
  Couche 0: View backgroundColor = PIXEL_GROUND[season]
  Couche 1: LinearGradient grass texture
  Couche 2: PixelDiorama (flowers, stones)
  Couche 3: WorldGridView (crops + buildings)
  HarvestBurst (zIndex: 10)
  CropWhisper (zIndex: 20)
  Couche 4: treeOverlay > TreeView + sagaSceneElement
```

<!-- After diorama (tree.tsx:616-626): -->
```
LinearGradient groundTransition
FarmStats
WeeklyGoal
infoCard (profile + level)
```

<!-- tree.tsx imports (lines 27-34): -->
```typescript
import Animated, {
  useSharedValue, useAnimatedStyle, withSpring,
  FadeIn, FadeInDown, FadeInUp,
} from 'react-native-reanimated';
```

<!-- profile.streak is available via useVault(): -->
```typescript
const { activeProfile: profile } = useVault();
// profile.streak: number (calculated by calculateStreak in engine.ts)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Creer lib/mascot/ambiance.ts et les composants AmbientParticles + StreakFlames</name>
  <files>lib/mascot/ambiance.ts, components/mascot/AmbientParticles.tsx, components/mascot/StreakFlames.tsx</files>
  <read_first>components/mascot/TreeView.tsx (lines 1480-1580 pour le pattern SeasonParticle), components/mascot/FarmPlots.tsx (lines 40-60 pour le pattern pulse animation), lib/gamification/engine.ts (lines 24-47 pour STREAK_MILESTONES et getStreakMilestone)</read_first>
  <action>
**Fichier 1: `lib/mascot/ambiance.ts`** (nouveau)

Creer un module de logique pure (aucun import React) avec:

```typescript
/** Moment de la journee pour l'ambiance du diorama */
export type TimeSlot = 'matin' | 'jour' | 'soir' | 'nuit';

/** Retourne le slot horaire pour une date donnee */
export function getTimeSlot(date: Date = new Date()): TimeSlot {
  const h = date.getHours();
  if (h >= 5 && h < 9) return 'matin';
  if (h >= 9 && h < 19) return 'jour';
  if (h >= 19 && h < 21) return 'soir';
  return 'nuit';
}

export interface AmbiantConfig {
  particleColor: string;
  particleCount: number;
  particleSize: number;
  direction: 'down' | 'float';
  opacity: number;
  duration: number;
  colorOverlay?: string;
}

export const AMBIENT_CONFIGS: Record<TimeSlot, AmbiantConfig | null> = {
  matin: {
    particleColor: '#C8E6FF',
    particleCount: 7,
    particleSize: 3,
    direction: 'down',
    opacity: 0.45,
    duration: 7000,
    colorOverlay: 'rgba(180, 220, 255, 0.06)',
  },
  jour: null,
  soir: {
    particleColor: '#FFD59E',
    particleCount: 4,
    particleSize: 4,
    direction: 'float',
    opacity: 0.35,
    duration: 5000,
    colorOverlay: 'rgba(255, 170, 80, 0.07)',
  },
  nuit: {
    particleColor: '#AAFF66',
    particleCount: 6,
    particleSize: 5,
    direction: 'float',
    opacity: 0.70,
    duration: 4500,
    colorOverlay: 'rgba(20, 10, 60, 0.12)',
  },
};
```

**Fichier 2: `components/mascot/AmbientParticles.tsx`** (nouveau)

Composant overlay qui:
- Appelle `getTimeSlot()` et `AMBIENT_CONFIGS` au mount pour determiner la config
- Si config est `null` (jour), ne rend rien (`return null`)
- Genere `particleCount` particules positionnees aleatoirement (Math.random pour x initial, y initial)
- Chaque particule est un `Animated.View` circulaire (`borderRadius: particleSize`)
- Pour `direction: 'down'`: `translateY` anime de -20 a containerHeight avec `withRepeat(withTiming(..., { duration, easing: Easing.linear }), -1, false)`. Ajouter un wobble X leger avec `withRepeat(withSequence(withTiming(10), withTiming(-10)), -1, true)`
- Pour `direction: 'float'`: oscillation verticale `withRepeat(withSequence(withTiming(-15, {duration}), withTiming(15, {duration})), -1, true)` + derive horizontale lente
- Opacity pulse: `withRepeat(withSequence(withTiming(config.opacity), withTiming(config.opacity * 0.3)), -1, true)`
- Pour les lucioles (nuit): ajouter `shadowColor: particleColor, shadowRadius: 4, shadowOpacity: 0.8` sur le View (glow iOS)
- Si `colorOverlay` est defini, rendre un View avec `StyleSheet.absoluteFill` et `backgroundColor: colorOverlay` comme premier enfant
- Le wrapper doit avoir `pointerEvents="none"` et `StyleSheet.absoluteFill`
- Appeler `useReducedMotion()` de reanimated et guard: si reducedMotion, rendre uniquement le colorOverlay (pas de particules animees)
- Delay de chaque particule: `index * 600` (meme pattern que SeasonParticle)
- Props: `containerHeight: number`
- Exporter: `export function AmbientParticles({ containerHeight }: { containerHeight: number })`

**Fichier 3: `components/mascot/StreakFlames.tsx`** (nouveau)

Composant qui:
- Importe `STREAK_MILESTONES` depuis `../../lib/gamification/engine` — les seuils de jours sont definis la-bas, NE PAS coder les valeurs 2/7/14/30 en dur
- Recoit `streak: number` comme prop
- Si `streak < STREAK_MILESTONES[STREAK_MILESTONES.length - 1].days` (soit le seuil minimum, actuellement 2), retourne `null`
- Determine le tier en iterant sur `STREAK_MILESTONES` (le tableau est trie decroissant par `days`): `const milestoneIdx = STREAK_MILESTONES.findIndex(m => streak >= m.days)`. Si trouve, `tier = STREAK_MILESTONES.length - milestoneIdx` (tier 4 = index 0 = 30+ jours, tier 1 = index 3 = 2+ jours). Si pas trouve, retourne `null`.
- Rend `tier` elements `FlameItem` dans un `View` horizontal (flexDirection: 'row', justifyContent: 'center', gap: 4)
- Chaque `FlameItem`:
  - Appelle `useReducedMotion()` — si active, pas d'animation (render statique)
  - `scale` anime avec `withRepeat(withSequence(withTiming(1.0 + tier * 0.08, {duration: 400 + tier * 100, easing: Easing.inOut(Easing.sin)}), withTiming(0.9, {duration: 400 + tier * 100})), -1, true)`
  - `opacity` anime avec `withRepeat(withSequence(withTiming(1.0, {duration: 400 + tier * 100}), withTiming(0.6, {duration: 400 + tier * 100})), -1, true)`
  - Delay par index: `index * 200`
  - Taille emoji: `18 + tier * 4` (fontSize)
  - Emoji: `'🔥'` pour tous les tiers, sauf tier 4 qui a un diamond en plus (le dernier FlameItem affiche `'💎'`)
  - NE PAS utiliser `perspective` dans les transforms (CLAUDE.md l'interdit)
- Wrapper avec `marginVertical: Spacing.sm` (importer depuis constants/spacing)
- Exporter: `export function StreakFlames({ streak }: { streak: number })`
  </action>
  <verify>
    <automated>cd /Users/gabrielwaltio/Documents/family-vault && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
- `lib/mascot/ambiance.ts` exporte `getTimeSlot`, `AMBIENT_CONFIGS`, `TimeSlot`, `AmbiantConfig`
- `lib/mascot/ambiance.ts` contient `type TimeSlot = 'matin' | 'jour' | 'soir' | 'nuit'`
- `lib/mascot/ambiance.ts` contient `jour: null`
- `components/mascot/AmbientParticles.tsx` contient `pointerEvents="none"` ou `pointerEvents={'none'}`
- `components/mascot/AmbientParticles.tsx` contient `useReducedMotion`
- `components/mascot/AmbientParticles.tsx` contient `getTimeSlot`
- `components/mascot/AmbientParticles.tsx` contient `export function AmbientParticles`
- `components/mascot/StreakFlames.tsx` contient `export function StreakFlames`
- `components/mascot/StreakFlames.tsx` contient `useReducedMotion`
- `components/mascot/StreakFlames.tsx` contient `streak < 2` (guard pour ne rien rendre)
- `components/mascot/StreakFlames.tsx` contient `import` et `STREAK_MILESTONES` depuis `lib/gamification/engine`
- `components/mascot/StreakFlames.tsx` NE contient PAS de seuils codes en dur (pas de `>= 30` ni `>= 14` ni `>= 7` comme valeurs litterales pour la determination du tier)
- Aucun fichier ne contient `perspective` dans un tableau de transforms
- `npx tsc --noEmit` ne rapporte aucune nouvelle erreur
  </acceptance_criteria>
  <done>Les trois fichiers existent, sont corrects TypeScript, et implementent la logique d'ambiance horaire et de flammes de streak avec STREAK_MILESTONES importe depuis engine.ts</done>
</task>

<task type="auto">
  <name>Task 2: Integrer AmbientParticles et StreakFlames dans tree.tsx</name>
  <files>app/(tabs)/tree.tsx</files>
  <read_first>app/(tabs)/tree.tsx (entier — c'est le fichier principal a modifier), components/mascot/AmbientParticles.tsx, components/mascot/StreakFlames.tsx</read_first>
  <action>
1. Ajouter les imports en haut de `app/(tabs)/tree.tsx` (apres les imports mascot existants, vers la ligne 50):
```typescript
import { AmbientParticles } from '../../components/mascot/AmbientParticles';
import { StreakFlames } from '../../components/mascot/StreakFlames';
```

2. Dans le diorama layer stack (a l'interieur du `TouchableOpacity` `treeBg`, apres la Couche 4 `treeOverlay` et apres le `CropWhisper`, vers la ligne 592), ajouter l'overlay ambiance avec `zIndex: 5` (sous HarvestBurst qui est a 10 et CropWhisper a 20):
```tsx
{/* Couche ambiance : particules horaires + tint */}
<View style={{ ...StyleSheet.absoluteFillObject, zIndex: 5 }}>
  <AmbientParticles containerHeight={DIORAMA_HEIGHT_BY_STAGE[stageIdx] ?? SCREEN_H * 0.60} />
</View>
```

Note: Le placer AVANT HarvestBurst dans le JSX pour que le zIndex fasse son travail. Le `pointerEvents="none"` est dans AmbientParticles lui-meme.

3. Ajouter le composant StreakFlames apres le `<FarmStats>` (vers la ligne 626) et avant le `<WeeklyGoal>`:
```tsx
{/* Flammes de streak */}
{profile && <StreakFlames streak={profile.streak ?? 0} />}
```

4. NE PAS modifier les imports Animated existants — AmbientParticles et StreakFlames gerent leurs propres imports Reanimated.

5. NE PAS passer `profile.streak` directement dans le diorama — StreakFlames est rendu en dehors du diorama pour eviter de re-rendre tout le diorama quand le streak change.
  </action>
  <verify>
    <automated>cd /Users/gabrielwaltio/Documents/family-vault && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
- `app/(tabs)/tree.tsx` contient `import { AmbientParticles } from '../../components/mascot/AmbientParticles'`
- `app/(tabs)/tree.tsx` contient `import { StreakFlames } from '../../components/mascot/StreakFlames'`
- `app/(tabs)/tree.tsx` contient `<AmbientParticles`
- `app/(tabs)/tree.tsx` contient `<StreakFlames`
- `app/(tabs)/tree.tsx` contient `zIndex: 5`
- `app/(tabs)/tree.tsx` contient `profile.streak`
- `npx tsc --noEmit` ne rapporte aucune nouvelle erreur
  </acceptance_criteria>
  <done>AmbientParticles est integre dans le diorama (zIndex 5), StreakFlames est rendu entre FarmStats et WeeklyGoal, le tout compile sans erreur</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passe sans nouvelles erreurs
- `lib/mascot/ambiance.ts` exporte getTimeSlot, AMBIENT_CONFIGS, TimeSlot, AmbiantConfig
- `components/mascot/AmbientParticles.tsx` est importe et rendu dans tree.tsx
- `components/mascot/StreakFlames.tsx` est importe et rendu dans tree.tsx
- `components/mascot/StreakFlames.tsx` importe STREAK_MILESTONES depuis lib/gamification/engine
- Les deux composants respectent useReducedMotion
- AmbientParticles a pointerEvents="none"
- Aucun `perspective` dans les transforms
</verification>

<success_criteria>
- L'ecran arbre affiche des particules ambiantes selon l'heure (rosee le matin, lucioles la nuit, rien en journee)
- Un tint colore subtil s'applique sur le diorama selon le moment
- Les flammes de streak s'affichent sous le diorama quand streak >= 2, avec une intensite croissante
- Toutes les animations se desactivent si Reduce Motion est active
- Aucun conflit de touch sur le diorama (pointerEvents="none")
- TypeScript compile sans erreur
</success_criteria>

<output>
After completion, create `.planning/phases/06-ambiance-retention-time-of-day-ambiance-golden-crop-mutation-streak-flames/06-02-SUMMARY.md`
</output>
