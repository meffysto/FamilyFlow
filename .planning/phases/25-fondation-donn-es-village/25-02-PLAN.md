---
phase: 25-fondation-donn-es-village
plan: 02
type: tdd
wave: 2
depends_on: ["25-01"]
files_modified:
  - lib/__tests__/village-parser.test.ts
autonomous: true
requirements: [DATA-01, DATA-02, DATA-04, MAP-02]

must_haves:
  truths:
    - "parseGardenFile('') retourne un VillageData par defaut valide"
    - "Round-trip parseGardenFile(serializeGardenFile(data)) preserve toutes les donnees"
    - "appendContribution insere dans la section Contributions, pas apres Historique"
    - "Les contributions avec type invalide ou amount NaN sont ignorees au parsing"
    - "VILLAGE_GRID a exactement 4 elements et tous prefixes village_"
    - "computeWeekTarget(N) retourne BASE_TARGET * max(1, N)"
  artifacts:
    - path: "lib/__tests__/village-parser.test.ts"
      provides: "Tests unitaires couvrant parser, grid, templates du module village"
      min_lines: 80
  key_links:
    - from: "lib/__tests__/village-parser.test.ts"
      to: "lib/village/index.ts"
      via: "import depuis le barrel"
      pattern: "import.*from.*village"
---

<objective>
Creer les tests unitaires Jest pour le module `lib/village/` — parser bidirectionnel, grille village, templates d'objectif.

Purpose: Valider la fiabilite du parseur avant que Phase 26 (useGarden.ts) ne l'utilise en production. Detecter les regressions sur le format append-only et le round-trip.
Output: `lib/__tests__/village-parser.test.ts` avec couverture des cas normaux, edge cases, et pitfalls identifies.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/25-fondation-donn-es-village/25-CONTEXT.md
@.planning/phases/25-fondation-donn-es-village/25-RESEARCH.md
@.planning/phases/25-fondation-donn-es-village/25-01-SUMMARY.md

@lib/village/types.ts
@lib/village/grid.ts
@lib/village/templates.ts
@lib/village/parser.ts
@lib/village/index.ts
@lib/__tests__/effects.test.ts

<interfaces>
From lib/village/types.ts:
```typescript
export type VillageRole = 'fountain' | 'stall' | 'board' | 'portal';
export interface VillageCell { id: string; x: number; y: number; role: VillageRole; }
export type ContributionType = 'harvest' | 'task';
export interface VillageContribution { timestamp: string; profileId: string; type: ContributionType; amount: number; }
export interface VillageWeekRecord { weekStart: string; target: number; total: number; claimed: boolean; }
export interface VillageData { version: number; createdAt: string; currentWeekStart: string; currentThemeIndex: number; rewardClaimed: boolean; contributions: VillageContribution[]; pastWeeks: VillageWeekRecord[]; }
export interface ObjectiveTemplate { id: string; name: string; icon: string; description: string; }
```

From lib/village/parser.ts:
```typescript
export const VILLAGE_FILE: string;
export function parseGardenFile(content: string): VillageData;
export function serializeGardenFile(data: VillageData): string;
export function appendContribution(content: string, entry: VillageContribution): string;
```

From lib/village/grid.ts:
```typescript
export const VILLAGE_GRID: VillageCell[];
```

From lib/village/templates.ts:
```typescript
export const BASE_TARGET: number;
export const OBJECTIVE_TEMPLATES: ObjectiveTemplate[];
export function computeWeekTarget(activeProfileCount: number): number;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tests unitaires module village</name>
  <files>lib/__tests__/village-parser.test.ts</files>
  <read_first>
    - lib/village/types.ts (interfaces a tester)
    - lib/village/parser.ts (implementation a tester)
    - lib/village/grid.ts (constantes a valider)
    - lib/village/templates.ts (constantes + fonction a tester)
    - lib/__tests__/effects.test.ts (pattern de test existant dans le projet — structure describe/it, conventions)
  </read_first>
  <behavior>
    **parseGardenFile:**
    - parseGardenFile('') retourne { version: 1, createdAt: '', currentWeekStart: '', currentThemeIndex: 0, rewardClaimed: false, contributions: [], pastWeeks: [] }
    - parseGardenFile avec un fichier complet (frontmatter + 2 contributions + 1 historique) retourne les donnees correctes
    - parseGardenFile ignore les lignes de contribution avec type invalide (ex: 'invalid')
    - parseGardenFile ignore les lignes de contribution avec amount NaN (ex: 'abc')
    - parseGardenFile ignore les lignes malformees (moins de 4 parties separees par |)

    **serializeGardenFile:**
    - serializeGardenFile produit un frontmatter YAML valide (contient `---` en debut et fin)
    - serializeGardenFile inclut toujours les sections `## Contributions` et `## Historique` meme vides
    - serializeGardenFile formate les contributions en `- timestamp | profileId | type | amount`
    - serializeGardenFile formate l'historique en `- weekStart | cible:N | total:N | claimed:bool`

    **Round-trip:**
    - parseGardenFile(serializeGardenFile(data)) est egal a data pour un VillageData avec contributions et historique

    **appendContribution:**
    - appendContribution insere avant `## Historique` (pas en fin de fichier)
    - appendContribution sur un fichier sans section Contributions cree la section
    - appendContribution preserve le contenu existant de la section Contributions

    **VILLAGE_GRID (assertions structurelles):**
    - VILLAGE_GRID a exactement 4 elements
    - Tous les IDs commencent par 'village_'
    - Contient exactement 1 fountain, 2 stalls, 1 board
    - x et y sont entre 0 et 1

    **Templates:**
    - OBJECTIVE_TEMPLATES a 7 elements
    - Chaque template a id, name, icon, description non-vides
    - BASE_TARGET vaut 15
    - computeWeekTarget(0) retourne 15 (min 1 profil)
    - computeWeekTarget(3) retourne 45
    - computeWeekTarget(1) retourne 15
  </behavior>
  <action>
Creer `lib/__tests__/village-parser.test.ts` avec les groupes de tests suivants :

**Structure :** Utiliser le pattern existant du projet (describe/it, pas de mock externe sauf VaultManager).

```typescript
import {
  parseGardenFile,
  serializeGardenFile,
  appendContribution,
  VILLAGE_FILE,
  VILLAGE_GRID,
  OBJECTIVE_TEMPLATES,
  BASE_TARGET,
  computeWeekTarget,
} from '../village';
import type { VillageData, VillageContribution } from '../village';
```

**Groupes describe :**
1. `describe('parseGardenFile')` — 5 tests (defaut vide, fichier complet, type invalide, amount NaN, ligne malformee)
2. `describe('serializeGardenFile')` — 4 tests (frontmatter, sections vides, format contributions, format historique)
3. `describe('round-trip')` — 1 test (parse(serialize(data)) === data)
4. `describe('appendContribution')` — 3 tests (insert avant historique, creation section, preservation existant)
5. `describe('VILLAGE_GRID')` — 4 tests (count, prefixes, roles, bounds)
6. `describe('templates')` — 4 tests (count, non-vides, BASE_TARGET, computeWeekTarget)

**Fixture fichier complet :** Utiliser une constante string multi-ligne simulant un jardin-familial.md valide avec frontmatter + 2 contributions + 1 historique.

**IMPORTANT :** Ne PAS mocker gray-matter. Le test doit valider le comportement reel du parseur avec la vraie librairie.
  </action>
  <verify>
    <automated>npx jest lib/__tests__/village-parser.test.ts --no-coverage 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - lib/__tests__/village-parser.test.ts existe et contient au minimum 21 tests (5+4+1+3+4+4)
    - `npx jest lib/__tests__/village-parser.test.ts --no-coverage` passe avec 0 failures
    - Le fichier importe depuis `'../village'` (barrel)
    - Le fichier contient `describe('parseGardenFile')`, `describe('serializeGardenFile')`, `describe('round-trip')`, `describe('appendContribution')`, `describe('VILLAGE_GRID')`, `describe('templates')`
    - Le test round-trip verifie `expect(parsed).toEqual(original)` apres serialize puis re-parse
    - Le test appendContribution verifie que la ligne inseree est AVANT `## Historique`
    - Le test VILLAGE_GRID verifie `.every(c => c.id.startsWith('village_'))`
  </acceptance_criteria>
  <done>Tous les tests passent, couvrant le parseur bidirectionnel, l'append-only, la grille village namespacee, et les templates d'objectif. Le module lib/village/ est valide pour consommation par Phase 26.</done>
</task>

</tasks>

<verification>
1. `npx jest lib/__tests__/village-parser.test.ts --no-coverage` — tous les tests passent
2. `npx tsc --noEmit` — pas de nouvelles erreurs de type
</verification>

<success_criteria>
- 21+ tests unitaires couvrant parseGardenFile, serializeGardenFile, appendContribution, VILLAGE_GRID, templates
- Round-trip fidelity verifiee
- Append-only insert dans la bonne section verifiee
- Namespace village_ valide sur tous les IDs
- Tous les tests passent avec 0 failures
</success_criteria>

<output>
After completion, create `.planning/phases/25-fondation-donn-es-village/25-02-SUMMARY.md`
</output>
