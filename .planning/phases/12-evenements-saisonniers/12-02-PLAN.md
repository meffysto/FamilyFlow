---
phase: 13-evenements-saisonniers
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - components/mascot/VisitorSlot.tsx
  - components/mascot/SagaWorldEvent.tsx
  - app/(tabs)/tree.tsx
autonomous: true
requirements: [EVT-01, EVT-02, EVT-03]

must_haves:
  truths:
    - "Quand un événement saisonnier est actif et non complété, un visiteur pixel apparaît côté gauche du diorama avec animation d'arrivée (EVT-01)"
    - "Taper sur le visiteur événementiel ouvre le dialogue SagaWorldEvent avec le contenu de l'événement (EVT-02)"
    - "Compléter le dialogue donne une récompense garantie du pool saisonnier + bonus XP 15 pts (EVT-03)"
    - "Le visiteur saga et le visiteur événement peuvent coexister sans collision (D-10)"
    - "Le visiteur disparaît après interaction et ne réapparaît pas pour cet événement+année (D-08)"
  artifacts:
    - path: "components/mascot/VisitorSlot.tsx"
      provides: "Props targetFX/targetFY pour positionner le visiteur événementiel"
      contains: "targetFX"
    - path: "components/mascot/SagaWorldEvent.tsx"
      provides: "Prop overrideSaga pour bypasser getSagaById lookup"
      contains: "overrideSaga"
    - path: "app/(tabs)/tree.tsx"
      provides: "Couche visiteur événementiel + dialogue + complétion récompense"
      contains: "eventProgressList"
  key_links:
    - from: "app/(tabs)/tree.tsx"
      to: "lib/mascot/seasonal-events-engine.ts"
      via: "import getVisibleEventId, shouldShowEventVisitor, buildSeasonalEventAsSaga, drawGuaranteedSeasonalReward"
      pattern: "getVisibleEventId"
    - from: "app/(tabs)/tree.tsx"
      to: "lib/mascot/seasonal-events-storage.ts"
      via: "import loadEventProgressList, saveEventProgress"
      pattern: "loadEventProgressList"
    - from: "app/(tabs)/tree.tsx"
      to: "components/mascot/VisitorSlot.tsx"
      via: "Second VisitorSlot instance with targetFX={0.28}"
      pattern: "targetFX.*0.28"
    - from: "app/(tabs)/tree.tsx"
      to: "components/mascot/SagaWorldEvent.tsx"
      via: "SagaWorldEvent with overrideSaga prop"
      pattern: "overrideSaga"
---

<objective>
Câbler le moteur d'événements saisonniers dans l'UI : ajouter les props targetFX/targetFY à VisitorSlot, ajouter la prop overrideSaga à SagaWorldEvent, et orchestrer le second visiteur + dialogue + récompenses dans tree.tsx.

Purpose: Rendre les événements saisonniers visibles et interactifs dans la scène arbre/ferme.
Output: VisitorSlot et SagaWorldEvent étendus + tree.tsx avec le flux complet événement saisonnier.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-evenements-saisonniers/13-CONTEXT.md
@.planning/phases/13-evenements-saisonniers/13-RESEARCH.md
@.planning/phases/13-evenements-saisonniers/13-01-SUMMARY.md

@components/mascot/VisitorSlot.tsx
@components/mascot/SagaWorldEvent.tsx
@app/(tabs)/tree.tsx
@lib/mascot/seasonal-events-types.ts
@lib/mascot/seasonal-events-engine.ts
@lib/mascot/seasonal-events-storage.ts
@lib/mascot/seasonal-events-content.ts

<interfaces>
<!-- Types créés dans Plan 01 — utiliser directement -->

From lib/mascot/seasonal-events-types.ts:
```typescript
export interface SeasonalEventProgress {
  eventId: string; year: number; profileId: string;
  completed: boolean; completedAt?: string; choiceId?: string;
}
export interface SeasonalEventContent {
  eventId: string; emoji: string; titleKey: string; visitorNameKey: string;
  chapter: SagaChapter; bonusXP: number;
}
```

From lib/mascot/seasonal-events-engine.ts:
```typescript
export function shouldShowEventVisitor(progressList: SeasonalEventProgress[], profileId: string, now?: Date): boolean;
export function getVisibleEventId(progressList: SeasonalEventProgress[], profileId: string, now?: Date): string | null;
export function drawGuaranteedSeasonalReward(event: SeasonalEvent, choiceIndex: number): { reward: SeasonalRewardItem; rarity: string };
export function buildSeasonalEventAsSaga(content: SeasonalEventContent, profileId: string): { saga: Saga; progress: SagaProgress };
export const SEASONAL_EVENT_BONUS_XP: number;
```

From lib/mascot/seasonal-events-storage.ts:
```typescript
export async function loadEventProgressList(profileId: string): Promise<SeasonalEventProgress[]>;
export async function saveEventProgress(progress: SeasonalEventProgress): Promise<void>;
```

From lib/mascot/seasonal-events-content.ts:
```typescript
export function getEventContent(eventId: string): SeasonalEventContent | undefined;
```

From components/mascot/VisitorSlot.tsx (current props):
```typescript
export interface VisitorSlotProps {
  visible: boolean; sagaId: string;
  containerWidth: number; containerHeight: number;
  onTap: () => void; shouldDepart: boolean; isLastChapter: boolean;
  onDepartComplete?: () => void;
  reactionType?: ReactionType; onReactionComplete?: () => void;
}
// Internal constants: TARGET_FX = 0.72, TARGET_FY = 0.62
```

From components/mascot/SagaWorldEvent.tsx (current):
```typescript
export interface SagaWorldEventProps {
  sagaProgress: SagaProgress; containerHeight: number;
  onChapterComplete: (choiceId: string, points: number, sagaNote: string,
    rewardItem?: { id: string; type: 'decoration' | 'inhabitant' }, bonusCropId?: string) => Promise<void>;
  onDismiss: () => void;
  visitorIdleFrame?: ImageSourcePropType;
  onChoiceReaction?: (reactionType: ReactionType) => void;
}
// Line 106: const activeSaga = getSagaById(sagaProgress.sagaId);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Étendre VisitorSlot + SagaWorldEvent pour les événements</name>
  <files>
    components/mascot/VisitorSlot.tsx,
    components/mascot/SagaWorldEvent.tsx
  </files>
  <read_first>
    components/mascot/VisitorSlot.tsx,
    components/mascot/SagaWorldEvent.tsx,
    lib/mascot/sagas-types.ts,
    lib/mascot/sagas-content.ts
  </read_first>
  <action>
    **VisitorSlot.tsx** — Ajouter 2 props optionnelles pour la position (per D-10, Pitfall 2):

    1. Ajouter à `VisitorSlotProps`:
       ```typescript
       /** Position X cible en fraction du container (défaut: 0.72 côté droit pour saga) */
       targetFX?: number;
       /** Position Y cible en fraction du container (défaut: 0.62) */
       targetFY?: number;
       ```

    2. Déstructurer dans le composant (à côté des autres props, ligne ~164):
       ```typescript
       targetFX: propTargetFX,
       targetFY: propTargetFY,
       ```

    3. Remplacer les deux lignes qui utilisent les constantes `TARGET_FX`/`TARGET_FY` (lignes ~181-182):
       ```typescript
       const TARGET_X = containerWidth * (propTargetFX ?? TARGET_FX);
       const TARGET_Y = containerHeight * (propTargetFY ?? TARGET_FY);
       ```

    C'est tout pour VisitorSlot — 3 modifications minimales, zéro changement de comportement pour les usages existants.

    **SagaWorldEvent.tsx** — Ajouter prop `overrideSaga` pour bypasser getSagaById (Pitfall 3):

    1. Importer le type `Saga` si pas déjà importé (vérifier les imports de sagas-content.ts — `getSagaById` y est défini)

    2. Ajouter à `SagaWorldEventProps`:
       ```typescript
       /** Saga fournie directement — bypass getSagaById() (utilisé par les événements saisonniers) */
       overrideSaga?: Saga;
       ```

    3. Déstructurer dans le composant (ligne ~93):
       ```typescript
       overrideSaga,
       ```

    4. Modifier la ligne 106 :
       ```typescript
       // AVANT:
       const activeSaga = getSagaById(sagaProgress.sagaId);
       // APRÈS:
       const activeSaga = overrideSaga ?? getSagaById(sagaProgress.sagaId);
       ```

    C'est tout pour SagaWorldEvent — 2 modifications, zéro changement de comportement pour les usages saga existants.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "VisitorSlot|SagaWorldEvent" | grep -c "error" | xargs -I{} test {} -eq 0 && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - VisitorSlotProps contient targetFX?: number et targetFY?: number
    - VisitorSlot utilise (propTargetFX ?? TARGET_FX) pour calculer TARGET_X (pas de remplacement direct de la constante)
    - SagaWorldEventProps contient overrideSaga?: Saga
    - Ligne 106 de SagaWorldEvent utilise overrideSaga ?? getSagaById(sagaProgress.sagaId)
    - Les usages existants de VisitorSlot et SagaWorldEvent dans tree.tsx ne sont pas modifiés dans cette tâche
  </acceptance_criteria>
  <done>VisitorSlot accepte targetFX/targetFY, SagaWorldEvent accepte overrideSaga, `npx tsc --noEmit` passe sans nouvelles erreurs</done>
</task>

<task type="auto">
  <name>Task 2: Câbler le visiteur événementiel dans tree.tsx</name>
  <files>
    app/(tabs)/tree.tsx
  </files>
  <read_first>
    app/(tabs)/tree.tsx,
    lib/mascot/seasonal-events-engine.ts,
    lib/mascot/seasonal-events-storage.ts,
    lib/mascot/seasonal-events-content.ts,
    lib/mascot/seasonal-events-types.ts,
    lib/gamification/seasonal.ts,
    lib/gamification/seasonal-rewards.ts,
    components/mascot/VisitorSlot.tsx,
    components/mascot/SagaWorldEvent.tsx,
    hooks/useVault.ts
  </read_first>
  <action>
    Ajouter un second visiteur (événement saisonnier) dans tree.tsx, parallèle au visiteur saga existant. Suivre le même pattern que le câblage saga (lignes ~274-297 pour l'état, ~1451-1531 pour le rendu).

    **1. Imports** — Ajouter en haut de tree.tsx :
    ```typescript
    import type { SeasonalEventProgress } from '../../lib/mascot/seasonal-events-types';
    import { getVisibleEventId, buildSeasonalEventAsSaga, drawGuaranteedSeasonalReward, SEASONAL_EVENT_BONUS_XP } from '../../lib/mascot/seasonal-events-engine';
    import { loadEventProgressList, saveEventProgress } from '../../lib/mascot/seasonal-events-storage';
    import { getEventContent } from '../../lib/mascot/seasonal-events-content';
    import { getActiveEvent } from '../../lib/gamification/seasonal';
    ```

    **2. État** — À côté de l'état saga (vers ligne ~276), ajouter :
    ```typescript
    // ── État visiteur événement saisonnier ──
    const [eventProgressList, setEventProgressList] = useState<SeasonalEventProgress[]>([]);
    const [eventProgressLoaded, setEventProgressLoaded] = useState(false);
    const [showEventDialogue, setShowEventDialogue] = useState(false);
    const [eventVisitorShouldDepart, setEventVisitorShouldDepart] = useState(false);
    const [eventVisitorReaction, setEventVisitorReaction] = useState<ReactionType | undefined>(undefined);
    ```

    **3. useEffect chargement** — Après le useEffect de chargement saga, ajouter :
    ```typescript
    useEffect(() => {
      if (!profile?.id) return;
      loadEventProgressList(profile.id).then(list => {
        setEventProgressList(list);
        setEventProgressLoaded(true);
      });
      setEventVisitorShouldDepart(false);
      setEventVisitorReaction(undefined);
    }, [profile?.id]);
    ```

    **4. Calcul dérivé** — Après le calcul sagaChapterAvailable :
    ```typescript
    const activeEventId = eventProgressLoaded
      ? getVisibleEventId(eventProgressList, profile?.id ?? '', new Date())
      : null;
    const activeEventContent = activeEventId ? getEventContent(activeEventId) : undefined;
    ```

    **5. Callback complétion événement** — Créer la fonction de complétion (modèle : le callback saga onChapterComplete ~ligne 1036). Cette fonction :
    ```typescript
    const handleEventComplete = useCallback(async (
      choiceId: string,
      points: number,
      _sagaNote: string,
    ) => {
      if (!profile?.id || !activeEventId || !activeEventContent) return;

      // 1. Récompense garantie (per D-11, D-13)
      const activeEvent = getActiveEvent();
      if (!activeEvent) return;
      const choiceIndex = activeEventContent.chapter.choices.findIndex(c => c.id === choiceId);
      const { reward, rarity } = drawGuaranteedSeasonalReward(activeEvent, choiceIndex >= 0 ? choiceIndex : 0);

      // 2. Ajouter XP : points du choix + bonus événement (per D-12)
      // Utiliser addPoints du vault si disponible, sinon passer par le mécanisme existant
      // Les points du choix sont déjà inclus dans `points` passé par SagaWorldEvent
      // Ajouter le bonus XP événementiel
      if (typeof addPoints === 'function') {
        addPoints(SEASONAL_EVENT_BONUS_XP);
      }

      // 3. Appliquer la récompense saisonnière (badge, points bonus, multiplier, etc.)
      // Utiliser le même mécanisme que le loot box saisonnier existant
      if (reward.bonusPoints && reward.bonusPoints > 0 && typeof addPoints === 'function') {
        addPoints(reward.bonusPoints);
      }
      // Pour les multipliers, badges, etc. — stocker comme récompense obtenue
      // (le système existant de loot gère déjà ces types dans openLootBox)

      // 4. Persister la complétion (per D-08)
      const progress: SeasonalEventProgress = {
        eventId: activeEventId,
        year: new Date().getFullYear(),
        profileId: profile.id,
        completed: true,
        completedAt: new Date().toISOString().slice(0, 10),
        choiceId,
      };
      await saveEventProgress(progress);
      setEventProgressList(prev => [
        ...prev.filter(p => !(p.eventId === activeEventId && p.year === progress.year)),
        progress,
      ]);

      // 5. Déclencher le départ du visiteur
      setEventVisitorShouldDepart(true);
    }, [profile?.id, activeEventId, activeEventContent, addPoints]);
    ```

    **IMPORTANT:** Adapter le callback ci-dessus au pattern exact de tree.tsx. Le `addPoints` provient de `useVault()` — vérifier comment il est déstructuré dans tree.tsx. Si `addPoints` n'est pas directement accessible, utiliser le mécanisme existant (ex: `vault.addPoints` ou passer par `gamiData`). Lire le code existant de `handleSagaChapterComplete` pour reproduire le même pattern.

    **6. Rendu** — Après le bloc VisitorSlot saga (~ligne 1451), ajouter un second bloc pour le visiteur événement. Le positionner côté gauche avec targetFX={0.28} (per D-10) :
    ```tsx
    {/* ── Visiteur événement saisonnier (couche 3.7) ── */}
    {activeEventId && activeEventContent && (
      <View style={{
        ...StyleSheet.absoluteFillObject,
        zIndex: showEventDialogue ? 20 : 3,
      }} pointerEvents={showSagaEvent ? 'none' : 'box-none'}>
        <VisitorSlot
          visible={!!activeEventId && !showEventDialogue}
          sagaId={activeEventId}
          containerWidth={dioramaWidth}
          containerHeight={dioramaHeight}
          targetFX={0.28}
          targetFY={0.62}
          onTap={() => setShowEventDialogue(true)}
          shouldDepart={eventVisitorShouldDepart}
          isLastChapter={true}
          onDepartComplete={() => {
            setEventVisitorShouldDepart(false);
          }}
          reactionType={eventVisitorReaction}
          onReactionComplete={() => setEventVisitorReaction(undefined)}
        />
      </View>
    )}
    ```

    **7. Dialogue événement** — Après le bloc SagaWorldEvent saga (~ligne 1522), ajouter :
    ```tsx
    {showEventDialogue && activeEventContent && profile && (() => {
      const { saga, progress } = buildSeasonalEventAsSaga(activeEventContent, profile.id);
      return (
        <SagaWorldEvent
          sagaProgress={progress}
          containerHeight={dioramaHeight}
          overrideSaga={saga}
          onChapterComplete={handleEventComplete}
          onDismiss={() => setShowEventDialogue(false)}
          visitorIdleFrame={require('../../assets/garden/animals/voyageur/idle_1.png')}
          onChoiceReaction={(reaction) => setEventVisitorReaction(reaction)}
        />
      );
    })()}
    ```

    **NOTE:** Le `buildSeasonalEventAsSaga` est appelé dans le render — c'est acceptable car c'est un calcul léger (pas de side-effect). Si des problèmes de perf apparaissent, extraire dans un useMemo.

    **8. pointerEvents mutuels** — Modifier le bloc du visiteur saga existant (~ligne 1451) pour ajouter `pointerEvents={showEventDialogue ? 'none' : 'box-none'}` — empêcher les taps sur le visiteur saga quand le dialogue événement est ouvert, et vice versa (Pitfall 5).

    **9. Variables diorama** — Vérifier que `dioramaWidth` et `dioramaHeight` existent déjà dans tree.tsx (utilisés par le VisitorSlot saga). Si les noms sont différents (ex: `containerWidth`), utiliser les mêmes variables. Lire le code autour de la ligne 1452 pour identifier les noms exacts.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -c "tree.tsx.*error" | xargs -I{} test {} -eq 0 && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - tree.tsx importe les 4 fichiers seasonal-events-* (types, engine, storage, content)
    - tree.tsx a un useEffect qui charge loadEventProgressList au changement de profile?.id
    - tree.tsx calcule activeEventId via getVisibleEventId
    - tree.tsx rend un second VisitorSlot avec targetFX={0.28} quand activeEventId est non-null
    - tree.tsx rend un SagaWorldEvent avec overrideSaga quand showEventDialogue est true
    - handleEventComplete appelle saveEventProgress et setEventVisitorShouldDepart(true)
    - Le pointerEvents du bloc saga est conditionné par showEventDialogue
    - Le pointerEvents du bloc événement est conditionné par showSagaEvent
    - `npx tsc --noEmit` passe sans nouvelles erreurs
  </acceptance_criteria>
  <done>Le visiteur événementiel apparaît côté gauche quand un événement est actif, le tap ouvre le dialogue, la complétion donne les récompenses et fait disparaître le visiteur. Coexistence saga+événement sans conflit.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passe sans nouvelles erreurs
- VisitorSlot a les props targetFX/targetFY (optionnelles, backward-compatible)
- SagaWorldEvent a la prop overrideSaga (optionnelle, backward-compatible)
- tree.tsx a deux couches VisitorSlot indépendantes (saga côté droit, événement côté gauche)
- Les pointerEvents sont mutuellement exclusifs entre saga et événement
- La complétion persiste en SecureStore et empêche la réapparition du visiteur
</verification>

<success_criteria>
L'intégralité du flux événement saisonnier est câblé : détection calendaire → visiteur pixel → dialogue interactif → récompense garantie → persistance → disparition. Les visiteurs saga et événement coexistent sans conflit. `npx tsc --noEmit` passe.
</success_criteria>

<output>
After completion, create `.planning/phases/13-evenements-saisonniers/13-02-SUMMARY.md`
</output>
