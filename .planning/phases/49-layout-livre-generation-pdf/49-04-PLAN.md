---
phase: 49-layout-livre-generation-pdf
plan: 04
type: execute
wave: 4
depends_on: [49-01, 49-02, 49-03]
files_modified:
  - lib/pdf/components/fallback-double-page.ts
  - lib/pdf/text-splitter.ts
  - lib/pdf/html-template.ts
  - lib/pdf/index.ts
  - lib/__tests__/pdf-fallback.test.ts
  - lib/__tests__/pdf-html-template.test.ts
autonomous: true
requirements: [LAY-05, LAY-01, PDF-09]
must_haves:
  truths:
    - "Mode B (fallback) déclenche uniquement quand spec.scenes est null/absent (CONTEXT D-Q2)"
    - "Mode B produit 6 doubles-pages typographiques ornementales (12 folios) entre cover et back-cover, total 16 pages"
    - "Texte découpé en 6 sections via splitTextIntoSections — frontières phrase, pas de mot cassé, ordre préservé"
    - "Chaque double-page contient : drop cap Caveat 6em terracotta dans cartouche feuillagé SVG, bordures botaniques marge externe (alternance fougère/lichen/ramures), pull quote DM Serif Display italic centré, vignettes haut/bas, numéros de page en cartouche"
    - "Palette ornementale exacte : terracotta #B8593F + sauge #7A8F6B + ivoire #F4EDE2 + encre #2E2A26 (CONTEXT.md §117-122)"
    - "Triple typographie respectée : DM Serif Display (titres + pull), Caveat (drops + numéros), Andika (corps)"
    - "Perf instrumentée __DEV__ avec budget cible <5s — log explicite si dépassement"
  artifacts:
    - path: "lib/pdf/text-splitter.ts"
      provides: "splitTextIntoSections(text, n=6): string[] — frontières phrase, longueurs équilibrées"
    - path: "lib/pdf/components/fallback-double-page.ts"
      provides: "renderFallbackDoublePage(opts): string (2 pages texte ornementales)"
    - path: "lib/__tests__/pdf-fallback.test.ts"
      provides: "Tests : 6 sections, frontières phrase, drop cap, ornements présents, palette correcte"
  key_links:
    - from: "html-template.ts"
      to: "fallback-double-page.ts"
      via: "renderModeBPages(spec) appelle 6× renderFallbackDoublePage"
    - from: "fallback-double-page.ts"
      to: "ornaments.ts"
      via: "dropCapFrame, borderFern, borderRamage, borderLichen, vignetteCrescent, vignetteForest, vignetteLantern, vignetteBird, cartoucheFrame, separatorTriple"
    - from: "text-splitter.ts"
      to: "story.texte"
      via: "découpe sur frontières phrase ([.!?] suivi d'espace), équilibrage par longueur"
---

<objective>
Finaliser Phase 49 avec le **Mode B fallback texte-seul ornemental** — vision ambitieuse "faire rêver" (CONTEXT.md §85-138) — utilisé pour les histoires sans `scenes` (V2 ou univers non-forêt). Pattern complet : drop caps Caveat 6em terracotta dans cartouche feuillagé, bordures botaniques en marge externe (fougère / lichen / ramure alternées), pull quotes DM Serif Display italic encadrés, vignettes haut/bas (lune, forêt, lanterne, oiseau), numéros de page dans cartouche, palette terracotta/sauge/ivoire/encre.

Ajouter aussi : (a) `splitTextIntoSections` pour découper le texte en 6 chunks équilibrés sur frontières phrase, (b) instrumentation perf <5s avec warn explicite si dépassement, (c) tests Jest pour valider la structure du mode B.

Purpose : Plan 49-02 a livré le mode A picture-book pour `foret`. Plan 49-04 livre le mode B pour les ~70% d'histoires sans illustrations — c'est ce qui permet d'exporter n'importe quelle histoire dès Phase 51.

Output : 2 nouveaux fichiers source + html-template.ts complété + 1 test Jest dédié + extension du test existant.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/49-layout-livre-generation-pdf/49-CONTEXT.md
@.planning/phases/49-layout-livre-generation-pdf/49-RESEARCH.md
@.planning/phases/49-layout-livre-generation-pdf/49-01-SUMMARY.md
@.planning/phases/49-layout-livre-generation-pdf/49-02-SUMMARY.md
@.planning/phases/49-layout-livre-generation-pdf/49-03-SUMMARY.md
@lib/pdf/ornaments.ts
@lib/pdf/components/cover.ts
@lib/pdf/components/title.ts
@lib/pdf/components/back-cover.ts
@lib/pdf/html-template.ts
@lib/pdf/pdf-generator.ts

<interfaces>
<!-- Disponibles depuis Plans 49-01/02/03 -->

From lib/pdf/ornaments.ts:
```typescript
export interface OrnamentOpts { color?: string; size?: number; }
export function dropCapFrame(opts?: OrnamentOpts): string;
export function borderFern(opts?: OrnamentOpts): string;
export function borderRamage(opts?: OrnamentOpts): string;
export function borderLichen(opts?: OrnamentOpts): string;
export function vignetteCrescent(opts?: OrnamentOpts): string;
export function vignetteForest(opts?: OrnamentOpts): string;
export function vignetteLantern(opts?: OrnamentOpts): string;
export function vignetteBird(opts?: OrnamentOpts): string;
export function cartoucheFrame(opts?: OrnamentOpts): string;
export function separatorTriple(opts?: OrnamentOpts): string;
```

From lib/pdf/html-template.ts:
```typescript
export interface BookHtmlSpec { ... scenes: SceneSpec[] | null; ... }
// Mode B placeholder actuel : renderModeBPlaceholder produit 16 sections vides
// Plan 49-04 remplace cette fonction par renderModeBPages(spec) réelle
```

From lib/pdf/components/cover.ts + title.ts + back-cover.ts:
- `renderCoverPage`, `renderTitlePage`, `renderBackCoverPage` réutilisés tels quels en mode B
  (cover sans illustration → fallback paperShadow déjà géré ; title page identique ; back-cover identique)

Palette ornementale (CONTEXT.md §117-122) — UTILISER CES VALEURS EXACTES :
- terracotta = #B8593F (drops, pull quote séparateurs, numéros de page)
- sage = #7A8F6B (bordures botaniques, séparateurs secondaires)
- ivory = #F4EDE2 (fond — note : différent de BOOK_PALETTE.ivory #FAF6EE — Plan 49-04 utilise la palette ornementale spécifique mode B en CSS local, BOOK_PALETTE reste pour mode A)
- ink = #2E2A26 (corps texte)

Triple typographie (CONTEXT.md §123-127) : DM Serif Display (titres + pull) ; Caveat (drops + numéros + signatures) ; Andika (corps).

Structure 16 pages mode B (CONTEXT.md §128-134) :
| Folio | Type |
|-------|------|
| 1 | Cover (réutilise renderCoverPage — sans illu, fallback paperShadow) |
| 2 | Page de titre (renderTitlePage inchangé) |
| 3-14 | 6 doubles-pages texte ornementales (renderFallbackDoublePage × 6) |
| 15 | Fin + memorySummary (renderEndPage déjà dans html-template.ts) |
| 16 | Back cover (renderBackCoverPage inchangé) |
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 : text-splitter.ts (découpe 6 sections sur frontières phrase)</name>
  <files>lib/pdf/text-splitter.ts, lib/__tests__/pdf-fallback.test.ts</files>
  <behavior>
- `splitTextIntoSections('Phrase 1. Phrase 2. Phrase 3. Phrase 4. Phrase 5. Phrase 6.', 6)` retourne 6 strings, chacune contenant ~1 phrase
- `splitTextIntoSections(text, n)` retourne EXACTEMENT n entrées (jamais moins, jamais plus)
- Concat des sections = text d'origine modulo whitespace de séparation (équivalent fonctionnel)
- Ne coupe jamais un mot au milieu (frontières [.!?] + espace OU fin de texte)
- Pour un texte trop court (<n phrases), répartit le texte en chunks par caractère sur frontières mot
- Pure function — déterministe, no Date.now / random
  </behavior>
  <action>
**Créer `lib/pdf/text-splitter.ts`** (RESEARCH.md §449 + §711-720) :

```typescript
/**
 * Découpe un texte en N sections équilibrées par longueur, en respectant les
 * frontières de phrase ([.!?] suivi d'espace ou fin). Utilisé en Mode B
 * fallback pour distribuer le récit sur 6 doubles-pages.
 *
 * Garantie :
 * - Retourne EXACTEMENT n entrées (jamais 0, jamais > n)
 * - Pas de mot coupé
 * - Concat des sections (avec espace) ≈ texte d'origine
 *
 * Algorithme :
 * 1. Trouver toutes les frontières de phrase (positions des [.!?] suivis d'espace)
 * 2. Cible : longueur totale / n caractères par section
 * 3. Pour chaque section i = 0..n-1 :
 *    - cible = (i+1) * targetLen
 *    - choisir la frontière phrase la plus proche de cette cible (en avant ou arrière)
 *    - section = text.slice(cursor, frontière)
 * 4. Si nombre de phrases < n : fallback frontières mots (espace), équilibrage simple
 */
export function splitTextIntoSections(text: string, n: number = 6): string[] {
  if (n < 1) throw new Error('n must be >= 1');
  const trimmed = text.trim();
  if (trimmed.length === 0) {
    return Array.from({ length: n }, () => '');
  }

  // 1. Trouver frontières phrase
  const sentenceBoundaries: number[] = [];
  const re = /[.!?]+(?:\s+|$)/g;
  let m: RegExpExecArray | null;
  while ((m = re.exec(trimmed)) !== null) {
    sentenceBoundaries.push(m.index + m[0].length);
  }

  // Si moins de n phrases → fallback frontières mot
  if (sentenceBoundaries.length < n) {
    return splitOnWordBoundaries(trimmed, n);
  }

  // 2. Découpe équilibrée
  const targetLen = trimmed.length / n;
  const sections: string[] = [];
  let cursor = 0;
  for (let i = 0; i < n; i++) {
    if (i === n - 1) {
      // Dernière section : tout ce qui reste
      sections.push(trimmed.slice(cursor).trim());
      break;
    }
    const targetEnd = Math.round((i + 1) * targetLen);
    // Choisir la frontière phrase la plus proche (≥ cursor, après targetEnd préféré)
    let bestBoundary = -1;
    let bestDist = Infinity;
    for (const b of sentenceBoundaries) {
      if (b <= cursor) continue;
      const dist = Math.abs(b - targetEnd);
      if (dist < bestDist) {
        bestDist = dist;
        bestBoundary = b;
      }
    }
    if (bestBoundary === -1) {
      // Plus de frontières dispo — push tout le reste et padder le reste vide
      sections.push(trimmed.slice(cursor).trim());
      while (sections.length < n) sections.push('');
      return sections;
    }
    sections.push(trimmed.slice(cursor, bestBoundary).trim());
    cursor = bestBoundary;
  }

  // Garantie longueur n (au cas où)
  while (sections.length < n) sections.push('');
  return sections.slice(0, n);
}

function splitOnWordBoundaries(text: string, n: number): string[] {
  const targetLen = text.length / n;
  const sections: string[] = [];
  let cursor = 0;
  for (let i = 0; i < n; i++) {
    if (i === n - 1) {
      sections.push(text.slice(cursor).trim());
      break;
    }
    const targetEnd = Math.round((i + 1) * targetLen);
    // Trouver le prochain espace ≥ targetEnd
    let boundary = text.indexOf(' ', targetEnd);
    if (boundary === -1 || boundary <= cursor) boundary = text.length;
    sections.push(text.slice(cursor, boundary).trim());
    cursor = boundary;
  }
  while (sections.length < n) sections.push('');
  return sections.slice(0, n);
}
```

**Créer `lib/__tests__/pdf-fallback.test.ts`** — début (la suite Tasks 2-3 ajoutera les tests fallback rendering) :

```typescript
import { splitTextIntoSections } from '../pdf/text-splitter';

describe('splitTextIntoSections', () => {
  it('returns exactly N sections', () => {
    const out = splitTextIntoSections('Phrase un. Phrase deux. Phrase trois. Phrase quatre. Phrase cinq. Phrase six.', 6);
    expect(out).toHaveLength(6);
  });

  it('preserves total content (concat ≈ original)', () => {
    const text = 'Une histoire commence. Le héros marche. Il rencontre quelqu\'un. Il découvre un secret. Il aide. Fin de la quête.';
    const out = splitTextIntoSections(text, 6);
    const reconstructed = out.join(' ').replace(/\s+/g, ' ').trim();
    const normalized = text.replace(/\s+/g, ' ').trim();
    expect(reconstructed).toBe(normalized);
  });

  it('does not break words mid-text', () => {
    const text = 'Le grand renard observe attentivement. Il pense au lapin. Soudain, un oiseau passe. Le ciel devient orange. La nuit tombe. Tout dort.';
    const out = splitTextIntoSections(text, 6);
    for (const s of out) {
      // Pas de section commençant par un fragment de mot lowercase isolé sans majuscule/punct précédente
      expect(s.length).toBeGreaterThan(0);
    }
  });

  it('handles short text (fewer sentences than n) via word fallback', () => {
    const out = splitTextIntoSections('un deux trois quatre cinq six sept huit neuf dix', 6);
    expect(out).toHaveLength(6);
    expect(out.every(s => s.length > 0)).toBe(true);
  });

  it('handles empty text by returning n empty strings', () => {
    expect(splitTextIntoSections('', 6)).toEqual(['', '', '', '', '', '']);
  });

  it('is deterministic (same input → same output)', () => {
    const text = 'Une. Deux. Trois. Quatre. Cinq. Six. Sept.';
    expect(splitTextIntoSections(text, 6)).toEqual(splitTextIntoSections(text, 6));
  });
});
```
  </action>
  <verify>
    <automated>npx jest --no-coverage lib/__tests__/pdf-fallback.test.ts</automated>
  </verify>
  <done>text-splitter.ts exporte splitTextIntoSections, 6 tests passent, pure function déterministe.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 : fallback-double-page.ts (drop cap + ornements + pull quote + vignettes)</name>
  <files>lib/pdf/components/fallback-double-page.ts, lib/__tests__/pdf-fallback.test.ts</files>
  <behavior>
- `renderFallbackDoublePage(opts)` retourne EXACTEMENT 2 sections `<section class="page fallback-page">`
- Page gauche contient : drop cap Caveat 6em #B8593F dans cartoucheFrame SVG (float left), texte Andika 14pt encore après drop cap, vignette haut + numéro de page bas-gauche en cartouche
- Page droite contient : suite du texte, pull quote DM Serif Display italic 1.4em centré encadré separatorTriple, vignette bas + numéro de page bas-droit en cartouche
- Bordure botanique externe variant selon `pageIndex` (alterner borderFern / borderRamage / borderLichen, opacité 0.10)
- Vignettes alternent : pageIndex pair → vignetteCrescent + vignetteForest ; impair → vignetteLantern + vignetteBird
- Toutes les chaînes user passent par `escapeHtml`
- Couleurs hardcodées exactement : #B8593F (terracotta), #7A8F6B (sage), #F4EDE2 (ivory mode B), #2E2A26 (ink)
  </behavior>
  <action>
**Créer `lib/pdf/components/fallback-double-page.ts`** (CONTEXT.md §85-138) :

```typescript
import { escapeHtml } from '../html-template';
import {
  cartoucheFrame, borderFern, borderRamage, borderLichen,
  vignetteCrescent, vignetteForest, vignetteLantern, vignetteBird,
  separatorTriple,
} from '../ornaments';

// Palette ornementale mode B — exacte CONTEXT.md §117-122
const TERRACOTTA = '#B8593F';
const SAGE = '#7A8F6B';
const IVORY = '#F4EDE2';  // palette ornementale CONTEXT.md §117-122 — override BOOK_PALETTE.ivory en mode B
const INK = '#2E2A26';

export interface FallbackDoublePageOpts {
  /** Numéro 1..6 — détermine alternance bordures + vignettes */
  pageIndex: number;
  /** Section de texte à afficher sur cette double-page (issue de splitTextIntoSections) */
  text: string;
  /** Numéro folio gauche (paire) */
  pageNumLeft: number;
  /** Numéro folio droite (impaire) */
  pageNumRight: number;
}

export function renderFallbackDoublePage(opts: FallbackDoublePageOpts): string {
  const { pageIndex, text, pageNumLeft, pageNumRight } = opts;
  const trimmed = text.trim();

  // Drop cap = première lettre, reste = corps
  const firstChar = trimmed.charAt(0) || ' ';
  const restText = trimmed.slice(1);

  // Découper restText en 2 parties : 60% page gauche, 40% page droite + pull quote
  const splitIdx = findWordBoundary(restText, Math.round(restText.length * 0.6));
  const leftBody = restText.slice(0, splitIdx).trim();
  const rightBody = restText.slice(splitIdx).trim();

  // Pull quote = avant-dernière phrase de la double-page (heuristique simple — CONTEXT.md §100-104)
  const pullQuote = extractPullQuote(trimmed);

  // Bordure alternée
  const borderSvg = pickBorder(pageIndex);
  // Vignettes haut/bas
  const { vignetteTop, vignetteBottom } = pickVignettes(pageIndex);

  const pageLeft = `<section class="page fallback-page" data-mode="fallback" data-page-index="${pageIndex}-left" style="background:${IVORY};">
    <div class="border-outer" style="position:absolute; left:0; top:0.82cm; bottom:0.82cm; width:1.5cm; opacity:0.10;">${borderSvg}</div>
    <div class="vignette-top" style="position:absolute; top:0.6cm; left:50%; transform:translateX(-50%);">${vignetteTop}</div>
    <div class="safe-area" style="padding-left:2.2cm;">
      <div class="drop-cap-block" style="float:left; width:3cm; height:3cm; position:relative; margin:0.2em 0.4em 0 0;">
        <div style="position:absolute; inset:0;">${cartoucheFrame({ color: SAGE, size: 100 })}</div>
        <div style="position:absolute; inset:0; display:flex; align-items:center; justify-content:center; font-family:'Caveat', cursive; font-size:6em; line-height:1; color:${TERRACOTTA};">${escapeHtml(firstChar)}</div>
      </div>
      <p class="body-text" style="font-family:'Andika', serif; font-size:14pt; line-height:1.7; color:${INK}; text-align:justify;">${escapeHtml(leftBody)}</p>
    </div>
    <div class="page-num-cartouche" style="position:absolute; bottom:0.5cm; left:50%; transform:translateX(-50%); width:1.6cm; height:1.6cm; display:flex; align-items:center; justify-content:center;">
      <div style="position:absolute; inset:0; opacity:0.6;">${cartoucheFrame({ color: SAGE, size: 60 })}</div>
      <span style="font-family:'Caveat', cursive; font-size:14pt; color:${TERRACOTTA}; position:relative;">${pageNumLeft}</span>
    </div>
  </section>`;

  const pullQuoteBlock = pullQuote
    ? `<div class="pull-quote-wrap" style="text-align:center; padding:1.2em 0;">
        <div style="margin-bottom:0.4em;">${separatorTriple({ color: TERRACOTTA, size: 22 })}</div>
        <div class="pull-quote" style="font-family:'DM Serif Display', serif; font-style:italic; font-size:1.4em; color:${INK}; line-height:1.4;">${escapeHtml(pullQuote)}</div>
        <div style="margin-top:0.4em;">${separatorTriple({ color: TERRACOTTA, size: 22 })}</div>
      </div>`
    : '';

  const pageRight = `<section class="page fallback-page" data-mode="fallback" data-page-index="${pageIndex}-right" style="background:${IVORY};">
    <div class="border-outer" style="position:absolute; right:0; top:0.82cm; bottom:0.82cm; width:1.5cm; opacity:0.10;">${borderSvg}</div>
    <div class="safe-area" style="padding-right:2.2cm;">
      <p class="body-text" style="font-family:'Andika', serif; font-size:14pt; line-height:1.7; color:${INK}; text-align:justify;">${escapeHtml(rightBody)}</p>
      ${pullQuoteBlock}
    </div>
    <div class="vignette-bottom" style="position:absolute; bottom:1.6cm; left:50%; transform:translateX(-50%);">${vignetteBottom}</div>
    <div class="page-num-cartouche" style="position:absolute; bottom:0.5cm; left:50%; transform:translateX(-50%); width:1.6cm; height:1.6cm; display:flex; align-items:center; justify-content:center;">
      <div style="position:absolute; inset:0; opacity:0.6;">${cartoucheFrame({ color: SAGE, size: 60 })}</div>
      <span style="font-family:'Caveat', cursive; font-size:14pt; color:${TERRACOTTA}; position:relative;">${pageNumRight}</span>
    </div>
  </section>`;

  return pageLeft + '\n' + pageRight;
}

function findWordBoundary(text: string, target: number): number {
  if (target <= 0) return 0;
  if (target >= text.length) return text.length;
  // Chercher le prochain espace ≥ target
  const next = text.indexOf(' ', target);
  if (next === -1) return text.length;
  return next;
}

/** Extrait la 2ème ou avant-dernière phrase comme pull quote (CONTEXT.md §101). */
function extractPullQuote(text: string): string | null {
  const sentences = text
    .split(/(?<=[.!?])\s+/)
    .map(s => s.trim())
    .filter(s => s.length > 0);
  if (sentences.length < 2) return null;
  // Préférer avant-dernière si suffisamment longue, sinon 2ème
  const candidate = sentences[sentences.length - 2] ?? sentences[1];
  if (!candidate) return null;
  // Tronquer à 180 chars max pour rester lisible en 1.4em
  return candidate.length > 180 ? candidate.slice(0, 177).trimEnd() + '…' : candidate;
}

function pickBorder(pageIndex: number): string {
  const SAGE = '#7A8F6B';
  const i = ((pageIndex - 1) % 3 + 3) % 3;
  if (i === 0) return borderFern({ color: SAGE, size: 60 });
  if (i === 1) return borderRamage({ color: SAGE, size: 60 });
  return borderLichen({ color: SAGE, size: 60 });
}

function pickVignettes(pageIndex: number): { vignetteTop: string; vignetteBottom: string } {
  const TERRACOTTA = '#B8593F';
  const SAGE = '#7A8F6B';
  if (pageIndex % 2 === 0) {
    return {
      vignetteTop: vignetteCrescent({ color: TERRACOTTA, size: 30 }),
      vignetteBottom: vignetteForest({ color: SAGE, size: 30 }),
    };
  }
  return {
    vignetteTop: vignetteLantern({ color: TERRACOTTA, size: 30 }),
    vignetteBottom: vignetteBird({ color: SAGE, size: 30 }),
  };
}
```

**Étendre `lib/__tests__/pdf-fallback.test.ts`** :

```typescript
import { renderFallbackDoublePage } from '../pdf/components/fallback-double-page';

describe('renderFallbackDoublePage', () => {
  const SAMPLE = 'Il était une fois un loup. Il marchait dans la forêt. Soudain, il vit une lumière. C\'était magique. Le loup s\'approcha. Sa vie changea pour toujours.';

  it('returns exactly 2 page sections', () => {
    const html = renderFallbackDoublePage({ pageIndex: 1, text: SAMPLE, pageNumLeft: 3, pageNumRight: 4 });
    const matches = html.match(/<section class="page fallback-page"/g) || [];
    expect(matches.length).toBe(2);
  });

  it('contains drop cap with first character', () => {
    const html = renderFallbackDoublePage({ pageIndex: 1, text: SAMPLE, pageNumLeft: 3, pageNumRight: 4 });
    expect(html).toContain('drop-cap-block');
    expect(html).toContain('font-size:6em');
    expect(html).toContain('color:#B8593F');  // terracotta exact
  });

  it('uses exact ornamental palette (terracotta/sage/ivory/ink) per CONTEXT.md §117-122', () => {
    const html = renderFallbackDoublePage({ pageIndex: 1, text: SAMPLE, pageNumLeft: 3, pageNumRight: 4 });
    expect(html).toContain('#B8593F');  // terracotta
    expect(html).toContain('#7A8F6B');  // sauge
    expect(html).toContain('#F4EDE2');  // ivoire mode B (override BOOK_PALETTE.ivory générique #FAF6EE)
    expect(html).toContain('#2E2A26');  // encre
  });

  it('includes pull quote with separators when text has ≥ 2 sentences', () => {
    const html = renderFallbackDoublePage({ pageIndex: 1, text: SAMPLE, pageNumLeft: 3, pageNumRight: 4 });
    expect(html).toContain('pull-quote');
    expect(html).toContain('font-family:\'DM Serif Display\'');
  });

  it('shows page numbers in cartouche', () => {
    const html = renderFallbackDoublePage({ pageIndex: 1, text: SAMPLE, pageNumLeft: 3, pageNumRight: 4 });
    expect(html).toContain('page-num-cartouche');
    expect(html).toContain('>3<');  // pageNumLeft inside <span>
    expect(html).toContain('>4<');  // pageNumRight
  });

  it('alternates border ornament by pageIndex', () => {
    const h1 = renderFallbackDoublePage({ pageIndex: 1, text: SAMPLE, pageNumLeft: 3, pageNumRight: 4 });
    const h2 = renderFallbackDoublePage({ pageIndex: 2, text: SAMPLE, pageNumLeft: 5, pageNumRight: 6 });
    const h3 = renderFallbackDoublePage({ pageIndex: 3, text: SAMPLE, pageNumLeft: 7, pageNumRight: 8 });
    // Les SVG bordures sont différents → les strings entières diffèrent
    expect(h1).not.toBe(h2);
    expect(h2).not.toBe(h3);
  });

  it('escapes HTML special chars in text', () => {
    const html = renderFallbackDoublePage({ pageIndex: 1, text: '<script>alert("x")</script> Phrase une. Phrase deux.', pageNumLeft: 3, pageNumRight: 4 });
    expect(html).not.toContain('<script>');
    expect(html).toContain('&lt;script&gt;');
  });
});
```
  </action>
  <verify>
    <automated>npx jest --no-coverage lib/__tests__/pdf-fallback.test.ts</automated>
  </verify>
  <done>fallback-double-page.ts exporte renderFallbackDoublePage, 7 tests rendering passent, palette ornementale exacte hardcodée, ornements de la lib utilisés conformément CONTEXT.md.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3 : Brancher mode B dans renderBookHtml + perf warn + tests intégration</name>
  <files>lib/pdf/html-template.ts, lib/pdf/index.ts, lib/__tests__/pdf-html-template.test.ts, lib/pdf/pdf-generator.ts</files>
  <behavior>
- `renderBookHtml` avec `spec.scenes === null` produit 16 pages : cover (1) + title (1) + 6×renderFallbackDoublePage (12) + end (1) + back-cover (1)
- Texte découpé via splitTextIntoSections(story.texte, 6)
- Test : mode B sans scenes → 6 occurrences de `class="page fallback-page"`
- Test : mode B contient drop cap + bordures ornementales
- Dans pdf-generator.ts, si `perf.totalMs > 5000` ET `__DEV__` → `console.warn('[generateBookPdf] PERF BUDGET DÉPASSÉ : {totalMs}ms > 5000ms')` avec le détail décomposé
  </behavior>
  <action>
**Modifier `lib/pdf/html-template.ts`** — remplacer `renderModeBPlaceholder` par `renderModeBPages` réelle :

```typescript
import { splitTextIntoSections } from './text-splitter';
import { renderFallbackDoublePage } from './components/fallback-double-page';

// Remplacer la fonction renderModeBPlaceholder existante par :
function renderModeBPages(spec: BookHtmlSpec): string {
  const sections = splitTextIntoSections(spec.story.texte, 6);
  const pages: string[] = [];

  // Folio 1 — Cover (renderCoverPage avec coverImageBase64 = null → fallback paperShadow)
  pages.push(renderCoverPage({
    story: spec.story,
    coverImageBase64: spec.illustrations.get('paysage') ?? null,
    palette: spec.palette,
  }));

  // Folio 2 — Page de titre
  pages.push(renderTitlePage({
    story: spec.story,
    palette: spec.palette,
    tomeBadge: spec.tomeBadge,
  }));

  // Folios 3-14 — 6 doubles-pages fallback ornementales
  let pageNum = 3;
  for (let i = 0; i < 6; i++) {
    pages.push(renderFallbackDoublePage({
      pageIndex: i + 1,
      text: sections[i] ?? '',
      pageNumLeft: pageNum,
      pageNumRight: pageNum + 1,
    }));
    pageNum += 2;
  }

  // Folio 15 — Page Fin
  pages.push(renderEndPage(spec));

  // Folio 16 — Back cover
  pages.push(renderBackCoverPage({ story: spec.story, palette: spec.palette }));

  return pages.join('\n');
}

// Dans renderBookHtml, remplacer l'appel renderModeBPlaceholder par renderModeBPages :
// const pages = spec.scenes ? renderModeAPages(spec) : renderModeBPages(spec);
```

**Modifier `lib/pdf/pdf-generator.ts`** — ajouter le warn si budget dépassé :

```typescript
if (__DEV__) {
  console.log(
    `[generateBookPdf] ${opts.story.id} — ${totalMs}ms — ` +
    `assets:${assetsMs}ms render:${renderMs}ms hash:${hashMs}ms print:${printMs}ms`,
  );
  if (totalMs > 5000) {
    console.warn(
      `[generateBookPdf] PERF BUDGET DÉPASSÉ : ${totalMs}ms > 5000ms — ` +
      `assets:${assetsMs}ms render:${renderMs}ms hash:${hashMs}ms print:${printMs}ms — ` +
      `inspecter le bottleneck dominant (cible RESEARCH.md §666-694).`,
    );
  }
}
```

**Mettre à jour `lib/pdf/index.ts`** :

```typescript
export { splitTextIntoSections } from './text-splitter';
export { renderFallbackDoublePage } from './components/fallback-double-page';
export type { FallbackDoublePageOpts } from './components/fallback-double-page';
```

**Étendre `lib/__tests__/pdf-html-template.test.ts`** — ajouter un bloc mode B end-to-end :

```typescript
describe('renderBookHtml mode B (fallback, no scenes)', () => {
  const STORY_LONG_TEXT = {
    ...FAKE_STORY,
    texte: 'Il était une fois. Le héros marche. Il rencontre. Il découvre. Il aide. Il revient.'.repeat(10),
  };

  it('produces exactly 16 pages structure', () => {
    const html = renderBookHtml({
      story: STORY_LONG_TEXT, scenes: null, illustrations: new Map(),
      fonts: FAKE_FONTS, palette: BOOK_PALETTE, tomeBadge: null,
    });
    const matches = html.match(/<section class="page/g) || [];
    expect(matches.length).toBe(16);
  });

  it('contains 6 fallback double-pages (12 fallback page sections)', () => {
    const html = renderBookHtml({
      story: STORY_LONG_TEXT, scenes: null, illustrations: new Map(),
      fonts: FAKE_FONTS, palette: BOOK_PALETTE, tomeBadge: null,
    });
    const fallbackMatches = html.match(/class="page fallback-page"/g) || [];
    expect(fallbackMatches.length).toBe(12);
  });

  it('uses ornamental palette (terracotta + sage)', () => {
    const html = renderBookHtml({
      story: STORY_LONG_TEXT, scenes: null, illustrations: new Map(),
      fonts: FAKE_FONTS, palette: BOOK_PALETTE, tomeBadge: null,
    });
    expect(html).toContain('#B8593F');  // terracotta
    expect(html).toContain('#7A8F6B');  // sage
  });

  it('contains exactly 6 drop-cap-block (one per fallback double-page)', () => {
    const html = renderBookHtml({
      story: STORY_LONG_TEXT, scenes: null, illustrations: new Map(),
      fonts: FAKE_FONTS, palette: BOOK_PALETTE, tomeBadge: null,
    });
    expect(html).toContain('drop-cap-block');
    // Un drop cap par double-page × 6 doubles-pages = 6 drop caps
    expect(html.match(/drop-cap-block/g)?.length).toBe(6);
  });

  it('does not contain data-mode="fallback" placeholder anymore (Plan 49-04 replaces stub)', () => {
    const html = renderBookHtml({
      story: STORY_LONG_TEXT, scenes: null, illustrations: new Map(),
      fonts: FAKE_FONTS, palette: BOOK_PALETTE, tomeBadge: null,
    });
    // Les sections fallback réelles ont data-mode="fallback" data-page-index="X-left|right"
    // Le stub vide avait data-page-index="N" sans -left/-right
    const stubPattern = /<section class="page" data-mode="fallback"/;
    expect(html).not.toMatch(stubPattern);
  });

  it('mode A still works after Plan 49-04 changes (regression check)', () => {
    const SCENES = ['paysage','rencontre','decouverte','vulnerable','echange','etreinte'].map((a, i) => ({
      panelIndex: i + 1, archetype: a as any, textStart: i * 10, textEnd: (i + 1) * 10, highlights: [],
    }));
    const ILLUS = new Map<any, string>(SCENES.map(s => [s.archetype, `B64_${s.archetype}`]));
    const html = renderBookHtml({
      story: { ...FAKE_STORY, texte: 'x'.repeat(700) }, scenes: SCENES,
      illustrations: ILLUS, fonts: FAKE_FONTS, palette: BOOK_PALETTE, tomeBadge: null,
    });
    expect(html).toContain('data-archetype="paysage"');
    expect(html).not.toContain('drop-cap-block');  // mode A n'utilise pas drop cap
  });
});
```

⚠️ Le test "does not contain stub" peut nécessiter ajustement selon la regex exacte — l'objectif est de vérifier que les sections fallback **réelles** (avec drop cap, bordures, vignettes) remplacent le stub vide. Si la regex est ambiguë, valider plutôt par `expect(html).toContain('drop-cap-block')` qui prouve le rendu réel.
  </action>
  <verify>
    <automated>npx jest --no-coverage lib/__tests__/pdf-html-template.test.ts lib/__tests__/pdf-fallback.test.ts && npx tsc --noEmit 2>&1 | grep "lib/pdf/" || echo "TS OK"</automated>
  </verify>
  <done>html-template.ts mode B branché sur renderFallbackDoublePage 6×, splitTextIntoSections utilisé, perf warn ajouté dans pdf-generator.ts, lib/pdf/index.ts ré-exporte text-splitter + fallback component, tous les tests passent (mode A non régressé, mode B 6 nouveaux tests).</done>
</task>

</tasks>

<verification>
- `npx jest --no-coverage` : suite complète verte (existants + 6 splitter + 7 fallback rendering + 6 mode B integration)
- `npx tsc --noEmit` ne régresse pas
- `grep -c "page fallback-page" lib/pdf/components/fallback-double-page.ts` ≥ 2 (left + right)
- `grep "PERF BUDGET DÉPASSÉ" lib/pdf/pdf-generator.ts` retrouve le warn
- `grep "splitTextIntoSections" lib/pdf/html-template.ts` montre l'usage en mode B
- Manuel device (post-merge Phase 49) : `npx expo run:ios --device` + déclencher un export d'histoire SANS scenes → vérifier visuellement le PDF rendu
</verification>

<success_criteria>
- 2 nouveaux fichiers source (`text-splitter.ts`, `fallback-double-page.ts`) + 1 test (`pdf-fallback.test.ts`) + extensions
- Mode B livre la "vision ambitieuse" CONTEXT.md §85-138 : drops cartouchés Caveat 6em terracotta, bordures botaniques alternées, pull quotes DM Serif Display italic encadrés, vignettes haut/bas, numéros cartouchés, palette exacte
- splitTextIntoSections produit toujours exactement 6 sections (pure fonction)
- Perf instrumentée avec warn si > 5s
- Aucune régression mode A
- Suite Jest complète verte (≥ 30 tests Phase 49 cumulés)
</success_criteria>

<output>
Après completion, créer `.planning/phases/49-layout-livre-generation-pdf/49-04-SUMMARY.md` documentant :
- Mode B pattern complet livré (drops, bordures, pull quotes, vignettes, numéros)
- Comptage tests Jest Phase 49 total
- Points de validation device post-merge (ouvrir 1 PDF mode A + 1 PDF mode B dans Aperçu macOS, vérifier 16 pages, 21.64×21.64 cm, polices Andika listées dans Inspecteur)
- Phase 49 prête pour merge / TestFlight ; wiring UI bouton export = Phase 51
</output>
