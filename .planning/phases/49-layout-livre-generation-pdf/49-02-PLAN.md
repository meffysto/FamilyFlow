---
phase: 49-layout-livre-generation-pdf
plan: 02
type: execute
wave: 2
depends_on: [49-01]
files_modified:
  - lib/pdf/components/cover.ts
  - lib/pdf/components/title.ts
  - lib/pdf/components/scene-double-page.ts
  - lib/pdf/components/back-cover.ts
  - lib/pdf/html-template.ts
  - lib/pdf/index.ts
  - lib/__tests__/pdf-html-template.test.ts
  - lib/__tests__/pdf-scene-double-page.test.ts
autonomous: true
requirements: [LAY-01, LAY-02, LAY-06]
must_haves:
  truths:
    - "renderBookHtml en mode A (avec scenes 6) produit exactement 16 sections .page avec contenu structuré (cover + title + 12 scene-double-page + fin + back-cover)"
    - "Couverture utilise illustration paysage en full-bleed avec overlay titre"
    - "Page de titre affiche titre, 'Pour {enfant}', date et badge tome si tomeBadge non null"
    - "Chaque scène = double-page (page paire = illustration full-bleed PNG base64 / page impaire = texte de la scène en safe-area Andika 14pt)"
    - "4ème de couverture contient placeholder QR (Phase 50) + résumé court + 'FamilyVault — Histoires du soir'"
  artifacts:
    - path: "lib/pdf/components/cover.ts"
      provides: "renderCoverPage(opts): string"
    - path: "lib/pdf/components/title.ts"
      provides: "renderTitlePage(opts): string"
    - path: "lib/pdf/components/scene-double-page.ts"
      provides: "renderSceneDoublePage(scene, story, illustrationBase64, palette): string (retourne 2 sections .page)"
    - path: "lib/pdf/components/back-cover.ts"
      provides: "renderBackCoverPage(opts): string"
  key_links:
    - from: "lib/pdf/html-template.ts"
      to: "lib/pdf/components/*"
      via: "import + appel quand spec.scenes !== null (mode A)"
    - from: "renderSceneDoublePage"
      to: "spec.illustrations Map<archetype, base64>"
      via: "lookup archetype scene.archetype"
    - from: "title.ts"
      to: "spec.tomeBadge"
      via: "rendu conditionnel `Tome ${current} sur ${total} du livre ${livreTitre}`"
---

<objective>
Construire les 4 composants layout du **Mode A picture-book** : couverture, page de titre, scène double-page, 4ème de couverture. Brancher dans `renderBookHtml` pour qu'en présence de `spec.scenes` (6 scènes), le template produise un livre structuré 16 pages.

Purpose : c'est le rendu visuel principal pour l'univers `foret` (MVP). Sans ces composants, le PDF n'a que des pages vides.

Output : 4 nouveaux fichiers `lib/pdf/components/*.ts` + `html-template.ts` mis à jour pour brancher mode A + tests Jest étendus.

Mode B (fallback texte-seul ornemental) reste stub dans ce plan — détaillé Plan 49-04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/49-layout-livre-generation-pdf/49-CONTEXT.md
@.planning/phases/49-layout-livre-generation-pdf/49-RESEARCH.md
@.planning/phases/49-layout-livre-generation-pdf/49-01-SUMMARY.md
@lib/pdf/html-template.ts
@lib/pdf/ornaments.ts
@lib/pdf/index.ts
@lib/types.ts

<interfaces>
<!-- Disponible depuis Plan 49-01 -->

From lib/pdf/html-template.ts:
```typescript
export interface BookHtmlSpec {
  story: BedtimeStory;
  scenes: SceneSpec[] | null;
  illustrations: Map<SceneArchetype, string>;
  fonts: { andikaRegular: string; andikaBold: string };
  palette: BookPalette;
  tomeBadge: { current: number; total: number; livreTitre: string } | null;
}
export function renderBookHtml(spec: BookHtmlSpec): string;
export function escapeHtml(s: string): string;
```

From lib/pdf/ornaments.ts:
```typescript
export function vignetteCrescent(opts?: OrnamentOpts): string;
export function vignetteForest(opts?: OrnamentOpts): string;
export function separatorTriple(opts?: OrnamentOpts): string;
// + 7 autres
```

From lib/types.ts (déjà exposé Plan 49-01) :
```typescript
export interface SceneSpec {
  panelIndex: number;
  archetype: SceneArchetype;
  textStart: number;
  textEnd: number;
  highlights: HighlightSpan[];
}
export interface BedtimeStory { ... titre, enfant, texte, date, livreTitre?, ...; }
```

Convention extraction texte de scène (lib/types.ts:917-920) :
- `story.texte.slice(scene.textStart, scene.textEnd)` retourne le texte exact de la scène

Structure 16 pages mode A (RESEARCH.md §425-431 + CONTEXT.md ligne 60) :
| Folio | Type |
|-------|------|
| 1 | Cover (illustration paysage full-bleed + titre overlay) |
| 2 | Page de titre |
| 3-4 | Double-page scène 1 (illustration / texte) |
| 5-6 | Double-page scène 2 |
| 7-8 | Double-page scène 3 |
| 9-10 | Double-page scène 4 |
| 11-12 | Double-page scène 5 |
| 13-14 | Double-page scène 6 |
| 15 | Page Fin / dédicace |
| 16 | 4ème couverture |
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 : Composants cover.ts + title.ts + back-cover.ts</name>
  <files>lib/pdf/components/cover.ts, lib/pdf/components/title.ts, lib/pdf/components/back-cover.ts</files>
  <behavior>
- `renderCoverPage` retourne UNE `<section class="page cover">` qui contient l'image base64 paysage en full-bleed + overlay titre + univers en sous-titre
- `renderTitlePage` retourne UNE `<section class="page title-page">` avec : titre principal (DM Serif Display), `Pour ${enfant}`, date FR (JJ/MM/AAAA), et SI `tomeBadge` non null : `Tome ${current} sur ${total} du livre ${livreTitre}` (Caveat italic)
- `renderBackCoverPage` retourne UNE `<section class="page back-cover">` avec placeholder QR `<div class="qr-placeholder" data-phase50></div>` (3×3cm centré bas), résumé court (memorySummary tronqué 250 chars max ou texte par défaut), label "FamilyVault — Histoires du soir"
- Toutes les chaînes user (titre, enfant, livreTitre, memorySummary) passent par `escapeHtml`
  </behavior>
  <action>
**Créer `lib/pdf/components/cover.ts`** :

```typescript
import type { BedtimeStory } from '../../types';
import type { BookPalette } from '../types';
import { escapeHtml } from '../html-template';

export interface CoverPageOpts {
  story: BedtimeStory;
  coverImageBase64: string | null;  // illustration 'paysage' base64 (mode A)
  palette: BookPalette;
}

export function renderCoverPage(opts: CoverPageOpts): string {
  const { story, coverImageBase64, palette } = opts;
  const universLabel = capitalizeFirst(story.univers);

  const fullBleedImg = coverImageBase64
    ? `<img class="full-bleed scene-illustration" src="data:image/png;base64,${coverImageBase64}" alt="" />`
    : `<div class="full-bleed" style="background:${palette.paperShadow};"></div>`;

  return `<section class="page cover">
    ${fullBleedImg}
    <div class="cover-overlay" style="position:absolute; inset:0; display:flex; flex-direction:column; justify-content:flex-end; padding:1.5cm;">
      <h1 class="cover-title" style="font-family:'DM Serif Display', serif; font-size:48pt; color:${palette.ivory}; text-shadow:0 2px 8px rgba(0,0,0,0.6); line-height:1.05; margin-bottom:0.3cm;">${escapeHtml(story.titre)}</h1>
      <div class="cover-univers" style="font-family:'Caveat', cursive; font-size:22pt; color:${palette.ivory}; opacity:0.9;">${escapeHtml(universLabel)}</div>
    </div>
  </section>`;
}

function capitalizeFirst(s: string): string {
  return s.charAt(0).toUpperCase() + s.slice(1);
}
```

**Créer `lib/pdf/components/title.ts`** :

```typescript
import type { BedtimeStory } from '../../types';
import type { BookPalette } from '../types';
import { escapeHtml } from '../html-template';
import { separatorTriple } from '../ornaments';

export interface TitlePageOpts {
  story: BedtimeStory;
  palette: BookPalette;
  tomeBadge: { current: number; total: number; livreTitre: string } | null;
}

export function renderTitlePage(opts: TitlePageOpts): string {
  const { story, palette, tomeBadge } = opts;
  const dateFr = formatDateFr(story.date);
  const tomeBlock = tomeBadge
    ? `<div class="tome-badge" style="font-family:'Caveat', cursive; font-style:italic; font-size:18pt; color:${palette.terracotta}; margin-top:1.5cm;">Tome ${tomeBadge.current} sur ${tomeBadge.total} du livre ${escapeHtml(tomeBadge.livreTitre)}</div>`
    : '';

  return `<section class="page title-page">
    <div class="safe-area" style="display:flex; flex-direction:column; justify-content:center; align-items:center; text-align:center;">
      <div style="margin-bottom:1cm;">${separatorTriple({ color: palette.terracotta, size: 28 })}</div>
      <h1 style="font-family:'DM Serif Display', serif; font-size:42pt; color:${palette.ink}; line-height:1.1; margin-bottom:0.8cm;">${escapeHtml(story.titre)}</h1>
      <div style="font-family:'Caveat', cursive; font-size:22pt; color:${palette.ink};">Pour ${escapeHtml(story.enfant)}</div>
      <div style="font-family:'Andika', serif; font-size:12pt; color:${palette.ink}; opacity:0.7; margin-top:0.6cm;">${escapeHtml(dateFr)}</div>
      ${tomeBlock}
      <div style="margin-top:2cm;">${separatorTriple({ color: palette.sage, size: 22 })}</div>
    </div>
  </section>`;
}

/** YYYY-MM-DD → JJ/MM/AAAA (CLAUDE.md convention). */
function formatDateFr(iso: string): string {
  const [y, m, d] = iso.split('-');
  if (!y || !m || !d) return iso;
  return `${d}/${m}/${y}`;
}
```

**Créer `lib/pdf/components/back-cover.ts`** :

```typescript
import type { BedtimeStory } from '../../types';
import type { BookPalette } from '../types';
import { escapeHtml } from '../html-template';

export interface BackCoverOpts {
  story: BedtimeStory;
  palette: BookPalette;
}

export function renderBackCoverPage(opts: BackCoverOpts): string {
  const { story, palette } = opts;
  const summary = (story.memorySummary && story.memorySummary.trim().length > 0)
    ? truncate(story.memorySummary, 280)
    : 'Une histoire du soir, à relire ensemble, encore et encore.';

  return `<section class="page back-cover">
    <div class="safe-area" style="display:flex; flex-direction:column; justify-content:space-between;">
      <div class="back-summary" style="font-family:'Andika', serif; font-size:13pt; line-height:1.55; color:${palette.ink}; padding:2cm 1cm 0 1cm; text-align:center;">${escapeHtml(summary)}</div>
      <div style="display:flex; flex-direction:column; align-items:center; padding-bottom:1cm;">
        <div class="qr-placeholder" data-phase50 style="width:3cm; height:3cm; border:1px dashed ${palette.sage}; display:flex; align-items:center; justify-content:center; font-family:'Caveat', cursive; font-size:10pt; color:${palette.sage}; margin-bottom:0.6cm;">QR — Phase 50</div>
        <div style="font-family:'Caveat', cursive; font-size:14pt; color:${palette.terracotta};">FamilyVault — Histoires du soir</div>
      </div>
    </div>
  </section>`;
}

function truncate(s: string, max: number): string {
  if (s.length <= max) return s;
  return s.slice(0, max - 1).trimEnd() + '…';
}
```

⚠️ Important :
- Toutes les chaînes user-supplied passent par `escapeHtml` (Pitfall XSS dans WKWebView)
- Pas de `file://` URLs (RESEARCH.md §741-745) — uniquement `data:image/png;base64,${...}`
- Si `coverImageBase64` est `null` (pas d'illustration print pour cet univers, futur cas non-forêt), fallback bloc plein avec `paperShadow` — la cover reste visuellement valide
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "lib/pdf/components/(cover|title|back-cover)" || echo "TS OK"</automated>
  </verify>
  <done>3 fichiers créés, exportent les fonctions `renderCoverPage`/`renderTitlePage`/`renderBackCoverPage` avec leurs interfaces typées, escapeHtml appliqué partout, pas d'URLs file://, types compilent.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 : Composant scene-double-page.ts (illustration + texte avec highlights)</name>
  <files>lib/pdf/components/scene-double-page.ts, lib/__tests__/pdf-scene-double-page.test.ts</files>
  <behavior>
- `renderSceneDoublePage(opts)` retourne EXACTEMENT deux `<section class="page">` concaténées
- Page paire = illustration full-bleed PNG base64 (key archetype lookup dans illustrations Map). Si pas d'illustration → bloc paperShadow + emoji univers (fallback non-forêt MVP)
- Page impaire = texte de la scène en safe-area, Andika 14pt, line-height 1.6, color ink. Highlights wrappés dans `<span class="highlight" style="color:#0F8B8D">...</span>` aux indices `startChar/endChar` (relatifs au texte de la scène, lib/types.ts:917-920)
- Chaque page contient un numéro de page en cartouche (Caveat 1em terracotta) — left pour pages paires, right pour pages impaires
  </behavior>
  <action>
**Créer `lib/pdf/components/scene-double-page.ts`** :

```typescript
import type { BedtimeStory, SceneSpec, HighlightSpan } from '../../types';
import type { BookPalette } from '../types';
import { escapeHtml } from '../html-template';

const HIGHLIGHT_COLOR = '#0F8B8D';  // teal — cohérent avec lib/types.ts:903 commentaire

export interface SceneDoublePageOpts {
  scene: SceneSpec;
  story: BedtimeStory;
  illustrationBase64: string | null;  // null si univers non-illustré
  palette: BookPalette;
  pageNumLeft: number;   // numéro affiché page paire (illustration)
  pageNumRight: number;  // numéro affiché page impaire (texte)
}

export function renderSceneDoublePage(opts: SceneDoublePageOpts): string {
  const { scene, story, illustrationBase64, palette, pageNumLeft, pageNumRight } = opts;

  // Extraire texte de la scène
  const sceneText = story.texte.slice(scene.textStart, scene.textEnd);

  // Page paire — illustration full-bleed
  const illustrationBlock = illustrationBase64
    ? `<img class="full-bleed scene-illustration" src="data:image/png;base64,${illustrationBase64}" alt="" />`
    : `<div class="full-bleed" style="background:${palette.paperShadow}; display:flex; align-items:center; justify-content:center; font-family:'DM Serif Display', serif; font-size:48pt; color:${palette.sage};">${escapeHtml(scene.archetype)}</div>`;

  const leftPage = `<section class="page scene-illustration-page" data-archetype="${scene.archetype}">
    ${illustrationBlock}
    <div class="page-num left" style="position:absolute; bottom:0.6cm; left:0.82cm; font-family:'Caveat', cursive; font-size:12pt; color:${palette.terracotta}; background:${palette.ivory}; padding:0.1cm 0.3cm; border-radius:0.2cm;">${pageNumLeft}</div>
  </section>`;

  // Page impaire — texte avec highlights
  const textHtml = renderTextWithHighlights(sceneText, scene.highlights);

  const rightPage = `<section class="page scene-text-page" data-archetype="${scene.archetype}">
    <div class="safe-area" style="display:flex; align-items:center;">
      <p class="body-text" style="font-family:'Andika', serif; font-size:14pt; line-height:1.7; color:${palette.ink}; text-align:justify;">${textHtml}</p>
    </div>
    <div class="page-num right" style="position:absolute; bottom:0.6cm; right:0.82cm; font-family:'Caveat', cursive; font-size:12pt; color:${palette.terracotta};">${pageNumRight}</div>
  </section>`;

  return leftPage + '\n' + rightPage;
}

/**
 * Découpe le texte en segments {plain, highlight} selon les spans, escape chaque
 * segment, et concatène avec des <span class="highlight"> autour des highlights.
 * Les indices sont RELATIFS au texte de la scène (lib/types.ts:920).
 */
function renderTextWithHighlights(text: string, highlights: HighlightSpan[]): string {
  if (!highlights || highlights.length === 0) {
    return escapeHtml(text);
  }

  // Trier par startChar pour merger en un pass
  const sorted = [...highlights].filter(h => h.endChar > h.startChar).sort((a, b) => a.startChar - b.startChar);

  const parts: string[] = [];
  let cursor = 0;
  for (const span of sorted) {
    const start = Math.max(cursor, Math.min(span.startChar, text.length));
    const end = Math.max(start, Math.min(span.endChar, text.length));
    if (start > cursor) parts.push(escapeHtml(text.slice(cursor, start)));
    if (end > start) {
      parts.push(`<span class="highlight" style="color:${HIGHLIGHT_COLOR}; font-weight:700;">${escapeHtml(text.slice(start, end))}</span>`);
    }
    cursor = end;
  }
  if (cursor < text.length) parts.push(escapeHtml(text.slice(cursor)));
  return parts.join('');
}
```

**Notes implémentation** :
- Le slicing `story.texte.slice(scene.textStart, scene.textEnd)` est stable et déterministe (RESEARCH.md §503-509 condition #1 hash)
- `renderTextWithHighlights` doit être déterministe — tri stable (Array.sort est stable depuis ES2019, OK pour Hermes/RN 0.81)
- Highlights overlapping/dégénérés (start >= end, hors range) sont silencieusement ignorés
- Pas de `Date.now()` ni `Math.random()` — pure function

**Créer `lib/__tests__/pdf-scene-double-page.test.ts`** — tests Jest dédiés au composant scene-double-page (couverture isolée, indépendamment du test d'intégration `pdf-html-template.test.ts`) :

```typescript
import { renderSceneDoublePage } from '../pdf/components/scene-double-page';
import { BOOK_PALETTE } from '../pdf/constants';
import type { BedtimeStory, SceneSpec } from '../types';

const FAKE_STORY: BedtimeStory = {
  id: 'foret-test', titre: 'Le Loup', enfant: 'Lucas', enfantId: 'lucas',
  univers: 'foret',
  texte: 'Il était une fois un loup. Il rencontra un renard. Ensemble, ils découvrirent une clairière. Soudain, un orage éclata. Ils s\'entraidèrent. Ils rentrèrent à la maison.',
  date: '2026-05-04', duree_lecture: 300,
  voice: { engine: 'expo-speech', language: 'fr' }, version: 1,
  sourceFile: '09 - Histoires/Lucas/2026-05-04-foret.md',
};

const SCENE_PAYSAGE: SceneSpec = {
  panelIndex: 1, archetype: 'paysage', textStart: 0, textEnd: 30, highlights: [],
};

describe('renderSceneDoublePage', () => {
  it('produces exactly 2 page sections (gauche illustration + droite texte)', () => {
    const html = renderSceneDoublePage({
      scene: SCENE_PAYSAGE, story: FAKE_STORY,
      illustrationBase64: 'PAYSAGE_B64', palette: BOOK_PALETTE,
      pageNumLeft: 3, pageNumRight: 4,
    });
    const matches = html.match(/<section class="page/g) || [];
    expect(matches.length).toBe(2);
    expect(html).toContain('scene-illustration-page');
    expect(html).toContain('scene-text-page');
  });

  it('slices texte exactement entre textStart et textEnd', () => {
    const scene: SceneSpec = { panelIndex: 2, archetype: 'rencontre', textStart: 27, textEnd: 51, highlights: [] };
    const html = renderSceneDoublePage({
      scene, story: FAKE_STORY, illustrationBase64: 'RENCONTRE_B64',
      palette: BOOK_PALETTE, pageNumLeft: 5, pageNumRight: 6,
    });
    const expected = FAKE_STORY.texte.slice(27, 51);
    expect(html).toContain(expected);
    // Ne doit PAS contenir le début (slice 0..30)
    expect(html).not.toContain('Il était une fois un loup');
  });

  it('rend les highlights teal #0F8B8D avec font-weight bold', () => {
    const scene: SceneSpec = {
      panelIndex: 3, archetype: 'decouverte', textStart: 0, textEnd: 30,
      highlights: [{ startChar: 16, endChar: 20, kind: 'keyword' as const }],
    };
    const html = renderSceneDoublePage({
      scene, story: FAKE_STORY, illustrationBase64: 'DECOUVERTE_B64',
      palette: BOOK_PALETTE, pageNumLeft: 7, pageNumRight: 8,
    });
    expect(html).toContain('class="highlight"');
    expect(html).toContain('#0F8B8D');
    expect(html).toContain('font-weight:700');
  });

  it('utilise paperShadow comme fallback si pas d\'illustration (univers non-forêt)', () => {
    const html = renderSceneDoublePage({
      scene: SCENE_PAYSAGE, story: FAKE_STORY,
      illustrationBase64: null, palette: BOOK_PALETTE,
      pageNumLeft: 3, pageNumRight: 4,
    });
    expect(html).toContain(BOOK_PALETTE.paperShadow);  // #E8E0D0
    expect(html).not.toContain('data:image/png;base64');
  });

  it('injecte data-archetype sur les 2 pages', () => {
    const html = renderSceneDoublePage({
      scene: { ...SCENE_PAYSAGE, archetype: 'etreinte' }, story: FAKE_STORY,
      illustrationBase64: 'ETREINTE_B64', palette: BOOK_PALETTE,
      pageNumLeft: 13, pageNumRight: 14,
    });
    const archetypeMatches = html.match(/data-archetype="etreinte"/g) || [];
    expect(archetypeMatches.length).toBe(2);  // un par page de la double-page
  });

  it('place numéro page gauche à gauche et droit à droite', () => {
    const html = renderSceneDoublePage({
      scene: SCENE_PAYSAGE, story: FAKE_STORY,
      illustrationBase64: 'PAYSAGE_B64', palette: BOOK_PALETTE,
      pageNumLeft: 9, pageNumRight: 10,
    });
    expect(html).toContain('page-num left');
    expect(html).toContain('page-num right');
    expect(html).toContain('>9<');
    expect(html).toContain('>10<');
  });

  it('ignore highlights dégénérés (endChar <= startChar) sans crash', () => {
    const scene: SceneSpec = {
      panelIndex: 1, archetype: 'paysage', textStart: 0, textEnd: 30,
      highlights: [{ startChar: 10, endChar: 5, kind: 'keyword' as const }],
    };
    const html = renderSceneDoublePage({
      scene, story: FAKE_STORY, illustrationBase64: null,
      palette: BOOK_PALETTE, pageNumLeft: 3, pageNumRight: 4,
    });
    expect(html).not.toContain('class="highlight"');
  });
});
```
  </action>
  <verify>
    <automated>npx jest --no-coverage lib/__tests__/pdf-scene-double-page.test.ts && npx tsc --noEmit 2>&1 | grep "lib/pdf/components/scene-double-page" || echo "TS OK"</automated>
  </verify>
  <done>Fichier `scene-double-page.ts` créé + test Jest dédié `pdf-scene-double-page.test.ts` (≥7 tests) passent : 2 sections produites, slicing textStart/textEnd, highlights teal, fallback paperShadow, data-archetype sur les 2 pages.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3 : Brancher les composants dans renderBookHtml + tests Jest étendus</name>
  <files>lib/pdf/html-template.ts, lib/pdf/index.ts, lib/__tests__/pdf-html-template.test.ts</files>
  <behavior>
- En présence de `spec.scenes` (length 6), `renderBookHtml` produit 16 pages structurées : cover (1), title (1), 6×scene-double-page (12), end (1), back-cover (1)
- En absence de scenes (`null`), renderBookHtml produit toujours 16 pages mais avec stubs (mode B sera détaillé Plan 49-04 — laisser placeholder identifiable `data-mode="fallback"`)
- Strict refus si `spec.scenes` est non-null mais `length !== 6` → throw `Error("L'histoire doit avoir exactement 6 scènes pour être imprimée. Édite le sidecar .scenes.json.")` (CONTEXT.md ligne 82)
- Tests Jest couvrent : présence cover-title, présence titre escapé, badge tome conditionnel, 6 scenes → 12 pages scene + 4 autres = 16, refus 5 scènes
  </behavior>
  <action>
**Modifier `lib/pdf/html-template.ts`** — remplacer `renderPagesStub` par le branchement réel mode A.

```typescript
import { renderCoverPage } from './components/cover';
import { renderTitlePage } from './components/title';
import { renderSceneDoublePage } from './components/scene-double-page';
import { renderBackCoverPage } from './components/back-cover';

// Conserver renderCss et renderBookHtml signature inchangée.

export function renderBookHtml(spec: BookHtmlSpec): string {
  const css = renderCss(spec.palette, spec.fonts);
  const pages = spec.scenes
    ? renderModeAPages(spec)
    : renderModeBPlaceholder(spec);  // détaillé Plan 49-04
  return `<!DOCTYPE html>
<html lang="fr">
<head>
<meta charset="UTF-8" />
<title>${escapeHtml(spec.story.titre)}</title>
<style>${css}</style>
</head>
<body>
${pages}
</body>
</html>`;
}

function renderModeAPages(spec: BookHtmlSpec): string {
  const scenes = spec.scenes!;
  if (scenes.length !== 6) {
    throw new Error("L'histoire doit avoir exactement 6 scènes pour être imprimée. Édite le sidecar .scenes.json.");
  }

  const coverImg = spec.illustrations.get('paysage') ?? null;
  const pages: string[] = [];

  // Folio 1 — Cover
  pages.push(renderCoverPage({ story: spec.story, coverImageBase64: coverImg, palette: spec.palette }));

  // Folio 2 — Page de titre
  pages.push(renderTitlePage({ story: spec.story, palette: spec.palette, tomeBadge: spec.tomeBadge }));

  // Folios 3-14 — 6 doubles-pages scènes
  let pageNum = 3;
  for (const scene of scenes) {
    const illu = spec.illustrations.get(scene.archetype) ?? null;
    pages.push(renderSceneDoublePage({
      scene,
      story: spec.story,
      illustrationBase64: illu,
      palette: spec.palette,
      pageNumLeft: pageNum,
      pageNumRight: pageNum + 1,
    }));
    pageNum += 2;
  }

  // Folio 15 — Page Fin / dédicace
  pages.push(renderEndPage(spec));

  // Folio 16 — 4ème de couverture
  pages.push(renderBackCoverPage({ story: spec.story, palette: spec.palette }));

  return pages.join('\n');
}

function renderEndPage(spec: BookHtmlSpec): string {
  const { palette, story } = spec;
  const memory = story.memorySummary && story.memorySummary.trim().length > 0
    ? `<div style="font-family:'Andika', serif; font-size:12pt; line-height:1.6; color:${palette.ink}; max-width:14cm; text-align:center; margin-top:1.5cm;">${escapeHtml(story.memorySummary)}</div>`
    : '';
  return `<section class="page end-page">
    <div class="safe-area" style="display:flex; flex-direction:column; justify-content:center; align-items:center;">
      <div style="font-family:'DM Serif Display', serif; font-size:60pt; color:${palette.terracotta};">Fin.</div>
      ${memory}
    </div>
  </section>`;
}

/** Mode B placeholder — détaillé Plan 49-04. Produit 16 sections vides identifiables. */
function renderModeBPlaceholder(_spec: BookHtmlSpec): string {
  const arr: string[] = [];
  for (let i = 0; i < 16; i++) {
    arr.push(`<section class="page" data-mode="fallback" data-page-index="${i + 1}"><div class="safe-area"></div></section>`);
  }
  return arr.join('\n');
}
```

**Mettre à jour `lib/pdf/index.ts`** :

```typescript
// Ajouter aux exports :
export { renderCoverPage } from './components/cover';
export { renderTitlePage } from './components/title';
export { renderSceneDoublePage } from './components/scene-double-page';
export { renderBackCoverPage } from './components/back-cover';
```

**Étendre `lib/__tests__/pdf-html-template.test.ts`** — ajouter un bloc `describe('renderBookHtml mode A')`.

```typescript
import type { SceneSpec } from '../types';

const FAKE_SCENES: SceneSpec[] = ['paysage','rencontre','decouverte','vulnerable','echange','etreinte'].map((archetype, i) => ({
  panelIndex: i + 1,
  archetype: archetype as SceneSpec['archetype'],
  textStart: i * 100,
  textEnd: (i + 1) * 100,
  highlights: [],
}));

const FAKE_ILLUS = new Map<SceneSpec['archetype'], string>([
  ['paysage', 'PAYSAGE_B64'],
  ['rencontre', 'RENCONTRE_B64'],
  ['decouverte', 'DECOUVERTE_B64'],
  ['vulnerable', 'VULNERABLE_B64'],
  ['echange', 'ECHANGE_B64'],
  ['etreinte', 'ETREINTE_B64'],
]);

const STORY_LONG = { ...FAKE_STORY, texte: 'x'.repeat(700) };

describe('renderBookHtml mode A (6 scenes)', () => {
  it('produces exactly 16 page sections', () => {
    const html = renderBookHtml({
      story: STORY_LONG, scenes: FAKE_SCENES, illustrations: FAKE_ILLUS,
      fonts: FAKE_FONTS, palette: BOOK_PALETTE, tomeBadge: null,
    });
    const matches = html.match(/<section class="page/g) || [];
    expect(matches.length).toBe(16);
  });

  it('cover uses paysage illustration full-bleed', () => {
    const html = renderBookHtml({
      story: STORY_LONG, scenes: FAKE_SCENES, illustrations: FAKE_ILLUS,
      fonts: FAKE_FONTS, palette: BOOK_PALETTE, tomeBadge: null,
    });
    expect(html).toContain('data:image/png;base64,PAYSAGE_B64');
    expect(html).toMatch(/class="page cover"/);
  });

  it('title page shows tome badge when tomeBadge provided', () => {
    const html = renderBookHtml({
      story: STORY_LONG, scenes: FAKE_SCENES, illustrations: FAKE_ILLUS,
      fonts: FAKE_FONTS, palette: BOOK_PALETTE,
      tomeBadge: { current: 2, total: 3, livreTitre: 'Le Royaume Endormi' },
    });
    expect(html).toContain('Tome 2 sur 3');
    expect(html).toContain('Le Royaume Endormi');
  });

  it('title page omits tome badge when null', () => {
    const html = renderBookHtml({
      story: STORY_LONG, scenes: FAKE_SCENES, illustrations: FAKE_ILLUS,
      fonts: FAKE_FONTS, palette: BOOK_PALETTE, tomeBadge: null,
    });
    expect(html).not.toMatch(/Tome\s+\d+\s+sur/);
  });

  it('renders 6 scene double-pages with their archetypes', () => {
    const html = renderBookHtml({
      story: STORY_LONG, scenes: FAKE_SCENES, illustrations: FAKE_ILLUS,
      fonts: FAKE_FONTS, palette: BOOK_PALETTE, tomeBadge: null,
    });
    for (const arche of ['paysage','rencontre','decouverte','vulnerable','echange','etreinte']) {
      expect(html).toContain(`data-archetype="${arche}"`);
    }
  });

  it('back cover contains QR placeholder + FamilyVault label', () => {
    const html = renderBookHtml({
      story: STORY_LONG, scenes: FAKE_SCENES, illustrations: FAKE_ILLUS,
      fonts: FAKE_FONTS, palette: BOOK_PALETTE, tomeBadge: null,
    });
    expect(html).toContain('data-phase50');
    expect(html).toContain('FamilyVault');
  });

  it('throws on scenes.length !== 6 (strict per CONTEXT D-Q2)', () => {
    expect(() => renderBookHtml({
      story: STORY_LONG, scenes: FAKE_SCENES.slice(0, 5), illustrations: FAKE_ILLUS,
      fonts: FAKE_FONTS, palette: BOOK_PALETTE, tomeBadge: null,
    })).toThrow(/exactement 6 scènes/);
  });

  it('mode B placeholder when scenes null (detailed in Plan 49-04)', () => {
    const html = renderBookHtml({
      story: FAKE_STORY, scenes: null, illustrations: new Map(),
      fonts: FAKE_FONTS, palette: BOOK_PALETTE, tomeBadge: null,
    });
    expect(html).toContain('data-mode="fallback"');
  });
});
```
  </action>
  <verify>
    <automated>npx jest --no-coverage lib/__tests__/pdf-html-template.test.ts && npx tsc --noEmit 2>&1 | grep "lib/pdf/" || echo "TS OK"</automated>
  </verify>
  <done>html-template.ts branche les 4 composants en mode A, throws si scenes.length !== 6, mode B placeholder identifiable, tous les tests existants + 8 nouveaux tests passent, lib/pdf/index.ts ré-exporte les 4 composants.</done>
</task>

</tasks>

<verification>
- `npx jest --no-coverage lib/__tests__/pdf-html-template.test.ts` : tous tests passent
- `npx tsc --noEmit` : pas de nouvelle régression
- `grep -c "data-archetype" lib/pdf/components/scene-double-page.ts` ≥ 2 (un par page de la double-page)
- `grep "throw new Error" lib/pdf/html-template.ts` retrouve le message strict 6 scènes
</verification>

<success_criteria>
- 4 nouveaux fichiers `lib/pdf/components/{cover,title,scene-double-page,back-cover}.ts`
- `html-template.ts` mode A produit 16 sections .page structurées
- Mode B placeholder identifiable `data-mode="fallback"` (détaillé 49-04)
- Strict 6 scènes enforced (CONTEXT D-Q2)
- Tests Jest étendus (≥ 17 tests total au total dans pdf-html-template.test.ts)
- `escapeHtml` partout, aucun `file://` URL, pas de Date.now() / Math.random()
</success_criteria>

<output>
Après completion, créer `.planning/phases/49-layout-livre-generation-pdf/49-02-SUMMARY.md` documentant :
- Structure 16 pages mode A confirmée
- Stratégie highlights (color teal #0F8B8D, font-weight bold)
- Décision strict 6 scènes (message d'erreur retenu)
- Stub mode B délégué Plan 49-04
</output>
