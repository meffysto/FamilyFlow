---
phase: 49-layout-livre-generation-pdf
plan: 03
type: execute
wave: 3
depends_on: [49-01, 49-02]
files_modified:
  - lib/pdf/pdf-generator.ts
  - lib/pdf/book-storage.ts
  - lib/pdf/saga-detection.ts
  - lib/pdf/index.ts
  - lib/__tests__/pdf-hash.test.ts
  - lib/__tests__/pdf-saga-detection.test.ts
autonomous: true
requirements: [PDF-06, PDF-07, PDF-08, LAY-06]
must_haves:
  truths:
    - "generateBookPdf orchestre : preload assets parallèle → renderBookHtml → SHA-256 du HTML source → printToFileAsync 613.4×613.4pt → return uri+hash+entry"
    - "Hash SHA-256 du HTML est déterministe : 2 appels avec même story+scenes+illustrations produisent un hash identique"
    - "persistBookPdf copie le PDF cache → vault iCloud `12 - Impressions/PDFs/{storyId}-{YYYY-MM-DD}.pdf` + upsert manifeste sur (id, date)"
    - "detectTomeBadge retourne null si pas de livreId/chapitre, sinon { current, total, livreTitre } avec total = max(chapitre) sur même livreId"
    - "Mesure perf via Date.now() loggée sous __DEV__ avec étapes décomposées"
  artifacts:
    - path: "lib/pdf/pdf-generator.ts"
      provides: "generateBookPdf({story, allStories}): Promise<{uri, hash, entry}>"
    - path: "lib/pdf/book-storage.ts"
      provides: "persistBookPdf(vault, cacheUri, entry): Promise<BookManifestEntry>"
    - path: "lib/pdf/saga-detection.ts"
      provides: "detectTomeBadge(story, allStories): TomeBadge | null"
    - path: "lib/__tests__/pdf-hash.test.ts"
      provides: "Tests déterminisme hash (même HTML → même hash, HTML différent → hash différent)"
    - path: "lib/__tests__/pdf-saga-detection.test.ts"
      provides: "Tests detectTomeBadge (null/single/multi-chapitre)"
  key_links:
    - from: "pdf-generator.ts"
      to: "asset-loader.ts + html-template.ts + saga-detection.ts"
      via: "preloadAllAssets + renderBookHtml + detectTomeBadge"
    - from: "pdf-generator.ts"
      to: "expo-print.printToFileAsync"
      via: "html, width: 613.4, height: 613.4, margins 0"
    - from: "book-storage.ts"
      to: "VaultManager + manifest-parser"
      via: "ensureDir(PDFS_DIR) + FileSystem.copyAsync + parseManifeste/serializeManifeste/writeFile"
---

<objective>
Brancher le pipeline complet : `generateBookPdf` (orchestrateur preload + render + hash + printToFileAsync), `persistBookPdf` (copie vault + update manifeste), et `detectTomeBadge` (pure func saga). Hash SHA-256 calculé sur HTML source pour déterminisme (RESEARCH.md §481-515 — métadonnées PDF non-déterministes).

Purpose : Plan 49-01 a posé l'infra, Plan 49-02 les composants — Plan 49-03 transforme tout ça en pipeline end-to-end appelable. Sans ce plan, `renderBookHtml` n'a personne pour l'appeler ni rien pour persister son output.

Output : 3 nouveaux fichiers source + 2 tests Jest (hash déterminisme + saga detection). Aucune UI — le wiring depuis l'écran génération histoire est Phase 51.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/49-layout-livre-generation-pdf/49-CONTEXT.md
@.planning/phases/49-layout-livre-generation-pdf/49-RESEARCH.md
@.planning/phases/49-layout-livre-generation-pdf/49-01-SUMMARY.md
@.planning/phases/49-layout-livre-generation-pdf/49-02-SUMMARY.md
@lib/pdf/index.ts
@lib/pdf/manifest-parser.ts
@lib/pdf/types.ts
@lib/pdf/constants.ts
@lib/vault.ts

<interfaces>
<!-- Disponible depuis Plans 49-01 / 49-02 -->

From lib/pdf/asset-loader.ts:
```typescript
export function loadFontsBase64(): Promise<{ andikaRegular: string; andikaBold: string }>;
export function loadIllustrationBase64(univers: StoryUniverseId, archetype: SceneArchetype): Promise<string | null>;
export function preloadAllAssets(archetypes: SceneArchetype[]): Promise<void>;
```

From lib/pdf/html-template.ts:
```typescript
export function renderBookHtml(spec: BookHtmlSpec): string;
// throw si spec.scenes !== null && spec.scenes.length !== 6
```

From lib/pdf/manifest-parser.ts (Phase 48):
```typescript
export const MANIFESTE_FILE = '12 - Impressions/manifeste.md';
export function parseManifeste(content: string): BookManifestEntry[];
export function serializeManifeste(entries: BookManifestEntry[]): string;
```

From lib/pdf/types.ts (Phase 48):
```typescript
export interface BookManifestEntry { id: string; hash: string; date: string; format: string; chemin: string; }
```

From lib/vault.ts:
```typescript
export class VaultManager {
  vaultPath: string;
  uri(relativePath: string): string;       // PRIVATE — utiliser via writeFile / readFile
  readFile(relativePath: string): Promise<string>;
  writeFile(relativePath: string, content: string): Promise<void>;
  ensureDir(relativeDir: string): Promise<void>;
  exists(relativePath: string): Promise<boolean>;
}
```
⚠️ `uri()` est private dans la classe — pour copier un fichier binaire vers le vault, deux options :
  (a) lire le PDF en base64 via FileSystem puis appeler `vault.writeFile` (mais writeFile est UTF-8, ne fonctionnera pas pour binaire — KO)
  (b) construire l'URI cible manuellement via `vault.vaultPath + '/' + relativePath` puis utiliser `FileSystem.copyAsync({ from, to })` directement
  
**Choix retenu** : option (b) — exposer un helper local dans book-storage.ts qui réplique la logique de `vault.uri` (file:// prefix iOS + traversal check). Ne pas modifier la classe VaultManager dans ce plan.

From CONTEXT.md (lignes 56-58 + RESEARCH.md §605-660) :
- Storage path : `12 - Impressions/PDFs/{storyId}-{YYYY-MM-DD}.pdf`
- Manifeste upsert sur `(id, date)` (pas seulement `id`) — autorise multi-exports
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 : Créer saga-detection.ts (pure func) + tests Jest</name>
  <files>lib/pdf/saga-detection.ts, lib/__tests__/pdf-saga-detection.test.ts</files>
  <behavior>
- `detectTomeBadge(story, [])` → null
- `detectTomeBadge(story sans livreId, allStories)` → null
- `detectTomeBadge(story sans chapitre, allStories)` → null
- `detectTomeBadge(story chapitre 2, [story chapitre 1, story chapitre 2, story chapitre 3 même livreId])` → `{ current: 2, total: 3, livreTitre: ... }`
- `detectTomeBadge(story livreId='A', [stories de livreId='B'])` → null (filter livreId match)
- `livreTitre` fallback sur `story.titre` si `story.livreTitre` absent
- Pure func — pas de side effects, idempotent
  </behavior>
  <action>
**Créer `lib/pdf/saga-detection.ts`** (RESEARCH.md §389-413 + CONTEXT.md ligne 60-62) :

```typescript
import type { BedtimeStory } from '../types';

export interface TomeBadge {
  current: number;       // story.chapitre
  total: number;         // max(chapitre) sur même livreId
  livreTitre: string;    // story.livreTitre ?? story.titre
}

/**
 * Pure function : retourne le badge "Tome X sur Y du livre {titre}" si
 * l'histoire fait partie d'un livre (livreId + chapitre numériques), sinon null.
 *
 * Critères : groupe par `livreId`, total = max(chapitre) sur le groupe.
 * `allStories` typiquement = `storiesHook.stories` du VaultContext.
 */
export function detectTomeBadge(
  story: BedtimeStory,
  allStories: BedtimeStory[],
): TomeBadge | null {
  if (!story.livreId || typeof story.chapitre !== 'number' || story.chapitre < 1) {
    return null;
  }
  const sameLivre = allStories.filter(s => s.livreId === story.livreId && typeof s.chapitre === 'number' && s.chapitre >= 1);
  if (sameLivre.length === 0) return null;

  const total = Math.max(...sameLivre.map(s => s.chapitre as number));
  if (total < 1) return null;

  return {
    current: story.chapitre,
    total,
    livreTitre: story.livreTitre ?? story.titre,
  };
}
```

**Créer `lib/__tests__/pdf-saga-detection.test.ts`** :

```typescript
import { detectTomeBadge } from '../pdf/saga-detection';
import type { BedtimeStory } from '../types';

const baseStory = (over: Partial<BedtimeStory>): BedtimeStory => ({
  id: 'foret-x', titre: 'Histoire X', enfant: 'Lucas', enfantId: 'lucas',
  univers: 'foret', texte: 't', date: '2026-05-04', duree_lecture: 300,
  voice: { engine: 'expo-speech', language: 'fr' }, version: 1,
  sourceFile: '09 - Histoires/Lucas/2026-05-04-foret.md',
  ...over,
});

describe('detectTomeBadge', () => {
  it('returns null when story has no livreId', () => {
    const s = baseStory({});
    expect(detectTomeBadge(s, [s])).toBeNull();
  });

  it('returns null when story has no chapitre', () => {
    const s = baseStory({ livreId: 'royaume', livreTitre: 'Le Royaume' });
    expect(detectTomeBadge(s, [s])).toBeNull();
  });

  it('returns null when chapitre < 1', () => {
    const s = baseStory({ livreId: 'r', livreTitre: 'R', chapitre: 0 });
    expect(detectTomeBadge(s, [s])).toBeNull();
  });

  it('returns null when allStories is empty', () => {
    const s = baseStory({ livreId: 'r', livreTitre: 'R', chapitre: 1 });
    expect(detectTomeBadge(s, [])).toBeNull();
  });

  it('returns badge with total = max(chapitre) on same livreId', () => {
    const s1 = baseStory({ id: 's1', livreId: 'r', livreTitre: 'Royaume Endormi', chapitre: 1 });
    const s2 = baseStory({ id: 's2', livreId: 'r', livreTitre: 'Royaume Endormi', chapitre: 2 });
    const s3 = baseStory({ id: 's3', livreId: 'r', livreTitre: 'Royaume Endormi', chapitre: 3 });
    expect(detectTomeBadge(s2, [s1, s2, s3])).toEqual({ current: 2, total: 3, livreTitre: 'Royaume Endormi' });
  });

  it('filters out stories from other livreId', () => {
    const sA = baseStory({ id: 'a', livreId: 'A', chapitre: 1 });
    const sB1 = baseStory({ id: 'b1', livreId: 'B', chapitre: 1 });
    const sB2 = baseStory({ id: 'b2', livreId: 'B', chapitre: 5 });
    const result = detectTomeBadge(sA, [sA, sB1, sB2]);
    expect(result).toEqual({ current: 1, total: 1, livreTitre: 'Histoire X' });
  });

  it('falls back to story.titre when livreTitre absent', () => {
    const s = baseStory({ titre: 'Mon Histoire', livreId: 'x', chapitre: 1 });
    const result = detectTomeBadge(s, [s]);
    expect(result?.livreTitre).toBe('Mon Histoire');
  });

  it('is pure (does not mutate inputs)', () => {
    const s1 = baseStory({ id: 's1', livreId: 'r', chapitre: 1 });
    const s2 = baseStory({ id: 's2', livreId: 'r', chapitre: 2 });
    const arr = [s1, s2];
    const before = JSON.stringify(arr);
    detectTomeBadge(s1, arr);
    expect(JSON.stringify(arr)).toBe(before);
  });
});
```
  </action>
  <verify>
    <automated>npx jest --no-coverage lib/__tests__/pdf-saga-detection.test.ts</automated>
  </verify>
  <done>saga-detection.ts exporte detectTomeBadge, 8 tests Jest passent, fonction est pure (no mutation), pas de side effects.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 : pdf-generator.ts (orchestrateur preload + render + hash + print) + test hash déterminisme</name>
  <files>lib/pdf/pdf-generator.ts, lib/__tests__/pdf-hash.test.ts</files>
  <behavior>
- `generateBookPdf({ story, allStories })` retourne `{ uri: string; hash: string; entry: BookManifestEntry }`
- Loads fonts + 6 illustrations forêt en parallèle via `Promise.all` (perf budget RESEARCH.md §666-694)
- Construit `BookHtmlSpec` avec scenes, illustrations Map, fonts, palette BOOK_PALETTE, tomeBadge via `detectTomeBadge`
- Hash : `Crypto.digestStringAsync(SHA256, html)` → hex 64 chars
- `printToFileAsync` appelé avec `width: 613.4`, `height: 613.4`, margins 0
- Sous `__DEV__`, log `[generateBookPdf] {storyId} — {ms}ms — assets:{ms} render:{ms} hash:{ms} print:{ms}` (CONTEXT.md §183 + RESEARCH.md §688-694)
- Test : 2 appels avec exactement la même `BookHtmlSpec` → exactement le même HTML string → exactement le même hash (déterminisme RESEARCH.md §503-509)
- Test : changer le titre change le hash
  </behavior>
  <action>
**Créer `lib/pdf/pdf-generator.ts`** (RESEARCH.md §521-591 + CONTEXT.md ligne 51-58) :

```typescript
import * as Print from 'expo-print';
import * as Crypto from 'expo-crypto';
import type { BedtimeStory, SceneArchetype } from '../types';
import type { BookManifestEntry } from './types';
import { TRIM_SIZE_CM, BLEED_CM, BOOK_PALETTE, LULU_FORMAT_LABEL } from './constants';
import { renderBookHtml } from './html-template';
import { loadFontsBase64, loadIllustrationBase64 } from './asset-loader';
import { detectTomeBadge } from './saga-detection';

const PT_PER_CM = 28.346456693;
export const PAGE_SIZE_PT = (TRIM_SIZE_CM + 2 * BLEED_CM) * PT_PER_CM; // = 613.4

const ALL_ARCHETYPES: readonly SceneArchetype[] = [
  'paysage', 'rencontre', 'decouverte', 'vulnerable', 'echange', 'etreinte',
] as const;

export interface GenerateBookPdfOptions {
  story: BedtimeStory;
  allStories: BedtimeStory[];
}

export interface GenerateBookPdfResult {
  uri: string;
  hash: string;
  entry: Omit<BookManifestEntry, 'chemin'>;  // chemin rempli par persistBookPdf
  html: string;  // exposé pour debug + tests (pas écrit sur disque)
  perf: { totalMs: number; assetsMs: number; renderMs: number; hashMs: number; printMs: number };
}

export async function generateBookPdf(opts: GenerateBookPdfOptions): Promise<GenerateBookPdfResult> {
  const t0 = Date.now();

  // 1. Charger assets en parallèle (fonts + 6 illustrations forêt si univers='foret')
  const tA0 = Date.now();
  const [fonts, ...illuResults] = await Promise.all([
    loadFontsBase64(),
    ...ALL_ARCHETYPES.map(a => loadIllustrationBase64(opts.story.univers, a)),
  ]);
  const illustrations = new Map<SceneArchetype, string>();
  ALL_ARCHETYPES.forEach((arche, idx) => {
    const b64 = illuResults[idx];
    if (b64) illustrations.set(arche, b64);
  });
  const assetsMs = Date.now() - tA0;

  // 2. Détecter tome
  const tomeBadge = detectTomeBadge(opts.story, opts.allStories);

  // 3. Construire HTML (renderBookHtml throw si scenes.length !== 6)
  const tR0 = Date.now();
  const html = renderBookHtml({
    story: opts.story,
    scenes: opts.story.scenes?.scenes ?? null,
    illustrations,
    fonts,
    palette: BOOK_PALETTE,
    tomeBadge,
  });
  const renderMs = Date.now() - tR0;

  // 4. Hash SHA-256 du HTML source (PDF-07 — RESEARCH.md §481-515)
  const tH0 = Date.now();
  const hash = await Crypto.digestStringAsync(
    Crypto.CryptoDigestAlgorithm.SHA256,
    html,
  );
  const hashMs = Date.now() - tH0;

  // 5. expo-print → PDF (Pitfall 2 mitigation : width/height en POINTS)
  const tP0 = Date.now();
  const result = await Print.printToFileAsync({
    html,
    width: PAGE_SIZE_PT,
    height: PAGE_SIZE_PT,
    margins: { top: 0, bottom: 0, left: 0, right: 0 },
  });
  const printMs = Date.now() - tP0;

  const totalMs = Date.now() - t0;

  if (__DEV__) {
    console.log(
      `[generateBookPdf] ${opts.story.id} — ${totalMs}ms — ` +
      `assets:${assetsMs}ms render:${renderMs}ms hash:${hashMs}ms print:${printMs}ms`,
    );
  }

  const entry: Omit<BookManifestEntry, 'chemin'> = {
    id: opts.story.id,
    hash,
    date: new Date().toISOString().slice(0, 10),  // YYYY-MM-DD UTC — figé à l'instant export
    format: LULU_FORMAT_LABEL,
  };

  return {
    uri: result.uri,
    hash,
    entry,
    html,
    perf: { totalMs, assetsMs, renderMs, hashMs, printMs },
  };
}
```

**Note déterminisme** : la `entry.date` est calculée via `new Date().toISOString().slice(0,10)` au moment de l'export — elle dépend de la date courante, mais elle ne fait PAS partie du HTML hashé (le HTML contient `story.date` qui est figé). Le hash du HTML reste déterministe ; seule la `entry` (ligne du manifeste) varie selon la date d'export — c'est volontaire (manifeste = audit trail temporel).

**Créer `lib/__tests__/pdf-hash.test.ts`** — test déterminisme du HTML rendu (mock expo-crypto + expo-print + asset-loader pour rester pure-JS) :

```typescript
import { renderBookHtml } from '../pdf/html-template';
import { BOOK_PALETTE } from '../pdf/constants';
import type { BedtimeStory, SceneSpec } from '../types';

// Polyfill SHA-256 minimaliste pour Jest — utilise crypto Node natif
import { createHash } from 'crypto';

function sha256(s: string): string {
  return createHash('sha256').update(s, 'utf8').digest('hex');
}

const FONTS = { andikaRegular: 'AAAA', andikaBold: 'BBBB' };
const STORY: BedtimeStory = {
  id: 'foret-test', titre: 'Le Loup', enfant: 'Lucas', enfantId: 'lucas',
  univers: 'foret', texte: 'x'.repeat(700), date: '2026-05-04', duree_lecture: 300,
  voice: { engine: 'expo-speech', language: 'fr' },
  version: 1, sourceFile: '09 - Histoires/Lucas/2026-05-04-foret.md',
};
const SCENES: SceneSpec[] = ['paysage','rencontre','decouverte','vulnerable','echange','etreinte'].map((a, i) => ({
  panelIndex: i + 1, archetype: a as any, textStart: i * 100, textEnd: (i + 1) * 100, highlights: [],
}));
const ILLUS = new Map<any, string>(SCENES.map(s => [s.archetype, `B64_${s.archetype}`]));

describe('PDF hash determinism (PDF-07)', () => {
  it('same input → same HTML → same SHA-256', () => {
    const h1 = renderBookHtml({ story: STORY, scenes: SCENES, illustrations: ILLUS, fonts: FONTS, palette: BOOK_PALETTE, tomeBadge: null });
    const h2 = renderBookHtml({ story: STORY, scenes: SCENES, illustrations: ILLUS, fonts: FONTS, palette: BOOK_PALETTE, tomeBadge: null });
    expect(h1).toBe(h2);
    expect(sha256(h1)).toBe(sha256(h2));
    expect(sha256(h1)).toMatch(/^[a-f0-9]{64}$/);
  });

  it('changing titre changes hash', () => {
    const h1 = renderBookHtml({ story: STORY, scenes: SCENES, illustrations: ILLUS, fonts: FONTS, palette: BOOK_PALETTE, tomeBadge: null });
    const h2 = renderBookHtml({ story: { ...STORY, titre: 'Le Renard' }, scenes: SCENES, illustrations: ILLUS, fonts: FONTS, palette: BOOK_PALETTE, tomeBadge: null });
    expect(sha256(h1)).not.toBe(sha256(h2));
  });

  it('changing scene highlights changes hash', () => {
    const SCENES2 = [...SCENES];
    SCENES2[0] = { ...SCENES2[0], highlights: [{ startChar: 0, endChar: 5, kind: 'keyword' }] };
    const h1 = renderBookHtml({ story: STORY, scenes: SCENES, illustrations: ILLUS, fonts: FONTS, palette: BOOK_PALETTE, tomeBadge: null });
    const h2 = renderBookHtml({ story: STORY, scenes: SCENES2, illustrations: ILLUS, fonts: FONTS, palette: BOOK_PALETTE, tomeBadge: null });
    expect(sha256(h1)).not.toBe(sha256(h2));
  });

  it('changing tomeBadge changes hash', () => {
    const h1 = renderBookHtml({ story: STORY, scenes: SCENES, illustrations: ILLUS, fonts: FONTS, palette: BOOK_PALETTE, tomeBadge: null });
    const h2 = renderBookHtml({ story: STORY, scenes: SCENES, illustrations: ILLUS, fonts: FONTS, palette: BOOK_PALETTE, tomeBadge: { current: 2, total: 3, livreTitre: 'X' } });
    expect(sha256(h1)).not.toBe(sha256(h2));
  });

  it('HTML has no Date.now() / Math.random() residue', () => {
    const h1 = renderBookHtml({ story: STORY, scenes: SCENES, illustrations: ILLUS, fonts: FONTS, palette: BOOK_PALETTE, tomeBadge: null });
    // Si quelqu'un ajoute Date.now() dans le template, cette regex pourrait matcher un timestamp 13-digit
    // (heuristique faible — on cherche surtout l'absence de patterns comme "20260504123045")
    expect(h1).not.toMatch(/Date\.now/);
    expect(h1).not.toMatch(/Math\.random/);
  });
});
```

⚠️ **Test note** : le test n'invoque PAS `generateBookPdf` (qui appelle `expo-print` + `expo-crypto` natifs). Il valide la PROPRIÉTÉ déterministe du HTML rendu. Cette propriété + l'usage de `Crypto.digestStringAsync` sur ce même HTML → garantit le déterminisme PDF-07. Validation device (manuelle) viendra Phase 51 wiring.
  </action>
  <verify>
    <automated>npx jest --no-coverage lib/__tests__/pdf-hash.test.ts && npx tsc --noEmit 2>&1 | grep "lib/pdf/pdf-generator" || echo "TS OK"</automated>
  </verify>
  <done>pdf-generator.ts exporte generateBookPdf + PAGE_SIZE_PT, mesure perf décomposée par étape, log __DEV__, 5 tests hash déterminisme passent.</done>
</task>

<task type="auto">
  <name>Task 3 : book-storage.ts (copie vault iCloud + upsert manifeste) + barrel index.ts</name>
  <files>lib/pdf/book-storage.ts, lib/pdf/index.ts</files>
  <action>
**Créer `lib/pdf/book-storage.ts`** (RESEARCH.md §622-660 + CONTEXT.md lignes 56-58) :

```typescript
import * as FileSystem from 'expo-file-system/legacy';
import { Platform } from 'react-native';
import type { VaultManager } from '../vault';
import type { BookManifestEntry } from './types';
import { MANIFESTE_FILE, parseManifeste, serializeManifeste } from './manifest-parser';

const PDFS_DIR = '12 - Impressions/PDFs';

/**
 * Copie le PDF du cache app vers le vault iCloud + met à jour le manifeste.
 * Upsert sur (id, date) pour autoriser plusieurs exports du même storyId
 * à des dates différentes (CONTEXT.md ligne 58).
 *
 * Path final : 12 - Impressions/PDFs/{storyId}-{YYYY-MM-DD}.pdf
 */
export async function persistBookPdf(
  vault: VaultManager,
  cacheUri: string,
  entry: Omit<BookManifestEntry, 'chemin'>,
): Promise<BookManifestEntry> {
  const filename = `${entry.id}-${entry.date}.pdf`;
  const relativePath = `${PDFS_DIR}/${filename}`;

  // 1. Ensure dir
  await vault.ensureDir(PDFS_DIR);

  // 2. Construire URI cible (réplique logique vault.uri private — RESEARCH.md note iCloud)
  const targetUri = buildVaultUri(vault.vaultPath, relativePath);

  // 3. Copier cache → vault. FileSystem.copyAsync est OK pour binaires sur iOS
  //    (NSFileCoordinator intervient à la lecture côté Obsidian — RESEARCH.md §662).
  await FileSystem.copyAsync({ from: cacheUri, to: targetUri });

  // 4. Lire manifeste existant (création paresseuse — Phase 48 SUMMARY)
  let entries: BookManifestEntry[] = [];
  try {
    const existing = await vault.readFile(MANIFESTE_FILE);
    entries = parseManifeste(existing);
  } catch {
    // Manifeste pas encore créé → première écriture
  }

  // 5. Upsert sur (id, date)
  const fullEntry: BookManifestEntry = { ...entry, chemin: relativePath };
  const idx = entries.findIndex(e => e.id === fullEntry.id && e.date === fullEntry.date);
  if (idx >= 0) entries[idx] = fullEntry;
  else entries.push(fullEntry);

  // 6. Écrire manifeste mis à jour
  await vault.writeFile(MANIFESTE_FILE, serializeManifeste(entries));

  return fullEntry;
}

/**
 * Réplique la logique vault.uri (private). file:// prefix sur iOS si pas déjà
 * un URI. Path traversal check identique à vault.ts:74-91.
 */
function buildVaultUri(vaultPath: string, relativePath: string): string {
  if (relativePath.includes('..') || relativePath.startsWith('/') || relativePath.includes('\0')) {
    throw new Error(`Chemin invalide (traversal détecté) : ${relativePath}`);
  }
  const isUri = vaultPath.startsWith('file://') || vaultPath.startsWith('content://');
  const rel = isUri
    ? relativePath.split('/').map(c => encodeURIComponent(c)).join('/')
    : relativePath;
  const path = `${vaultPath.replace(/\/$/, '')}/${rel}`;
  if (isUri) return path;
  // Sur web (jamais utilisé en pratique pour PDF) ou Android, le préfixe file:// est attendu
  return Platform.OS === 'web' ? path : `file://${path}`;
}
```

**Mettre à jour `lib/pdf/index.ts`** — ré-exporter le pipeline :

```typescript
// Ajouter aux exports existants :
export { detectTomeBadge } from './saga-detection';
export type { TomeBadge } from './saga-detection';
export { generateBookPdf, PAGE_SIZE_PT } from './pdf-generator';
export type { GenerateBookPdfOptions, GenerateBookPdfResult } from './pdf-generator';
export { persistBookPdf } from './book-storage';
```

⚠️ **Pas de test Jest pour book-storage.ts** dans ce plan — il dépend de FileSystem.copyAsync natif et de VaultManager (lourd à mocker, marginal). La validation est manuelle device (Phase 51 wiring) + le test du parser manifeste round-trip est déjà couvert Phase 48.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep "lib/pdf/book-storage" || echo "TS OK"; grep -c "persistBookPdf\|generateBookPdf\|detectTomeBadge" lib/pdf/index.ts</automated>
  </verify>
  <done>book-storage.ts exporte persistBookPdf, lib/pdf/index.ts ré-exporte les 3 nouvelles fonctions + types, path traversal check répliqué de vault.ts, no breaking change sur VaultManager.</done>
</task>

</tasks>

<verification>
- `npx jest --no-coverage lib/__tests__/pdf-hash.test.ts lib/__tests__/pdf-saga-detection.test.ts lib/__tests__/pdf-html-template.test.ts` : tous tests passent
- `npx tsc --noEmit` ne régresse pas
- `grep "PAGE_SIZE_PT" lib/pdf/pdf-generator.ts` ≥ 2 (constant + usage)
- `grep "12 - Impressions/PDFs" lib/pdf/book-storage.ts` retrouve la convention path
- `grep "console.log" lib/pdf/pdf-generator.ts` doit être SOUS un `if (__DEV__)`
</verification>

<success_criteria>
- 3 nouveaux fichiers source (`saga-detection.ts`, `pdf-generator.ts`, `book-storage.ts`) + 2 tests (`pdf-saga-detection.test.ts`, `pdf-hash.test.ts`)
- Pipeline complet appelable : `generateBookPdf({story, allStories})` → `{uri, hash, entry, html, perf}`
- Hash SHA-256 déterministe sur HTML source (5 tests passent)
- detectTomeBadge pure func (8 tests passent)
- persistBookPdf : copie iCloud + upsert manifeste (id, date)
- Mesure perf décomposée logguée __DEV__
- No mod sur VaultManager class

## Notes

- **BookExportSpec (Phase 48) reste typé mais non utilisé runtime Phase 49** — la pipeline `generateBookPdf` consomme directement `BedtimeStory` + `BedtimeStory[]` (allStories) et construit son propre `BookHtmlSpec` interne via `detectTomeBadge` + `loadFontsBase64` + `loadIllustrationBase64`. Le type `BookExportSpec` sera consommé Phase 51 lors du wiring UI bouton export (assemblage côté écran génération histoire avant appel `generateBookPdf`).
</success_criteria>

<output>
Après completion, créer `.planning/phases/49-layout-livre-generation-pdf/49-03-SUMMARY.md` documentant :
- Pipeline `generateBookPdf` → `persistBookPdf` end-to-end
- Constante `PAGE_SIZE_PT = 613.4` exposée
- Validation device (Phase 51 wiring) à prévoir : log perf + ouvrir PDF Aperçu macOS pour vérifier dimensions 21.64×21.64 cm + polices Andika listées
- Mode B fallback toujours en placeholder — détaillé Plan 49-04
</output>
