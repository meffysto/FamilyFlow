---
phase: 11-sagas-immersives
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - components/dashboard/DashboardGarden.tsx
autonomous: true
requirements:
  - SAG-03

must_haves:
  truths:
    - "Le dashboard n'affiche plus de carte saga (plus de boutons, plus de carte dediee)"
    - "Le dashboard affiche un texte inline discret quand une saga est active"
    - "Le texte montre la progression compacte (chapitre X/Y — titre)"
    - "Taper sur l'indicateur navigue vers l'ecran arbre"
  artifacts:
    - path: "components/dashboard/DashboardGarden.tsx"
      provides: "Section jardin avec indicateur saga texte inline"
  key_links:
    - from: "components/dashboard/DashboardGarden.tsx"
      to: "app/(tabs)/tree"
      via: "router.push('/(tabs)/tree') sur tap indicateur"
      pattern: "router.push.*tree"
---

<objective>
Remplacer la carte saga complete de DashboardGarden par un indicateur texte inline discret qui pousse l'utilisateur vers l'ecran arbre.

Purpose: Le dashboard ne gere plus l'experience saga — il cree de la curiosite et redirige vers l'arbre ou vit le visiteur.
Output: DashboardGarden.tsx simplifie sans carte saga, avec un texte indicateur compact a la place.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-sagas-immersives/11-CONTEXT.md
@.planning/phases/11-sagas-immersives/11-RESEARCH.md

@components/dashboard/DashboardGarden.tsx

<interfaces>
From components/dashboard/DashboardGarden.tsx (state saga existant):
```typescript
const [sagaProgress, setSagaProgress] = useState<SagaProgress | null>(null);
const sagaChapterDone = sagaProgress?.lastChapterDate === today;
const activeSaga = sagaProgress ? getSagaById(sagaProgress.sagaId) : null;
const hasSaga = !!activeSaga && !!sagaProgress && sagaProgress.status === 'active';
```

From locales/fr/gamification.json (cles ajoutees par Plan 01):
```json
"mascot.saga.indicator.waiting": "Un visiteur attend pres de ton arbre"
"mascot.saga.indicator.done": "Suite de la saga demain..."
"mascot.saga.indicator.progress": "Chapitre {{current}}/{{total}} — {{title}}"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Supprimer renderSagaCard et renderSagaDots, ajouter indicateur texte inline</name>
  <files>components/dashboard/DashboardGarden.tsx</files>
  <read_first>
    components/dashboard/DashboardGarden.tsx (lire entierement — comprendre renderSagaCard, renderSagaDots, hasSaga, sagaChapterDone, sagaProgress, completeSagaChapter, l'endroit dans le render ou renderSagaCard() est appele)
    locales/fr/gamification.json (verifier que les cles indicator existent — si Plan 01 pas encore execute, ajouter les cles ici)
  </read_first>
  <action>
    **1. Supprimer la fonction renderSagaCard() (per D-12) :**
    Chercher `const renderSagaCard = ()` ou `function renderSagaCard()` (probablement lignes ~175-298). Supprimer la fonction entiere — plus de carte saga avec bouton, dots, recompenses.

    **2. Supprimer la fonction renderSagaDots() :**
    Chercher `const renderSagaDots` ou `function renderSagaDots` (probablement au-dessus de renderSagaCard). Supprimer entierement.

    **3. Supprimer l'appel a renderSagaCard() dans le rendu :**
    Chercher `{!loading && activeProfile && hasSaga && renderSagaCard()}` (probablement ligne ~404). Supprimer cette ligne.

    **4. Supprimer les variables/callbacks saga qui ne sont plus necessaires :**
    - `completeSagaChapter` callback si present (la completion se fait dans tree.tsx maintenant)
    - `currentChapter` et `isLastChapter` si definis uniquement pour renderSagaCard
    - Les imports qui ne sont plus utilises apres suppression (verifier avec tsc)
    - NE PAS supprimer : `sagaProgress`, `hasSaga`, `sagaChapterDone`, `activeSaga`, `shouldStartSaga`, `loadSagaProgress` — ces variables restent necessaires pour l'indicateur texte et le demarrage de saga

    **5. Ajouter l'indicateur texte inline (per D-13, D-14) :**
    A l'endroit ou `renderSagaCard()` etait appele, inserer :
    ```typescript
    {hasSaga && sagaProgress && sagaProgress.status !== 'completed' && (
      <TouchableOpacity
        style={[styles.sagaIndicator, { borderColor: colors.borderLight }]}
        onPress={() => router.push('/(tabs)/tree' as any)}
        activeOpacity={0.8}
      >
        <Text style={[styles.sagaIndicatorText, {
          color: sagaChapterDone ? colors.textMuted : primary
        }]}>
          {sagaChapterDone
            ? t('mascot.saga.indicator.done')
            : t('mascot.saga.indicator.waiting')}
        </Text>
        {activeSaga && (
          <Text style={[styles.sagaIndicatorProgress, { color: colors.textSub }]}>
            {t('mascot.saga.indicator.progress', {
              current: sagaProgress.currentChapter,
              total: activeSaga.chapters.length,
              title: t(activeSaga.titleKey),
            })}
          </Text>
        )}
      </TouchableOpacity>
    )}
    ```
    Verifier que `router` est importe depuis `expo-router` (`import { useRouter } from 'expo-router'` — probablement deja present). Si pas present, ajouter `const router = useRouter();`.

    **6. Ajouter les styles dans StyleSheet.create :**
    ```typescript
    sagaIndicator: {
      paddingVertical: Spacing.sm,
      paddingHorizontal: Spacing.md,
      borderWidth: 1,
      borderRadius: Radius.lg,
      marginTop: Spacing.md,
    },
    sagaIndicatorText: {
      fontSize: FontSize.sm,
      fontWeight: FontWeight.medium,
    },
    sagaIndicatorProgress: {
      fontSize: FontSize.xs,
      marginTop: Spacing[2],
    },
    ```

    **7. Si les cles i18n n'existent pas encore (Plan 01 pas execute) :**
    Utiliser des strings inline en fallback :
    ```typescript
    t('mascot.saga.indicator.done', 'Suite de la saga demain...')
    t('mascot.saga.indicator.waiting', 'Un visiteur attend pres de ton arbre')
    ```
    Le 2eme argument de `t()` sert de fallback si la cle n'existe pas.

    **8. Supprimer les styles devenus inutiles :**
    Chercher dans `StyleSheet.create` les styles utilises uniquement par renderSagaCard/renderSagaDots (ex: `sagaCard`, `sagaDot`, `sagaDotActive`, `sagaButton`, etc.) et les supprimer.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -c "DashboardGarden" || echo "0 errors"</automated>
  </verify>
  <acceptance_criteria>
    - `DashboardGarden.tsx` ne contient plus `renderSagaCard` (fonction supprimee)
    - `DashboardGarden.tsx` ne contient plus `renderSagaDots` (fonction supprimee)
    - `DashboardGarden.tsx` contient `sagaIndicator` (nouveau style)
    - `DashboardGarden.tsx` contient `router.push` avec `tree` (navigation vers arbre)
    - `DashboardGarden.tsx` contient `mascot.saga.indicator.waiting` ou le fallback string "Un visiteur attend"
    - `DashboardGarden.tsx` contient `mascot.saga.indicator.done` ou le fallback string "Suite de la saga"
    - `DashboardGarden.tsx` conserve `loadSagaProgress` et `shouldStartSaga` (demarrage saga reste ici)
    - `npx tsc --noEmit` ne produit pas de nouvelle erreur liee a DashboardGarden
  </acceptance_criteria>
  <done>DashboardGarden n'a plus de carte saga. Un texte indicateur inline affiche l'etat saga avec tap vers l'arbre. Le demarrage de saga reste dans DashboardGarden.</done>
</task>

</tasks>

<verification>
- `grep -c "renderSagaCard" components/dashboard/DashboardGarden.tsx` retourne 0
- `grep "sagaIndicator" components/dashboard/DashboardGarden.tsx` trouve des resultats
- `grep "router.push.*tree" components/dashboard/DashboardGarden.tsx` trouve des resultats
- `npx tsc --noEmit` passe sans nouvelles erreurs
</verification>

<success_criteria>
Le dashboard ne montre plus la carte saga complete — juste un texte indicateur discret qui cree de la curiosite et redirige vers l'arbre.
</success_criteria>

<output>
After completion, create `.planning/phases/11-sagas-immersives/11-03-SUMMARY.md`
</output>
