---
phase: 11-sagas-immersives
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - components/mascot/SagaWorldEvent.tsx
  - app/(tabs)/tree.tsx
autonomous: true
requirements:
  - SAG-02
  - SAG-04

must_haves:
  truths:
    - "Taper sur le visiteur dans la scene ouvre le dialogue saga SagaWorldEvent"
    - "Le spiritGlow emoji est remplace par le sprite du voyageur comme portrait"
    - "Apres completion du chapitre, le visiteur part en marchant vers la droite"
    - "Le visiteur n'apparait que quand un chapitre est disponible (saga active + pas joue aujourd'hui)"
  artifacts:
    - path: "components/mascot/SagaWorldEvent.tsx"
      provides: "Dialogue saga refactorise (tap-triggered, portrait sprite)"
      exports: ["SagaWorldEvent", "SagaWorldEventProps"]
    - path: "app/(tabs)/tree.tsx"
      provides: "Integration VisitorSlot dans le diorama + orchestration saga"
  key_links:
    - from: "app/(tabs)/tree.tsx"
      to: "components/mascot/VisitorSlot.tsx"
      via: "import et render dans couche 3.6 du diorama"
      pattern: "VisitorSlot"
    - from: "app/(tabs)/tree.tsx"
      to: "components/mascot/SagaWorldEvent.tsx"
      via: "showSagaEvent state declenche par onTap du VisitorSlot"
      pattern: "setShowSagaEvent.*true"
    - from: "components/mascot/SagaWorldEvent.tsx"
      to: "app/(tabs)/tree.tsx"
      via: "onDismiss callback declenche visitorShouldDepart"
      pattern: "onDismiss"
---

<objective>
Refactorer SagaWorldEvent pour etre declenche par tap (plus auto) et integrer VisitorSlot dans la scene de l'arbre avec l'orchestration complete arrivee → dialogue → depart.

Purpose: C'est le coeur de l'experience immersive — le visiteur vit dans le diorama et le joueur interagit avec lui pour avancer la saga.
Output: SagaWorldEvent refactorise avec portrait sprite, tree.tsx avec VisitorSlot integre et orchestration depart.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-sagas-immersives/11-CONTEXT.md
@.planning/phases/11-sagas-immersives/11-RESEARCH.md
@.planning/phases/11-sagas-immersives/11-01-SUMMARY.md

@components/mascot/SagaWorldEvent.tsx
@app/(tabs)/tree.tsx
@components/mascot/VisitorSlot.tsx (cree dans Plan 01)

<interfaces>
From components/mascot/VisitorSlot.tsx (Plan 01):
```typescript
export interface VisitorSlotProps {
  visible: boolean;
  sagaId: string;
  containerWidth: number;
  containerHeight: number;
  onTap: () => void;
  shouldDepart: boolean;
  isLastChapter: boolean;
  onDepartComplete?: () => void;
}
export function VisitorSlot(props: VisitorSlotProps): React.ReactElement | null;
```

From components/mascot/SagaWorldEvent.tsx:
```typescript
export interface SagaWorldEventProps {
  sagaProgress: SagaProgress;
  containerHeight: number;
  onChapterComplete: (choiceId: string, points: number, sagaNote: string, rewardItem?) => Promise<void>;
  onDismiss: () => void;
}
```

From app/(tabs)/tree.tsx (state existant):
```typescript
const [sagaProgress, setSagaProgress] = useState<SagaProgress | null>(null);
const [showSagaEvent, setShowSagaEvent] = useState(false);
const sagaChapterAvailable = !!sagaProgress && sagaProgress.status === 'active' && !sagaChapterDone;
const handleSagaChapterComplete = useCallback(async (...) => { ... }, [...]);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactorer SagaWorldEvent — portrait sprite voyageur au lieu de spiritGlow</name>
  <files>components/mascot/SagaWorldEvent.tsx</files>
  <read_first>
    components/mascot/SagaWorldEvent.tsx (lire entierement — 550 lignes, comprendre spiritGlow, phases, animation dismiss)
  </read_first>
  <action>
    Modifications chirurgicales de SagaWorldEvent (per D-15, D-16). Le composant reste fonctionnel — seul le declencheur et le visuel "esprit" changent.

    **1. Ajouter prop optionnelle pour le portrait visiteur (per D-16) :**
    ```typescript
    // Ajouter a SagaWorldEventProps :
    visitorIdleFrame?: ImageSourcePropType;  // sprite pour le portrait
    ```
    Ajouter `import { Image, ImageSourcePropType } from 'react-native';` si pas deja present.

    **2. Remplacer le rendu spiritGlow par le portrait du visiteur :**
    Chercher le bloc qui rend le `spiritGlow` (cercle lumineux + emoji thematique). Il utilise probablement un `Animated.View` avec `borderRadius: 999`, une couleur de fond, et un emoji `<Text>`.

    Remplacer ce bloc par :
    ```typescript
    {visitorIdleFrame ? (
      <Animated.View style={[styles.visitorPortrait, spiritAnimStyle]}>
        <Image
          source={visitorIdleFrame}
          style={{ width: 72, height: 72 }}
          resizeMode="contain"
        />
      </Animated.View>
    ) : (
      // Garder l'ancien spiritGlow comme fallback si pas de sprite
      <Animated.View style={[styles.spiritGlow, spiritAnimStyle]}>
        ...ancien contenu...
      </Animated.View>
    )}
    ```
    - Le `visitorPortrait` style : memes dimensions que spiritGlow (~72x72), centre en haut du diorama
    - Reutiliser l'animation existante du spiritGlow (pulsation, scale) sur le portrait

    **3. Ajouter le style `visitorPortrait` dans StyleSheet.create :**
    ```typescript
    visitorPortrait: {
      width: 72,
      height: 72,
      alignItems: 'center',
      justifyContent: 'center',
    },
    ```

    **4. NE PAS modifier :**
    - La logique des phases (entering, narrative, choices, chosen, cliffhanger, finale_reveal, dismissing)
    - Le typewriter
    - Les choix cards slide-in
    - Le trait flash
    - Le cliffhanger / finale_reveal
    - La fonction animateDismiss et le callback onDismiss
    - Les animations de vignette
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -c "SagaWorldEvent" || echo "0 errors"</automated>
  </verify>
  <acceptance_criteria>
    - `SagaWorldEvent.tsx` contient `visitorIdleFrame` dans l'interface SagaWorldEventProps
    - `SagaWorldEvent.tsx` contient `ImageSourcePropType` (import)
    - `SagaWorldEvent.tsx` contient `visitorPortrait` (style ou rendu conditionnel)
    - `SagaWorldEvent.tsx` conserve les phases : 'entering', 'narrative', 'choices', 'chosen', 'cliffhanger'
    - `SagaWorldEvent.tsx` conserve `onDismiss` dans les props
    - `SagaWorldEvent.tsx` conserve `onChapterComplete` dans les props
  </acceptance_criteria>
  <done>SagaWorldEvent affiche le sprite du voyageur comme portrait au lieu du spiritGlow emoji. L'ancien rendu est garde en fallback. Toute la logique narrative est intacte.</done>
</task>

<task type="auto">
  <name>Task 2: Integrer VisitorSlot dans tree.tsx + orchestration arrivee/dialogue/depart</name>
  <files>app/(tabs)/tree.tsx</files>
  <read_first>
    app/(tabs)/tree.tsx (lire entierement — comprendre les couches diorama, sagaProgress state, showSagaEvent, handleSagaChapterComplete, le bouton saga existant lignes ~1407-1431, le bandeau saga lignes ~1026-1038)
    components/mascot/VisitorSlot.tsx (interface et props)
    components/mascot/SagaWorldEvent.tsx (props mises a jour)
  </read_first>
  <action>
    **1. Ajouter les imports :**
    ```typescript
    import { VisitorSlot } from '../../components/mascot/VisitorSlot';
    ```
    Ajouter aussi le require pour le sprite idle du visiteur (pour le portrait SagaWorldEvent) :
    ```typescript
    const VISITOR_IDLE_FRAME = require('../../assets/garden/animals/voyageur/idle_1.png');
    ```

    **2. Ajouter le state pour l'orchestration depart :**
    ```typescript
    const [visitorShouldDepart, setVisitorShouldDepart] = useState(false);
    ```
    Ce state est `false` par defaut, passe a `true` quand `onDismiss` de SagaWorldEvent est appele.

    **3. Ajouter VisitorSlot dans le diorama — couche 3.6 (per D-04) :**
    Inserer entre le CompanionSlot (couche ~3.5) et les particules ambiantes, au meme niveau z que les habitants :
    ```typescript
    {/* Couche 3.6 : Visiteur saga */}
    {sagaChapterAvailable && isOwnTree && sagaProgress && (
      <View style={{ ...StyleSheet.absoluteFillObject, zIndex: visitorDialogOpen ? 20 : 3 }}
            pointerEvents="box-none">
        <VisitorSlot
          visible={sagaChapterAvailable && !showSagaEvent}
          sagaId={sagaProgress.sagaId}
          containerWidth={SCREEN_W}
          containerHeight={DIORAMA_HEIGHT_BY_STAGE[stageIdx] ?? SCREEN_H * 0.60}
          onTap={() => {
            Haptics.impactAsync(Haptics.ImpactFeedbackStyle.Medium);
            setShowSagaEvent(true);
          }}
          shouldDepart={visitorShouldDepart}
          isLastChapter={activeSaga ? sagaProgress.currentChapter >= activeSaga.chapters.length : false}
          onDepartComplete={() => setVisitorShouldDepart(false)}
        />
      </View>
    )}
    ```

    **4. Modifier le callback onDismiss de SagaWorldEvent (per D-10, pitfall 1 recherche) :**
    Le `onDismiss` existant dans tree.tsx fait `setShowSagaEvent(false)`. Ajouter le declenchement du depart :
    ```typescript
    // Dans le rendu de SagaWorldEvent, modifier onDismiss :
    onDismiss={() => {
      setShowSagaEvent(false);
      setVisitorShouldDepart(true);
    }}
    ```
    La sequence correcte est : SagaWorldEvent joue sa propre animation de dismiss (600ms) → appelle onDismiss → tree.tsx ferme SagaWorldEvent + declenche depart du visiteur.

    **5. Passer le portrait au SagaWorldEvent refactorise :**
    ```typescript
    <SagaWorldEvent
      sagaProgress={sagaProgress}
      containerHeight={DIORAMA_HEIGHT_BY_STAGE[stageIdx] ?? SCREEN_H * 0.60}
      onChapterComplete={handleSagaChapterComplete}
      onDismiss={() => { setShowSagaEvent(false); setVisitorShouldDepart(true); }}
      visitorIdleFrame={VISITOR_IDLE_FRAME}
    />
    ```

    **6. Supprimer le bouton saga existant (per D-05) :**
    Chercher le bloc conditionnel `{sagaChapterAvailable && !showSagaEvent && isOwnTree && activeSaga && (` (lignes ~1407-1431) qui rend un `TouchableOpacity` avec le texte "Saga en attente" ou similaire. **Supprimer entierement ce bloc.** Le tap sur le visiteur remplace ce bouton.

    **7. Reset visitorShouldDepart quand le profil change ou la saga change :**
    Ajouter dans le useEffect existant qui reset les states quand `profile?.id` change :
    ```typescript
    setVisitorShouldDepart(false);
    ```

    **8. Recharger sagaProgress sur focus (pitfall 2 recherche) :**
    Verifier que `loadSagaProgress` est appele dans un `useFocusEffect` ou dans un `useEffect` avec les bonnes deps pour que le visiteur apparaisse meme si la saga a ete demarree depuis le dashboard. Si deja present, ne rien changer.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -c "tree.tsx" || echo "0 errors"</automated>
  </verify>
  <acceptance_criteria>
    - `tree.tsx` contient `import { VisitorSlot }` ou `import VisitorSlot`
    - `tree.tsx` contient `<VisitorSlot` (rendu du composant)
    - `tree.tsx` contient `visitorShouldDepart` (state orchestration depart)
    - `tree.tsx` contient `setVisitorShouldDepart(true)` dans le onDismiss
    - `tree.tsx` contient `visitorIdleFrame={VISITOR_IDLE_FRAME}` sur SagaWorldEvent
    - `tree.tsx` ne contient plus le TouchableOpacity "Saga en attente" (bouton supprime)
    - `npx tsc --noEmit` ne produit pas de nouvelle erreur
  </acceptance_criteria>
  <done>Le visiteur saga apparait dans le diorama quand un chapitre est disponible, le tap ouvre SagaWorldEvent avec portrait sprite, et apres completion le visiteur repart en marchant. Le bouton saga est supprime.</done>
</task>

</tasks>

<verification>
- `grep "VisitorSlot" app/\(tabs\)/tree.tsx` trouve des resultats
- `grep "visitorIdleFrame" components/mascot/SagaWorldEvent.tsx` trouve des resultats
- `grep -c "Saga en attente" app/\(tabs\)/tree.tsx` retourne 0 (bouton supprime)
- `npx tsc --noEmit` passe sans nouvelles erreurs
</verification>

<success_criteria>
L'experience immersive est complete : le visiteur apparait dans la scene, le joueur tape dessus pour dialoguer, et le visiteur repart apres completion. SagaWorldEvent affiche le sprite du voyageur comme portrait.
</success_criteria>

<output>
After completion, create `.planning/phases/11-sagas-immersives/11-02-SUMMARY.md`
</output>
