---
phase: 11-sagas-immersives
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - components/mascot/SagaWorldEvent.tsx
  - app/(tabs)/tree.tsx
autonomous: true
requirements:
  - SAG-02
  - SAG-04

must_haves:
  truths:
    - "Taper sur le visiteur dans la scene ouvre le dialogue saga SagaWorldEvent"
    - "Le spiritGlow emoji est remplace par le sprite du voyageur comme portrait"
    - "Apres completion du chapitre, le visiteur part en marchant vers la droite"
    - "Le visiteur n'apparait que quand un chapitre est disponible (saga active + pas joue aujourd'hui)"
    - "Quand le joueur fait un choix saga, le visiteur joue une animation de reaction (joy, surprise ou mystery) avant le depart"
  artifacts:
    - path: "components/mascot/SagaWorldEvent.tsx"
      provides: "Dialogue saga refactorise (tap-triggered, portrait sprite, onChoiceReaction callback)"
      exports: ["SagaWorldEvent", "SagaWorldEventProps"]
    - path: "app/(tabs)/tree.tsx"
      provides: "Integration VisitorSlot dans le diorama + orchestration saga + reaction aux choix"
  key_links:
    - from: "app/(tabs)/tree.tsx"
      to: "components/mascot/VisitorSlot.tsx"
      via: "import et render dans couche 3.6 du diorama"
      pattern: "VisitorSlot"
    - from: "app/(tabs)/tree.tsx"
      to: "components/mascot/SagaWorldEvent.tsx"
      via: "showSagaEvent state declenche par onTap du VisitorSlot"
      pattern: "setShowSagaEvent.*true"
    - from: "components/mascot/SagaWorldEvent.tsx"
      to: "app/(tabs)/tree.tsx"
      via: "onDismiss callback declenche visitorShouldDepart"
      pattern: "onDismiss"
    - from: "components/mascot/SagaWorldEvent.tsx"
      to: "components/mascot/VisitorSlot.tsx"
      via: "onChoiceReaction callback remonte un ReactionType vers tree.tsx qui le passe en prop au VisitorSlot"
      pattern: "onChoiceReaction.*reactionType"
---

<objective>
Refactorer SagaWorldEvent pour etre declenche par tap (plus auto), ajouter un callback de reaction aux choix, et integrer VisitorSlot dans la scene de l'arbre avec l'orchestration complete arrivee -> dialogue -> reaction -> depart.

Purpose: C'est le coeur de l'experience immersive — le visiteur vit dans le diorama et le joueur interagit avec lui pour avancer la saga. Les animations de reaction aux choix rendent l'interaction vivante (per SAG-04).
Output: SagaWorldEvent refactorise avec portrait sprite et callback reaction, tree.tsx avec VisitorSlot integre et orchestration reaction + depart.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-sagas-immersives/11-CONTEXT.md
@.planning/phases/11-sagas-immersives/11-RESEARCH.md
@.planning/phases/11-sagas-immersives/11-01-SUMMARY.md

@components/mascot/SagaWorldEvent.tsx
@app/(tabs)/tree.tsx
@components/mascot/VisitorSlot.tsx (cree dans Plan 01)

<interfaces>
From components/mascot/VisitorSlot.tsx (Plan 01):
```typescript
export type ReactionType = 'joy' | 'surprise' | 'mystery';

export interface VisitorSlotProps {
  visible: boolean;
  sagaId: string;
  containerWidth: number;
  containerHeight: number;
  onTap: () => void;
  shouldDepart: boolean;
  isLastChapter: boolean;
  onDepartComplete?: () => void;
  reactionType?: ReactionType;       // NOUVEAU — reaction visuelle aux choix
  onReactionComplete?: () => void;   // NOUVEAU — callback fin de reaction
}
export function VisitorSlot(props: VisitorSlotProps): React.ReactElement | null;
```

From components/mascot/SagaWorldEvent.tsx:
```typescript
export interface SagaWorldEventProps {
  sagaProgress: SagaProgress;
  containerHeight: number;
  onChapterComplete: (choiceId: string, points: number, sagaNote: string, rewardItem?) => Promise<void>;
  onDismiss: () => void;
}
```

From app/(tabs)/tree.tsx (state existant):
```typescript
const [sagaProgress, setSagaProgress] = useState<SagaProgress | null>(null);
const [showSagaEvent, setShowSagaEvent] = useState(false);
const sagaChapterAvailable = !!sagaProgress && sagaProgress.status === 'active' && !sagaChapterDone;
const handleSagaChapterComplete = useCallback(async (...) => { ... }, [...]);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactorer SagaWorldEvent — portrait sprite voyageur + callback reaction aux choix</name>
  <files>components/mascot/SagaWorldEvent.tsx</files>
  <read_first>
    components/mascot/SagaWorldEvent.tsx (lire entierement — 550 lignes, comprendre spiritGlow, phases, animation dismiss, le moment ou un choix est selectionne)
    lib/mascot/sagas-types.ts (SagaChoice — pour voir si les choix ont un champ reaction/emotion)
  </read_first>
  <action>
    Modifications chirurgicales de SagaWorldEvent (per D-15, D-16, SAG-04). Le composant reste fonctionnel — le visuel "esprit" change et un callback de reaction est ajoute.

    **1. Ajouter les props pour portrait visiteur et reaction (per D-16, SAG-04) :**
    ```typescript
    // Ajouter a SagaWorldEventProps :
    visitorIdleFrame?: ImageSourcePropType;  // sprite pour le portrait
    onChoiceReaction?: (reactionType: 'joy' | 'surprise' | 'mystery') => void;  // NOUVEAU — reaction aux choix
    ```
    Ajouter `import { Image, ImageSourcePropType } from 'react-native';` si pas deja present.
    Ajouter `import type { ReactionType } from './VisitorSlot';` pour coherence de type (optionnel — peut aussi utiliser le literal type inline).

    **2. Remplacer le rendu spiritGlow par le portrait du visiteur :**
    Chercher le bloc qui rend le `spiritGlow` (cercle lumineux + emoji thematique). Il utilise probablement un `Animated.View` avec `borderRadius: 999`, une couleur de fond, et un emoji `<Text>`.

    Remplacer ce bloc par :
    ```typescript
    {visitorIdleFrame ? (
      <Animated.View style={[styles.visitorPortrait, spiritAnimStyle]}>
        <Image
          source={visitorIdleFrame}
          style={{ width: 72, height: 72 }}
          resizeMode="contain"
        />
      </Animated.View>
    ) : (
      // Garder l'ancien spiritGlow comme fallback si pas de sprite
      <Animated.View style={[styles.spiritGlow, spiritAnimStyle]}>
        ...ancien contenu...
      </Animated.View>
    )}
    ```
    - Le `visitorPortrait` style : memes dimensions que spiritGlow (~72x72), centre en haut du diorama
    - Reutiliser l'animation existante du spiritGlow (pulsation, scale) sur le portrait

    **3. Ajouter le style `visitorPortrait` dans StyleSheet.create :**
    ```typescript
    visitorPortrait: {
      width: 72,
      height: 72,
      alignItems: 'center',
      justifyContent: 'center',
    },
    ```

    **4. Appeler onChoiceReaction quand le joueur fait un choix (per SAG-04) :**
    Trouver le handler qui est appele quand le joueur selectionne un choix (probablement dans la phase 'choices' → transition vers 'chosen'). C'est le moment ou `onChapterComplete` est appele ou juste avant.

    Ajouter l'appel a `onChoiceReaction` JUSTE AVANT la transition vers 'chosen' ou l'appel a `onChapterComplete` :
    ```typescript
    // Determiner le type de reaction en fonction du choix ou du chapitre
    // Logique de mapping : utiliser le champ choice.trait ou choice.id pour deduire la reaction
    const choiceReactionMap: Record<string, 'joy' | 'surprise' | 'mystery'> = {
      // Traits positifs / encourageants → joie
      // Traits de decouverte / revelation → surprise
      // Traits enigmatiques / profonds → mystere
    };

    // Heuristique simple basee sur l'index du choix et le chapitre :
    // - Premier choix dans la liste → 'joy' (choix "safe", positif)
    // - Deuxieme choix → 'surprise' (choix alternatif, inattendu)
    // - Troisieme choix (si existe) → 'mystery' (choix enigmatique)
    // OU utiliser choice.trait si disponible :
    // - courage, compassion, creativity → 'joy'
    // - curiosity, wisdom → 'surprise'
    // - intuition, resilience → 'mystery'
    const reactionForChoice = (choiceIndex: number, choiceTrait?: string): 'joy' | 'surprise' | 'mystery' => {
      if (choiceTrait) {
        if (['courage', 'compassion', 'creativity', 'humor'].includes(choiceTrait)) return 'joy';
        if (['curiosity', 'wisdom', 'observation'].includes(choiceTrait)) return 'surprise';
        return 'mystery'; // intuition, resilience, patience, etc.
      }
      // Fallback par index
      return choiceIndex === 0 ? 'joy' : choiceIndex === 1 ? 'surprise' : 'mystery';
    };

    // Dans le handler de selection de choix :
    onChoiceReaction?.(reactionForChoice(selectedIndex, selectedChoice.trait));
    ```

    **5. NE PAS modifier :**
    - La logique des phases (entering, narrative, choices, chosen, cliffhanger, finale_reveal, dismissing)
    - Le typewriter
    - Les choix cards slide-in
    - Le trait flash
    - Le cliffhanger / finale_reveal
    - La fonction animateDismiss et le callback onDismiss
    - Les animations de vignette
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -c "SagaWorldEvent" || echo "0 errors"</automated>
  </verify>
  <acceptance_criteria>
    - `SagaWorldEvent.tsx` contient `visitorIdleFrame` dans l'interface SagaWorldEventProps
    - `SagaWorldEvent.tsx` contient `ImageSourcePropType` (import)
    - `SagaWorldEvent.tsx` contient `visitorPortrait` (style ou rendu conditionnel)
    - `SagaWorldEvent.tsx` contient `onChoiceReaction` dans l'interface SagaWorldEventProps
    - `SagaWorldEvent.tsx` contient un appel a `onChoiceReaction` dans le handler de choix
    - `SagaWorldEvent.tsx` contient `reactionForChoice` ou logique equivalente de mapping choix → reaction
    - `SagaWorldEvent.tsx` conserve les phases : 'entering', 'narrative', 'choices', 'chosen', 'cliffhanger'
    - `SagaWorldEvent.tsx` conserve `onDismiss` dans les props
    - `SagaWorldEvent.tsx` conserve `onChapterComplete` dans les props
  </acceptance_criteria>
  <done>SagaWorldEvent affiche le sprite du voyageur comme portrait au lieu du spiritGlow emoji. Un callback onChoiceReaction est appele avec le type de reaction (joy/surprise/mystery) quand le joueur selectionne un choix, permettant au VisitorSlot de jouer l'animation correspondante. L'ancien rendu spiritGlow est garde en fallback. Toute la logique narrative est intacte.</done>
</task>

<task type="auto">
  <name>Task 2: Integrer VisitorSlot dans tree.tsx + orchestration arrivee/dialogue/reaction/depart</name>
  <files>app/(tabs)/tree.tsx</files>
  <read_first>
    app/(tabs)/tree.tsx (lire entierement — comprendre les couches diorama, sagaProgress state, showSagaEvent, handleSagaChapterComplete, le bouton saga existant lignes ~1407-1431, le bandeau saga lignes ~1026-1038)
    components/mascot/VisitorSlot.tsx (interface et props — notamment reactionType et onReactionComplete)
    components/mascot/SagaWorldEvent.tsx (props mises a jour — notamment onChoiceReaction)
  </read_first>
  <action>
    **1. Ajouter les imports :**
    ```typescript
    import { VisitorSlot } from '../../components/mascot/VisitorSlot';
    import type { ReactionType } from '../../components/mascot/VisitorSlot';
    ```
    Ajouter aussi le require pour le sprite idle du visiteur (pour le portrait SagaWorldEvent) :
    ```typescript
    const VISITOR_IDLE_FRAME = require('../../assets/garden/animals/voyageur/idle_1.png');
    ```

    **2. Ajouter les states pour l'orchestration depart + reaction (per SAG-04) :**
    ```typescript
    const [visitorShouldDepart, setVisitorShouldDepart] = useState(false);
    const [visitorReaction, setVisitorReaction] = useState<ReactionType | undefined>(undefined);
    ```
    - `visitorShouldDepart` : passe a `true` apres la fin de la reaction (ou directement si pas de reaction)
    - `visitorReaction` : le type de reaction en cours, `undefined` quand aucune reaction

    **3. Ajouter VisitorSlot dans le diorama — couche 3.6 (per D-04) :**
    Inserer entre le CompanionSlot (couche ~3.5) et les particules ambiantes, au meme niveau z que les habitants :
    ```typescript
    {/* Couche 3.6 : Visiteur saga */}
    {sagaChapterAvailable && isOwnTree && sagaProgress && (
      <View style={{ ...StyleSheet.absoluteFillObject, zIndex: visitorDialogOpen ? 20 : 3 }}
            pointerEvents="box-none">
        <VisitorSlot
          visible={sagaChapterAvailable && !showSagaEvent}
          sagaId={sagaProgress.sagaId}
          containerWidth={SCREEN_W}
          containerHeight={DIORAMA_HEIGHT_BY_STAGE[stageIdx] ?? SCREEN_H * 0.60}
          onTap={() => {
            Haptics.impactAsync(Haptics.ImpactFeedbackStyle.Medium);
            setShowSagaEvent(true);
          }}
          shouldDepart={visitorShouldDepart}
          isLastChapter={activeSaga ? sagaProgress.currentChapter >= activeSaga.chapters.length : false}
          onDepartComplete={() => setVisitorShouldDepart(false)}
          reactionType={visitorReaction}
          onReactionComplete={() => {
            setVisitorReaction(undefined);
            setVisitorShouldDepart(true);
          }}
        />
      </View>
    )}
    ```

    **4. Modifier le callback onDismiss de SagaWorldEvent (per D-10, SAG-04) :**
    Le `onDismiss` existant dans tree.tsx fait `setShowSagaEvent(false)`. Maintenant il declenche soit la reaction (si une reaction a ete enregistree), soit directement le depart :
    ```typescript
    // Callback pour la reaction aux choix — stocke le type de reaction
    const handleChoiceReaction = useCallback((reaction: ReactionType) => {
      setVisitorReaction(reaction);
    }, []);

    // onDismiss modifie : ferme le dialogue, le depart sera declenche par onReactionComplete
    // OU directement si pas de reaction enregistree
    const handleSagaDismiss = useCallback(() => {
      setShowSagaEvent(false);
      // Si une reaction est en cours (visitorReaction est set), le depart sera declenche
      // par onReactionComplete du VisitorSlot apres la fin de l'animation de reaction.
      // Si pas de reaction (cas ou le joueur ferme sans choisir), declencher le depart directement.
      if (!visitorReaction) {
        setVisitorShouldDepart(true);
      }
      // Si visitorReaction est deja set, onReactionComplete fera setVisitorShouldDepart(true)
    }, [visitorReaction]);
    ```

    **SEQUENCE COMPLETE reaction → depart (per SAG-04) :**
    1. Joueur tape sur visiteur → `setShowSagaEvent(true)` → SagaWorldEvent s'ouvre
    2. Joueur fait un choix → SagaWorldEvent appelle `onChoiceReaction('joy')` → `setVisitorReaction('joy')`
    3. SagaWorldEvent joue ses animations de choix (trait flash, etc.) puis appelle `onDismiss`
    4. `handleSagaDismiss` → `setShowSagaEvent(false)` (le dialogue se ferme, le visiteur redevient visible)
    5. VisitorSlot recoit `reactionType='joy'` → passe en etat 'reacting' → joue l'animation bounce
    6. Animation de reaction terminee → `onReactionComplete` → `setVisitorReaction(undefined)` + `setVisitorShouldDepart(true)`
    7. VisitorSlot recoit `shouldDepart=true` → passe en etat 'departing' → marche vers la droite
    8. Depart termine → `onDepartComplete` → `setVisitorShouldDepart(false)`

    **5. Passer le portrait et le callback reaction au SagaWorldEvent refactorise :**
    ```typescript
    <SagaWorldEvent
      sagaProgress={sagaProgress}
      containerHeight={DIORAMA_HEIGHT_BY_STAGE[stageIdx] ?? SCREEN_H * 0.60}
      onChapterComplete={handleSagaChapterComplete}
      onDismiss={handleSagaDismiss}
      visitorIdleFrame={VISITOR_IDLE_FRAME}
      onChoiceReaction={handleChoiceReaction}
    />
    ```

    **6. Supprimer le bouton saga existant (per D-05) :**
    Chercher le bloc conditionnel `{sagaChapterAvailable && !showSagaEvent && isOwnTree && activeSaga && (` (lignes ~1407-1431) qui rend un `TouchableOpacity` avec le texte "Saga en attente" ou similaire. **Supprimer entierement ce bloc.** Le tap sur le visiteur remplace ce bouton.

    **7. Reset des states quand le profil change ou la saga change :**
    Ajouter dans le useEffect existant qui reset les states quand `profile?.id` change :
    ```typescript
    setVisitorShouldDepart(false);
    setVisitorReaction(undefined);
    ```

    **8. Recharger sagaProgress sur focus (pitfall 2 recherche) :**
    Verifier que `loadSagaProgress` est appele dans un `useFocusEffect` ou dans un `useEffect` avec les bonnes deps pour que le visiteur apparaisse meme si la saga a ete demarree depuis le dashboard. Si deja present, ne rien changer.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -c "tree.tsx" || echo "0 errors"</automated>
  </verify>
  <acceptance_criteria>
    - `tree.tsx` contient `import { VisitorSlot }` ou `import VisitorSlot`
    - `tree.tsx` contient `import type { ReactionType }`
    - `tree.tsx` contient `<VisitorSlot` (rendu du composant)
    - `tree.tsx` contient `visitorShouldDepart` (state orchestration depart)
    - `tree.tsx` contient `visitorReaction` (state orchestration reaction)
    - `tree.tsx` contient `setVisitorReaction` dans le handler de reaction
    - `tree.tsx` contient `onChoiceReaction={handleChoiceReaction}` sur SagaWorldEvent
    - `tree.tsx` contient `reactionType={visitorReaction}` sur VisitorSlot
    - `tree.tsx` contient `onReactionComplete` sur VisitorSlot
    - `tree.tsx` contient `visitorIdleFrame={VISITOR_IDLE_FRAME}` sur SagaWorldEvent
    - `tree.tsx` ne contient plus le TouchableOpacity "Saga en attente" (bouton supprime)
    - `npx tsc --noEmit` ne produit pas de nouvelle erreur
  </acceptance_criteria>
  <done>Le visiteur saga apparait dans le diorama quand un chapitre est disponible, le tap ouvre SagaWorldEvent avec portrait sprite. Quand le joueur fait un choix, le visiteur joue une animation de reaction (joy/surprise/mystery) visible dans la scene, puis repart en marchant. Le bouton saga est supprime.</done>
</task>

</tasks>

<verification>
- `grep "VisitorSlot" app/\(tabs\)/tree.tsx` trouve des resultats
- `grep "visitorIdleFrame" components/mascot/SagaWorldEvent.tsx` trouve des resultats
- `grep "onChoiceReaction" components/mascot/SagaWorldEvent.tsx` trouve des resultats
- `grep "visitorReaction" app/\(tabs\)/tree.tsx` trouve des resultats
- `grep "reactionType" app/\(tabs\)/tree.tsx` trouve des resultats
- `grep -c "Saga en attente" app/\(tabs\)/tree.tsx` retourne 0 (bouton supprime)
- `npx tsc --noEmit` passe sans nouvelles erreurs
</verification>

<success_criteria>
L'experience immersive est complete : le visiteur apparait dans la scene, le joueur tape dessus pour dialoguer, le visiteur reagit visuellement au choix du joueur (joie, surprise ou mystere per SAG-04), puis repart apres completion. SagaWorldEvent affiche le sprite du voyageur comme portrait et remonte les reactions de choix.
</success_criteria>

<output>
After completion, create `.planning/phases/11-sagas-immersives/11-02-SUMMARY.md`
</output>
