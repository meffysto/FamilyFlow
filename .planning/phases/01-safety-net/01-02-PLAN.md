---
phase: 01-safety-net
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/telegram.ts
  - lib/ai-service.ts
  - hooks/useVault.ts
  - eslint.config.js
  - package.json
autonomous: true
requirements: [QUAL-01, QUAL-02, QUAL-04, QUAL-05]

must_haves:
  truths:
    - "Les 5 fonctions deprecated de telegram.ts sont supprimees"
    - "La propriete menageTasks est absente de l'interface VaultContext dans ai-service.ts"
    - "Le bloc migration menageTasks dans useVault.ts est supprime"
    - "Les 6 assertions as any dans useVault.ts sont remplacees par des types corrects"
    - "npx tsc --noEmit ne rapporte aucun nouveau as any sur les chemins de mutation"
    - "ESLint est configure avec no-explicit-any en warn"
  artifacts:
    - path: "lib/telegram.ts"
      provides: "Fichier nettoye sans fonctions deprecated"
    - path: "hooks/useVault.ts"
      provides: "Fichier sans as any sur les chemins de mutation et sans bloc migration menageTasks"
    - path: "lib/ai-service.ts"
      provides: "Interface VaultContext sans menageTasks"
    - path: "eslint.config.js"
      provides: "Config ESLint flat avec no-explicit-any warn"
  key_links:
    - from: "hooks/useVault.ts"
      to: "types dans lib/mascot/types.ts"
      via: "import des types pour remplacer as any"
      pattern: "import.*TreeSpecies"
    - from: "eslint.config.js"
      to: "hooks/*.ts, lib/*.ts"
      via: "files glob pattern"
      pattern: "hooks/\\*\\*"
---

<objective>
Nettoyer le code mort, corriger les assertions `as any` sur les chemins de mutation, et configurer ESLint.

Purpose: Eliminer le code mort qui accumule de la dette, typer correctement les mutations pour prevenir les bugs, et mettre un garde-fou ESLint.
Output: telegram.ts nettoye, useVault.ts type correctement, ESLint configure.
</objective>

<execution_context>
@/Users/gabrielwaltio/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gabrielwaltio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-safety-net/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Supprimer code mort — telegram.ts deprecated + menageTasks</name>
  <files>lib/telegram.ts, lib/ai-service.ts, hooks/useVault.ts</files>
  <read_first>lib/telegram.ts, lib/ai-service.ts, hooks/useVault.ts</read_first>
  <action>
    Dans lib/telegram.ts:
    - Supprimer les 5 fonctions deprecated (lignes ~49-110): formatTaskCompletedMessage, formatLootBoxMessage, formatAllTasksDoneMessage, formatLeaderboardMessage, formatDailySummaryMessage
    - Garder tout le reste du fichier intact (sendTelegramMessage, formatTelegramMessage, etc.)
    - Zero callers confirmes par la recherche

    Dans lib/ai-service.ts:
    - Supprimer la propriete `menageTasks?: Task[];` de l'interface VaultContext (ligne ~47)
    - Supprimer le fallback `ctx.menageTasks ?? ctx.tasks.filter(...)` a la ligne ~210 et le remplacer par directement `ctx.tasks.filter(t => ...)` (le calcul inline que le fallback fait deja)
    - NE PAS toucher aux parametres `menageTasks` dans buildWeeklyRecapData — c'est un parametre local, pas la prop VaultContext

    Dans hooks/useVault.ts:
    - Supprimer le bloc migrateMenageToTasks() (lignes ~393-481) — la fonction entiere
    - Supprimer l'appel a migrateMenageToTasks() (ligne ~584 environ)
    - Supprimer la constante MENAGE_FILE si elle n'est plus utilisee apres
    - Verifier qu'aucune autre reference a migrateMenageToTasks n'existe dans le fichier
  </action>
  <verify>
    <automated>cd /Users/gabrielwaltio/Documents/family-vault && npx tsc --noEmit 2>&1 | grep -c "error TS" || echo "0 errors"</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "formatTaskCompletedMessage\|formatLootBoxMessage\|formatAllTasksDoneMessage\|formatLeaderboardMessage\|formatDailySummaryMessage" lib/telegram.ts retourne 0
    - grep -c "menageTasks" lib/ai-service.ts retourne 0
    - grep -c "migrateMenageToTasks" hooks/useVault.ts retourne 0
    - npx tsc --noEmit ne montre pas de nouvelles erreurs
  </acceptance_criteria>
  <done>Les 5 fonctions deprecated sont supprimees de telegram.ts. menageTasks est absent de VaultContext. Le bloc de migration est supprime de useVault.ts.</done>
</task>

<task type="auto">
  <name>Task 2: Corriger as any dans useVault.ts + configurer ESLint</name>
  <files>hooks/useVault.ts, eslint.config.js, package.json</files>
  <read_first>hooks/useVault.ts, lib/mascot/types.ts, package.json</read_first>
  <action>
    Dans hooks/useVault.ts, corriger les 6 assertions `as any` sur les chemins de mutation:

    1. Ligne ~703 (routines catch): remplacer `[] as any[]` par le type Routine[] — ajouter le type de retour annote sur le catch:
       `.catch((e): Routine[] => { debugErrors.push(...); return []; })`
       Verifier que le type `Routine` est bien importe/defini dans le fichier.

    2. Ligne ~876 (gamification catch): remplacer `[] as any[]` par `null` avec type annote:
       `.catch((): GamificationData | null => null)`
       Ce n'est PAS un array — c'est un objet gamification ou null.

    3. Ligne ~879 (gratitude catch): remplacer `[] as any[]` par:
       `.catch((): GratitudeEntry[] => [])`

    4. Ligne ~882 (wishlist catch): remplacer `[] as any[]` par:
       `.catch((): WishlistItem[] => [])`

    5. Ligne ~1282 (treeSpecies cast): remplacer `species as any` par `species as TreeSpecies`
       Importer TreeSpecies depuis '../lib/mascot/types' si pas deja importe.

    6. Ligne ~1705 (delete lineIndex): remplacer `delete (updated as any).lineIndex` par destructuring:
       `const { lineIndex: _, ...updatedClean } = updated;`
       Puis utiliser updatedClean a la place de updated pour l'appel suivant.

    Installer ESLint et configurer:
    ```bash
    npm install --save-dev eslint @typescript-eslint/eslint-plugin @typescript-eslint/parser typescript-eslint
    ```

    Creer eslint.config.js (ESLint v9 flat config, format CommonJS car le projet n'a pas "type": "module"):
    ```javascript
    const tseslint = require('typescript-eslint');

    module.exports = tseslint.config(
      {
        files: ['hooks/**/*.ts', 'hooks/**/*.tsx', 'lib/**/*.ts'],
        extends: [tseslint.configs.recommended],
        rules: {
          '@typescript-eslint/no-explicit-any': 'warn',
        },
      }
    );
    ```

    Ajouter script dans package.json scripts:
    `"lint": "eslint hooks/ lib/ --max-warnings=0"`

    NOTE: Le --max-warnings=0 est ambitieux. Si trop de warnings pre-existants, changer en `"lint": "eslint hooks/ lib/"` et documenter le nombre de warnings restants.
  </action>
  <verify>
    <automated>cd /Users/gabrielwaltio/Documents/family-vault && grep -c "as any" hooks/useVault.ts && npx tsc --noEmit 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - grep "as any" hooks/useVault.ts retourne 0 lignes (les 6 sont corrigees)
    - npx tsc --noEmit ne montre pas de nouvelles erreurs
    - Le fichier eslint.config.js existe avec la regle no-explicit-any
    - npm run lint s'execute (peut avoir des warnings mais pas de crash)
    - grep "typescript-eslint" package.json retourne au moins 1 ligne
  </acceptance_criteria>
  <done>Les 6 as any sont remplaces par des types corrects. ESLint est configure avec no-explicit-any en warn. `npm run lint` fonctionne.</done>
</task>

</tasks>

<verification>
1. grep -rn "as any" hooks/useVault.ts — doit retourner 0 resultats
2. grep -c "formatTaskCompletedMessage" lib/telegram.ts — doit retourner 0
3. grep -c "menageTasks" lib/ai-service.ts — doit retourner 0
4. npx tsc --noEmit — pas de nouvelles erreurs
5. npx eslint --version — confirme installation
</verification>

<success_criteria>
- Code mort supprime (5 fonctions telegram, menageTasks, migration)
- 0 assertions `as any` dans useVault.ts
- ESLint configure et fonctionnel
- `npx tsc --noEmit` passe sans nouvelles erreurs
- `npx jest` passe toujours (pas de regression)
</success_criteria>

<output>
After completion, create `.planning/phases/01-safety-net/01-02-SUMMARY.md`
</output>
