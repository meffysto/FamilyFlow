---
phase: 01-safety-net
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/__tests__/budget.test.ts
  - lib/__tests__/farm-engine.test.ts
  - lib/__tests__/sagas-engine.test.ts
  - lib/__tests__/world-grid.test.ts
autonomous: true
requirements: [TEST-01, TEST-02, TEST-03, TEST-04, TEST-05]

must_haves:
  truths:
    - "npx jest tourne sans erreur et les 4 nouveaux fichiers test passent"
    - "budget.test.ts couvre parsing config, parsing mois, serialisation, calcul stats"
    - "farm-engine.test.ts couvre plantCrop, advanceFarmCrops, harvestCrop, golden mutation, CSV round-trip"
    - "sagas-engine.test.ts couvre getDominantTrait, getChapterNarrativeKey, getSagaCompletionResult, restDaysRemaining"
    - "world-grid.test.ts couvre WORLD_GRID shape, CROP_CELLS filtering, getUnlockedCropCells, CELL_SIZES"
  artifacts:
    - path: "lib/__tests__/budget.test.ts"
      provides: "Tests unitaires budget parsing/serialisation/calculs"
      min_lines: 60
    - path: "lib/__tests__/farm-engine.test.ts"
      provides: "Tests unitaires farm-engine planter/recolter/avancer/golden/CSV"
      min_lines: 80
    - path: "lib/__tests__/sagas-engine.test.ts"
      provides: "Tests unitaires sagas-engine traits/chapters/completion/rest"
      min_lines: 60
    - path: "lib/__tests__/world-grid.test.ts"
      provides: "Tests unitaires world-grid shape/cells/unlock/sizes"
      min_lines: 40
  key_links:
    - from: "lib/__tests__/budget.test.ts"
      to: "lib/budget.ts"
      via: "import { parseBudgetConfig, parseBudgetMonth, serializeBudgetMonth }"
      pattern: "import.*from.*budget"
    - from: "lib/__tests__/farm-engine.test.ts"
      to: "lib/mascot/farm-engine.ts"
      via: "import { plantCrop, advanceFarmCrops, harvestCrop, serializeCrops, parseCrops }"
      pattern: "import.*from.*farm-engine"
    - from: "lib/__tests__/sagas-engine.test.ts"
      to: "lib/mascot/sagas-engine.ts"
      via: "import { getDominantTrait, getChapterNarrativeKey, getSagaCompletionResult }"
      pattern: "import.*from.*sagas-engine"
    - from: "lib/__tests__/world-grid.test.ts"
      to: "lib/mascot/world-grid.ts"
      via: "import { WORLD_GRID, getUnlockedCropCells, CROP_CELLS, CELL_SIZES }"
      pattern: "import.*from.*world-grid"
---

<objective>
Ajouter des tests unitaires pour les 4 modules critiques sans couverture : budget, farm-engine, sagas-engine, world-grid.

Purpose: Creer le filet de securite avant toute modification future — ces modules gerent les donnees financieres et la gamification.
Output: 4 fichiers test dans lib/__tests__/, tous passent avec `npx jest`.
</objective>

<execution_context>
@/Users/gabrielwaltio/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gabrielwaltio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-safety-net/01-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tests unitaires budget.ts + world-grid.ts</name>
  <files>lib/__tests__/budget.test.ts, lib/__tests__/world-grid.test.ts</files>
  <read_first>lib/budget.ts, lib/mascot/world-grid.ts, jest.config.js, lib/__tests__/mascot-engine.test.ts</read_first>
  <behavior>
    budget.test.ts:
    - parseBudgetConfig avec contenu valide retourne categories avec emoji/name/limit
    - parseBudgetConfig avec contenu vide retourne categories vide
    - parseBudgetMonth parse une ligne "- 2026-01-15 | emoji Cat | 45.50 | Desc" en BudgetEntry avec amount=45.5
    - parseBudgetMonth gere les montants negatifs (revenus)
    - serializeBudgetMonth fait le round-trip avec parseBudgetMonth
    - calculateBudgetStats (si exporte) calcule total depenses et reste par categorie

    world-grid.test.ts:
    - WORLD_GRID a exactement 20 cellules (6x4 grille - 4 tree + 2 deco adjustments)
    - CROP_CELLS ne contient que des cellules de type 'crop'
    - BUILDING_CELLS ne contient que des cellules de type 'building'
    - getUnlockedCropCells(1) retourne les premieres parcelles debloquees pour stage 1
    - getUnlockedCropCells retourne plus de parcelles pour un stage plus eleve
    - CELL_SIZES a les cles attendues (crop, building, tree, deco)
  </behavior>
  <action>
    Creer lib/__tests__/budget.test.ts en suivant le pattern existant de lib/__tests__/parser.test.ts (import direct, describe/it, donnees inline).

    Pour budget.test.ts:
    - Importer parseBudgetConfig, parseBudgetMonth, serializeBudgetMonth depuis '../budget'
    - Utiliser des strings markdown inline comme donnees de test (voir pattern dans 01-RESEARCH.md Code Examples)
    - Tester le round-trip serialisation: serializeBudgetMonth(parseBudgetMonth(input)) preserves data
    - Nommer les describes et it en francais

    Pour world-grid.test.ts:
    - Importer WORLD_GRID, getUnlockedCropCells, CROP_CELLS, BUILDING_CELLS, CELL_SIZES depuis '../mascot/world-grid'
    - Tester la shape des constantes (length, types de cellules)
    - Tester getUnlockedCropCells avec differents treeStage values
    - Verifier que unlockOrder est croissant dans CROP_CELLS
  </action>
  <verify>
    <automated>cd /Users/gabrielwaltio/Documents/family-vault && npx jest budget.test.ts world-grid.test.ts --no-coverage 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - budget.test.ts existe avec au moins 5 it() blocks
    - world-grid.test.ts existe avec au moins 5 it() blocks
    - `npx jest budget.test.ts` passe: "Tests: X passed, X total"
    - `npx jest world-grid.test.ts` passe: "Tests: X passed, X total"
  </acceptance_criteria>
  <done>Les deux fichiers test passent. budget.ts a couverture sur parsing/serialisation, world-grid.ts a couverture sur shape et unlock logic.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tests unitaires farm-engine.ts + sagas-engine.ts</name>
  <files>lib/__tests__/farm-engine.test.ts, lib/__tests__/sagas-engine.test.ts</files>
  <read_first>lib/mascot/farm-engine.ts, lib/mascot/sagas-engine.ts, lib/mascot/types.ts, lib/mascot/seasons.ts, lib/__tests__/mascot-engine.test.ts</read_first>
  <behavior>
    farm-engine.test.ts:
    - plantCrop sur parcelle vide cree une culture avec cropId et plotIndex corrects
    - plantCrop sur parcelle occupee ne duplique pas
    - plantCrop avec Math.random < 0.03 cree une culture golden (isGolden=true)
    - plantCrop avec Math.random >= 0.03 cree une culture normale (isGolden=false ou undefined)
    - advanceFarmCrops avance la premiere culture non-mature de 1 etape
    - advanceFarmCrops avec bonus saisonnier avance de 2 etapes
    - harvestCrop sur culture mature retourne recompense = harvestReward
    - harvestCrop sur culture golden retourne recompense = harvestReward * 5
    - serializeCrops/parseCrops round-trip preserve toutes les proprietes
    - parseCrops avec CSV contenant goldenFlag=1 retourne isGolden=true

    sagas-engine.test.ts:
    - getDominantTrait retourne le trait avec le plus haut score
    - getDominantTrait avec scores egaux retourne le premier
    - getChapterNarrativeKey retourne une cle narrative valide pour un chapitre donne
    - getSagaCompletionResult retourne les recompenses de fin de saga
    - restDaysRemaining calcule les jours restants correctement
    - restDaysRemaining retourne 0 si le cooldown est expire
  </behavior>
  <action>
    Creer lib/__tests__/farm-engine.test.ts:
    - Importer plantCrop, advanceFarmCrops, harvestCrop, serializeCrops, parseCrops, GOLDEN_CROP_CHANCE, GOLDEN_HARVEST_MULTIPLIER depuis '../mascot/farm-engine'
    - Mock getCurrentSeason via jest.mock('../mascot/seasons', () => ({ getCurrentSeason: jest.fn().mockReturnValue('printemps') }))
    - Pour tester golden: jest.spyOn(Math, 'random').mockReturnValueOnce(0.01) puis verifier isGolden===true
    - Pour tester non-golden: jest.spyOn(Math, 'random').mockReturnValueOnce(0.99) puis verifier isGolden falsy
    - Tester CSV round-trip: parseCrops(serializeCrops(crops)) deep-equals crops
    - afterEach: jest.restoreAllMocks()

    Creer lib/__tests__/sagas-engine.test.ts:
    - Importer getDominantTrait, getChapterNarrativeKey, getSagaCompletionResult, restDaysRemaining depuis '../mascot/sagas-engine'
    - Lire lib/mascot/types.ts pour comprendre les types Profile, Saga, SagaChapter utilises
    - Creer des fixtures minimales pour Profile et Saga
    - Pour restDaysRemaining: passer des dates explicites (pas Date.now()) si la fonction accepte un param date
    - Nommer les describes et it en francais
  </action>
  <verify>
    <automated>cd /Users/gabrielwaltio/Documents/family-vault && npx jest farm-engine.test.ts sagas-engine.test.ts --no-coverage 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - farm-engine.test.ts existe avec au moins 8 it() blocks
    - sagas-engine.test.ts existe avec au moins 5 it() blocks
    - `npx jest farm-engine.test.ts` passe: "Tests: X passed, X total"
    - `npx jest sagas-engine.test.ts` passe: "Tests: X passed, X total"
    - Le test golden utilise jest.spyOn(Math, 'random')
  </acceptance_criteria>
  <done>Les deux fichiers test passent. farm-engine a couverture sur plant/advance/harvest/golden/CSV. sagas-engine a couverture sur traits/chapters/completion/rest.</done>
</task>

</tasks>

<verification>
npx jest --no-coverage 2>&1 | tail -5
Doit afficher: tous les tests passent (13 existants + 4 nouveaux = 17 fichiers test)
</verification>

<success_criteria>
- 4 nouveaux fichiers test dans lib/__tests__/
- Chaque fichier a au moins 5 test cases
- `npx jest` passe sans erreur avec 17+ fichiers test
- Aucun test existant casse
</success_criteria>

<output>
After completion, create `.planning/phases/01-safety-net/01-01-SUMMARY.md`
</output>
