---
phase: 01-safety-net
plan: 04
type: execute
wave: 2
depends_on: [01, 03]
files_modified:
  - .maestro/flows/dashboard-navigation.yaml
  - .maestro/flows/task-completion.yaml
  - .maestro/flows/meal-planning.yaml
  - .maestro/config.yaml
autonomous: false
requirements: [TEST-07]

must_haves:
  truths:
    - "Maestro est installe et maestro test fonctionne"
    - "3 flows E2E couvrent les parcours critiques: navigation dashboard, completion tache, planification repas"
    - "Les flows utilisent des selecteurs accessibilityLabel ou testID stables"
  artifacts:
    - path: ".maestro/flows/dashboard-navigation.yaml"
      provides: "Flow E2E navigation dashboard — tabs + retour"
      min_lines: 15
    - path: ".maestro/flows/task-completion.yaml"
      provides: "Flow E2E cocher une tache"
      min_lines: 15
    - path: ".maestro/flows/meal-planning.yaml"
      provides: "Flow E2E naviguer vers repas et interagir"
      min_lines: 15
  key_links:
    - from: ".maestro/flows/dashboard-navigation.yaml"
      to: "app/(tabs)/index.tsx"
      via: "launchApp + tap sur elements du dashboard"
      pattern: "launchApp"
    - from: ".maestro/flows/task-completion.yaml"
      to: "app/(tabs)/tasks.tsx"
      via: "tap sur tache + verification etat"
      pattern: "tapOn"
---

<objective>
Configurer Maestro et creer 3 flows E2E pour les parcours utilisateur critiques.

Purpose: Les tests unitaires couvrent la logique pure, mais pas les parcours utilisateur reels. Maestro valide que l'app demarre, navigue, et que les interactions fonctionnent de bout en bout.
Output: 3 flows Maestro executables couvrant dashboard, taches, et repas.
</objective>

<execution_context>
@/Users/gabrielwaltio/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gabrielwaltio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-safety-net/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Installer Maestro et creer 3 flows E2E</name>
  <files>.maestro/flows/dashboard-navigation.yaml, .maestro/flows/task-completion.yaml, .maestro/flows/meal-planning.yaml, .maestro/config.yaml</files>
  <read_first>app/(tabs)/_layout.tsx, app/(tabs)/index.tsx, app/(tabs)/tasks.tsx, app/(tabs)/meals.tsx, package.json</read_first>
  <action>
    Installer Maestro CLI (si pas deja installe):
    ```bash
    curl -Ls "https://get.maestro.mobile.dev" | bash
    ```

    Creer .maestro/config.yaml:
    ```yaml
    appId: com.familyvault.dev
    ```

    Creer .maestro/flows/dashboard-navigation.yaml:
    - launchApp
    - assertVisible sur un element du dashboard (titre "FamilyFlow" ou premier widget visible)
    - tapOn l'onglet "Taches" (ou le label correspondant dans _layout.tsx)
    - assertVisible sur l'ecran taches
    - tapOn l'onglet accueil pour revenir
    - assertVisible sur le dashboard

    Creer .maestro/flows/task-completion.yaml:
    - launchApp
    - tapOn l'onglet "Taches"
    - assertVisible sur la liste de taches
    - Si une tache est visible, tapOn dessus
    - Verifier que l'interaction ne crash pas (assertVisible sur l'ecran apres interaction)

    Creer .maestro/flows/meal-planning.yaml:
    - launchApp
    - tapOn l'onglet "Repas" (ou naviguer via le dashboard)
    - assertVisible sur l'ecran repas
    - Verifier qu'un element de la liste repas est visible ou que l'ecran vide s'affiche correctement

    IMPORTANT: Les selecteurs doivent utiliser les textes visibles a l'ecran (labels des tabs, titres) plutot que des testID (qui n'existent probablement pas encore). Lire _layout.tsx pour trouver les noms exacts des tabs.

    Les flows doivent etre simples et robustes — pas de scenarios complexes avec creation de donnees. L'objectif est de valider que l'app demarre et que la navigation fonctionne.
  </action>
  <verify>
    <automated>cd /Users/gabrielwaltio/Documents/family-vault && ls .maestro/flows/*.yaml | wc -l && cat .maestro/config.yaml</automated>
  </verify>
  <acceptance_criteria>
    - .maestro/config.yaml existe avec appId: com.familyvault.dev
    - .maestro/flows/dashboard-navigation.yaml existe avec launchApp et au moins 2 tapOn
    - .maestro/flows/task-completion.yaml existe avec launchApp et navigation vers taches
    - .maestro/flows/meal-planning.yaml existe avec launchApp et navigation vers repas
    - Chaque flow YAML est valide (pas d'erreur de syntaxe)
  </acceptance_criteria>
  <done>3 flows Maestro crees, couvrant navigation dashboard, completion tache, et planification repas.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Valider les flows Maestro sur device</name>
  <files>none</files>
  <action>
    Checkpoint de verification humaine. Les flows Maestro ont ete crees et doivent etre valides sur un simulateur ou device physique.
  </action>
  <what-built>3 flows Maestro E2E ont ete crees. Ils doivent etre valides sur un simulateur ou device.</what-built>
  <how-to-verify>
    1. Verifier que Maestro est installe: `maestro --version`
    2. Lancer l'app sur le simulateur: `npx expo run:ios`
    3. Lancer les flows: `maestro test .maestro/flows/`
    4. Verifier que les 3 flows passent sans erreur
    5. Si un flow echoue, noter le message d'erreur pour ajustement
  </how-to-verify>
  <verify>Verification humaine — l'utilisateur execute maestro test et confirme</verify>
  <done>Les 3 flows Maestro passent sur simulateur ou device</done>
  <resume-signal>Tapez "approved" si les flows passent, ou decrivez les erreurs pour ajustement</resume-signal>
</task>

</tasks>

<verification>
1. maestro --version — installe
2. ls .maestro/flows/ — 3 fichiers yaml
3. maestro test .maestro/flows/ — 3 flows passent (verification manuelle)
</verification>

<success_criteria>
- Maestro CLI installe
- 3 flows E2E couvrant les parcours critiques
- Les flows sont executables (syntaxe YAML valide)
- Validation manuelle sur simulateur confirme le fonctionnement
</success_criteria>

<output>
After completion, create `.planning/phases/01-safety-net/01-04-SUMMARY.md`
</output>
