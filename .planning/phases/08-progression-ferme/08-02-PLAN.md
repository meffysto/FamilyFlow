---
phase: 08-progression-ferme
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - components/mascot/TechTreeSheet.tsx
  - components/mascot/WorldGridView.tsx
  - app/(tabs)/tree.tsx
autonomous: false

requirements: [PRO-01, PRO-02, PRO-03]

must_haves:
  truths:
    - "L'ecran arbre affiche un bouton Technologies qui ouvre le TechTreeSheet"
    - "Le TechTreeSheet montre les 3 branches avec noeuds debloques, debloquables et verrouilles"
    - "Tapper un noeud debloquable montre une confirmation avec cout et effet"
    - "Apres deblocage, le noeud passe en debloque et les feuilles sont deduites"
    - "Les parcelles d'extension apparaissent sur la grille avec un cadenas si verrouillees"
    - "Debloquer expansion-1 fait apparaitre les nouvelles parcelles avec animation"
  artifacts:
    - path: "components/mascot/TechTreeSheet.tsx"
      provides: "Bottom sheet tech tree avec 3 branches, noeuds interactifs, confirmation deblocage"
      min_lines: 150
    - path: "components/mascot/WorldGridView.tsx"
      provides: "Rendu des parcelles d'extension avec cadenas verrouille"
    - path: "app/(tabs)/tree.tsx"
      provides: "Bouton Technologies + state TechTreeSheet + passage bonus tech aux composants"
  key_links:
    - from: "components/mascot/TechTreeSheet.tsx"
      to: "hooks/useFarm.ts"
      via: "unlockTech called on confirmation"
      pattern: "unlockTech"
    - from: "components/mascot/TechTreeSheet.tsx"
      to: "lib/mascot/tech-engine.ts"
      via: "TECH_TREE, canUnlockTech, getTechBonuses imported"
      pattern: "TECH_TREE|canUnlockTech"
    - from: "app/(tabs)/tree.tsx"
      to: "components/mascot/TechTreeSheet.tsx"
      via: "visible/onClose props, profile data"
      pattern: "TechTreeSheet"
    - from: "components/mascot/WorldGridView.tsx"
      to: "lib/mascot/world-grid.ts"
      via: "getExpandedCropCells for rendering expansion cells"
      pattern: "getExpandedCropCells|EXPANSION_CROP_CELLS"
---

<objective>
Construire le TechTreeSheet (bottom sheet) avec affichage des 3 branches et noeuds interactifs, integrer les parcelles d'extension avec cadenas dans WorldGridView, et connecter le tout dans tree.tsx.

Purpose: Donner a l'utilisateur l'interface pour progresser dans le tech tree et voir l'impact visuel (nouvelles parcelles).
Output: TechTreeSheet fonctionnel, WorldGridView etendu avec cadenas, tree.tsx integre.
</objective>

<execution_context>
@/Users/gabrielwaltio/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gabrielwaltio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-progression-ferme/08-CONTEXT.md
@.planning/phases/08-progression-ferme/08-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 — tech-engine.ts exports -->
```typescript
// lib/mascot/tech-engine.ts
export type TechBranchId = 'culture' | 'elevage' | 'expansion';
export interface TechNode {
  id: string; branch: TechBranchId; order: number;
  labelKey: string; descriptionKey: string; emoji: string;
  cost: number; requires: string | null;
}
export interface TechBonuses {
  tasksPerStageReduction: number;
  harvestRewardMultiplier: number;
  newCrops: string[];
  productionIntervalMultiplier: number;
  buildingCapacityMultiplier: number;
  newResources: string[];
  extraCropCells: number;
  extraBuildingCells: number;
  hasLargeCropCell: boolean;
}
export const TECH_TREE: TechNode[];
export function getUnlockedTechs(farmTechCSV: string): string[];
export function canUnlockTech(techId: string, unlockedTechs: string[], coins: number): { canUnlock: boolean; reason?: string };
export function unlockTechNode(unlockedTechs: string[], techId: string): string[];
export function getTechBonuses(unlockedTechs: string[]): TechBonuses;
export function serializeTechs(techs: string[]): string;
```

```typescript
// lib/mascot/world-grid.ts (updated in Plan 01)
export const EXPANSION_CROP_CELLS: WorldCell[];   // 5 cells (c15-c19)
export const EXPANSION_BUILDING_CELL: WorldCell;  // b3
export const EXPANSION_LARGE_CROP_CELL: WorldCell; // c20
export function getExpandedCropCells(treeStage: TreeStage, techBonuses: TechBonuses): WorldCell[];
export function getExpandedBuildingCells(techBonuses: TechBonuses): WorldCell[];
```

```typescript
// hooks/useFarm.ts (updated in Plan 01)
export function useFarm(): {
  ...existing,
  unlockTech: (profileId: string, techId: string) => Promise<boolean>;
};
```

```typescript
// lib/types.ts (Profile updated in Plan 01)
farmTech?: string[];  // IDs des noeuds tech debloques
```

<!-- Existing CraftSheet pattern for reference -->
```typescript
// components/mascot/CraftSheet.tsx — pattern to follow
interface CraftSheetProps {
  visible: boolean;
  onClose: () => void;
  profileId: string;
  harvestInventory: HarvestInventory;
  farmInventory: FarmInventory;
  craftedItems: CraftedItem[];
  coins: number;
  onCraft: (recipeId: string) => Promise<CraftedItem | null>;
  onSellHarvest: (cropId: string) => Promise<number>;
  onSellCrafted: (recipeId: string) => Promise<number>;
}
// Uses Modal with presentationStyle="pageSheet", Chip tabs, ScrollView content
```

<!-- tree.tsx action buttons section (around line 801-836) -->
```typescript
{/* Boutons boutique + decorer (uniquement son propre arbre) */}
{isOwnTree && (
<View style={styles.actionBtns}>
  <TouchableOpacity style={[styles.shopBtn, ...]} onPress={() => setShowShop(true)}>
    <Text>{'🛒 ' + t('mascot.shop.title')}</Text>
  </TouchableOpacity>
  <TouchableOpacity style={[styles.shopBtn, ...]} onPress={() => setShowCraftSheet(true)}>
    <Text>{'🔨 ' + t('craft.atelier')}</Text>
  </TouchableOpacity>
  {/* ... decorer button */}
</View>
)}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Creer TechTreeSheet et integrer parcelles extension dans WorldGridView</name>
  <files>components/mascot/TechTreeSheet.tsx, components/mascot/WorldGridView.tsx</files>
  <read_first>
    - components/mascot/CraftSheet.tsx (pattern complet a suivre)
    - components/mascot/WorldGridView.tsx
    - lib/mascot/tech-engine.ts
    - lib/mascot/world-grid.ts
    - constants/spacing.ts
    - constants/typography.ts
    - constants/shadows.ts
  </read_first>
  <action>
**1. Creer `components/mascot/TechTreeSheet.tsx` :**

Pattern identique a CraftSheet.tsx (per D-07) : Modal presentationStyle pageSheet + drag-to-dismiss.

Props interface :
```typescript
interface TechTreeSheetProps {
  visible: boolean;
  onClose: () => void;
  profileId: string;
  unlockedTechs: string[];
  coins: number;
  onUnlock: (techId: string) => Promise<boolean>;
}
```

Layout (per D-08) — arbre vertical avec 3 branches cote a cote :
- Header avec titre "Technologies" (ModalHeader ou equivalent CraftSheet)
- 3 colonnes ScrollView horizontales, chaque colonne = 1 branche
- Titre de branche en haut de chaque colonne (Culture/Elevage/Expansion) avec emoji
- Noeuds empiles verticalement dans chaque colonne, connectes par des lignes verticales (View avec borderLeftWidth ou trait SVG simple)
- Chaque noeud = cercle 56x56 avec emoji au centre
  - Debloque : fond `colors.primary` avec opacity 1, emoji visible, checkmark en overlay
  - Debloquable (prereq OK, assez de feuilles) : fond `colors.tint` avec bordure `colors.primary`, cout affiche en dessous, pulsation subtile (withRepeat + withTiming opacity 0.7->1.0)
  - Verrouille : fond `colors.cardAlt` avec opacity 0.4, emoji grise, cadenas en overlay, message "Necessite X" en dessous

Interaction (per D-09) :
- Tap sur noeud debloquable → Alert.alert avec titre t('tech.confirm_title'), message avec cout et effet, boutons "Annuler" et "Debloquer"
- Sur confirmation → appeler onUnlock(techId), Haptics.notificationAsync(Success), fermer l'alert
- Tap sur noeud verrouille → showToast avec t('tech.requires', { name: prerequisName })
- Tap sur noeud deja debloque → rien (ou showToast "Deja debloque")

Utiliser :
- `useThemeColors()` pour toutes les couleurs
- `Spacing`, `Radius`, `FontSize`, `FontWeight` pour les tokens design
- `Animated` de react-native-reanimated pour la pulsation des noeuds debloquables
- `Haptics.impactAsync(Haptics.ImpactFeedbackStyle.Medium)` sur deblocage
- `useTranslation()` pour tous les textes

**2. Modifier `components/mascot/WorldGridView.tsx` :**

Ajouter une prop optionnelle `techBonuses?: TechBonuses` a WorldGridView (ou au composant parent qui genere les cellules).

Dans le rendu des cellules :
- Importer `EXPANSION_CROP_CELLS`, `EXPANSION_BUILDING_CELL`, `EXPANSION_LARGE_CROP_CELL` depuis world-grid.ts
- Si `techBonuses.extraCropCells > 0`, rendre les EXPANSION_CROP_CELLS avec le composant CropCell existant
- Si `techBonuses.extraCropCells === 0`, rendre les cellules d'expansion avec un overlay cadenas (View avec emoji cadenas centre, fond semi-transparent `colors.cardAlt` opacity 0.5)
- Meme logique pour EXPANSION_BUILDING_CELL et EXPANSION_LARGE_CROP_CELL

Animation de deblocage (per D-06) : quand une cellule passe de verrouillee a debloquee (detection par changement de techBonuses), animer avec :
- `useSharedValue` pour scale (0 -> 1 avec withSpring damping 8, stiffness 150)
- `Haptics.impactAsync(Haptics.ImpactFeedbackStyle.Heavy)` au moment du deblocage
- L'emoji cadenas fait un fadeOut pendant que la cellule fait un scaleIn

IMPORTANT : ne pas casser les cellules existantes (c0-c14, b0-b2). Les nouvelles cellules sont additionnelles.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - components/mascot/TechTreeSheet.tsx exists, exports TechTreeSheet component
    - TechTreeSheet uses Modal with presentationStyle="pageSheet"
    - TechTreeSheet imports TECH_TREE, canUnlockTech from tech-engine
    - TechTreeSheet renders 3 columns (one per TechBranchId)
    - TechTreeSheet uses useThemeColors() for all colors (no hardcoded hex)
    - TechTreeSheet uses Alert.alert for unlock confirmation
    - WorldGridView renders EXPANSION_CROP_CELLS with lock overlay when techBonuses.extraCropCells === 0
    - WorldGridView renders expansion cells normally when techBonuses.extraCropCells > 0
    - npx tsc --noEmit passes without new errors
  </acceptance_criteria>
  <done>TechTreeSheet affiche les 3 branches avec interaction complete (deblocage, verrouillage, confirmation). WorldGridView rend les parcelles d'extension avec cadenas.</done>
</task>

<task type="auto">
  <name>Task 2: Integrer TechTreeSheet et bonus tech dans tree.tsx</name>
  <files>app/(tabs)/tree.tsx</files>
  <read_first>
    - app/(tabs)/tree.tsx
    - components/mascot/TechTreeSheet.tsx (just created)
    - components/mascot/WorldGridView.tsx (just modified)
    - lib/mascot/tech-engine.ts
    - hooks/useFarm.ts
  </read_first>
  <action>
**Dans tree.tsx :**

1. **Imports** — ajouter en haut du fichier :
```typescript
import { TechTreeSheet } from '../../components/mascot/TechTreeSheet';
import { getTechBonuses } from '../../lib/mascot/tech-engine';
import { getExpandedCropCells, getExpandedBuildingCells } from '../../lib/mascot/world-grid';
```

2. **State** — ajouter apres la ligne `const [showCraftSheet, setShowCraftSheet] = useState(false);` :
```typescript
// Tech tree
const [showTechTree, setShowTechTree] = useState(false);
```

3. **Memo pour bonus tech** — ajouter un useMemo qui calcule les bonus :
```typescript
const techBonuses = useMemo(() => {
  return getTechBonuses(profile?.farmTech ?? []);
}, [profile?.farmTech]);
```

4. **useFarm** — ajouter `unlockTech` a la destructuration :
```typescript
const { plant, harvest, buyBuilding, upgradeBuildingAction, collectBuildingResources, collectPassiveIncome, craft, sellHarvest, sellCrafted, unlockTech } = useFarm();
```

5. **Bouton Technologies** — ajouter dans la View `actionBtns`, apres le bouton Atelier et avant le bouton Decorer :
```tsx
{/* Bouton Technologies */}
<TouchableOpacity
  style={[styles.shopBtn, { backgroundColor: tint, borderColor: primary }]}
  onPress={() => setShowTechTree(true)}
  activeOpacity={0.7}
>
  <Text style={[styles.shopBtnText, { color: primary }]}>
    {'🔬 ' + t('tech.title')}
  </Text>
</TouchableOpacity>
```

6. **TechTreeSheet** — ajouter apres le CraftSheet (vers la fin du JSX, avant la fermeture du SafeAreaView) :
```tsx
{/* Tech tree */}
<TechTreeSheet
  visible={showTechTree}
  onClose={() => setShowTechTree(false)}
  profileId={profile?.id ?? ''}
  unlockedTechs={profile?.farmTech ?? []}
  coins={profile?.coins ?? 0}
  onUnlock={async (techId) => {
    const success = await unlockTech(profile?.id ?? '', techId);
    return success;
  }}
/>
```

7. **Passer techBonuses a WorldGridView** — trouver le composant `<WorldGridView .../>` dans le JSX et ajouter la prop `techBonuses={techBonuses}`.

8. **Passer techBonuses aux fonctions farm** — partout ou `getUnlockedCropCells` est appele dans tree.tsx, le remplacer par `getExpandedCropCells(treeStage, techBonuses)` pour que les parcelles d'extension soient prises en compte dans les calculs de parcelles disponibles (ex: pour le seed picker).

9. **Passer techBonuses a advanceFarmCrops** — si tree.tsx appelle advanceFarmCrops directement, ajouter techBonuses en parametre. Si c'est fait dans useVault.ts via les actions, noter dans le summary que useVault devra etre mis a jour separement.

10. **Passer techBonuses pour getAvailableCrops** — dans le seed picker, utiliser `getAvailableCrops(treeStage, profile?.farmTech ?? [])` au lieu de filtrer CROP_CATALOG directement, pour que sunflower apparaisse uniquement quand culture-3 est debloque.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - tree.tsx imports TechTreeSheet from components/mascot/TechTreeSheet
    - tree.tsx imports getTechBonuses from lib/mascot/tech-engine
    - tree.tsx has state `showTechTree` with useState(false)
    - tree.tsx renders TechTreeSheet with visible={showTechTree}
    - tree.tsx has a TouchableOpacity with text containing t('tech.title')
    - tree.tsx passes techBonuses to WorldGridView
    - tree.tsx uses getExpandedCropCells instead of getUnlockedCropCells where appropriate
    - tree.tsx destructures unlockTech from useFarm()
    - npx tsc --noEmit passes without new errors
  </acceptance_criteria>
  <done>tree.tsx affiche le bouton Technologies, ouvre le TechTreeSheet, passe les bonus tech a WorldGridView, et utilise unlockTech depuis useFarm</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verification visuelle du tech tree et des parcelles d'extension</name>
  <files>app/(tabs)/tree.tsx</files>
  <action>
Verification humaine du systeme complet de progression ferme. Aucune modification de code — uniquement verification visuelle et fonctionnelle sur device.
  </action>
  <what-built>
    Systeme complet de progression ferme : tech tree avec 3 branches (Culture, Elevage, Expansion), 10 noeuds debloquables, parcelles d'extension avec cadenas, et integration dans l'ecran arbre.
  </what-built>
  <how-to-verify>
    1. Ouvrir l'app sur device (`npx expo run:ios --device`)
    2. Aller sur l'ecran Arbre (onglet arbre)
    3. Verifier qu'un bouton "Technologies" apparait a cote de Boutique et Atelier
    4. Tapper sur Technologies — le bottom sheet s'ouvre avec 3 colonnes
    5. Verifier les 3 branches : Culture (4 noeuds), Elevage (3 noeuds), Expansion (3 noeuds)
    6. Le premier noeud de chaque branche doit etre debloquable (bordure coloree, cout affiche)
    7. Les noeuds suivants doivent etre verrouilles (grise, cadenas)
    8. Debloquer "Engrais naturel" (100 feuilles) — confirmation, puis noeud passe en vert
    9. Verifier que le 2eme noeud Culture devient debloquable
    10. Debloquer "Nouveau potager" (expansion-1, 100 feuilles)
    11. Fermer le tech tree — verifier que la rangee 4 de parcelles apparait sur la grille (les cadenas disparaissent avec animation)
    12. Les parcelles d'extension non debloquees doivent avoir un cadenas visible
    13. Planter sur une nouvelle parcelle d'extension — verifier que ca fonctionne
    14. Quitter et relancer l'app — verifier que les techs debloquees sont toujours visibles
  </how-to-verify>
  <verify>Verification visuelle manuelle sur device iOS</verify>
  <done>L'utilisateur confirme que le tech tree fonctionne visuellement, que les parcelles d'extension se debloquent correctement, et que la progression persiste apres redemarrage</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passe sans erreurs
2. Le bouton Technologies est visible dans la barre d'action de tree.tsx
3. Le TechTreeSheet s'ouvre et affiche les 3 branches
4. Debloquer un noeud deduit les feuilles et persiste dans famille.md
5. Les parcelles d'extension apparaissent avec cadenas verrouille ou deverrouille selon les techs
6. La progression survit a un restart de l'app
</verification>

<success_criteria>
- TechTreeSheet est un composant fonctionnel avec Modal pageSheet, 3 branches, noeuds interactifs
- WorldGridView affiche les parcelles d'extension avec cadenas
- tree.tsx integre le bouton, le sheet, et passe les bonus tech
- L'utilisateur peut debloquer des noeuds et voir l'effet sur la ferme
- Verification visuelle approuvee par l'utilisateur
</success_criteria>

<output>
After completion, create `.planning/phases/08-progression-ferme/08-02-SUMMARY.md`
</output>
