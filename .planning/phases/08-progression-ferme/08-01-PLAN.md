---
phase: 08-progression-ferme
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/mascot/tech-engine.ts
  - lib/mascot/farm-engine.ts
  - lib/mascot/building-engine.ts
  - lib/mascot/world-grid.ts
  - lib/mascot/types.ts
  - lib/types.ts
  - lib/parser.ts
  - hooks/useFarm.ts
  - locales/fr/common.json
  - locales/en/common.json
autonomous: true
requirements: [PRO-01, PRO-02, PRO-03]

must_haves:
  truths:
    - "Un profil peut debloquer un noeud tech en depensant des feuilles"
    - "Debloquer un noeud Culture reduit tasksPerStage ou augmente harvestReward dans les calculs ferme"
    - "Debloquer un noeud Elevage reduit l'intervalle de production des batiments"
    - "Debloquer un noeud Expansion ajoute de nouvelles parcelles accessibles dans la grille"
    - "La progression tech est persistee dans famille.md et survit a un redemarrage"
  artifacts:
    - path: "lib/mascot/tech-engine.ts"
      provides: "TECH_TREE definition, unlockTechNode, getUnlockedTechs, getTechBonuses"
      exports: ["TECH_TREE", "TechNode", "TechBranch", "TechBonuses", "unlockTechNode", "getUnlockedTechs", "getTechBonuses", "canUnlockTech"]
    - path: "lib/mascot/types.ts"
      provides: "TechNodeId type export"
    - path: "lib/types.ts"
      provides: "farmTech field on Profile"
      contains: "farmTech"
    - path: "lib/parser.ts"
      provides: "farm_tech parsing in parseFamille"
      contains: "farm_tech"
    - path: "hooks/useFarm.ts"
      provides: "unlockTech action"
      exports: ["unlockTech"]
  key_links:
    - from: "lib/mascot/farm-engine.ts"
      to: "lib/mascot/tech-engine.ts"
      via: "getTechBonuses applied in advanceFarmCrops"
      pattern: "getTechBonuses"
    - from: "lib/mascot/building-engine.ts"
      to: "lib/mascot/tech-engine.ts"
      via: "getTechBonuses adjusts production intervals"
      pattern: "getTechBonuses"
    - from: "lib/mascot/world-grid.ts"
      to: "lib/mascot/tech-engine.ts"
      via: "getTechBonuses adds extra crop/building cells"
      pattern: "getTechBonuses|getExpandedCropCells"
    - from: "hooks/useFarm.ts"
      to: "lib/mascot/tech-engine.ts"
      via: "unlockTechNode called from unlockTech action"
      pattern: "unlockTechNode"
---

<objective>
Creer le moteur de tech tree ferme avec 3 branches (Culture, Elevage, Expansion), integrer les bonus dans farm-engine et building-engine, ajouter les parcelles d'extension dans world-grid, et persister farm_tech dans le profil.

Purpose: Fournir tout le backend et la logique pure pour que le Plan 02 puisse construire l'UI sans reimplementer aucune logique metier.
Output: tech-engine.ts complet, farm-engine/building-engine modifies pour appliquer les bonus, world-grid etendu, parser et useFarm mis a jour.
</objective>

<execution_context>
@/Users/gabrielwaltio/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gabrielwaltio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-progression-ferme/08-CONTEXT.md

<interfaces>
<!-- Existing types and contracts the executor needs -->

From lib/mascot/types.ts:
```typescript
export type TreeStage = 'graine' | 'pousse' | 'arbuste' | 'arbre' | 'majestueux' | 'legendaire';
export interface CropDefinition {
  id: string; labelKey: string; emoji: string;
  tasksPerStage: number; harvestReward: number;
  minTreeStage: TreeStage; cost: number;
}
export interface PlantedCrop {
  cropId: string; plotIndex: number; currentStage: number;
  tasksCompleted: number; plantedAt: string; isGolden?: boolean;
}
export type ResourceType = 'oeuf' | 'lait' | 'farine';
export interface BuildingTier {
  level: number; productionRateHours: number;
  upgradeCoins: number; spriteSuffix: string;
}
```

From lib/mascot/farm-engine.ts:
```typescript
export function advanceFarmCrops(crops: PlantedCrop[]): { crops: PlantedCrop[]; matured: PlantedCrop[] };
export function plantCrop(crops: PlantedCrop[], plotIndex: number, cropId: string): PlantedCrop[];
export function harvestCrop(crops: PlantedCrop[], plotIndex: number): { crops: PlantedCrop[]; harvestedCropId: string | null; isGolden: boolean };
```

From lib/mascot/building-engine.ts:
```typescript
export function collectBuilding(buildings: PlacedBuilding[], inventory: FarmInventory, cellId: string): { buildings: PlacedBuilding[]; inventory: FarmInventory; collected: number };
```

From lib/mascot/world-grid.ts:
```typescript
export const WORLD_GRID: WorldCell[];
export const CROP_CELLS: WorldCell[];
export const BUILDING_CELLS: WorldCell[];
export function getUnlockedCropCells(treeStage: TreeStage): WorldCell[];
```

From hooks/useFarm.ts:
```typescript
export function useFarm(): {
  plant, harvest, buyBuilding, upgradeBuildingAction,
  collectBuildingResources, collectPassiveIncome, craft,
  sellHarvest, sellCrafted
};
// Uses writeProfileField, writeProfileFields, deductCoins, addCoins internally
```

From lib/types.ts (Profile):
```typescript
farmCrops?: string;
farmBuildings?: import('../lib/mascot/types').PlacedBuilding[];
farmInventory?: import('../lib/mascot/types').FarmInventory;
harvestInventory?: import('../lib/mascot/types').HarvestInventory;
craftedItems?: import('../lib/mascot/types').CraftedItem[];
```

From lib/parser.ts (parseFamille extract):
```typescript
const farmCrops = currentProps.farm_crops ?? '';
const farmBuildings = parseBuildings(currentProps.farm_buildings);
const farmInventory = parseInventory(currentProps.farm_inventory);
const harvestInventory = parseHarvestInventory(currentProps.farm_harvest_inventory);
const craftedItems = parseCraftedItems(currentProps.farm_crafted_items);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Creer tech-engine.ts avec TECH_TREE, bonus, et logique de deblocage</name>
  <files>lib/mascot/tech-engine.ts, lib/mascot/types.ts</files>
  <read_first>
    - lib/mascot/types.ts
    - lib/mascot/farm-engine.ts
    - lib/mascot/building-engine.ts
    - lib/mascot/world-grid.ts
    - .planning/phases/08-progression-ferme/08-CONTEXT.md
  </read_first>
  <action>
Creer `lib/mascot/tech-engine.ts` — moteur pur (pas de React) pour le tech tree ferme.

**Types a definir dans tech-engine.ts :**
```typescript
export type TechBranchId = 'culture' | 'elevage' | 'expansion';

export interface TechNode {
  id: string;              // ex: 'culture-1', 'elevage-2', 'expansion-3'
  branch: TechBranchId;
  order: number;           // position dans la branche (1, 2, 3, 4)
  labelKey: string;        // cle i18n
  descriptionKey: string;  // cle i18n description de l'effet
  emoji: string;
  cost: number;            // cout en feuilles
  requires: string | null; // id du noeud prerequis (null pour le premier de chaque branche)
}

export interface TechBonuses {
  tasksPerStageReduction: number;    // reduction du tasksPerStage (0 ou 1)
  harvestRewardMultiplier: number;   // multiplicateur recolte (1.0 = pas de bonus, 1.25 = +25%)
  newCrops: string[];                // IDs des cultures debloquees par tech
  productionIntervalMultiplier: number; // multiplicateur intervalle batiments (1.0 = normal, 0.75 = -25%)
  buildingCapacityMultiplier: number;   // multiplicateur capacite stockage (1 ou 2)
  newResources: string[];              // nouvelles ressources debloquees
  extraCropCells: number;              // parcelles crop supplementaires
  extraBuildingCells: number;          // parcelles building supplementaires
  hasLargeCropCell: boolean;           // parcelle geante disponible
}
```

**TECH_TREE constant — 3 branches, 3-4 noeuds chacune (per D-01, D-02, D-03, D-04) :**

Branche Culture (4 noeuds) :
- `culture-1` : "Engrais naturel" — tasksPerStage -1 (min 1) — cout 100 — emoji fertilisant
- `culture-2` : "Recolte abondante" — harvestReward +25% — cout 250 — emoji gerbe ble
- `culture-3` : "Tournesol" — debloque culture 'sunflower' — cout 500 — emoji tournesol
- `culture-4` : "Maitre jardinier" — harvestReward +50% total — cout 1000 — emoji couronne

Branche Elevage (3 noeuds) :
- `elevage-1` : "Alimentation optimale" — production interval -25% — cout 100 — emoji foin
- `elevage-2` : "Grange agrandie" — capacite stockage x2 (MAX_PENDING 3->6) — cout 250 — emoji grange
- `elevage-3` : "Ruche a miel" — debloque ressource 'miel' — cout 500 — emoji miel

Branche Expansion (3 noeuds) :
- `expansion-1` : "Nouveau potager" — +5 parcelles crop (rangee 4) — cout 100 — emoji cloture
- `expansion-2` : "Atelier supplementaire" — +1 parcelle building — cout 250 — emoji marteau
- `expansion-3` : "Mega parcelle" — debloque parcelle geante (size large pour crops) — cout 500 — emoji etoile

**Fonctions pures a implementer :**

1. `getUnlockedTechs(farmTechCSV: string): string[]` — parse le CSV farm_tech et retourne la liste des IDs debloques.

2. `canUnlockTech(techId: string, unlockedTechs: string[], coins: number): { canUnlock: boolean; reason?: string }` — verifie si le prerequis est debloque et si le joueur a assez de feuilles. Retourne la raison si non debloquable.

3. `unlockTechNode(unlockedTechs: string[], techId: string): string[]` — ajoute le techId a la liste et retourne la nouvelle liste. Ne verifie pas les prerequis (c'est le caller qui valide avec canUnlockTech).

4. `serializeTechs(techs: string[]): string` — join par virgule.

5. `getTechBonuses(unlockedTechs: string[]): TechBonuses` — calcule les bonus agrages de tous les noeuds debloques. Retourne un objet TechBonuses avec les valeurs cumulees.

**Ajouter dans types.ts** a la fin du fichier, apres le BUILDING_CATALOG :
- La culture `sunflower` dans CROP_CATALOG : `{ id: 'sunflower', labelKey: 'farm.crop.sunflower', emoji: '🌻', tasksPerStage: 2, harvestReward: 100, minTreeStage: 'pousse', cost: 20 }`. IMPORTANT: marquer cette culture avec un flag `techRequired: 'culture-3'` — ajouter le champ optionnel `techRequired?: string` a l'interface CropDefinition.

Le fichier doit etre pur (pas de React, pas d'import React). Pattern identique a farm-engine.ts et building-engine.ts.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `lib/mascot/tech-engine.ts` exists with exports: TECH_TREE, TechNode, TechBranch, TechBonuses, unlockTechNode, getUnlockedTechs, getTechBonuses, canUnlockTech, serializeTechs
    - TECH_TREE has 10 nodes total (4 culture + 3 elevage + 3 expansion)
    - Costs are exactly: 100, 250, 500, 1000 for culture; 100, 250, 500 for elevage; 100, 250, 500 for expansion
    - getTechBonuses(['culture-1']) returns tasksPerStageReduction === 1
    - getTechBonuses(['culture-2']) returns harvestRewardMultiplier === 1.25
    - getTechBonuses(['elevage-1']) returns productionIntervalMultiplier === 0.75
    - getTechBonuses(['expansion-1']) returns extraCropCells === 5
    - canUnlockTech('culture-2', ['culture-1'], 250).canUnlock === true
    - canUnlockTech('culture-2', [], 250).canUnlock === false (prereq missing)
    - CROP_CATALOG contains sunflower entry with techRequired: 'culture-3'
    - CropDefinition interface has optional techRequired field
  </acceptance_criteria>
  <done>tech-engine.ts exporte toutes les fonctions pures, TECH_TREE est defini avec 10 noeuds, sunflower est dans CROP_CATALOG, tsc compile sans nouvelles erreurs</done>
</task>

<task type="auto">
  <name>Task 2: Integrer les bonus tech dans farm-engine, building-engine, world-grid + persistence parser/useFarm</name>
  <files>lib/mascot/farm-engine.ts, lib/mascot/building-engine.ts, lib/mascot/world-grid.ts, lib/types.ts, lib/parser.ts, hooks/useFarm.ts, locales/fr/common.json, locales/en/common.json</files>
  <read_first>
    - lib/mascot/tech-engine.ts (just created in Task 1)
    - lib/mascot/farm-engine.ts
    - lib/mascot/building-engine.ts
    - lib/mascot/world-grid.ts
    - lib/types.ts
    - lib/parser.ts
    - hooks/useFarm.ts
    - locales/fr/common.json
    - locales/en/common.json
  </read_first>
  <action>
**1. farm-engine.ts — appliquer bonus Culture :**

Modifier `advanceFarmCrops` pour accepter un parametre optionnel `techBonuses?: TechBonuses`. Si fourni :
- Utiliser `cropDef.tasksPerStage - techBonuses.tasksPerStageReduction` (min 1) au lieu de `cropDef.tasksPerStage` dans le check `if (target.tasksCompleted >= ...)`.
- NE PAS modifier la signature de plantCrop ou harvestCrop — les bonus rendement s'appliquent au moment de la vente, pas a la recolte.

Ajouter `getEffectiveHarvestReward(cropId: string, techBonuses: TechBonuses): number` — retourne `Math.round(cropDef.harvestReward * techBonuses.harvestRewardMultiplier)`. Exporter cette fonction.

Ajouter `getAvailableCrops(treeStage: TreeStage, unlockedTechs: string[]): CropDefinition[]` — filtre CROP_CATALOG pour retourner les cultures disponibles : celles dont minTreeStage est atteint ET (pas de techRequired OU techRequired est dans unlockedTechs). Exporter cette fonction.

**2. building-engine.ts — appliquer bonus Elevage :**

Modifier `collectBuilding` pour accepter un parametre optionnel `techBonuses?: TechBonuses`. Si fourni :
- `effectiveRate = tier.productionRateHours * techBonuses.productionIntervalMultiplier`
- `effectiveMaxPending = MAX_PENDING * techBonuses.buildingCapacityMultiplier`
- Utiliser ces valeurs dans le calcul des ressources en attente au lieu des constantes.

**3. world-grid.ts — ajouter parcelles d'extension :**

Ajouter apres le WORLD_GRID existant :
```typescript
/** Cellules de la rangee 4 — debloquees par tech expansion-1 */
export const EXPANSION_CROP_CELLS: WorldCell[] = [
  { id: 'c15', col: 0, row: 3, x: 0.14, y: 0.41, cellType: 'crop', unlockOrder: 20, size: 'small' },
  { id: 'c16', col: 1, row: 3, x: 0.28, y: 0.41, cellType: 'crop', unlockOrder: 21, size: 'small' },
  { id: 'c17', col: 2, row: 3, x: 0.42, y: 0.41, cellType: 'crop', unlockOrder: 22, size: 'small' },
  { id: 'c18', col: 3, row: 3, x: 0.56, y: 0.41, cellType: 'crop', unlockOrder: 23, size: 'small' },
  { id: 'c19', col: 4, row: 3, x: 0.70, y: 0.41, cellType: 'crop', unlockOrder: 24, size: 'small' },
];

/** Cellule building supplementaire — debloquee par tech expansion-2 */
export const EXPANSION_BUILDING_CELL: WorldCell =
  { id: 'b3', col: 5, row: 5, x: 0.86, y: 0.90, cellType: 'building', unlockOrder: 25, size: 'large' };

/** Parcelle geante crop — debloquee par tech expansion-3 */
export const EXPANSION_LARGE_CROP_CELL: WorldCell =
  { id: 'c20', col: 2, row: 3, x: 0.42, y: 0.53, cellType: 'crop', unlockOrder: 26, size: 'large' };
```

Ajouter la fonction `getExpandedCropCells(treeStage: TreeStage, techBonuses: TechBonuses): WorldCell[]` qui retourne `getUnlockedCropCells(treeStage)` + les cellules d'expansion si les bonus correspondants sont actifs. Meme pattern pour `getExpandedBuildingCells`.

**4. lib/types.ts — ajouter farmTech au Profile :**

Ajouter dans l'interface Profile (apres craftedItems) :
```typescript
farmTech?: string[];  // IDs des noeuds tech debloques
```

**5. lib/parser.ts — parser farm_tech :**

Dans parseFamille, apres la ligne `const craftedItems = parseCraftedItems(...)`, ajouter :
```typescript
const farmTechRaw = currentProps.farm_tech ?? '';
const farmTech = farmTechRaw ? farmTechRaw.split(',').map((s: string) => s.trim()).filter(Boolean) : [];
```

Ajouter `farmTech` dans l'objet profile retourne (apres craftedItems).

**6. hooks/useFarm.ts — ajouter unlockTech action :**

Importer `unlockTechNode, canUnlockTech, serializeTechs, TECH_TREE` depuis `tech-engine.ts`.

Ajouter la fonction `unlockTech` dans le hook :
```typescript
const unlockTech = useCallback(async (profileId: string, techId: string): Promise<boolean> => {
  if (!vault) return false;
  const content = await vault.readFile(FAMILLE_FILE);
  const freshProfiles = parseFamille(content);
  const profile = freshProfiles.find(p => p.id === profileId);
  if (!profile) return false;

  const currentTechs = profile.farmTech ?? [];
  const node = TECH_TREE.find(n => n.id === techId);
  if (!node) return false;

  const check = canUnlockTech(techId, currentTechs, profile.coins ?? 0);
  if (!check.canUnlock) throw new Error(check.reason ?? 'Impossible de debloquer');

  const newTechs = unlockTechNode(currentTechs, techId);
  await writeProfileField(profileId, 'farm_tech', serializeTechs(newTechs));
  await deductCoins(profileId, node.cost, `🔬 Tech : ${techId}`);
  await refresh();
  return true;
}, [vault, writeProfileField, deductCoins, refresh]);
```

Ajouter `unlockTech` au return du hook.

**7. Locales i18n :**

Ajouter dans `locales/fr/common.json` sous une nouvelle section `"tech"` :
```json
"tech": {
  "title": "Technologies",
  "branch_culture": "Culture",
  "branch_elevage": "Elevage",
  "branch_expansion": "Expansion",
  "culture-1": "Engrais naturel",
  "culture-1_desc": "Les cultures poussent plus vite (-1 tache par stade)",
  "culture-2": "Recolte abondante",
  "culture-2_desc": "Les recoltes rapportent 25% de plus",
  "culture-3": "Tournesol",
  "culture-3_desc": "Debloque la culture du tournesol",
  "culture-4": "Maitre jardinier",
  "culture-4_desc": "Les recoltes rapportent 50% de plus au total",
  "elevage-1": "Alimentation optimale",
  "elevage-1_desc": "Les batiments produisent 25% plus vite",
  "elevage-2": "Grange agrandie",
  "elevage-2_desc": "Capacite de stockage doublee",
  "elevage-3": "Ruche a miel",
  "elevage-3_desc": "Debloque la production de miel",
  "expansion-1": "Nouveau potager",
  "expansion-1_desc": "5 nouvelles parcelles de culture",
  "expansion-2": "Atelier supplementaire",
  "expansion-2_desc": "1 nouvelle parcelle pour batiment",
  "expansion-3": "Mega parcelle",
  "expansion-3_desc": "Debloque une parcelle geante",
  "unlock": "Debloquer",
  "unlocked": "Debloque",
  "locked": "Verrouille",
  "requires": "Necessite : {{name}}",
  "cost": "{{amount}} feuilles",
  "not_enough_coins": "Pas assez de feuilles",
  "confirm_title": "Debloquer {{name}} ?",
  "confirm_message": "Cout : {{cost}} feuilles\n\nEffet : {{effect}}"
}
```

Ajouter aussi `"farm.crop.sunflower": "Tournesol"`.

Ajouter les equivalents anglais dans `locales/en/common.json`.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - advanceFarmCrops accepts optional techBonuses parameter
    - collectBuilding accepts optional techBonuses parameter
    - getExpandedCropCells and getExpandedBuildingCells are exported from world-grid.ts
    - getAvailableCrops and getEffectiveHarvestReward are exported from farm-engine.ts
    - Profile interface in lib/types.ts has `farmTech?: string[]`
    - parser.ts parses farm_tech from famille.md into farmTech array
    - useFarm returns unlockTech function
    - locales/fr/common.json contains "tech.title": "Technologies"
    - locales/en/common.json contains "tech.title": "Technologies"
    - locales/fr/common.json contains "farm.crop.sunflower": "Tournesol"
    - EXPANSION_CROP_CELLS array has 5 cells (c15-c19)
    - EXPANSION_BUILDING_CELL has id 'b3'
    - npx tsc --noEmit passes without new errors
  </acceptance_criteria>
  <done>Les bonus tech sont appliques dynamiquement dans farm-engine et building-engine, les parcelles d'extension sont definies dans world-grid, farm_tech est persiste dans le profil, unlockTech est disponible dans useFarm, i18n est complete</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passe sans nouvelles erreurs
2. `grep -r "getTechBonuses" lib/mascot/` retourne farm-engine.ts, building-engine.ts, tech-engine.ts
3. `grep "farmTech" lib/types.ts` retourne la ligne dans Profile
4. `grep "farm_tech" lib/parser.ts` retourne le parsing
5. `grep "unlockTech" hooks/useFarm.ts` retourne la fonction
6. `grep "tech.title" locales/fr/common.json` retourne "Technologies"
</verification>

<success_criteria>
- tech-engine.ts est complet avec 10 noeuds, 3 branches, fonctions pures testables
- farm-engine lit les bonus tech pour ajuster tasksPerStage et harvestReward
- building-engine lit les bonus tech pour ajuster les intervalles de production
- world-grid a les cellules d'extension conditionnelles
- Le profil persiste farm_tech en CSV dans famille.md
- useFarm expose unlockTech
- tsc compile sans erreurs
</success_criteria>

<output>
After completion, create `.planning/phases/08-progression-ferme/08-01-SUMMARY.md`
</output>
