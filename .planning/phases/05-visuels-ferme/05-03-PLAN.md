---
phase: 05-visuels-ferme
plan: 03
type: execute
wave: 2
depends_on: [05-02]
files_modified:
  - assets/garden/animals/
  - components/mascot/TreeView.tsx
autonomous: false
requirements: [VIS-03]

must_haves:
  truths:
    - "Les animaux ont une animation idle visible (alternance 2 frames)"
    - "Les animaux se deplacent aleatoirement avec une animation de marche differenciee"
    - "La direction de marche est visible — mouvement horizontal utilise les frames walk_left (avec scaleX flip pour la droite)"
    - "Les 5 animaux (poussin, poulet, canard, cochon, vache) paraissent vivants au repos"
    - "Les sprites sont de vrais pixel art Mana Seed (pas des placeholders)"
  artifacts:
    - path: "components/mascot/TreeView.tsx"
      provides: "AnimatedAnimal avec walk_left frames et direction flip"
      contains: "ANIMAL_WALK_LEFT_FRAMES"
    - path: "assets/garden/animals/poussin/idle_1.png"
      provides: "Vrai sprite Mana Seed poussin idle"
  key_links:
    - from: "components/mascot/TreeView.tsx"
      to: "assets/garden/animals/"
      via: "require() dans ANIMAL_IDLE_FRAMES, ANIMAL_WALK_FRAMES, ANIMAL_WALK_LEFT_FRAMES"
      pattern: "ANIMAL_WALK_LEFT_FRAMES"
---

<objective>
Remplacer les placeholders animaux par de vrais sprites Mana Seed et ajouter le support de direction de marche (walk_left + scaleX flip).

Purpose: Les sprites animaux actuels sont des placeholders (~300 bytes). VIS-03 exige des animations idle et marche plus fluides. L'infrastructure `AnimatedAnimal` existe deja avec idle cycle + random walk + thought bubbles. Le travail consiste a (a) placer les vrais sprites et (b) ajouter les frames `walk_left` avec detection de direction pour que les animaux marchent visuellement dans la bonne direction.

Output: Vrais sprites animaux places, `ANIMAL_WALK_LEFT_FRAMES` ajoute, `AnimatedAnimal` utilise les frames walk_left quand le mouvement est horizontal avec `scaleX: -1` pour la droite.
</objective>

<execution_context>
@/Users/gabrielwaltio/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gabrielwaltio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-visuels-ferme/05-CONTEXT.md
@.planning/phases/05-visuels-ferme/05-RESEARCH.md

<interfaces>
<!-- AnimatedAnimal existant — a etendre pour walk_left -->

From components/mascot/TreeView.tsx (lines 1959-2107):
```typescript
// Idle frames — 2 frames par animal (deja existants)
const ANIMAL_IDLE_FRAMES: Record<string, [any, any]> = {
  poussin: [require('.../poussin/idle_1.png'), require('.../poussin/idle_2.png')],
  poulet:  [require('.../poulet/idle_1.png'),  require('.../poulet/idle_2.png')],
  canard:  [require('.../canard/idle_1.png'),  require('.../canard/idle_2.png')],
  cochon:  [require('.../cochon/idle_1.png'),  require('.../cochon/idle_2.png')],
  vache:   [require('.../vache/idle_1.png'),   require('.../vache/idle_2.png')],
};

// Walk down frames — 4 frames par animal (actuel, seulement walk_down)
const ANIMAL_WALK_FRAMES: Record<string, any[]> = {
  poussin: [require('.../poussin/walk_down_1.png'), ...walk_down_4],
  // ... same for poulet, canard, cochon, vache
};

// AnimatedAnimal component — ligne 2009
function AnimatedAnimal({ frames, x, y, size, animalId, containerWidth }) {
  // Idle: alternates frames[0] / frames[1] every 600ms
  // Walk: random dx/dy every 3-6s, uses ANIMAL_WALK_FRAMES[animalId]
  // Walk frames: walkFrames[walkFrameIdx % walkFrames.length], 200ms per frame
  // Movement: offsetX.value = withTiming(offsetX.value + dx, { duration: 1500 })
  //   dx = (Math.random() - 0.5) * 20
  //   dy = (Math.random() - 0.5) * 12
  // Currently NO direction detection — always uses walk_down frames
}
```

From assets/garden/animals/ (file structure, all placeholders ~300B):
```
poussin/  idle_1.png idle_2.png walk_down_1..8.png walk_left_1..8.png
poulet/   idle_1.png idle_2.png walk_down_1..8.png walk_left_1..8.png
canard/   idle_1.png idle_2.png walk_down_1..6.png walk_left_1..6.png  (6 frames, not 8)
cochon/   idle_1.png idle_2.png walk_down_1..8.png walk_left_1..8.png
vache/    idle_1.png idle_2.png walk_down_1..8.png walk_left_1..8.png
```
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Placer les vrais sprites Mana Seed pour les animaux</name>
  <files>assets/garden/animals/</files>

  <read_first>
    - components/mascot/TreeView.tsx (lines 1959-2003 pour les require() actuels)
    - .planning/phases/05-visuels-ferme/05-RESEARCH.md
  </read_first>

  <action>
Les sprites animaux actuels dans `assets/garden/animals/` sont des placeholders (~300 bytes). Les vrais sprites Mana Seed doivent etre places.

**Ce qui est necessaire:**
- 5 animaux : poussin, poulet, canard, cochon, vache
- Pour chaque animal : 2 idle + walk_down + walk_left frames
  - poussin: idle_1/2 + walk_down_1..8 + walk_left_1..8 = 18 fichiers
  - poulet: 18 fichiers
  - canard: idle_1/2 + walk_down_1..6 + walk_left_1..6 = 14 fichiers (D-12 note: canard a 6 frames walk, pas 8)
  - cochon: 18 fichiers
  - vache: 18 fichiers
- Total: 86 fichiers PNG (4x18 + 1x14)
- Format: pixel 32x32 ou 48x48, style Mana Seed uniforme (D-13)
- Taille standardisee: tous les animaux memes dimensions source (D-14 research pitfall 4)
- Les noms de fichiers existants restent IDENTIQUES (idle_1.png, walk_down_1.png, walk_left_1.png, etc.)

**Action developpeur:**
1. Localiser le dossier Mana Seed source (meme source que Task 1 du plan 02)
2. Extraire les sprites animaux et les placer dans `assets/garden/animals/{animal}/`
3. Les noms de fichiers doivent correspondre exactement a ceux deja scaffoldes

**Verification:**
- Chaque fichier PNG animal doit faire > 500 bytes
- Les fichiers walk_left existent pour les 5 animaux
  </action>

  <verify>
    <automated>cd /Users/gabrielwaltio/Documents/family-vault && find assets/garden/animals -name "*.png" -size +500c | wc -l</automated>
  </verify>

  <acceptance_criteria>
    - `find assets/garden/animals -name "idle_*.png" -size +500c | wc -l` retourne 10 (5 animaux x 2 idle)
    - `find assets/garden/animals -name "walk_down_*.png" -size +500c | wc -l` retourne au moins 36
    - `find assets/garden/animals -name "walk_left_*.png" -size +500c | wc -l` retourne au moins 36
  </acceptance_criteria>

  <done>
    86 vrais sprites Mana Seed animaux sont places dans assets/garden/animals/. Chaque fichier fait plus de 500 bytes. Les 5 animaux ont idle, walk_down, et walk_left.
  </done>
</task>

<task type="auto">
  <name>Task 2: Ajouter ANIMAL_WALK_LEFT_FRAMES et detection de direction dans AnimatedAnimal</name>
  <files>components/mascot/TreeView.tsx</files>

  <read_first>
    - components/mascot/TreeView.tsx (lines 1959-2107 pour AnimatedAnimal complet)
    - CLAUDE.md (convention scaleX pour flips)
  </read_first>

  <action>
**1. Ajouter `ANIMAL_WALK_LEFT_FRAMES` dans TreeView.tsx** (apres `ANIMAL_WALK_FRAMES`, ~ligne 2003) :

```typescript
const ANIMAL_WALK_LEFT_FRAMES: Record<string, any[]> = {
  poussin: [
    require('../../assets/garden/animals/poussin/walk_left_1.png'),
    require('../../assets/garden/animals/poussin/walk_left_2.png'),
    require('../../assets/garden/animals/poussin/walk_left_3.png'),
    require('../../assets/garden/animals/poussin/walk_left_4.png'),
    require('../../assets/garden/animals/poussin/walk_left_5.png'),
    require('../../assets/garden/animals/poussin/walk_left_6.png'),
    require('../../assets/garden/animals/poussin/walk_left_7.png'),
    require('../../assets/garden/animals/poussin/walk_left_8.png'),
  ],
  poulet: [
    require('../../assets/garden/animals/poulet/walk_left_1.png'),
    require('../../assets/garden/animals/poulet/walk_left_2.png'),
    require('../../assets/garden/animals/poulet/walk_left_3.png'),
    require('../../assets/garden/animals/poulet/walk_left_4.png'),
    require('../../assets/garden/animals/poulet/walk_left_5.png'),
    require('../../assets/garden/animals/poulet/walk_left_6.png'),
    require('../../assets/garden/animals/poulet/walk_left_7.png'),
    require('../../assets/garden/animals/poulet/walk_left_8.png'),
  ],
  canard: [
    require('../../assets/garden/animals/canard/walk_left_1.png'),
    require('../../assets/garden/animals/canard/walk_left_2.png'),
    require('../../assets/garden/animals/canard/walk_left_3.png'),
    require('../../assets/garden/animals/canard/walk_left_4.png'),
    require('../../assets/garden/animals/canard/walk_left_5.png'),
    require('../../assets/garden/animals/canard/walk_left_6.png'),
  ],
  cochon: [
    require('../../assets/garden/animals/cochon/walk_left_1.png'),
    require('../../assets/garden/animals/cochon/walk_left_2.png'),
    require('../../assets/garden/animals/cochon/walk_left_3.png'),
    require('../../assets/garden/animals/cochon/walk_left_4.png'),
    require('../../assets/garden/animals/cochon/walk_left_5.png'),
    require('../../assets/garden/animals/cochon/walk_left_6.png'),
    require('../../assets/garden/animals/cochon/walk_left_7.png'),
    require('../../assets/garden/animals/cochon/walk_left_8.png'),
  ],
  vache: [
    require('../../assets/garden/animals/vache/walk_left_1.png'),
    require('../../assets/garden/animals/vache/walk_left_2.png'),
    require('../../assets/garden/animals/vache/walk_left_3.png'),
    require('../../assets/garden/animals/vache/walk_left_4.png'),
    require('../../assets/garden/animals/vache/walk_left_5.png'),
    require('../../assets/garden/animals/vache/walk_left_6.png'),
    require('../../assets/garden/animals/vache/walk_left_7.png'),
    require('../../assets/garden/animals/vache/walk_left_8.png'),
  ],
};
```

**2. Ajouter un state `lastDx` dans `AnimatedAnimal`** pour tracker la derniere direction de mouvement :

```typescript
const [lastDx, setLastDx] = React.useState(0);
```

Dans la fonction `startWalk` (ligne ~2030), apres le calcul de `dx`, ajouter :
```typescript
setLastDx(dx);
```

**3. Modifier la selection des frames de marche** pour utiliser walk_left quand le mouvement est horizontal :

Remplacer la logique de `currentFrame` (actuellement ligne 2079-2081) :

```typescript
// Ancien:
// const currentFrame = isWalking && walkFrames
//   ? walkFrames[walkFrameIdx]
//   : frames[frameIdx];

// Nouveau: selection directionnelle
const walkDownFrames = ANIMAL_WALK_FRAMES[animalId];
const walkLeftFrames = ANIMAL_WALK_LEFT_FRAMES[animalId];
const isHorizontal = Math.abs(lastDx) > Math.abs(lastDy);
const activeWalkFrames = isHorizontal && walkLeftFrames ? walkLeftFrames : walkDownFrames;
const currentFrame = isWalking && activeWalkFrames
  ? activeWalkFrames[walkFrameIdx % activeWalkFrames.length]
  : frames[frameIdx];
const flipX = isWalking && isHorizontal && lastDx > 0;
```

Note: `lastDy` aussi est necessaire — ajouter `const [lastDy, setLastDy] = React.useState(0);` et `setLastDy(dy);` dans `startWalk`.

**4. Appliquer le flip scaleX sur l'Image** quand l'animal se deplace vers la droite :

Modifier le `<Image>` dans le return de `AnimatedAnimal` (ligne 2085-2088) :
```typescript
<Image
  source={currentFrame}
  style={[
    { width: size, height: size },
    flipX ? { transform: [{ scaleX: -1 }] } : {},
  ] as any}
/>
```

IMPORTANT: Ne PAS utiliser `perspective` dans le transform (convention CLAUDE.md). Utiliser uniquement `scaleX: -1`.

**5. NE PAS modifier** les thought bubbles, l'idle cycle, les intervalles de marche, ou le calcul des offsets. Seule la selection de frames et le flip directionnel changent.
  </action>

  <verify>
    <automated>cd /Users/gabrielwaltio/Documents/family-vault && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>

  <acceptance_criteria>
    - `TreeView.tsx` contient `ANIMAL_WALK_LEFT_FRAMES`
    - `TreeView.tsx` contient `walk_left_1.png`
    - `TreeView.tsx` contient `lastDx` et `lastDy` (ou equivalent pour la direction)
    - `TreeView.tsx` contient `scaleX: -1` (flip pour direction droite)
    - `TreeView.tsx` NE contient PAS `perspective` dans le meme transform array
    - `TreeView.tsx` contient `activeWalkFrames` ou equivalent pour la selection directionnelle
    - `npx tsc --noEmit` passe sans nouvelles erreurs
  </acceptance_criteria>

  <done>
    AnimatedAnimal utilise walk_left frames quand le mouvement est horizontal (Math.abs(dx) > Math.abs(dy)), avec scaleX: -1 pour le mouvement vers la droite. Le canard fonctionne avec 6 frames grace au modulo existant. Les walk_down frames sont utilises pour les mouvements verticaux. npx tsc --noEmit passe.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verification visuelle complete de la ferme</name>
  <files>N/A</files>

  <action>
Verification visuelle de toute la phase 5 sur device physique. Aucune modification de code — uniquement observation et validation.

1. Lancer l'app : `npx expo run:ios --device`
2. Aller sur l'ecran ferme (onglet arbre)
3. **Verifier VIS-02 — Cultures:**
   - Observer les cultures plantees — elles doivent alterner entre 2 frames (~800ms), effet de balancement doux
   - Verifier a differents stades de croissance (planter une nouvelle culture si necessaire)
   - Les sprites doivent etre du vrai pixel art Mana Seed, pas des placeholders flous
4. **Verifier VIS-03 — Animaux:**
   - Observer les animaux — idle visible (alternance 2 frames)
   - Attendre 3-6 secondes — un animal doit se deplacer
   - Le mouvement horizontal doit utiliser des frames de marche laterale
   - Verifier que le mouvement vers la droite flippe le sprite (pas de marche a reculons)
5. **Verifier VIS-01 — Jour/nuit:**
   - En journee (9h-19h), aucun overlay visible
   - Changer l'heure du device sur 21h00 (Reglages > General > Date et heure > desactiver auto) — l'overlay nuit doit apparaitre (teinte bleu/violet subtile, ~12% opacite)
   - Remettre l'heure en auto
6. Verifier que les bulles de pensee des animaux fonctionnent toujours
  </action>

  <verify>
    <automated>cd /Users/gabrielwaltio/Documents/family-vault && npx tsc --noEmit 2>&1 | head -10</automated>
  </verify>

  <acceptance_criteria>
    - L'utilisateur confirme visuellement que les cultures s'animent (2 frames alternees)
    - L'utilisateur confirme visuellement que les animaux marchent dans la bonne direction
    - L'utilisateur confirme visuellement que l'overlay nuit apparait quand l'heure est changee
    - Aucune regression visible (bulles de pensee, pulse mature, etc.)
  </acceptance_criteria>

  <done>
    L'utilisateur a approuve visuellement les 3 features VIS-01, VIS-02, VIS-03 sur device physique. La ferme est visuellement vivante.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passe sans nouvelles erreurs
2. `grep "ANIMAL_WALK_LEFT_FRAMES" components/mascot/TreeView.tsx` confirme les frames directionnels
3. `grep "scaleX" components/mascot/TreeView.tsx` confirme le flip
4. Verification visuelle manuelle de la ferme sur device
</verification>

<success_criteria>
- Les animaux ont une animation idle visible et se deplacent avec des frames directionnels
- Le mouvement horizontal utilise walk_left, le vertical utilise walk_down
- Le flip scaleX fonctionne pour la direction droite
- Les sprites sont de vrais Mana Seed, pas des placeholders
- `npx tsc --noEmit` passe
</success_criteria>

<output>
After completion, create `.planning/phases/05-visuels-ferme/05-03-SUMMARY.md`
</output>
