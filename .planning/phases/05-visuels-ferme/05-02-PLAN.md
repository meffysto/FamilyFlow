---
phase: 05-visuels-ferme
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - assets/garden/crops/
  - assets/garden/animals/
  - lib/mascot/crop-sprites.ts
  - components/mascot/WorldGridView.tsx
  - components/mascot/FarmPlots.tsx
autonomous: false
requirements: [VIS-02]

must_haves:
  truths:
    - "Chaque culture affiche 2 frames distinctes alternees — l'animation de balancement est perceptible"
    - "L'animation est visible a tous les stades de croissance (0 a 4)"
    - "Les cultures dorees ont la meme animation que les normales"
    - "useReducedMotion desactive l'alternance de frames"
  artifacts:
    - path: "lib/mascot/crop-sprites.ts"
      provides: "CROP_SPRITES avec 2 frames par stade"
      contains: "stage_0_a.png"
    - path: "components/mascot/WorldGridView.tsx"
      provides: "CropCell avec frame swap anime"
      contains: "frameIdx"
    - path: "assets/garden/crops/carrot/stage_0_a.png"
      provides: "Frame A du premier stade carotte"
  key_links:
    - from: "components/mascot/WorldGridView.tsx"
      to: "lib/mascot/crop-sprites.ts"
      via: "CROP_SPRITES[cropId][stage][frameIdx]"
      pattern: "CROP_SPRITES.*frameIdx"
---

<objective>
Remplacer les placeholders de cultures par de vrais sprites Mana Seed et ajouter l'animation 2-frames (balancement doux).

Purpose: Actuellement les sprites de culture sont des placeholders de ~300 bytes. VIS-02 exige 2 frames d'animation par stade de croissance (10 cultures x 5 stades x 2 frames = 100 PNGs). Les vrais sprites Mana Seed doivent etre places, `CROP_SPRITES` restructure pour supporter `[frameA, frameB]`, et `CropCell` modifie pour alterner entre les frames.

Output: 100 vrais PNGs dans `assets/garden/crops/`, `CROP_SPRITES` restructure, `CropCell` anime avec frame swap ~800ms.
</objective>

<execution_context>
@/Users/gabrielwaltio/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gabrielwaltio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-visuels-ferme/05-CONTEXT.md
@.planning/phases/05-visuels-ferme/05-RESEARCH.md

<interfaces>
<!-- CROP_SPRITES actuel — a restructurer -->

From lib/mascot/crop-sprites.ts:
```typescript
// Signature actuelle : Record<string, Record<number, any>>
export const CROP_SPRITES: Record<string, Record<number, any>> = {
  carrot: {
    0: require('../../assets/garden/crops/carrot/stage_0.png'),
    1: require('../../assets/garden/crops/carrot/stage_1.png'),
    // ... stages 2-4
  },
  // 10 cultures: carrot, wheat, tomato, strawberry, potato, corn, pumpkin, cabbage, beetroot, cucumber
};

export const CROP_ICONS: Record<string, any> = { /* unchanged */ };
```

From components/mascot/WorldGridView.tsx (CropCell, lines 43-122):
```typescript
function CropCell({ cell, crop, cropDef, isMature, containerWidth, containerHeight, onPress }: {
  cell: WorldCell;
  crop: PlantedCrop | null;
  cropDef: typeof CROP_CATALOG[0] | null;
  isMature: boolean;
  // ...
}) {
  // Existing animations: pulse mature, growth wiggle
  // Line 115-118 — current sprite usage:
  {CROP_SPRITES[crop.cropId]?.[crop.currentStage] ? (
    <Image source={CROP_SPRITES[crop.cropId][crop.currentStage]} style={styles.cropSprite as any} />
  ) : (
    <Text style={styles.cropEmoji}>{cropDef.emoji}</Text>
  )}
}
```

From components/mascot/FarmPlots.tsx:
```typescript
// Also references CROP_SPRITES — must keep in sync to avoid TS errors
// FarmPlots is NOT the active renderer (WorldGridView is), but still imports CROP_SPRITES
```
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Localiser et placer les assets Mana Seed pour les cultures</name>

  <read_first>
    - lib/mascot/crop-sprites.ts
    - .planning/phases/05-visuels-ferme/05-RESEARCH.md
  </read_first>

  <action>
Les sprites actuels dans `assets/garden/crops/` sont des placeholders (~300 bytes chacun). Les vrais assets Mana Seed sont achetes mais pas encore extraits.

**Ce qui est necessaire:**
- 10 cultures : carrot, wheat, tomato, strawberry, potato, corn, pumpkin, cabbage, beetroot, cucumber
- Pour chaque culture : 5 stades (0-4), 2 frames par stade (a et b)
- Nommage cible : `assets/garden/crops/{culture}/stage_{N}_a.png` et `stage_{N}_b.png`
- Format : pixel 32x32, style Mana Seed uniforme (D-13)
- Les fichiers `icon.png` restent inchanges

**Action developpeur:**
1. Localiser le dossier Mana Seed sur la machine (probablement `~/Downloads/`, `~/Desktop/`, ou un sous-dossier de `~/Documents/`)
2. Indiquer le chemin au agent qui se chargera d'extraire et renommer les sprites
3. Si les assets Mana Seed ne contiennent pas exactement 2 frames par stade, le developpeur decide comment generer les frames B (ex: leger crop/decalage du frame A)

**Verification apres placement:**
- Chaque fichier `stage_*_a.png` et `stage_*_b.png` doit faire > 500 bytes (les placeholders font ~300 bytes)
- 100 fichiers crop total (10 cultures x 5 stades x 2 frames)
- Les anciens `stage_0.png` ... `stage_4.png` peuvent etre supprimes apres verification
  </action>

  <acceptance_criteria>
    - `find assets/garden/crops -name "stage_*_a.png" | wc -l` retourne 50
    - `find assets/garden/crops -name "stage_*_b.png" | wc -l` retourne 50
    - `find assets/garden/crops -name "stage_*_a.png" -size +500c | wc -l` retourne 50 (pas des placeholders)
  </acceptance_criteria>

  <done>
    100 vrais sprites Mana Seed (stage_X_a.png et stage_X_b.png) sont presents dans assets/garden/crops/ pour les 10 cultures x 5 stades. Chaque fichier fait plus de 500 bytes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Restructurer CROP_SPRITES pour 2 frames et ajouter frame swap dans CropCell</name>
  <files>lib/mascot/crop-sprites.ts, components/mascot/WorldGridView.tsx, components/mascot/FarmPlots.tsx</files>

  <read_first>
    - lib/mascot/crop-sprites.ts
    - components/mascot/WorldGridView.tsx
    - components/mascot/FarmPlots.tsx
    - components/mascot/AmbientParticles.tsx (pour le pattern useReducedMotion)
  </read_first>

  <action>
**1. Restructurer `lib/mascot/crop-sprites.ts`:**

Changer la signature de `CROP_SPRITES` de `Record<string, Record<number, any>>` vers `Record<string, Record<number, [any, any]>>`.

Pour chaque culture et chaque stade, remplacer le `require` unique par un tuple de 2 requires:
```typescript
export const CROP_SPRITES: Record<string, Record<number, [any, any]>> = {
  carrot: {
    0: [require('../../assets/garden/crops/carrot/stage_0_a.png'),
        require('../../assets/garden/crops/carrot/stage_0_b.png')],
    1: [require('../../assets/garden/crops/carrot/stage_1_a.png'),
        require('../../assets/garden/crops/carrot/stage_1_b.png')],
    2: [require('../../assets/garden/crops/carrot/stage_2_a.png'),
        require('../../assets/garden/crops/carrot/stage_2_b.png')],
    3: [require('../../assets/garden/crops/carrot/stage_3_a.png'),
        require('../../assets/garden/crops/carrot/stage_3_b.png')],
    4: [require('../../assets/garden/crops/carrot/stage_4_a.png'),
        require('../../assets/garden/crops/carrot/stage_4_b.png')],
  },
  // Meme structure pour: wheat, tomato, strawberry, potato, corn, pumpkin, cabbage, beetroot, cucumber
};
```

`CROP_ICONS` reste inchange.

**2. Modifier `CropCell` dans `components/mascot/WorldGridView.tsx`:**

Ajouter un state `frameIdx` et un timer d'alternance :

```typescript
import { useReducedMotion } from 'react-native-reanimated';

function CropCell({ cell, crop, cropDef, isMature, containerWidth, containerHeight, onPress }: { ... }) {
  // ... existing pulse and growScale code ...

  // Frame swap animation (VIS-02)
  const reducedMotion = useReducedMotion();
  const [frameIdx, setFrameIdx] = React.useState(0);

  React.useEffect(() => {
    if (reducedMotion || !crop) return;
    const timer = setInterval(() => setFrameIdx(i => 1 - i), 800);
    return () => clearInterval(timer);
  }, [reducedMotion, crop?.cropId]);
```

Modifier le rendu du sprite (actuellement lignes 115-118) :
```typescript
// Ancien:
// {CROP_SPRITES[crop.cropId]?.[crop.currentStage] ? (
//   <Image source={CROP_SPRITES[crop.cropId][crop.currentStage]} ... />
// Nouveau:
const frames = CROP_SPRITES[crop.cropId]?.[crop.currentStage];
const spriteSource = frames ? frames[frameIdx] : null;
// ...
{spriteSource ? (
  <Image source={spriteSource} style={styles.cropSprite as any} />
) : (
  <Text style={styles.cropEmoji}>{cropDef.emoji}</Text>
)}
```

**3. Mettre a jour `components/mascot/FarmPlots.tsx`:**

`FarmPlots.tsx` importe aussi `CROP_SPRITES`. Adapter son usage pour supporter le nouveau type `[any, any]`. Chercher toute reference a `CROP_SPRITES[...][...]` et ajouter `[0]` pour prendre la frame A par defaut (FarmPlots n'est pas le rendu actif, mais doit compiler).

**4. Cultures dorees (D-08):** Aucune modification specifique — les cultures dorees utilisent le meme `CROP_SPRITES` et la meme animation. Le lisere dore existant les distingue deja.
  </action>

  <verify>
    <automated>cd /Users/gabrielwaltio/Documents/family-vault && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>

  <acceptance_criteria>
    - `crop-sprites.ts` contient `Record<string, Record<number, [any, any]>>`
    - `crop-sprites.ts` contient `stage_0_a.png`
    - `crop-sprites.ts` contient `stage_0_b.png`
    - `WorldGridView.tsx` contient `frameIdx`
    - `WorldGridView.tsx` contient `setInterval` avec `800`
    - `WorldGridView.tsx` contient `useReducedMotion`
    - `WorldGridView.tsx` contient `frames[frameIdx]` ou `frames ? frames[frameIdx]`
    - `FarmPlots.tsx` compile sans erreur TS liee a CROP_SPRITES
    - `npx tsc --noEmit` passe sans nouvelles erreurs
  </acceptance_criteria>

  <done>
    CROP_SPRITES restructure avec 2 frames par stade ([frameA, frameB]). CropCell dans WorldGridView alterne entre les 2 frames toutes les 800ms. useReducedMotion desactive l'alternance. FarmPlots mis a jour pour compiler avec le nouveau type. Cultures dorees utilisent la meme animation.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passe sans nouvelles erreurs
2. `grep -c "stage_0_a.png" lib/mascot/crop-sprites.ts` retourne 10 (une par culture)
3. `grep "frameIdx" components/mascot/WorldGridView.tsx` confirme le frame swap
4. `grep "useReducedMotion" components/mascot/WorldGridView.tsx` confirme l'accessibilite
5. `find assets/garden/crops -name "stage_*_a.png" | wc -l` retourne 50
</verification>

<success_criteria>
- Les 10 cultures ont 2 frames par stade (100 vrais PNGs dans assets/garden/crops/)
- CropCell alterne visuellement entre frame A et frame B toutes les ~800ms
- L'animation est visible a l'oeil nu dans l'app
- `npx tsc --noEmit` passe
</success_criteria>

<output>
After completion, create `.planning/phases/05-visuels-ferme/05-02-SUMMARY.md`
</output>
