---
phase: 47-auberge-sprites-pixel-art-animations-microcopy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - assets/buildings/auberge_lv1.png
  - assets/buildings/auberge_lv2.png
  - assets/buildings/auberge_lv3.png
  - lib/mascot/building-sprites.ts
autonomous: false
requirements:
  - AUBERGE-SPRITES-BUILDING
must_haves:
  truths:
    - "Construire l'auberge L1 affiche un sprite pixel art (pas un emoji)"
    - "Améliorer l'auberge à L2/L3 change le sprite affiché"
    - "Les 3 sprites partagent un style cohérent avec poulailler/grange/moulin/ruche"
  artifacts:
    - path: "assets/buildings/auberge_lv1.png"
      provides: "Sprite auberge niveau 1 (cabane simple, 64x64, palette DB16)"
    - path: "assets/buildings/auberge_lv2.png"
      provides: "Sprite auberge niveau 2 (toit étoffé + lanterne)"
    - path: "assets/buildings/auberge_lv3.png"
      provides: "Sprite auberge niveau 3 (bâtiment complet avec enseigne)"
    - path: "lib/mascot/building-sprites.ts"
      provides: "Registry étendu avec entrée 'auberge'"
      contains: "auberge: expandSprites"
  key_links:
    - from: "lib/mascot/building-sprites.ts"
      to: "assets/buildings/auberge_lv*.png"
      via: "require() top-level"
      pattern: "require.*auberge_lv"
---

<objective>
Générer 3 sprites pixel art pour le bâtiment Auberge (L1/L2/L3) via MCP pixellab, et les enregistrer dans `building-sprites.ts` pour qu'ils soient automatiquement consommés par la grille ferme et les modals existantes.

Purpose: Remplacer le fallback emoji 🛖 par un visuel pixel art progressif, cohérent avec les autres bâtiments productifs (poulailler, grange, moulin, ruche).

Output: 3 PNG 64×64 dans `assets/buildings/` + extension du registry.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/47-auberge-sprites-pixel-art-animations-microcopy/47-CONTEXT.md
@lib/mascot/building-sprites.ts
@CLAUDE.md

<interfaces>
<!-- Pattern registry à étendre (déjà en place pour 4 autres bâtiments). -->

From lib/mascot/building-sprites.ts:
```typescript
function expandSprites(lv1: any, lv2: any, lv3: any): Record<number, any>;
export const BUILDING_SPRITES: Record<string, Record<number, any>>;
// Clés actuelles : poulailler, grange, moulin, ruche.
// Niveaux 4-10 réutilisent automatiquement le sprite L3.
```

Références visuelles (à imiter pour cohérence) :
- assets/buildings/poulailler_lv1.png, poulailler_lv2.png, poulailler_lv3.png
- assets/buildings/grange_lv1.png, grange_lv2.png, grange_lv3.png
- assets/buildings/moulin_lv1.png, moulin_lv2.png, moulin_lv3.png
- assets/buildings/ruche_lv1.png, ruche_lv2.png, ruche_lv3.png

Contraintes générales (pixellab) :
- Taille : 64×64
- Palette : DB16 stricte (pas de pur noir #000000 ni pur blanc #FFFFFF)
- Lumière : top-left, ombres bottom-right
- Outline : selout coloré (jamais pur noir)
- Fond transparent
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Générer les 3 sprites auberge via MCP pixellab</name>
  <files>assets/buildings/auberge_lv1.png, assets/buildings/auberge_lv2.png, assets/buildings/auberge_lv3.png</files>
  <action>
    Avant de générer : lire les 4 sprites L1 de référence (poulailler_lv1.png, grange_lv1.png, moulin_lv1.png, ruche_lv1.png) avec le tool Read pour calibrer le style attendu (silhouette, palette, niveau de détail).

    Générer 3 sprites via `mcp__pixellab__create_object` (un appel par niveau, séquentiels — éviter rate-limit).

    **L1 — auberge_lv1.png (cabane simple)** :
    - Prompt suggéré : "small cozy medieval inn cabin, pixel art, wooden walls, simple sloped roof with a chimney, single door, one round window, warm wood tones, top-left lighting, DB16 palette"
    - Taille 64×64, pas de fond, palette DB16.

    **L2 — auberge_lv2.png (toit étoffé + lanterne)** :
    - Prompt suggéré : "medieval inn building, pixel art, wooden walls with stone foundation, larger thatched roof, hanging lit lantern by the door, two windows, smoke from chimney, top-left lighting, DB16 palette"
    - Taille 64×64.

    **L3 — auberge_lv3.png (bâtiment complet avec enseigne)** :
    - Prompt suggéré : "grand medieval tavern inn, pixel art, two-story wooden and stone building, ornate slate roof, hanging painted sign on iron bracket, multiple windows with shutters, lit lantern, smoke from large chimney, flower box, top-left lighting, DB16 palette"
    - Taille 64×64.

    Fallback si MCP `pixellab` indisponible/échec : utiliser l'API HTTP directe avec `PIXELLAB_API_KEY` (lu dans `.env`). Vérifier la doc avant d'appeler. Si la qualité est insuffisante après 1-2 itérations, l'exécuteur peut générer un placeholder via MCP `dogsprite` en notant la dette dans SUMMARY.

    Télécharger les PNG et les écrire dans `assets/buildings/auberge_lv{1,2,3}.png`.

    Vérification rapide après chaque génération :
    - `file assets/buildings/auberge_lv1.png` doit confirmer "PNG image data, 64 x 64".
    - Comparer visuellement avec poulailler_lv* (silhouette progressive, cohérence palette).

    Cohérence inter-niveaux : la silhouette L3 doit englober L2 qui doit englober L1 (l'auberge "grandit", pas changement complet).
  </action>
  <verify>
    <automated>file assets/buildings/auberge_lv1.png assets/buildings/auberge_lv2.png assets/buildings/auberge_lv3.png | grep -c "64 x 64"</automated>
  </verify>
  <done>
    3 fichiers PNG 64×64 existent dans assets/buildings/. Style cohérent avec les 4 autres bâtiments (vérifié visuellement contre les références).
  </done>
</task>

<task type="auto">
  <name>Task 2: Étendre BUILDING_SPRITES avec l'entrée auberge</name>
  <files>lib/mascot/building-sprites.ts</files>
  <action>
    Modifier `lib/mascot/building-sprites.ts` :

    1. Ajouter 3 require au top-level (cohérence avec les 4 autres bâtiments) :
       ```ts
       const AUBERGE_LV1 = require('../../assets/buildings/auberge_lv1.png');
       const AUBERGE_LV2 = require('../../assets/buildings/auberge_lv2.png');
       const AUBERGE_LV3 = require('../../assets/buildings/auberge_lv3.png');
       ```

    2. Ajouter l'entrée dans `BUILDING_SPRITES` :
       ```ts
       auberge: expandSprites(AUBERGE_LV1, AUBERGE_LV2, AUBERGE_LV3),
       ```

    Aucune autre modification — la grille ferme et les modals (BuildingShopSheet/BuildingDetailSheet) consomment déjà ce registry par `buildingId`.

    Vérifier qu'aucun autre fichier ne hardcode l'emoji 🛖 pour la grille — si trouvé, ajuster pour utiliser le sprite via le registry. Sinon, no-op.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "building-sprites|auberge" | head -5</automated>
  </verify>
  <done>
    `BUILDING_SPRITES.auberge` exposé avec 3 niveaux mappés. Métro résout les require sans erreur. Aucune régression TypeScript.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: 3 sprites auberge L1/L2/L3 générés et enregistrés dans BUILDING_SPRITES.</name>
  <what-built>3 sprites auberge L1/L2/L3 générés et enregistrés dans BUILDING_SPRITES.</what-built>
  <files>N/A — verification only</files>
  <action>3 sprites auberge L1/L2/L3 générés et enregistrés dans BUILDING_SPRITES.

Étapes de vérification :
    1. Lancer le dev-client : `npx expo run:ios --device`.
    2. Aller sur la ferme (mascotte → grille bâtiments).
    3. Construire l'auberge (si pas déjà fait, profil suffisamment avancé).
    4. Confirmer : sprite pixel art L1 visible (pas l'emoji 🛖).
    5. Améliorer vers L2 puis L3 — le sprite change à chaque niveau.
    6. Style cohérent avec poulailler/grange/moulin/ruche (palette DB16, échelle similaire, lumière top-left).
  </action>
  <how-to-verify>
    1. Lancer le dev-client : `npx expo run:ios --device`.
    2. Aller sur la ferme (mascotte → grille bâtiments).
    3. Construire l'auberge (si pas déjà fait, profil suffisamment avancé).
    4. Confirmer : sprite pixel art L1 visible (pas l'emoji 🛖).
    5. Améliorer vers L2 puis L3 — le sprite change à chaque niveau.
    6. Style cohérent avec poulailler/grange/moulin/ruche (palette DB16, échelle similaire, lumière top-left).
  </how-to-verify>
  <verify>Validation manuelle par l'utilisateur (voir how-to-verify)</verify>
  <done>Utilisateur a tapé "approved" après validation visuelle.</done>
  <resume-signal>Type "approved" ou décrire les soucis visuels (régénérer si besoin).</resume-signal>
</task>

</tasks>

<verification>
- 3 PNG 64×64 commités dans `assets/buildings/`.
- `BUILDING_SPRITES.auberge` exposé.
- `npx tsc --noEmit` clean.
- Vérification visuelle dev-client OK.
</verification>

<success_criteria>
Auberge construite affiche un sprite pixel art à L1/L2/L3 dans la grille ferme. Style cohérent avec les autres bâtiments. Aucune régression TypeScript.
</success_criteria>

<output>
After completion, create `.planning/phases/47-auberge-sprites-pixel-art-animations-microcopy/47-01-SUMMARY.md`
</output>
