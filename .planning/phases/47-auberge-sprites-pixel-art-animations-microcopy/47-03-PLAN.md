---
phase: 47-auberge-sprites-pixel-art-animations-microcopy
plan: 03
type: execute
wave: 2
depends_on:
  - 47-01
  - 47-02
files_modified:
  - lib/mascot/types.ts
  - lib/mascot/auberge-engine.ts
  - components/mascot/AubergeSheet.tsx
  - components/dashboard/DashboardAuberge.tsx
autonomous: true
requirements:
  - AUBERGE-WIRING-SPRITES
  - AUBERGE-THEME-COLORS
  - AUBERGE-LOOTCHANCE-SNAPSHOT
must_haves:
  truths:
    - "Les cartes visiteur dans AubergeSheet affichent le portrait pixel art quand disponible"
    - "Le dashboard Auberge affiche le portrait pixel art quand disponible"
    - "Aucune couleur hex hardcodée pour les timers (TIMER_AMBER/TIMER_RED supprimés)"
    - "Le pourcentage de loot affiché correspond à la rareté du visiteur (8/18/35%)"
    - "Les 76 tests Auberge existants passent toujours"
  artifacts:
    - path: "lib/mascot/types.ts"
      provides: "ActiveVisitor.lootChance?: number ajouté"
      contains: "lootChance?:"
    - path: "lib/mascot/auberge-engine.ts"
      provides: "spawnVisitor snapshote lootChance"
      contains: "lootChance:"
    - path: "components/mascot/AubergeSheet.tsx"
      provides: "Wiring sprites + theme colors + lootChance"
    - path: "components/dashboard/DashboardAuberge.tsx"
      provides: "Wiring sprites + theme colors"
  key_links:
    - from: "components/mascot/AubergeSheet.tsx"
      to: "lib/mascot/visitor-sprites.ts"
      via: "VISITOR_SPRITES[visitor.visitorId]"
      pattern: "VISITOR_SPRITES\\["
    - from: "components/dashboard/DashboardAuberge.tsx"
      to: "lib/mascot/visitor-sprites.ts"
      via: "VISITOR_SPRITES[visitor.visitorId]"
      pattern: "VISITOR_SPRITES\\["
    - from: "lib/mascot/auberge-engine.ts"
      to: "ActiveVisitor.lootChance"
      via: "spawnVisitor snapshot"
      pattern: "lootChance:"
---

<objective>
Câbler les portraits PNJ et le sprite auberge dans l'UI, migrer les couleurs de timer hardcodées vers `useThemeColors()`, et snapshotter `lootChance` dans `ActiveVisitor` au spawn.

Purpose: Faire passer Auberge du fallback emoji aux vrais sprites, finir le travail de tokenisation thème, et corriger l'affichage trompeur "18%" qui ne reflète pas la rareté.

Output: 4 fichiers modifiés, aucune régression sur les 76 tests Auberge.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/47-auberge-sprites-pixel-art-animations-microcopy/47-CONTEXT.md
@lib/mascot/types.ts
@lib/mascot/auberge-engine.ts
@components/mascot/AubergeSheet.tsx
@components/dashboard/DashboardAuberge.tsx
@CLAUDE.md

<interfaces>
<!-- Types et constantes utilisés -->

From lib/mascot/types.ts (extrait):
```typescript
export interface ActiveVisitor {
  visitorId: string;
  instanceId: string;
  arrivedAt: string;
  deadlineAt: string;
  request: VisitorRequestItem[];
  status: VisitorStatus;
  rewardCoins: number;
  rewardLootKey?: string;
  // À AJOUTER : lootChance?: number (snapshot 0..1, optionnel pour rétrocompat)
}
```

From lib/mascot/auberge-engine.ts (extrait):
```typescript
const LOOT_CHANCE: Record<'common' | 'uncommon' | 'rare', number> = {
  common: 0.08,
  uncommon: 0.18,
  rare: 0.35,
};
// Déjà utilisé par deliverVisitor, à snapshoter aussi dans spawnVisitor.
```

From lib/mascot/visitor-sprites.ts (Plan 02):
```typescript
export const VISITOR_SPRITES: Record<string, any>;
// Clés : hugo_boulanger, meme_lucette, voyageuse, yann_apiculteur, marchand_ambulant, comtesse
```

From contexts/ThemeContext.tsx :
```typescript
export function useThemeColors(): { primary, tint, colors, isDark, ... };
```

From constants/colors.ts (LightColors / DarkColors) :
```typescript
warning: string; // ambre — pour timer < 6h
warningText: string;
error: string;   // rouge — pour timer < 1h ou < 2h
errorText: string;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Ajouter ActiveVisitor.lootChance et snapshot au spawn</name>
  <files>lib/mascot/types.ts, lib/mascot/auberge-engine.ts</files>
  <behavior>
    - Ajout `lootChance?: number` (optionnel, valeur 0..1) sur `ActiveVisitor` — pas de bump CACHE_VERSION (champ optionnel rétrocompat).
    - `spawnVisitor` populate `lootChance: LOOT_CHANCE[def.rarity]` au snapshot.
    - `deliverVisitor` continue d'utiliser `LOOT_CHANCE[def.rarity]` (fallback) — préserve la compat avec les visiteurs persistés sans lootChance. Pas de changement de logique de roll.
    - Les 76 tests existants Auberge passent (`npx jest auberge --no-coverage`) sans modification.
  </behavior>
  <action>
    1. Dans `lib/mascot/types.ts`, sur l'interface `ActiveVisitor` (≈ ligne 595), ajouter après `rewardLootKey?: string` :
       ```ts
       /** Phase 47 — chance de drop loot (0..1) snapshotée au spawn selon rareté.
        *  Optionnel : visiteurs pré-Phase 47 n'ont pas le champ → fallback LOOT_CHANCE moteur. */
       lootChance?: number;
       ```

    2. Dans `lib/mascot/auberge-engine.ts`, fonction `spawnVisitor` (≈ ligne 275), ajouter `lootChance` dans la construction du `visitor: ActiveVisitor` :
       ```ts
       const visitor: ActiveVisitor = {
         visitorId: def.id,
         instanceId: generateInstanceId(),
         arrivedAt,
         deadlineAt,
         request,
         status: 'active',
         rewardCoins,
         lootChance: LOOT_CHANCE[def.rarity],
       };
       ```

    3. Vérifier que `deliverVisitor` n'a pas besoin de modification — il utilise déjà `LOOT_CHANCE[def.rarity]` en lecture du catalogue. Ne PAS changer la logique de roll (la cohérence avec les tests existants compte).

    4. Le sérialiseur `encodeVisitor` utilise `JSON.stringify(v)` — il sérialise donc automatiquement `lootChance`. Aucune modif persistance.

    5. Run `npx tsc --noEmit` puis `npx jest auberge --no-coverage` (ou suite équivalente : grep `auberge` dans `lib/__tests__/`).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx jest --no-coverage --testPathPattern="auberge" 2>&1 | tail -5</automated>
  </verify>
  <done>
    `ActiveVisitor.lootChance?: number` ajouté. `spawnVisitor` snapshote la valeur. tsc clean. Tests Auberge passent.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Wiring sprites + theme colors + lootChance dans AubergeSheet</name>
  <files>components/mascot/AubergeSheet.tsx</files>
  <behavior>
    - Le portrait du visiteur dans la `VisitorCard` utilise `<Image source={VISITOR_SPRITES[visitor.visitorId]} />` quand disponible, fallback emoji sinon.
    - Les constantes hardcodées `TIMER_AMBER` et `TIMER_RED` sont supprimées et remplacées par `colors.warning` et `colors.error` (lus depuis `useThemeColors()`).
    - L'affichage du loot pct utilise `Math.round((visitor.lootChance ?? 0.18) * 100)` au lieu du `0.18` codé en dur — fallback 0.18 pour visiteurs persistés pré-Phase 47.
    - Aucune régression visuelle : le portrait remplace l'emoji 56px dans une frame 72×72, l'emoji reste en fallback si la clé n'existe pas dans VISITOR_SPRITES.
  </behavior>
  <action>
    Modifier `components/mascot/AubergeSheet.tsx` :

    1. **Imports** : ajouter
       ```ts
       import { Image } from 'react-native';
       import { VISITOR_SPRITES } from '../../lib/mascot/visitor-sprites';
       ```

    2. **Supprimer les constantes hardcodées** (lignes ≈51-52) :
       ```ts
       // SUPPRIMER :
       const TIMER_AMBER = '#F59E0B';
       const TIMER_RED = '#EF4444';
       ```

    3. **Refactor `timerColor`** : passer les couleurs en paramètres pour éviter qu'elles soient module-level (move à l'intérieur du composant ou prendre amber/red en args). Solution simple : transformer `timerColor` pour accepter `(minutes, fallback, amber, red)` ou faire un helper inline dans `VisitorCard`.

       Pattern recommandé :
       ```ts
       function timerColor(minutes: number, fallback: string, amber: string, red: string): string {
         if (minutes <= 0) return red;
         if (minutes < 60) return red;
         if (minutes < 360) return amber;
         return fallback;
       }
       ```
       Et dans `VisitorCard` :
       ```ts
       const tColor = timerColor(minutes, colors.textMuted, colors.warning, colors.error);
       ```

    4. **Wiring portrait** dans `VisitorCard` (≈ lignes 176-180), remplacer :
       ```tsx
       <View style={[styles.portrait, { backgroundColor: colors.cardAlt }]}>
         <Text style={styles.portraitEmoji}>{emoji}</Text>
       </View>
       ```
       par :
       ```tsx
       <View style={[styles.portrait, { backgroundColor: colors.cardAlt }]}>
         {VISITOR_SPRITES[visitor.visitorId] ? (
           <Image
             source={VISITOR_SPRITES[visitor.visitorId]}
             style={styles.portraitSprite}
             resizeMode="contain"
           />
         ) : (
           <Text style={styles.portraitEmoji}>{emoji}</Text>
         )}
       </View>
       ```
       Ajouter dans `styles` :
       ```ts
       portraitSprite: { width: 64, height: 64 },
       ```

    5. **lootPct depuis snapshot** (≈ ligne 164) :
       ```ts
       const lootPct = def?.preferredLoot && def.preferredLoot.length > 0
         ? Math.round((visitor.lootChance ?? 0.18) * 100)
         : 0;
       ```

    6. Run `npx tsc --noEmit`.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "AubergeSheet" | head -5</automated>
  </verify>
  <done>
    AubergeSheet sans hex hardcodé pour les timers, portraits affichés depuis VISITOR_SPRITES, lootPct calculé depuis lootChance. tsc clean.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Wiring sprites + theme colors dans DashboardAuberge</name>
  <files>components/dashboard/DashboardAuberge.tsx</files>
  <behavior>
    - Les `VisitorRow` du dashboard utilisent `<Image source={VISITOR_SPRITES[...]} />` (taille 32-40px) avec fallback emoji.
    - Les constantes `TIMER_AMBER` et `TIMER_RED` sont supprimées, remplacées par `colors.warning` / `colors.error`.
    - Le pulse Reanimated et le reste du composant restent intacts.
  </behavior>
  <action>
    Modifier `components/dashboard/DashboardAuberge.tsx` :

    1. **Imports** : ajouter
       ```ts
       import { Image } from 'react-native';
       import { VISITOR_SPRITES } from '../../lib/mascot/visitor-sprites';
       ```

    2. **Supprimer hardcoded** (≈ lignes 40-41) :
       ```ts
       // SUPPRIMER : const TIMER_AMBER = '#F59E0B'; const TIMER_RED = '#EF4444';
       ```

    3. **Adapter `timerColor`** pour prendre amber/red en paramètres (même approche que Plan task 2). Passer `colors.warning` et `colors.error` depuis le composant.

    4. **VisitorRow signature** : ajouter `amberColor` et `redColor` aux props (ou simplifier en ne passant que la couleur déjà résolue). Plus simple : calculer `tColor` côté `DashboardAubergeInner` avec `useThemeColors()` et le passer en prop `tColor` à `VisitorRow`.

       Pattern recommandé : calculer dans le parent :
       ```ts
       const tColors = previewVisitors.map((_, idx) =>
         timerColor(remainingForPreview[idx], colors.textMuted, colors.warning, colors.error)
       );
       ```
       Et passer `tColor={tColors[idx]}` au lieu d'`amberColor/redColor`.

    5. **Wiring sprite** dans `VisitorRow` (≈ ligne 100), remplacer :
       ```tsx
       <Text style={styles.rowEmoji}>{getVisitorEmoji(visitor.visitorId)}</Text>
       ```
       par :
       ```tsx
       {VISITOR_SPRITES[visitor.visitorId] ? (
         <Image
           source={VISITOR_SPRITES[visitor.visitorId]}
           style={styles.rowSprite}
           resizeMode="contain"
         />
       ) : (
         <Text style={styles.rowEmoji}>{getVisitorEmoji(visitor.visitorId)}</Text>
       )}
       ```
       Ajouter dans `styles` :
       ```ts
       rowSprite: { width: 32, height: 32 },
       ```

    6. Run `npx tsc --noEmit`.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "DashboardAuberge" | head -5</automated>
  </verify>
  <done>
    DashboardAuberge sans hex hardcodé pour les timers, portraits affichés. tsc clean. Pulse Reanimated intact.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Vérifier régression couleurs hardcodées + tests Auberge</name>
  <files>components/mascot/AubergeSheet.tsx, components/dashboard/DashboardAuberge.tsx</files>
  <behavior>
    - Aucune occurrence de `#F59E0B` ou `#EF4444` ne subsiste dans AubergeSheet.tsx ni DashboardAuberge.tsx.
    - Les 76 tests Auberge passent.
  </behavior>
  <action>
    1. Grep de contrôle :
       ```bash
       grep -nE "#F59E0B|#EF4444" components/mascot/AubergeSheet.tsx components/dashboard/DashboardAuberge.tsx
       ```
       Doit retourner 0 résultat.

    2. `npx tsc --noEmit` clean.

    3. `npx jest --no-coverage --testPathPattern="auberge"` — tous les tests passent.

    Si une régression test apparaît : enquêter, ne PAS désactiver de tests. La modification du moteur (Task 1) est minimale et compatible — toute casse est probablement un test trop strict sur la shape d'`ActiveVisitor` qu'il faudra étendre pour inclure `lootChance` optionnellement. Préserver le comportement attendu.
  </action>
  <verify>
    <automated>! grep -E "#F59E0B|#EF4444" components/mascot/AubergeSheet.tsx components/dashboard/DashboardAuberge.tsx && npx jest --no-coverage --testPathPattern="auberge" 2>&1 | tail -3</automated>
  </verify>
  <done>
    Aucune couleur hex timer hardcodée ne subsiste. Tests Auberge tous passent. tsc clean.
  </done>
</task>

</tasks>

<verification>
- `grep -E "#F59E0B|#EF4444" components/mascot/AubergeSheet.tsx components/dashboard/DashboardAuberge.tsx` → 0 résultat.
- `npx tsc --noEmit` clean.
- `npx jest --no-coverage --testPathPattern="auberge"` → tous verts.
- Lancement dev-client (manuel, validation Plan 04) — visiteurs affichent portraits.
</verification>

<success_criteria>
Plus aucun hex de timer hardcodé. Sprites visiteurs affichés (avec fallback emoji). lootChance snapshoté au spawn et affiché correctement (8/18/35%). 76 tests Auberge passent.
</success_criteria>

<output>
After completion, create `.planning/phases/47-auberge-sprites-pixel-art-animations-microcopy/47-03-SUMMARY.md`
</output>
