---
phase: 47-auberge-sprites-pixel-art-animations-microcopy
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - assets/visitors/boulanger.png
  - assets/visitors/lucette.png
  - assets/visitors/voyageuse.png
  - assets/visitors/apiculteur.png
  - assets/visitors/marchand.png
  - assets/visitors/comtesse.png
  - lib/mascot/visitor-sprites.ts
autonomous: false
requirements:
  - AUBERGE-SPRITES-VISITORS
must_haves:
  truths:
    - "6 fichiers PNG portraits PNJ existent dans assets/visitors/"
    - "VISITOR_SPRITES exporte un mapping visitorId → require"
    - "Chaque PNJ du VISITOR_CATALOG a un sprite associé"
  artifacts:
    - path: "assets/visitors/boulanger.png"
      provides: "Portrait Hugo le boulanger"
    - path: "assets/visitors/lucette.png"
      provides: "Portrait Mémé Lucette"
    - path: "assets/visitors/voyageuse.png"
      provides: "Portrait La Voyageuse"
    - path: "assets/visitors/apiculteur.png"
      provides: "Portrait Yann l'apiculteur"
    - path: "assets/visitors/marchand.png"
      provides: "Portrait Le Marchand ambulant"
    - path: "assets/visitors/comtesse.png"
      provides: "Portrait La Comtesse"
    - path: "lib/mascot/visitor-sprites.ts"
      provides: "Registry portraits PNJ (nouveau)"
      contains: "export const VISITOR_SPRITES"
  key_links:
    - from: "lib/mascot/visitor-sprites.ts"
      to: "assets/visitors/*.png"
      via: "require() top-level"
      pattern: "require.*assets/visitors"
---

<objective>
Générer 6 portraits pixel art (32×32) pour les PNJ visiteurs de l'Auberge via MCP pixellab, et créer le registry `lib/mascot/visitor-sprites.ts` qui mappe `visitorId → asset`.

Purpose: Préparer les assets et le registry consommables par AubergeSheet et DashboardAuberge (wiring fait au Plan 03).

Output: 6 PNG 32×32 + 1 fichier TS registry.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/47-auberge-sprites-pixel-art-animations-microcopy/47-CONTEXT.md
@lib/mascot/building-sprites.ts
@CLAUDE.md

<interfaces>
<!-- visitorId values du VISITOR_CATALOG (Phase 43) -->
<!-- Source: lib/mascot/visitor-catalog.ts — IDs canoniques -->

VisitorIds (canoniques — DOIVENT matcher exactement) :
- hugo_boulanger
- meme_lucette
- yann_apiculteur
- voyageuse
- marchand_ambulant
- comtesse

Pattern registry (à imiter sur building-sprites.ts) :
```typescript
const HUGO = require('../../assets/visitors/boulanger.png');
// ...
export const VISITOR_SPRITES: Record<string, any> = {
  hugo_boulanger: HUGO,
  // ...
};
```

Référence visuelle (style attendu) :
- assets/garden/animals/oiseau/idle_1.png, ecureuil/idle_1.png, etc. (24×24, palette DB16, style "buste")
- Pour les humains, augmenter à 32×32 pour lisibilité.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Générer les 6 portraits PNJ via MCP pixellab</name>
  <files>assets/visitors/boulanger.png, assets/visitors/lucette.png, assets/visitors/voyageuse.png, assets/visitors/apiculteur.png, assets/visitors/marchand.png, assets/visitors/comtesse.png</files>
  <action>
    Avant de générer : créer le dossier `assets/visitors/` (`mkdir -p assets/visitors`).

    Lire 2-3 sprites de référence dans `assets/garden/animals/` pour calibrer le style.

    Générer 6 portraits via `mcp__pixellab__create_character` (séquentiels). Style "buste" (tête + épaules), 32×32, palette DB16, lumière top-left, fond transparent.

    Si la taille 32×32 manque de lisibilité après essai, basculer à 48×48 (Claude's Discretion documenté en CONTEXT.md). Garder une taille uniforme pour les 6.

    **1. boulanger.png — Hugo le boulanger**
    Prompt : "friendly bearded baker character bust portrait, white apron stained with flour, white chef's toque, warm smile, rosy cheeks, pixel art, DB16 palette, top-left lighting, transparent background"

    **2. lucette.png — Mémé Lucette**
    Prompt : "kind elderly grandma character bust portrait, gray hair in a bun, knit shawl over shoulders, gentle warm gaze, wrinkles, pixel art, DB16 palette, top-left lighting, transparent background"

    **3. voyageuse.png — La Voyageuse**
    Prompt : "mysterious hooded female traveler character bust portrait, dark green cloak with hood up, leather pack strap visible, mysterious shadowed eyes, pixel art, DB16 palette, top-left lighting, transparent background"

    **4. apiculteur.png — Yann l'apiculteur**
    Prompt : "young beekeeper character bust portrait, straw hat with veil pulled up, light beige beekeeping suit collar, calm smile, two tiny bees flying nearby, pixel art, DB16 palette, top-left lighting, transparent background"

    **5. marchand.png — Le Marchand ambulant**
    Prompt : "shrewd traveling merchant character bust portrait, pointed felt hat, monocle on right eye, small brass scale held up, fancy waistcoat, sly grin, pixel art, DB16 palette, top-left lighting, transparent background"

    **6. comtesse.png — La Comtesse**
    Prompt : "noble countess character bust portrait, small golden crown, elegant burgundy gown collar with white lace ruff, dignified expression, refined features, pixel art, DB16 palette, top-left lighting, transparent background"

    Fallback : API HTTP `https://api.pixellab.ai` avec `PIXELLAB_API_KEY` si MCP échoue. Si qualité insuffisante après 1-2 itérations, l'exécuteur peut basculer sur MCP `dogsprite` (en respectant les règles DB16/lumière).

    Vérifier après chaque génération : `file assets/visitors/{name}.png` confirme PNG aux dimensions choisies.
  </action>
  <verify>
    <automated>ls assets/visitors/*.png | wc -l | grep -q 6 && file assets/visitors/*.png | grep -c "PNG image"</automated>
  </verify>
  <done>
    6 PNG portraits dans assets/visitors/, taille uniforme (32×32 ou 48×48), palette DB16, style cohérent.
  </done>
</task>

<task type="auto">
  <name>Task 2: Créer lib/mascot/visitor-sprites.ts</name>
  <files>lib/mascot/visitor-sprites.ts</files>
  <action>
    Créer un nouveau fichier `lib/mascot/visitor-sprites.ts` calqué sur `building-sprites.ts` :

    ```ts
    /**
     * visitor-sprites.ts — Mapping portraits PNJ Auberge (Phase 47)
     */

    // Assets chargés au top-level pour garantir la résolution Metro
    const HUGO = require('../../assets/visitors/boulanger.png');
    const LUCETTE = require('../../assets/visitors/lucette.png');
    const VOYAGEUSE = require('../../assets/visitors/voyageuse.png');
    const YANN = require('../../assets/visitors/apiculteur.png');
    const MARCHAND = require('../../assets/visitors/marchand.png');
    const COMTESSE = require('../../assets/visitors/comtesse.png');

    /**
     * Mapping visitorId (du VISITOR_CATALOG) → asset PNG.
     * Les consommateurs (AubergeSheet, DashboardAuberge) doivent fallback emoji
     * si la clé n'est pas trouvée (rétrocompat futurs PNJ).
     */
    export const VISITOR_SPRITES: Record<string, any> = {
      hugo_boulanger:    HUGO,
      meme_lucette:      LUCETTE,
      voyageuse:         VOYAGEUSE,
      yann_apiculteur:   YANN,
      marchand_ambulant: MARCHAND,
      comtesse:          COMTESSE,
    };
    ```

    **Critique** : les clés DOIVENT matcher exactement les `id` du VISITOR_CATALOG (Phase 43). Vérifier en lisant `lib/mascot/visitor-catalog.ts` avant de commit.

    `npx tsc --noEmit` doit passer.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "visitor-sprites" | head -5</automated>
  </verify>
  <done>
    `visitor-sprites.ts` créé, exporte VISITOR_SPRITES avec les 6 entrées matching VISITOR_CATALOG ids. tsc clean.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: 6 portraits PNJ pixel art + registry visitor-sprites.ts.</name>
  <what-built>6 portraits PNJ pixel art + registry visitor-sprites.ts.</what-built>
  <files>N/A — verification only</files>
  <action>6 portraits PNJ pixel art + registry visitor-sprites.ts.

Étapes de vérification :
    1. `ls assets/visitors/` → 6 fichiers PNG.
    2. Ouvrir chaque PNG (preview macOS) — vérifier lisibilité de la silhouette + cohérence stylistique entre les 6.
    3. Confirmer que les IDs dans `visitor-sprites.ts` matchent ceux de `visitor-catalog.ts`.
    4. (Note : aucune intégration UI à ce stade — wiring au Plan 03.)
  </action>
  <how-to-verify>
    1. `ls assets/visitors/` → 6 fichiers PNG.
    2. Ouvrir chaque PNG (preview macOS) — vérifier lisibilité de la silhouette + cohérence stylistique entre les 6.
    3. Confirmer que les IDs dans `visitor-sprites.ts` matchent ceux de `visitor-catalog.ts`.
    4. (Note : aucune intégration UI à ce stade — wiring au Plan 03.)
  </how-to-verify>
  <verify>Validation manuelle par l'utilisateur (voir how-to-verify)</verify>
  <done>Utilisateur a tapé "approved" après validation visuelle.</done>
  <resume-signal>Type "approved" ou demander régénération de portraits spécifiques.</resume-signal>
</task>

</tasks>

<verification>
- 6 PNG dans `assets/visitors/`.
- `visitor-sprites.ts` exporte VISITOR_SPRITES avec 6 entrées correctement nommées.
- `npx tsc --noEmit` clean.
</verification>

<success_criteria>
Les 6 portraits sont prêts à être consommés par le Plan 03 (wiring UI). Style cohérent. Mapping ID correct.
</success_criteria>

<output>
After completion, create `.planning/phases/47-auberge-sprites-pixel-art-animations-microcopy/47-02-SUMMARY.md`
</output>
