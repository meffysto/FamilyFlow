---
phase: 22-ui-config-famille
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/semantic/coupling-overrides.ts
  - lib/semantic/index.ts
  - hooks/useGamification.ts
  - locales/fr/common.json
  - locales/en/common.json
autonomous: true
requirements:
  - COUPLING-03
  - COUPLING-05
  - COUPLING-06

must_haves:
  truths:
    - "Les overrides per-categorie sont persistes dans SecureStore et relus apres restart"
    - "Le dispatcher skip les effets des categories desactivees"
    - "Les stats semaine sont incrementees a chaque effet applique"
    - "Les stats se reset automatiquement au changement de semaine"
  artifacts:
    - path: "lib/semantic/coupling-overrides.ts"
      provides: "loadOverrides, saveOverrides, isCategoryEnabled, loadWeekStats, incrementWeekStat"
      exports: ["loadOverrides", "saveOverrides", "isCategoryEnabled", "loadWeekStats", "incrementWeekStat", "OVERRIDES_KEY", "WEEK_STATS_KEY"]
    - path: "hooks/useGamification.ts"
      provides: "Override check + stats increment dans completeTask"
      contains: "loadOverrides"
  key_links:
    - from: "hooks/useGamification.ts"
      to: "lib/semantic/coupling-overrides.ts"
      via: "import loadOverrides + incrementWeekStat"
      pattern: "import.*coupling-overrides"
    - from: "lib/semantic/coupling-overrides.ts"
      to: "expo-secure-store"
      via: "SecureStore.getItemAsync/setItemAsync"
      pattern: "SecureStore"
---

<objective>
Creer le module coupling-overrides.ts (persistance overrides + stats semaine) et l'injecter dans le dispatcher useGamification.ts pour que les categories desactivees soient skippees et les stats incrementees.

Purpose: Fournir le backend data layer que l'ecran Settings (Plan 02) consommera, et activer le filtrage per-categorie dans le hot path.
Output: lib/semantic/coupling-overrides.ts + useGamification.ts modifie + cles i18n coupling
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@lib/semantic/categories.ts
@lib/semantic/flag.ts
@lib/semantic/caps.ts
@lib/semantic/index.ts
@hooks/useGamification.ts

<interfaces>
<!-- Key types and contracts the executor needs -->

From lib/semantic/categories.ts:
```typescript
export type CategoryId =
  | 'menage_quotidien' | 'menage_hebdo' | 'courses'
  | 'enfants_routines' | 'enfants_devoirs' | 'rendez_vous'
  | 'gratitude_famille' | 'budget_admin' | 'bebe_soins' | 'cuisine_repas';
```

From lib/semantic/caps.ts:
```typescript
export function getWeekStart(now: Date = new Date()): string;
// Calcul du lundi de la semaine — REUTILISER cette fonction pour weekKey stats
```

From lib/semantic/flag.ts:
```typescript
export async function isSemanticCouplingEnabled(): Promise<boolean>;
export async function setSemanticCouplingEnabled(enabled: boolean): Promise<void>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Creer lib/semantic/coupling-overrides.ts + exporter dans barrel</name>
  <files>lib/semantic/coupling-overrides.ts, lib/semantic/index.ts</files>
  <read_first>
    - lib/semantic/flag.ts (pattern SecureStore a suivre)
    - lib/semantic/caps.ts (getWeekStart a reutiliser — NE PAS dupliquer)
    - lib/semantic/index.ts (barrel a etendre)
    - lib/semantic/categories.ts (type CategoryId)
  </read_first>
  <action>
Creer `lib/semantic/coupling-overrides.ts` avec ces exports exacts :

1. **Constantes cles SecureStore** (per D-02a, D-04a) :
   - `OVERRIDES_KEY = 'semantic-overrides'`
   - `WEEK_STATS_KEY = 'semantic-stats-week'`

2. **Cache module-level** (per Research Pitfall 6 — eviter 5-15ms SecureStore sur hot path) :
   - `let _overridesCache: Record<string, boolean> | null = null;`

3. **`loadOverrides(): Promise<Record<string, boolean>>`** :
   - Si `_overridesCache !== null`, retourner le cache immediatement
   - Sinon lire `SecureStore.getItemAsync(OVERRIDES_KEY)`
   - Parser JSON, stocker dans cache, retourner
   - Catch silencieux retourne `{}`

4. **`saveOverrides(overrides: Record<string, boolean>): Promise<void>`** :
   - Mettre a jour `_overridesCache = overrides`
   - Ecrire `SecureStore.setItemAsync(OVERRIDES_KEY, JSON.stringify(overrides))`

5. **`isCategoryEnabled(catId: CategoryId, overrides: Record<string, boolean>): boolean`** :
   - Retourner `overrides[catId] !== false` (cle absente = active par defaut, per D-02a)

6. **`loadWeekStats(): Promise<{ weekKey: string; counts: Record<string, number> }>`** :
   - Importer `getWeekStart` depuis `./caps` (REUTILISER, ne pas dupliquer — per Decision STATE.md Phase 20-04)
   - Lire `SecureStore.getItemAsync(WEEK_STATS_KEY)`
   - Si `parsed.weekKey !== getWeekStart()` → reset auto (D-04b/D-04d)
   - Catch silencieux retourne `{ weekKey: getWeekStart(), counts: {} }`

7. **`incrementWeekStat(catId: CategoryId): Promise<void>`** :
   - Charger stats via `loadWeekStats()`
   - `stats.counts[catId] = (stats.counts[catId] ?? 0) + 1`
   - Ecrire `SecureStore.setItemAsync(WEEK_STATS_KEY, JSON.stringify(stats))`
   - Catch silencieux (stats — non-critical)

Import pattern identique a flag.ts : `import * as SecureStore from 'expo-secure-store';`

Puis mettre a jour `lib/semantic/index.ts` barrel — ajouter en fin de fichier :
```typescript
// Phase 22 — coupling overrides + stats
export {
  loadOverrides,
  saveOverrides,
  isCategoryEnabled,
  loadWeekStats,
  incrementWeekStat,
  OVERRIDES_KEY,
  WEEK_STATS_KEY,
} from './coupling-overrides';
```
  </action>
  <verify>
    <automated>cd /Users/gabrielwaltio/Documents/family-vault && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "OVERRIDES_KEY.*=.*'semantic-overrides'" lib/semantic/coupling-overrides.ts
    - grep -q "WEEK_STATS_KEY.*=.*'semantic-stats-week'" lib/semantic/coupling-overrides.ts
    - grep -q "_overridesCache" lib/semantic/coupling-overrides.ts
    - grep -q "getWeekStart" lib/semantic/coupling-overrides.ts
    - grep -q "isCategoryEnabled" lib/semantic/coupling-overrides.ts
    - grep -q "coupling-overrides" lib/semantic/index.ts
    - npx tsc --noEmit (pas de nouvelle erreur)
  </acceptance_criteria>
  <done>coupling-overrides.ts exporte 7 symbols, barrel mis a jour, tsc passe</done>
</task>

<task type="auto">
  <name>Task 2: Injecter overrides check + stats increment dans useGamification.ts completeTask</name>
  <files>hooks/useGamification.ts</files>
  <read_first>
    - hooks/useGamification.ts (lire lignes 210-290 — bloc Phase 20 semantic coupling)
    - lib/semantic/coupling-overrides.ts (vient d'etre cree — Task 1)
  </read_first>
  <action>
Modifier `hooks/useGamification.ts` — injection dans `completeTask()` bloc Phase 20 (lignes ~210-286) :

1. **Ajouter import** en haut du fichier, a cote des imports `../lib/semantic` :
   ```typescript
   import { loadOverrides, isCategoryEnabled, incrementWeekStat } from '../lib/semantic/coupling-overrides';
   ```

2. **Injecter override check** — APRES `if (category) {` (ligne ~222) et APRES `const caps = await loadCaps(profile.id);` (ligne ~223), AVANT `if (!isCapExceeded(category.id, caps))` (ligne ~224) :
   ```typescript
   // Phase 22 COUPLING-03 : check override per-categorie (D-02c)
   const overrides = await loadOverrides();
   if (!isCategoryEnabled(category.id, overrides)) {
     // Categorie desactivee par l'utilisateur → skip effet (D-02c)
   } else if (!isCapExceeded(category.id, caps)) {
   ```
   Cela remplace le `if (!isCapExceeded(...))` existant par un `if (!isCategoryEnabled) {} else if (!isCapExceeded)`.
   Le bloc de code existant a l'interieur de `if (!isCapExceeded(...))` reste intact — il se retrouve dans le `else if`.

3. **Injecter stats increment** — APRES `await saveCaps(profile.id, incrementCap(caps, category.id));` (ligne ~228), dans le meme bloc `if (effectResult.effectApplied)` :
   ```typescript
   // Phase 22 COUPLING-05 : incrementer stats semaine (D-04b)
   try { await incrementWeekStat(category.id); } catch { /* stats — non-critical */ }
   ```

Le pattern est : overrides check gate l'entree, stats increment se fait apres l'effet applique avec succes.
  </action>
  <verify>
    <automated>cd /Users/gabrielwaltio/Documents/family-vault && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "loadOverrides" hooks/useGamification.ts
    - grep -q "isCategoryEnabled" hooks/useGamification.ts
    - grep -q "incrementWeekStat" hooks/useGamification.ts
    - grep -q "COUPLING-03" hooks/useGamification.ts
    - grep -q "COUPLING-05" hooks/useGamification.ts
    - npx tsc --noEmit (pas de nouvelle erreur)
  </acceptance_criteria>
  <done>completeTask filtre les categories desactivees et incremente les stats semaine</done>
</task>

<task type="auto">
  <name>Task 3: Ajouter cles i18n FR+EN pour l'ecran Couplage semantique</name>
  <files>locales/fr/common.json, locales/en/common.json</files>
  <read_first>
    - locales/fr/common.json (lignes 3030-3070 — structure settingsScreen.rows/modalTitles)
    - locales/en/common.json (meme zone — parite stricte)
  </read_first>
  <action>
Ajouter les cles i18n dans les deux fichiers JSON. Modifications dans 3 zones :

**Zone 1 — `settingsScreen.rows`** (ajouter apres `automationsSubtitle`) :
- FR: `"coupling": "Couplage semantique"`, `"couplingSubtitle": "10 categories, effets personnalisables"`
- EN: `"coupling": "Semantic coupling"`, `"couplingSubtitle": "10 categories, customizable effects"`

**Zone 2 — `settingsScreen.modalTitles`** (ajouter apres `automations`) :
- FR: `"coupling": "Couplage semantique"`
- EN: `"coupling": "Semantic coupling"`

**Zone 3 — Nouveau namespace `settings.coupling`** (ajouter dans l'objet `settings` existant ou creer `settings` au top-level s'il n'existe pas) :

FR:
```json
"coupling": {
  "masterTitle": "Couplage semantique",
  "masterSubtitle": "Les taches declenchent des effets ferme",
  "weekStatsTotal_zero": "Aucun effet cette semaine",
  "weekStatsTotal_one": "{{count}} effet cette semaine",
  "weekStatsTotal_other": "{{count}} effets cette semaine",
  "weekStatsCat_zero": "Jamais",
  "weekStatsCat_one": "{{count}} fois",
  "weekStatsCat_other": "{{count}} fois",
  "variantAmbient": "courant",
  "variantRare": "rare",
  "variantGolden": "epique",
  "disabledHint": "Activez le couplage pour personnaliser"
}
```

EN:
```json
"coupling": {
  "masterTitle": "Semantic coupling",
  "masterSubtitle": "Tasks trigger farm effects",
  "weekStatsTotal_zero": "No effects this week",
  "weekStatsTotal_one": "{{count}} effect this week",
  "weekStatsTotal_other": "{{count}} effects this week",
  "weekStatsCat_zero": "Never",
  "weekStatsCat_one": "{{count}} time",
  "weekStatsCat_other": "{{count}} times",
  "variantAmbient": "common",
  "variantRare": "rare",
  "variantGolden": "epic",
  "disabledHint": "Enable coupling to customize"
}
```

Note : les accents sont volontairement absents des valeurs JSON pour coherence encodage (ex: "semantique" et non "s\u00e9mantique"). Verifier si le fichier existant utilise des accents — si oui, les conserver.
  </action>
  <verify>
    <automated>cd /Users/gabrielwaltio/Documents/family-vault && grep -c "coupling" locales/fr/common.json && grep -c "coupling" locales/en/common.json</automated>
  </verify>
  <acceptance_criteria>
    - grep -q '"coupling"' locales/fr/common.json
    - grep -q '"coupling"' locales/en/common.json
    - grep -q "weekStatsTotal_other" locales/fr/common.json
    - grep -q "weekStatsTotal_other" locales/en/common.json
    - grep -q "variantGolden" locales/fr/common.json
    - grep -q "variantGolden" locales/en/common.json
    - grep -q "disabledHint" locales/fr/common.json
    - grep -q "disabledHint" locales/en/common.json
  </acceptance_criteria>
  <done>Cles i18n FR+EN ajoutees avec parite stricte — 14 cles coupling + 4 cles settingsScreen</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passe sans nouvelles erreurs
2. `lib/semantic/coupling-overrides.ts` exporte les 7 symbols requis
3. `hooks/useGamification.ts` contient la logique override + stats
4. Les deux fichiers locales ont les cles coupling en parite
</verification>

<success_criteria>
- coupling-overrides.ts cree avec cache module-level, loadOverrides, saveOverrides, isCategoryEnabled, loadWeekStats, incrementWeekStat
- useGamification.ts filtre les categories desactivees AVANT applyTaskEffect
- useGamification.ts incremente stats semaine APRES effet applique
- Cles i18n FR+EN en parite stricte (14 cles settings.coupling + 4 cles settingsScreen)
- tsc passe
</success_criteria>

<output>
After completion, create `.planning/phases/22-ui-config-famille/22-01-SUMMARY.md`
</output>
