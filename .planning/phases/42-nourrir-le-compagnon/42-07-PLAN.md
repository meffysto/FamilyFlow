---
phase: 42-nourrir-le-compagnon
plan: 07
type: execute
wave: 3
depends_on: [42-03, 42-05, 42-06]
files_modified:
  - app/(tabs)/tree.tsx
  - hooks/useVault.ts
autonomous: true
requirements: [FEED-13, FEED-14]
must_haves:
  truths:
    - "hooks/useVault.ts expose une nouvelle fonction feedCompanion(profileId, cropId, grade) dans le return (à côté de setCompanion)"
    - "feedCompanion utilise le même pattern que setCompanion (L1859-1874) : parseFarmProfile → mutate farmData → serializeFarmProfile → writeFile en UNE SEULE opération I/O"
    - "Dans la même opération, farmData.harvestInventory[cropId][grade] est décrémenté de 1 (pattern déjà présent L1776-1789 pour bonus saga)"
    - "Si la décrémentation tombe à 0, on laisse la clé grade à 0 (ne pas supprimer — cohérence avec harvest existant)"
    - "setProfiles update local après write réussi (pattern setCompanion L1868-1870)"
    - "tree.tsx wire CompanionCard ouvert depuis le chip cozy existant, FeedPicker accessible depuis CompanionCard ET tap long sur sprite (D-01)"
    - "Retour { applied, affinity, newBuff, cooldownMs } permet à tree.tsx de déclencher les animations/toasts"
  artifacts:
    - path: "app/(tabs)/tree.tsx"
      provides: "CompanionCard intégré + wiring FeedPicker + tap long sprite"
      contains: "CompanionCard"
    - path: "hooks/useVault.ts"
      provides: "Action feedCompanion exposée"
      contains: "feedCompanion"
  key_links:
    - from: "tree.tsx:handleFeedCrop"
      to: "useVault.feedCompanion"
      via: "mutation profile.companion + décrément harvestInventory, 1 seule écriture farm-{id}.md"
      pattern: "feedCompanion"
---

<objective>
Câbler CompanionCard et FeedPicker dans `tree.tsx`, ajouter l'action `feedCompanion` dans `hooks/useVault.ts` en **réutilisant le pattern setCompanion existant** (L1859-1874) pour garantir une SEULE écriture farm-{id}.md qui inclut à la fois la mutation `companion` et le décrément `harvestInventory`.

Purpose: D-01 (2 entry points : bouton + tap long), D-28 (CompanionCard remplace), D-29 (tap long), D-30 (persist via parser).

**Stratégie transaction résolue (Blocker 2 checker) :** `companion` et `harvestInventory` vivent dans le MÊME fichier `farm-{id}.md` (cf setCompanion L1858-1874 et bonus saga L1772-1790). Une seule écriture via `writeFile(fp, serializeFarmProfile(name, farmData))` → pas besoin de rollback multi-fichier. Si l'I/O échoue, `catch` silencieux (pattern existant) et rien n'est persisté — fail-open.

Output: boucle UX feed fonctionnelle end-to-end (picker → mutation vault → refresh UI).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/42-nourrir-le-compagnon/42-CONTEXT.md
@app/(tabs)/tree.tsx
@hooks/useVault.ts
@lib/mascot/companion-engine.ts
@components/mascot/CompanionCard.tsx
@components/mascot/FeedPicker.tsx
@components/mascot/CompanionSlot.tsx
</context>

<interfaces>
**Fonctions RÉELLES de hooks/useVault.ts identifiées :**

- `setCompanion(profileId, companion)` — L1859-1874. Pattern : `readFile → parseFarmProfile → mutate → serializeFarmProfile → writeFile → setProfiles`.
- `farmFile(profileId)` — L326. Retourne `farm-${profileId}.md`.
- `parseFarmProfile(content)` / `serializeFarmProfile(name, farmData)` — imports L67-68 depuis `../lib/parser`.
- `vaultRef.current.readFile(fp)` / `vaultRef.current.writeFile(fp, content)` — pattern VaultManager.
- `profiles` state + `setProfiles` — pour update local après écriture.
- Décrément harvestInventory : **pattern existe déjà L1776-1789** (opposé : increment). Adapter symétriquement.

**Nouvelle fonction à ajouter :**
```typescript
feedCompanion(
  profileId: string,
  cropId: string,
  grade: HarvestGrade,
): Promise<FeedResult | null>
```
- Retourne `null` si I/O échoue ou profil introuvable.
- Retourne `FeedResult` (du moteur pur) sinon : `{ applied, affinity, newBuff, cooldownMs, updated }`.
- Si `applied=false` (cooldown) : pas d'écriture, le résultat est juste propagé à l'UI.
- Si `applied=true` : écriture unique farm-{id}.md (companion + harvestInventory), puis setProfiles.
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Ajouter feedCompanion dans hooks/useVault.ts (à côté de setCompanion L1859)</name>
  <files>hooks/useVault.ts</files>
  <read_first>
    - hooks/useVault.ts L60-75 (imports : parseFarmProfile, serializeFarmProfile, CompanionData)
    - hooks/useVault.ts L290-300 (type du retour hook — vérifier où ajouter `feedCompanion`)
    - hooks/useVault.ts L320-330 (farmFile helper)
    - hooks/useVault.ts L1770-1800 (pattern increment harvestInventory — à inverser pour décrément)
    - hooks/useVault.ts L1856-1895 (setCompanion + unlockCompanion — pattern à imiter)
    - hooks/useVault.ts L2085-2140 (return destructuring — ajouter feedCompanion)
    - lib/mascot/companion-engine.ts (feedCompanion pur + FeedResult)
    - lib/parser.ts (serializeCompanion étendu via Plan 02)
    - CONTEXT.md D-30, D-31, D-15
  </read_first>
  <behavior>
    - feedCompanion(profileId, cropId, grade) appelle le moteur pur feedCompanion (Plan 03), renomé en import.
    - Si result.applied=false (cooldown) : retour direct sans I/O.
    - Si result.applied=true : lire farm-{id}.md, merger farmData.companion = result.updated, décrémenter harvestInventory[cropId][grade] de 1, écrire en UNE SEULE opération.
    - Après écriture réussie : setProfiles pour refléter companion + optionnellement harvest.
    - Catch silencieux : return null, __DEV__ warn.
  </behavior>
  <action>
    **1. Vérifier les imports en haut du fichier** (L60-75). Ajouter si absents :
    ```typescript
    import {
      feedCompanion as feedCompanionEngine,
      type FeedResult,
    } from '../lib/mascot/companion-engine';
    import type { HarvestGrade } from '../lib/mascot/companion-types';
    ```
    (Note : rename via `as` pour éviter collision avec la fonction exposée par le hook.)

    **2. Ajouter le type dans l'interface de retour du hook** (~L296 près de `setCompanion`) :
    ```typescript
    feedCompanion: (profileId: string, cropId: string, grade: HarvestGrade) => Promise<FeedResult | null>;
    ```

    **3. Ajouter la fonction juste après `unlockCompanion` (~L1894)** :
    ```typescript
    /**
     * Phase 42 — Nourrir le compagnon avec un crop de grade donné.
     *
     * Transaction single-file : companion + harvestInventory vivent dans farm-{id}.md,
     * donc une seule écriture suffit. Si I/O échoue, retour null (fail-open).
     *
     * @returns null si I/O échoue ou profil introuvable.
     * @returns FeedResult avec applied=false si cooldown actif (pas d'écriture).
     * @returns FeedResult avec applied=true + updated=nouveau CompanionData si succès.
     */
    const feedCompanion = useCallback(
      async (profileId: string, cropId: string, grade: HarvestGrade): Promise<FeedResult | null> => {
        if (!vaultRef.current) return null;
        const profile = profiles.find(p => p.id === profileId);
        if (!profile || !profile.companion) return null;
        // 1. Appliquer le feed pur (cooldown check inclus)
        const result = feedCompanionEngine(profile.companion, cropId, grade);
        if (!result.applied) return result; // cooldown — UI affiche message, pas d'I/O
        // 2. Lire + mutate farm-{id}.md (companion + harvestInventory) en une seule transaction
        try {
          const fp = farmFile(profileId);
          const farmContent = await vaultRef.current.readFile(fp).catch(() => '');
          const farmData = parseFarmProfile(farmContent);
          // 2a. Mutate companion
          farmData.companion = result.updated;
          // 2b. Décrémenter harvestInventory[cropId][grade] de 1 (pattern inverse L1776-1789)
          const harvestInv = { ...(farmData.harvestInventory ?? {}) };
          const existing = harvestInv[cropId];
          if (existing != null && typeof existing === 'object') {
            const currentQty = (existing as Record<HarvestGrade, number>)[grade] ?? 0;
            if (currentQty > 0) {
              harvestInv[cropId] = { ...existing, [grade]: currentQty - 1 };
              farmData.harvestInventory = harvestInv;
            }
          }
          // 3. Écriture unique
          const profileName = profile.name ?? profileId;
          await vaultRef.current.writeFile(fp, serializeFarmProfile(profileName, farmData));
          // 4. Update local state (pattern setCompanion L1868-1870)
          setProfiles(prev => prev.map(p =>
            p.id === profileId ? { ...p, companion: result.updated } : p
          ));
          return result;
        } catch (e) {
          if (__DEV__) console.warn('[feedCompanion]', e);
          return null;
        }
      },
      [profiles],
    );
    ```

    **4. Ajouter feedCompanion au return du hook** (~L2094 à côté de `setCompanion, unlockCompanion,`) :
    ```typescript
    return {
      // ... existing
      setCompanion,
      unlockCompanion,
      feedCompanion,
      // ... rest
    };
    ```

    **5. Vérifier l'absence de régression** :
    - Le pattern est strictement aligné sur setCompanion (L1859-1874) + bonus saga harvest (L1776-1789).
    - Le catch silencieux respecte le pattern fail-open du codebase.
    - setProfiles update local ne touche QUE `companion` (pas de harvest dans profiles state — harvest est lu via parseFarmProfile à la volée côté écrans, voir comment les autres écrans accèdent à harvestInventory).

    **Pitfall attention** : si `harvestInventory` est également caché dans le state local via un autre mécanisme (ex: `farmDataByProfile` state), il faut aussi mettre ce state à jour. Sinon, l'UI affichera un stock périmé. **Action de vérification à faire pendant l'implémentation** : `grep -n "harvestInventory\|farmDataByProfile\|setFarmData" hooks/useVault.ts | head -30` pour identifier tous les slots state où harvest pourrait être stocké. Si `farmDataByProfile` existe (vu ligne 1048), ajouter un setState pour refléter le décrément.
  </action>
  <verify>
    <automated>grep -q "const feedCompanion = useCallback" hooks/useVault.ts &amp;&amp; grep -q "feedCompanionEngine" hooks/useVault.ts &amp;&amp; grep -q "farmData.companion = result.updated" hooks/useVault.ts &amp;&amp; grep -q "harvestInv\[cropId\] = { ...existing, \[grade\]: currentQty - 1 }" hooks/useVault.ts &amp;&amp; grep -q "feedCompanion: (profileId: string" hooks/useVault.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "const feedCompanion = useCallback" hooks/useVault.ts` réussit
    - `grep -q "feedCompanionEngine" hooks/useVault.ts` réussit (import renommé)
    - `grep -q "HarvestGrade" hooks/useVault.ts` réussit (import type)
    - `grep -q "farmData.companion = result.updated" hooks/useVault.ts` réussit (mutation companion)
    - `grep -q "currentQty - 1" hooks/useVault.ts` réussit (décrément harvest)
    - `grep -c "writeFile(fp, serializeFarmProfile" hooks/useVault.ts` doit augmenter de 1 vs avant (une nouvelle écriture ajoutée, pas de duplication du pattern setCompanion)
    - `grep -q "feedCompanion," hooks/useVault.ts` dans le return block (export)
    - `npx tsc --noEmit` pas de NOUVELLE erreur dans useVault.ts (les erreurs pré-existantes restent tolérées)
    - **Transaction single-file** : dans la fonction feedCompanion, il n'y a QU'UN SEUL appel à `writeFile` (grep contextuel : `awk '/const feedCompanion/,/^  \], \[profiles\]\);/' hooks/useVault.ts | grep -c "writeFile"` retourne 1)
  </acceptance_criteria>
  <done>
    Action feedCompanion exposée par le hook, réutilise le pattern setCompanion + increment saga, en UNE SEULE écriture farm-{id}.md (companion + harvest). Retour FeedResult permet à l'UI de réagir (cooldown/applied/affinity).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Intégrer CompanionCard + FeedPicker dans tree.tsx avec tap long sur sprite</name>
  <files>app/(tabs)/tree.tsx</files>
  <read_first>
    - app/(tabs)/tree.tsx L70-90 (imports existants CompanionPicker, CompanionSlot)
    - app/(tabs)/tree.tsx L495-515 (state variables — showCompanionPicker etc.)
    - app/(tabs)/tree.tsx L2600-2620 (chip cozy companion qui ouvre CompanionPicker actuellement)
    - app/(tabs)/tree.tsx L2335-2360 (usage CompanionSlot — pour ajouter onLongPress)
    - components/mascot/CompanionSlot.tsx L1-100 (pour comprendre l'API sprite et la gesture handling)
    - components/mascot/CompanionCard.tsx (créé Plan 05 — CompanionPicker déjà intégré en interne)
    - components/mascot/FeedPicker.tsx (créé Plan 06)
    - CONTEXT.md D-01, D-28, D-29
  </read_first>
  <behavior>
    - Le chip existant qui ouvre `CompanionPicker` pour le switch est remplacé par un bouton ouvrant `CompanionCard` dans un modal pageSheet dédié
    - CompanionCard reçoit `companion`, `level`, `onPressFeed={() => setShowFeedPicker(true)}`, `onSelectSpecies={handleCompanionSelect}`
    - FeedPicker rendu en bas du JSX, reçoit `visible`, `onClose`, `inventory`, `companionSpecies`, `onPick={handleFeedCrop}`
    - Tap long sur sprite compagnon ouvre aussi FeedPicker directement (shortcut D-29)
    - handleFeedCrop(cropId, grade) appelle `feedCompanion(profile.id, cropId, grade)` du hook et gère le retour
    - CompanionPicker (L2936) reste utilisé POUR le choix initial (premier compagnon au niveau 5, L737 setShowCompanionPicker). On ne le supprime PAS.
  </behavior>
  <action>
    **1. Imports à ajouter** (en haut du fichier) :
    ```typescript
    import { CompanionCard } from '../../components/mascot/CompanionCard';
    import { FeedPicker } from '../../components/mascot/FeedPicker';
    import type { HarvestGrade } from '../../lib/mascot/companion-types';
    ```

    **2. Destructurer `feedCompanion` depuis `useVault()`** :
    Localiser le `useVault()` destructuring (probablement début du composant). Ajouter `feedCompanion` à la liste.

    **3. State supplémentaire** (à côté de `showCompanionPicker` L501) :
    ```typescript
    const [showCompanionCard, setShowCompanionCard] = useState(false);
    const [showFeedPicker, setShowFeedPicker] = useState(false);
    ```

    **4. Handler feed** (à côté des autres handlers ~L730-760) :
    ```typescript
    const handleFeedCrop = useCallback(
      async (cropId: string, grade: HarvestGrade) => {
        if (!profile?.id) return;
        const result = await feedCompanion(profile.id, cropId, grade);
        if (!result) return;
        if (!result.applied) {
          // Cooldown — feedback silencieux (le bouton est déjà disabled dans CompanionCard)
          return;
        }
        // applied=true : affinity détermine la réaction (anim Plan 08 s'en occupera ensuite)
        if (result.affinity === 'preferred') {
          Haptics.impactAsync(ImpactFeedbackStyle.Heavy).catch(() => {});
        } else {
          Haptics.impactAsync(ImpactFeedbackStyle.Light).catch(() => {});
        }
      },
      [profile?.id, feedCompanion],
    );
    ```

    **5. Remplacer le chip L2613** pour utiliser setShowCompanionCard (PAS setShowCompanionPicker — ce dernier reste pour le choix initial L737) :
    ```typescript
    <TouchableOpacity
      style={[styles.chipCozy, styles.chipCozyCompanion]}
      onPress={() => {
        Haptics.selectionAsync();
        // Phase 42 — si compagnon existant, ouvrir la CompanionCard (refonte).
        // Si pas de compagnon (cas rare), fallback CompanionPicker init.
        if (companion) setShowCompanionCard(true);
        else setShowCompanionPicker(true);
      }}
      activeOpacity={0.7}
    >
      {/* contenu existant du chip */}
    </TouchableOpacity>
    ```

    **6. Ajouter le modal CompanionCard + FeedPicker** (à côté du modal CompanionPicker existant) :
    ```typescript
    <Modal
      visible={showCompanionCard}
      animationType="slide"
      presentationStyle="pageSheet"
      onRequestClose={() => setShowCompanionCard(false)}
    >
      <SafeAreaView style={{ flex: 1, backgroundColor: colors.bg }}>
        <ModalHeader title="Compagnon" onClose={() => setShowCompanionCard(false)} />
        {companion && (
          <View style={{ padding: Spacing.lg }}>
            <CompanionCard
              companion={companion}
              level={level}
              onPressFeed={() => {
                setShowCompanionCard(false);
                // Gotcha pageSheet stacking (Phase 40 G1) : 300ms entre fermeture et ouverture suivante
                setTimeout(() => setShowFeedPicker(true), 300);
              }}
              onSelectSpecies={handleCompanionSelect}
            />
          </View>
        )}
      </SafeAreaView>
    </Modal>

    <FeedPicker
      visible={showFeedPicker}
      onClose={() => setShowFeedPicker(false)}
      inventory={harvestInventory || {}}
      companionSpecies={companion?.activeSpecies ?? 'chat'}
      onPick={handleFeedCrop}
    />
    ```

    **7. Tap long sur sprite compagnon (D-29)** — localiser l'usage de `CompanionSlot` L2351.
    - Si `CompanionSlot` accepte une prop `onLongPress` : l'ajouter directement :
      ```tsx
      <CompanionSlot
        /* ... */
        onLongPress={() => { Haptics.selectionAsync().catch(() => {}); setShowFeedPicker(true); }}
      />
      ```
    - Si `CompanionSlot` n'expose pas `onLongPress` : wrapper l'appel dans `Pressable onLongPress={() => setShowFeedPicker(true)}` (délai long press système par défaut).

    **IMPORTANT — Ne pas casser CompanionPicker existant** : le modal CompanionPicker pour le choix initial (L737 `setShowCompanionPicker(true)` via chooseInitialCompanion, L2936 rendu) reste EN PLACE. CompanionCard/Modal est un NOUVEAU chemin pour les compagnons déjà choisis.

    **Pitfalls résolus :**
    - `harvestInventory` — à récupérer depuis l'état (probablement via `farmDataByProfile[profileId].harvestInventory` ou via `useVault()` si exposé). Lire tree.tsx pour identifier le call site exact.
    - `feedCompanion` — destructuré depuis `useVault()` (ajouté Task 1).
    - Pattern stacking pageSheet iOS : setTimeout(300ms) entre fermeture d'un sheet et ouverture du suivant (précédent Phase 40 gotcha G1).
    - Import `Haptics, ImpactFeedbackStyle` depuis `expo-haptics` si pas déjà importés.
  </action>
  <verify>
    <automated>grep -q "CompanionCard" "app/(tabs)/tree.tsx" &amp;&amp; grep -q "FeedPicker" "app/(tabs)/tree.tsx" &amp;&amp; grep -q "handleFeedCrop" "app/(tabs)/tree.tsx" &amp;&amp; grep -q "setShowFeedPicker" "app/(tabs)/tree.tsx" &amp;&amp; grep -q "setShowCompanionCard" "app/(tabs)/tree.tsx" &amp;&amp; grep -q "feedCompanion" "app/(tabs)/tree.tsx" &amp;&amp; npx tsc --noEmit 2>&amp;1 | grep "tree.tsx" ; RC=$? ; test $RC -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "CompanionCard" "app/(tabs)/tree.tsx"` réussit
    - `grep -q "FeedPicker" "app/(tabs)/tree.tsx"` réussit
    - `grep -q "handleFeedCrop" "app/(tabs)/tree.tsx"` réussit
    - `grep -q "setShowFeedPicker" "app/(tabs)/tree.tsx"` réussit
    - `grep -q "setShowCompanionCard" "app/(tabs)/tree.tsx"` réussit
    - `grep -q "feedCompanion" "app/(tabs)/tree.tsx"` réussit (call site)
    - `grep -q "import.*CompanionPicker" "app/(tabs)/tree.tsx"` toujours présent (choix initial conservé)
    - `grep -q "setTimeout.*300" "app/(tabs)/tree.tsx"` (gotcha pageSheet stacking respecté)
    - `npx tsc --noEmit` pas de nouvelle erreur dans tree.tsx
  </acceptance_criteria>
  <done>
    tree.tsx rend CompanionCard en modal ouvert depuis le chip cozy (quand compagnon existant), FeedPicker accessible depuis CompanionCard ET tap long sur sprite. handleFeedCrop branché au hook feedCompanion. CompanionPicker conservé pour le choix initial.
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` sans nouvelle erreur
- Pas de régression : le chip cozy doit toujours fonctionner, les compagnons existants s'affichent via CompanionCard
- Transaction single-file vérifiée (un seul writeFile par feedCompanion)
- Test manuel fonctionnel différé aux Plans 08-09 (animation + message)
</verification>

<success_criteria>
- Commit FR : `feat(42): integration CompanionCard + FeedPicker tree.tsx + hook feedCompanion`
- Boucle complète : tap chip → CompanionCard → bouton Nourrir → FeedPicker → sélection crop → hook feedCompanion → lastFedAt + feedBuff + harvest-1 persistés en 1 écriture
</success_criteria>

<output>
.planning/phases/42-nourrir-le-compagnon/42-07-SUMMARY.md avec :
- Stratégie finale (modal pageSheet CompanionCard, transaction single-file)
- Approche tap long sur CompanionSlot (prop native vs Pressable wrapper)
- Confirmation absence de double-écriture
- farmDataByProfile state : mis à jour ou non (selon audit L1040-1060)
</output>
