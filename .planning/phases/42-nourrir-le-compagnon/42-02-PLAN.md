---
phase: 42-nourrir-le-compagnon
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/parser.ts
  - lib/vault-cache.ts
  - lib/__tests__/companion-parser.test.ts
autonomous: true
requirements: [FEED-04, FEED-05]
must_haves:
  truths:
    - "parseCompanion lit lastFedAt et feedBuff depuis la ligne CSV companion de farm-{id}.md"
    - "serializeCompanion écrit lastFedAt et feedBuff en CSV flat étendu (v3)"
    - "Round-trip lossless : parse(serialize(data)) === data pour un CompanionData avec feedBuff"
    - "CACHE_VERSION passé de 6 à 7 (shape CompanionData change)"
    - "Compagnons existants sans lastFedAt/feedBuff parsés sans erreur (backward compat v1/v2 D-16)"
    - "Edge cases ISO (sans secondes, avec millisecondes, Z vs +00:00) testés en Jest"
  artifacts:
    - path: "lib/parser.ts"
      provides: "parseCompanion/serializeCompanion étendus"
      contains: "lastFedAt"
    - path: "lib/vault-cache.ts"
      provides: "CACHE_VERSION bump"
      contains: "CACHE_VERSION = 7"
    - path: "lib/__tests__/companion-parser.test.ts"
      provides: "Tests round-trip + edge cases ISO"
  key_links:
    - from: "lib/parser.ts:parseCompanion"
      to: "CompanionData.lastFedAt + feedBuff"
      via: "format CSV étendu (v3)"
      pattern: "lastFedAt"
---

<objective>
Étendre le couple `parseCompanion`/`serializeCompanion` dans `lib/parser.ts` pour round-tripper les champs optionnels `lastFedAt` et `feedBuff`, bumper `CACHE_VERSION` à 7, et ajouter une suite Jest dédiée aux edge cases ISO.

Purpose: Couvrir D-17 (CACHE_VERSION bump), D-30 (parser étendu), D-31 (format sérialisation — on tranche **flat CSV étendu** avec justification + tests edge-case compensatoires).

**Décision format (D-31 Claude's Discretion résolu — Info 7 checker) :**
- Format retenu : **CSV flat v3** (extension du pattern v1/v2 existant)
- Rationale : cohérence avec pattern v1/v2 companion (ligne CSV unique en inline), `parseActiveExpeditions` utilise déjà le même pattern slice+join pour ISO avec `:`, pas d'introduction de nouvelle clé YAML dans le frontmatter farm (minimise diff parser). Alternative YAML dédiée (`companion_last_fed_at:` / `companion_feed_buff:`) considérée mais rejetée car elle fragmente le domaine companion sur 3 clés au lieu d'une, et nécessite modifs additionnelles dans `parseFarmProfile` pour regrouper.
- Mitigation fragilité : **suite Jest dédiée `lib/__tests__/companion-parser.test.ts`** couvrant edge-cases ISO (sans secondes, avec millisecondes, timezone offset, chaînes vides, format legacy).

Output: round-trip parseCompanion préservé + 2 nouveaux champs + suite Jest edge-cases.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/42-nourrir-le-compagnon/42-CONTEXT.md
@lib/parser.ts
@lib/vault-cache.ts
@CLAUDE.md
</context>

<interfaces>
Format CSV retenu (D-31 résolu — flat, pas de JSON imbriqué, pas de YAML dédié) :

```
activeSpecies:name:unlocked1|unlocked2:lastFedAtISO:feedBuffMul:feedBuffExpiresAtISO
```

- Parts 0-2 : inchangés (backward compat v1/v2)
- Part 3-5 : ISO lastFedAt reconstitué via slice (pattern parseActiveExpeditions — une ISO contient 2 `:`)
- Part 6 : `feedBuff.multiplier` numérique (ex: `1.1500`) ou vide si null/absent
- Part 7-9 : ISO feedBuff.expiresAt reconstitué via slice

Longueur effective :
- v1/v2 = 3 parts (pas de Phase 42)
- v3 avec lastFedAt seul, buff null = 8 parts (3 + 3 ISO + 2 vides)
- v3 complet = 10 parts

Ancien format 2-parts, 3-parts ou 4-parts legacy (mood) continue d'être parsé sans erreur (backward compat D-16).
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Étendre parseCompanion/serializeCompanion pour lastFedAt + feedBuff</name>
  <files>lib/parser.ts</files>
  <read_first>
    - lib/parser.ts L535-570 (parseCompanion + serializeCompanion actuels)
    - lib/parser.ts L570-605 (parseActiveExpeditions — pattern de gestion ISO avec ':' à reproduire)
    - lib/mascot/companion-types.ts (après Plan 01 — shape CompanionData étendue + type FeedBuff)
    - .planning/phases/42-nourrir-le-compagnon/42-CONTEXT.md D-30, D-31
  </read_first>
  <behavior>
    - parseCompanion("chat:Mimi:chat") retourne { activeSpecies: 'chat', name: 'Mimi', unlockedSpecies: ['chat'] } (backward compat — pas de champs Phase 42)
    - parseCompanion avec 8 parts (v3 lastFedAt seul) retourne objet avec lastFedAt défini, feedBuff absent
    - parseCompanion avec 10 parts (v3 full) retourne feedBuff complet
    - Round-trip: parse(serialize(data)) === data pour un objet avec feedBuff
    - parseCompanion(undefined) retourne null (inchangé)
    - parseCompanion("") retourne null (inchangé)
  </behavior>
  <action>
    Remplacer les corps de `parseCompanion` (L544-557) et `serializeCompanion` (L563-566) dans `lib/parser.ts`.

    **Import à ajouter en haut** (si absent du fichier — vérifier ligne 63) :
    ```typescript
    import type { CompanionData, CompanionSpecies, FeedBuff } from './mascot/companion-types';
    ```

    **parseCompanion — nouvelle version** :
    ```typescript
    /**
     * Parse le champ companion d'un profil farm-{id}.md.
     * Format v3 (Phase 42) : "activeSpecies:name:unlocked1|unlocked2:lastFedAtISO:feedBuffMul:feedBuffExpiresAtISO"
     * Format v2 (Phase 20) : "activeSpecies:name:unlocked1|unlocked2"
     * Format v1 legacy     : "activeSpecies:name:unlocked1|unlocked2:mood" — la 4e partie (mood) est ignorée si non-ISO.
     * Les parts 3 (lastFedAt) et 5 (feedBuff.expiresAt) sont des ISO → reconstitution via slice+join (pattern parseActiveExpeditions).
     */
    export function parseCompanion(raw: string | undefined): CompanionData | null {
      if (!raw || !raw.trim()) return null;
      const parts = raw.trim().split(':');
      if (parts.length < 2) return null;
      const activeSpecies = parts[0] as CompanionSpecies;
      const name = parts[1] || '';
      const unlockedRaw = parts[2] || activeSpecies;
      const base: CompanionData = {
        activeSpecies,
        name,
        unlockedSpecies: unlockedRaw.split('|') as CompanionSpecies[],
      };
      // Phase 42 — v3 minimum 8 parts (3 base + 3 ISO + 2 vides). Gate exclut v1 4-parts legacy (mood).
      if (parts.length >= 8) {
        const lastFedCandidate = parts.slice(3, 6).join(':');
        const lastFedDate = new Date(lastFedCandidate);
        if (!isNaN(lastFedDate.getTime())) {
          base.lastFedAt = lastFedCandidate;
        }
        const mulRaw = parts[6] || '';
        const mul = parseFloat(mulRaw);
        if (!isNaN(mul) && mul > 0 && parts.length >= 10) {
          const expiresCandidate = parts.slice(7, 10).join(':');
          const expDate = new Date(expiresCandidate);
          if (!isNaN(expDate.getTime())) {
            base.feedBuff = { multiplier: mul, expiresAt: expiresCandidate };
          }
        }
      }
      return base;
    }
    ```

    **serializeCompanion — nouvelle version** :
    ```typescript
    /**
     * Sérialise un CompanionData en chaîne CSV pour farm-{id}.md.
     * Format v3 : "activeSpecies:name:unlocked:lastFedAt:feedBuffMul:feedBuffExpiresAt"
     * Champs Phase 42 omis si absents → format v2 compatible pour compagnons sans feed.
     * toFixed(4) pour multiplicateur (évite imprécision flottante).
     */
    export function serializeCompanion(data: CompanionData): string {
      const unlocked = data.unlockedSpecies.join('|');
      const base = `${data.activeSpecies}:${data.name}:${unlocked}`;
      const hasFeedData = data.lastFedAt || data.feedBuff;
      if (!hasFeedData) return base;
      const lastFed = data.lastFedAt || '';
      const buffMul = data.feedBuff ? data.feedBuff.multiplier.toFixed(4) : '';
      const buffExp = data.feedBuff ? data.feedBuff.expiresAt : '';
      return `${base}:${lastFed}:${buffMul}:${buffExp}`;
    }
    ```

    **Pitfalls à éviter** :
    - Ne PAS casser le format v1/v2 legacy (parts.length 2, 3, 4 possibles). Le test `parts.length >= 8` garantit qu'on ne confond pas avec l'ancien format mood 4-parts.
    - ISO reconstitution via `slice(start, end).join(':')` (même pattern que parseActiveExpeditions).
    - `toFixed(4)` pour la sérialisation du multiplicateur évite les flottants imprécis (1.495 stocké exact).
    - Si le nom compagnon contient un `:`, le parsing actuel CSV casse — limitation héritée v1/v2, non régressée ici (nom validé 20 chars alphanumériques côté CompanionPicker).
  </action>
  <verify>
    <automated>grep -q "lastFedAt" lib/parser.ts &amp;&amp; grep -q "feedBuff" lib/parser.ts &amp;&amp; grep -q "toFixed(4)" lib/parser.ts &amp;&amp; grep -q "parts.length >= 8" lib/parser.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "lastFedAt" lib/parser.ts` réussit
    - `grep -q "feedBuff" lib/parser.ts` réussit
    - `grep -q "parts.length >= 8" lib/parser.ts` réussit (test backward compat)
    - `grep -q "toFixed(4)" lib/parser.ts` réussit
    - `npx tsc --noEmit` n'introduit PAS de nouvelle erreur dans parser.ts
  </acceptance_criteria>
  <done>
    parseCompanion et serializeCompanion étendus en round-trip lossless. Backward compat v1/v2 préservée.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Bumper CACHE_VERSION 6 → 7</name>
  <files>lib/vault-cache.ts</files>
  <read_first>
    - lib/vault-cache.ts L30-60 (CACHE_VERSION actuel et CACHE_FILE_URI)
    - CLAUDE.md section Cache (règle de bump)
  </read_first>
  <behavior>
    - CACHE_VERSION passe de 6 à 7
    - CACHE_FILE_URI passe de `vault-cache-v6.json` à `vault-cache-v7.json`
    - Commentaire historique préservé + ligne v7 ajoutée
  </behavior>
  <action>
    Modifier `lib/vault-cache.ts` L45-50 :

    **Avant** :
    ```typescript
    // v5 : Phase 40 — shape WagerModifier étendu ...
    // v6 : Phase B grades (260421-obd) — HarvestInventory shape change (cropId → grade → qty)
    //                + CraftedItem.grade optional
    const CACHE_VERSION = 6;
    const CACHE_FILE_URI = FileSystem.documentDirectory + 'vault-cache-v6.json';
    ```

    **Après** :
    ```typescript
    // v5 : Phase 40 — shape WagerModifier étendu ...
    // v6 : Phase B grades (260421-obd) — HarvestInventory shape change (cropId → grade → qty)
    //                + CraftedItem.grade optional
    // v7 : Phase 42 — CompanionData étendu (lastFedAt?: string + feedBuff?: FeedBuff | null)
    const CACHE_VERSION = 7;
    const CACHE_FILE_URI = FileSystem.documentDirectory + 'vault-cache-v7.json';
    ```

    Vérifier qu'aucune autre référence hardcoded à `'vault-cache-v6'` n'existe ailleurs.
  </action>
  <verify>
    <automated>grep -q "CACHE_VERSION = 7" lib/vault-cache.ts &amp;&amp; grep -q "vault-cache-v7.json" lib/vault-cache.ts &amp;&amp; grep -q "Phase 42 — CompanionData" lib/vault-cache.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "CACHE_VERSION = 7" lib/vault-cache.ts` réussit
    - `grep -q "vault-cache-v7.json" lib/vault-cache.ts` réussit
    - `grep -q "v7 : Phase 42" lib/vault-cache.ts` réussit
  </acceptance_criteria>
  <done>
    CACHE_VERSION bumpé à 7. Commentaire FR ajouté. Pas de référence orpheline au v6 dans le code source.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Suite Jest companion-parser.test.ts — round-trip + edge cases ISO</name>
  <files>lib/__tests__/companion-parser.test.ts</files>
  <read_first>
    - lib/__tests__ (pattern existant)
    - lib/parser.ts (parseCompanion + serializeCompanion après Task 1)
  </read_first>
  <behavior>
    - Round-trip lossless : parse(serialize(data)) === data pour 3 shapes (v2 sans phase42, v3 lastFedAt seul, v3 full)
    - Backward compat : parseCompanion de format v1 4-parts (mood legacy) retourne { activeSpecies, name, unlockedSpecies } sans crasher
    - ISO edge cases : lastFedAt/expiresAt avec secondes, millisecondes, Z, +02:00 timezone → tous round-trip
    - Multiplicateur edge : 1.0500 et 1.4950 round-trip exactement (toFixed(4))
    - Chaînes corrompues : parseCompanion(':::::') retourne null ou objet minimal sans crasher
  </behavior>
  <action>
    Créer `lib/__tests__/companion-parser.test.ts` :

    ```typescript
    import { parseCompanion, serializeCompanion } from '../parser';
    import type { CompanionData } from '../mascot/companion-types';

    describe('Phase 42 — parseCompanion / serializeCompanion', () => {
      describe('Backward compat v1/v2', () => {
        it('format v2 3-parts (pas Phase 42) → pas de lastFedAt/feedBuff', () => {
          const d = parseCompanion('chat:Mimi:chat|chien');
          expect(d).not.toBeNull();
          expect(d!.activeSpecies).toBe('chat');
          expect(d!.name).toBe('Mimi');
          expect(d!.unlockedSpecies).toEqual(['chat', 'chien']);
          expect(d!.lastFedAt).toBeUndefined();
          expect(d!.feedBuff).toBeUndefined();
        });
        it('format v1 4-parts legacy (mood) → ignoré, pas de Phase 42', () => {
          const d = parseCompanion('chat:Mimi:chat:joyeux');
          expect(d).not.toBeNull();
          expect(d!.lastFedAt).toBeUndefined();
        });
        it('empty string → null', () => {
          expect(parseCompanion('')).toBeNull();
          expect(parseCompanion(undefined)).toBeNull();
        });
      });

      describe('Round-trip v3', () => {
        it('lastFedAt seul (buff null)', () => {
          const data: CompanionData = {
            activeSpecies: 'renard',
            name: 'Foxie',
            unlockedSpecies: ['renard'],
            lastFedAt: '2026-04-22T10:00:00.000Z',
          };
          const s = serializeCompanion(data);
          const parsed = parseCompanion(s);
          expect(parsed!.lastFedAt).toBe('2026-04-22T10:00:00.000Z');
          expect(parsed!.feedBuff).toBeUndefined();
        });
        it('lastFedAt + feedBuff complet', () => {
          const data: CompanionData = {
            activeSpecies: 'chat',
            name: 'Mimi',
            unlockedSpecies: ['chat'],
            lastFedAt: '2026-04-22T10:00:00.000Z',
            feedBuff: { multiplier: 1.495, expiresAt: '2026-04-22T11:30:00.000Z' },
          };
          const s = serializeCompanion(data);
          const parsed = parseCompanion(s);
          expect(parsed!.lastFedAt).toBe(data.lastFedAt);
          expect(parsed!.feedBuff!.multiplier).toBeCloseTo(1.495, 3);
          expect(parsed!.feedBuff!.expiresAt).toBe(data.feedBuff!.expiresAt);
        });
      });

      describe('Edge cases ISO', () => {
        it('ISO sans millisecondes round-trip', () => {
          const data: CompanionData = {
            activeSpecies: 'chien',
            name: 'Rex',
            unlockedSpecies: ['chien'],
            lastFedAt: '2026-04-22T10:00:00Z',
          };
          const s = serializeCompanion(data);
          const parsed = parseCompanion(s);
          expect(parsed!.lastFedAt).toBe('2026-04-22T10:00:00Z');
        });
        it('ISO timezone offset +02:00 round-trip', () => {
          const data: CompanionData = {
            activeSpecies: 'lapin',
            name: 'Hop',
            unlockedSpecies: ['lapin'],
            lastFedAt: '2026-04-22T10:00:00+02:00',
          };
          const s = serializeCompanion(data);
          const parsed = parseCompanion(s);
          expect(parsed!.lastFedAt).toBe('2026-04-22T10:00:00+02:00');
        });
        it('Multiplicateur 1.0500 toFixed(4) stable', () => {
          const data: CompanionData = {
            activeSpecies: 'chat',
            name: 'Mimi',
            unlockedSpecies: ['chat'],
            lastFedAt: '2026-04-22T10:00:00.000Z',
            feedBuff: { multiplier: 1.05, expiresAt: '2026-04-22T10:30:00.000Z' },
          };
          const s = serializeCompanion(data);
          expect(s).toContain('1.0500');
          const parsed = parseCompanion(s);
          expect(parsed!.feedBuff!.multiplier).toBeCloseTo(1.05, 4);
        });
      });

      describe('Corruption tolérée', () => {
        it('chaîne dégénérée ne crash pas', () => {
          expect(() => parseCompanion(':::::')).not.toThrow();
          expect(() => parseCompanion('chat::::::::::')).not.toThrow();
        });
      });
    });
    ```

    Lancer `npx jest lib/__tests__/companion-parser.test.ts --no-coverage` — tous les tests doivent passer.
  </action>
  <verify>
    <automated>test -f lib/__tests__/companion-parser.test.ts &amp;&amp; grep -c "it(" lib/__tests__/companion-parser.test.ts | awk '{ exit !($1 >= 9) }' &amp;&amp; npx jest lib/__tests__/companion-parser.test.ts --no-coverage 2>&amp;1 | tail -5 | grep -q "passed"</automated>
  </verify>
  <acceptance_criteria>
    - Fichier existe, ≥9 tests (it blocks)
    - `npx jest lib/__tests__/companion-parser.test.ts --no-coverage` exit 0
    - Edge cases ISO sans ms, avec timezone, toFixed stable tous couverts
  </acceptance_criteria>
  <done>
    Suite Jest valide round-trip CSV flat v3 et mitige fragilité de sérialisation positional via edge-case coverage.
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` ne dégrade pas la liste d'erreurs pré-existantes
- Jest companion-parser vert
- Aucune référence v6 résiduelle hors .planning/
</verification>

<success_criteria>
- Round-trip lossless vérifié par Jest
- Commit FR : `feat(42): parser companion feedBuff v3 + tests edge-cases + cache v7`
</success_criteria>

<output>
.planning/phases/42-nourrir-le-compagnon/42-02-SUMMARY.md avec :
- Format CSV final retenu (flat v3) — rationale + alternative YAML rejetée documentée
- Liste edge cases couverts
- Confirmation CACHE_VERSION = 7
</output>
