---
phase: 42-nourrir-le-compagnon
plan: 04
type: execute
wave: 2
depends_on: [42-01, 42-03]
files_modified:
  - lib/mascot/companion-engine.ts
  - hooks/useGamification.ts
  - lib/__tests__/companion-feed.test.ts
autonomous: true
requirements: [FEED-08]
must_haves:
  truths:
    - "getCompanionXpBonus accepte un 2e paramètre optionnel nowMs et multiplie COMPANION_XP_BONUS par feedBuff actif (si présent)"
    - "XP calculé dans useGamification.ts intègre le feedBuff : base × 1.05 × feedBuff.multiplier"
    - "Si feedBuff expiré, le bonus retombe à 1.05 (comportement pré-Phase 42)"
    - "Si aucun compagnon, bonus 1.0 (inchangé)"
    - "Refactor L121-145 useGamification.ts : remplacer la condition `!== 1.0` par `> 1.0` (clarté, même résultat pour 1.05 et 1.2075+)"
    - "Tests Jest confirment stacking multiplicatif"
  artifacts:
    - path: "lib/mascot/companion-engine.ts"
      provides: "getCompanionXpBonus étendu avec feedBuff stacking"
      contains: "feedBuff"
  key_links:
    - from: "hooks/useGamification.ts:122 (getCompanionXpBonus call)"
      to: "calcul XP finalisé"
      via: "empilage multiplicatif D-07"
      pattern: "getCompanionXpBonus"
---

<objective>
Étendre `getCompanionXpBonus()` pour empiler le `feedBuff` actif sur le bonus compagnon de base (1.05). Refactorer le site d'appel dans `hooks/useGamification.ts` pour clarifier l'application (condition `> 1.0` plus lisible, résultat inchangé pour 1.05 existant, correctement appliqué pour buffs 1.2075+). Couvrir par tests Jest supplémentaires.

Purpose: D-07 (empilage multiplicatif), D-08 (buff XP uniquement — pas les feuilles).

**Audit L115-145 (Warning 3 checker résolu) :** le code actuel à L122-145 applique bien `companionBonus` aux points (voir L139 : `bonusPoints = Math.round(basePointsGained * companionBonus) - basePointsGained`). Le bonus est additionné correctement. MAIS :
- Le gate `companionBonus !== 1.0` (L123, L139) fonctionne pour 1.05 actuel, et continuera pour 1.2075+.
- Issue de clarté : `!== 1.0` est ambigu si companionBonus devenait <1.0 un jour. Refactor en `> 1.0` pour intention explicite (bonus multiplicatif uniquement).
- Issue de correctness : L123-125 crée `profileWithCompanionBonus` avec `points: Math.round(profileWithStreak.points)` — ce n'est PAS un boost, juste un round. Le vrai boost s'applique à L139-142 sur le delta. Conserver ce pattern (non-bug) mais ajouter commentaire clarifiant.

Output: 1 fonction étendue + 1 refactor clarifiant + tests stacking.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/42-nourrir-le-compagnon/42-CONTEXT.md
@lib/mascot/companion-engine.ts
@hooks/useGamification.ts
@lib/__tests__/companion-feed.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Étendre getCompanionXpBonus avec feedBuff stacking + tests</name>
  <files>lib/mascot/companion-engine.ts, lib/__tests__/companion-feed.test.ts</files>
  <read_first>
    - lib/mascot/companion-engine.ts L99-106 (getCompanionXpBonus actuel)
    - lib/__tests__/companion-feed.test.ts (suite créée Plan 03)
    - CONTEXT.md D-07, D-08
  </read_first>
  <behavior>
    - getCompanionXpBonus(null) retourne 1.0 (inchangé)
    - getCompanionXpBonus(companion sans feedBuff) retourne 1.05
    - getCompanionXpBonus(companion avec feedBuff actif mul 1.15) retourne 1.05 × 1.15 = 1.2075
    - getCompanionXpBonus(companion avec feedBuff expiré) retourne 1.05 (expiration lazy)
    - getCompanionXpBonus(companion avec feedBuff futur mul 1.495) retourne 1.05 × 1.495 ≈ 1.56975
  </behavior>
  <action>
    **Dans `lib/mascot/companion-engine.ts`**, remplacer `getCompanionXpBonus` (L99-106) :

    ```typescript
    /**
     * Retourne le multiplicateur de bonus XP du compagnon.
     * — Base : +5% si un compagnon est actif (COMPANION_XP_BONUS)
     * — Phase 42 : empilage multiplicatif avec feedBuff actif (D-07)
     *   XP final = base × 1.05 × feedBuff.multiplier (si buff actif)
     */
    export function getCompanionXpBonus(
      companion: CompanionData | null | undefined,
      nowMs: number = Date.now(),
    ): number {
      if (!companion) return 1.0;
      const base = COMPANION_XP_BONUS;
      const activeBuff = getActiveFeedBuff(companion, nowMs);
      if (!activeBuff) return base;
      return +(base * activeBuff.multiplier).toFixed(4);
    }
    ```

    **Ajouter à `lib/__tests__/companion-feed.test.ts` (nouvelle describe block en bas)** :

    ```typescript
    import { getCompanionXpBonus } from '../mascot/companion-engine';

    describe('Phase 42 — getCompanionXpBonus stacking', () => {
      it('null → 1.0', () => {
        expect(getCompanionXpBonus(null)).toBe(1.0);
      });
      it('pas de feedBuff → 1.05', () => {
        expect(getCompanionXpBonus(baseCompanion())).toBeCloseTo(1.05, 3);
      });
      it('feedBuff actif mul 1.15 → 1.05 × 1.15', () => {
        const future = new Date(Date.now() + 3600_000).toISOString();
        const comp: CompanionData = {
          ...baseCompanion(),
          feedBuff: { multiplier: 1.15, expiresAt: future },
        };
        expect(getCompanionXpBonus(comp)).toBeCloseTo(1.05 * 1.15, 3);
      });
      it('feedBuff expiré → 1.05 (expiration lazy)', () => {
        const past = new Date(Date.now() - 60_000).toISOString();
        const comp: CompanionData = {
          ...baseCompanion(),
          feedBuff: { multiplier: 1.15, expiresAt: past },
        };
        expect(getCompanionXpBonus(comp)).toBeCloseTo(1.05, 3);
      });
      it('stacking max (preferred + perfect) → 1.05 × 1.495', () => {
        const future = new Date(Date.now() + 3600_000).toISOString();
        const comp: CompanionData = {
          ...baseCompanion(),
          feedBuff: { multiplier: 1.15 * 1.3, expiresAt: future },
        };
        expect(getCompanionXpBonus(comp)).toBeCloseTo(1.05 * 1.15 * 1.3, 3);
      });
    });
    ```

    Lancer `npx jest lib/__tests__/companion-feed.test.ts --no-coverage` — tous les tests doivent passer.
  </action>
  <verify>
    <automated>grep -q "getActiveFeedBuff(companion, nowMs)" lib/mascot/companion-engine.ts &amp;&amp; grep -q "getCompanionXpBonus stacking" lib/__tests__/companion-feed.test.ts &amp;&amp; npx jest lib/__tests__/companion-feed.test.ts --no-coverage 2>&amp;1 | tail -5 | grep -q "passed"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "nowMs: number = Date.now" lib/mascot/companion-engine.ts` confirmé dans getCompanionXpBonus
    - `grep -c "it(" lib/__tests__/companion-feed.test.ts` retourne ≥17
    - `npx jest lib/__tests__/companion-feed.test.ts --no-coverage` exit 0
    - `npx tsc --noEmit` pas de nouvelle erreur
  </acceptance_criteria>
  <done>
    getCompanionXpBonus intègre feedBuff actif multiplicativement. Tests stacking verts.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Refactor useGamification.ts L121-145 — condition `> 1.0` + commentaire Phase 42</name>
  <files>hooks/useGamification.ts</files>
  <read_first>
    - hooks/useGamification.ts L110-160 (site complet completeTask)
    - CONTEXT.md D-07, D-08
  </read_first>
  <behavior>
    Before (actuel L121-145) :
    - L122 : `companionBonus = getCompanionXpBonus(profileWithStreak.companion)` → 1.0 ou 1.05
    - L123-125 : `profileWithCompanionBonus` créé via round (non-boost, juste rounding sur existing points)
    - L139 : `bonusPoints = companionBonus !== 1.0 ? Math.round(basePointsGained * companionBonus) - basePointsGained : 0`
    - L140-142 : ajout du bonusPoints aux points gagnés

    After (Phase 42 refactor) :
    - Commentaire Phase 42 au-dessus de L122 explicitant empilage feedBuff
    - L123 : condition passée de `!== 1.0` à `> 1.0` (clarté + safety si 1.0 exact)
    - L139 : même substitution `> 1.0`
    - Comportement inchangé pour companionBonus ∈ {1.0, 1.05} → même résultat que l'actuel
    - Nouveau comportement pour companionBonus > 1.05 (feedBuff actif) → bonus correctement appliqué
  </behavior>
  <action>
    Modifier `hooks/useGamification.ts` L120-145.

    **Avant** (L120-145) :
    ```typescript
    // Calculate new points (uses updated streak for bonus)
    // Bonus compagnon +5% XP (per D-09)
    const companionBonus = getCompanionXpBonus(profileWithStreak.companion);
    let profileWithCompanionBonus: Profile = companionBonus !== 1.0
      ? { ...profileWithStreak, points: Math.round(profileWithStreak.points) }
      : profileWithStreak;
    // ...
    const { profile: updatedProfileRaw, entry: entryRaw, lootAwarded } = awardTaskCompletion(profileWithCompanionBonus, taskText);
    const basePointsGained = updatedProfileRaw.points - profileWithCompanionBonus.points;
    const bonusPoints = companionBonus !== 1.0 ? Math.round(basePointsGained * companionBonus) - basePointsGained : 0;
    ```

    **Après** (même logique, conditions clarifiées + commentaire Phase 42) :
    ```typescript
    // Calculate new points (uses updated streak for bonus)
    // Phase 42 — getCompanionXpBonus intègre le feedBuff actif (empilage multiplicatif D-07).
    //   - pas de compagnon : 1.0
    //   - compagnon sans buff : COMPANION_XP_BONUS = 1.05
    //   - compagnon + buff actif : 1.05 × feedBuff.multiplier (ex: 1.2075 ou 1.56975)
    const companionBonus = getCompanionXpBonus(profileWithStreak.companion);
    let profileWithCompanionBonus: Profile = companionBonus > 1.0
      ? { ...profileWithStreak, points: Math.round(profileWithStreak.points) }
      : profileWithStreak;
    // ...
    const { profile: updatedProfileRaw, entry: entryRaw, lootAwarded } = awardTaskCompletion(profileWithCompanionBonus, taskText);
    // Appliquer le bonus compagnon (base 1.05 ± feedBuff Phase 42) sur le delta de points gagnés.
    const basePointsGained = updatedProfileRaw.points - profileWithCompanionBonus.points;
    const bonusPoints = companionBonus > 1.0 ? Math.round(basePointsGained * companionBonus) - basePointsGained : 0;
    ```

    **Vérification post-refactor** : exécuter `grep -n "getCompanionXpBonus" hooks/useGamification.ts lib/` et confirmer qu'il n'y a qu'un seul site d'appel (completeTask). Si d'autres sites existent (ex: routines, défis dans un autre hook), auditer et appliquer le même pattern.

    **Non-cassant** : pour companion sans feedBuff (companionBonus === 1.05), `1.05 > 1.0` est true, donc le bonus 5% continue d'être appliqué comme avant. Pour companion avec feedBuff (ex 1.2075), le bonus correspondant est correctement appliqué. Pour null companion (1.0), rien n'est appliqué.

    **Commit inclut** : commentaire Phase 42 explicatif + refactor condition `!== 1.0` → `> 1.0` aux 2 emplacements (L123 + L139).
  </action>
  <verify>
    <automated>grep -q "Phase 42" hooks/useGamification.ts &amp;&amp; grep -q "companionBonus > 1.0" hooks/useGamification.ts &amp;&amp; ! grep -q "companionBonus !== 1.0" hooks/useGamification.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "Phase 42" hooks/useGamification.ts` réussit (commentaire ajouté L120-123)
    - `grep -q "companionBonus > 1.0" hooks/useGamification.ts` réussit (au moins 2 occurrences)
    - `! grep -q "companionBonus !== 1.0" hooks/useGamification.ts` (ancien pattern supprimé)
    - `npx tsc --noEmit` pas de nouvelle erreur dans useGamification.ts
    - Grep `getCompanionXpBonus` dans hooks/ et lib/ : nombre de sites d'appel audité et documenté en SUMMARY
  </acceptance_criteria>
  <done>
    Site d'appel `getCompanionXpBonus` profite du feedBuff automatiquement via le refactor. Condition clarifiée (`> 1.0`). Commentaire Phase 42 en place. Aucune régression (1.05 continue d'être appliqué comme avant).
  </done>
</task>

</tasks>

<verification>
- Jest vert (≥17 tests)
- tsc vert
- `grep "companionBonus !== 1.0" hooks/` retourne vide (ancien pattern supprimé)
- Aucun autre site d'appel `getCompanionXpBonus` ne doit être cassé
</verification>

<success_criteria>
- Commit FR : `feat(42): empilage feedBuff dans calcul XP compagnon`
- feedBuff ×1.15 × COMPANION_XP_BONUS = +20.75% XP effectif observable en runtime
</success_criteria>

<output>
.planning/phases/42-nourrir-le-compagnon/42-04-SUMMARY.md avec :
- Sites d'appel `getCompanionXpBonus` audités (nombre + emplacements)
- Before/After du refactor condition
- Résultats jest (total tests, pass/fail)
</output>
