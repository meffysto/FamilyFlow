---
phase: 42-nourrir-le-compagnon
plan: 03
type: execute
wave: 2
depends_on: [42-01, 42-02]
files_modified:
  - lib/mascot/companion-engine.ts
  - lib/__tests__/companion-feed.test.ts
autonomous: true
requirements: [FEED-06, FEED-07]
must_haves:
  truths:
    - "feedCompanion(companion, cropId, grade) retourne { updated: CompanionData, affinity: CropAffinity, applied: boolean } sans muter l'argument"
    - "feedCompanion refuse si cooldown actif (retourne applied: false, updated inchangé sauf expiration lazy)"
    - "feedCompanion applique lastFedAt + feedBuff (ou pas de buff si hated) si cooldown expiré"
    - "Expiration lazy : getActiveFeedBuff nettoie un feedBuff expiré (retourne null) sans écrire"
    - "Suite Jest ≥10 tests passe (companion-feed.test.ts)"
  artifacts:
    - path: "lib/mascot/companion-engine.ts"
      provides: "feedCompanion + getActiveFeedBuff"
      contains: "export function feedCompanion"
    - path: "lib/__tests__/companion-feed.test.ts"
      provides: "Couverture feedCompanion + getBuffForCrop + isBuffActive + getCooldownRemainingMs"
  key_links:
    - from: "feedCompanion"
      to: "getBuffForCrop (Plan 01)"
      via: "import depuis companion-types"
      pattern: "getBuffForCrop"
---

<objective>
Ajouter au moteur compagnon la fonction pure `feedCompanion()` qui applique un nourrissage (update lastFedAt + feedBuff) en respectant le cooldown et l'affinité. Ajouter `getActiveFeedBuff()` helper de lecture non-mutatif (expiration lazy). Couvrir par tests Jest exhaustifs.

Purpose: D-06, D-07, D-09, D-10, D-12. Moteur pur testable isolément avant tout câblage hook.
Output: 2 fonctions pures exportées + suite de tests verte.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/42-nourrir-le-compagnon/42-CONTEXT.md
@lib/mascot/companion-engine.ts
@lib/mascot/companion-types.ts
@lib/__tests__
</context>

<interfaces>
```typescript
// À ajouter dans lib/mascot/companion-engine.ts
import type { CompanionData, FeedBuff, HarvestGrade, CropAffinity } from './companion-types';
import {
  getBuffForCrop,
  getAffinity,
  isBuffActive,
  getCooldownRemainingMs,
} from './companion-types';

export interface FeedResult {
  /** État updated du compagnon (nouvel objet, pas de mutation) */
  updated: CompanionData;
  /** Affinité évaluée (preferred/neutral/hated) pour choisir animations/messages */
  affinity: CropAffinity;
  /** true si le feed a été appliqué, false si cooldown actif */
  applied: boolean;
  /** Nouveau buff appliqué (null si hated ou cooldown actif) */
  newBuff: FeedBuff | null;
  /** ms restantes avant prochain feed (0 si applied=true) */
  cooldownMs: number;
}

export function feedCompanion(
  companion: CompanionData,
  cropId: string,
  grade: HarvestGrade,
  nowMs?: number,
): FeedResult;

export function getActiveFeedBuff(
  companion: CompanionData | null | undefined,
  nowMs?: number,
): FeedBuff | null;
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Créer suite Jest companion-feed.test.ts (RED avant implémentation)</name>
  <files>lib/__tests__/companion-feed.test.ts</files>
  <read_first>
    - lib/__tests__ (liste — pour voir le pattern existant)
    - lib/mascot/companion-types.ts (après Plan 01 — helpers exportés)
    - lib/mascot/companion-engine.ts (signatures actuelles)
  </read_first>
  <behavior>
    Suite Jest ≥12 tests couvrant :
    - getBuffForCrop('perfect','renard','strawberry') → multiplier ≈ 1.495, duration 5400s
    - getBuffForCrop('ordinary','chat','carrot') → multiplier 1.05 (neutre)
    - getBuffForCrop('good','lapin','corn') → null (détesté)
    - isBuffActive avec expiresAt futur → true
    - isBuffActive avec expiresAt passé → false
    - isBuffActive(null/undefined) → false
    - getCooldownRemainingMs(undefined) → 0
    - getCooldownRemainingMs avec lastFedAt il y a 3h01min → 0
    - getCooldownRemainingMs avec lastFedAt il y a 1h → ~7_200_000
    - feedCompanion jamais nourri, perfect+preferred → applied=true, updated.lastFedAt défini, updated.feedBuff avec mul 1.495
    - feedCompanion cooldown actif (lastFedAt il y a 1h) → applied=false, cooldownMs > 0, updated inchangé (pas de nouveau lastFedAt)
    - feedCompanion cooldown expiré (lastFedAt il y a 4h), crop hated → applied=true, lastFedAt rafraîchi, feedBuff=null, affinity='hated'
    - feedCompanion ne mute pas l'argument d'origine (strict equality check avant/après sur l'input)
    - feedCompanion buff remplace un buff existant (D-09 : un seul buff actif)
    - getActiveFeedBuff retourne null si feedBuff expiré
    - getActiveFeedBuff retourne feedBuff si actif
  </behavior>
  <action>
    Créer `lib/__tests__/companion-feed.test.ts` :

    ```typescript
    import {
      getAffinity,
      getBuffForCrop,
      isBuffActive,
      getCooldownRemainingMs,
      FEED_COOLDOWN_MS,
      GRADE_BUFF_TABLE,
      AFFINITY_MULTIPLIER,
      COMPANION_PREFERENCES,
      type CompanionData,
    } from '../mascot/companion-types';
    import { feedCompanion, getActiveFeedBuff } from '../mascot/companion-engine';

    const baseCompanion = (): CompanionData => ({
      activeSpecies: 'renard',
      name: 'Foxie',
      unlockedSpecies: ['renard'],
    });

    describe('Phase 42 — getAffinity', () => {
      it('préféré matche', () => {
        const preferred = COMPANION_PREFERENCES.renard.preferred;
        expect(getAffinity('renard', preferred)).toBe('preferred');
      });
      it('détesté matche', () => {
        const hated = COMPANION_PREFERENCES.renard.hated;
        expect(getAffinity('renard', hated)).toBe('hated');
      });
      it('autres crops = neutre', () => {
        expect(getAffinity('renard', 'wheat')).toBe('neutral');
      });
    });

    describe('Phase 42 — getBuffForCrop', () => {
      it('perfect + preferred = mul ×1.495 durée 5400s', () => {
        const now = Date.now();
        const preferred = COMPANION_PREFERENCES.renard.preferred;
        const buff = getBuffForCrop('perfect', 'renard', preferred, now);
        expect(buff).not.toBeNull();
        expect(buff!.multiplier).toBeCloseTo(1.15 * 1.3, 3);
        const delta = new Date(buff!.expiresAt).getTime() - now;
        expect(delta).toBeGreaterThanOrEqual(5400 * 1000 - 5);
        expect(delta).toBeLessThanOrEqual(5400 * 1000 + 5);
      });
      it('ordinary + neutral = mul 1.05', () => {
        const buff = getBuffForCrop('ordinary', 'chat', 'wheat');
        expect(buff!.multiplier).toBeCloseTo(1.05, 3);
      });
      it('hated = null', () => {
        const hated = COMPANION_PREFERENCES.lapin.hated;
        expect(getBuffForCrop('good', 'lapin', hated)).toBeNull();
      });
    });

    describe('Phase 42 — isBuffActive', () => {
      it('expiresAt futur → true', () => {
        const future = new Date(Date.now() + 60_000).toISOString();
        expect(isBuffActive({ multiplier: 1.1, expiresAt: future })).toBe(true);
      });
      it('expiresAt passé → false', () => {
        const past = new Date(Date.now() - 60_000).toISOString();
        expect(isBuffActive({ multiplier: 1.1, expiresAt: past })).toBe(false);
      });
      it('null/undefined → false', () => {
        expect(isBuffActive(null)).toBe(false);
        expect(isBuffActive(undefined)).toBe(false);
      });
    });

    describe('Phase 42 — getCooldownRemainingMs', () => {
      it('lastFedAt undefined → 0', () => {
        expect(getCooldownRemainingMs(undefined)).toBe(0);
      });
      it('lastFedAt il y a 4h → 0', () => {
        const iso = new Date(Date.now() - 4 * 3600 * 1000).toISOString();
        expect(getCooldownRemainingMs(iso)).toBe(0);
      });
      it('lastFedAt il y a 1h → ~2h restantes', () => {
        const iso = new Date(Date.now() - 1 * 3600 * 1000).toISOString();
        const remaining = getCooldownRemainingMs(iso);
        expect(remaining).toBeGreaterThan(1.95 * 3600 * 1000);
        expect(remaining).toBeLessThan(2.05 * 3600 * 1000);
      });
    });

    describe('Phase 42 — feedCompanion', () => {
      it('jamais nourri + preferred perfect → applied, buff set', () => {
        const comp = baseCompanion();
        const preferred = COMPANION_PREFERENCES.renard.preferred;
        const result = feedCompanion(comp, preferred, 'perfect');
        expect(result.applied).toBe(true);
        expect(result.affinity).toBe('preferred');
        expect(result.updated.lastFedAt).toBeDefined();
        expect(result.updated.feedBuff).not.toBeNull();
        expect(result.newBuff!.multiplier).toBeCloseTo(1.15 * 1.3, 3);
        // Immutabilité
        expect(comp.lastFedAt).toBeUndefined();
      });

      it('cooldown actif → applied=false, pas de mutation', () => {
        const recent = new Date(Date.now() - 1 * 3600 * 1000).toISOString();
        const comp: CompanionData = { ...baseCompanion(), lastFedAt: recent };
        const result = feedCompanion(comp, 'wheat', 'good');
        expect(result.applied).toBe(false);
        expect(result.cooldownMs).toBeGreaterThan(0);
        expect(result.updated.lastFedAt).toBe(recent);
      });

      it('hated → applied=true mais feedBuff null', () => {
        const hated = COMPANION_PREFERENCES.lapin.hated;
        const comp: CompanionData = { ...baseCompanion(), activeSpecies: 'lapin' };
        const result = feedCompanion(comp, hated, 'ordinary');
        expect(result.applied).toBe(true);
        expect(result.affinity).toBe('hated');
        expect(result.updated.feedBuff).toBeNull();
        expect(result.updated.lastFedAt).toBeDefined();
      });

      it('remplace un buff existant (D-09)', () => {
        const oldExp = new Date(Date.now() + 30 * 60 * 1000).toISOString();
        const oldFed = new Date(Date.now() - 4 * 3600 * 1000).toISOString(); // cooldown écoulé
        const comp: CompanionData = {
          ...baseCompanion(),
          lastFedAt: oldFed,
          feedBuff: { multiplier: 1.05, expiresAt: oldExp },
        };
        const preferred = COMPANION_PREFERENCES.renard.preferred;
        const result = feedCompanion(comp, preferred, 'perfect');
        expect(result.applied).toBe(true);
        expect(result.updated.feedBuff!.multiplier).toBeCloseTo(1.15 * 1.3, 3);
      });
    });

    describe('Phase 42 — getActiveFeedBuff', () => {
      it('buff futur → retourné', () => {
        const future = new Date(Date.now() + 60_000).toISOString();
        const comp: CompanionData = { ...baseCompanion(), feedBuff: { multiplier: 1.1, expiresAt: future } };
        expect(getActiveFeedBuff(comp)).not.toBeNull();
      });
      it('buff expiré → null', () => {
        const past = new Date(Date.now() - 60_000).toISOString();
        const comp: CompanionData = { ...baseCompanion(), feedBuff: { multiplier: 1.1, expiresAt: past } };
        expect(getActiveFeedBuff(comp)).toBeNull();
      });
      it('compagnon null → null', () => {
        expect(getActiveFeedBuff(null)).toBeNull();
      });
    });
    ```

    Exécuter une première fois `npx jest lib/__tests__/companion-feed.test.ts --no-coverage` : les tests `feedCompanion`/`getActiveFeedBuff` DOIVENT échouer (pas encore implémentés) → RED phase confirmée.
  </action>
  <verify>
    <automated>test -f lib/__tests__/companion-feed.test.ts &amp;&amp; grep -q "feedCompanion" lib/__tests__/companion-feed.test.ts &amp;&amp; grep -q "getActiveFeedBuff" lib/__tests__/companion-feed.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Fichier `lib/__tests__/companion-feed.test.ts` existe
    - `grep -c "it(" lib/__tests__/companion-feed.test.ts` retourne ≥12
    - `grep -q "feedCompanion(comp" lib/__tests__/companion-feed.test.ts` réussit
    - `npx jest lib/__tests__/companion-feed.test.ts --no-coverage` échoue sur les tests feedCompanion (RED phase) — erreur type "is not a function" ou équivalent
  </acceptance_criteria>
  <done>
    Suite Jest en place avec RED confirmée sur feedCompanion/getActiveFeedBuff. Les tests sur helpers du Plan 01 passent déjà (GREEN partiel).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implémenter feedCompanion + getActiveFeedBuff (GREEN)</name>
  <files>lib/mascot/companion-engine.ts</files>
  <read_first>
    - lib/mascot/companion-engine.ts L1-30 (imports existants et patterns purs)
    - lib/__tests__/companion-feed.test.ts (après Task 1 — contrats de test)
    - lib/mascot/companion-types.ts (helpers + constantes Plan 01)
  </read_first>
  <behavior>
    - Tous les tests de `companion-feed.test.ts` passent
    - feedCompanion jamais mute l'argument (Object.is(input, output) === false si applied)
    - getActiveFeedBuff n'écrit rien — retourne null pour buff expiré
  </behavior>
  <action>
    Dans `lib/mascot/companion-engine.ts`, **étendre les imports depuis `./companion-types`** pour inclure les helpers ajoutés au Plan 01 :

    ```typescript
    import {
      COMPANION_STAGES,
      COMPANION_XP_BONUS,
      SPECIES_PERSONALITY,
      getAffinity,
      getBuffForCrop,
      isBuffActive,
      getCooldownRemainingMs,
      type CompanionData,
      type CompanionEvent,
      type CompanionMood,
      type CompanionMessageContext,
      type CompanionStage,
      type CropAffinity,
      type FeedBuff,
      type HarvestGrade,
    } from './companion-types';
    ```

    **Ajouter après `getCompanionXpBonus` (après L106)** :

    ```typescript
    // ── Phase 42 — Nourrissage compagnon ────────────────────

    /** Résultat structuré d'une tentative de nourrissage */
    export interface FeedResult {
      /** Nouvel objet CompanionData (jamais mutation) */
      updated: CompanionData;
      /** Affinité évaluée (pour animations/messages) */
      affinity: CropAffinity;
      /** true si le feed a été appliqué, false si cooldown actif */
      applied: boolean;
      /** Nouveau buff créé (null si hated ou cooldown actif) */
      newBuff: FeedBuff | null;
      /** ms restantes avant prochain feed autorisé (0 si applied=true) */
      cooldownMs: number;
    }

    /**
     * Tente de nourrir le compagnon avec `cropId` au grade `grade`.
     * — Si cooldown actif (getCooldownRemainingMs > 0) : applied=false, rien n'est modifié.
     * — Sinon : lastFedAt rafraîchi, feedBuff défini (ou null si hated).
     * Fonction pure : retourne un nouvel objet CompanionData sans muter l'argument (D-09, D-10).
     */
    export function feedCompanion(
      companion: CompanionData,
      cropId: string,
      grade: HarvestGrade,
      nowMs: number = Date.now(),
    ): FeedResult {
      const affinity = getAffinity(companion.activeSpecies, cropId);
      const cooldownMs = getCooldownRemainingMs(companion.lastFedAt, nowMs);
      if (cooldownMs > 0) {
        return {
          updated: companion,
          affinity,
          applied: false,
          newBuff: null,
          cooldownMs,
        };
      }
      const newBuff = getBuffForCrop(grade, companion.activeSpecies, cropId, nowMs);
      const updated: CompanionData = {
        ...companion,
        lastFedAt: new Date(nowMs).toISOString(),
        feedBuff: newBuff, // null si hated — D-06
      };
      return { updated, affinity, applied: true, newBuff, cooldownMs: 0 };
    }

    /**
     * Retourne le feedBuff s'il est encore actif (expiration lazy).
     * Ne modifie pas le CompanionData — le nettoyage persistant se fait à la prochaine écriture.
     */
    export function getActiveFeedBuff(
      companion: CompanionData | null | undefined,
      nowMs: number = Date.now(),
    ): FeedBuff | null {
      if (!companion || !companion.feedBuff) return null;
      if (!isBuffActive(companion.feedBuff, nowMs)) return null;
      return companion.feedBuff;
    }
    ```

    Lancer `npx jest lib/__tests__/companion-feed.test.ts --no-coverage` — tous les tests doivent passer (GREEN).
  </action>
  <verify>
    <automated>grep -q "export function feedCompanion" lib/mascot/companion-engine.ts &amp;&amp; grep -q "export function getActiveFeedBuff" lib/mascot/companion-engine.ts &amp;&amp; npx jest lib/__tests__/companion-feed.test.ts --no-coverage 2>&amp;1 | tail -5 | grep -q "Tests:.*passed"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "export function feedCompanion" lib/mascot/companion-engine.ts` réussit
    - `grep -q "export function getActiveFeedBuff" lib/mascot/companion-engine.ts` réussit
    - `grep -q "interface FeedResult" lib/mascot/companion-engine.ts` réussit
    - `npx jest lib/__tests__/companion-feed.test.ts --no-coverage` retourne exit 0 et tous les tests passent
    - `npx tsc --noEmit` n'introduit pas de nouvelle erreur dans companion-engine.ts
  </acceptance_criteria>
  <done>
    feedCompanion et getActiveFeedBuff implémentés purs. Suite Jest verte ≥12 tests. Backward compat préservée.
  </done>
</task>

</tasks>

<verification>
- `npx jest lib/__tests__/companion-feed.test.ts --no-coverage` verte
- `npx tsc --noEmit` sans nouvelle erreur
- feedCompanion est pure (test immutabilité dans la suite)
</verification>

<success_criteria>
- Commit FR : `feat(42): moteur feedCompanion pur + tests Jest`
- getBuffForCrop, isBuffActive, getCooldownRemainingMs, feedCompanion, getActiveFeedBuff tous testés
</success_criteria>

<output>
.planning/phases/42-nourrir-le-compagnon/42-03-SUMMARY.md avec :
- Signatures finales FeedResult
- Décision immutabilité (spread CompanionData, jamais de mutation)
- Résultats `npx jest` (nombre de tests passés)
</output>
