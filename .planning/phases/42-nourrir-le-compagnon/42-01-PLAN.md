---
phase: 42-nourrir-le-compagnon
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/mascot/companion-types.ts
autonomous: true
requirements: [FEED-01, FEED-02, FEED-03]
must_haves:
  truths:
    - "Types CompanionData étendus avec lastFedAt?: string et feedBuff?: { multiplier: number; expiresAt: string } | null"
    - "Constantes exposées : GRADE_BUFF_TABLE, AFFINITY_MULTIPLIER, FEED_COOLDOWN_MS, COMPANION_PREFERENCES"
    - "Helpers pures exportés : getAffinity(species, cropId), getBuffForCrop(grade, species, cropId), isBuffActive(feedBuff, now), getCooldownRemainingMs(lastFedAt, now)"
    - "Mapping COMPANION_PREFERENCES redistribué OPTION A (D-13 révisé) : chaque espèce a un préféré et un détesté UNIQUES parmi CROP_CATALOG existant"
  artifacts:
    - path: "lib/mascot/companion-types.ts"
      provides: "Types CompanionData étendus + constantes buff + helpers pure"
      contains: "GRADE_BUFF_TABLE"
  key_links:
    - from: "lib/mascot/companion-types.ts"
      to: "CompanionData interface"
      via: "optional fields lastFedAt + feedBuff"
      pattern: "lastFedAt\\?: string"
---

<objective>
Étendre `lib/mascot/companion-types.ts` avec les types, constantes et helpers purs nécessaires au nourrissage compagnon. Fondation zéro dépendance utilisable par le parser (Plan 02), l'engine (Plan 03), le moteur XP (Plan 04) et l'UI (Plans 05-07).

Purpose: Couvrir décisions D-05 (tableau grade→buff), D-06 (affinité), D-10 (cooldown 3h), **D-13 révisé Option A** (mapping redistribué — tous uniques), D-15 (champs CompanionData optionnels).
Output: types exportés + constantes figées + 4 helpers purs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/42-nourrir-le-compagnon/42-CONTEXT.md
@lib/mascot/companion-types.ts
@lib/mascot/types.ts
@CLAUDE.md
</context>

<interfaces>
```typescript
// Extension CompanionData (backward compat : deux champs optionnels)
export interface CompanionData {
  activeSpecies: CompanionSpecies;
  name: string;
  unlockedSpecies: CompanionSpecies[];
  lastEventType?: string;
  lastEventAt?: string;
  // Phase 42
  lastFedAt?: string; // ISO datetime du dernier feed (cooldown 3h)
  feedBuff?: FeedBuff | null; // buff XP actif (null = aucun)
}

export interface FeedBuff {
  multiplier: number;   // multiplicateur XP final (ex: 1.15)
  expiresAt: string;    // ISO datetime d'expiration
}

export type HarvestGrade = 'ordinary' | 'good' | 'excellent' | 'perfect';
export type CropAffinity = 'preferred' | 'neutral' | 'hated';

// Constantes exactes (D-05, D-06, D-10, D-13 révisé Option A)
export const FEED_COOLDOWN_MS: number; // 3h = 10_800_000
export const GRADE_BUFF_TABLE: Record<HarvestGrade, { multiplier: number; durationSec: number }>;
export const AFFINITY_MULTIPLIER: Record<CropAffinity, number>;
export const COMPANION_PREFERENCES: Record<CompanionSpecies, { preferred: string; hated: string }>;

// Helpers purs
export function getAffinity(species: CompanionSpecies, cropId: string): CropAffinity;
export function getBuffForCrop(grade: HarvestGrade, species: CompanionSpecies, cropId: string): FeedBuff | null;
export function isBuffActive(feedBuff: FeedBuff | null | undefined, nowMs?: number): boolean;
export function getCooldownRemainingMs(lastFedAt: string | undefined, nowMs?: number): number;
```
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Étendre CompanionData + exporter constantes et types buff (mapping Option A)</name>
  <files>lib/mascot/companion-types.ts</files>
  <read_first>
    - lib/mascot/companion-types.ts (intégralité — comprendre l'interface CompanionData existante et ses exports)
    - lib/mascot/types.ts L360-390 (CROP_CATALOG — vérifier que strawberry, cucumber, pumpkin, tomato, carrot, corn, beetroot, wheat, potato, cabbage existent avec ces IDs exacts)
    - .planning/phases/42-nourrir-le-compagnon/42-CONTEXT.md sections D-05, D-06, D-10, D-13 (révisé Option A), D-15
  </read_first>
  <behavior>
    - getAffinity('chat', 'strawberry') retourne 'preferred' (D-13 Option A)
    - getAffinity('chat', 'cucumber') retourne 'hated'
    - getAffinity('renard', 'beetroot') retourne 'preferred'
    - getAffinity('renard', 'wheat') retourne 'hated'
    - getAffinity('chien', 'carrot') retourne 'neutral'
    - isBuffActive({ multiplier: 1.1, expiresAt: <futur ISO> }) retourne true
    - isBuffActive({ multiplier: 1.1, expiresAt: <passé ISO> }) retourne false
    - isBuffActive(null) retourne false
    - isBuffActive(undefined) retourne false
    - getCooldownRemainingMs(undefined) retourne 0 (jamais nourri → nourrissable immédiatement)
    - getCooldownRemainingMs(<ISO il y a 1h>) retourne environ 7_200_000 (2h)
    - getCooldownRemainingMs(<ISO il y a 5h>) retourne 0
    - getBuffForCrop('perfect', 'renard', 'beetroot') retourne { multiplier: 1.15 × 1.3 = 1.495, expiresAt: now + 5400s }
    - getBuffForCrop('good', 'chat', 'carrot') retourne { multiplier: 1.10, expiresAt: now + 2700s } (neutre)
    - getBuffForCrop('ordinary', 'lapin', 'corn') retourne null (détesté)
  </behavior>
  <action>
    Ajouter au fichier `lib/mascot/companion-types.ts` après les types existants, AVANT les exports de constantes finales :

    1. **Types** :
       ```typescript
       export type HarvestGrade = 'ordinary' | 'good' | 'excellent' | 'perfect';
       export type CropAffinity = 'preferred' | 'neutral' | 'hated';

       export interface FeedBuff {
         multiplier: number;
         expiresAt: string; // ISO
       }
       ```

    2. **Étendre l'interface `CompanionData`** (ajouter les 2 champs optionnels à la fin, AVANT la closing brace) :
       ```typescript
       /** Phase 42 — ISO du dernier nourrissage (cooldown 3h) */
       lastFedAt?: string;
       /** Phase 42 — buff XP actif (null = aucun) */
       feedBuff?: FeedBuff | null;
       ```

    3. **Constantes** (ajouter après `COMPANION_XP_BONUS`) :
       ```typescript
       /** Phase 42 — Cooldown entre feeds (3h en ms) — D-10 */
       export const FEED_COOLDOWN_MS = 3 * 60 * 60 * 1000; // 10_800_000

       /** Phase 42 — Table grade → (multiplicateur, durée secondes) — D-05 */
       export const GRADE_BUFF_TABLE: Record<HarvestGrade, { multiplier: number; durationSec: number }> = {
         ordinary:  { multiplier: 1.05, durationSec: 1800 },  // 30min
         good:      { multiplier: 1.10, durationSec: 2700 },  // 45min
         excellent: { multiplier: 1.12, durationSec: 3600 },  // 60min
         perfect:   { multiplier: 1.15, durationSec: 5400 },  // 90min
       };

       /** Phase 42 — Multiplicateur par affinité espèce/crop — D-06 */
       export const AFFINITY_MULTIPLIER: Record<CropAffinity, number> = {
         preferred: 1.3,
         neutral:   1.0,
         hated:     0, // produit un buff null
       };

       /** Phase 42 — Préférence alimentaire par espèce — D-13 (OPTION A validée user 2026-04-22)
        *  Mapping officiel : redistribution parmi CROP_CATALOG existant.
        *  Chaque espèce a un préféré ET un détesté UNIQUES (pas de collision).
        *  Rationale : poisson/os/champignon/oignon absents de CROP_CATALOG.
        *  Extension future (fish/bone/mushroom/onion) = deferred v1.8+ (D-13-bis CONTEXT.md).
        */
       export const COMPANION_PREFERENCES: Record<CompanionSpecies, { preferred: string; hated: string }> = {
         chat:     { preferred: 'strawberry', hated: 'cucumber' }, // fraise / concombre
         chien:    { preferred: 'pumpkin',    hated: 'tomato'   }, // potiron / tomate
         lapin:    { preferred: 'carrot',     hated: 'corn'     }, // carotte / maïs
         renard:   { preferred: 'beetroot',   hated: 'wheat'    }, // betterave / blé
         herisson: { preferred: 'potato',     hated: 'cabbage'  }, // pomme de terre / chou
       };
       ```

       **IMPORTANT — Règle de validation IDs crops** : avant de finaliser, exécuter `grep -n "id: '" lib/mascot/types.ts | head -30` et vérifier que chaque cropId dans COMPANION_PREFERENCES (strawberry, cucumber, pumpkin, tomato, carrot, corn, beetroot, wheat, potato, cabbage) existe. Les 10 IDs DOIVENT exister dans CROP_CATALOG. Si un ID manque, arrêter et escalader — le mapping Option A est validé par le user et tout écart est un bug des assets.

    4. **Helpers purs** (ajouter à la fin du fichier) :
       ```typescript
       /** Phase 42 — Retourne l'affinité d'un crop pour une espèce donnée. */
       export function getAffinity(species: CompanionSpecies, cropId: string): CropAffinity {
         const prefs = COMPANION_PREFERENCES[species];
         if (!prefs) return 'neutral';
         if (cropId === prefs.preferred) return 'preferred';
         if (cropId === prefs.hated) return 'hated';
         return 'neutral';
       }

       /** Phase 42 — Calcule le FeedBuff à appliquer, ou null si crop détesté. */
       export function getBuffForCrop(
         grade: HarvestGrade,
         species: CompanionSpecies,
         cropId: string,
         nowMs: number = Date.now(),
       ): FeedBuff | null {
         const base = GRADE_BUFF_TABLE[grade];
         if (!base) return null;
         const affinity = getAffinity(species, cropId);
         const affMul = AFFINITY_MULTIPLIER[affinity];
         if (affMul === 0) return null; // détesté — pas de buff
         const multiplier = +(base.multiplier * affMul).toFixed(4);
         const expiresAt = new Date(nowMs + base.durationSec * 1000).toISOString();
         return { multiplier, expiresAt };
       }

       /** Phase 42 — Buff actif si expiresAt dans le futur. */
       export function isBuffActive(
         feedBuff: FeedBuff | null | undefined,
         nowMs: number = Date.now(),
       ): boolean {
         if (!feedBuff) return false;
         const exp = new Date(feedBuff.expiresAt).getTime();
         if (isNaN(exp)) return false;
         return exp > nowMs;
       }

       /** Phase 42 — Retourne ms restants avant qu'un nouveau feed soit permis (0 si feed possible). */
       export function getCooldownRemainingMs(
         lastFedAt: string | undefined,
         nowMs: number = Date.now(),
       ): number {
         if (!lastFedAt) return 0;
         const last = new Date(lastFedAt).getTime();
         if (isNaN(last)) return 0;
         const elapsed = nowMs - last;
         return Math.max(0, FEED_COOLDOWN_MS - elapsed);
       }
       ```

    Commentaires FR obligatoires, convention existante du fichier respectée. Aucun champ non-optionnel ajouté (backward compat D-16).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&amp;1 | grep "companion-types" ; test $? -eq 1 &amp;&amp; grep -q "FEED_COOLDOWN_MS" lib/mascot/companion-types.ts &amp;&amp; grep -q "GRADE_BUFF_TABLE" lib/mascot/companion-types.ts &amp;&amp; grep -q "COMPANION_PREFERENCES" lib/mascot/companion-types.ts &amp;&amp; grep -q "getBuffForCrop" lib/mascot/companion-types.ts &amp;&amp; grep -q "lastFedAt" lib/mascot/companion-types.ts &amp;&amp; grep -q "feedBuff" lib/mascot/companion-types.ts &amp;&amp; grep -q "preferred: 'beetroot'" lib/mascot/companion-types.ts &amp;&amp; grep -q "preferred: 'potato'" lib/mascot/companion-types.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "FEED_COOLDOWN_MS = 3 \* 60 \* 60 \* 1000" lib/mascot/companion-types.ts` réussit
    - `grep -q "ordinary:  { multiplier: 1.05, durationSec: 1800 }" lib/mascot/companion-types.ts` réussit
    - `grep -q "perfect:   { multiplier: 1.15, durationSec: 5400 }" lib/mascot/companion-types.ts` réussit
    - `grep -q "preferred: 1.3" lib/mascot/companion-types.ts` réussit
    - `grep -q "hated:     0" lib/mascot/companion-types.ts` réussit
    - `grep -q "lastFedAt\\?: string" lib/mascot/companion-types.ts` réussit
    - `grep -q "feedBuff\\?: FeedBuff | null" lib/mascot/companion-types.ts` réussit
    - **Mapping Option A vérifié** : `grep -q "chat:     { preferred: 'strawberry', hated: 'cucumber' }" lib/mascot/companion-types.ts`, `grep -q "chien:    { preferred: 'pumpkin',    hated: 'tomato'   }"`, `grep -q "lapin:    { preferred: 'carrot',     hated: 'corn'     }"`, `grep -q "renard:   { preferred: 'beetroot',   hated: 'wheat'    }"`, `grep -q "herisson: { preferred: 'potato',     hated: 'cabbage'  }"`
    - `grep -q "export function getAffinity" lib/mascot/companion-types.ts` réussit
    - `grep -q "export function getBuffForCrop" lib/mascot/companion-types.ts` réussit
    - `grep -q "export function isBuffActive" lib/mascot/companion-types.ts` réussit
    - `grep -q "export function getCooldownRemainingMs" lib/mascot/companion-types.ts` réussit
    - `npx tsc --noEmit` exit 0 (modulo erreurs pré-existantes MemoryEditor/cooklang/useVault)
    - Validation CROP_CATALOG : `for id in strawberry cucumber pumpkin tomato carrot corn beetroot wheat potato cabbage ; do grep -q "id: '$id'" lib/mascot/types.ts || echo "MISSING: $id" ; done` doit afficher aucun MISSING
  </acceptance_criteria>
  <done>
    CompanionData étendu avec lastFedAt/feedBuff optionnels. 4 helpers purs exportés. Constantes GRADE_BUFF_TABLE, AFFINITY_MULTIPLIER, COMPANION_PREFERENCES (mapping Option A validé user), FEED_COOLDOWN_MS disponibles pour tous les plans aval. Typecheck vert.
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passe
- Tous les exports sont utilisables sans import de dépendance externe (types purs)
- Backward compat : un CompanionData sans lastFedAt ni feedBuff est toujours valide
- Mapping Option A : tous préférés/détestés existent dans CROP_CATALOG et sont uniques
</verification>

<success_criteria>
- Fichier modifié contient les 4 helpers + 4 constantes + 2 champs CompanionData
- Commit FR : `feat(42): types feed compagnon (buff table + affinités Option A + helpers)`
</success_criteria>

<output>
.planning/phases/42-nourrir-le-compagnon/42-01-SUMMARY.md avec :
- Mapping COMPANION_PREFERENCES final (Option A validé user)
- Signatures finales des helpers
- Confirmation tous IDs crops présents dans CROP_CATALOG
</output>
