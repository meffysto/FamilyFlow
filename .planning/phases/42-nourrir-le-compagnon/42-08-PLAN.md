---
phase: 42-nourrir-le-compagnon
plan: 08
type: execute
wave: 4
depends_on: [42-07]
files_modified:
  - components/mascot/CompanionSlot.tsx
  - components/mascot/FeedParticles.tsx
  - app/(tabs)/tree.tsx
autonomous: true
requirements: [FEED-15, FEED-16]
must_haves:
  truths:
    - "CompanionSlot expose une prop feedState?: 'eating-preferred' | 'eating-neutral' | 'eating-hated' | null"
    - "Quand feedState non-null, le sprite joue une courte animation (scale punch via reanimated withSpring puis withTiming reset) et reset après 1500ms"
    - "FeedParticles.tsx rend des particules emoji flottantes selon affinité : 💕 (preferred), 😊 (neutral), 💨 (hated)"
    - "Particules animées via useSharedValue + withTiming pour translateY up -60px + opacity 1→0 sur 1200ms"
    - "Haptics.impactAsync(Heavy) sur preferred, Light sinon (D-20)"
    - "tree.tsx déclenche feedState temporairement après handleFeedCrop success selon result.affinity"
  artifacts:
    - path: "components/mascot/FeedParticles.tsx"
      provides: "Overlay particules emoji contextualisées"
      min_lines: 80
    - path: "components/mascot/CompanionSlot.tsx"
      provides: "Animation eat pulse via prop feedState"
  key_links:
    - from: "tree.tsx"
      to: "CompanionSlot + FeedParticles"
      via: "state feedState mis à jour après handleFeedCrop success"
      pattern: "feedState"
---

<objective>
Ajouter un feedback visuel immédiat quand le compagnon mange : pulse du sprite dans CompanionSlot + overlay particules emoji contextualisées (cœurs/bulles/vent) + haptics calibré selon affinité.

Purpose: D-18 (anim eat — **résolu v1 via scale-pulse + FeedParticles**, voir note ci-dessous), D-19 (particules selon affinité), D-20 (haptics).

**Note D-18 résolu (Claude'''s Discretion validé user 2026-04-22) :** le CONTEXT.md D-18 original proposait 3-5 frames sprite "eat" dédiées, mais AUCUN asset frame "eat" n'''existe dans  (seules idle/happy/walk existent). Solution v1 retenue et validée implicitement par user : **scale-pulse reanimated (1.0→1.3→1.0 preferred / 1.0→1.15→1.0 neutral / recul -8px + 0.9 scale hated) + overlay FeedParticles** = équivalent perceptif d'''une animation eat, ship-ready sans blocage asset. Les vraies sprite frames "eat" sont déférées à une milestone future (v1.8+ "Compagnon Vivant").

Output: animation runtime en <1.5s + particules flottantes émouvantes — substitut D-18 validé.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/42-nourrir-le-compagnon/42-CONTEXT.md
@components/mascot/CompanionSlot.tsx
@app/(tabs)/tree.tsx
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Créer FeedParticles.tsx avec particules emoji flottantes par affinité</name>
  <files>components/mascot/FeedParticles.tsx</files>
  <read_first>
    - components/mascot/HarvestBurst.tsx (pattern existant de particules burst — à imiter)
    - CLAUDE.md (reanimated 4.1 obligatoire, SPRING_CONFIG pattern module-level)
    - CONTEXT.md D-19
  </read_first>
  <behavior>
    - Props : { visible: boolean; affinity: 'preferred' | 'neutral' | 'hated'; x: number; y: number; onEnd?: () => void }
    - Quand visible passe à true : 5 particules émoji dispersées autour de (x, y), animées en float-up (-60px) + opacity 1→0 sur 1200ms
    - Emoji par affinité : preferred='💕', neutral='😊', hated='💨'
    - onEnd callback après fin de l'animation
    - Absolute positioned overlay (pointerEvents: 'none')
    - Reanimated : useSharedValue + useAnimatedStyle + withTiming
  </behavior>
  <action>
    Créer `components/mascot/FeedParticles.tsx` :

    ```typescript
    /**
     * FeedParticles.tsx — Particules emoji flottantes après feed (Phase 42 D-19)
     *
     * Affiche 5 emojis qui montent de -60px avec fade-out sur 1200ms.
     * Emoji dépend de l'affinité : préféré = cœurs, neutre = bulles, détesté = vent.
     */

    import React, { useEffect } from 'react';
    import { View, Text, StyleSheet } from 'react-native';
    import Animated, {
      useSharedValue,
      useAnimatedStyle,
      withTiming,
      withDelay,
      runOnJS,
      Easing,
    } from 'react-native-reanimated';
    import type { CropAffinity } from '../../lib/mascot/companion-types';

    const AFFINITY_EMOJI: Record<CropAffinity, string> = {
      preferred: '💕',
      neutral:   '😊',
      hated:     '💨',
    };

    const PARTICLE_COUNT = 5;
    const FLOAT_DISTANCE = -60;
    const DURATION_MS = 1200;

    interface FeedParticlesProps {
      visible: boolean;
      affinity: CropAffinity;
      x: number;
      y: number;
      onEnd?: () => void;
    }

    function Particle({
      emoji,
      delay,
      offsetX,
      onLastEnd,
    }: {
      emoji: string;
      delay: number;
      offsetX: number;
      onLastEnd?: () => void;
    }) {
      const translateY = useSharedValue(0);
      const opacity = useSharedValue(0);

      useEffect(() => {
        opacity.value = withDelay(delay, withTiming(1, { duration: 100 }));
        translateY.value = withDelay(
          delay,
          withTiming(FLOAT_DISTANCE, { duration: DURATION_MS, easing: Easing.out(Easing.quad) }),
        );
        opacity.value = withDelay(
          delay + DURATION_MS - 400,
          withTiming(0, { duration: 400 }, (finished) => {
            if (finished && onLastEnd) runOnJS(onLastEnd)();
          }),
        );
      }, [delay]);

      const style = useAnimatedStyle(() => ({
        transform: [{ translateY: translateY.value }, { translateX: offsetX }],
        opacity: opacity.value,
      }));

      return (
        <Animated.View style={[styles.particle, style]} pointerEvents="none">
          <Text style={styles.emoji}>{emoji}</Text>
        </Animated.View>
      );
    }

    export function FeedParticles({ visible, affinity, x, y, onEnd }: FeedParticlesProps) {
      if (!visible) return null;
      const emoji = AFFINITY_EMOJI[affinity];
      // Offsets horizontaux dispersés
      const offsets = [-24, -12, 0, 12, 24];
      return (
        <View style={[styles.container, { left: x, top: y }]} pointerEvents="none">
          {Array.from({ length: PARTICLE_COUNT }).map((_, i) => (
            <Particle
              key={i}
              emoji={emoji}
              delay={i * 60}
              offsetX={offsets[i] ?? 0}
              onLastEnd={i === PARTICLE_COUNT - 1 ? onEnd : undefined}
            />
          ))}
        </View>
      );
    }

    const styles = StyleSheet.create({
      container: {
        position: 'absolute',
        width: 0,
        height: 0,
      },
      particle: {
        position: 'absolute',
      },
      emoji: {
        fontSize: 20,
      },
    });
    ```
  </action>
  <verify>
    <automated>test -f components/mascot/FeedParticles.tsx &amp;&amp; grep -q "useSharedValue" components/mascot/FeedParticles.tsx &amp;&amp; grep -q "withTiming" components/mascot/FeedParticles.tsx &amp;&amp; grep -q "AFFINITY_EMOJI" components/mascot/FeedParticles.tsx &amp;&amp; grep -q "pointerEvents=\"none\"" components/mascot/FeedParticles.tsx &amp;&amp; npx tsc --noEmit 2>&amp;1 | grep "FeedParticles.tsx" ; test $? -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - Fichier existe, `export function FeedParticles` présent
    - `grep -q "react-native-reanimated" components/mascot/FeedParticles.tsx` (pas RN Animated)
    - `grep -q "💕" components/mascot/FeedParticles.tsx` (emoji preferred)
    - `grep -q "💨" components/mascot/FeedParticles.tsx` (emoji hated)
    - `wc -l components/mascot/FeedParticles.tsx` ≥80
    - `npx tsc --noEmit` pas d'erreur
  </acceptance_criteria>
  <done>
    Composant particules réutilisable, animation reanimated conforme CLAUDE.md, onEnd callback correct.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Ajouter prop feedState + animation pulse dans CompanionSlot.tsx</name>
  <files>components/mascot/CompanionSlot.tsx</files>
  <read_first>
    - components/mascot/CompanionSlot.tsx L1-120 (imports + constants + types existants)
    - components/mascot/CompanionSlot.tsx (chercher useSharedValue existants et handleTap — pattern pulse probablement déjà présent)
    - CLAUDE.md (SPRING_CONFIG module-level)
  </read_first>
  <behavior>
    - Prop `feedState?: 'eating-preferred' | 'eating-neutral' | 'eating-hated' | null` ajoutée aux props
    - Quand feedState passe à non-null : animation scale 1 → 1.25 → 1 via useSharedValue + withSequence(withSpring(1.25), withSpring(1.0))
    - Si feedState === 'eating-hated' : translateX -8 → 0 (recule — D-19)
    - Animation dure ~1000ms, se reset naturellement quand feedState repasse à null
    - Composant n'appelle pas de callback — tree.tsx gère le reset via setTimeout
  </behavior>
  <action>
    **1. Ajouter la prop** dans l'interface Props de CompanionSlot.tsx :
    ```typescript
    feedState?: 'eating-preferred' | 'eating-neutral' | 'eating-hated' | null;
    ```

    **2. Ajouter les shared values** dans le composant (à côté des autres useSharedValue existants) :
    ```typescript
    const feedScale = useSharedValue(1);
    const feedTranslateX = useSharedValue(0);
    ```

    **3. Ajouter un useEffect qui réagit à feedState** :
    ```typescript
    // Phase 42 — Animation feed (pulse + recul si détesté)
    useEffect(() => {
      if (!feedState) {
        feedScale.value = withTiming(1, { duration: 200 });
        feedTranslateX.value = withTiming(0, { duration: 200 });
        return;
      }
      if (feedState === 'eating-hated') {
        // recule + petite secousse
        feedTranslateX.value = withSequence(
          withTiming(-8, { duration: 200 }),
          withTiming(0, { duration: 400 }),
        );
        feedScale.value = withSequence(
          withSpring(0.9, SPRING_FEED),
          withSpring(1.0, SPRING_FEED),
        );
      } else {
        // eating-preferred OR eating-neutral : pulse joyeux
        const peak = feedState === 'eating-preferred' ? 1.3 : 1.15;
        feedScale.value = withSequence(
          withSpring(peak, SPRING_FEED),
          withSpring(1.0, SPRING_FEED),
        );
      }
    }, [feedState]);
    ```

    **4. Ajouter la constante SPRING_FEED au module level** (pattern CLAUDE.md) :
    ```typescript
    const SPRING_FEED = { damping: 10, stiffness: 180 } as const;
    ```

    **5. Composer l'animation avec le style existant du sprite** — trouver l'animatedStyle déjà en place et ajouter :
    ```typescript
    const feedAnimStyle = useAnimatedStyle(() => ({
      transform: [
        { scale: feedScale.value },
        { translateX: feedTranslateX.value },
      ],
    }));
    ```
    L'appliquer sur le wrapper Animated.View du sprite (combiner avec d'autres transforms si déjà présents — les transforms sont un array, rajouter les deux entrées).

    **6. Imports à ajouter** (vérifier imports reanimated en haut) :
    ```typescript
    import { withSequence, withSpring, withTiming } from 'react-native-reanimated';
    ```

    **Pitfall** : ne PAS mettre `perspective` dans le transform array (interdiction CLAUDE.md). Scale + translateX uniquement.
  </action>
  <verify>
    <automated>grep -q "feedState" components/mascot/CompanionSlot.tsx &amp;&amp; grep -q "SPRING_FEED" components/mascot/CompanionSlot.tsx &amp;&amp; grep -q "withSequence" components/mascot/CompanionSlot.tsx &amp;&amp; grep -q "eating-preferred" components/mascot/CompanionSlot.tsx &amp;&amp; npx tsc --noEmit 2>&amp;1 | grep "CompanionSlot.tsx" ; test $? -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "feedState\\?:" components/mascot/CompanionSlot.tsx` réussit
    - `grep -q "'eating-preferred' \\| 'eating-neutral' \\| 'eating-hated'" components/mascot/CompanionSlot.tsx` réussit
    - `grep -q "SPRING_FEED" components/mascot/CompanionSlot.tsx` réussit
    - Pas de référence à `perspective` dans l'animation feed
    - `npx tsc --noEmit` pas de nouvelle erreur
  </acceptance_criteria>
  <done>
    CompanionSlot expose feedState et pulse visuellement selon affinité. Backward compat : absence de feedState = comportement inchangé.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Wirer feedState + FeedParticles dans tree.tsx post-handleFeedCrop</name>
  <files>app/(tabs)/tree.tsx</files>
  <read_first>
    - app/(tabs)/tree.tsx (handleFeedCrop ajouté Plan 07)
    - app/(tabs)/tree.tsx (usage CompanionSlot L2351 — position dans le JSX)
    - components/mascot/FeedParticles.tsx (Task 1 — signature props)
    - components/mascot/CompanionSlot.tsx (Task 2 — prop feedState)
  </read_first>
  <behavior>
    - State `feedState` ajouté : `'eating-preferred' | 'eating-neutral' | 'eating-hated' | null`
    - Dans handleFeedCrop, après result.applied, setFeedState(`eating-${result.affinity}`) puis setTimeout(1500ms) pour reset à null
    - CompanionSlot reçoit prop `feedState={feedState}`
    - FeedParticles rendu au-dessus de CompanionSlot avec visible={!!feedState}, affinity=(dérivée du feedState), x/y positionnés sur le sprite
    - Haptics déjà géré dans handleFeedCrop (Plan 07)
  </behavior>
  <action>
    **1. Imports** :
    ```typescript
    import { FeedParticles } from '../../components/mascot/FeedParticles';
    import type { CropAffinity } from '../../lib/mascot/companion-types';
    ```

    **2. State** (à côté des autres `useState` pour compagnon ~L500) :
    ```typescript
    const [feedState, setFeedState] = useState<null | 'eating-preferred' | 'eating-neutral' | 'eating-hated'>(null);
    ```

    **3. Étendre handleFeedCrop** ajouté Plan 07 :
    ```typescript
    const handleFeedCrop = useCallback(
      async (cropId: string, grade: HarvestGrade) => {
        if (!profile?.id) return;
        const result = await feedCompanion(profile.id, cropId, grade);
        if (!result || !result.applied) return;
        // Haptics
        if (result.affinity === 'preferred') {
          Haptics.impactAsync(ImpactFeedbackStyle.Heavy).catch(() => {});
        } else {
          Haptics.impactAsync(ImpactFeedbackStyle.Light).catch(() => {});
        }
        // Animation pulse + particules
        const nextState = `eating-${result.affinity}` as const;
        setFeedState(nextState);
        setTimeout(() => setFeedState(null), 1500);
      },
      [profile?.id, feedCompanion],
    );
    ```

    **4. Wirer CompanionSlot** L2351 — ajouter la prop :
    ```tsx
    <CompanionSlot
      species={companion.activeSpecies}
      {/* ... autres props existantes ... */}
      feedState={feedState}
    />
    ```

    **5. Ajouter FeedParticles** juste après CompanionSlot dans le même container (en position absolute overlay) :
    ```tsx
    <FeedParticles
      visible={!!feedState}
      affinity={feedState ? (feedState.replace('eating-', '') as CropAffinity) : 'neutral'}
      x={/* position sprite en pixels — dérivée du layout CompanionSlot — utiliser une approximation ou mesurer via onLayout */}
      y={/* idem */}
    />
    ```

    **Note pratique** : la position exacte x/y dépend du layout de la scène. Si CompanionSlot est rendu dans un SVG/viewbox fixe, dériver de CompanionSlot position (L2351 devrait contenir un calcul). Valeurs de fallback acceptables : approximation du centre du container.
  </action>
  <verify>
    <automated>grep -q "feedState" "app/(tabs)/tree.tsx" &amp;&amp; grep -q "FeedParticles" "app/(tabs)/tree.tsx" &amp;&amp; grep -q "setFeedState" "app/(tabs)/tree.tsx" &amp;&amp; grep -q "setTimeout.*1500" "app/(tabs)/tree.tsx" &amp;&amp; npx tsc --noEmit 2>&amp;1 | grep "tree.tsx" ; test $? -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "setFeedState" "app/(tabs)/tree.tsx"` réussit
    - `grep -q "FeedParticles" "app/(tabs)/tree.tsx"` réussit
    - `grep -q "feedState={feedState}" "app/(tabs)/tree.tsx"` (prop passée à CompanionSlot)
    - setTimeout reset après 1500ms présent
    - `npx tsc --noEmit` pas de nouvelle erreur
  </acceptance_criteria>
  <done>
    Feed déclenche visuellement pulse + particules pendant 1.5s puis retour à l'idle. Haptics calibré preferred/neutre.
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` sans nouvelle erreur
- Test manuel : feed un crop préféré → scale pulse + cœurs flottants ; feed détesté → recul + vent
</verification>

<success_criteria>
- Commit FR : `feat(42): animation feed compagnon + particules affinité`
</success_criteria>

<output>
.planning/phases/42-nourrir-le-compagnon/42-08-SUMMARY.md avec : position x/y retenue pour FeedParticles, timing final animation, validation checkpoint visuel (ou différé au Plan 09).
</output>
