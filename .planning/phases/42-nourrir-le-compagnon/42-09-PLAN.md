---
phase: 42-nourrir-le-compagnon
plan: 09
type: execute
wave: 4
depends_on: [42-07]
files_modified:
  - lib/mascot/companion-engine.ts
  - lib/mascotte-live-activity.ts
  - hooks/useVault.ts
autonomous: true
requirements: [FEED-17, FEED-18]
must_haves:
  truths:
    - "buildFeedMessage(affinity, grade, cropLabel) retourne une string message selon D-21 (4 variantes)"
    - "Le message est poussé dans companion_messages SecureStore via saveCompanionMessages existant"
    - "MascotteSnapshot étendue avec feedBuffActive?: { multiplier, expiresAtIso } | null"
    - "buildFeedSpeechBubble helper exporté, longueur ≤44 chars"
    - "refreshMascotte override speechBubble depuis feedBuffActive si speechBubble non-fourni (Option A locked)"
    - "Le wiring message + Live Activity se fait dans hooks/useVault.ts:feedCompanion (PAS dans tree.tsx) — évite overlap Plan 08"
    - "patchMascotte(Partial<MascotteSnapshot>) merge feedBuffActive au lastSnapshot existant (déjà supporté signature)"
  artifacts:
    - path: "lib/mascot/companion-engine.ts"
      provides: "buildFeedMessage helper pur"
    - path: "lib/mascotte-live-activity.ts"
      provides: "MascotteSnapshot étendue + buildFeedSpeechBubble helper + refreshMascotte override"
      contains: "feedBuffActive"
  key_links:
    - from: "hooks/useVault.ts:feedCompanion"
      to: "companion_messages + patchMascotte"
      via: "push message + live activity update, intégrés dans la même fonction que l'écriture farm"
      pattern: "patchMascotte"
---

<objective>
Générer un message contextualisé après feed (D-21) et mettre à jour la Live Activity iOS (D-22 à D-25) pour refléter le buff actif sur le lock screen. **Tout le wiring est centralisé dans `hooks/useVault.ts:feedCompanion`** pour éviter l'overlap avec Plan 08 sur tree.tsx.

Purpose: D-21 (4 variantes de messages), D-22 (feedBuffActive dans MascotteSnapshot), D-23 (speechBubble override ≤44 chars), D-24 (refreshMascotte immédiat), D-25 (via speechBubble, pas de champ natif dédié v1).

**Résolution Warning 4 (Option A locked) :** la signature `patchMascotte(patch: Partial<MascotteSnapshot>)` existante (lib/mascotte-live-activity.ts L131-135) accepte déjà toute clé de `MascotteSnapshot` puisqu'elle utilise `Partial<MascotteSnapshot>`. Ajouter `feedBuffActive` à l'interface suffit à l'accepter comme patch — **aucune extension de signature patchMascotte nécessaire**. Option B (fallback runtime tree.tsx) supprimée.

**Résolution Warning 5 (overlap Plan 08/09 sur tree.tsx) :** tout le wiring Live Activity + message vit dans `hooks/useVault.ts:feedCompanion` (Plan 07 Task 1 étendu ici). Plan 09 ne modifie PAS tree.tsx, seulement companion-engine + mascotte-live-activity + useVault (dans la fonction déjà créée Plan 07). Pas de risque de conflit d'édition avec Plan 08.

Output: messages + Live Activity intégrés dans la boucle feed via le hook — tree.tsx n'a rien à ajouter.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/42-nourrir-le-compagnon/42-CONTEXT.md
@lib/mascot/companion-engine.ts
@lib/mascot/companion-storage.ts
@lib/mascotte-live-activity.ts
@hooks/useVault.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Ajouter buildFeedMessage dans companion-engine.ts + tests Jest</name>
  <files>lib/mascot/companion-engine.ts, lib/__tests__/companion-feed.test.ts</files>
  <read_first>
    - lib/mascot/companion-engine.ts (fonctions existantes pickCompanionMessage + templates)
    - CONTEXT.md D-21
  </read_first>
  <behavior>
    - buildFeedMessage({ affinity: 'preferred', grade: 'perfect', cropLabel: 'fraise' }) → contient "parfaite" + "fraise"
    - buildFeedMessage({ affinity: 'preferred', grade: 'good', ... }) → contient "préférée"
    - buildFeedMessage({ affinity: 'neutral', ... }) → contient "Merci"
    - buildFeedMessage({ affinity: 'hated', ... }) → contient "Berk"
  </behavior>
  <action>
    Ajouter dans `lib/mascot/companion-engine.ts` après `getActiveFeedBuff` :

    ```typescript
    /**
     * Phase 42 — Construit un message contextualisé après un feed (D-21).
     * Variantes :
     *  - preferred + perfect : message enthousiaste avec emoji crop + ✨
     *  - preferred (autre grade) : message content
     *  - neutral : remerciement neutre
     *  - hated : rejet clair (compagnon recule)
     */
    export function buildFeedMessage(params: {
      affinity: CropAffinity;
      grade: HarvestGrade;
      cropLabel: string;   // nom traduit du crop (ex: "fraise")
      cropEmoji?: string;  // emoji du crop pour préfixer si fourni
    }): string {
      const { affinity, grade, cropLabel, cropEmoji = '' } = params;
      const prefix = cropEmoji ? `${cropEmoji} ` : '';
      if (affinity === 'hated') {
        return `😖 Berk… je déteste ça.`;
      }
      if (affinity === 'preferred' && grade === 'perfect') {
        return `${prefix}✨ Ma ${cropLabel} préférée en version parfaite ! Je t'adore !`;
      }
      if (affinity === 'preferred') {
        return `${prefix}Mmm ma préférée, merci !`;
      }
      return `${prefix}Merci, c'était bon.`;
    }
    ```

    Ajouter dans `lib/__tests__/companion-feed.test.ts` :
    ```typescript
    import { buildFeedMessage } from '../mascot/companion-engine';

    describe('Phase 42 — buildFeedMessage', () => {
      it('preferred + perfect → mention parfait', () => {
        const msg = buildFeedMessage({ affinity: 'preferred', grade: 'perfect', cropLabel: 'fraise', cropEmoji: '🍓' });
        expect(msg).toContain('parfaite');
        expect(msg).toContain('fraise');
      });
      it('hated → Berk', () => {
        const msg = buildFeedMessage({ affinity: 'hated', grade: 'good', cropLabel: 'maïs', cropEmoji: '🌽' });
        expect(msg).toContain('Berk');
      });
      it('neutral → merci sobre', () => {
        expect(buildFeedMessage({ affinity: 'neutral', grade: 'good', cropLabel: 'blé', cropEmoji: '🌾' }))
          .toContain('Merci');
      });
      it('preferred non-perfect → préférée', () => {
        expect(buildFeedMessage({ affinity: 'preferred', grade: 'good', cropLabel: 'betterave', cropEmoji: '🫒' }))
          .toContain('préférée');
      });
    });
    ```
  </action>
  <verify>
    <automated>grep -q "export function buildFeedMessage" lib/mascot/companion-engine.ts &amp;&amp; grep -q "Berk" lib/mascot/companion-engine.ts &amp;&amp; grep -q "parfaite" lib/mascot/companion-engine.ts &amp;&amp; npx jest lib/__tests__/companion-feed.test.ts --no-coverage 2>&amp;1 | tail -5 | grep -q "passed"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "export function buildFeedMessage" lib/mascot/companion-engine.ts` réussit
    - `grep -q "buildFeedMessage" lib/__tests__/companion-feed.test.ts` réussit (≥4 nouveaux tests)
    - `npx jest lib/__tests__/companion-feed.test.ts --no-coverage` exit 0 (total ≥21 tests)
  </acceptance_criteria>
  <done>
    buildFeedMessage pur avec 4 variantes couvrant D-21, testé par Jest.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Étendre MascotteSnapshot avec feedBuffActive + buildFeedSpeechBubble + override (Option A locked)</name>
  <files>lib/mascotte-live-activity.ts</files>
  <read_first>
    - lib/mascotte-live-activity.ts (intégralité, ~155 lignes — confirmer signature actuelle patchMascotte L131 Partial<MascotteSnapshot>)
    - CONTEXT.md D-22, D-23, D-25
  </read_first>
  <behavior>
    - MascotteSnapshot a un champ optionnel `feedBuffActive?: { multiplier: number; expiresAtIso: string } | null`
    - buildFeedSpeechBubble({ multiplier, expiresAtIso }) retourne une string ≤44 chars format "Boosté ! +X% XP ⚡ (Ymin)"
    - Quand feedBuffActive présent ET speechBubble non-fourni, refreshMascotte override automatiquement speechBubble via buildFeedSpeechBubble
    - patchMascotte accepte feedBuffActive sans modification (Partial<MascotteSnapshot> couvre déjà)
  </behavior>
  <action>
    **1. Étendre l'interface `MascotteSnapshot`** (L26-45). Ajouter le champ juste avant la closing brace :
    ```typescript
    /** Phase 42 — Buff XP actif (D-22). Override speechBubble par défaut en "Boosté ! +X%". */
    feedBuffActive?: { multiplier: number; expiresAtIso: string } | null;
    ```

    **2. Ajouter helper buildFeedSpeechBubble** (exporté, avant `startMascotte` L75) :
    ```typescript
    /**
     * Phase 42 — Construit la bulle de dialogue Live Activity pour un buff actif (D-23).
     * Format ≤44 chars : "Boosté ! +X% XP ⚡ (Ymin)"
     */
    export function buildFeedSpeechBubble(
      buff: { multiplier: number; expiresAtIso: string },
      nowMs: number = Date.now(),
    ): string {
      const pct = Math.round((buff.multiplier - 1) * 100);
      const remainingMs = Math.max(0, new Date(buff.expiresAtIso).getTime() - nowMs);
      const min = Math.ceil(remainingMs / 60000);
      const label = `Boosté ! +${pct}% XP ⚡ (${min}min)`;
      return label.length <= 44 ? label : label.slice(0, 44);
    }
    ```

    **3. Modifier `refreshMascotte` (L103-125)** pour override speechBubble quand feedBuffActive présent et speechBubble non-fourni :
    ```typescript
    export async function refreshMascotte(snap: MascotteSnapshot): Promise<void> {
      if (Platform.OS !== 'ios') return;
      // Phase 42 — Override speechBubble si buff actif et non-fourni explicitement (D-23)
      const effectiveBubble =
        snap.speechBubble ??
        (snap.feedBuffActive ? buildFeedSpeechBubble(snap.feedBuffActive) : null);
      lastSnapshot = { ...snap, speechBubble: effectiveBubble };
      try {
        const active = await isMascotteActivityActive();
        if (!active) return;
        await updateMascotteActivity(
          snap.tasksDone,
          snap.tasksTotal,
          snap.xpGained,
          snap.currentMeal,
          snap.stageOverride ?? null,
          snap.companionSpriteBase64 ?? null,
          snap.bonusText ?? null,
          snap.nextTaskText ?? null,
          snap.nextTaskId ?? null,
          snap.nextRdvText ?? null,
          effectiveBubble,
        );
      } catch {
        // silencieux
      }
    }
    ```

    **4. Aligner `startMascotte` (L75-97)** avec le même override pour cohérence (si feedBuffActive fourni au démarrage) :
    ```typescript
    const effectiveBubble =
      snap.speechBubble ??
      (snap.feedBuffActive ? buildFeedSpeechBubble(snap.feedBuffActive) : null);
    lastSnapshot = { ...snap, speechBubble: effectiveBubble };
    // ... startMascotteActivity(...) avec effectiveBubble en dernier argument
    ```

    **5. `patchMascotte` L131-135 : pas de modification nécessaire.**
    Raison : la signature est `patch: Partial<MascotteSnapshot>` + `const merged = { ...lastSnapshot, ...patch }` + `return refreshMascotte(merged)`. Dès que `feedBuffActive` est une clé de `MascotteSnapshot` (Task 2 step 1), elle devient automatiquement acceptée par patchMascotte et propagée à refreshMascotte qui applique l'override. **Option A locked.**
  </action>
  <verify>
    <automated>grep -q "feedBuffActive" lib/mascotte-live-activity.ts &amp;&amp; grep -q "export function buildFeedSpeechBubble" lib/mascotte-live-activity.ts &amp;&amp; grep -q "Boosté" lib/mascotte-live-activity.ts &amp;&amp; grep -q "effectiveBubble" lib/mascotte-live-activity.ts &amp;&amp; npx tsc --noEmit 2>&amp;1 | grep "mascotte-live-activity.ts" ; test $? -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "feedBuffActive\\?:" lib/mascotte-live-activity.ts` réussit
    - `grep -q "export function buildFeedSpeechBubble" lib/mascotte-live-activity.ts` réussit
    - `grep -q "Boosté !" lib/mascotte-live-activity.ts` réussit
    - `grep -c "effectiveBubble" lib/mascotte-live-activity.ts` ≥ 2 (utilisé dans startMascotte ET refreshMascotte)
    - `patchMascotte` n'a PAS été modifié (signature Partial<MascotteSnapshot> inchangée — vérifier via `grep "patchMascotte(patch: Partial<MascotteSnapshot>)"` retourne 1 match)
    - `npx tsc --noEmit` pas de nouvelle erreur
  </acceptance_criteria>
  <done>
    MascotteSnapshot étendue, helper speechBubble disponible, refreshMascotte + startMascotte override cohérent, patchMascotte accepte feedBuffActive sans modification de signature (Option A locked).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Wirer message + Live Activity dans hooks/useVault.ts:feedCompanion (PAS dans tree.tsx)</name>
  <files>hooks/useVault.ts</files>
  <read_first>
    - hooks/useVault.ts (feedCompanion ajouté Plan 07 — zone L1894+ environ)
    - lib/mascot/companion-storage.ts (loadCompanionMessages + saveCompanionMessages signatures)
    - lib/mascotte-live-activity.ts (patchMascotte après Task 2)
    - lib/mascot/types.ts (CROP_CATALOG pour labelKey + emoji)
    - CONTEXT.md D-24
  </read_first>
  <behavior>
    - Après que feedCompanion a écrit farm-{id}.md avec succès (result.applied=true et write OK) :
      1. Push message contextualisé dans companion_messages via saveCompanionMessages
      2. patchMascotte({ feedBuffActive: result.newBuff ? {...} : null })
    - Les deux opérations sont fire-and-forget : si échec, feedCompanion retourne quand même result (l'écriture vault a réussi)
    - Aucune modification de tree.tsx — tout le wiring vit dans useVault
  </behavior>
  <action>
    **1. Imports à ajouter en haut de hooks/useVault.ts** (si absents) :
    ```typescript
    import { buildFeedMessage } from '../lib/mascot/companion-engine';
    import { loadCompanionMessages, saveCompanionMessages } from '../lib/mascot/companion-storage';
    import { patchMascotte } from '../lib/mascotte-live-activity';
    import { CROP_CATALOG } from '../lib/mascot/types';
    ```

    **2. Étendre la fonction `feedCompanion` ajoutée Plan 07** (insérer APRÈS le `await vaultRef.current.writeFile(...)` et AVANT le `setProfiles`, ou juste après `setProfiles`) :

    ```typescript
    // 4. Update local state (déjà Plan 07)
    setProfiles(prev => prev.map(p =>
      p.id === profileId ? { ...p, companion: result.updated } : p
    ));

    // 5. Phase 42 — Push message contextualisé + Live Activity (fire-and-forget)
    try {
      const cropDef = CROP_CATALOG.find(c => c.id === cropId);
      // labelKey format "farm.crop.carrot" → extrait "carrot" comme fallback label si i18n non dispo côté hook
      const cropLabel = cropDef?.labelKey?.replace(/^farm\.crop\./, '') ?? cropId;
      const msg = buildFeedMessage({
        affinity: result.affinity,
        grade,
        cropLabel,
        cropEmoji: cropDef?.emoji,
      });
      const existing = await loadCompanionMessages(profileId);
      // Garde les 5 derniers (pattern existant companion_storage)
      const updated = [...existing, {
        text: msg,
        event: `feed_${result.affinity}`,
        timestamp: new Date().toISOString(),
      }].slice(-5);
      await saveCompanionMessages(profileId, updated);
    } catch { /* non-critical */ }

    try {
      // Live Activity : patchMascotte merge feedBuffActive au lastSnapshot (Option A locked)
      await patchMascotte({
        feedBuffActive: result.newBuff
          ? { multiplier: result.newBuff.multiplier, expiresAtIso: result.newBuff.expiresAt }
          : null,
      });
    } catch { /* non-critical */ }

    return result;
    ```

    **Note importante signatures** :
    - `saveCompanionMessages(profileId, messages)` — vérifier la signature réelle dans `lib/mascot/companion-storage.ts`. Si l'API diffère (ex: `pushCompanionMessage(profileId, message)` singleton), adapter l'appel.
    - `loadCompanionMessages` — si absent, éventuellement remplacer par un set direct via `saveCompanionMessages(profileId, [msg])` si le storage gère déjà le ring buffer en interne.
    - Ces noms sont cités dans le CONTEXT.md code_context (L158) — les vérifier à l'implémentation.

    **Non-régression** :
    - Les 2 try/catch silencieux garantissent que l'échec de ces side-effects ne casse pas feedCompanion.
    - Le pattern est identique à ce que fait `openLootBox` et autres mutations du hook (fire-and-forget sur side-effects non critiques).
  </action>
  <verify>
    <automated>grep -q "buildFeedMessage" hooks/useVault.ts &amp;&amp; grep -q "patchMascotte" hooks/useVault.ts &amp;&amp; grep -q "saveCompanionMessages" hooks/useVault.ts &amp;&amp; grep -q "CROP_CATALOG" hooks/useVault.ts &amp;&amp; grep -q "feedBuffActive" hooks/useVault.ts &amp;&amp; npx tsc --noEmit 2>&amp;1 | grep -E "useVault.ts" | grep -v "MemoryEditor\\|cooklang" ; RC=$? ; test $RC -eq 1 || test $RC -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "buildFeedMessage" hooks/useVault.ts` réussit
    - `grep -q "patchMascotte" hooks/useVault.ts` réussit
    - `grep -q "saveCompanionMessages" hooks/useVault.ts` réussit
    - `grep -q "CROP_CATALOG" hooks/useVault.ts` réussit
    - `grep -q "feedBuffActive:" hooks/useVault.ts` réussit
    - `! grep -q "buildFeedMessage" "app/(tabs)/tree.tsx"` (wiring PAS dans tree.tsx — évite overlap Plan 08)
    - `! grep -q "patchMascotte" "app/(tabs)/tree.tsx"` (idem)
    - `npx tsc --noEmit` pas de NOUVELLE erreur dans useVault.ts
  </acceptance_criteria>
  <done>
    Après un feed, le message contextualisé est persisté et la Live Activity affiche la bulle "Boosté ! +X% XP" sur le lock screen — tout déclenché depuis le hook, zéro modif tree.tsx (overlap Plan 08 éliminé).
  </done>
</task>

</tasks>

<verification>
- `npx jest lib/__tests__/companion-feed.test.ts --no-coverage` vert (≥21 tests)
- `npx tsc --noEmit` sans nouvelle erreur
- Phase 42 COMPLÈTE : boucle feed end-to-end fonctionnelle (picker → engine → persist + harvest-1 + message + Live Activity → animation tree.tsx)
- **Pas de conflit d'édition avec Plan 08** : Plan 09 ne touche pas tree.tsx ; Plan 08 ne touche pas mascotte-live-activity.ts ni companion-engine.ts (buildFeedMessage)
</verification>

<success_criteria>
- Commit FR : `feat(42): messages feed + live activity speechBubble boost (wiring hook)`
- Phase livrée : bouton Nourrir → pick crop → anim + particules + haptics + message + lock screen updated
</success_criteria>

<output>
.planning/phases/42-nourrir-le-compagnon/42-09-SUMMARY.md — récap final Phase 42 :
- Liste des 31 décisions D-01..D-31 couvertes (mapping D → plan/task)
- Résultat final jest (total tests Phase 42)
- Liens vers SUMMARYs précédents
- Confirmation Option A Live Activity locked (patchMascotte signature inchangée)
- Flag milestone : Phase 42 shippable oui/non
</output>
