---
phase: 42-nourrir-le-compagnon
plan: 05
type: execute
wave: 3
depends_on: [42-03, 42-04]
files_modified:
  - components/mascot/CompanionCard.tsx
autonomous: true
requirements: [FEED-09, FEED-10]
must_haves:
  truths:
    - "CompanionCard affiche avatar + nom + espèce + stade du compagnon"
    - "CompanionCard affiche un chip buff actif (✨ +X% XP · Ymin) si getActiveFeedBuff retourne un buff"
    - "CompanionCard affiche un bouton primaire 'Nourrir' (ou 'Rassasié · Xh Ym' si cooldown)"
    - "CompanionCard affiche un bouton secondaire 'Changer d'espèce' qui déclenche CompanionPicker (déjà Modal-self-rendering)"
    - "Utilise useThemeColors() — jamais de hardcoded"
    - "Appel CompanionPicker conforme à la signature réelle : { visible, onClose, onSelect, unlockedSpecies, isInitialChoice }"
  artifacts:
    - path: "components/mascot/CompanionCard.tsx"
      provides: "Carte compagnon refondue avec actions Nourrir/Changer espèce"
      min_lines: 150
  key_links:
    - from: "CompanionCard"
      to: "CompanionPicker (sous-composant inchangé)"
      via: "rendu conditionnel — CompanionPicker contient déjà son propre Modal"
      pattern: "CompanionPicker"
---

<objective>
Créer `CompanionCard.tsx` — nouveau composant qui englobe l'ancien `CompanionPicker.tsx` et expose deux actions : "Nourrir" (primaire, ouvre FeedPicker Plan 06) et "Changer d'espèce" (secondaire, ouvre CompanionPicker existant).

Purpose: D-26 (CompanionCard), D-27 (CompanionPicker conservé comme sous-composant).
Output: 1 composant React Native réutilisable avec props claires + typage TS, intégrant le CompanionPicker existant sans briser sa signature actuelle.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/42-nourrir-le-compagnon/42-CONTEXT.md
@components/mascot/CompanionPicker.tsx
@components/mascot/CompanionAvatarMini.tsx
@lib/mascot/companion-engine.ts
@lib/mascot/companion-types.ts
@CLAUDE.md
</context>

<interfaces>
**Signature RÉELLE du CompanionPicker existant** (lue dans components/mascot/CompanionPicker.tsx L49-55) :
```typescript
interface CompanionPickerProps {
  visible: boolean;
  onClose: () => void;
  onSelect: (species: CompanionSpecies, name: string) => void;
  unlockedSpecies: CompanionSpecies[];
  isInitialChoice: boolean;
}
```

**Points importants :**
- `CompanionPicker` rend déjà son propre `<Modal presentationStyle="pageSheet">` (L98-102). **NE PAS le wrapper dans un autre Modal** — juste le rendre conditionnellement.
- Pas de props `currentSpecies`, `companionName` — elles n'existent pas dans la vraie signature.
- `isInitialChoice` : `false` pour le switch d'espèce (cas CompanionCard). `true` uniquement pour le tout premier choix.

**CompanionCard — signature proposée :**
```typescript
interface CompanionCardProps {
  /** Profil actif avec companion */
  companion: CompanionData;
  /** Niveau du profil (pour afficher le stade) */
  level: number;
  /** Callback pour ouvrir le FeedPicker — plan 06 */
  onPressFeed: () => void;
  /** Callback pour confirmer un changement d'espèce (réutilisé de CompanionPicker) */
  onSelectSpecies: (species: CompanionSpecies, name: string) => void | Promise<void>;
}
```
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Créer CompanionCard.tsx avec buff chip + boutons Nourrir/Changer</name>
  <files>components/mascot/CompanionCard.tsx</files>
  <read_first>
    - components/mascot/CompanionPicker.tsx L49-110 (signature EXACTE : { visible, onClose, onSelect, unlockedSpecies, isInitialChoice }, et le Modal self-rendering L97-103)
    - components/mascot/CompanionAvatarMini.tsx (pattern avatar compact — vérifier les props acceptées, notamment si `stage` existe)
    - lib/mascot/companion-engine.ts (getActiveFeedBuff + getCompanionStage)
    - lib/mascot/companion-types.ts (FEED_COOLDOWN_MS + getCooldownRemainingMs + types)
    - CLAUDE.md (conventions FR, useThemeColors, Haptics patterns)
  </read_first>
  <behavior>
    - Rendu avatar + nom compagnon + label espèce + stade (calculé via getCompanionStage(level))
    - Si getActiveFeedBuff retourne un buff : afficher chip "✨ +{((mul-1)*100).toFixed(0)}% XP · {Math.ceil((expiresAt-now)/60000)}min"
    - Si getCooldownRemainingMs > 0 : bouton primaire disabled affiche "😋 Rassasié · {formatted HhMm}"
    - Sinon : bouton primaire actif "🥕 Nourrir" → appelle onPressFeed()
    - Bouton secondaire "🔄 Changer d'espèce" → setShowPicker(true), CompanionPicker rendu avec sa propre Modal
    - CompanionPicker reçoit UNIQUEMENT ses 5 props réelles : visible, onClose, onSelect, unlockedSpecies, isInitialChoice={false}
    - Haptics.selectionAsync() sur tap bouton
    - Refresh visuel du cooldown chaque 30s via setInterval
  </behavior>
  <action>
    Créer `components/mascot/CompanionCard.tsx`. Structure :

    ```typescript
    /**
     * CompanionCard.tsx — Carte compagnon avec actions Nourrir + Changer espèce (Phase 42 D-26)
     *
     * Englobe le CompanionPicker existant comme sous-composant "Changer espèce".
     * Action primaire : Nourrir (déclenche FeedPicker — callback onPressFeed).
     * Affiche le buff XP actif si présent, et le cooldown si pas prêt.
     *
     * IMPORTANT : CompanionPicker rend déjà son propre Modal pageSheet, donc on ne le wrapper PAS dans un autre Modal.
     */

    import React, { useState, useEffect, useCallback, useMemo } from 'react';
    import { View, Text, Pressable, StyleSheet } from 'react-native';
    import * as Haptics from 'expo-haptics';
    import { useTranslation } from 'react-i18next';
    import { useThemeColors } from '../../contexts/ThemeContext';
    import { FontSize, FontWeight } from '../../constants/typography';
    import { Spacing, Radius } from '../../constants/spacing';
    import { CompanionPicker } from './CompanionPicker';
    import { CompanionAvatarMini } from './CompanionAvatarMini';
    import {
      getCompanionStage,
      getActiveFeedBuff,
    } from '../../lib/mascot/companion-engine';
    import {
      getCooldownRemainingMs,
      type CompanionData,
      type CompanionSpecies,
    } from '../../lib/mascot/companion-types';

    interface CompanionCardProps {
      companion: CompanionData;
      level: number;
      onPressFeed: () => void;
      onSelectSpecies: (species: CompanionSpecies, name: string) => void | Promise<void>;
    }

    /** Formate un cooldown ms → "Xh Ym" ou "Ymin" */
    function formatCooldown(ms: number): string {
      const totalMin = Math.ceil(ms / 60000);
      const h = Math.floor(totalMin / 60);
      const m = totalMin % 60;
      if (h > 0) return `${h}h ${m.toString().padStart(2, '0')}`;
      return `${m}min`;
    }

    /** Formate un buff restant ms → "Xmin" ou "Xh Ym" */
    function formatBuffRemaining(expiresAtIso: string, now: number): string {
      const exp = new Date(expiresAtIso).getTime();
      const remaining = Math.max(0, exp - now);
      return formatCooldown(remaining);
    }

    export function CompanionCard({
      companion,
      level,
      onPressFeed,
      onSelectSpecies,
    }: CompanionCardProps) {
      const { t } = useTranslation();
      const { colors, primary } = useThemeColors();
      const [showPicker, setShowPicker] = useState(false);
      const [tick, setTick] = useState(0);

      // Refresh toutes les 30s pour mettre à jour cooldown/buff affiché
      useEffect(() => {
        const id = setInterval(() => setTick(t => t + 1), 30_000);
        return () => clearInterval(id);
      }, []);

      const now = Date.now();
      const cooldownMs = useMemo(
        () => getCooldownRemainingMs(companion.lastFedAt, now),
        [companion.lastFedAt, tick],
      );
      const activeBuff = useMemo(
        () => getActiveFeedBuff(companion, now),
        [companion.feedBuff, tick],
      );
      const stage = useMemo(() => getCompanionStage(level), [level]);

      const handleFeed = useCallback(() => {
        if (cooldownMs > 0) return;
        Haptics.selectionAsync().catch(() => {});
        onPressFeed();
      }, [cooldownMs, onPressFeed]);

      const handleChange = useCallback(() => {
        Haptics.selectionAsync().catch(() => {});
        setShowPicker(true);
      }, []);

      const handleSelectSpecies = useCallback(
        async (species: CompanionSpecies, name: string) => {
          await onSelectSpecies(species, name);
          setShowPicker(false);
        },
        [onSelectSpecies],
      );

      const feedDisabled = cooldownMs > 0;
      const buffPercent = activeBuff ? Math.round((activeBuff.multiplier - 1) * 100) : 0;

      return (
        <View style={[styles.card, { backgroundColor: colors.surface ?? colors.card, borderColor: colors.border }]}>
          {/* Avatar + infos */}
          <View style={styles.header}>
            <CompanionAvatarMini species={companion.activeSpecies} stage={stage} size={56} />
            <View style={styles.info}>
              <Text style={[styles.name, { color: colors.text }]}>
                {companion.name || t(`companion.species.${companion.activeSpecies}`)}
              </Text>
              <Text style={[styles.meta, { color: colors.textMuted ?? colors.textFaint }]}>
                {t(`companion.species.${companion.activeSpecies}`)} · {t(`companion.stage.${stage}`)}
              </Text>
            </View>
          </View>

          {/* Chip buff actif */}
          {activeBuff && (
            <View style={[styles.buffChip, { backgroundColor: primary + '22', borderColor: primary }]}>
              <Text style={[styles.buffText, { color: primary }]}>
                ✨ +{buffPercent}% XP · {formatBuffRemaining(activeBuff.expiresAt, now)}
              </Text>
            </View>
          )}

          {/* Boutons */}
          <View style={styles.actions}>
            <Pressable
              onPress={handleFeed}
              disabled={feedDisabled}
              style={({ pressed }) => [
                styles.btnPrimary,
                {
                  backgroundColor: feedDisabled ? colors.border : primary,
                  opacity: pressed ? 0.85 : 1,
                },
              ]}
            >
              <Text style={[styles.btnPrimaryText, { color: colors.bg }]}>
                {feedDisabled
                  ? `😋 Rassasié · ${formatCooldown(cooldownMs)}`
                  : '🥕 Nourrir'}
              </Text>
            </Pressable>
            <Pressable
              onPress={handleChange}
              style={({ pressed }) => [
                styles.btnSecondary,
                { borderColor: colors.border, opacity: pressed ? 0.85 : 1 },
              ]}
            >
              <Text style={[styles.btnSecondaryText, { color: colors.text }]}>
                🔄 Changer d'espèce
              </Text>
            </Pressable>
          </View>

          {/* Sous-composant CompanionPicker — rend son propre Modal pageSheet.
              Signature RÉELLE : { visible, onClose, onSelect, unlockedSpecies, isInitialChoice }.
              On NE PAS wrapper dans un autre Modal (double sheet gotcha). */}
          <CompanionPicker
            visible={showPicker}
            onClose={() => setShowPicker(false)}
            onSelect={handleSelectSpecies}
            unlockedSpecies={companion.unlockedSpecies}
            isInitialChoice={false}
          />
        </View>
      );
    }

    const styles = StyleSheet.create({
      card: {
        borderRadius: Radius.lg,
        borderWidth: 1,
        padding: Spacing.lg,
        gap: Spacing.md,
      },
      header: {
        flexDirection: 'row',
        alignItems: 'center',
        gap: Spacing.md,
      },
      info: { flex: 1 },
      name: {
        fontSize: FontSize.lg,
        fontWeight: FontWeight.bold,
      },
      meta: {
        fontSize: FontSize.sm,
        marginTop: 2,
      },
      buffChip: {
        alignSelf: 'flex-start',
        paddingHorizontal: Spacing.md,
        paddingVertical: Spacing.xs,
        borderRadius: Radius.full,
        borderWidth: 1,
      },
      buffText: {
        fontSize: FontSize.sm,
        fontWeight: FontWeight.semiBold,
      },
      actions: {
        gap: Spacing.sm,
      },
      btnPrimary: {
        paddingVertical: Spacing.md,
        borderRadius: Radius.md,
        alignItems: 'center',
      },
      btnPrimaryText: {
        fontSize: FontSize.md,
        fontWeight: FontWeight.bold,
      },
      btnSecondary: {
        paddingVertical: Spacing.sm,
        borderRadius: Radius.md,
        alignItems: 'center',
        borderWidth: 1,
      },
      btnSecondaryText: {
        fontSize: FontSize.sm,
        fontWeight: FontWeight.medium,
      },
    });
    ```

    **Pitfalls résolus a priori (signature CompanionPicker confirmée) :**
    - Props CompanionPicker : `{ visible, onClose, onSelect, unlockedSpecies, isInitialChoice }` — pas d'autres props (currentSpecies/companionName n'existent pas).
    - CompanionPicker rend son propre `<Modal presentationStyle="pageSheet">` → NE PAS le wrapper.
    - `CompanionAvatarMini` : vérifier la prop `stage` lors de la lecture. Si elle n'existe pas, fallback en passant uniquement `species` (adapter le JSX).
    - `colors.surface`/`colors.textMuted` : fallbacks inline `?? colors.card` / `?? colors.textFaint` pour robustesse thème.
    - Clés i18n `companion.species.*` et `companion.stage.*` : existent déjà (utilisées par CompanionPicker) — OK.

    **Note** : comme CompanionPicker rend son propre Modal, il n'y a PAS de `presentationStyle="pageSheet"` directement dans CompanionCard. Le grep de verify cible doit donc être adapté (voir verify ci-dessous).
  </action>
  <verify>
    <automated>test -f components/mascot/CompanionCard.tsx &amp;&amp; grep -q "export function CompanionCard" components/mascot/CompanionCard.tsx &amp;&amp; grep -q "getActiveFeedBuff" components/mascot/CompanionCard.tsx &amp;&amp; grep -q "getCooldownRemainingMs" components/mascot/CompanionCard.tsx &amp;&amp; grep -q "CompanionPicker" components/mascot/CompanionCard.tsx &amp;&amp; grep -q "isInitialChoice={false}" components/mascot/CompanionCard.tsx &amp;&amp; grep -q "useThemeColors" components/mascot/CompanionCard.tsx &amp;&amp; grep -q "Haptics.selectionAsync" components/mascot/CompanionCard.tsx &amp;&amp; npx tsc --noEmit 2>&amp;1 | grep "CompanionCard.tsx" ; test $? -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - `test -f components/mascot/CompanionCard.tsx` réussit
    - `grep -q "export function CompanionCard" components/mascot/CompanionCard.tsx` réussit
    - `grep -q "isInitialChoice={false}" components/mascot/CompanionCard.tsx` réussit (prop réelle passée)
    - `grep -q "unlockedSpecies={companion.unlockedSpecies}" components/mascot/CompanionCard.tsx` réussit
    - **CompanionPicker invoqué avec UNIQUEMENT les 5 props réelles** : visible, onClose, onSelect, unlockedSpecies, isInitialChoice
    - `! grep -q "currentSpecies=" components/mascot/CompanionCard.tsx` (prop inventée supprimée)
    - `! grep -q "companionName={companion.name}" components/mascot/CompanionCard.tsx` (prop inventée supprimée)
    - `grep -q "useThemeColors" components/mascot/CompanionCard.tsx` réussit
    - `grep -q "onPressFeed" components/mascot/CompanionCard.tsx` réussit
    - `grep -q "getActiveFeedBuff" components/mascot/CompanionCard.tsx` réussit
    - `grep -q "getCooldownRemainingMs" components/mascot/CompanionCard.tsx` réussit
    - `wc -l components/mascot/CompanionCard.tsx` retourne ≥150
    - `npx tsc --noEmit` pas d'erreur dans CompanionCard.tsx
  </acceptance_criteria>
  <done>
    CompanionCard rend correctement un compagnon avec ses 2 actions. CompanionPicker invoqué conforme à sa vraie signature (5 props, pas de wrap Modal). Thème cohérent. Aucune hardcoded color.
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` sans nouvelle erreur
- Manuel (sera testé Plan 07) : le composant s'affiche sans crash et ouvre CompanionPicker sans double-Modal
</verification>

<success_criteria>
- Commit FR : `feat(42): composant CompanionCard (nourrir + changer espèce)`
</success_criteria>

<output>
.planning/phases/42-nourrir-le-compagnon/42-05-SUMMARY.md avec signatures props réelles CompanionPicker confirmées, ajustements CompanionAvatarMini.stage si fallback appliqué.
</output>
