---
phase: 30
plan: 02
type: execute
wave: 2
depends_on: [30-01]
autonomous: true
requirements: [VILL-04, VILL-05]
files_modified:
  - hooks/useGarden.ts
  - lib/__tests__/village-parser.test.ts

must_haves:
  truths:
    - "useGarden exposes familyLifetimeLeaves computed as sum of profiles[].points"
    - "useGarden automatically appends new building to vault when a palier is crossed"
    - "Unlock effect is idempotent — no infinite loop even after writing gardenRaw"
    - "profile.points undefined is safely handled (?? 0) — no crash for grossesse profiles"
    - "useGarden exposes unlockedBuildings to downstream consumers"
  artifacts:
    - path: "hooks/useGarden.ts"
      provides: "familyLifetimeLeaves memo + unlock-on-threshold effect + exposed unlockedBuildings"
      contains: "familyLifetimeLeaves"
  key_links:
    - from: "hooks/useGarden.ts"
      to: "lib/village/catalog.ts"
      via: "import computeBuildingsToUnlock + BUILDINGS_CATALOG"
      pattern: "computeBuildingsToUnlock"
    - from: "hooks/useGarden.ts"
      to: "lib/village/parser.ts"
      via: "import appendBuildingToVault"
      pattern: "appendBuildingToVault"
    - from: "hooks/useGarden.ts effet"
      to: "vault.writeFile(VILLAGE_FILE)"
      via: "appendBuildingToVault → setGardenRaw"
      pattern: "setGardenRaw"
---

<objective>
Brancher le moteur de déblocage automatique des bâtiments dans useGarden : détecter quand familyLifetimeLeaves franchit un palier et appeler appendBuildingToVault, avec idempotence stricte pour éviter la boucle infinie.

Purpose : Sans cette couche, les utilisateurs peuvent gagner 25000 feuilles famille sans qu'aucun bâtiment n'apparaisse. L'effet réactif garantit qu'à chaque changement de profile.points, les nouveaux paliers franchis sont persistés automatiquement dans jardin-familial.md.

Output :
- hooks/useGarden.ts étendu avec : `familyLifetimeLeaves`, effet unlock-on-threshold, exposition `unlockedBuildings` et `familyLifetimeLeaves` dans le return
- Tests pour `computeBuildingsToUnlock` déjà couverts Plan 01 — ce plan ajoute des tests de résilience (profil sans points, idempotence)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-decorations-persistantes/30-CONTEXT.md
@.planning/phases/30-decorations-persistantes/30-RESEARCH.md
@.planning/phases/30-decorations-persistantes/30-01-PLAN.md
@CLAUDE.md

# Reference implementations — MUST READ
@hooks/useGarden.ts
@lib/village/parser.ts
@lib/village/catalog.ts
@lib/village/types.ts
@contexts/VaultContext.tsx

<interfaces>
<!-- After Plan 01 completes, these are available: -->

From lib/village/catalog.ts (Plan 01):
```typescript
export interface BuildingCatalogEntry {
  id: string;
  labelFR: string;
  palier: number;
  sprite: ReturnType<typeof require>;
}
export const BUILDINGS_CATALOG: BuildingCatalogEntry[];
export function computeBuildingsToUnlock(
  familyLifetimeLeaves: number,
  alreadyUnlocked: UnlockedBuilding[],
): BuildingCatalogEntry[];
```

From lib/village/parser.ts (Plan 01):
```typescript
export async function appendBuildingToVault(
  vault: VaultManager,
  entry: UnlockedBuilding,
): Promise<void>;
```

From lib/village/types.ts (Plan 01):
```typescript
export interface UnlockedBuilding {
  timestamp: string;
  buildingId: string;
  palier: number;
}
// VillageData now has: unlockedBuildings: UnlockedBuilding[];
```

From hooks/useGarden.ts (current state — Phase 26):
```typescript
const { vault, gardenRaw, setGardenRaw, profiles } = useVault();
const gardenData = useMemo(() => parseGardenFile(gardenRaw), [gardenRaw]);
// profiles is Profile[] — each profile has profile.points: number (XP lifetime, monotone croissant)
// profile.points can be undefined for newly-created / grossesse profiles → use ?? 0
```

From contexts/VaultContext.tsx (current — what useVault returns):
```typescript
// vault: VaultManager
// gardenRaw: string
// setGardenRaw: Dispatch<SetStateAction<string>>
// profiles: Profile[]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 — familyLifetimeLeaves + unlock effect dans useGarden</name>
  <files>hooks/useGarden.ts</files>

  <read_first>
    - hooks/useGarden.ts (current state — wire into the existing flow, don't rewrite the file)
    - lib/village/parser.ts (après Plan 01 — vérifier signatures appendBuildingToVault)
    - lib/village/catalog.ts (après Plan 01 — BUILDINGS_CATALOG + computeBuildingsToUnlock)
    - lib/village/types.ts (après Plan 01 — UnlockedBuilding type)
    - .planning/phases/30-decorations-persistantes/30-RESEARCH.md (Pattern 2, Unlock Engine Design, Pitfall 1 — infinite loop)
    - .planning/phases/30-decorations-persistantes/30-CONTEXT.md (D-05, D-08, D-23, D-24)
  </read_first>

  <behavior>
    - `familyLifetimeLeaves` est un useMemo dépendant de `profiles` : `profiles.reduce((sum, p) => sum + (p.points ?? 0), 0)`
    - Le useEffect unlock-on-threshold se déclenche au mount ET à chaque changement de `familyLifetimeLeaves` OU `gardenData.unlockedBuildings`
    - Early return si `toUnlock.length === 0` → pas d'écriture → pas de changement gardenRaw → pas de boucle infinie
    - Si plusieurs paliers franchis en même temps (ex: utilisateur gagne 500 feuilles d'un coup passant de 0 à 500) : appel appendBuilding successifs en cascade sur currentContent qui est mis à jour localement entre chaque append, puis un seul writeFile final
    - Erreurs silencieuses : `catch { /* Constructions — non-critical */ }`
    - `UseGardenReturn` étendu avec `familyLifetimeLeaves: number` et `unlockedBuildings: UnlockedBuilding[]`
    - Profils avec `statut === 'grossesse'` OU `points === undefined` ne crashent pas (déjà géré par `?? 0`)
  </behavior>

  <action>
    1. **Imports** — En haut de `hooks/useGarden.ts`, étendre les imports existants depuis `../lib/village` :
       ```typescript
       import {
         parseGardenFile,
         serializeGardenFile,
         appendContributionToVault,
         appendBuildingToVault,              // NEW
         VILLAGE_FILE,
         computeWeekTarget,
         OBJECTIVE_TEMPLATES,
         BUILDINGS_CATALOG,                   // NEW
         computeBuildingsToUnlock,            // NEW
       } from '../lib/village';
       ```
       Et étendre les types importés :
       ```typescript
       import type {
         VillageData,
         VillageWeekRecord,
         ContributionType,
         ObjectiveTemplate,
         UnlockedBuilding,                    // NEW
       } from '../lib/village';
       ```

    2. **UseGardenReturn interface** — Ajouter deux champs :
       ```typescript
       export interface UseGardenReturn {
         gardenData: VillageData;
         currentTarget: number;
         progress: number;
         isGoalReached: boolean;
         currentTemplate: ObjectiveTemplate;
         addContribution: (type: ContributionType, profileId: string) => Promise<void>;
         claimReward: (profileId: string) => Promise<boolean>;
         weekHistory: VillageWeekRecord[];
         isLoading: boolean;
         // Phase 30 — bâtiments persistants (VILL-04, VILL-05)
         familyLifetimeLeaves: number;
         unlockedBuildings: UnlockedBuilding[];
       }
       ```

    3. **useMemo familyLifetimeLeaves** — Ajouter ce memo juste après `activeProfileCount` (environ ligne 91) :
       ```typescript
       /**
        * Somme des profile.points sur tous les profils (feuilles lifetime famille).
        * Per D-05 CONTEXT.md. Monotone croissant — `points` est XP lifetime, jamais déduit.
        * `p.points ?? 0` pour profils grossesse / nouvellement créés (Pitfall 7).
        */
       const familyLifetimeLeaves = useMemo(
         () => profiles.reduce((sum, p) => sum + (p.points ?? 0), 0),
         [profiles],
       );
       ```

    4. **useMemo unlockedBuildings** — Ajouter ce memo juste après `weekHistory` :
       ```typescript
       /** Bâtiments déjà débloqués (parsés depuis jardin-familial.md) */
       const unlockedBuildings = useMemo(
         () => gardenData.unlockedBuildings ?? [],
         [gardenData],
       );
       ```

    5. **useEffect unlock-on-threshold** — Ajouter cet effet après les autres effets existants (mais AVANT le `return` du hook). Pattern exact de RESEARCH.md Pattern 2 :
       ```typescript
       // -----------------------------------------------------------------------
       // Phase 30 — Effet unlock-on-threshold (VILL-04, VILL-05)
       // -----------------------------------------------------------------------
       // Détecte quand familyLifetimeLeaves franchit un palier catalogue et
       // appending le bâtiment correspondant dans jardin-familial.md (append-only).
       //
       // Idempotence : computeBuildingsToUnlock retourne [] si tout est déjà
       // débloqué → early return, pas d'écriture, pas de boucle infinie (Pitfall 1).
       useEffect(() => {
         if (!vault) return;
         const toUnlock = computeBuildingsToUnlock(familyLifetimeLeaves, unlockedBuildings);
         if (toUnlock.length === 0) return;

         let cancelled = false;
         (async () => {
           try {
             // Lecture + cascade d'appends sur currentContent local pour éviter
             // N writeFile en cas de franchissement de plusieurs paliers simultanés.
             let currentContent = await vault.readFile(VILLAGE_FILE).catch(() => '');
             const now = new Date().toISOString();
             for (const entry of toUnlock) {
               const building: UnlockedBuilding = {
                 timestamp: now,
                 buildingId: entry.id,
                 palier: entry.palier,
               };
               // Utilise appendBuilding (pur) en cascade pour capturer tous les ajouts
               // dans une seule écriture finale. Import appendBuilding si nécessaire.
               const { appendBuilding } = await import('../lib/village/parser');
               currentContent = appendBuilding(currentContent, building);
             }
             if (cancelled) return;
             await vault.writeFile(VILLAGE_FILE, currentContent);
             setGardenRaw(currentContent);
           } catch {
             /* Constructions — non-critical */
           }
         })();

         return () => { cancelled = true; };
       }, [vault, familyLifetimeLeaves, unlockedBuildings, setGardenRaw]);
       ```

       **Note sur l'import dynamique** : `import('../lib/village/parser')` est utilisé ici pour appeler directement `appendBuilding` (pur) plutôt que `appendBuildingToVault` (qui re-read+re-write à chaque appel). Si l'executor préfère, alternative équivalente : importer `appendBuilding` en haut du fichier de manière statique (import direct depuis `'../lib/village'` — vérifier que index.ts l'exporte, sinon ajouter l'export) et supprimer l'import dynamique. **Préférer l'import statique** pour la clarté. Action finale : ajouter `appendBuilding` aux imports du haut du fichier et retirer le `await import(...)` du bloc async.

    6. **Return étendu** — Dans le `return` de useGarden, ajouter les deux nouveaux champs :
       ```typescript
       return {
         gardenData,
         currentTarget,
         progress,
         isGoalReached,
         currentTemplate,
         addContribution,
         claimReward,
         weekHistory,
         isLoading,
         // Phase 30
         familyLifetimeLeaves,
         unlockedBuildings,
       };
       ```

    7. **Vérification exports** — Vérifier que `appendBuilding` est bien exporté depuis `lib/village/index.ts` (devrait l'être via `export * from './parser'` existant, mais double-check). Si absent, l'ajouter.
  </action>

  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n "familyLifetimeLeaves" hooks/useGarden.ts` matches au moins 4 fois (interface, useMemo, dependency, return)
    - `grep -n "computeBuildingsToUnlock" hooks/useGarden.ts` matches (import + call dans l'effet)
    - `grep -n "appendBuilding" hooks/useGarden.ts` matches (import + call dans l'effet)
    - `grep -n "p.points ?? 0" hooks/useGarden.ts` matches (profil sans points safe)
    - `grep -n "Constructions — non-critical" hooks/useGarden.ts` matches (catch silencieux)
    - `grep -n "unlockedBuildings: UnlockedBuilding\[\]" hooks/useGarden.ts` matches (interface return)
    - `grep -n "if (toUnlock.length === 0) return" hooks/useGarden.ts` matches (early return idempotence — Pitfall 1)
    - `grep -n "import type.*UnlockedBuilding" hooks/useGarden.ts` matches
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>

  <done>
    useGarden expose `familyLifetimeLeaves` (sum profile.points) et `unlockedBuildings`, l'effet unlock-on-threshold déclenche automatiquement les appends via `appendBuilding` en cascade locale puis un seul `writeFile + setGardenRaw`, avec idempotence garantie par early return, et catch silencieux non-critique. TypeScript compile sans erreur.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 — Tests résilience unlock engine</name>
  <files>lib/__tests__/village-parser.test.ts</files>

  <read_first>
    - lib/__tests__/village-parser.test.ts (voir la structure des describes existants — ajouter un nouveau bloc)
    - lib/village/catalog.ts (Plan 01 — BUILDINGS_CATALOG + computeBuildingsToUnlock)
    - .planning/phases/30-decorations-persistantes/30-RESEARCH.md (Pitfall 7 — profile sans points)
  </read_first>

  <behavior>
    - Test : computeBuildingsToUnlock avec exactement le palier frontière (familyLifetimeLeaves === palier) → débloque
    - Test : computeBuildingsToUnlock avec palier - 1 → ne débloque pas
    - Test : computeBuildingsToUnlock idempotent sur plusieurs appels consécutifs avec le résultat de l'appel précédent ajouté à alreadyUnlocked
    - Test : l'ordre de BUILDINGS_CATALOG garantit que le résultat est toujours dans l'ordre narratif (puits d'abord, bibliothèque en dernier)
  </behavior>

  <action>
    Ajouter ce bloc describe dans lib/__tests__/village-parser.test.ts, après le bloc `describe('computeBuildingsToUnlock (Phase 30)')` créé en Plan 01 Task 1 (ou créer si absent) :

    ```typescript
    describe('computeBuildingsToUnlock — résilience (Phase 30)', () => {
      it('débloque exactement à la frontière du palier (familyLifetimeLeaves === palier)', () => {
        expect(computeBuildingsToUnlock(100, []).map(r => r.id)).toContain('puits');
        expect(computeBuildingsToUnlock(300, []).map(r => r.id)).toContain('boulangerie');
        expect(computeBuildingsToUnlock(25000, []).map(r => r.id)).toContain('bibliotheque');
      });

      it('ne débloque pas à palier - 1', () => {
        expect(computeBuildingsToUnlock(99, []).map(r => r.id)).not.toContain('puits');
        expect(computeBuildingsToUnlock(299, []).map(r => r.id)).not.toContain('boulangerie');
      });

      it('idempotent sur appels consécutifs — pas de double déblocage', () => {
        let unlocked: UnlockedBuilding[] = [];
        // Premier appel à 1500 feuilles — débloque puits, boulangerie, marché, café
        const first = computeBuildingsToUnlock(1500, unlocked);
        unlocked = [
          ...unlocked,
          ...first.map(e => ({ timestamp: '2026-04-12T00:00:00', buildingId: e.id, palier: e.palier })),
        ];
        expect(first).toHaveLength(4);

        // Deuxième appel à 1500 feuilles avec les précédents dans alreadyUnlocked
        const second = computeBuildingsToUnlock(1500, unlocked);
        expect(second).toHaveLength(0); // Idempotence stricte
      });

      it('retourne les bâtiments dans l ordre narratif BUILDINGS_CATALOG', () => {
        const result = computeBuildingsToUnlock(25000, []);
        expect(result.map(r => r.id)).toEqual([
          'puits', 'boulangerie', 'marche', 'cafe', 'forge', 'moulin', 'port', 'bibliotheque'
        ]);
      });

      it('gère un profil avec points undefined sans crash (simulation Pitfall 7)', () => {
        // Simulation du reduce dans useGarden avec profile.points undefined
        const profiles = [
          { points: 500 },
          { points: undefined as number | undefined },
          { points: 1000 },
        ];
        const sum = profiles.reduce((acc, p) => acc + (p.points ?? 0), 0);
        expect(sum).toBe(1500);
        const result = computeBuildingsToUnlock(sum, []);
        expect(result.map(r => r.id)).toEqual(['puits', 'boulangerie', 'marche', 'cafe']);
      });
    });
    ```

    **Note** : si le describe `computeBuildingsToUnlock (Phase 30)` du Plan 01 a déjà tous ces cas, merger plutôt que dupliquer — éviter les tests redondants, mais garder tous les cas de cette liste distincts.

    Vérifier que l'import de `UnlockedBuilding` existe en haut du fichier de test :
    ```typescript
    import type { UnlockedBuilding } from '../village/types';
    ```
  </action>

  <verify>
    <automated>npx jest --no-coverage lib/__tests__/village-parser.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n "computeBuildingsToUnlock — résilience" lib/__tests__/village-parser.test.ts` matches
    - `grep -n "débloque exactement à la frontière" lib/__tests__/village-parser.test.ts` matches
    - `grep -n "ne débloque pas à palier - 1" lib/__tests__/village-parser.test.ts` matches
    - `grep -n "profil avec points undefined" lib/__tests__/village-parser.test.ts` matches
    - `npx jest --no-coverage lib/__tests__/village-parser.test.ts` exits 0 with all tests passing
  </acceptance_criteria>

  <done>
    Cinq nouveaux tests résilience ajoutés : frontière exacte, palier-1, idempotence consecutive, ordre narratif, profile.points undefined. Tous passent.
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` — aucune erreur TypeScript
- `npx jest --no-coverage lib/__tests__/village-parser.test.ts` — tous les tests passent
- Smoke test manuel prévu en Plan 03 : ouvrir l'app, vérifier logs no-crash au mount de village.tsx (hook useGarden s'exécute, effet unlock-on-threshold se déclenche)
</verification>

<success_criteria>
- VILL-04 partiellement couvert : le moteur de déclenchement est câblé (la persistance déclenche l'apparition visuelle, qui sera branchée en Plan 03)
- VILL-05 partiellement couvert : la persistance append-only fonctionne end-to-end (hook + parser + vault)
- Idempotence stricte garantie : pas de boucle infinie, pas de double déblocage
- Plan 03 (UI) peut consommer : `useGarden().familyLifetimeLeaves`, `useGarden().unlockedBuildings`
</success_criteria>

<output>
After completion, create `.planning/phases/30-decorations-persistantes/30-02-SUMMARY.md`
</output>
