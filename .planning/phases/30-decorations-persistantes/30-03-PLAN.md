---
phase: 30
plan: 03
type: execute
wave: 3
depends_on: [30-01, 30-02]
autonomous: false
requirements: [VILL-04, VILL-06]
files_modified:
  - components/village/BuildingSprite.tsx
  - components/village/BuildingTooltip.tsx
  - components/village/BuildingsCatalog.tsx
  - app/(tabs)/village.tsx

must_haves:
  truths:
    - "User voit une nouvelle construction sur la carte village dès qu'un palier feuilles est franchi"
    - "User tape un bâtiment village → tooltip auto-dismiss 2.5s avec nom + palier"
    - "User ouvre le catalogue via bouton header (icône castle/home-city)"
    - "Catalogue affiche 8 tuiles : débloquées (full color) vs verrouillées (silhouette + palier requis)"
    - "Badge 'Nouveau' apparaît sur bâtiments débloqués depuis dernière visite (SecureStore persistence)"
    - "Tap tuile verrouillée → toast 'Encore N feuilles' + haptic light"
  artifacts:
    - path: "components/village/BuildingSprite.tsx"
      provides: "Sprite 72×72 positionné fractionnel + fade-in 300ms + onPress"
      contains: "BuildingSprite"
    - path: "components/village/BuildingTooltip.tsx"
      provides: "Tooltip bâtiment auto-dismiss 2.5s (fork AvatarTooltip)"
      contains: "BuildingTooltip"
    - path: "components/village/BuildingsCatalog.tsx"
      provides: "Modal pageSheet 2-col grid + badge Nouveau + SecureStore seen tracking"
      contains: "BuildingsCatalog"
    - path: "app/(tabs)/village.tsx"
      provides: "Header button catalog + building sprites overlay + tooltip state"
      contains: "BuildingSprite"
  key_links:
    - from: "app/(tabs)/village.tsx"
      to: "useGarden().unlockedBuildings + familyLifetimeLeaves"
      via: "hook consumption from Plan 02"
      pattern: "unlockedBuildings"
    - from: "components/village/BuildingSprite.tsx"
      to: "lib/village/catalog.ts BUILDINGS_CATALOG"
      via: "import pour spritemap par id"
      pattern: "BUILDINGS_CATALOG"
    - from: "components/village/BuildingsCatalog.tsx"
      to: "expo-secure-store clé village_buildings_seen_at"
      via: "SecureStore.getItemAsync / setItemAsync"
      pattern: "village_buildings_seen_at"
---

<objective>
Construire la couche UI Phase 30 : sprites bâtiments sur la carte village (tap→tooltip), modal catalogue plein écran (grille 2 colonnes, silhouettes verrouillées, badge Nouveau via SecureStore), bouton header dans village.tsx.

Purpose : C'est la couche visible par l'utilisateur. Sans elle, les bâtiments s'écrivent silencieusement dans le vault mais rien n'apparaît à l'écran. Le plan termine la boucle UX : gagner feuilles → palier franchi (Plan 02) → bâtiment visible sur carte + catalogue (Plan 03).

Output :
- components/village/BuildingSprite.tsx créé (sprite positionné fractionnel, tap handler, fade-in 300ms)
- components/village/BuildingTooltip.tsx créé (fork AvatarTooltip — même animation contract, prop `label: string`)
- components/village/BuildingsCatalog.tsx créé (modal pageSheet, grille 2-col, badge Nouveau, toast locked tile)
- app/(tabs)/village.tsx étendu : import + overlay BuildingSprite + bouton header + state tooltip + modal

Non-autonome : ce plan a un checkpoint checkpoint:human-verify en fin pour valider visuellement le rendu sur device (les coordonnées D-11 des 8 slots doivent être testées visuellement).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-decorations-persistantes/30-CONTEXT.md
@.planning/phases/30-decorations-persistantes/30-RESEARCH.md
@.planning/phases/30-decorations-persistantes/30-UI-SPEC.md
@.planning/phases/30-decorations-persistantes/30-01-PLAN.md
@.planning/phases/30-decorations-persistantes/30-02-PLAN.md
@CLAUDE.md

# Reference implementations — MUST READ
@components/village/VillageAvatar.tsx
@components/village/AvatarTooltip.tsx
@components/village/PortalSprite.tsx
@app/(tabs)/village.tsx
@contexts/ToastContext.tsx

<interfaces>
<!-- Available after Plans 01 and 02 complete -->

From lib/village/catalog.ts (Plan 01):
```typescript
export interface BuildingCatalogEntry {
  id: string;
  labelFR: string;
  palier: number;
  sprite: ReturnType<typeof require>;
}
export const BUILDINGS_CATALOG: BuildingCatalogEntry[]; // 8 entries
```

From lib/village/types.ts (Plan 01):
```typescript
export interface UnlockedBuilding {
  timestamp: string;
  buildingId: string;
  palier: number;
}
```

From lib/village/grid.ts (Plan 01):
```typescript
// VILLAGE_GRID now contains 8 additional slots with role 'building':
// village_building_puits at 0.08/0.15, village_building_boulangerie at 0.22/0.10,
// village_building_marche at 0.45/0.08, village_building_cafe at 0.68/0.10,
// village_building_forge at 0.90/0.20, village_building_moulin at 0.08/0.50,
// village_building_port at 0.45/0.92, village_building_bibliotheque at 0.92/0.55
```

From hooks/useGarden.ts (Plan 02):
```typescript
export interface UseGardenReturn {
  // ... existing fields ...
  familyLifetimeLeaves: number;
  unlockedBuildings: UnlockedBuilding[];
}
```

From components/village/AvatarTooltip.tsx (Phase 29 — REFERENCE pattern to fork):
```typescript
interface AvatarTooltipProps {
  profileName: string;
  count: number;
  x: number;       // centered px of avatar
  y: number;
  containerWidth: number;  // for horizontal clamp
  onDismiss: () => void;
}
// Animation contract:
// - Enter: opacity 0→1 + translateY -4→0 via withTiming duration 180ms
// - Auto-dismiss after 2500ms: opacity 1→0 + translateY 0→-4 via withTiming duration 150ms
// - runOnJS(onDismiss)() in timing callback
// - MAX_WIDTH = 200, OFFSET_ABOVE_AVATAR = 48
```

From contexts/ToastContext.tsx:
```typescript
// useToast() returns { showToast: (message: string) => void }
// Already wired in village.tsx (line ~37)
```

From CLAUDE.md conventions:
- useThemeColors() for all colors (jamais de hex hardcodé sauf constantes cosmétiques StyleSheet)
- react-native-reanimated ~4.1 only (jamais RN Animated)
- Spacing/Radius/Typography tokens
- Modals: presentation: 'pageSheet' + drag-to-dismiss
- Haptics.selectionAsync() on tap, Haptics.impactAsync(ImpactFeedbackStyle.Light) on locked
- expo-secure-store for device-local UI preferences
- Language: FR strictly
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1 — BuildingSprite + BuildingTooltip (composants atomiques)</name>
  <files>components/village/BuildingSprite.tsx, components/village/BuildingTooltip.tsx</files>

  <read_first>
    - components/village/VillageAvatar.tsx (pattern sprite positionné fractionnel — fork direct)
    - components/village/AvatarTooltip.tsx (pattern tooltip auto-dismiss — fork avec prop `label`)
    - components/village/PortalSprite.tsx (pattern `require()` inline + fade-in)
    - lib/village/catalog.ts (BUILDINGS_CATALOG — import pour lookup sprite par id)
    - .planning/phases/30-decorations-persistantes/30-UI-SPEC.md (sections Component Inventory, Interaction Contracts, Animation Specifications)
    - .planning/phases/30-decorations-persistantes/30-CONTEXT.md (D-12, D-13, D-14)
    - constants/spacing.ts, constants/typography.ts (tokens utilisables)
  </read_first>

  <action>
    1. **components/village/BuildingSprite.tsx** — Créer le fichier. Pattern miroir de VillageAvatar mais sprite depuis BUILDINGS_CATALOG par id.
       ```typescript
       // components/village/BuildingSprite.tsx
       // Phase 30 — Sprite bâtiment village positionné fractionnel sur la carte.
       // Per D-12 CONTEXT.md (72×72), UI-SPEC.md Animation Specifications.

       import React, { useEffect, useMemo } from 'react';
       import { Image, Pressable, StyleSheet } from 'react-native';
       import Animated, {
         useSharedValue,
         useAnimatedStyle,
         withTiming,
       } from 'react-native-reanimated';
       import * as Haptics from 'expo-haptics';
       import { BUILDINGS_CATALOG } from '../../lib/village';

       const SPRITE_SIZE = 72;
       const APPEAR_MS = 300;

       interface BuildingSpriteProps {
         buildingId: string;
         /** Position centree px sur la carte (slot.x * mapSize.width) */
         slotX: number;
         /** Position centree px sur la carte (slot.y * mapSize.height) */
         slotY: number;
         onPress: () => void;
       }

       export function BuildingSprite({ buildingId, slotX, slotY, onPress }: BuildingSpriteProps) {
         const entry = useMemo(
           () => BUILDINGS_CATALOG.find(b => b.id === buildingId),
           [buildingId],
         );
         const opacity = useSharedValue(0);

         useEffect(() => {
           opacity.value = withTiming(1, { duration: APPEAR_MS });
         }, [opacity]);

         const animStyle = useAnimatedStyle(() => ({
           opacity: opacity.value,
         }));

         if (!entry) return null;

         const handlePress = () => {
           Haptics.selectionAsync();
           onPress();
         };

         return (
           <Animated.View
             style={[
               styles.container,
               {
                 left: slotX - SPRITE_SIZE / 2,
                 top: slotY - SPRITE_SIZE / 2,
               },
               animStyle,
             ]}
           >
             <Pressable onPress={handlePress} hitSlop={8} accessibilityLabel={entry.labelFR}>
               <Image source={entry.sprite} style={styles.sprite} resizeMode="contain" />
             </Pressable>
           </Animated.View>
         );
       }

       const styles = StyleSheet.create({
         container: {
           position: 'absolute',
           width: SPRITE_SIZE,
           height: SPRITE_SIZE,
         },
         sprite: {
           width: SPRITE_SIZE,
           height: SPRITE_SIZE,
         },
       });
       ```

    2. **components/village/BuildingTooltip.tsx** — Fork direct de AvatarTooltip. Même animation contract, mais 1 seule prop contenu `label: string` au lieu de `profileName + count`.
       ```typescript
       // components/village/BuildingTooltip.tsx
       // Phase 30 — Bulle tooltip flottante au tap sur un BuildingSprite.
       // Fork de AvatarTooltip (Phase 29) — même animation contract, prop `label` unique.
       // Per D-13, D-14 CONTEXT.md — VILL-04.

       import React, { useEffect } from 'react';
       import { StyleSheet, Text } from 'react-native';
       import Animated, {
         useSharedValue,
         useAnimatedStyle,
         withTiming,
         runOnJS,
       } from 'react-native-reanimated';
       import { useThemeColors } from '../../contexts/ThemeContext';
       import { Spacing, Radius } from '../../constants/spacing';
       import { FontSize, FontWeight } from '../../constants/typography';

       // ── Constantes animation et layout (mirror AvatarTooltip) ──────────
       const DISMISS_MS = 2500;
       const ENTER_MS = 180;
       const EXIT_MS = 150;
       const MAX_WIDTH = 200;
       const TRANSLATE_Y_START = -4;
       const OFFSET_ABOVE_SPRITE = 52; // bâtiment 72×72 > avatar 48×48 → offset +4

       interface BuildingTooltipProps {
         /** Texte à afficher, ex: "Café — Débloqué à 1 500 feuilles familiales" */
         label: string;
         /** Position centree px du sprite cible */
         x: number;
         y: number;
         /** Largeur du container map pour clamp horizontal */
         containerWidth: number;
         onDismiss: () => void;
       }

       export function BuildingTooltip({ label, x, y, containerWidth, onDismiss }: BuildingTooltipProps) {
         const { colors } = useThemeColors();
         const opacity = useSharedValue(0);
         const translateY = useSharedValue(TRANSLATE_Y_START);

         useEffect(() => {
           opacity.value = withTiming(1, { duration: ENTER_MS });
           translateY.value = withTiming(0, { duration: ENTER_MS });

           const timer = setTimeout(() => {
             opacity.value = withTiming(0, { duration: EXIT_MS });
             translateY.value = withTiming(
               TRANSLATE_Y_START,
               { duration: EXIT_MS },
               (finished) => {
                 if (finished) runOnJS(onDismiss)();
               },
             );
           }, DISMISS_MS);

           return () => clearTimeout(timer);
         }, [opacity, translateY, onDismiss]);

         const animStyle = useAnimatedStyle(() => ({
           opacity: opacity.value,
           transform: [{ translateY: translateY.value }],
         }));

         // Clamp horizontal — éviter de dépasser les bords
         const half = MAX_WIDTH / 2;
         const clampedX = Math.max(half + Spacing.md, Math.min(containerWidth - half - Spacing.md, x));

         return (
           <Animated.View
             pointerEvents="none"
             style={[
               styles.container,
               {
                 left: clampedX - half,
                 top: y - OFFSET_ABOVE_SPRITE,
                 backgroundColor: colors.card,
                 borderColor: colors.border,
               },
               animStyle,
             ]}
           >
             <Text
               numberOfLines={1}
               style={[styles.label, { color: colors.text }]}
             >
               {label}
             </Text>
           </Animated.View>
         );
       }

       const styles = StyleSheet.create({
         container: {
           position: 'absolute',
           width: MAX_WIDTH,
           paddingHorizontal: Spacing['2xl'],
           paddingVertical: Spacing.md,
           borderRadius: Radius.md,
           borderWidth: StyleSheet.hairlineWidth,
           alignItems: 'center',
           shadowColor: '#000',
           shadowOffset: { width: 0, height: 2 },
           shadowOpacity: 0.15,
           shadowRadius: 4,
           elevation: 3,
         },
         label: {
           fontSize: FontSize.sm,
           fontWeight: FontWeight.normal,
           textAlign: 'center',
         },
       });
       ```

       **Note** : vérifier que les tokens `Spacing.md`, `Spacing['2xl']`, `Radius.md`, `FontSize.sm`, `FontWeight.normal` existent bien dans constants/ (ils sont utilisés par AvatarTooltip donc normalement OK). Si `Radius.md` n'existe pas, utiliser la valeur utilisée par AvatarTooltip (lire le fichier).
  </action>

  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>

  <acceptance_criteria>
    - `components/village/BuildingSprite.tsx` exists (test with Read)
    - `components/village/BuildingTooltip.tsx` exists (test with Read)
    - `grep -n "export function BuildingSprite" components/village/BuildingSprite.tsx` matches
    - `grep -n "export function BuildingTooltip" components/village/BuildingTooltip.tsx` matches
    - `grep -n "BUILDINGS_CATALOG" components/village/BuildingSprite.tsx` matches (import + find)
    - `grep -n "Haptics.selectionAsync" components/village/BuildingSprite.tsx` matches (tap haptic)
    - `grep -n "withTiming(1, { duration: 300" components/village/BuildingSprite.tsx` matches (fade-in 300ms)
    - `grep -n "DISMISS_MS = 2500" components/village/BuildingTooltip.tsx` matches (auto-dismiss timing)
    - `grep -n "useThemeColors" components/village/BuildingTooltip.tsx` matches (aucune couleur hardcodée)
    - `grep -c "#[0-9A-Fa-f]\{3,6\}" components/village/BuildingTooltip.tsx` returns `1` maximum (only shadowColor '#000' allowed — cosmetic)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>

  <done>
    BuildingSprite et BuildingTooltip créés, fork propre d'AvatarTooltip, animation contract identique (180/150/2500ms), useThemeColors utilisé, `npx tsc --noEmit` passe.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2 — BuildingsCatalog modal + wiring village.tsx</name>
  <files>components/village/BuildingsCatalog.tsx, app/(tabs)/village.tsx</files>

  <read_first>
    - components/village/BuildingSprite.tsx (Task 1 — pour import dans village.tsx)
    - components/village/BuildingTooltip.tsx (Task 1 — pour import dans village.tsx)
    - app/(tabs)/village.tsx (state actuel — insérer les overlays après les avatars, ne pas casser Phase 29)
    - components/village/VillageAvatar.tsx (référence — comment les avatars sont rendus dans le mapContainer)
    - contexts/ToastContext.tsx (useToast().showToast signature exacte)
    - .planning/phases/30-decorations-persistantes/30-UI-SPEC.md (Catalog modal layout, Copywriting Contract, Animation Specifications)
    - .planning/phases/30-decorations-persistantes/30-CONTEXT.md (D-15, D-16, D-17, D-18, D-19, D-20)
    - constants/colors.ts (pour vérifier colors.success, colors.textMuted, colors.textFaint disponibles)
  </read_first>

  <action>
    1. **components/village/BuildingsCatalog.tsx** — Créer le fichier.
       ```typescript
       // components/village/BuildingsCatalog.tsx
       // Phase 30 — Catalogue des 8 bâtiments village, modal pageSheet grille 2 colonnes.
       // Per D-15, D-16, D-17, D-18, D-19, D-20 CONTEXT.md — VILL-06.

       import React, { useEffect, useMemo, useState } from 'react';
       import {
         Modal,
         ScrollView,
         StyleSheet,
         Text,
         TouchableOpacity,
         View,
         Image,
       } from 'react-native';
       import Animated, {
         useSharedValue,
         useAnimatedStyle,
         withSpring,
       } from 'react-native-reanimated';
       import * as Haptics from 'expo-haptics';
       import * as SecureStore from 'expo-secure-store';
       import { MaterialCommunityIcons } from '@expo/vector-icons';
       import { useThemeColors } from '../../contexts/ThemeContext';
       import { useToast } from '../../contexts/ToastContext';
       import { Spacing, Radius } from '../../constants/spacing';
       import { FontSize, FontWeight, LineHeight } from '../../constants/typography';
       import { BUILDINGS_CATALOG, type BuildingCatalogEntry } from '../../lib/village';
       import type { UnlockedBuilding } from '../../lib/village';

       const SEEN_KEY = 'village_buildings_seen_at';
       const SPRITE_SIZE = 96;
       const BADGE_GOLD = '#FFD700'; // cosmetic constant per UI-SPEC
       const SPRING_CATALOG = { damping: 12, stiffness: 180 } as const;

       interface BuildingsCatalogProps {
         visible: boolean;
         onClose: () => void;
         unlockedBuildings: UnlockedBuilding[];
         familyLifetimeLeaves: number;
       }

       export function BuildingsCatalog({
         visible,
         onClose,
         unlockedBuildings,
         familyLifetimeLeaves,
       }: BuildingsCatalogProps) {
         const { colors } = useThemeColors();
         const { showToast } = useToast();
         const [lastSeen, setLastSeen] = useState<Date | null>(null);

         // Lire SecureStore au mount — badge 'Nouveau' lifecycle (D-19)
         useEffect(() => {
           if (!visible) return;
           (async () => {
             try {
               const raw = await SecureStore.getItemAsync(SEEN_KEY);
               setLastSeen(raw ? new Date(raw) : new Date(0));
             } catch {
               setLastSeen(new Date(0));
             }
           })();
         }, [visible]);

         // Au close — update SecureStore
         const handleClose = async () => {
           try {
             await SecureStore.setItemAsync(SEEN_KEY, new Date().toISOString());
           } catch {
             /* Constructions — non-critical */
           }
           onClose();
         };

         const unlockedMap = useMemo(() => {
           const map = new Map<string, UnlockedBuilding>();
           for (const b of unlockedBuildings) map.set(b.buildingId, b);
           return map;
         }, [unlockedBuildings]);

         const handleLockedPress = (entry: BuildingCatalogEntry) => {
           const remaining = Math.max(0, entry.palier - familyLifetimeLeaves);
           const plural = remaining > 1 ? 's' : '';
           showToast(`Encore ${remaining} feuille${plural} familiale${plural} pour débloquer ${entry.labelFR}`);
           Haptics.impactAsync(Haptics.ImpactFeedbackStyle.Light);
         };

         return (
           <Modal
             visible={visible}
             onRequestClose={handleClose}
             presentationStyle="pageSheet"
             animationType="slide"
           >
             <View style={[styles.container, { backgroundColor: colors.bg }]}>
               <View style={styles.header}>
                 <Text style={[styles.title, { color: colors.text }]}>Bâtiments du village</Text>
                 <TouchableOpacity onPress={handleClose} accessibilityLabel="Fermer">
                   <MaterialCommunityIcons name="close" size={24} color={colors.text} />
                 </TouchableOpacity>
               </View>

               <ScrollView contentContainerStyle={styles.scroll}>
                 {unlockedBuildings.length === 0 && (
                   <View style={styles.emptyState}>
                     <Text style={[styles.emptyTitle, { color: colors.text }]}>
                       Aucun bâtiment encore débloqué
                     </Text>
                     <Text style={[styles.emptyBody, { color: colors.textMuted }]}>
                       Gagnez des feuilles en famille pour construire votre village
                     </Text>
                   </View>
                 )}

                 <View style={styles.grid}>
                   {BUILDINGS_CATALOG.map(entry => {
                     const unlock = unlockedMap.get(entry.id);
                     const isUnlocked = !!unlock;
                     const isNew = isUnlocked && lastSeen && new Date(unlock!.timestamp) > lastSeen;
                     return (
                       <CatalogTile
                         key={entry.id}
                         entry={entry}
                         isUnlocked={isUnlocked}
                         isNew={!!isNew}
                         familyLifetimeLeaves={familyLifetimeLeaves}
                         onLockedPress={() => handleLockedPress(entry)}
                         colors={colors}
                       />
                     );
                   })}
                 </View>
               </ScrollView>
             </View>
           </Modal>
         );
       }

       // ── CatalogTile ────────────────────────────────────────────────────

       interface CatalogTileProps {
         entry: BuildingCatalogEntry;
         isUnlocked: boolean;
         isNew: boolean;
         familyLifetimeLeaves: number;
         onLockedPress: () => void;
         colors: ReturnType<typeof useThemeColors>['colors'];
       }

       function CatalogTile({
         entry, isUnlocked, isNew, familyLifetimeLeaves, onLockedPress, colors,
       }: CatalogTileProps) {
         const tileScale = useSharedValue(1);
         const badgeScale = useSharedValue(0);

         useEffect(() => {
           if (isNew) {
             badgeScale.value = withSpring(1, SPRING_CATALOG);
           }
         }, [isNew, badgeScale]);

         const tileAnim = useAnimatedStyle(() => ({
           transform: [{ scale: tileScale.value }],
         }));
         const badgeAnim = useAnimatedStyle(() => ({
           transform: [{ scale: badgeScale.value }],
         }));

         const handlePress = () => {
           if (isUnlocked) {
             // Pulse subtle spring (D-20)
             tileScale.value = withSpring(1.05, SPRING_CATALOG, () => {
               tileScale.value = withSpring(1, SPRING_CATALOG);
             });
             Haptics.selectionAsync();
           } else {
             onLockedPress();
           }
         };

         const progressLabel = isUnlocked
           ? 'Débloqué'
           : `À ${entry.palier} feuilles`;
         const progressDetail = isUnlocked
           ? null
           : `${familyLifetimeLeaves}/${entry.palier}`;

         return (
           <Animated.View style={[styles.tile, { backgroundColor: colors.card }, tileAnim]}>
             <TouchableOpacity
               onPress={handlePress}
               style={styles.tileTouch}
               activeOpacity={0.85}
               accessibilityLabel={`${entry.labelFR}, ${progressLabel}`}
             >
               <Image
                 source={entry.sprite}
                 style={[
                   styles.tileSprite,
                   !isUnlocked && { tintColor: colors.textMuted, opacity: 0.4 },
                 ]}
                 resizeMode="contain"
               />
               <Text style={[styles.tileLabel, { color: colors.text }]}>{entry.labelFR}</Text>
               <Text
                 style={[
                   styles.tileStatus,
                   { color: isUnlocked ? colors.success : colors.textMuted },
                 ]}
               >
                 {progressLabel}
               </Text>
               {progressDetail && (
                 <Text style={[styles.tileProgress, { color: colors.textMuted }]}>
                   {progressDetail}
                 </Text>
               )}
               {isNew && (
                 <Animated.View
                   style={[
                     styles.badge,
                     { backgroundColor: colors.successBg ?? colors.success },
                     badgeAnim,
                   ]}
                 >
                   <Text style={[styles.badgeText, { color: BADGE_GOLD }]}>Nouveau ✨</Text>
                 </Animated.View>
               )}
             </TouchableOpacity>
           </Animated.View>
         );
       }

       const styles = StyleSheet.create({
         container: { flex: 1 },
         header: {
           flexDirection: 'row',
           justifyContent: 'space-between',
           alignItems: 'center',
           paddingHorizontal: Spacing['2xl'],
           paddingVertical: Spacing.xl,
         },
         title: {
           fontSize: FontSize.heading,
           fontWeight: FontWeight.semibold,
           lineHeight: LineHeight.title,
         },
         scroll: {
           paddingHorizontal: Spacing['2xl'],
           paddingBottom: Spacing['4xl'],
         },
         grid: {
           flexDirection: 'row',
           flexWrap: 'wrap',
           gap: Spacing.md,
         },
         tile: {
           width: '48%',
           borderRadius: Radius.md,
           padding: Spacing.xl,
           minHeight: 160,
         },
         tileTouch: {
           alignItems: 'center',
           minHeight: 44,
         },
         tileSprite: {
           width: SPRITE_SIZE,
           height: SPRITE_SIZE,
         },
         tileLabel: {
           marginTop: Spacing.md,
           fontSize: FontSize.sm,
           fontWeight: FontWeight.semibold,
         },
         tileStatus: {
           marginTop: Spacing.xs,
           fontSize: FontSize.caption,
         },
         tileProgress: {
           fontSize: FontSize.caption,
         },
         badge: {
           position: 'absolute',
           top: 0,
           right: 0,
           paddingHorizontal: Spacing.md,
           paddingVertical: Spacing.xs,
           borderRadius: Radius.sm,
         },
         badgeText: {
           fontSize: FontSize.caption,
           fontWeight: FontWeight.semibold,
         },
         emptyState: {
           padding: Spacing['4xl'],
           alignItems: 'center',
         },
         emptyTitle: {
           fontSize: FontSize.sm,
           fontWeight: FontWeight.semibold,
           marginBottom: Spacing.md,
         },
         emptyBody: {
           fontSize: FontSize.caption,
           textAlign: 'center',
         },
       });
       ```

       **Note sur les tokens** : si `colors.successBg`, `colors.textFaint`, `Radius.sm`, `LineHeight.title` n'existent pas, remplacer par la valeur existante la plus proche après lecture de constants/colors.ts, constants/spacing.ts, constants/typography.ts. Si `Radius.md` absent, utiliser `Radius.lg` ou la valeur utilisée par AvatarTooltip.

    2. **app/(tabs)/village.tsx** — Étendre le fichier sans toucher aux blocs Phase 29 :

       a. **Ajouter imports** (à côté des imports Phase 29) :
          ```typescript
          import { BuildingSprite } from '../../components/village/BuildingSprite';
          import { BuildingTooltip } from '../../components/village/BuildingTooltip';
          import { BuildingsCatalog } from '../../components/village/BuildingsCatalog';
          import { BUILDINGS_CATALOG } from '../../lib/village';
          import { MaterialCommunityIcons } from '@expo/vector-icons';
          ```

       b. **Consommer Plan 02 hook return** — trouver la ligne `const { gardenData, ... } = useGarden();` et étendre la destructuration :
          ```typescript
          const {
            gardenData,
            // ... existing fields ...
            familyLifetimeLeaves,       // NEW Plan 02
            unlockedBuildings,          // NEW Plan 02
          } = useGarden();
          ```

       c. **State local nouveaux** — près des autres `useState` :
          ```typescript
          const [showCatalog, setShowCatalog] = useState(false);
          const [buildingTooltip, setBuildingTooltip] = useState<{
            label: string; x: number; y: number;
          } | null>(null);
          ```

       d. **Handler tap bâtiment** — à côté des autres handlers :
          ```typescript
          const handleBuildingPress = useCallback((ub: UnlockedBuilding) => {
            const entry = BUILDINGS_CATALOG.find(b => b.id === ub.buildingId);
            const slot = VILLAGE_GRID.find(s => s.id === `village_building_${ub.buildingId}`);
            if (!entry || !slot) return;
            setBuildingTooltip({
              label: `${entry.labelFR} — Débloqué à ${entry.palier} feuilles familiales`,
              x: slot.x * mapSize.width,
              y: slot.y * mapSize.height,
            });
          }, [mapSize]);
          ```

          Vérifier l'import `UnlockedBuilding` : `import type { UnlockedBuilding } from '../../lib/village';`

       e. **Bouton header catalogue** — dans le JSX du header de l'écran, ajouter une TouchableOpacity côté droit du titre (localiser `styles.header` et le bloc titre actuel, ajouter en position absolute right ou via flexDirection row). Forme :
          ```tsx
          <TouchableOpacity
            onPress={() => setShowCatalog(true)}
            style={styles.headerCatalogButton}
            accessibilityLabel="Bâtiments du village"
          >
            <MaterialCommunityIcons name="home-city" size={22} color={colors.text} />
          </TouchableOpacity>
          ```
          Et dans les styles en bas du fichier :
          ```typescript
          headerCatalogButton: {
            position: 'absolute',
            right: Spacing['2xl'],
            top: Spacing.xl,
            padding: Spacing.md,
          },
          ```
          (Ajuster le padding/position selon le layout actuel du header — test device au checkpoint.)

       f. **Overlay bâtiments dans mapContainer** — localiser le bloc où les VillageAvatar sont rendus (siblings du TileMapRenderer) et ajouter immédiatement APRÈS le bloc avatars, AVANT le bloc tooltip Phase 29 :
          ```tsx
          {/* Phase 30 — overlay bâtiments (VILL-04) */}
          {unlockedBuildings.map(ub => {
            const slot = VILLAGE_GRID.find(s => s.id === `village_building_${ub.buildingId}`);
            if (!slot) return null;
            return (
              <BuildingSprite
                key={ub.buildingId}
                buildingId={ub.buildingId}
                slotX={slot.x * mapSize.width}
                slotY={slot.y * mapSize.height}
                onPress={() => handleBuildingPress(ub)}
              />
            );
          })}
          {buildingTooltip && (
            <BuildingTooltip
              label={buildingTooltip.label}
              x={buildingTooltip.x}
              y={buildingTooltip.y}
              containerWidth={mapSize.width}
              onDismiss={() => setBuildingTooltip(null)}
            />
          )}
          ```

       g. **Modal catalogue** — avant le `</ScrollView>` ou juste avant le return final, ajouter :
          ```tsx
          <BuildingsCatalog
            visible={showCatalog}
            onClose={() => setShowCatalog(false)}
            unlockedBuildings={unlockedBuildings}
            familyLifetimeLeaves={familyLifetimeLeaves}
          />
          ```
  </action>

  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>

  <acceptance_criteria>
    - `components/village/BuildingsCatalog.tsx` exists
    - `grep -n "export function BuildingsCatalog" components/village/BuildingsCatalog.tsx` matches
    - `grep -n "village_buildings_seen_at" components/village/BuildingsCatalog.tsx` matches (SEEN_KEY SecureStore)
    - `grep -n "presentationStyle=\"pageSheet\"" components/village/BuildingsCatalog.tsx` matches
    - `grep -n "SPRING_CATALOG = { damping: 12, stiffness: 180" components/village/BuildingsCatalog.tsx` matches
    - `grep -n "tintColor: colors.textMuted" components/village/BuildingsCatalog.tsx` matches (silhouette locked)
    - `grep -n "Haptics.ImpactFeedbackStyle.Light" components/village/BuildingsCatalog.tsx` matches
    - `grep -n "Encore.*feuille.*familiale.*pour débloquer" components/village/BuildingsCatalog.tsx` matches (toast locked)
    - `grep -n "Bâtiments du village" components/village/BuildingsCatalog.tsx` matches (modal title)
    - `grep -n "Nouveau ✨" components/village/BuildingsCatalog.tsx` matches (badge text)
    - `grep -n "BuildingSprite" "app/(tabs)/village.tsx"` matches (import + render)
    - `grep -n "BuildingsCatalog" "app/(tabs)/village.tsx"` matches (import + render)
    - `grep -n "unlockedBuildings" "app/(tabs)/village.tsx"` matches (hook destructuring)
    - `grep -n "familyLifetimeLeaves" "app/(tabs)/village.tsx"` matches
    - `grep -n "handleBuildingPress" "app/(tabs)/village.tsx"` matches
    - `grep -n "name=\"home-city\"" "app/(tabs)/village.tsx"` matches (header button icon)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>

  <done>
    BuildingsCatalog créé avec modal pageSheet, grille 2 colonnes, silhouette verrouillée, badge Nouveau via SecureStore, toast locked tile. village.tsx étendu : header button, overlay sprites, tooltip state, modal câblée. TypeScript compile sans erreur.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3 — Checkpoint vérification device Phase 30</name>

  <what-built>
    - 8 sprites bâtiments village avec système de déblocage par palier de feuilles famille (100 → 25000)
    - Tap sur un bâtiment → tooltip auto-dismiss 2.5s avec nom + palier
    - Bouton header (icône home-city) → ouvre modal catalogue grille 2 colonnes
    - Catalogue : bâtiments débloqués full color, verrouillés en silhouette + palier requis + progression
    - Badge "Nouveau ✨" sur bâtiments débloqués depuis dernière visite (SecureStore)
    - Tap tuile verrouillée → toast "Encore N feuilles" + haptic light
  </what-built>

  <how-to-verify>
    **Prérequis build :**
    1. `npx tsc --noEmit` — doit passer sans erreur
    2. `npx jest --no-coverage lib/__tests__/village-parser.test.ts` — doit passer sans erreur
    3. `npx expo run:ios --device` — rebuild dev-client avec les nouveaux composants

    **Test scénario A — Bâtiment déjà débloqué (si points famille déjà ≥ 100) :**
    1. Ouvrir l'app, aller sur l'onglet Village
    2. Vérifier : au moins un bâtiment apparaît sur la carte village (coin haut-gauche = Puits si ≥100 feuilles)
    3. Vérifier : le sprite est en fade-in 300ms au premier rendu
    4. Tap sur le bâtiment → tooltip flottant avec "Puits — Débloqué à 100 feuilles familiales", auto-dismiss ~2.5s
    5. Tap header icône "home-city" (côté droit) → modal catalogue s'ouvre
    6. Vérifier : grille 2 colonnes, 8 tuiles visibles
    7. Vérifier : Puits en full color avec label "Débloqué" vert
    8. Vérifier : Bibliothèque en silhouette sombre avec "À 25000 feuilles" + progression actuelle
    9. Tap tuile verrouillée → toast "Encore N feuilles familiales pour débloquer..."
    10. Tap tuile débloquée → scale pulse subtle + haptic
    11. Drag down pour fermer le modal → modal se ferme

    **Test scénario B — Pas de bâtiment débloqué (si points famille < 100) :**
    1. Ouvrir modal catalogue → empty state "Aucun bâtiment encore débloqué" + sous-titre motivationnel
    2. Aucun sprite visible sur la carte village
    3. Toutes les tuiles du catalogue en silhouette

    **Test scénario C — Persistance après restart :**
    1. Si un bâtiment est débloqué : fermer l'app (kill complet)
    2. Relancer l'app → retourner sur village
    3. Vérifier : le bâtiment est TOUJOURS visible sur la carte (persistance append-only jardin-familial.md)
    4. Ouvrir le vault Obsidian manuellement → vérifier présence de `## Constructions` avec la/les ligne(s)

    **Test scénario D — Calibration coordonnées (Claude's Discretion D-11) :**
    1. Vérifier visuellement qu'aucun bâtiment ne chevauche les slots existants (fontaine, 2 stalls, board, 6 avatars, portail retour)
    2. Zones à tester : haut (puits/boulangerie/marché/café), droite (forge/bibliothèque), bas (port), gauche (moulin)
    3. Si collision → noter laquelle et quelle coord ajuster dans lib/village/grid.ts Phase 30 slots

    **Test scénario E — Badge Nouveau :**
    1. Ouvrir catalogue → fermer
    2. Re-ouvrir catalogue → vérifier qu'AUCUN badge "Nouveau" n'apparaît (déjà vu)
    3. Si possible : déclencher un nouveau déblocage (ajouter points manuellement via gamification ou attendre gain naturel)
    4. Ouvrir catalogue → le nouveau bâtiment doit avoir le badge "Nouveau ✨" en top-right

    **Test accessibilité :**
    - VoiceOver activable sur chaque sprite et chaque tuile catalogue (accessibilityLabel présent)

    **Si erreur :**
    - Décrire : bâtiment qui manque, collision visuelle, coord à ajuster, crash console, badge qui ne disparaît pas
  </how-to-verify>

  <resume-signal>Type "approved" si tout OK, ou décrire les issues à corriger (coord collision, crash, copy, etc.)</resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` — aucune erreur
- `npx jest --no-coverage lib/__tests__/village-parser.test.ts` — tous les tests passent
- Device test (checkpoint) — rendu visuel OK, tooltip OK, catalogue OK, persistance OK
- Aucune régression Phase 29 : avatars toujours visibles, halo actif, portail retour fonctionnel
</verification>

<success_criteria>
- VILL-04 couvert : bâtiments apparaissent sur la carte quand paliers franchis
- VILL-06 couvert : catalogue 8 bâtiments avec état débloqué/verrouillé + progression visible
- Persistance restart vérifiée au checkpoint (complétude VILL-05 end-to-end)
- Phase 29 intacte : pas de régression avatars/portail retour
- Conventions CLAUDE.md respectées : useThemeColors, Reanimated, Spacing tokens, FR uniquement, pageSheet modal
</success_criteria>

<output>
After completion, create `.planning/phases/30-decorations-persistantes/30-03-SUMMARY.md`
</output>
