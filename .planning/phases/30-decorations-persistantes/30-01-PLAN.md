---
phase: 30
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [VILL-05, VILL-06]
files_modified:
  - lib/village/types.ts
  - lib/village/parser.ts
  - lib/village/grid.ts
  - lib/village/catalog.ts
  - lib/village/index.ts
  - lib/__tests__/village-parser.test.ts

must_haves:
  truths:
    - "parseGardenFile parses ## Constructions section into unlockedBuildings[] round-trip"
    - "appendBuilding inserts a line BEFORE ## Historique, never at end of file"
    - "BUILDINGS_CATALOG exports 8 entries with paliers 100/300/700/1500/3000/6000/12000/25000"
    - "computeBuildingsToUnlock is idempotent (never returns already-unlocked ids)"
    - "VILLAGE_GRID contains 8 new slots with role 'building' (village_building_*)"
    - "Stale village-parser.test.ts VILLAGE_GRID.toHaveLength(4) assertion is fixed"
  artifacts:
    - path: "lib/village/catalog.ts"
      provides: "BUILDINGS_CATALOG static list + computeBuildingsToUnlock pure function"
      contains: "BUILDINGS_CATALOG"
    - path: "lib/village/types.ts"
      provides: "UnlockedBuilding type + 'building' in VillageRole union + unlockedBuildings in VillageData"
      contains: "UnlockedBuilding"
    - path: "lib/village/parser.ts"
      provides: "appendBuilding + appendBuildingToVault + parsing/serialization of ## Constructions"
      contains: "appendBuilding"
    - path: "lib/village/grid.ts"
      provides: "8 building slots in VILLAGE_GRID"
      contains: "village_building_puits"
  key_links:
    - from: "lib/village/parser.ts"
      to: "lib/village/types.ts"
      via: "import UnlockedBuilding"
      pattern: "import.*UnlockedBuilding.*from.*types"
    - from: "lib/village/catalog.ts"
      to: "assets/buildings/village/*.png"
      via: "static require() per entry"
      pattern: "require\\(.*assets/buildings/village/"
---

<objective>
Construire la couche données Phase 30 — bâtiments persistants village : types, parser append-only, catalogue statique des 8 bâtiments, slots dans VILLAGE_GRID.

Purpose : Toutes les couches supérieures (unlock engine Plan 02, UI Plan 03) dépendent d'un schéma de données stable. Cette couche est orthogonale, 100% testable avec Jest, et fixe au passage le test stale VILLAGE_GRID.toHaveLength(4) qui date de Phase 25.

Output :
- lib/village/types.ts étendu (UnlockedBuilding, role 'building', unlockedBuildings dans VillageData)
- lib/village/parser.ts étendu (parse/serialize ## Constructions, appendBuilding, appendBuildingToVault)
- lib/village/catalog.ts créé (BUILDINGS_CATALOG 8 entrées + computeBuildingsToUnlock)
- lib/village/grid.ts étendu (8 slots building)
- lib/village/index.ts étendu (export catalog)
- lib/__tests__/village-parser.test.ts corrigé + nouveaux tests (round-trip, appendBuilding, idempotence)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-decorations-persistantes/30-CONTEXT.md
@.planning/phases/30-decorations-persistantes/30-RESEARCH.md
@.planning/phases/30-decorations-persistantes/30-UI-SPEC.md
@CLAUDE.md

# Reference implementations — MUST READ before writing new code
@lib/village/parser.ts
@lib/village/types.ts
@lib/village/grid.ts
@lib/__tests__/village-parser.test.ts

<interfaces>
<!-- Key types and functions the executor must reproduce/extend -->

From lib/village/types.ts (current state):
```typescript
export type VillageRole = 'fountain' | 'stall' | 'board' | 'portal' | 'avatar';
export interface VillageCell { id: string; x: number; y: number; role: VillageRole; }
export interface VillageData {
  version: number;
  createdAt: string;
  currentWeekStart: string;
  currentThemeIndex: number;
  rewardClaimed: boolean;
  contributions: VillageContribution[];
  pastWeeks: VillageWeekRecord[];
}
```

From lib/village/parser.ts — appendContribution is the CANONICAL reference pattern:
```typescript
export function appendContribution(content: string, entry: VillageContribution): string {
  const newLine = `- ${entry.timestamp} | ${entry.profileId} | ${entry.type} | ${entry.amount}`;
  const lines = content.split('\n');
  let contribStart = -1;
  let nextSectionIdx = -1;
  for (let i = 0; i < lines.length; i++) {
    if (lines[i].trim().toLowerCase() === '## contributions') { contribStart = i; continue; }
    if (contribStart !== -1 && lines[i].startsWith('## ')) { nextSectionIdx = i; break; }
  }
  if (contribStart === -1) {
    const historiquelIdx = lines.findIndex(l => l.trim().toLowerCase() === '## historique');
    if (historiquelIdx !== -1) {
      const before = lines.slice(0, historiquelIdx);
      const after = lines.slice(historiquelIdx);
      while (before.length > 0 && before[before.length - 1].trim() === '') before.pop();
      return [...before, '', '## Contributions', newLine, '', ...after].join('\n');
    } else {
      const trimmed = content.trimEnd();
      return `${trimmed}\n\n## Contributions\n${newLine}\n`;
    }
  }
  if (nextSectionIdx !== -1) {
    let insertAt = nextSectionIdx;
    while (insertAt > contribStart + 1 && lines[insertAt - 1].trim() === '') insertAt--;
    const before = lines.slice(0, insertAt);
    const after = lines.slice(insertAt);
    return [...before, newLine, ...after].join('\n');
  } else {
    const trimmed = content.trimEnd();
    return `${trimmed}\n${newLine}\n`;
  }
}
```

From lib/village/grid.ts (current state — 11 slots after Phase 29):
```typescript
export const VILLAGE_GRID: VillageCell[] = [
  { id: 'village_fountain', x: 0.50, y: 0.45, role: 'fountain' },
  { id: 'village_stall_0',  x: 0.22, y: 0.65, role: 'stall' },
  { id: 'village_stall_1',  x: 0.78, y: 0.65, role: 'stall' },
  { id: 'village_board',    x: 0.15, y: 0.25, role: 'board' },
  { id: 'village_avatar_slot_0', x: 0.35, y: 0.40, role: 'avatar' },
  { id: 'village_avatar_slot_1', x: 0.65, y: 0.40, role: 'avatar' },
  { id: 'village_avatar_slot_2', x: 0.30, y: 0.55, role: 'avatar' },
  { id: 'village_avatar_slot_3', x: 0.70, y: 0.55, role: 'avatar' },
  { id: 'village_avatar_slot_4', x: 0.40, y: 0.72, role: 'avatar' },
  { id: 'village_avatar_slot_5', x: 0.60, y: 0.72, role: 'avatar' },
  { id: 'village_portal_home', x: 0.85, y: 0.85, role: 'portal' },
];
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 — Types + catalogue statique + slots grille</name>
  <files>lib/village/types.ts, lib/village/grid.ts, lib/village/catalog.ts, lib/village/index.ts</files>

  <read_first>
    - lib/village/types.ts (current state — add to existing unions/interfaces, don't rewrite)
    - lib/village/grid.ts (current state — 11 slots, add 8 building slots at the end of the array)
    - lib/village/index.ts (current barrel exports)
    - .planning/phases/30-decorations-persistantes/30-CONTEXT.md (D-02, D-03, D-06, D-09, D-11, D-22)
    - .planning/phases/30-decorations-persistantes/30-RESEARCH.md (Pattern 6 : BUILDINGS_CATALOG statique, File-Level Plan, Pitfall 4 & 5)
    - assets/buildings/village/ directory (must contain 8 PNG files already committed)
  </read_first>

  <behavior>
    - parseGardenFile continues to parse existing content unchanged (types.ts must not break existing code)
    - VillageRole union accepts 'building' as a new value
    - VillageData.unlockedBuildings is an array (defaults to [] — never undefined in constructors)
    - BUILDINGS_CATALOG exports exactly 8 entries, ids: puits, boulangerie, marche, cafe, forge, moulin, port, bibliotheque
    - Paliers exactement: 100, 300, 700, 1500, 3000, 6000, 12000, 25000 (même ordre que ids ci-dessus)
    - computeBuildingsToUnlock(lifetime, []) returns all entries with palier ≤ lifetime
    - computeBuildingsToUnlock(lifetime, alreadyUnlocked) never returns an id already in alreadyUnlocked (idempotence)
    - VILLAGE_GRID contains 19 entries total (11 existing + 8 new) with ids village_building_{id}
  </behavior>

  <action>
    1. **lib/village/types.ts** — Étendre sans casser :
       - Changer `export type VillageRole = 'fountain' | 'stall' | 'board' | 'portal' | 'avatar';` → `export type VillageRole = 'fountain' | 'stall' | 'board' | 'portal' | 'avatar' | 'building';`
       - Ajouter nouvelle interface :
         ```typescript
         /** Un bâtiment débloqué — ligne append-only dans ## Constructions (Phase 30, VILL-05) */
         export interface UnlockedBuilding {
           timestamp: string;   // ISO 8601 sans Z — ex: 2026-04-12T14:32:00
           buildingId: string;  // 'puits' | 'boulangerie' | 'marche' | 'cafe' | 'forge' | 'moulin' | 'port' | 'bibliotheque'
           palier: number;      // palier franchi au déblocage (100, 300, 700, 1500, 3000, 6000, 12000, 25000)
         }
         ```
       - Ajouter champ dans VillageData interface : `unlockedBuildings: UnlockedBuilding[];` (juste après `pastWeeks`)

    2. **lib/village/catalog.ts** — Créer le fichier complet :
       ```typescript
       // lib/village/catalog.ts
       // Catalogue statique des 8 bâtiments village + fonction pure de calcul des déblocages.
       // Phase 30 — décorations persistantes (v1.5). Per D-01, D-02, D-06 CONTEXT.md.
       //
       // Module pur — zéro import hook/context. Les require() de sprites sont STATIQUES
       // (Pitfall 4 : Metro bundler ne supporte pas les require() dynamiques).

       import type { UnlockedBuilding } from './types';

       export interface BuildingCatalogEntry {
         id: string;          // 'puits', 'boulangerie', etc.
         labelFR: string;     // 'Puits', 'Boulangerie', etc.
         palier: number;      // 100, 300, 700, ...
         sprite: ReturnType<typeof require>;
       }

       /**
        * Liste verrouillée des 8 bâtiments village débloquables par paliers de feuilles famille.
        * Per D-02, D-06 CONTEXT.md. Ordre = ordre narratif hameau → ville vivante (per D-07).
        * Sprites dans assets/buildings/village/ (sous-dossier obligatoire — collision moulin.png ferme, Pitfall 5).
        */
       export const BUILDINGS_CATALOG: BuildingCatalogEntry[] = [
         { id: 'puits',        labelFR: 'Puits',        palier: 100,   sprite: require('../../assets/buildings/village/puits.png') },
         { id: 'boulangerie',  labelFR: 'Boulangerie',  palier: 300,   sprite: require('../../assets/buildings/village/boulangerie.png') },
         { id: 'marche',       labelFR: 'Marché',       palier: 700,   sprite: require('../../assets/buildings/village/marche.png') },
         { id: 'cafe',         labelFR: 'Café',         palier: 1500,  sprite: require('../../assets/buildings/village/cafe.png') },
         { id: 'forge',        labelFR: 'Forge',        palier: 3000,  sprite: require('../../assets/buildings/village/forge.png') },
         { id: 'moulin',       labelFR: 'Moulin',       palier: 6000,  sprite: require('../../assets/buildings/village/moulin.png') },
         { id: 'port',         labelFR: 'Port',         palier: 12000, sprite: require('../../assets/buildings/village/port.png') },
         { id: 'bibliotheque', labelFR: 'Bibliothèque', palier: 25000, sprite: require('../../assets/buildings/village/bibliotheque.png') },
       ];

       /**
        * Retourne les entrées catalogue qui devraient être débloquées mais ne le sont pas encore.
        * Pure — testable sans vault. Idempotent : si tout est déjà débloqué, retourne [].
        * Per D-23 CONTEXT.md + Pitfall 1 RESEARCH.md.
        */
       export function computeBuildingsToUnlock(
         familyLifetimeLeaves: number,
         alreadyUnlocked: UnlockedBuilding[],
       ): BuildingCatalogEntry[] {
         const unlockedIds = new Set(alreadyUnlocked.map(b => b.buildingId));
         return BUILDINGS_CATALOG.filter(
           entry => entry.palier <= familyLifetimeLeaves && !unlockedIds.has(entry.id),
         );
       }
       ```

    3. **lib/village/grid.ts** — Ajouter 8 slots à la fin du tableau VILLAGE_GRID (ne pas toucher aux 11 existants). Coordonnées D-11 CONTEXT.md :
       ```typescript
         // Phase 30 — slots batiments village debloquables par palier (per D-09, D-11 — VILL-04)
         { id: 'village_building_puits',        x: 0.08, y: 0.15, role: 'building' },
         { id: 'village_building_boulangerie',  x: 0.22, y: 0.10, role: 'building' },
         { id: 'village_building_marche',       x: 0.45, y: 0.08, role: 'building' },
         { id: 'village_building_cafe',         x: 0.68, y: 0.10, role: 'building' },
         { id: 'village_building_forge',        x: 0.90, y: 0.20, role: 'building' },
         { id: 'village_building_moulin',       x: 0.08, y: 0.50, role: 'building' },
         { id: 'village_building_port',         x: 0.45, y: 0.92, role: 'building' },
         { id: 'village_building_bibliotheque', x: 0.92, y: 0.55, role: 'building' },
       ```

    4. **lib/village/index.ts** — Ajouter export : `export * from './catalog';` (préserver tous les exports existants).

    5. **Tests Jest** — Dans lib/__tests__/village-parser.test.ts, CORRIGER le test stale à ligne 239 : `expect(VILLAGE_GRID).toHaveLength(4);` → `expect(VILLAGE_GRID).toHaveLength(19);` (11 Phase 25+29 + 8 Phase 30). Ajouter un nouveau bloc describe pour le catalogue :
       ```typescript
       import { BUILDINGS_CATALOG, computeBuildingsToUnlock } from '../village/catalog';

       describe('BUILDINGS_CATALOG (Phase 30)', () => {
         it('contient exactement 8 entrées', () => {
           expect(BUILDINGS_CATALOG).toHaveLength(8);
         });

         it('contient les ids attendus dans l ordre narratif', () => {
           expect(BUILDINGS_CATALOG.map(b => b.id)).toEqual([
             'puits', 'boulangerie', 'marche', 'cafe', 'forge', 'moulin', 'port', 'bibliotheque'
           ]);
         });

         it('contient les paliers exacts 100/300/700/1500/3000/6000/12000/25000', () => {
           expect(BUILDINGS_CATALOG.map(b => b.palier)).toEqual([
             100, 300, 700, 1500, 3000, 6000, 12000, 25000
           ]);
         });

         it('chaque entrée a un sprite non-null', () => {
           for (const entry of BUILDINGS_CATALOG) {
             expect(entry.sprite).toBeTruthy();
           }
         });
       });

       describe('computeBuildingsToUnlock (Phase 30)', () => {
         it('retourne [] si familyLifetimeLeaves < 100', () => {
           expect(computeBuildingsToUnlock(0, [])).toEqual([]);
           expect(computeBuildingsToUnlock(99, [])).toEqual([]);
         });

         it('retourne puits seul à 100 feuilles', () => {
           const result = computeBuildingsToUnlock(100, []);
           expect(result.map(r => r.id)).toEqual(['puits']);
         });

         it('retourne les 4 premiers à 1500 feuilles', () => {
           const result = computeBuildingsToUnlock(1500, []);
           expect(result.map(r => r.id)).toEqual(['puits', 'boulangerie', 'marche', 'cafe']);
         });

         it('retourne tous les 8 à 25000+ feuilles', () => {
           const result = computeBuildingsToUnlock(25000, []);
           expect(result).toHaveLength(8);
         });

         it('idempotence — skip les ids déjà débloqués', () => {
           const already = [
             { timestamp: '2026-04-12T00:00:00', buildingId: 'puits', palier: 100 },
             { timestamp: '2026-04-12T00:00:00', buildingId: 'boulangerie', palier: 300 },
           ];
           const result = computeBuildingsToUnlock(1500, already);
           expect(result.map(r => r.id)).toEqual(['marche', 'cafe']);
         });

         it('idempotence complète — tout débloqué → []', () => {
           const all = BUILDINGS_CATALOG.map(e => ({
             timestamp: '2026-04-12T00:00:00', buildingId: e.id, palier: e.palier
           }));
           expect(computeBuildingsToUnlock(99999, all)).toEqual([]);
         });
       });
       ```
  </action>

  <verify>
    <automated>npx tsc --noEmit && npx jest --no-coverage lib/__tests__/village-parser.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n "'building'" lib/village/types.ts` matches (VillageRole union contient 'building')
    - `grep -n "export interface UnlockedBuilding" lib/village/types.ts` matches
    - `grep -n "unlockedBuildings: UnlockedBuilding\[\]" lib/village/types.ts` matches
    - `lib/village/catalog.ts` exists (test with Read)
    - `grep -n "export const BUILDINGS_CATALOG" lib/village/catalog.ts` matches
    - `grep -c "require('../../assets/buildings/village/" lib/village/catalog.ts` returns `8`
    - `grep -n "export function computeBuildingsToUnlock" lib/village/catalog.ts` matches
    - `grep -c "village_building_" lib/village/grid.ts` returns `8`
    - `grep -n "export \* from './catalog'" lib/village/index.ts` matches
    - `grep -n "toHaveLength(19)" lib/__tests__/village-parser.test.ts` matches (stale toHaveLength(4) fix)
    - `grep -n "BUILDINGS_CATALOG (Phase 30)" lib/__tests__/village-parser.test.ts` matches
    - `npx tsc --noEmit` exits 0
    - `npx jest --no-coverage lib/__tests__/village-parser.test.ts` exits 0
  </acceptance_criteria>

  <done>
    Types étendus, catalogue créé avec 8 entrées + fonction idempotente pure, 8 slots ajoutés à VILLAGE_GRID, test stale corrigé, nouveaux tests catalogue passent. `npx tsc --noEmit` et `npx jest --no-coverage lib/__tests__/village-parser.test.ts` passent tous les deux.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 — Parser/serializer ## Constructions + appendBuilding</name>
  <files>lib/village/parser.ts, lib/__tests__/village-parser.test.ts</files>

  <read_first>
    - lib/village/parser.ts (CANONICAL reference — copier le pattern de appendContribution EXACTEMENT pour appendBuilding)
    - lib/village/types.ts (après Task 1 — UnlockedBuilding + VillageData.unlockedBuildings)
    - lib/__tests__/village-parser.test.ts (voir la structure des tests existants pour appendContribution — modèle)
    - .planning/phases/30-decorations-persistantes/30-RESEARCH.md (Data Schema Proposal, Code Examples parseGardenFile étendu)
    - .planning/phases/30-decorations-persistantes/30-CONTEXT.md (D-21, D-22, D-23)
  </read_first>

  <behavior>
    - parseGardenFile retourne unlockedBuildings: [] si section absente (backward compat — vaults existants non cassés)
    - parseGardenFile parse section ## Constructions format `- timestamp | buildingId | palier`
    - Lignes malformées silencieusement ignorées (consistent avec parsing contributions)
    - serializeGardenFile émet TOUJOURS section ## Constructions (même vide) entre ## Contributions et ## Historique
    - Round-trip fidelity : parse(serialize(x)) === x pour toutes données valides
    - appendBuilding insère ligne avant prochaine section ## (jamais fin de fichier — Pitfall 3/4)
    - Si ## Constructions absente mais ## Historique présente : créer ## Constructions AVANT ## Historique
    - Si ni l'une ni l'autre : append en fin de fichier avec nouvelle section
    - appendBuildingToVault : read → appendBuilding → write (pattern identique appendContributionToVault)
  </behavior>

  <action>
    1. **lib/village/parser.ts — parseGardenFile** : Étendre sans casser l'existant.
       - Importer UnlockedBuilding : `import type { VillageData, VillageContribution, VillageWeekRecord, UnlockedBuilding } from './types';`
       - Initialiser `const unlockedBuildings: UnlockedBuilding[] = [];` près des autres collections (ligne ~74)
       - Étendre le type de `section` : `let section: 'none' | 'contributions' | 'historique' | 'constructions' = 'none';`
       - Dans la boucle de headers (ligne ~80), ajouter :
         ```typescript
         } else if (header === '## constructions') {
           section = 'constructions';
         ```
       - Dans le traitement des lignes (après le bloc `section === 'historique'`), ajouter :
         ```typescript
         } else if (section === 'constructions') {
           // Format : timestamp | buildingId | palier
           const parts = raw.split(' | ');
           if (parts.length < 3) continue;
           const [timestamp, buildingId, rawPalier] = parts;
           if (!timestamp || !buildingId || !rawPalier) continue;
           const palier = parseInt(rawPalier.trim(), 10);
           if (isNaN(palier)) continue;
           unlockedBuildings.push({
             timestamp: timestamp.trim(),
             buildingId: buildingId.trim(),
             palier,
           });
         }
         ```
       - Dans le `return` (ligne ~152), ajouter `unlockedBuildings,` au bon endroit. ÉGALEMENT : Dans le early return du contenu vide (ligne ~39), ajouter `unlockedBuildings: [],`.

    2. **lib/village/parser.ts — serializeGardenFile** : Insérer section ## Constructions entre ## Contributions et ## Historique. Ajouter ce bloc APRÈS le `lines.push('');` qui suit la boucle contributions, AVANT `lines.push('## Historique');` :
       ```typescript
       // Section Constructions (Phase 30 — VILL-05)
       lines.push('## Constructions');
       for (const b of data.unlockedBuildings ?? []) {
         lines.push(`- ${b.timestamp} | ${b.buildingId} | ${b.palier}`);
       }
       lines.push('');
       ```

    3. **lib/village/parser.ts — appendBuilding** : Ajouter la nouvelle fonction après `appendContribution` (ligne ~273). Pattern EXACTEMENT miroir de appendContribution, mais la section cible est `## Constructions` et le fallback de création se fait avant `## Historique`.
       ```typescript
       // ---------------------------------------------------------------------------
       // appendBuilding
       // ---------------------------------------------------------------------------

       /**
        * Insere une ligne dans la section `## Constructions` du contenu jardin-familial.md.
        * Pattern identique a appendContribution — jamais append en fin de fichier (Pitfall 3/4).
        *
        * - Trouve la section `## Constructions`
        * - Insere AVANT la prochaine section `## `
        * - Si section absente, la creer avant `## Historique` ou en fin de fichier
        */
       export function appendBuilding(content: string, entry: UnlockedBuilding): string {
         const newLine = `- ${entry.timestamp} | ${entry.buildingId} | ${entry.palier}`;

         const lines = content.split('\n');

         // Trouver la section ## Constructions
         let constrStart = -1;
         let nextSectionIdx = -1;

         for (let i = 0; i < lines.length; i++) {
           if (lines[i].trim().toLowerCase() === '## constructions') {
             constrStart = i;
             continue;
           }
           if (constrStart !== -1 && lines[i].startsWith('## ')) {
             nextSectionIdx = i;
             break;
           }
         }

         if (constrStart === -1) {
           // La section n'existe pas — la creer avant ## Historique ou en fin de fichier
           const historiquelIdx = lines.findIndex(l => l.trim().toLowerCase() === '## historique');
           if (historiquelIdx !== -1) {
             const before = lines.slice(0, historiquelIdx);
             const after = lines.slice(historiquelIdx);
             while (before.length > 0 && before[before.length - 1].trim() === '') {
               before.pop();
             }
             return [...before, '', '## Constructions', newLine, '', ...after].join('\n');
           } else {
             const trimmed = content.trimEnd();
             return `${trimmed}\n\n## Constructions\n${newLine}\n`;
           }
         }

         // La section existe
         if (nextSectionIdx !== -1) {
           let insertAt = nextSectionIdx;
           while (insertAt > constrStart + 1 && lines[insertAt - 1].trim() === '') {
             insertAt--;
           }
           const before = lines.slice(0, insertAt);
           const after = lines.slice(insertAt);
           return [...before, newLine, ...after].join('\n');
         } else {
           const trimmed = content.trimEnd();
           return `${trimmed}\n${newLine}\n`;
         }
       }

       // ---------------------------------------------------------------------------
       // appendBuildingToVault
       // ---------------------------------------------------------------------------

       /**
        * Lit jardin-familial.md, ajoute le bâtiment, reecrit le fichier.
        * Pattern identique a appendContributionToVault. Seule fonction async du bloc batiment.
        */
       export async function appendBuildingToVault(
         vault: VaultManager,
         entry: UnlockedBuilding,
       ): Promise<void> {
         const content = await vault.readFile(VILLAGE_FILE).catch(() => '');
         const updated = appendBuilding(content, entry);
         await vault.writeFile(VILLAGE_FILE, updated);
       }
       ```

    4. **Tests** — Ajouter dans lib/__tests__/village-parser.test.ts un nouveau describe :
       ```typescript
       describe('parseGardenFile + serializeGardenFile — section Constructions (Phase 30)', () => {
         it('retourne unlockedBuildings: [] pour contenu vide (backward compat)', () => {
           const data = parseGardenFile('');
           expect(data.unlockedBuildings).toEqual([]);
         });

         it('retourne unlockedBuildings: [] si section absente (vault legacy Phase 25)', () => {
           const content = [
             '---', 'version: 1', 'created: 2026-04-01', 'current_week_start: 2026-04-07',
             'current_theme_index: 0', 'reward_claimed: false', '---', '',
             '## Contributions',
             '- 2026-04-10T14:32:00 | profile-abc | harvest | 1',
             '',
             '## Historique',
             '- 2026-03-31 | cible:45 | total:52 | claimed:true',
             '',
           ].join('\n');
           const data = parseGardenFile(content);
           expect(data.unlockedBuildings).toEqual([]);
         });

         it('parse les lignes ## Constructions format timestamp | id | palier', () => {
           const content = [
             '---', 'version: 1', 'created: 2026-04-01', 'current_week_start: 2026-04-07',
             'current_theme_index: 0', 'reward_claimed: false', '---', '',
             '## Contributions', '',
             '## Constructions',
             '- 2026-04-12T14:32:00 | puits | 100',
             '- 2026-04-15T09:15:00 | boulangerie | 300',
             '',
             '## Historique', '',
           ].join('\n');
           const data = parseGardenFile(content);
           expect(data.unlockedBuildings).toEqual([
             { timestamp: '2026-04-12T14:32:00', buildingId: 'puits', palier: 100 },
             { timestamp: '2026-04-15T09:15:00', buildingId: 'boulangerie', palier: 300 },
           ]);
         });

         it('ignore silencieusement les lignes malformées', () => {
           const content = [
             '## Constructions',
             '- malformed',
             '- 2026-04-12T14:32:00 | puits | 100',
             '- too | few',
             '- 2026-04-13T00:00:00 | cafe | notanumber',
           ].join('\n');
           const data = parseGardenFile(content);
           expect(data.unlockedBuildings).toHaveLength(1);
           expect(data.unlockedBuildings[0].buildingId).toBe('puits');
         });

         it('serializeGardenFile émet section ## Constructions même vide', () => {
           const data: VillageData = {
             version: 1, createdAt: '2026-04-01', currentWeekStart: '2026-04-07',
             currentThemeIndex: 0, rewardClaimed: false,
             contributions: [], pastWeeks: [], unlockedBuildings: [],
           };
           const out = serializeGardenFile(data);
           expect(out).toContain('## Constructions');
           // Ordre : Contributions avant Constructions avant Historique
           expect(out.indexOf('## Contributions')).toBeLessThan(out.indexOf('## Constructions'));
           expect(out.indexOf('## Constructions')).toBeLessThan(out.indexOf('## Historique'));
         });

         it('round-trip fidelity pour unlockedBuildings', () => {
           const data: VillageData = {
             version: 1, createdAt: '2026-04-01', currentWeekStart: '2026-04-07',
             currentThemeIndex: 0, rewardClaimed: false,
             contributions: [], pastWeeks: [],
             unlockedBuildings: [
               { timestamp: '2026-04-12T14:32:00', buildingId: 'puits', palier: 100 },
               { timestamp: '2026-04-15T09:15:00', buildingId: 'boulangerie', palier: 300 },
             ],
           };
           const serialized = serializeGardenFile(data);
           const reparsed = parseGardenFile(serialized);
           expect(reparsed.unlockedBuildings).toEqual(data.unlockedBuildings);
         });
       });

       describe('appendBuilding (Phase 30)', () => {
         it('crée la section ## Constructions avant ## Historique si absente', () => {
           const content = [
             '## Contributions', '',
             '## Historique',
             '- 2026-03-31 | cible:45 | total:52 | claimed:true',
             '',
           ].join('\n');
           const out = appendBuilding(content, {
             timestamp: '2026-04-12T14:32:00', buildingId: 'puits', palier: 100,
           });
           expect(out).toContain('## Constructions');
           expect(out).toContain('- 2026-04-12T14:32:00 | puits | 100');
           expect(out.indexOf('## Constructions')).toBeLessThan(out.indexOf('## Historique'));
         });

         it('insère AVANT la prochaine section ## (jamais fin de fichier — Pitfall 3/4)', () => {
           const content = [
             '## Constructions',
             '- 2026-04-12T14:32:00 | puits | 100',
             '',
             '## Historique',
             '- 2026-03-31 | cible:45 | total:52 | claimed:true',
           ].join('\n');
           const out = appendBuilding(content, {
             timestamp: '2026-04-15T09:15:00', buildingId: 'boulangerie', palier: 300,
           });
           // La nouvelle ligne doit être APRÈS puits mais AVANT ## Historique
           const idxNew = out.indexOf('boulangerie');
           const idxHist = out.indexOf('## Historique');
           expect(idxNew).toBeGreaterThan(-1);
           expect(idxNew).toBeLessThan(idxHist);
         });

         it('préserve les lignes existantes', () => {
           const content = [
             '## Constructions',
             '- 2026-04-12T14:32:00 | puits | 100',
             '## Historique',
           ].join('\n');
           const out = appendBuilding(content, {
             timestamp: '2026-04-15T00:00:00', buildingId: 'boulangerie', palier: 300,
           });
           expect(out).toContain('- 2026-04-12T14:32:00 | puits | 100');
           expect(out).toContain('- 2026-04-15T00:00:00 | boulangerie | 300');
         });
       });
       ```
  </action>

  <verify>
    <automated>npx tsc --noEmit && npx jest --no-coverage lib/__tests__/village-parser.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n "export function appendBuilding" lib/village/parser.ts` matches
    - `grep -n "export async function appendBuildingToVault" lib/village/parser.ts` matches
    - `grep -n "'## Constructions'" lib/village/parser.ts` matches (serialize emits section)
    - `grep -n "section === 'constructions'" lib/village/parser.ts` matches (parser handles section)
    - `grep -n "unlockedBuildings: \[\]" lib/village/parser.ts` matches (default empty on early return)
    - `grep -n "round-trip fidelity pour unlockedBuildings" lib/__tests__/village-parser.test.ts` matches
    - `grep -n "appendBuilding (Phase 30)" lib/__tests__/village-parser.test.ts` matches
    - `npx tsc --noEmit` exits 0
    - `npx jest --no-coverage lib/__tests__/village-parser.test.ts` exits 0 with ≥7 new tests passing (3 parse+serialize + 3 appendBuilding + 1 round-trip)
  </acceptance_criteria>

  <done>
    parser.ts étend proprement parseGardenFile/serializeGardenFile avec la section ## Constructions backward-compatible, appendBuilding et appendBuildingToVault suivent EXACTEMENT le pattern de appendContribution (jamais append en fin de fichier), round-trip fidelity garantie, tous les tests passent.
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` — aucune erreur TypeScript
- `npx jest --no-coverage lib/__tests__/village-parser.test.ts` — tous les tests passent (existants + nouveaux)
- `grep -n "toHaveLength(4)" lib/__tests__/village-parser.test.ts` — doit retourner VIDE (test stale corrigé)
- Les 8 fichiers sprites dans assets/buildings/village/ ne sont pas modifiés
</verification>

<success_criteria>
- VILL-05 partiellement couvert : schéma append-only complet, parser round-trip, appendBuilding idempotence testée
- VILL-06 partiellement couvert : catalogue statique 8 entrées avec paliers exacts, fonction pure computeBuildingsToUnlock testée en isolation
- Plan 02 (unlock engine) peut consommer : `appendBuildingToVault`, `computeBuildingsToUnlock`, `BUILDINGS_CATALOG`, `VillageData.unlockedBuildings`
- Plan 03 (UI) peut consommer : `BUILDINGS_CATALOG` (sprites+labels+paliers), `VILLAGE_GRID` étendu, `UnlockedBuilding` type
- Test stale village-parser.test.ts fixé au passage (dette Phase 25 éliminée)
</success_criteria>

<output>
After completion, create `.planning/phases/30-decorations-persistantes/30-01-SUMMARY.md`
</output>
